# SYSTEM MAP — Trading System v2

<!-- ─────────────────────────────────────────────────────────────────────────────
     HOW TO READ THIS FILE  (P4-6: it is ~450KB / 1,600+ lines and still growing —
     269KB at the 04-Jul audit. It is TWO documents wearing one hat.)

       1. STATUS BANNERS (top, before the index) — what changed most recently, newest
          first. Read these when you sit down; skip them when you are looking something up.
       2. REFERENCE (the sections below) — the durable operational map: paths, cron,
          services, runbooks. This is what you Ctrl-F for.
       3. BUILD LOG — the "V3 SHARED-ENGINE" run and the Changelog are a historical
          record of how things got here. Rarely what you want mid-incident.

     THE INDEX BELOW IS BY SECTION TITLE, NOT LINE NUMBER — line numbers rot on every
     edit; Ctrl-F the title instead.

     ⚠️ DO NOT SPLIT THIS FILE SILENTLY. Every audit report and memory entry points INTO
     it by section name. A split needs an agreed shape first (which sections move where,
     and what the pointers become) — that is P4-6, an open decision, not a chore.
     ───────────────────────────────────────────────────────────────────────────── -->

## INDEX

**Start here when you need the system, not its history:**
- `Pre-Work Checklist` — read before any VM/system work
- `⚠️ MANDATORY — Broker-Truth Verification BEFORE Every Manual Position Intervention` —
  **the 8-step runbook. NEVER flatten from DB state.**
- `VM Paths` · `PC Paths` — where everything actually lives
- `Cron Jobs` — 44 command-lines; **`config/cron_registry.yaml` is the source of truth**
- `Systemd Services` — units, restart policy, exit-code meanings
- `Related Docs` — the cross-reference map (do not duplicate those docs here)

**Live mechanisms / gates:**
- `Circuit-band placeability gate` (NOCIL fix) · `MIS Learned Blocklist` (NO_FILL Mech-B)
- `Control Tower` — VM operations monitoring
- `Delivery (Slice 2.5)` — ~~BUILT / DORMANT / awaiting T2 validation (triple-locked)~~ ~~UPDATED 05-Aug: `<DEPLOYED — ENTRY PATH VERIFIED LIVE 05-Aug; EXIT PATH UNVERIFIED>`~~ ⭐ **UPDATED 05-Aug-2026 23:0x: `<ROUND TRIP VERIFIED LIVE 05-Aug; CLEAN SHUTDOWN WITH POSITION HELD VERIFIED 05-Aug; T+1 CARRY UNVERIFIED>`. The three locks are OFF and delivery has traded.** ⛔ **Nothing wider — the carry is Thursday's.**

**Open work / debt:**
- `Known Duplicates / Issues` · `PENDING CLEANUP` · `Deferred items board`

**Historical build log (skip unless you are tracing how something got here):**
- `V3 SHARED-ENGINE` ×11 — Phase-0/Step-1 substrate → 03.01 S&R · 03.02 Regime ·
  03.03/03.04 Hard-Gate+Scorer · 03.06 Risk/Sizing · 03.05 Allocator · 03.07 Entry ·
  03.08 Trade-Mgmt · 03.09 Exit · Step-10 Pipeline Plumbing · Step-10b PB-01 Watchlist
- `GUI Project (Frontend)` — Phase G0
- `Changelog`

> 🟢🖥️🔝 **02-Sep-2026 23:39 IST — THE 22-SCREEN GUI CAMPAIGN IS MERGED AND PUSHED.** `origin/main` `39292d3` → **`686df1c`** (a MERGE, ⛔ not a rebase; parents `39292d3` + `0bbe127`; 121 GUI commits, 0 code collision). 🔬 **origin/main == VM bare == VM deployed work-tree**, all three measured independently. 🔬 **Zero new failures on BOTH axes** — trading gate **10F/6007P/5S** at A *and* C, GUI suite **1F/2171P** at B *and* C, and the failure **SETS are identical**, ⛔ not merely the counts. 🔬 Outside `ops_dashboard/` and `docs/` the diff is only `PATHS.md` + `UNPUSHED_LEDGER.md`; across `core/ capital/ orders/ signals/ screening/ strategies/ alerts/ broker/ config/ deploy/ tests/ main.py` it is **EMPTY** ⇒ ⭐ **the engine executes byte-identical code at tomorrow's 08:15 boot.** ⏸ 👤 **RAMA OWES `sudo systemctl restart gui-dashboard`** — 🔬 the post-receive hook contains **zero** `systemctl`/`restart`/`reload` references (measured on the LIVE VM hook, ⛔ not the repo copy) and the unit has been up since **06:19:53** with no file-watching ⇒ ⛔ **it is still serving the OLD templates.** 🔴 ⛔ **NO screen is `VERIFIED LIVE`** — every approval so far was a LOCAL render. ⛔ **F2-CORE `587b306` stays UNPUSHED** (`feat/f2-core-30aug`, on ⛔ no remote). 📄 `UNPUSHED_LEDGER.md` — *WINDOW CLOSED — 02-Sep-2026*

> 🖥️🎛️🔝 **01-Sep-2026 — S17 CONTROLS `<APPROVED>`; GUI TRACK NOW **19 GREEN / 2 QUALIFIED / 1 PENDING (S16 only)**. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED.** 🔴🔴 **THE CONTROL PLANE WAS DESIGNED 17-Aug AND NEVER BUILT** — 🔬 nothing on `:8600`, ⛔ no `CONTROL_SECRET`, ⛔ **the trading process serves NO HTTP AT ALL**; `control_client.py` is a client for a server that does not exist and is ⛔ not even deployed. ⇒ ⭐ **S17 is truthfully 100% VIEW, ⛔ not 95/5**, every control disabled WITH its reason. ⛔ **NO write path was created.** 👤 Rama REJECTED the first render — *"no overlap is not visual acceptance"* ⇒ 🔬 **2366px (2.19× viewport, 9 scattered rows) → 1540px, 4 clean bands, 0 overlaps.** 📄 `UNPUSHED_LEDGER.md` Entry 32

> 🖥️📦🔝 **01-Sep-2026 — S22 HOLDINGS `<APPROVED>`; GUI TRACK NOW **18 GREEN / 2 QUALIFIED / 2 PENDING**. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED.** 👤 Rama approved the render. 🔬 **2 defects fixed, ⛔ neither the KPI:** a filter now returns to page 1, and a **stale stamp shows its date** (`18-Aug 15:45:02`), which is ⭐ *why* `System Holdings 0` looked wrong beside `System Qty 1` — 🔬 two TIME BASES (open-now vs the 18-Aug record), ⛔ not an error; ⛔ **no number was changed to make panels agree**. ⚠️ Symbol stays **78px** ⇒ 🔬 `UTTAMSUGAR` truncates at a TRUE 1920 — 👤 a SEPARATE visual decision. ⛔ Deferred global table rule NOT applied (🔬 `style.css` **zero** diff). 📄 `UNPUSHED_LEDGER.md` Entry 31

> 🖥️🔎🔝 **01-Sep-2026 — S21 SCANNER ATTRIBUTION `<APPROVED>`; GUI TRACK NOW **17 GREEN / 2 QUALIFIED / 3 PENDING**. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED.** 👤 Rama approved ~16:0x IST; build `921969c`. ⚖️⚖️ **A DATED RULING OF RAMA'S WAS REVERSED, and that is the entry's point.** 📄 The template AND service both carried *“⛔⛔ NO SCANNER COLUMN IN THE MAIN TABLE (Rama, 16-Aug) — Scanner and Strategy are 1:1, 🔬 measured: 16 scanners onto 16 distinct strategies”*, with the identity surviving in SCANNER MAPPING. 👤 **The 01-Sep contract reverses it and answers that reasoning head-on:** *“Keep the word Scanner wherever it is meaningful in this screen; do not rename or remove the Scanner concept merely because it maps 1:1 to Strategy.”* ⚠⚠ **THE 1:1 MEASUREMENT STILL HOLDS — it was never wrong, only its CONCLUSION was overturned** ⇒ the cell reads the **SAME row's own `scanners` list**: 🔬 **16/16 rows `scanners == [strategy]`, zero divergence** — ⭐ one identity shown twice by request, ⛔ NOT a second dataset. ⭐ And what the old rule was REALLY protecting — that Scanner must never become a second independent dataset — is now guarded by **its own test** rather than by the column's absence; ⭐ that is the durable part, the column was only the means. 🔧 **THE TWO COLUMNS:** ⭐ **`#` is a SERIAL, ⛔ NOT the payload's `rank`** — the index of the CURRENT sort, so re-sorting renumbers 1..n (🔬 confirmed: sorting by Signals kept serials **1,2,3,4** while the SCANNERS beneath them changed); ⛔ `nosort`; ⭐ still draggable; ⭐ medals follow the top-3 POSITIONS; ⛔ `rank` untouched and still driving SCANNER RANKING · ⭐ **Trade Type needed NO new calculation** — already in the payload as the strategy's own YAML `intent` via the shared `strategy_meta` path S19/S20 use, ⛔ never inferred from the scanner NAME (the contract forbids it in as many words). ⚠⚠ **`COLS_KEY`→v2, ⛔ NOT cosmetic:** a stored v1 order lists the OLD thirteen keys and `initCols` **appends what is missing**, so a returning operator would have got Scanner and Trade Type at the **FAR RIGHT and no `#` at all** — ⭐ Screens 14 and 20 both paid for this, and **this is the third occurrence and the FIRST caught BEFORE shipping**. ⚠️ **ONE BACKEND FILE, and only because the change forced it:** `EXPORT_HEADER` is bound to the table labels **by an existing test**, so the export had to follow — 🔬 verified against a **real downloaded workbook** (header matches, three sheets survive). ⚠️ **THREE STALE RECORDS CORRECTED** — the footer note still said *“Strategy is the identity shown”*, the service docstring's HEADING still read *“NO SCANNER COLUMN”* above a body describing its own supersession, and the payload still published a `scanner_column` **GAP for a column that now exists**; ⭐ a stale explanation is worse than none — it teaches the wrong model. ⛔ **THE DEFERRED GLOBAL TABLE RULE WAS NOT APPLIED and its absence was MEASURED, ⛔ not merely skipped:** 🔬 `thead` `position: static`, wrap `max-height: none`, ⛔ no vertical scroll, all 16 rows in view; ⛔ no global selector touched, ⛔ no compensating visual change. ⭐ **DATA IS THE 15:54 VM RE-PULL, ⛔ not the 11:41 extract** — 👤 stale logs are forbidden where newer exist, and 🔬 the gap was large: **787 → 4,083 signals**, 2 → 3 closed trades. 🔬 **S21 142 passed**; ⭐ two superseded tests **rewritten, ⛔ not deleted** (the no-Scanner test became the **not-a-second-dataset** guard); full suite **2117 passed, 1 failed** = the `kiteconnect` environment artifact ⇒ ⛔ zero regressions. 🔴 ⛔ **NOT `VERIFIED LIVE`** — one real trading day. 📄 `UNPUSHED_LEDGER.md` **Entry 30**.

> 🖥️🩺🔝 **01-Sep-2026 — S20 STRATEGY HEALTH `<APPROVED>`; GUI TRACK NOW **16 GREEN / 2 QUALIFIED / 4 PENDING**. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED.** 👤 Rama approved ~14:2x IST; build `a2fe2e4`. ⭐ Two changes asked for — a serial column and S19's table interaction — and the investigation ran FIRST: ⭐ **both proved UI-ONLY**, 🔬 `git diff --name-only` returning **nothing** under `backend/`/`services/`/`readers/`/`api/` with every added selector scoped to `.sh-page` ⇒ the verdict is **measured**, ⛔ not asserted. ⭐ **WHAT ALREADY EXISTED** (so ⛔ nothing was rebuilt): the health score is the **real weighted composite** at exactly **Activity 30 / Signal Quality 25 / Acceptance Rate 20 / Trade Activity 15 / Errors 10** (`strategy_health.py:57-61`), ⛔ not a status lookup · **silent detection reads `gui_config.silence.yellow_max_min` = 120 min = 2 Hours, READ-TIME ONLY with ⛔ NOTHING WRITTEN** (:186-203) ⇒ ⭐ the read-only dashboard boundary holds · **“Scanner Offline” keeps the artwork's EXACT label** under the recorded 16-Aug ruling (:64, :550), **derived** ⛔ not invented · export `/api/export/strategy-health` · column drag already worked. 🔧 **(1) THE SERIAL COLUMN IS NOT DATA — that is the whole point.** It renders the **index of the CURRENTLY SORTED, FILTERED set** via `x-for="(r, i)"` ⇒ ⛔ never read from the row, ⛔ never stored, so re-sorting/filtering **renumbers 1..n on the spot**. 🔬 **VERIFIED:** sorting by health score kept serials **1,2,3,4** while the STRATEGIES beneath them changed; Disabled⇒`1`, Silent⇒`1..15`, unfiltered⇒`1..16`. ⛔ It carries `nosort` (sorting BY a row number would sort by the order the sort itself produces) ⭐ but stays **draggable**. ⚠⚠ **THE `COLS_KEY` BUMP TO v3 IS NOT COSMETIC** — a stored **v2** order lists the OLD eleven keys and `initCols` **appends anything missing**, so a returning operator would have found **`#` at the FAR RIGHT** instead of first: ⭐ precisely the **Screen-14 lesson the template's own v2 note already records**, the same trap one screen later. 🔧 **(2) THE FREEZE-PANE**, reusing S11/S18/S19 rather than a **fourth** scrolling implementation. ⭐ **FOURTEEN rows, ⛔ NOT S19's twelve** — this artwork draws 14 and says *“Showing 1 to 14 of 14”*. 🔬 **546px MEASURED AT SUB-PIXEL**: thead **32.00** + 14×**36.67** ⇒ row 14's bottom at **545.33** — ⭐ **S19's rounding lesson APPLIED rather than repeated** (there it showed 11 rows instead of 12). ⛔ `min-height: 350px` **KEPT** (the empty-day footprint, a different job). ⛔ **S20 ONLY** — 👤 the global table rule stays **deferred** until every screen is built. 🔬 **BROWSER-VERIFIED, ⛔ not from source:** 14 rows visible · wrap **546 client vs 619 scroll** · **the header's top does not move** across a full scroll, reaching **row 16** · drag works with the sticky header · the serial header **refuses to sort** · page **1485→1412**. ⚠️ **A WIDTH I DID NOT CLAIM:** the 1440px check ⛔ could not be done as a resize (the browser sits at **75% page zoom**, so the CSS viewport never changed) ⇒ ⭐ rather than report a width not really rendered, the **MECHANISM** was tested — squeezing the panel to **1400/1100/900px**, the table clamps to its **1258px** min-width and the **WRAP** scrolls horizontally while the **PAGE never overflows**, header sticky throughout. ⚠️ **TWO EXISTING TESTS REPAIRED, both left SHARPER:** `APPROVED_COLUMNS` now leads with `#`, and 🔴 the heading-fits-on-one-line test matched `.sh-page .sh-tbl th` **BY PREFIX**, silently grabbing the new `thead th` rule and asserting `nowrap` against it — ⭐ the match is now **exact**, ⛔ the property unchanged. 🔴 ⛔ **NOT `VERIFIED LIVE`** — approved on a genuinely **QUIET** real day (🔬 15 Silent / 1 Disabled, 787 signals, 6 trades) and ⭐ that is exactly what the screen shows; ⛔ no demo rows, ⛔ nothing fabricated. ⚠️ Still **NOT INSTRUMENTED**: **HEALTH TIMELINE** and **RECENT HEALTH EVENTS** — ⛔ the artwork's *“(Example)”* timeline was **NOT** turned into production history. ⛔ **Scanner not duplicated** — 🔬 exactly ONE occurrence, the approved **“Scanner Offline”** label. 🔬 **S20 102 passed** (100 + 2 guards), ⭐ **RED-CAPABILITY PROVEN BY THREE MUTATIONS**; full suite **2115 passed, 1 failed** = the `kiteconnect` artifact, ⭐ the count rising **2113→2115** by exactly the two guards ⇒ ⛔ zero regressions. 📄 `UNPUSHED_LEDGER.md` **Entry 29**.

> 🖥️🏆🔝 **01-Sep-2026 — S19 STRATEGY RANKING `<APPROVED>`; GUI TRACK NOW **15 GREEN / 2 QUALIFIED / 5 PENDING**. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED.** 👤 Rama approved ~13:4x IST; build `f1e97a9`. ⭐⭐ **THE BRIEF INVOKED THE PRE-BUILD REVIEW GATE** (S19 is business/signal-path adjacent) ⇒ the investigation ran FIRST and ⛔ no code was touched until it cleared — ⭐ **and it cleared as UI-ONLY**, which is the entry's point. 🔬 **`git diff --name-only` returned NOTHING under `backend/`, `services/`, `readers/`, `api/`** ⇒ the verdict is **measured**, ⛔ not asserted. ⭐ **WHAT ALREADY EXISTED:** the ranking engine is complete and ⛔ is NOT a parallel engine — `services/strategy_ranking.py` owns the composite score (`_score` :93) in the artwork's own **35/25/20/20** weights (:56), the trend (`_classify_trend` :142) against a **real previous equal-length window**, the six KPIs (:324) and five modes (:48), and 🔬 it reads `db_reader.closed_trades_range` :172 aggregating through `analytics_period._aggregate` :174 ⇒ ⭐ it **REUSES** the existing engine; ⛔ no duplicate, ⛔ no shadow calculation. Column drag already worked (`colDragMixin()` :313, `draggable` :136). ⚖️ **A DATED RULING FOUND AND RESPECTED:** the 16th header **TRADE TYPE** is a **recorded prior instruction** (strategy YAML `intent` via `strategy_meta.py`, ⛔ explicitly NOT the order product) ⇒ ⛔ left alone, ⛔ not reported as a deviation. 🔴 **THE ONLY GAP — THE TABLE CONTRACT:** 🔬 `.sr-tbl-wrap` carried `overflow-x` **alone** (`style.css:4486`) with **no height bound**, so it grew to fit every row and the **WHOLE PAGE** scrolled (**1353px vs a 1264px viewport**) to reach rows 13-16 of 16, and the header computed `position: static` ⇒ ⛔ nothing was frozen. ⭐ Now a **freeze-pane over a scrolling 12-row body**, ⭐ **reusing S11's `.exec-rank-scroll` (:2850) and S18's `.lav-feed` (:4081)** rather than writing a second scrolling implementation. 🔬🔬 **491px IS MEASURED AT SUB-PIXEL AND THE ROUNDING MATTERED:** thead **30.92** + 12×**38.33** ⇒ row 12's bottom at **490.92**; ⚠️ rounding to 31 and 38 gives **487**, which shows only **ELEVEN** rows — ⭐ on this table a 4px arithmetic error costs a whole row, so the figure comes from the rendered box, ⛔ never from integer arithmetic. ⛔ `max-height`, ⛔ never `height` (a quiet day must not open an empty region); ⛔ no pagination substituted, ⛔ no row truncated. ⭐ **VERIFIED IN THE BROWSER, ⛔ not from source:** 🔬 12 rows fully visible · wrap **491 client vs 645 scroll** · **the header's top does not move** across a full scroll, which reveals **ranks 13-16** · **column drag still works with the sticky header** (real HTML5 handlers driven: TRADES 3→5, sort fired, order restored) · page **1353 → 1199** · ⛔ no page-wide horizontal overflow. 🔴 ⛔ **NOT `VERIFIED LIVE`** — ⚠️ the default *Today* window holds only **2** completed trades and reads as dashes, so the approval render used **the screen's OWN Date Range filter**: *This Month* = **117 REAL trades across 13 of 16 strategies** (2026-08-03→09-01); ⛔ no demo rows, ⛔ no config pointer, ⛔ nothing fabricated, ⛔ default period unchanged in code. 🔬 **S19 75 passed** (74 + the one guard), ⭐ its **RED-CAPABILITY PROVEN BY MUTATION** (`max-height`→`height` red; dropping `position: sticky` red; both reverted); full suite **2113 passed, 1 failed** = the `kiteconnect` environment artifact, ⭐ the passed count rising **2112→2113** by exactly the guard added ⇒ ⛔ zero regressions. ⛔ Scanner absent (0 in text/classes/attributes). ⛔ S16/S17 untouched — 👤 held to be built LAST. ⛔ Paper/live parity untouched. 📄 `UNPUSHED_LEDGER.md` **Entry 28**.

> 🖥️📡🔝 **01-Sep-2026 — S18 LIVE ACTIVITY `<APPROVED>`; GUI TRACK NOW **14 GREEN / 2 QUALIFIED / 6 PENDING**. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED.** 👤 Rama approved ~13:0x IST; builds `f9d5fe3` + `f860f82`. 👤 **S16 and S17 are HELD by Rama's 01-Sep decision — they are built LAST**, so the queue is S19–S22 then S16–S17. 🔧 **PASS 1 — THE ARTWORK'S THREE BANDS:** the work area was two independent strips of four panels; the PNG draws band A as two strips (feed ǀ pipeline·strategy) and then bands B and C running **ACROSS BOTH** (x205→1240) ⇒ **FEED FILTERS** moved beside Winners/Losers and System Events, **CAPITAL UTILIZATION** beside ACTIVE POSITIONS, both spanning `1 / -1` at the artwork's own ratios. ⚠⚠ **AN EARLIER SESSION HAD BUILT FULL-WIDTH ROWS AND RECORDED THEM REJECTED** — a grid row is as tall as its tallest cell, so a short feed left a band beneath it — and ⭐ **that mechanism is ADDRESSED, ⛔ not ignored**: only `.lav-main` stretches and the feed's own scroll WINDOW takes the slack, so band A closes on **REAL ROWS becoming visible** (430→560px against 540 stored events); 🔬 feed 694, side 694, **difference 0**. 🔧 **PASS 2 — 🔴 THE RESIDUAL BAND, AND MY OWN WRONG EXPLANATION OVERTURNED.** A 150px band remained under WINNERS, and ⛔ I had called it unavoidable (*"stretching would only relocate the emptiness"*). ⭐⭐ **MEASURING THE ARTWORK PROVED THAT WRONG:** 🔬 a pixel scan of three blank columns, one inside each band-B panel (**x612 / x918 / x1235**), returns the **SAME** border rows for all three — top **y=586**, bottom **y=785** ⇒ the artwork's panels are **EXACTLY equal height (199px)**; band C the same, 797→969. ⇒ **the artwork STRETCHES its bands.** ⭐ The scale also proved the tall panel was never the fault: PNG left band x202-1247 = **1045px** vs our **1961px** = **×1.877**, so the artwork's 199px band scales to **373.4** and SYSTEM EVENTS measures **375** here. ⚠️ The `align-items: start` replaced came from a **BAND-A** lesson **over-generalised** to the bands. 🔧 Three changes, every figure read off the artwork: `align-items: stretch` on the bands · 🔬 WINNERS card footprint **174→218px** (artwork card y634-750 = 116 × 1.877; ⚠️ the first pass used 174 from an **inferred** 1.66 scale) · 🔬 FEED FILTERS tile footprint **149→240px** (artwork rows y629-686 + y701-757 = 128 × 1.877) — ⭐ our tiles were genuinely **undersized**. ⭐⭐ **IT FIXED THE GEOMETRY RATHER THAN HIDING IT**, the test 👤 Rama set: 🔬 the space BELOW the winners cards is **157** vs the artwork's **155.8**, and the card:panel ratio is **58% in both**; band B 375 vs 373.4, card 218 vs 217.7, tiles 240 vs 240.2, slack 135 vs 133.2 — all within ~2px. 🔬 **DEAD BAND 206px → 150px → 0px**; band B tops all 1006 / bottoms all 1381, band C 1397→1690, ⛔ no overflow, ⛔ no clipping. ⚠️ **NOT CLOSED and REPORTED, ⛔ not padded:** band C measures **293** vs the artwork's **322.8** — both panels equal and full so ⛔ no gap, and forcing it would need arbitrary padding. ⭐ **REAL DATA:** `signals`/`orders`/`trades`/`fm_ledger` pulled **READ-ONLY** from the live VM DB at **11:41:02** — 🔬 787 signals, 533 feed events, 88 ledger rows; ⛔ the 428 MB DB was never copied. 🔴 ⛔ **NOT `VERIFIED LIVE`** — 👤 at Rama's instruction ALERTS BANNER (1 critical + 2 warning) and ACTIVE POSITIONS (4 rows) carry **DEMO** data held **ENTIRELY out-of-repo**; ⚠️ 🔬 I caught one incoherence in my OWN demo first — it drove capital to **90.33%** because the figures resolve against the **REAL** ₹10,469.40 opening INIT row, ⛔ not the ₹18,469 I assumed; resized to the artwork's **42.17% / 57.83%**. 🔬 **S18 116 passed**; full suite **2112 passed, 1 failed** = the `kiteconnect` environment artifact. ⭐ **FOUR guards updated and still guards** — notably the artificial-height guard now reads a rule's **SUBJECT** rather than any ancestor, and asserts the bands **DO** stretch while band A is ⛔ still not a full-width row. ⛔ Scanner absent (0 in text, classes, attributes). ⚠️ 🏷️ **OPEN for Rama:** the feed renders **all 540 rows** client-side (`feed_page` is returned but unused), which defeated screenshot capture — the S14 strain again. 📄 `UNPUSHED_LEDGER.md` **Entry 27**.

> 🖥️🔍🔝 **01-Sep-2026 — S15 SYSTEM LOGS `<APPROVED>`; GUI TRACK NOW **13 GREEN / 2 QUALIFIED / 7 PENDING**. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED.** 👤 Rama approved ~11:2x IST; build `7ec180a`. ⭐ S15 was already built and already had the wide-page fix (`0ca38e2`); 👤 Rama returned it with three corrections, and ⭐⭐ **THE FIRST WAS NOT A MISSING FEATURE AT ALL BUT A LIVE DEFECT NO TEST COULD SEE.** 🔧 **(1)** 🔴 **EVENT TYPE and STATUS rendered as BLANK CELLS** — the `NOT INSTRUMENTED` markup was ALREADY THERE, but each `<template x-if>` held **TWO sibling spans**, and 🔬 **Alpine 3.14.1 builds an x-if branch with `content.cloneNode(true).firstElementChild`** ⇒ it renders **ONLY the first root and DISCARDS the rest**, so the gap span was **never created** and the survivor was `x-show`-hidden **exactly when the value was missing**. ⭐ The one thing this screen must never do, done **SILENTLY**: ⛔ no error, ⛔ no console warning, ⛔ no failing test. 🔬 **A/B on TWO live instances (`:8501` pristine vs `:8500` corrected): 20 of 20 cells BLANK → 0 of 20.** ⭐ **SWEPT THE CLASS, ⛔ not the instance** — all **33 templates** scanned; 🔬 **exactly two multi-root `x-if` blocks exist and both are these** ⇒ ⛔ no other screen affected, and a test now pins the rule. ⭐ Real values untouched: a Scheduler row still shows real `Status=Success`, `RECOV-7903` a real `Event Type=Recovery Completed`. 🔧 **(2)** **COMPONENT TIMELINE** rebuilt as the PNG's **HORIZONTAL four-node pictorial diagram** — a vertical `<ol>` was precisely what the TXT forbids (*"do not replace diagrams/pictorial elements with plain text"*); the TXT's arrow list is its ASCII rendering of the SAME four stages and **SOURCE AUTHORITY 1 (the PNG) is binding for composition**. ⛔ NO new backend, ⛔ no new data — same `detail.timeline`, same `measured` flag; the rail is a `::before` drawn from the previous node's centre so it is positioned **BY** the nodes and ⛔ cannot drift. ⛔ **The PNG's "Duration: 2 sec" has NO source and says NOT INSTRUMENTED** — 🔬 the measured stages share ONE timestamp, so subtracting them would have manufactured a `0 sec` dressed as a measurement. ⚠️ Two layout defects found by MEASURING: 🔬 `.slg-ni`'s `nowrap` made four labels overrun a **102px** column (label needs **123px**) and collide ⇒ nowrap lifted only inside the timeline (wrapped: **94px**); then row 3 moved to **the artwork's own ~1 : 1.34 : 1 : 1** ⇒ stage column **123px**. ⛔ No word shortened to a dash, ⛔ nothing below the 13px floor. 🔧 **(3)** **SEARCH + EVENT TYPES INTEGRATED into FILTERS, ⛔ not deleted** — 🔬 the **PNG places NEITHER as a panel** (its search is one toolbar box; Event Type is a dropdown) and the **TXT** asks to preserve the search ***capability***, ⛔ not a full-width block. All five approved fields move across unchanged, **Error Code still SHOWN and DISABLED** (no error-code scheme exists); the eleven counts compact to a chip strip keeping the distinction that matters — 🔬 `Service Restarted 0` is **instrumented and genuinely zero** while `Connection Lost NOT INSTRUMENTED` is **unmeasurable**, ⛔ and a 0 is never shown for the latter. ⛔ `.slg-rowx` **REMOVED, ⛔ not emptied**. 🔬 Page **2158px → 1941px (−217px)**; removed row **438px**, FILTERS **125px → 346px**. 🔧 **(4)** ⭐ The width fix of `0ca38e2` is **preserved exactly** — ⛔ no global CSS, every rule scoped to `.slg-page`. ⚠️ **(5) A REGRESSION FOUND ON ARRIVAL:** the S15 suite was **ALREADY RED at 8F / 72P** — 🔬 `0ca38e2` changed the root to `class="dash-page slg-page"` and the test helper hard-coded `'<div class="slg-page"'`, matching nothing, returning **-1** and taking **EIGHT tests** down, each reporting *"the page root is missing"* instead of the real change; the matcher is now **class-aware**. ⭐ **EVIDENCE:** the brief's HARD RULE honoured — today's dated `system_`/`reconciler_`/`trades_` logs (**28-Aug…01-Sep**) + `system_events`/`cron_heartbeat`/`reconciliation_log` pulled **READ-ONLY** from the live **428 MB** VM DB (⛔ the DB itself never copied, ⛔ nothing written on the VM outside `/tmp`). 🔬 **11,656 events · Info 11,565 (99.22%) · Warning 83 · Error 8 · Critical 0 · last event 10:49:24 TODAY** — ⭐ severity is genuinely Info-dominated and ⛔ NOT reshaped toward the PNG's 55%. ⛔ No Scanner anywhere. 🔬 **S15 82 passed, 0 failed** (from 8F/72P) with **+2 guards**; full suite **2112 passed, 1 failed** = `test_c_venv_has_no_kiteconnect`, ⭐ **proven environmental against a PRISTINE checkout of HEAD where it fails identically**. ⭐ RED-capability proven **by injecting a blank cell** (the probe caught it) and **by emitting a probe warning** (the console check captured it) ⇒ ⛔ neither zero was vacuous. 🔴 ⛔ **NOT `VERIFIED LIVE`** — a READ-ONLY VM EXTRACT via the git-ignored `gui_config.local.yaml`; ⛔ no repo edit, ⛔ no demo DB, ⛔ nothing to revert. 📄 `UNPUSHED_LEDGER.md` **Entry 26**.

> 🖥️🔍🔝 **30-Aug-2026 — S14 TRADE LOGS `<APPROVED>`; GUI TRACK NOW **12 GREEN / 2 QUALIFIED / 8 PENDING**. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED.** 👤 Rama approved ~22:4x IST; build `3885208`. ⭐ S14 was **ALREADY FULLY BUILT** — 12 panels, all bindings, 83 tests — and ⭐ **Scanner was ALREADY REMOVED and DEFENDED AT THREE LAYERS** (rendered page + JSON payload · the export header · **the SQL itself** — `signals.scanner` exists in the schema and is ⛔ deliberately UNREAD). ⭐ The separate **Scanner Attribution** nav link is correctly untouched. ⇒ a visual pass, ⛔ not a rebuild. 🔧 **(1)** ⭐ **THE TRADE REPLAY PANEL**, built into a 🔬 **MEASURED blank region**: `.tlg-rail` ran **543px past** `.tlg-row3`, leaving **1222×559 px** dead INSIDE `.tlg-left`. ⚠️ ⭐ That measurement drove the PLACEMENT: a full-page-width band below both columns would have left the hole exactly where it was and ⛔ only made the page taller. 🔬 Dead band **559px → 16px**; panel 1222×345. ⛔ **NO NEW BACKEND** — the 8 stages ARE `detail.timeline`, the same ones TRADE TIMELINE renders. ⭐⭐ **THE ANIMATION STEPS ONLY THROUGH `measured` STAGES** — that ONE rule makes a fabricated transition **structurally impossible**, ⛔ not merely discouraged, and delivers all three required behaviours with ⛔ no special-casing. 🔬 VERIFIED LIVE ON TWO TRADES: the completed one stepped Signal→Validation→Order→Fill→Position→Exit **SKIPPING Risk and Capital** (no timestamp); the zero-fill `TRD-2026-000807` marked **Order 10:58:34 TERMINAL**, Fill/Position **not reached**, Exit **pending / position still open**. ⭐ Four states drawn distinctly because they MEAN different things: recorded · NOT INSTRUMENTED (with the system's own reason) · not reached · replaying. 🔧 **(2)** Lower row **rebalanced** — AUTO-RECOVERY and RECENT ERRORS were both clipped while EXPORT held `.8fr` it does not use. ⚠️ ⭐ RECORDED HONESTLY: **the first attempt was WORSE than the defect** — wrapping the cells cleared the clip but turned every RECENT ERRORS row into THREE lines; 🔬 measured, reverted, width-change only. 🔧 **(3)** ⭐ An S13 test of mine fixed **at root**: its *touched-no-other-screen* scan ran to **EOF**, so the next screen's correctly-scoped block failed it (S14's `.tlg-page .tlg-row4` did exactly that) — ⭐ the WINDOW was wrong, ⛔ not the assertion. ⭐ **THE EVIDENCE SOURCE:** the brief names *VM > review*; 🔬 inspected `reports/log_review/eod_review_2026-08-28.md` and reproduced it — the payload's own counts land on the file EXACTLY: **Signal Received 4,686 · Signal Accepted 16 · Capital Rejected 58 · Order Created 16 · Position Closed 5 (TGT 1 / SL 3)**, real symbols, real rejection reasons, both slippage-guard figures, both kill-switch instants. ⭐ The REAL 28-Aug date is kept (it falls inside S14's own default 7-day window). ⚠️ **S14 SURFACES 7 GAPS and the demo ⛔ does NOT paper over them:** 🔴 **NO error-code scheme exists** (a repo-wide `XXX-0000` search returns ZERO) ⇒ the artwork's `EXCH-1016`/`RISK-2001`/`CAP-3002` ⛔ CANNOT be built · 🔴 **a PASSING risk or capital gate writes NO ROW** ⇒ *Risk Passed*/*Capital Passed* show **NOT INSTRUMENTED, ⛔ never 0** — a 0 would wrongly read as *nothing ever passed* · Risk and Capital have **no timestamp of their own** ⇒ the timeline reads *n of 8 stages recorded* · ⛔ no resolution state · ⛔ no *Recovered In* ⇒ the artwork's *18 sec* cannot be built · ⛔ no retention policy on signals/trades/orders ⇒ the artwork's *67 Days* is NOT INSTRUMENTED · ⛔ no human attribution anywhere. 🔬 **89 S14 tests pass (+6** pinning the replay's honesty rules**)**, RED-capability proven by mutation; full suite **2104 passed, 1 failed** = the `test_c_venv_has_no_kiteconnect` environment artifact. 🔴 ⛔ **NOT `VERIFIED LIVE` — REPRODUCED data via the out-of-repo harness on :8501**; ⛔ no repo edit, ⛔ no demo DB, ⛔ no pointer. 📄 `UNPUSHED_LEDGER.md` **Entry 25**.

> 🖥️🧾🔝 **30-Aug-2026 — S13 AUDIT `<APPROVED>`; GUI TRACK NOW **11 GREEN / 2 QUALIFIED / 9 PENDING**. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED.** 👤 Rama approved ~20:1x IST; build `3fa2e78`. ⭐ S13 was **ALREADY FULLY BUILT** — every panel the PNG draws existed and was bound to real audit readers ⇒ this was a **visual pass, ⛔ not a rebuild**. 🔧 **(1)** **CRITICAL CHANGES moved out of the table-row rail** to sit beside the lower analytics — the relationship the design TXT names verbatim. ⚠️ ⭐ It is ALSO a measured defect fix: with a record selected the rail stood 🔬 **1474px against a 528px table = 946px of DEAD SPACE** (page 2457px) ⇒ now 🔬 **448px** (page 2074px). 🔴 **The void appeared ONLY AFTER a row was clicked** — ⛔ which is why an unselected screenshot never showed it. ⭐ The new `.aud-lower` band deliberately MIRRORS `.aud-main` (`minmax(0,1fr) 330px`) so the panel lands under AUDIT DETAILS; ⭐ a test pins BOTH rules to the same columns, because if they drift the panel stops lining up and the move is pointless. 🔧 **(2)** **Bottom export bar** → note-left/buttons-right with the shared `.btn-export`, as S09/S11/S12. ⚠️ The rule was **ALREADY `space-between`** — `flex-wrap: wrap` plus the long honesty note wrapped the buttons onto their own line at the LEFT; ⭐ `nowrap` is the load-bearing part. 🔧 **(3)** **One width for every filter select** — Action rendered ~2× its siblings purely because its option strings are longer; ⛔ a control's width must not encode its data. ⏸ **REPORTED, ⛔ NOT CHANGED:** the artwork puts OLD/NEW in a right sub-COLUMN of Audit Details (🔬 its rail ~412 CSS px vs our 330); the design TXT specifies the STACK itself — *OLD VALUE ↓ NEW VALUE* — which is exactly what renders, and widening the rail would squeeze a 9-column table ⇒ 👤 Rama's ruling owed. ⚠️ **S13 SURFACES SIX REAL INSTRUMENTATION GAPS, and the demo does ⛔ NOT paper over them:** 🔴 **WHO changed a setting is NOT CAPTURED ANYWHERE** — no human attribution exists in the schema, so the PNG's `ramakrishnan` User column ⛔ CANNOT be built; the column shows the recorded PROCESS or an explicit gap, and Top Users states *all recorded actors are PROCESSES, not people* and **excludes** the unattributed rather than assigning them to anyone · **TIMELINE: only ONE instant per record exists** ⇒ Applied is real, Requested/Validated/Confirmed are NOT INSTRUMENTED · **SOURCE:** only an inbound webhook proves its own origin (API) · **RETENTION PERIOD:** no audit retention policy exists ⇒ the oldest→newest span is labelled a MEASUREMENT, ⛔ never a policy · **CONTROL HISTORY:** `kill_switch_state` is ONE row ⇒ no history · **AUTHENTICATION:** no source at all. ⭐ The **OLD→NEW values ARE GENUINE** — produced by the real `_config_changes` diff of consecutive config snapshots, the SAME code path production uses, ⛔ not written by hand. 🔴 ⛔ **NOT `VERIFIED LIVE` — APPROVED ON DEMO DATA:** 🔬 the real VM snapshot holds **3** audit records across all of Jul–Aug, and 30-Aug is a Sunday. ⭐ The fill ran through the **out-of-repo review harness on :8501**, feeding RAW TABLE ROWS to the real readers so `build_audit` did its own diffing, filtering and aggregation on top — ⛔ no repo edit, ⛔ no demo DB, ⛔ no pointer, ⛔ nothing to revert. 🔬 **77 S13 tests pass (+4)**, RED-capability proven by mutation; full suite **2100 passed, 1 failed** = the `test_c_venv_has_no_kiteconnect` environment artifact. 🔬 Every added CSS selector is `.aud-page`-scoped, pinned by a test that walks the added block. ⛔ No backend, ⛔ no readers, ⛔ no bindings touched. 📄 `UNPUSHED_LEDGER.md` **Entry 24**.

> 🖥️⚡🔝 **30-Aug-2026 — S12 SYSTEM HEALTH `<APPROVED>`; GUI TRACK NOW **10 GREEN / 2 QUALIFIED / 10 PENDING**. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED.** 👤 Rama approved ~17:3x IST; build `bb1e0a9`. ⭐ A real pass against the artwork, ⛔ not a cosmetic patch. 🔧 **(1)** The band is **FIVE CARDS** (Overall/Healthy/Warning/Failed/Uptime) — the **UNKNOWN card is GONE**; ⛔ dropping the card must ⛔ NOT drop the STATE, so UNKNOWN's survival in the table, the status filter, the legend AND the colour map is pinned by test. 🔧 **(2)** Icons + per-status shares restored; `pctOf` returns an **em dash, ⛔ never 0.00%**, when the fleet size is unknown — a confident zero is a fabricated denominator. 🔧 **(3)** **VM metrics NESTED INTO the Uptime card** as the artwork draws them; the rail panel is deleted and the service table takes the full row. ⭐ RAM-available has no row in the artwork but IS measured, so it is kept — ⛔ losing a real number to a layout change is a silent regression; 🔬 all six still route through `gapCell`. 🔧 **(4)** Dependencies become **FIVE HORIZONTAL ICON CARDS** in a band of their own. 🔧 **(5)** ⭐ **THE THROUGHPUT PANEL — in the PNG, ABSENT from the design TXT** — built on the **EXISTING `activity_pulse` reader** (⛔ no new query, ⛔ no new table): signals by `received_at` · entry orders by `placed_at` · fills by **`trades.entry_time`**. ⚠️ The third series is the **FILL**, ⛔ NOT an order status — reading `orders.status` would count a broker ACK as a fill. ⛔ **AN EMPTY CHART IS A REAL ZERO**, ⛔ never NOT INSTRUMENTED: those three tables are read live, and claiming otherwise would assert the system cannot count its own orders. ⚠️ The artwork says PER MINUTE; a session is ~375 minutes and will not fit, so the counts are **summed into hours and the panel says so** — ⭐ the label follows the AGGREGATION, ⛔ not the drawing. 🔧 **(6)** Bottom **export bar** on the existing `/api/export/system-health`, + a Throughput sheet so the workbook cannot omit a visible panel. ⭐ **PRE-EXISTING DEFECT FIXED, found by MEASUREMENT:** the **13px typography floor was acting as a CAP** — `.sysh-page .num-neg` sits at the SAME specificity as `.sysh-page .sysh-ov-v` and appears LATER, so the instant Alpine put the semantic colour on the status word the artwork's largest text collapsed; 🔬 computed **13px where 1.7rem was written**, now 27.2px. ⭐ Restated one level deeper — ⛔ the floor itself is untouched and `test_b1_typography_floor` still passes. 🔬 **37 of 37 added CSS selectors are `.sysh-page`-scoped** and `components.html` is untouched, so ⛔ no other screen's `kpi_card` render moves. 🔴 ⛔ **NOT `VERIFIED LIVE` — APPROVED ON DEMO DATA.** ⚠️ 👤 **30-Aug is a SUNDAY, and on weekends/holidays the VM and trading system DO NOT RUN (SOFT_KILL)** ⇒ engine down, no uptime, NOT READY and empty series are the **CORRECT** state, ⛔ not a fault — and there was **no live data to review against until Monday**. ⭐ So the fill came from an **out-of-repo REVIEW HARNESS** that patches the **reader layer in-process** and serves the real app on **:8501** — ⛔ NO repo edit, ⛔ no demo DB, ⛔ no config pointer, ⛔ nothing to revert; 🔬 verified 4 modified files and 0 new untracked. ⭐ Patching at the READER boundary was deliberate: every rollup, the worst-wins logic, the readiness gates, the semantic colouring and the export all ran FOR REAL on demo inputs. 🔬 **117 S12 tests pass (+13)**; full suite **2100 passed, 1 failed** = `test_c_venv_has_no_kiteconnect`, the environment artifact that fails identically on a pristine checkout. 📄 `UNPUSHED_LEDGER.md` **Entry 23**.

> 🖥️⚡🔝 **30-Aug-2026 — S11 EXECUTION ANALYTICS `<APPROVED>`; GUI TRACK NOW **9 GREEN / 2 QUALIFIED / 11 PENDING**. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED.** 👤 Rama approved ~15:5x IST. ⭐ S11 was already BUILT (`3979b1b`, 15-Aug) with Scanner already removed — this was the **visual-approval pass** and its three corrections, ⛔ not a rebuild. 🔧 **(1)** `INTRADAY & DELIVERY EXECUTION ANALYSIS` takes the footprint the reference gives Scanner Ranking (**between Strategy and Symbol**), built from the **SAME generic `_rank`** on a pipeline label derived from the ENTRY product by the SAME rule the capital path uses (`pipeline_for_product`: not-CNC ⇒ intraday) — 🔬 on real VM data Intraday **187** · Delivery **84** · UNKNOWN **36** = **307 = execution_count exactly**, a true partition. ⛔ **UNKNOWN is its OWN ROW** — a trade with no ENTRY order has no product, and folding it in would fabricate the very dimension the panel exists to separate. 🔧 **(2)** **Symbol became a LEFT label column** (heading AND data together); ⛔ column SET unchanged so `COLS_KEY` stays v1 and no saved order is discarded. 🔧 **(3)** **NEW `TRADE STATE` FILTER** — ⚠️ the screen rendered TWO status-like columns and only one was filterable: `status` = the **DELAY BAND** (fast/moderate/slow) vs `trade_state` = the **LIFECYCLE position** (closed/open/pending/rejected) ⇒ *"show me only the CLOSED ones"* was impossible on a screen that displays the column. ⭐ It runs through the **SAME single filter gate** every panel reads, so KPIs, table, rankings, distribution, throughput, warnings **and the XLSX export** narrow together — ⛔ no panel can describe a different population. ⭐ The dropdown offers the **FULL vocabulary**, ⛔ not just states present this period (the option to isolate REJECTED must not vanish on a clean day). 🔬 260 → CLOSED 173 → OPEN 54 → REJECTED 16. ⭐ Also: the artwork's **bottom EXPORT BAR** (S11 had none), and the last user-facing Scanner wording REMOVED outright ⇒ 🔬 **0** rendered mentions. 🔴 ⛔ **NOT `VERIFIED LIVE` — APPROVED ON DEMO DATA** (260 synthetic trades), because the real-VM snapshot was too sparse to judge density: 🔬 **202 of its 307 rows never filled**. ⭐ Demo DB lives OUTSIDE the repo; pointer reverted byte-identically (md5 `6a6d93df…`, 0 pointer lines); 🔬 real DB mtime unchanged at **03-Aug 16:08** ⇒ never opened for writing. ⚠️ **THE 25-COLUMN ARTWORK STILL CANNOT BE BUILT** — 🔬 re-verified: `order_execution_log.exchange_timestamp` is a real column **NEVER WRITTEN** (the sole caller, `orders/slippage_recorder.py:149`, omits the key); risk/capital have no timestamp; `orders` has ONE instant. ⇒ 10 columns have no source and stay **NOT INSTRUMENTED**, ⛔ never `0.00`. ⏸ **REPORTED, ⛔ NOT CHANGED — 👤 Rama's ruling owed:** KPI **icons + sub-labels** (the icon needs the SHARED `kpi_card` macro every screen uses; the `MINTRADAY_001`-style labels need a new backend field) · the **page title** (S09/S10, both approved, use the identical house `panel-title` pattern). 🔬 **142 S11 tests pass**; 6 pinning tests updated to the new design and the Scanner assertion got **STRICTER** (1 mention → 0). 📄 `UNPUSHED_LEDGER.md` **Entry 22**.

> 🖥️🎨🔝 **14→28-Aug-2026 — GUI TRACK: ALL 22 SCREENS `<BUILT>`; 8 VISUALLY APPROVED. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED** (branch `feat/screen10-slippage-analytics` @ `d0a4054`, worktree `D:\Projects\trading-system-gui09`; 🔬 `ls-remote` returns **0 refs**). 🔬 **APPROVAL STANDING, from `docs/audit/VISUAL_APPROVAL_LEDGER_19-Aug-2026.md` at HEAD: 🟢 8 APPROVED** (S01·S02·S03·S04·S05·S08·S09·S10) · **⏳ 2 QUALIFIED** — **S06** approved on the **PRE-correction** render (`b47e148` verified, re-rendered, ⛔ never seen) and **S07** owes a **RE-approval** (approved 14-Aug under the previous ledger; `66fc82e` landed after it, unseen; 👤 Rama's 19-Aug 13px-floor decision re-opened all 22) · **⏳ 12 NEVER SHOWN** (S11–S22). ⛔ **The earlier "9 approved / 13 awaiting" figure was WRONG in both halves — it is 8 / 12.** ⚠️ ⛔ **NEITHER S09 NOR S10 IS `VERIFIED LIVE`:** 🔬 S09 was approved on **SEEDED DEMO data** (the local DB holds **0 closed trades**, last written 03-Aug ⇒ 75 demo trades rendered) and S10 on a **read-only 2.5 MB extract of real VM data**, whose 🔬 amber/red slippage path is **LIVE BUT NEVER EXERCISED** (real slippage ~₹0.00–0.02, **100% within limit**, 0 near, 0 exceeded). ⭐ Composition/alignment are confirmed; ⛔ populated PRODUCTION behaviour is not. ⚠️ Also unseen: **S08**'s 2 post-approval restorations (gauge geometry · gated export button); **S04** owes Q1·Q2, **S05** owes Q3·Q4. 🔧 **ONE STRUCTURAL CHANGE, not a screen: `efeb0b7` made the column reorder ONE implementation, not fifteen** — `colDragMixin()` at `static/components.js:95`, with `Object.assign` placing page members LAST so existing screens keep their overrides. 🔬 **Measured 30-Aug: 16 of 30 screen templates use it, and ALL 16 iterate `cols` inside `<tbody>` ⇒ 16/16 genuinely move the DATA, 0 header-only** — ⚠️ which is the property that matters, because a table with POSITIONAL `<td>`s moves the LABEL and leaves the DATA behind. ✅ **THE REFIT IS STILL ESSENTIALLY FREE — 🔬 RE-MEASURED 30-Aug: ZERO COLLISIONS, whole tree.** 135 gui09 files vs 68 main files since merge-base `6fa8a1c` (14-Aug), **0 overlap**; 🔬 main has touched `ops_dashboard/` in **0** files, gui09 in **87**. ⇒ ⭐ a **rebase**, ⛔ not a merge fight. ⚠️ **89 ahead / 39 behind `effff24`** — ⛔ the previously recorded *"37 behind"* was measured against `52ccb4f`, ⛔ NOT `effff24` (a different comparand, ⛔ not drift); after the 30-Aug push it becomes **48 behind** `39292d3`, and it only grows with every main push. ✅ **DEMO-DB HYGIENE HELD:** the `gui_config.local.yaml` pointer was reverted **byte-identically**, the demo DB lives OUTSIDE the repo, and 🔬 the real DB's mtime is unchanged at **03-Aug 16:08** ⇒ never opened for writing. 🔴 **NEXT: S11 Execution Analytics — it is already BUILT (`3979b1b`, 15-Aug); ⛔ what it needs is its FIRST VISUAL APPROVAL, ⛔ not a build.** ⭐ The two cheapest wins are **S06** and **S07**, where the work is done and only 👤 Rama's sight is missing. 📄 Record: `UNPUSHED_LEDGER.md` **Entry 21** (this window's 90 commits, 🔬 measured).

> 🗂️✅🔝 **10-Aug-2026 — ITEM 6 / THE DAILY-CRITICAL EOD REPORT: SEED⁄STATE SPLIT. `<BUILT · TESTED · ⛔ NOT PUSHED · ⛔ NOT DEPLOYED>` — branch `fix/registry-seed-state-split` off the DEPLOYED `645728d`.** 🔑 **`config/strategy_direction_registry.yaml` STAYS TRACKED and becomes a READ-ONLY SEED; runtime state moves to `data_store/strategy_direction_registry.yaml`** (⭐ `data_store/` is ALREADY gitignored, `.gitignore:17` ⇒ ⛔ no new ignore line, ⛔ no new config key — (P) `git diff 645728d -- '*.yaml'` is EMPTY). **WHY IT WAS CRITICAL DAILY, TRACED:** `system_manager.py:1195` diffs the deployed tree → `:1218` `res.violation()` → `:884` sums → **`:1336` `severity = "CRITICAL" if (violations or reasons) else "INFO"`** — ⭐ **the report has NO WARNING severity, it is BINARY.** 🔴 **THE RECORDED PREMISE IS PARTLY REFUTED: the file is NOT “rewritten daily” — (P) `strategy_registry_officer.py:253-254` calls `save_registry` ONLY `if changed`. The real mechanism is a PERSISTENT UNCOMMITTED DIVERGENCE** (HEAD = the 17-Jul seed, **MEASURED 16 rows all `PENDING`, ⛔ not the recorded 18**) ⇒ ⛔ “treat a daily-regenerated file as expected” rests on a dead premise. 🔬 **UNTRACKING WAS MEASURED AND REJECTED: in a throwaway repo against the actual hook command (`deploy/hooks/post-receive:31`), the first `checkout -f` after `git rm --cached` DELETES the live file** — untracked-file protection does not apply to a path tracked at the OLD head ⇒ it would have cost `first_seen` for 16 strategies + a 16-row NEW alert burst. ⭐ **The split loses NOTHING: the seed is a FALLBACK (never an override), so the first post-deploy run re-CONFIRMs silently and `first_seen` survives.** 🧪 **RED-first MEASURED `8F/3P` pre-fix → `11P` post-fix; ⭐ the 3 pre-fix passes are exactly the anti-suppression CONTROLS (a genuine tracked drift MUST still go CRITICAL) — adjacent set `56P`.** ⛔ **`scripts/system_manager.py` is UNTOUCHED — (P) `git diff --name-only 645728d -- scripts/system_manager.py` is EMPTY ⇒ no severity weakened, no path whitelisted.** ⛔ **`registered_direction` NOT retired** (zero callers CONFIRMED, width = whole tree all file types; retiring it is its own decision). 📄 `docs/audit/registry_seed_state_split_10aug2026.md`. ⚠️ **PARITY: cron/report path — MODE-INDEPENDENT; ⛔ not “covered by paper”.**

> ⏱️🎯🔝 **07-Aug-2026 EVENING — THE ENTRY THROTTLE MEASURES *ENTRY → ENTRY*; THE 300 s COOLDOWN IS NOT A FALLBACK FOR GATE 1; AND `330657774` IS CLEAN WHILE `DIFFNKG` IS NOT. ⛔ READ-ONLY — no code, no config, no schema, no GTT cancelled.** Report: **`docs/audit/rulings_1_2_verification_07aug2026.md` §8** — an **APPEND, ⛔ not a new file**, because it closes that document's own **OPEN-1/OPEN-2** and corrects its **H1 row 5 / H7 row 5**. **① Q1 — (b) CONFIRMED DESIGN (S)+(P): `_per_symbol_last[symbol]` is written at EXACTLY ONE place in the repository — `signals/entry_throttle.py:113`, inside `admit()`, on the admit path only.** *(Width: repo-wide grep for `entry_throttle|EntryThrottle|per_symbol_cooldown|\.admit\(` over every `*.py`; every other hit is a constructor arg, a metrics read, or one of the three `admit()` calls.)* **⛔ THERE IS NO EXIT HOOK, NO FILL HOOK AND NO RECONCILER TOUCH — the class cannot learn that a position closed.** `admit()` fires at **DISPATCH**: all **three** production `_placer.place(` sites (`signals/signal_processor.py:1263 · :2028 · :2305`) are each immediately preceded by it (`:1249 · :2017 · :2296`), and there is no fourth. **② Q3 — the SENCO arithmetic, every operand quoted from `logs/system_2026-07-27.log`:** entry dispatch **`10:02:12.687`** · TGT exit **`10:13:31.744`** · re-entry dispatch **`10:14:12.331`** ⇒ **ENTRY→ENTRY = 719.644 s = 2.399× the 300 s gate ⇒ ADMITTED, correctly.** EXIT→re-entry = **40.587 s** (dispatch) and **79.413 s** (fill — which *is* the incident record's "80 seconds", reconciled at full precision). ⭐ **THE THROTTLE NEVER SAW 80 SECONDS.** The two facts that looked contradictory describe **different intervals**, and **no control in this system measures the second** — the tension is dissolved, not explained away. **③ Q2 — ALL FOUR CANDIDATES SCORED, and three are REFUTED BY MEASUREMENT rather than left unsupported:** *wrong value* 🔴 refuted — `git show d3fa5b8:config/system_config.yaml` reads **`per_symbol_cooldown_sec: 300`**, and `git log -S` across the config, the class, the processor, `main.py` and the loader returns **exactly one commit ever** (`c0554c6`, 19-Jun) ⇒ **the value has never been edited**; *not on the path* 🔴 refuted — the gate fired **7×** that day, **twice in its `per_symbol` category**; *state lost to a restart* 🔴 **refuted decisively — the COMPLETE `systemd[1]` record for 27-Jul is THREE LINES** (`Started 08:15:30` · `Deactivated successfully 17:35:04` · the CPU summary) ⇒ **ONE continuous process spanning both entries**, so the in-memory map still held the 10:02 timestamp at 10:14. ⇒ ⭐⭐ **the throttle did not fail, was not reset and was not misconfigured — it held the right operand, computed the right interval and correctly admitted. THERE IS NOTHING HERE TO FIX.** **④ ⭐⭐ PROVEN ON PRODUCTION DATA, NOT ONLY READ FROM SOURCE — the reject string prints its own operand** (`f"per_symbol {symbol} {elapsed:.0f}s < …"`): `per_symbol PYRAMID 60s < 300s` @`10:07:14.035` sits **60.334 s** after `place_start` `10:06:13.701`, and `61s` @`10:12:14.835` sits **60.932 s** after `10:11:13.903` — **both match at printed precision. And the discriminator is CLEAN because PYRAMID never exited: its 10:06 entry was BROKER-REJECTED** (`Zerodha rejected order: MIS orders are currently blocked for PYRAMID`, `10:06:14.678`) ⇒ **an exit-keyed cooldown had no timestamp to measure from and could not have printed either value.** **⑤ ⚠️ NEW, IN NO DOCUMENT BEFORE TODAY — (b) CONFIRMED DESIGN with a consequence: `admit()` records BEFORE `place()` is called** (deliberate; the docstring's TOCTOU rationale is sound) **⇒ AN ORDER THE BROKER REJECTS STILL CONSUMES THE SYMBOL'S FULL 300 s.** PYRAMID is the measured instance — three later signals blocked on behalf of a position that never existed. 🏷️ Same family as *"it counts its own rows where it means the broker's reality"*. ⛔ **Not a defect; not proposed for change.** ⚠️ And PYRAMID's two dispatches are **300.202 s** apart — it re-entered **0.202 s after expiry**, so with a ~60 s scanner cadence **the cooldown SCHEDULES the re-entry rather than preventing it** (the measured form of H7's *"a throttle DELAYS"*), which is what **OPEN-2** actually turns on. **⑥ 🔑 THE NUMBER THAT PRICES OPEN-1 — the cooldown's residual protection is exactly `max(0, 300 − holding_period)`, i.e. ZERO for any trade held over five minutes. (P) MEASURED across 225 closed trades carrying both timestamps: 185 = 82.2 % were held ≥ 300 s; median hold 1,596 s = 5.3× the cooldown** (SENCO's own trade 1 was held **679.057 s** and its cooldown had been dead for **379.057 s** when it exited). ⇒ **turning gate 1 off does NOT fall back onto the cooldown — on ~82 % of trades it falls back onto NOTHING**: gates 2+3 release at the exit by design, the concurrency caps were freed by the exit, and `max_daily_trades` has not bound since 10-Jul. **⑦ 🔴🔴 AND THE FACT THAT REFRAMES OPEN-1 — GATE 1 DID NOT EXIST WHEN SENCO RE-ENTERED.** The deployed tree at 10:14 was `d3fa5b8` (26-Jul 23:19) and **both halves are absent from it**; `656b62d` **27-Jul 13:54:12** wrote the code (*"DEFAULT OFF"*) and `300a247` **27-Jul 15:24:31** set it `true` (*"TURN ON the one-trade-per-symbol+direction rule (Rama, 27-Jul eve)"*) — **+3 h 40 min and +5 h 10 min after the re-entry, the same afternoon.** ⇒ **SENCO is the incident that CREATED gate 1, not a trade gate 1 happens to cover**, and OPEN-1 asks whether to remove the control that incident produced. ⭐ Settles half of **OPEN-7**: there was no pre-existing rule for a deploy to lag *(28-Jul→03-Aug still undetermined)*. ⛔⛔ **THIS IS A MEASUREMENT AND A FRAMING, NOT A RECOMMENDATION — the flip is Rama's, and the counter-case is untouched: the SENCO report measured the re-entry population at n=2 over five weeks, total stake ≈ ₹5, "two anecdotes". A control can be the only one of its kind and still not be worth its cost; what changed is that the price of removing it is now KNOWN.** **⑧ Q4 — (b) direction-BLIND and product-BLIND: the state is `dict[str, float]` keyed on the bare symbol (`:58`) and `admit(self, symbol)` takes nothing else** ⇒ a CNC delivery entry and a MIS intraday entry on one symbol contend for **one** cooldown. **⑨ 📵 Q5 — (d) CANNOT DETERMINE BY OBSERVATION; parity is (I), never (P). ⛔ EXERCISED IN LIVE ONLY: all 24 retained daily logs (`2026-07-07`→`2026-08-07`, the full 30-day window) read `mode=live`; ZERO paper days.** ⛔ "Never in paper" is **not** established — retention bounds the check. ⭐ **But the standing PAPER-NETS-BY-SYMBOL / LIVE-NETS-PER-(SYMBOL,PRODUCT) hazard does NOT reach this control — it reads no broker quantity** ⇒ a paper drill here would be genuinely informative rather than vacuously green. **Not run, not proposed.** **📌 PREDICTIONS SCORED 4 HELD / 0 FAILED / 1 correctly ABSTAINED — and stated honestly as WEAKER evidence than this morning's P3 failure.** ⭐ The one that earned its keep is **Q2-P**: naming lost in-memory state *in advance* as the more serious alternative forced a check that would otherwise have been skipped once candidate 1 already explained everything — **and a skipped check would have left "the state was probably fine" where there is now a three-line proof.**
>
> 🚨🧺🔝 **07-Aug-2026 EVENING — THE `330657774` SAFETY CHECK PASSED AND ⛔ ITS PREMISE IS REFUTED: ATULAUTO IS CLEAN; THE LIVE-MONEY ITEM IS `DIFFNKG`.** Answered from the **BROKER API** (`get_gtts` / `orders` / `holdings` / `positions`), ⛔ **never from `gtt_state`** — the API lister is the only source and Kite's web UI never shows a GTT id. **✅ `330657774` IS ABSENT FROM THE BROKER** — 4 GTTs returned, **no ATULAUTO at any status.** **The system cancelled it itself at this morning's boot:** `08:15:41.704` `cnc_gtt_monitor.forensic … detail:"GTT active but holding flat (external close)"` → **`08:15:41.722` `delete_gtt call_end … gtt_id:"330657774", mode:"LIVE"`** → `08:15:41.795` `gtt_exit exit_price 579.55 pnl −9.35` ⇒ `trd_e66ee17b…` **CLOSED, `exit_reason='GTT_EXIT'`.** ⇒ the standing *"no manual ATULAUTO buy"* DO-NOT **can be lifted — ⛔ by RAMA, not by me.** **🔴 BUT `330944932` · DIFFNKG · `active` · SELL/SELL · CNC/CNC · triggers `[437.2, 459.45]` · created today `15:20:35` IS RESTING AT ZERODHA AGAINST A HOLDING THAT IS ALREADY FLAT AND ALREADY SOLD.** **(P)** `holdings()` DIFFNKG **`quantity=0, t1_quantity=0`**; `positions()` CNC **`net=-1, buy=0, sell=1`** — the completed SELL surviving as a NEGATIVE row, exactly the F6 mechanism; `orders()` shows **ONE** DIFFNKG order today, `260807170745584` SELL CNC LIMIT **`filled=1` COMPLETE at 14:50:52**; and `trades.trd_010f8e21…` is still **`OPEN`** — the phantom. **The respawn is visible as a sequence:** `330658430` TRIGGERED (producing the 14:50:52 fill) → `330940420` created `15:05:26`, TRIGGERED `15:08:11` → `330944932` created `15:20:35`, **ACTIVE now**. 🏷️ **This is F6 cost 4 — *"three live SELL GTTs on a flat holding"* — MEASURED LIVE TONIGHT, on the symbol no card named.** ⛔ **NOT CANCELLED — a live broker action, unauthorised here.** ⭐ **Markets are shut, so nothing can fire tonight; the exposure is MONDAY.** ⭐⭐ **`MANINFRA` is CORRECT and must NOT be swept with it:** `positions()` CNC **`net=+4, buy=4, sell=0`** — a real position — with one matching `active` GTT `330856765`. ⚠️ *Stated not chased (G3): the `15:08:11` trigger of `330940420` has NO corresponding order anywhere in the broker's order book.*
>
> 🏛️⚖️🔝 **07-Aug-2026 — TWO GOVERNANCE RULINGS TAKEN BY RAMA (ledger rows 1 + 2), AND THE VERIFICATION THEY COMMISSIONED REVERSED THE SEQUENCING. ⛔ DOCS-ONLY: `git diff 348c226..HEAD -- '*.py' '*.yaml' '*.sql'` is EMPTY.** Report `docs/audit/rulings_1_2_verification_07aug2026.md`; ruling text `docs/MASTER_PENDING_01-Aug-2026.md` §R. **① RULING 1 — `ops_dashboard/docs/G2a_capacity_inventory.md` IS THE CONTROL-INVENTORY AUTHORITY.** The 27-row set at `docs/audit/audit_05jul2026.md:527` is **SUPERSEDED** (frozen history, pointer added, rows untouched); the 113-key `config_surface_review_06aug2026.md` is **MERGED IN** and demoted in place to evidence. **⛔ LOCATION NOT DECIDED — the file stays under `ops_dashboard/docs/`; moving it breaks `capacity.py`'s consumer.** Merge arithmetic **`30 + 14 = 44` rows**, re-added independently two ways; leaf coverage **169 of 301** measured, and the 132 uncovered decompose **98 signal-generation + 34 infra = 132**, independently reproducing the review's own "98 excluded". **⛔ It does NOT claim to hold "113 keys" — the 113 is a SCOPE COUNT, never a row set**, and manufacturing 113 rows would have been a new measurement wearing a merge's clothes. **🔴 FIVE COLLISION CLASSES, none resolved silently: (H-1)** `risk.max_consecutive_losses` is **4**, both inventories said **5**; **(H-2)** both tag the delivery caps **"INERT (`force_intraday_only=true`)"** and the measured config is **`force_intraday_only: false` + `delivery_enabled: true` + `trade_type: BOTH`** with two CNC positions open ⇒ **the premise is FALSE at HEAD**; **(H-3)** ⭐⭐ **10 of 11 shared families disagree with the measured leaf count — only `drift_handler.*` (4) matches** (`alerts.*` is **31**, not 9; `entry_gate.*` is **17**, not 9) — *a family counted as if it were a key*, the same shape as the `31+34+6+42=113` accident; **(H-4)** the two documents disagree on whether **4 of the 30 existing rows belong at all** — ⭐ **the join on the dotted key worked perfectly and prevented none of this**; **(H-5)** a **fourth** `DUPLICATE_SYMBOL` site exists (`allocation/portfolio_allocator.py:182-183`, inert — `allocator_mode: shadow`) and appeared in **no** inventory. **⚠️ THE RULE BOUND TWICE AND STILL NEEDED AMENDING** — a dotted key is a sufficient JOIN identifier and an insufficient MERGE identifier, so the authority now also declares **ROW UNIT · INCLUSION RULE · VALUE PROVENANCE**. 🏷️ Fourth campaign rule found wanting **by being run, not by being read.** **② RULING 2 — ONE SIMULTANEOUS OPEN POSITION PER SYMBOL, ACCOUNT-WIDE, PIPELINE-INDEPENDENT** *(reject while open; eligible again the instant it is flat; "evaluated from the current account state, not from historical ownership")*. **🔴🔴 H3 REFUTED THE PRE-REGISTERED PREDICTION P3 AND IT IS THE FINDING: gates 2 and 3 ALREADY implement it.** `DUPLICATE_SYMBOL` (`capital/risk_engine.py:688-694` ← `has_active_position`, `core/state_store.py:845-852`) and `CONTRARY_POSITION` (`:665-686`) are **product-blind, have NO date term, and release the instant the trade leaves `PENDING_FILL/OPEN/PARTIAL`** — and they read **one predicate twice**, which is why `REJECTED_CONTRARY_POSITION` is **0 in 109,254 signals** (width: the complete status distribution). **Only gate 1 is date-scoped** — `SYMBOL_DIRECTION_DAILY_LIMIT`, `signals/signal_processor.py:706-720`, and it is **the only one with a config key** ⇒ ⭐ **the loosening half of Ruling 2 is ONE BOOLEAN, `risk.one_trade_per_symbol_direction_per_day: true→false`, byte-identical to the pre-27-Jul path by the code's own docstring. ⛔ NOT FLIPPED, NOT STAGED, NOT PROPOSED.** **(P) The masking is now MEASURED, not reasoned:** `REJECTED_DUPLICATE_SYMBOL` **107, last 31-Jul**; `REJECTED_SYMBOL_DIRECTION_DAILY_LIMIT` **65, first 03-Aug** — consecutive trading days ⇒ **anyone measuring the symbol block under the old code after 03-Aug reads ZERO.** **🔒🔴 H5 REVERSES THE SEQUENCING — F6 IS A PREREQUISITE OF RULING 2, NOT A BENEFICIARY: 0 of F6's 7 costs are retired.** The `abs()` is **not lexically** on the gate path (**P5a HELD** — the corrected, weaker prediction survived where the chat claim overreached), but **`held == 0` is the SOLE door to `_finalize_gtt_exit`** (`orders/cnc_gtt_monitor.py:487` · `:501-510`), so F6 is why a delivery trade never leaves `OPEN` — and `trades` is the gate's **only reachable** source. **Cost 3 (the symbol block) would retire ONLY on broker truth, and broker truth is UNREACHABLE at the gate:** `RiskEngine.__init__` takes **no adapter**, and `signals/signal_processor.py:26` says *"SP14 — Layer 5; no direct broker import"*; `positions()` is blind to delivery from T+1, `holdings()` blind to intraday ⇒ **neither alone covers both products.** **🔴 P5b CONFIRMED — `abs()` on a SIGNED broker quantity is a CLASS at SIX production sites** (`cnc_gtt_monitor:464` · `order_reconciler:2185/2291/2968/3120` · `eod_squareoff:1079/1487/1525/1613` · `kill_switch:1281` · `structure_exit_manager:631`), harmless where the intraday net reaches 0 and wrong wherever a completed CNC SELL survives as a negative row ⇒ ⭐ **a broker-truth predicate written in the house style would reproduce F6 at a new site on day one.** **📏 H6 — the loosening MEASURED: 65 rejections / 5 days / 16 symbols → 56 would still reject, 🔴 9 would now be PERMITTED, on 2 days across 4 symbols (56+9=65 ✅).** ⭐⭐ **But the number that matters is 11: 05-Aug executed 8 (+3) and 06-Aug executed 5 (+6) ⇒ BOTH days reach 11 against `max_daily_trades = 10`.** ⚠️ Lower bound — **7 of the 56 are blocked by a trade with `exit_time IS NULL`** (`DIFFNKG`, `MANINFRA`), so **F6 contaminates the denominator in the same direction as the defect.** **🔴 H7 DISPROVED ITS OWN PREMISE: no DAILY cap has bound since 10-Jul** (`REJECTED_DAILY_TRADES` 5,146 — **all of it 09–10 Jul**; peak since **9** vs cap **10**). What still fires is **CONCURRENCY** — `max_open_positions` 500, per-strategy 302 — ⭐ **and a same-day re-entry after a complete exit does NOT consume a concurrency slot, because the exit released it.** The earliest-firing control on **19 of 21 days** is the **entry throttle** (`REJECTED_ENTRY_THROTTLED` 436) — ⛔ **a throttle DELAYS, it does not close the day.** `daily_loss_limit_pct` has **never** rejected anything; `sector_cap_mode: observe` ⇒ the sector cap is inert. **📌 SCORED 5 HELD / 1 FAILED (P3) / 1 SPLIT (P4 — reachability held, sufficiency failed).** ⚠️ **OPEN, owed to Rama:** the config flip is **not authorised**; `per_symbol_cooldown_sec: 300` **contradicts** the ruling's *"immediately becomes eligible again"* and needs one sentence from him; `CONTRARY_POSITION` is an unreachable gate whose retirement is a decision, **not proposed**. ⚠️ *Stated not chased (G3): 17-Jun shows 12 executed trades against a cap of 10.*

> 🕯️🔒🔝 **25-Jul-2026 (BATCH 5: THE TICK-FEED DECISION + SHADOWTRACKER OFF + M-A2 8s — latest) — ⭐⭐ THE LIVE TICK→CANDLE FEED IS DELIBERATELY DORMANT. THIS IS A DECISION, NOT A BACKLOG ITEM — it is not "we never got to it", and it has a REOPEN TRIGGER, in the same discipline as the exits closure.** **DEPLOYED off-market `488e7e3`** (`d31759f` §A ShadowTracker OFF · `488e7e3` §B M-A2 8s; crontab byte-IDENTICAL; service NOT restarted — `inactive`, `NRestarts=0`, start still Fri 24-Jul 08:15:27; both land Mon 27-Jul 08:15). **⭐ §C — THE DECISION: leave the tick feed dormant. Nothing subscribes; nothing ever has** (`live_feed.subscribe()` is never called in any boot path — the socket has **connected 32 times and carried 0 tokens, 0 ticks, ever**). **THE FOUR LEGS OF THE REASONING:** (1) **the 15:40 historical 1-minute fetch covers every ANALYTICAL need and carries TRUE DELTA volume** — the tick path under `MODE_LTP` carries **no** `volume_traded` at all, so wiring it would give strictly WORSE data; (2) **four of the five starved consumers are blocked INDEPENDENTLY anyway** — `smart_tgt` (never registered: `order_placer.py:949` sets the protocol unconditionally, `smart_tgt_state`=0 rows) · `StructureExitManager` (`structure_exit_enabled: false`, deliberate) · `BreakevenManager` (**never constructed** in `main.py`, whole history) · so a subscription alone would light up none of them; (3) **the fifth (`ShadowTracker`) is now DISABLED** (§A below); (4) ⭐ **EVERY feature the tick feed would serve is an EXIT feature, and the exits thread is CLOSED on measured numbers** (8 policies over 115 real trades with real costs: current −0.100R, best alternative −0.076R, worst −0.144R — **none profitable**). ⇒ **wiring it would cost real work to feed features that are themselves closed or dark.** **⭐⭐ THE REOPEN TRIGGER (a trigger, not a note — revisit ONLY if):** **(a) a LIVE SAFETY dependency on ticks is identified** — none exists today: the broker-side SL is independent of the feed, the tick-age watchdog and H-9 guard are the feed's own health checks (not position protection), and **⭐ FIX-029's consumer-thread death detector — the one genuine safety casualty — was un-gated on 25-Jul (B1) and no longer depends on ticks arriving**; **or (b) the ENTRY work materially changes and the exits are re-derived post-M-S4** (the same trigger the exits closure carries). **Any earlier reopen is a mistake.** ⚠️ **TWO HAZARDS CARRIED FORWARD so a future fix does not hit them: (i) SYNTHETIC-CANDLE STICKINESS** — one real tick gives a token `_history`, after which `_close_candles()` emits a carry-forward candle for it **every 60 s for the rest of the process, even after `unsubscribe()`** (no eviction but `gc_sweep`/`MAX_HISTORY`), each fanning out to every callback **including a `_persist_candle` DB insert** ⇒ subscribe-then-forget leaks a permanent 1/min write per token ever touched; **(ii) ⛔ THE MODE_FULL ORDERING TRAP — flipping to `MODE_FULL` would ACTIVATE M-D1's volume corruption** (`volume_traded` is CUMULATIVE and `candle_store.py:63` SUMS it per tick), which is latent **only** because MODE_LTP delivers nothing. **M-D1 FIRST, ALWAYS.** MODE_LTP is sufficient for every consumer (OHLC is built from LTP by construction, Audit 3.4). **⭐ §C5 — `_persist_candle` IS FULLY REDUNDANT:** wiring it would add a **second, worse-quality writer** to `analytics.candles`, a table the 15:40 historical fetch already fills correctly (275,129 rows, true delta volume) ⇒ that consumer should stay dark even if the feed is ever wired. **✅ §A SHADOWTRACKER OFF — DEPLOYED `d31759f`** (config-only, component intact; VM verified reading `enabled: false`). It did not fail quietly, it FABRICATED: 141 simulated innings, **`exit_price == entry_price` on 130/130, `pnl_pct` 0.0, SL=0/TGT=0, zero inning-3 ever.** ⛔ **The 141 rows STAY, marked VOID by predicate** (`is_real=0`, id 4..321, 2026-06-17→2026-07-24) — deleting evidence of a defect is worse than labelling it. Lossless: `daily_report.py:646`'s `max_favorable` loop is a **measured** no-op (309/309 innings' `exit_price` == the trade's, 0 exceeding). **✅ §B M-A2 DEADLINE 30s → 8s, CONFIGURABLE, EXPLICIT IN YAML — DEPLOYED `488e7e3`.** 30 sat **above** the measured ~26 s ladder so it barely bound on the very case it exists for. **8 s = one full HTTP attempt (5) + one backoff (2) + margin ⇒ attempts `[5.0, 1.0]`, total 8.0 s** on a hung endpoint (computed from the shipped config by execution); **a healthy send is untouched** (answers in ms, still gets the full 5 s timeout). ⭐ **SAFE because TG5 writes the CRITICAL sentinel BEFORE any HTTP: the deadline discards a delivery ATTEMPT, never a warning.** ⛔ **The alert's ORDERING relative to placement is UNTOUCHED** — moving it is a separate behavioural change with its own loop, recorded as a QUEUED OPTION, not a todo. ⚠️ **If `TELEGRAM_CHANNEL_SECONDARY` is ever enabled the ladder doubles to ~52 s and the budget is SHARED across channels — decide the two together.** **✅ §D TWO TEST-HYGIENE FIXES, one failure mode (a test failing for a reason unrelated to what it asserts, which makes every BASE-vs-MERGE gate ambiguous):** the **wall-clock time-bomb** (`test_holiday_guard_missing_yaml_proceeds` passed before 18:15 and failed after, on identical code — now pins the CLOCK so the real guard still executes; **the class is ONE instance, not three** — the holiday/weekend tests exit at `main.py:1720` before the window guard at `:1780`, now pinned by test) and the **encoding class** (6 tests read repo config via bare `read_text()` ⇒ cp1252 ⇒ a star `E2 AD 90` in a yaml COMMENT hard-failed three of them; the **application was never at risk** — `config_loader` uses `read_bytes()` + `yaml.safe_load`). Both proven by **PLANTING**. ⚠️ **`config/system_config.yaml` is now ASCII-only by convention.**
> 🛡️🕯️ **25-Jul-2026 (BATCH 4: SHADOWTRACKER DISABLE *held* + THE THREE WATCHDOGS + FIVE RECORDS) — A PROTECTION FOR AN UNRELATED THREAD HAD BEEN SWITCHED OFF BY THIS DEFECT; IT IS BACK.** **⭐ C2 — THE LIVE TICK→CANDLE PATH IS DORMANT AND ALWAYS HAS BEEN. The chain is FULLY WIRED except ONE missing call: `live_feed.subscribe([tokens])` is never invoked in any boot path.** `_tick_dispatcher` IS registered (`main.py:2547`), `_persist_candle` IS registered (`:3391`), `candle_store.start()` (`:3373`), token map injected (`:3393`), `live_feed.connect()` (`:3394`) — but `_subscribed` is empty so `_on_connect` returns at `if not tokens: return` (`live_feed.py:333`). ⚠️ **A fully-wired chain with no input reads as healthy — do not be misled.** MEASURED all-history: **`connected to KiteTicker`=32 · `exit_retry_subscribed_to_ltp`=0 · `re-subscribing to N tokens`=0 · `no tick for`=0 · `ZERO real ticks`=0.** **THE FIVE CONSUMERS, all starved:** `_persist_candle` (0 of 275,129 rows ever — all are `fetch_daily_candles`' space-`ts` format) · `smart_tgt._on_candle_close` · `StructureExitManager.on_1m_close` (flag-off anyway) · `BreakevenManager.on_candle_close` (never constructed anyway) · **`ShadowTracker.on_tick` — blocked ONLY by this.** Plus `candle_store.get_candles()` → always `[]` (`smart_tgt:796`). **⭐⭐ C3 — THE TRAIL IS A *THREE*-FIX JOB, each cause INDEPENDENT and INDIVIDUALLY SUFFICIENT: (1) nothing is ever REGISTERED** (`order_placer.py:949` `order_protocol = self._default_protocol` set unconditionally — "OP9: choose protocol" with no choosing — discarding the 13/16 `CO_PLUS_TGT` strategies; registration `:2192` gated on `== "CO_PLUS_TGT"`; `smart_tgt_state`=**0 rows**) · **(2) no candle EVER ARRIVES** (this banner) · **(3) `BreakevenManager` is NEVER CONSTRUCTED** in `main.py` (`git log -S breakeven_manager -- main.py` EMPTY for all history ⇒ `OrderPlacer` gets `None`; its `_get_sl_broker_order_id` also selects a non-existent `broker_order_id` column). **Fixing any ONE alone still yields 0 trails.** ⛔ **THIS DOES NOT REOPEN THE EXITS THREAD** (closed 24-Jul; reopen trigger is post-M-S4 re-derivation ONLY) — **it corrects the COST ESTIMATE feeding Rama's 5A, nothing more.** **⚠️⭐ C4 — THE MODE_FULL ORDERING TRAP: flipping the subscription to `MODE_FULL` would ACTIVATE M-D1's volume corruption.** `subscribe()` hardcodes `MODE_LTP` (`live_feed.py:182`), which carries **no** `volume_traded`, so `main.py:2543` records 0 and `_Accumulator.update` sums nothing. `volume_traded` is **CUMULATIVE** and `candle_store.py:63` **sums it per tick** ⇒ neither cumulative nor delta. M-D1 is latent **only** because MODE_LTP delivers nothing. **M-D1 MUST BE FIXED BEFORE ANY MODE_FULL FLIP — MODE_LTP is sufficient for every dormant consumer** (OHLC is built from LTP by construction, Audit 3.4; `_check_hit` compares LTP). **⚠️ C5 — SYNTHETIC-CANDLE STICKINESS (a write-leak trap for the eventual subscribe-fix): once a token receives ONE real tick it acquires `_history`, and `_close_candles()` then emits a SYNTHETIC carry-forward candle for it EVERY 60 s for the rest of the process — even after `unsubscribe()`** (no eviction but `gc_sweep` on `InstrumentsRefreshed` and `MAX_HISTORY`). Each synthetic fans out to **every** registered callback including a `_persist_candle` DB insert ⇒ subscribe-then-forget leaks a permanent 1/min write per token ever touched. Emission is bounded by what was subscribed, not by `token_map`'s 2,228. **✅ §B1 FIXED + DEPLOYED — FIX-029's consumer-thread DEATH DETECTOR had never run in production.** `_watchdog_loop` had a pre-arm loop spinning on `_last_tick_at is not None`, and `_check_consumer_health()` lived in the loop *after* it ⇒ with no ticks it was unreachable: **a protection for a thread with nothing to do with ticks, disabled by an unrelated module's wiring gap** ("watchdog started"=32, "no tick for"=0). Collapsed to ONE loop; the health check now runs every interval **ahead of** the arming gate. No new alerts (no-ops when healthy/not-started). RED-first 1-of-6, PARITY asserted (paper starts no consumer thread and no watchdog). **⛔ §B2 REPORTED, DELIBERATELY NOT ARMED — the tick-age watchdog and the H-9 starvation guard are silent because there are no ticks, which is CORRECT for this state; arming them now would alarm every interval of every day.** THE RIGHT SHAPE: **arm on "a subscription exists" (`_subscribed` non-empty), not on connect and not on first tick** — then "subscribed but no tick" becomes a genuine anomaly while "nothing subscribed" stays correctly silent. ⚠️ **H-9 is structurally blind to *never-started*:** `if to_close: reset / elif synthetic_candidates: warn` — under total dormancy BOTH are empty so NEITHER branch runs; it only catches ticks-that-*stop*. Pinned by tests so neither can be armed silently. **⭐⭐ §A3 — THE 141 FABRICATED `innings` ROWS ARE VOID. DO NOT READ THEM AS RESULTS; DO NOT DELETE THEM** (deleting evidence of a defect is worse than labelling it). **THE VOID SET, exactly: every `innings` row with `is_real=0` — 141 rows, `id` 4..321, `entry_ts` 2026-06-17T10:00:53 → 2026-07-24T13:56:20, all `inning_number=2`, 141 distinct trades, `exit_reason` EOD=130/NULL=11.** They are not measurements: **`exit_price == entry_price` on 130/130 with `pnl_pct` exactly 0.0** (real innings: 1/181, and 180/181 non-zero), because the EOD handler prices its exit as `_last_price.get(symbol, entry_price)` and `_last_price` is tick-fed. **SL=0 and TGT=0 across all 141** — for a *simulated* inning only `_check_hit` (the tick path) can write those, while real innings copy the TRADE RECORD (`:343-351`), which is why they look healthy. **Zero inning-3 rows EVER** (cascade needs SL/TGT). ⚠️ **Anyone reading this table would conclude trades go nowhere after exit. That conclusion is an artefact.** Any FUTURE `is_real=0` row created after ticks actually flow is valid — the void set is bounded by the dates above. **§A HELD:** `shadow_tracker.enabled: false` is built and committed on branch **`hold-shadowtracker-disable-25jul`** — **NOT pushed, awaiting Rama's "off"**; ship = one cherry-pick. **A2: `enabled:false` is a COMPLETE write-stop, proven by EXECUTION** — all three write sites (`:384`/`:510`/`:555`) are reachable only via four public entry points, each guarded first (`:209`/`:224`/`:269`/`:422`); driving all three handlers with `enabled=False` gives `insert_inning`=0, `update_inning_close`=0. It still constructs and still subscribes to the bus (`:190-191` unconditional) — both handlers no-op. **A4: nothing real is lost** — `daily_report.py:646`'s `max_favorable` loop is a **MEASURED no-op** (309/309 innings have `exit_price` EXACTLY equal to the trade's exit price, **0 exceeding**) so the 16:05 report is **byte-identical** without innings; the GUI `inning_no` is already always NULL (innings exist only post-close, that query filters `_OPEN_STATES`); the innings count tile merely stops growing; and `is_real=1` rows hold nothing `trades` does not. **🆕 FOUND, recorded not fixed: `test_interactive_startup.py::test_holiday_guard_missing_yaml_proceeds` is a WALL-CLOCK TIME-BOMB** — it calls `main()` without mocking the service-window guard, so after the **18:15 `SERVICE_START_CUTOFF`** (widened 25-Jul `570b3e8`) `main()` returns 0 at the window check instead of reaching the config load, and it asserts 5. Passes before 18:15, fails after. **An evening regression therefore shows 33-34 failures, not 32 — do NOT misread that as a code regression.** Report `docs/audit/tick_candle_dormancy_25jul2026.md`; the 02-Jul security audit's resilience claim is struck-through-and-corrected in place. **🆕 BOOT-LOG OBSERVABILITY, VERIFIED 25-Jul (a register audit found two of three proposed 08:15 operator greps DO NOT EXIST):** the app boot log is **`logs/system_<YYYY-MM-DD>.log`, NOT journald** (journald holds only ~6 lines/boot — WARNING+ and stdout; MEASURED on Fri 24-Jul). ✅ **Greppable:** `V3 Step 10b PB-01 watchlist: ENABLED …` (`main.py:3207`, DISABLED `:3219`, present once per boot 20-24 Jul) · the kill auto-clear (in journald) · `eod_self_exit: past <HH:MM> IST and flat …`, which is the ONLY place the configured service window appears. ⛔ **NOT greppable — do not instruct anyone to look:** the service window **at boot** (the guard runs `:1712`, config loads `:1743`, so it structurally cannot print it) and **`shadow_tracker.enabled`** (never logged; the only proof it took effect is `select count(*) from innings` staying at **322**). ⭐ **RULE: an operator instruction that says "grep X" must be VERIFIED against a real log before it ships — a check that silently finds nothing is WORSE than no check, because "no output" reads as "it failed".**
> 🕯️🚢 **25-Jul-2026 (BATCH 3: M-A2 SHIPPED + E1 + TICK→CANDLE DORMANCY) — THE HELD BRANCH IS DEPLOYED, AND CHASING THE DORMANCY FOUND A CONSUMER THAT FABRICATES DATA.** **DEPLOYED off-market `4a0c0b2`** (M-A2 `afefccb` + E1 `4a0c0b2`; PC == origin == VM bare, all 5 changed runtime files sha256-identical bare BLOB vs deployed file; **crontab byte-IDENTICAL** `892c9d0c…` 160 lines; **service NOT restarted** — `inactive`, `NRestarts=0`, start still Fri 24-Jul 08:15:27; live DBs unchanged). **Loads Mon 27-Jul 08:15.** **F2 gate OVERRIDDEN by written authorisation for these TWO items only — it returns to normal for future batches.** Gate: BASE `9ee8892` 32F/5134P vs MERGE 32F/**5152P**, failure sets **byte-identical, merge-only EMPTY, base-only EMPTY**, **+18 passed = exactly the 13 M-A2 + 5 E1 new tests** (anti-vacuity: collected, not skipped). **A4 VERIFIED CONFIGURED not defaulted** — `load_all` returns `30.0` from `system_config.yaml:265`, and a test pins the yaml key. **⭐ A5 — RAMA'S CALL, MEASURED: 30 s BARELY BINDS TODAY.** Ladder = (3+1)×5 + 3×2 = **26 s per chat**, 1 channel enabled; the deadline is computed ONCE (`:560`) and **SHARED across channels**, so its real effect today is bounding the previously-**unbounded** rate-limiter wait. **If `TELEGRAM_CHANNEL_SECONDARY` is ever enabled the ladder becomes 52 s and 30 s binds HARD, cutting channel 2 off mid-ladder — a behaviour change that would arrive silently with a config flip.** **My read: the order path wants ~8-10 s** (the alert fires BEFORE placement with capital reserved and the FIX-067/M-S1 re-anchor going stale; TG5 writes the CRITICAL sentinel BEFORE any HTTP so a deadline costs no alert; and `http_timeout = min(timeout, remaining)` makes a tighter budget degrade gracefully, not hard-cut). **NOT CHANGED — 30 s is what shipped.** **E1 is a PRODUCTION NO-OP proven against the LIVE crontab line** (`5 16 * * 1-5 … -m reports.daily_report`, no `--config-dir`) ⇒ Monday's 16:05 byte-identical. **⭐⭐ §C — THE TICK→CANDLE PATH: NEVER WIRED, not removed and not gated off.** `git log -S "subscribe([" --all` = 3 commits, all accounted for (the definition, BL-11's `_on_connect` re-subscribe, FIX-061's exit-retry caller); **MEASURED: `connected to KiteTicker` = 32, `exit_retry_subscribed_to_ltp` = 0, `re-subscribing to N tokens` = 0 — the socket has connected 32 times and NEVER carried one token.** ⛔ `config_loader.py:1259` reads like a global switch at grep level but is scoped to `StructureExitConfig` — premise checked and REJECTED. **⭐ THE REAL DAMAGE IS `ShadowTracker` (`enabled: true`), which FABRICATES rather than omits: 141 simulated innings, `exit_reason` SL=0/TGT=0/EOD=130/NULL=11, `exit_price == entry_price` on 130/130 with avg `pnl_pct` 0.0** (real innings 1/181, 180 non-zero), `inning_number` **{2:141} — zero inning-3 EVER** (the cascade needs SL/TGT). A silent no-op is obvious; this looks alive and answers nothing. **⚠️ AND IT DISARMS THREE WATCHDOGS** — the H-9 starvation guard is structurally blind to *never-started* (both its branches need a candle), the tick-age watchdog never leaves its pre-arm loop, and **FIX-029's consumer-thread death detector sits INSIDE that never-entered loop** ⇒ has never run in production. `system_security_audit_02jul2026.md:187`'s resilience claim is vacuous on all three legs. **⭐ NOT the same root as the trail never firing — INDEPENDENT and each individually sufficient** (Cause A: nothing is ever *registered*, `order_placer.py:949` sets protocol unconditionally, `smart_tgt_state`=0 rows; Cause B: no candle *arrives*) ⇒ **"wire the trail" is a THREE-fix job** (BreakevenManager never constructed) — this does NOT reopen the exits thread, it corrects its **cost estimate**. **§C4:** the 15:40 historical fetch is adequate for ANALYSIS (true delta volume) and **cannot** cover the live path ⇒ `_persist_candle` is redundant, the live consumers are not. **§C5:** need is **~5-20 tokens, not the 2,228** in `token_map`; **use MODE_LTP — MODE_FULL would ACTIVATE M-D1's corruption.** **§D:** D2 **CLOSED as superseded** by `5c70def` (one classifier + structured frozenset; production has only `KILL_AUTO_CLEARED`, 23 rows) · D1 **REPORTED not built** — `get_quote` IS instrumented (INFO `duration_ms`); the gap is **aggregation**, and it sits on the pre-placement critical path. Reports `tick_candle_dormancy_25jul2026.md` + `d1_quote_observability_d2_classification_25jul2026.md`.
> ⏱️🔬 **25-Jul-2026 (M-K3 / M-A2 / M-S3 / M-D1) — THREE OF THE FOUR TURNED OUT TO BE THE OPPOSITE OF THEIR FILING, AND THE ONE THAT IS REAL IS HELD AT THE PUSH GATE. DEPLOYED off-market: M-S3 + E2 + docs; no schema, no config, no cron; service NOT restarted (loads Mon 08:15).** **⭐ M-A2 IS REAL AND ON THE ORDER PATH — `signals/signal_processor.py:1130` says so itself ("Telegram alert: INTRADAY SIGNAL (fires before order placement)"), on all three entry paths (`:1131`/`:1901`/`:2196`) with the capital reservation ALREADY HELD** ⇒ a slow endpoint delays every entry and staleness-defeats the FIX-067/M-S1 fresh re-anchor. Its "no timeout" premise is HALF WRONG (`requests.post` has 5 s); what is UNBOUNDED is `_SlidingWindowRateLimiter.acquire()`, and the ladder is **26 s PER CHAT** (4×5 s + 3×2 s, shipped config, 1 channel). ⚠️ **the CRITICAL email fallback is INERT — the VM `.env` has no `ALERT_EMAIL_USER/_PASSWORD/_TO`, so `_send_email_fallback` returns before any socket op; the alert_watcher sentinel IS the backup.** FIX BUILT (one wall-clock deadline per `send()`, shared across channels, covering rate-limit wait + HTTP + backoffs; timeout NOT async, because M-O9) — **but ⛔ HELD: it FAILS the F2 import-graph gate** (`daily_report.py → telegram_notifier`; `forward_shadow_record.py → cron_heartbeat → cron_alerts → telegram_notifier`, and `→ config_loader`). Branch **`hold-ma2-send-deadline-25jul`** @ `9625b7d`, 13/13 RED-first, 188/188 blast radius; **ship = one cherry-pick.** ✅ **SHIPPED 25-Jul `afefccb` — gate OVERRIDDEN by written authorisation (branch deleted); see the 25-Jul batch-3 banner above.** **⭐ M-S3 IS INERT — MEASURED:** 287,600 recorded step latencies (`screener_results.latencies`, 12-Jun→24-Jul) put the slowest step EVER at **66.06 ms against a 5,000 ms timeout**, 0 rows ≥1,000 ms, 0 timeout signatures; the ten step bodies are pure arithmetic (no I/O/lock/DB). The defect is nonetheless REAL as code: `future.result(timeout=)` abandons the WAIT not the WORK, so with FIX-100's shared 1-worker pool **one hung step turns all ten steps into TIMEOUT and poisons every later signal forever** (the RED run showed exactly that). Fixed by ROTATING the poisoned pool; ⛔ `future.cancel()` is a placebo here, and DELETING the pool (arguably correct, would also make `latencies_ms` true step time) is a Rama design call — recorded, not acted. **⭐ M-D1: PB-01 IS CLEAN — the `confirm_volume_mult` gate reads `Candle.from_kite()` (historical API = TRUE DELTA volume), NOT the tick candle; Monday's expectations do not change.** Same for S&R and `daily_stats.avg_volume_20d`. The tick path is inert THREE times over: MODE_LTP carries no `volume_traded` (`main.py:2543` → 0) · **`live_feed.subscribe()` has exactly ONE production caller (`order_placer.py:3393`, the exit-retry path) so nothing is normally subscribed** · and all **275,129** production `analytics.candles` rows are `fetch_daily_candles`' space-`ts` format — **ZERO** rows from `main.py::_persist_candle`, ever. Measured DELTA from the data, not the field name (40/40 (symbol,date) groups non-monotonic). ⚠️ **CHASED 25-Jul batch 3 — RESOLVED, see the banner above: the whole live tick→candle path is DORMANT** — no subscription ⇒ no candles ⇒ every `register_on_candle_close` consumer (`smart_tgt`, `StructureExitManager`) is push-driven by an event that never arrives; independently corroborates the 24-Jul "trail never fired". **M-K3: ZERO latent FK violations** (0 rows / 14 FK tables / 92,318 rows; anti-vacuity proven by planting one) and no FK-off connection writes to ANY FK-carrying table ⇒ enabling would be a no-op; its `synchronous=FULL` half does not reproduce (default is already FULL on VM and PC). E1 HELD (fix site is inside the gated 16:05 report; and the cron never passes `--config-dir`, so it is a prod no-op). ✅ **SHIPPED 25-Jul `4a0c0b2`, same override.** Reports `mk3_fk_enforcement_25jul2026.md` + `ma2_ms3_md1_25jul2026.md`.
> 👁️✅ **25-Jul-2026 (SILENT-FAILURE HEARTBEATS) — TWO EXIT PATHS THAT PRODUCED NOTHING AND SAID NOTHING NOW SPEAK. DEPLOYED off-market; no schema, no config, no cron change; trading service NOT restarted (loads Mon 08:15).** **(a) PB-01 capture (`signals/webhook_receiver.py::_handle_eod`, live Monday):** one `cron_heartbeat("pb01_capture")` per EOD alert — `SUCCESS` / `SUCCESS`+`EMPTY_NO_DATA` at zero / **`FAILED`+`DISABLED`** on the `_eod_capture is None` boot-wiring branch, which until now answered 200 and looked exactly like "no breakouts". ⚠️ **IT RECORDS `queued=`, NOT `captured=`** — `submit()` is async (WR1), so the row proves the alert ARRIVED and was ACCEPTED, **never** that a `pb01_watchlist` row exists; the watchlist row count remains the only proof the worker finished. ⛔ **Signal ingress: the heartbeat is swallowed twice (`record_heartbeat` returns False rather than raising, plus a call-site `except`) — do NOT "clean that up"**; two tests post an alert whose heartbeat RAISES and assert a byte-identical 200. The EOD early return at `:533` (BEFORE the kill + entry-window gates — the only reason a 17:00 capture is possible) is UNTOUCHED and pinned by a test. Plus the missing **`DISABLED` boot line** (`main.py:3171` else): config-off logged NOTHING, so every boot now says which of `ENABLED` / `DISABLED` / wiring-failed `ERROR` it was. **(b) forward-shadow empty day (`scripts/forward_shadow_record.py`):** the `if not rows:` exit records `SUCCESS`+`EMPTY_NO_DATA` (execution status deliberately NOT downgraded). ⭐ **⇒ FROM 25-Jul A MISSING `forward_shadow_record` HEARTBEAT ON A TRADING DAY MEANS DEAD, NOT "a quiet day"** — all four exits now emit. ⛔ **The recorder was NEVER run to test it** (only OOS evidence producer; a run cannot be un-written) — tests stub `StateStore`/`OUT_PATH`/`record_heartbeat` and assert the real JSONL's mtime is unchanged AND that the only writable path was inside tmp. RED-first in a **disposable git worktree at `69ba15a`** (nothing planted in the real tree): 7/12 red on HEAD + 5 planted defects for the guard tests. Stubs are `create_autospec(record_heartbeat)` — a bare spy was caught by this repo's own `test_cron_heartbeat_contract.py` discovery guard. Report `silent_failure_gaps_2026-07-25.md` §SHIPPED.
> 📝 **Same batch, no behaviour:** the D2 daily-loss label now says the live reason `get_daily_realized_net_pnl` is excluded is the **EOD `RESET_PNL` counter-entry**, NOT W10 (fixed 17-Jul) — 4 sites + the test pin UPDATED, not loosened. Decision **#09 prune retention CLOSED = Option A** (leave as-is, no code). **§C `trade_review.txt` retirement STOPPED — no such file exists**; the closest match `data_store/candles/candle_data_*.csv` is a **live SOURCE** for the 16:05 report and is blocked by X3 (`trade_review_txt_retirement_STOP_25jul2026.md`).

> ⛔🔧🔝 **22-Jul-2026 (M-O5) — EOD `fire_now` NO LONGER LEAVES THE 15:17 SQUAREOFF STUCK ON FAILURE. DEPLOYED off-market (schema v44, no migration); service NOT restarted (EOD-path change, loads next boot).** `orders/eod_squareoff.py::fire_now` set `_fired_for_date[today]=True` before `_fire` with **no reset on failure**; `check_and_fire` (the 15:17 backstop) reads the SAME flag and skips if set ⇒ a `fire_now` (the AUTOMATIC daily-loss-breach callback `main.py:780`, its ONLY caller — no operator trigger) whose `_fire` **raised or returned with positions/cancels un-squared silently disabled the scheduled squareoff**. FIX mirrors `check_and_fire:223-229` (reset-on-exception + re-raise) + **Rider 1** (also reset when `positions_failed>0 or cancels_failed>0`); flag STAYS set on full success. Safe: the E.5 broker-position filter re-queries broker truth so a retry cannot double-square. Tests RED-first (raise · partial-fail · `reset_daily_pnl`-twice) + a `check_and_fire` reset-pin; full `test_eod_squareoff.py` **68✓**; regression `comm-23` **EMPTY both ways** (11==11 pre-existing, same env). Reachability LOW (0 breaches/30d, raise-resistant `_fire`) but live. Reports `mo5_investigation_22jul2026.md` + `mo5_deploy_22jul2026.md`; memory `mo5-deployed-22jul`.
> **⭐ B1 — RESET_PNL VERIFICATION (recorded where it will be found):** a Rider-1 retry runs `reset_daily_pnl` (`fund_manager.py:1617`, `entry_type='RESET_PNL'`, `pnl_delta=-old_pnl`) **twice in one day** — the 2nd row carries `-(net at 15:17)`, **NOT zero**. Verify the day's reset by **`SUM(pnl_delta)` over the RESET_PNL rows, NEVER a single-row read**. Sibling of the E4/W10 `pnl_delta`-is-NET contract (below).

> **⭐ F1 — ANY HALTED DAY FORFEITS THE DAILY-LOSS RESET (recorded 24-Jul; closes F1):** `reset_daily_pnl()` (writes the `RESET_PNL` ledger row) has **exactly one** production caller — `orders/eod_squareoff.py:525`, inside `_fire()`, **in-process**. So a day that ends **HALTED** (a SOFT_KILL/HARD_KILL persisting past 15:17, or a boot that never reaches `_fire`) writes **no `RESET_PNL` row** — the day's realized `Σpnl_delta` is never zeroed in-ledger. Harmless in isolation (the next 08:15 boot re-floors the day), but the next person to see a *missing* `RESET_PNL` row will investigate from scratch unless they read this. It is a design item (F1/F2), **NOT a defect**.
> **⭐ B2/B3 — NO NATURAL PRODUCTION-VERIFICATION DAY:** 0 daily-loss breaches in 30 d + a raise-resistant `_fire` ⇒ nothing will confirm M-O5 at a future 15:17; **the RED-first tests are the entire evidence base.** An 08:15 boot does NOT exercise an EOD-path change.

> 🔑🧾🔝 **19-Jul-2026 (baseline-recon — latest) — ARTIFACT BASELINE RECONCILED. READ-ONLY + docs; NO rec; no cron changed.** The live DB moved `6df0c09a…`(11:14)→`a7a1de53…`(18:00) between batches — **BENIGN:** sole writer `gemini_weekly_patterns` (`cron 0 18 * * 0`, monitored:true) = **ONE `cron_heartbeat` row** (id 2421 @ 18:00:17); table diff vs the 11:12 census snapshot = **only `cron_heartbeat` +5**, sensitive tables IDENTICAL (signals 32928/trades 361/orders 610/fm_ledger 2160/ks 1); no `zerodha_token.json`; service inactive; JSONL 7,827@16-Jul (no contamination). **NEW BASELINE (Mon ref):** `a7a1de53…`@18:00 / `f7c964fd…` 7,827@16-Jul / `bb229f44…`@02:30. **⭐⭐ RULE: within-batch before==after ≠ cross-batch unchanged — a moved baseline must be flagged + reconciled.** Live DB advances on monitored-cron heartbeats + the Sun `db_retention --vacuum` even while DOWN ⇒ Monday's post-session check = **row-level, not hash**. "Attribution gloss" class named; sweep queued after Monday. Report `docs/audit/artifact_baseline_reconciliation_19jul2026.md`; memory `artifact-baseline-reconciliation-19jul`.

> 🧹📉🔝 **19-Jul-2026 (masking-sweep) — MASKING-PREMISE SWEEP + REACHABILITY RE-CHECK. READ-ONLY + docs; NO rec.** 3 artifacts byte-identical before==after (live DB `a7a1de53…`; JSONL `f7c964fd…`; analytics `bb229f44…`). The refuted **"a size multiplier is masked by the concentration cap"** (perf_weight = a **post-cap multiplier**, changes final qty **233/298**) was a half-corrected record — **SWEPT from 5 docs** (`00_INDEX` coupling · `06` body ×3 · triage coupling · `decision_packages` · `e4_w10`) with dated legible notes. **§B: NO reachability verdict moved** — the 4 sizing-guard UNREACHABLE verdicts (Q9 matrix rows 10/13/17/18) rest on their own algebra/config/pinning; **batch-4 was right, `06` misattributed the claim to it.** **#06 gate corrected: merit(D2/D3)+leverage, NOT the cap.** **⭐ MEMORY-PALACE SNAPSHOT taken** (`memory_snapshots/memory_snapshot_2026-07-19_201338/`, 505 files) ⇒ compaction precondition MET; **NOT compacted (after Monday).** Report `docs/audit/masking_premise_sweep_19jul2026.md`; memory `masking-premise-sweep-19jul`.

> 📉🧮🔝 **19-Jul-2026 (candles/perfalloc) — CANDLE RETENTION (D4) = SURVIVES; PERFORMANCEALLOCATOR (#06) = the ONE "COMPUTABLE NOW" label that HELD. READ-ONLY; docs-only; NO recommendation.** All 3 artifacts byte-identical **before==after my session**; live DB now **`a7a1de53…`@18:00** — the resume's `6df0c09a…`@11:14 was advanced by the `gemini_weekly_patterns` cron_heartbeat (id 2421), NOT by me ⇒ **the live DB hash advances on heartbeat writes even while the system is DOWN; prove-untouched = before==after within a session, not equality to a prior hash.** **A:** `candles` pruned at **90d** (`db_retention.py:63-70`, runs daily, governs `analytics.candles` via `state_store.py:247`; anti-vacuity PASSED — Sunday VACUUM completed ⇒ DELETE ran, 0 deleted, earliest candle 19-Jun); seed 13-16 Jul expires ~12-15 Oct vs D4 sample matures ~mid-Sep ⇒ **D4 is POWER-bound, not data-bound.** **B:** breakdown persisted v34 (**298/361**); "masked by concentration" **REFUTED** — perf_weight is a **post-cap multiplier** (`position_sizer.py:446/503/506`), changes final qty on **233/298 (78%)** over [0.5,2.0], **151/298** over [0.8,1.25]; conditional on unlevered sizing. Report `docs/audit/candle_retention_and_perfallocator_feasibility_19jul2026.md`; memory `candle-retention-perfallocator-feasibility-19jul`.

> 📉🔬🔝 **19-Jul-2026 (forward-shadow) — FORWARD-SHADOW CAPACITY + D4 FEASIBILITY. DOCS-ONLY; READ-ONLY; NO recommendation.** PC == origin == VM bare (this batch's docs commit); **both artifacts proven untouched** (live DB `6df0c09a…`; forward-shadow JSONL 7,827 lines @ 16-Jul; analytics.db `bb229f44…` before==after); recorder NOT run. Report `docs/audit/forward_shadow_capacity_and_d4_feasibility_19jul2026.md`; memory `forward-shadow-capacity-d4-feasibility-19jul`.
> **⭐ THE FORWARD SHADOW WILL KEEP APPENDING** (it is now the sole confirmation path for D3/D4/#10/D1): cron `15 18 * * 1-5` → `scripts/forward_shadow_record.py` (`cron_registry.yaml:346-364`, `monitored:true`), **fail-LOUD** (a crash raises a Telegram sentinel + done-mark `$rc`), **idempotent per (date, signal_id)**, 72G free. **Monday = OOS day 4** (if the system trades + token present). ⚠️ **Contamination vector: a `--date <not-present>` backfill is NOT guarded ⇒ never run the recorder manually** (its header says mutation voids the evidence).
> **⭐ D4 IS PARTIALLY TESTABLE — MORE THAN D3 — but the binding constraint flipped DATA → POWER.** Facts REPLICATE (verified): no exit engine ever fired (`sl_trail_count>0` on **0/361**; the record's "0/134" was a subset), `order_protocol` LIMIT_TRIPLE 361/361 (dead config). The 1-min candles for 13-16 Jul **survived in `analytics.db`** (NOT in the DB snapshot — a preservation gap) + MFE/MAE in `trade_excursions` (109) ⇒ paths reconstructable read-only. **BUT only 19 OOS entered / 9 winners** ⇒ the **"~2.93R winners' MFE" claim does NOT reproduce OOS** (uncapped median **1.44R**, unstable — one in-window day swings the mean 1.75→3.63R) ⇒ **"cannot distinguish," not a refutation**; `trade_excursions` MFE (winners 1.56R) is **capped by the current exit**.
> **⭐ STANDING CONSTRAINT: the 13-Jul studies' per-signal basis was NOT persisted** (band study + exits study) — only aggregate tables + 1-min candles from 13-Jul on. Every re-examination of that era must rebuild from candles (13-16 Jul only) or wait for forward-shadow days. **⭐ CAPACITY:** D4 needs **~17-34 trading days**; #10 answered negatively so far (rho +0.003 + D3 non-replication); **cross-regime needs a regime change, not a number of days.** **⭐ TRIAGE RELIABILITY: 2 of 3 "COMPUTABLE NOW" proved otherwise** (D3 data gone; D4 underpowered) ⇒ treat PerformanceAllocator as UNVERIFIED until its data + power are checked.

> 📉🔬🔝 **19-Jul-2026 (d3) — D3 BAND INVERSION: it FAILED its first genuine out-of-sample test. DOCS-ONLY; READ-ONLY; NO recommendation.** PC == origin == VM bare (this batch's docs commit); live DB untouched (`6df0c09a…`); forward-shadow JSONL read-only, no scripts run. Report `docs/audit/d3_band_inversion_robustness_19jul2026.md`; memory `d3-band-inversion-robustness-19jul`.
> **⭐ THE LIVE GATE (from code):** `config/scoring_weights.yaml:22 min_pass_score: 60` — single source, `quality_scorer.py:119` gates; tiers med=65/high=80 ⇒ 60-65 = LOW; all 16 strategies `min_score:0`. ⚠️ `system_config.yaml:423 min_pass_score:60` is the SEPARATE **V3-chain shadow** seed (non-gating) — do NOT cite it as the live threshold.
> **⭐ THE FORWARD SHADOW = the confirmation path for every scoring decision (inventory it, don't assume):** a JSONL `data_store/v3/forward_shadow_fs-v1.jsonl` (7,827 records, fs-v1; per signal old_score/band, ms4_score, decision, true-path `sim_R`, realized_pnl). **Genuine OOS = 07-14/15/16 = 3 days, 4,360 signals** (07-13 = the last discovery day = overlap). Stopped 16-Jul (system down); grows only when live. It IS recording correctly.
> **⭐ RESULT — separate the robust half from the actionable one:** discovery (06-19…07-13, ONE regime, stratified 150/band) said 60-65 worst (31%) / 50-54 best (61%). OUT-OF-SAMPLE: the **bottom filter (0-34 junk) REPLICATES** (0-34 win 20.3%, R −0.47), but the **60-65 inversion does NOT** — 60-65 is best-of-band (win 46.3%, only positive meanR +0.085), the sharp contrast **reverses** (50-54 vs 60-65 z=−2.72 vs +5.22), pooled = **noise** (permutation p=0.27, control sound), roughly powered (3 days ≈ enough). In-window day 07-13 is **day-dominated** (0-34 flips to 75%). Natural dist = **73% 55-59, ~4% 60-65** (discovery stratified). **⚠️ 3 OOS days = one regime, as the discovery was one regime ⇒ NOT refuted, but no longer OOS-supported. D3 UNRESOLVED.**
> **⭐ TRIAGE: D3 COMPUTABLE NOW → NEEDS THE SYSTEM RUNNING** — the in-window re-slice is NOT reproducible read-only (regenerating writes the immutable OOS artifact); genuine confirmation = more forward-shadow days. D3 gates D1 / #10 (throttle) / #08 (FREEZE) ⇒ all inherit "unresolved." **📌 CAPITAL VOCABULARY (new PERMANENT doc rule):** ACTUAL CAPITAL — ⛔ **NOT a constant; do not pin a number here.** It is the latest `fm_ledger` INIT row, re-read: `SELECT amount FROM fm_ledger WHERE entry_type='INIT' ORDER BY date DESC LIMIT 1`, or the boot's `fund_manager.rehydrate_complete → total`. It moved every single day 19-Jul→28-Jul (9,875.60 → 9,872.30); order of magnitude ~Rs 9.8k, never a value to compute against. Split 70/30 MIS/CNC · BUYING POWER ×5 MIS/×1 CNC · RISK CAPITAL · POSITION EXPOSURE; **the system is UNAWARE of leverage (sizes unlevered).** Always label which. [[capital-vocabulary]] See `docs/audit/capital_figure_sweep_28jul2026.md`.

> 💰🔢🔝 **19-Jul-2026 (e4/w10) — E4/W10 OUTCOME-IMPACT: the cost double-count would have changed N=0 of 23 daily-loss outcomes. DOCS-ONLY; NO recommendation.** PC == origin == VM bare (this batch's docs commit); live DB untouched (`6df0c09a…`). Report `docs/audit/e4_w10_outcome_impact_19jul2026.md`; memory `e4-w10-outcome-impact-19jul`.
> **⭐ THE PRODUCTION DAILY-LOSS THRESHOLD, CORRECTED** (decision file 01 **and** the `dual-daily-loss-mechanism` memory carried FIXTURE values): **ONE limit `daily_loss_limit_pct = 0.03`** (`system_config.yaml:193`, SOLE authority; the absolute `capital.daily_loss_limit` was **DELETED 24-Jun** `:131-133`); both enforcement points read the same key (`main.py:2231` FundManager / `:2369` RiskEngine) = **3% × current capital**, realized-only. **Capital = broker margin ≈ Rs 10,000** (`fund_manager.py:440`; the Rs 1,000,000 12-Jun INIT is the pre-sync fixture seed) ⇒ **threshold ≈ Rs 300/day, NOT Rs 25,000.** No absolute, no second key ⇒ **batch 2's single-source conclusion CONFIRMED**; the "5%/25k/10k" were fixture literals.
> **⭐ N = 0, on the corrected threshold.** Deployed reader = **Option B** (Σpnl_delta − Σcosts), proven — every `RESET_PNL` row = −net_B on all 22 reset days. Worst day **07-07: intraday cum_B −56.47 vs −Rs 300 = Rs 243.5 margin** (worst day = 19% of the limit). **Structural (~5.3× headroom) but NOT robust to D1** — a ~5× sizing rise brings a day to the threshold, where it breaches under BOTH readings (the double-count is then moot). 36 pre-fix `costs=0` gross rows make Option B *less* divergent ⇒ cannot manufacture a straddle. **Posture call stays Rama's — now against a number.**
> **⭐ §C TRIAGE of the 10 decisions:** COMPUTABLE NOW = **D3** (out-of-sample band-inversion), **D4** (out-of-sample exits), **PerformanceAllocator** (reachability algebra vs the 100%-binding conc cap) · NEEDS RUNNING = D1, Regime · NEEDS RAMA = E4/W10 (posture), Freeze-min_pass, Prune-retention · BLOCKED = D2 + Regime (Kite token), Throttle (on D3). **LESSON: a settlement that computes against a threshold must read that threshold from CODE first — E4/W10's was assumed and wrong by ~50×; D1/D3 carry the same shape.**

> 🗳️📋🔝 **19-Jul-2026 (decisions) — DECISION PACKAGES + MEMORY-COMPACTION VERIFICATION. DOCS-ONLY; NO recommendations anywhere.** PC == origin == VM bare (this batch's docs commit); live DB PROVABLY untouched (`6df0c09a…` before/after). Report `docs/audit/decision_packages_19jul2026.md`; **the board = `docs/decisions/00_INDEX.md`**; memory `decision-packages-19jul`.
> **⭐ `docs/decisions/` — one file per open choice**, each = CHOICE (options) · KNOWN (corrected evidence + citations) · UNKNOWN (knowable-from-data / only-by-running / not-knowable) · EXPOSURE each way (₹ where the record supports) · WHAT WOULD SETTLE IT (+cost). **No recommendation, no ordering by preference.** 01 E4/W10 · 02 D1 concentration · 03 D2 mix · 04 D3 min_pass · 05 D4 exits · 06 PerformanceAllocator · 07 Regime · 08 Freeze min_pass · 09 Prune-retention (new) · 10 Throttle admission (new). **Corrected values carried in:** D1 = Rs990 direction **LONG 10.00% vs SHORT 10.72%** (was "4.5× harder"); D2 = **91% LONG** (was "58% short").
> **⭐ §A — the 8-pass MEMORY.md compaction (19.9→17.1KB) was LOSSLESS.** Every removal was (i) duplicative of a still-linked topic or (ii) a completed item; **0 category-(iii)** (existed-only-there); the two scary candidates ("alert-watcher soak", "live-test cert") both survive in topic files/reports; **every open board item confirmed present.** Nothing restored because nothing needed it. *A compaction CAN silently drop a board item — this was checked, not asserted.*
> **⭐ THROTTLE = decision #10, counter-case attached at equal weight.** Ranked admission only helps if the score ranks, and it doesn't — **M-S4 rho +0.003 (OUTCOME C); the 60–65 band the throttle sees is the WORST at 4–5σ** ⇒ FCFS discards no edge, and ranking by the current score ranks noise (could prefer the worst signals). Stands regardless: ~23 approved/day discarded, 87% of orders in hour 1, gate category **free-text only ⇒ invisible in reports.**
> **⭐ FREE-TEXT CLASSIFICATION IS NOW A CLASS OF FOUR** (the 3,098-misclassification shape): (1) sizing concentration-vs-capital `daily_report.py:464` FIXED · (2) `REJECTED_KILL_SWITCH` stage (`signal_processor.py:685` vs `risk_engine.py:405`) · (3) `is_sizing_rejection()`/`REJECTED_SIZING_VALID` · (4) throttle gate category. Related but distinct: **W9** (reason never persisted at all). All reported, not repaired.

> 📊🔎⚖️🔝 **19-Jul-2026 (throttle/record) — THROTTLE SELECTION + RECORD CORRECTION: does the traded book represent the approved book?** READ-ONLY; **docs-only**; every query on the `mode=ro` preserved snapshot; **live DB PROVABLY untouched** (sha256 `6df0c09a…` + mtime `11:14:29` identical before/after). Report `docs/audit/throttle_selection_and_record_correction_19jul2026.md`; memory `throttle_selection_record_correction_19jul`. Completes the census's open §B and sweeps §C.
> **⭐ VERDICT: the traded book is TIME-selected, not QUALITY-selected — a domain qualification on every expectancy number, NOT an invalidation.** After every gate says yes, **64% of approved signals (139/217) are throttled** and a further **30 of 78 orders never enter**. The throttle — `signals/entry_throttle.py` @ `signal_processor.py:1170`, **AFTER** `risk_engine.approve()` `:1075` — is a **pure in-memory temporal rate limiter** (20s global `min_gap` / 3-per-60s burst / 300s per-symbol), **NOT** `max_open_positions`. **132/139 throttles = the global 20s `min_gap`**, which is causally BLIND to strategy/score/direction. Dimensions: **score 60.09 ordered vs 59.878 throttled (0.21pt, non-monotone ⇒ not quality)** · direction LONG 36.2% vs SHORT 33.3% (flat) · **the strategy spread 21–72% is a CONFOUND with arrival density, not an effect** · **THE driver = arrival time: ordered% 12.8% at 10:00 → ~100% by 10:15+; throttling is 100% market-open** (hour 10: 139 throttled; hours 11–14: 0).
> **⭐ FILL STEP (78→48): three EXPECTED mechanisms, ZERO defects** ⇒ the "report-and-stop" gate was NOT tripped. **17** PROCESSED→FAILED = unfilled LIMIT entries auto-cancelled uniformly ~60s later (`fill_timeout_sec`; `order_placer.entry_cancelled_zero_fill:1919`) · **6** = broker RMS "MIS blocked for KOTIC/UNICHEMLAB/BIRLAMONEY" (0 orders) · **7** = the system's own `slippage_exceeded` guard (0 orders). Only the 17 unfilled LIMITs matter for §B5 (mechanical selection against fast-moving entries; counterfactual P&L unmeasurable). **Consequence: expectancy figures (38.8% win, R-multiples, cost-dominated net) describe "approved signals that cleared a 20s opening-minute rate limiter AND filled a resting LIMIT within ~60s" — same shape as §B1's batch-4 domain caveat, one layer down. D1/D2/D3 remain Rama's; expectancy NOT re-run.**
> **⭐ RECORD CORRECTED (swept, dated notes):** (a) ">Rs990 hits LONGs 4.5×" **DIRECTION INVERTED by rate** (LONG 10.00% vs SHORT 10.72%; the 4.5×/9.54× are COUNT ratios = the 10.2× long-volume skew) — **sits in D1's evidence, flagged** [`sizing_interaction_impact_report` §4 · `q9_batch4_sizing_floors_caps` · `expectancy_autopsy` + memory `q9_batch4_sizing_reachability`] · (b) "~249/day silent KeyError" **already CLOSED everywhere** (fixed 14-Jul `c22a25c`; never silent; same killer) — **no open occurrence remained** · (c) the 07-08 quote-cliff **interpretation inverted** (survivorship bias; **RE10 verdict + the no-bypass point STAND**) [`consecutive_losses_gate_wired` · `q9_coverage_matrix_final` + memory]. **C2:** batch-4 + the Q9 matrix now carry the **1.45× >Rs990 population-bias** qualifier (bounds domain, arithmetic unchanged).
> **⭐ CORRECTS THE CENSUS BANNER BELOW — THE DATA IS NOT ERODING.** Retention is **90d**, earliest data 12-Jun ⇒ the daily prune deletes **0 rows** until **~10-Sep** (90d→0 / 30d→0 / 14d→0; only ≤10d windows bite). The 09-Jul boundary was the **one-off 16-Jul manual Phase-B prune**, NOT a sliding window; the census's "keeps sliding" was WRONG. **Snapshot preserved** at `/home/ubuntu/preserved/signal_census_19jul2026/` (89.97MB DB @ sha256 `09a22ade…` + 2 gzipped CSVs, `chmod 444`, outside all managed paths; **6 complete days safe: 09,10,13,14,15,16-Jul**). **📌 DISCLOSURE:** modelling the prune with `python scripts/eod_cleanup.py --db <copy>` wrote **4 truthful `cron_heartbeat` rows** to the LIVE DB (ids 2417–2420) — `_cron_main` heartbeats BEFORE `main()` parses `--db`; **only table changed of 46**; NOT reverted (deleting truthful audit rows is worse). **RULE: `scripts/*.py --db <copy>` isolates only the code BELOW the cron entry point.**

> 📊🔎✅🔝 **19-Jul-2026 (census) — THE SIGNAL MORTALITY CENSUS: how often are the gates consulted at all?** READ-ONLY; **docs-only `9eb7d71`**; PC == origin == VM bare; delta vs tag `deploy-19jul-consecutive-losses` = **markdown ONLY**. No code/config/schema/flag/test touched; **no regression run (nothing executable changed)**. **Live DB PROVABLY untouched** — sha256 `e69fd1b4…` + mtime `09:20:01` identical before/after; every query ran on a `sqlite3 .backup` snapshot opened `mode=ro` (since deleted). Report `docs/audit/signal_mortality_census_19jul2026.md`; memory `signal_mortality_census_19jul`.
> **⭐ THE ANSWER: 5,845 of 30,769 admitted signals REACH THE RISK ENGINE = 19.00%** (6 complete days, ~975 `approve()` calls/day) ⇒ **Q9 was not testing a dead code path.** **Cross-verified to the unit from an independent source** — per-day `risk_engine.approve` log lines sum to **5,845 exactly**. **⚠️ DO NOT QUOTE "3.54% of arrivals"**: 81% of arrivals are *intentional* de-duplication (Chartink POSTs ~1/min/scanner vs a **300s** TTL ⇒ 80% expected, **81.5% observed**) — a true number with a false meaning.
> **⭐ SILENT DROPS = 0.** Every `signals` row maps to a known stage: **0 unmapped, 0 NULL, 0 stuck non-terminal**; **97.60% of deaths are deliberate DECISIONS**, 2.40% data failures. The **134,342** webhook-layer drops have **no row and no log line** (proven by grep over ~500MB of logs; the 294 `DUPLICATE` hits are the **risk engine's** check, not webhook dedup) — **but the COUNT is already reported daily**, tagged **`[W9] PENDING_CAPTURE`** (`daily_trade_review.py:699-710` documents the mechanism; `:897/:910/:1008/:2307` render it) ⇒ **a tracked capture gap, NOT a blind spot.** Retryable failures bounded near-zero by the 503 count (**18** all-window, **0** in the complete era).
> **🔴 NEW MEASUREMENT BOUNDARY — READ BEFORE ANY FUTURE `signals` ANALYSIS.** `scripts/eod_cleanup.py:234` prunes **status-selectively**: `(status IN ('EXPIRED','DUPLICATE') OR status GLOB 'REJECTED*') AND fingerprint_date < ? AND NOT EXISTS(trades)`. ⇒ **ONLY 2026-07-09 → 16-Jul is COMPLETE** (accepted == rows exactly); before it ~1.5% of rows survive and the residue is **86.89% `SKIPPED_QUOTE_UNAVAILABLE` + qualified, ZERO `REJECTED_*`**. **Any share computed over all 32,928 rows is meaningless.** ✅ Corollary: **`SKIPPED_*` counts ARE complete for all 24 days** (the prune never touches them). **Pre-09-Jul rejection history is GONE FOR GOOD** (⚠️ **CORRECTED 19-Jul — the window is NOT sliding:** 90d retention + earliest data 12-Jun ⇒ the daily prune deletes 0 rows until ~10-Sep; the 09-Jul boundary was the one-off 16-Jul manual prune, not the clock — see the throttle/record-correction banner above) — future rejection-composition work needs an unpruned window or the preserved snapshot at `/home/ubuntu/preserved/signal_census_19jul2026/`.
> **⚠️⚠️ THIS CORRECTS THE Q9 BANNER DIRECTLY BELOW (its §B9 reachability line).** "*all 63 signals after 10:22:23 were `SKIPPED_QUOTE_UNAVAILABLE`, dying upstream*" — **the COUNT is right, the INTERPRETATION is INVERTED.** On 07-08 the webhook **accepted 5,212** signals and acceptance **ACCELERATED** after 10:22 (hourly accepted 464 → 676 → 1,179 → 1,431 → **1,462**, the day's busiest hour). The 63 are **1.21% of that day — the 6th-LOWEST quote-failure rate of 21 days** (mean 1.78%; the genuinely bad days were 07-13 **3.57%**, 06-30 **3.46%**, 07-15 **3.01%**, none ever flagged). **The apparent cliff is survivorship bias**: 07-08 sits in the pruned era, so only skips + qualified rows survive. Last surviving non-skip = **10:20:20**, first surviving skip = **12:36:12** ⇒ a **2h16m hole with no row of any kind**, exactly where the deleted rejections lived. **Q9's conclusion that RE10 has never fired STANDS** (it rests on the gate's own recomputation semantics); only this *explanation* for it falls.
> **⭐ ALL THREE INHERITED CLAIMS WERE WRONG IN PART. (2) "~249/day silently-dropped `KeyError` at `secondary_screener.py:167`" — INVERTED and ALREADY FIXED.** It was **never silent**: `raise KeyError` was caught by the `except Exception` **two lines below it in the same function** and logged **ERROR + full traceback** — the noisiest thing in the log. It is **not a separate killer** either: both paths end in `_make_skipped("SKIPPED_QUOTE_UNAVAILABLE")` + `_persist`, so **adding it to claim 1 double-counts the same deaths.** Fixed **14-Jul in `c22a25c`** (logging only). The ~249 was a **log-line** count, not a signal count (measured mortality ≈124/day). **(3) "~Rs 990 / 23.3% / LONGs 4.5x"** — threshold **VERIFIED and exact** (**0.00%** concentration rejections below 990 in all three bands; **42.25%** / **41.01%** above), share **VERIFIED** (**24.02%**), **direction INVERTED** (**LONG 10.00% vs SHORT 10.72%** — the "4.5x" is rate/count confusion driven by the **10.2x** volume skew, 28,027 vs 2,742).
> **⭐ B1 — BATCH 4's REACHABILITY TABLE IS PRICE-BIASED (bounds its domain; does NOT invalidate it).** Share >Rs990 by stage: **admitted 24.02% → sizer 34.73%** (1.45x enrichment) **→ risk engine 0.14%**; mean trigger price Rs 732 → 875 → **441**. Screening *enriches* for expensive names; the concentration cap then deletes the whole band **before the risk engine ever sees it**. Batch 4's verdicts (incl. "only 4 of 15 sizing guards can bind") hold **for the population reaching the sizer**, but must **not** be read as statements about signals in general — a guard needing high notional/share is unreachable *because of an upstream cap*, not its own threshold.
> **🔵 REPORTED, NOT FIXED (it was a census):** the **one genuine blind spot** is the **403 population** — 25,960 POSTs refused before parse ⇒ `signals_accepted`/`signals_rejected` both **0** and the signals inside are **never counted anywhere**; correctly refused (outside entry window) but unmeasured, and **cheaply estimable** (39.32 bytes/signal × 512.7 avg ⇒ ~13/POST ≈ **338,000**) · `REJECTED_KILL_SWITCH` **cannot identify its stage** (pre-gate `signal_processor.py:685` vs risk check 1 `risk_engine.py:405`) · `signal_status.is_sizing_rejection()` would claim `REJECTED_SIZING_VALID`, a **risk-engine** check — both **LATENT, 0 rows** · **the LIVE quote path has NO partial-response detection**, while **paper logs a WARNING naming the missing symbols** (`main.py:546-549`) ⇒ **paper is better instrumented than live**; all 361 trades in the window are `mode=LIVE`, so there is no paper population to compare.
> **📌 RE-CONFIRMED FROM SOURCE:** risk-engine order is `KILL_SWITCH · SIZING_VALID · CAPITAL · OPEN_POSITIONS · DAILY_TRADES · CONSECUTIVE_LOSSES(6) · DAILY_LOSS(7) · SECTOR_EXPOSURE · CONTRARY_POSITION · DUPLICATE_SYMBOL`, and the pipeline runs **sizing BEFORE the risk engine** (`signal_processor.py:901` then `:1075`). **B2 (D2 data, no recommendation):** score gate is the largest single killer (10,949 = 35.58%); **91.1% of admitted signals are LONG** (28,027 vs 2,742) — the skew starts at the **scanner**, matching BK-1's 91%-long book; 3,098 (10.07%) die at a **price ceiling rather than a judgement**.

> 🏁✅⭐🔝 **19-Jul-2026 (earlier) — Q9 IS COMPLETE AT 22/22. Layer 14, the CONSECUTIVE-LOSSES gate (RE10), is WIRED — plus the measuring instrument itself was fixed.** Tag `deploy-19jul-consecutive-losses` → `d271525`; PC == VM; schema v44 unchanged. **TEST-ONLY — `git diff --name-only 1fc95c1..d271525` = 3 files, ALL under `tests/`.** Report `docs/audit/consecutive_losses_gate_wired_19jul2026.md`; memory `consecutive_losses_gate_wired_19jul`.
> **⭐ THERE IS NO COUNTER — the finding that shaped the whole batch.** The streak is **RECOMPUTED on every `approve()`** from `recent_trade_pnls(max_consec+1, today)` + `_count_trailing_losses` (`risk_engine.py:250-251`), never incremented and never held in memory — `core/schema.sql:459-460` confirms the stored column "was never written". Consequences, each PROVEN not described: **(a) it resets on ANY non-loss close** (a breakeven too: RE10 defines a loss as `net_pnl < -1e-6`) **and at the day boundary** (FIX-183, added because a cross-day streak was a **DEADLOCK** — breaking it needs a win and the block prevents one) ⇒ **⚠️ OPERATOR-RELEVANT: the halt is NOT for the rest of the day; ONE winning close lifts it immediately.** **(b) it SURVIVES A RESTART by construction** (same shape as batch 5's daily P&L) ⇒ **a restart is NOT a bypass.** **(c)** it is read at ENTRY but only changes at EXIT, so in-flight positions are not retro-blocked.
> **⭐ REACHABILITY — A CATEGORY OF ITS OWN, and the most interesting result.** Not UNREACHABLE like batch 4's dead guards (algebra), yet not routinely binding: **the precondition has been met on 3 of 21 production trading days** (streaks of **5, 5, 6**) while **`REJECTED_CONSECUTIVE_LOSSES` = 0 across ALL 32,928 signals.** Both halves checked before concluding: **(1) NO BYPASS** — on 2026-07-08 the streak hit 4 at exit **10:22:23** but all six trades had been **ENTERED by 10:14:19**; **(2) why it never fired** — all **63** signals after 10:22:23 were `SKIPPED_QUOTE_UNAVAILABLE`, dying upstream of the risk engine. ⇒ **LIVE, correctly configured, precondition demonstrably met, never yet binding.**
> **⚠️ THE BRIEF'S COLLISION PREMISE WAS INVERTED, and it matters:** RE5 order is `...5 DAILY_TRADES · 6 CONSECUTIVE_LOSSES · 7 DAILY_LOSS`, so the daily-loss **GATE** is checked **AFTER** this one and **cannot mask it at all**. The real hazard is the **post-close breach** (a *different* mechanism, `FundManager(daily_loss_limit_pct=0.02)` = ₹10,000 vs the gate's 5% = ₹25,000) arming a **SOFT KILL** that then fires `KILL_SWITCH` at check 1. The shared `_drive_close` defaults to **−9,000/close**; four would blow through both limits, so this batch drives **−1,000/close** and **asserts** the sizing stays inside both rather than trusting arithmetic.
> **🔧 THE MEASURING INSTRUMENT WAS BENT BY 18% — fixed first, deliberately, so §B could be measured with it.** `test_fix129_ntp_check`'s `drift_sec >= 1.0` / `>= 3.0` were **not flakes but wrong assertions**: `check_ntp_sync` (`utils/startup_checks.py:534-548`) computes `drift = offset − elapsed`, which is **always ≤ the injected offset**. Measured over 100k trials each: the old form failed **17.94%** and **18.32%**; the fixed form **0**. **⭐ That retro-explains the M-C1 batch's discarded regression pair** (base caught one, mine the other — the expected outcome at 18%, not a coincidence). **📌 RULE: an exact comparison on a small delta derived from a LARGE EPOCH is float64-unsafe** — at epoch ~1.784e9 one ULP is 2⁻²² = 2.384e-07. But the ULP only explains the *unsafety*; the **tolerance must be sized by the PHYSICAL term** (elapsed between the two clock reads: max 9.1e-06, ~38× the ULP) and ultimately by the **Windows scheduler quantum (~15.6 ms)** ⇒ **50 ms**, still **20× below** the 1.0 s margin to the nearest threshold, with two permanent guards proving it cannot blur a decision. **🔴 It failed on the VM too — a genuine cross-platform defect, not a PC artefact.** **~~THE BASELINE IS NOW 14, down from 16.~~ ⚠️ CORRECTED 21-Jul (attribution-gloss sweep): there is NO fixed-number test baseline** — it is a property of one environment at one moment (and this very banner contradicts itself at `:106` "moved 12→16"). Judge a regression by same-window BASE-vs-MINE `comm -23`, never a count. See `docs/decisions/RUNBOOK_e4_w10_deploy.md:44` + memory `feedback_no_fixed_test_baseline`.
> **⭐ A THIRD HARNESS HOLE, caught ONLY by the full-suite regression.** The first MINE run carried **one attributable failure** that **passed in isolation, passed under `-k`, and passed across all 103 `tests/integration` tests**. Cause: the fixture default `paper_auto_fill_delay_sec=0.05` makes the paper adapter fill a placed order 50 ms later **asynchronously**, racing the explicit fills these tests publish to control each close's **sign**; under full-suite load the adapter won and the P&L flipped. Every other Q9 batch already sets `60.0`; this module had omitted it. **📌 This is exactly why "STOP on any NEW failure" is the rule — re-running until green would have "worked".** Planting/regression has now found holes in the TESTS rather than in production **in 6 instances across 5 batches** (+ my own parity guard here matching `"live"` inside `"deLIVEry"`, and `_probe_signal_status` settling on `"PASSED"` — the SCREENING verdict, which lands BEFORE the risk engine rules, making any positive assertion on a specific rejection a race).
> **✅ PRE-MONDAY VM CHECK (read-only): NOTHING BLOCKS THE 08:15 BOOT.** All **94** modules `main.py` imports resolve on the deployed tree (0 failures); `reports/signal_status.py` + `daily_report.py` are **NOT on the boot path** (cron-only 16:05) and import cleanly anyway; crontab correct with `15 8 * * 1-5 auto_refresh_token` present and nothing untoward before 08:15; `trading-system.service` **enabled/loaded**, last `ExecMainStatus=0`; token file **absent** as designed; disk 25%. One stale `/tmp/trading-system.lock` was found and was **my own artefact** from running the instance-lock tests on the VM (PID 1491115, dead; no flock holder; port 5001 free ⇒ could not have blocked a boot) — removed.
>
> 🏁💰⭐🔝 **19-Jul-2026 — THE LIVE CAPITAL SEED (M-C1) IS WIRED; Q9 CLOSES.** Tag `deploy-19jul-q9-live-seed` → `abf276a`; PC == VM; schema v44 unchanged. **TEST-ONLY — `git diff --name-only abf276a~1 abf276a` is ONE file under `tests/integration/`, nothing outside `tests/`** ⇒ zero production impact before Monday's boot. Report `docs/audit/live_seed_mc1_wired_18jul2026.md`; final matrix `docs/audit/q9_coverage_matrix_final_18jul2026.md`; memory `q9_live_seed_mc1_wired_19jul`.
> **THE GAP IT CLOSED:** every wired test in Q9 batches 1–5 ran against the `paper_mode=True` fixture while production runs `--mode live`. The parity claim was **structural, not tested**, and it stopped exactly where the modes diverge — the seed. PAPER (`main.py:2243`) = static `paper_capital`; LIVE (`main.py:2255`) = `broker.get_margins().net − today_realized_pnl_carryover()`. The broker's `net` already includes today's realized P&L and rehydrate Phase 2 re-adds it, so **without the subtraction live reservable capital silently INFLATES on a warm restart**. Both sides sum `pnl_delta` from the **shared** `_today_release_used_pnl_rows` helper (call count **2**, asserted), so they cancel to `broker.net` by construction. **Measured: `_total` lands on `broker.net` within 5.8e-11; with the subtraction removed it lands 18,094.54 high — exactly the carryover, counted twice.**
> **⭐ CONTRACT-INDEPENDENT ⇒ SURVIVES E4/W10:** the cancellation is algebraic (`net − Σ + Σ`) whatever `pnl_delta` means, and the daily-loss **reader** — the quantity E4/W10 changes — is ~~*not involved at all*~~ **[⚠️ CORRECTED 20/21-Jul: FALSE as written — `get_daily_realized_net_pnl` IS called on the rehydrate/seed path (`fund_manager.py:1760`, +1303/1531/1627). The reader is absent only from the cancellation ALGEBRA, which is contract-independent because both sides sum the SAME rows via `_today_release_used_pnl_rows` — not because the reader is absent. See `mc1_live_seed_rederivation_20jul2026.md`.]** in the cancellation (measured: reader −18,189.08 vs carryover −18,094.54; the −94.54 difference is exactly `SUM(costs)`).
> **🔴 SAFETY — no live BROKER connection exists anywhere in the tests.** No live-mode fixture was built; the live seed needs exactly one number, so a 12-line local `_StubBroker` supplies it. `TestNoBrokerReachable` pins the whole test tree — constructing `KiteConnect` or reading `ZERODHA_*` fails, while `from kiteconnect import exceptions` (which the adapter tests need) does not. **HONEST SCOPE:** the two-line seed expression is inline in `main()` with no function boundary, so no test can invoke it without running `main()` — it stays under batch 5's structural regex pin; what the tests drive (`today_realized_pnl_carryover` · `_today_release_used_pnl_rows` · `initialize` · `rehydrate_from_open_trades`) is all real.
> **Q9 FINAL TALLY: 21 of 22 layers WIRED; 15 REACHABLE under current production config.** 🔴 **The one NOT closed: layer 14, the consecutive-losses gate (RE10) — still UNIT-ONLY** (its gate ORDER is proven, its enforcing branch has no wired positive; it needs ≥4 consecutive losing closes, which collides with the daily-loss limit and deserves its own design — deliberately not smuggled in). **⚠️ Monday 20-Jul 08:15 exercises only the TRIVIAL case** — a cold boot on a flat book ⇒ carryover = 0, the subtraction is a no-op, rehydrate is a NO-OP. **The cancellation this batch proves will not have run in production**; it first runs on a genuine mid-day restart after ≥1 close.
> **📌 PERMANENT RULE (fifth in the series): A REGRESSION RUN MUST NOT CROSS MIDNIGHT — "same time window" is necessary but NOT sufficient.** The first base/mine pair was **discarded**: MINE ran 23:30–23:56, BASE 23:57–00:13, and the comparison returned **both `comm` directions non-empty**, which is impossible for a test-only file addition (a new test cannot make `test_risk_engine::test_consecutive_losses_at_limit` *pass*). Cause, verified in source: `tests/unit/test_risk_engine.py:49` and `tests/unit/test_phase3_delivery_caps_conditional_capital.py:35` set **`_TODAY = now_ist().date().isoformat()` at module IMPORT (collection) time**, while the engine computes the date at EXECUTION time — so a run that collects on one date and executes on the next reads its own seeded trades as *yesterday's*. At ~15 min/run, **any regression started after ~23:15 IST is unsafe.** Same family as the git-ignored-`instruments.csv` masking rule: an environmental asymmetry that corrupts attribution silently instead of announcing itself. Joins *CONFIGURED IS NOT COVERED*, *WIRED IS NOT REACHABLE*, *LIVE vs LATENT* and *NEVER CLASSIFY BY FREE TEXT*.
> **⚠️ THE REGRESSION BASELINE HAS MOVED 12 → 16, and one half of it is NOT PC-only.** Both runs (Sun 19-Jul 00:15–00:44) showed **16 failed · BASE 5017 / MINE 5030 collected · `comm` both directions EMPTY ⇒ ZERO ATTRIBUTABLE · `xfailed=1, XPASS=0`**. The four new ones are in BASE too: **(a) `test_fix129_ntp_check` ×2 — 🔴 fails ON THE VM as well.** Pure float64 precision: `drift_sec` lands exactly one half-ULP below the bound (`assert 0.9999997615814209 >= 1.0`); at epoch ≈1.78e9 the ULP is ≈4.8e-7, so the low bits of the clock decide it — a coin flip per run (the discarded pair caught one of the pair in base and the *other* in mine). The fetcher is a stub; no network involved. **Wants `pytest.approx`; left for Rama, out of scope for a test-only batch.** **(b) `test_instance_lock` ×2 — PC-only** (all pass on the VM): stale `%TEMP%\trading-system.lock` naming **PID 8708, not running**.
> **⚡ OUTAGE NOTE (18-Jul ~23:00):** the build session died to a power cut after `abf276a` was committed. Two casualties, both repaired **before** any measurement: the test file was **deleted from the working tree** while the git object survived (restored, verified byte-identical), and the venv **lost `pyotp` + `waitress`** (both pinned in `requirements.txt`), which aborted collection for the **entire** suite via `tests/unit/test_gui_secret_key.py` → `ops_dashboard.backend.app`. Reinstalling at pinned versions returned base collection to exactly **5017**, proving the repair restored the declared state rather than altering it. The restored file was **re-proven to bite** before it was trusted.
>
> 📊🔧⚠️🔝 **18-Jul-2026 — DAILY-REPORT CLASSIFICATION FIX DEPLOYED. ⚠️ THE FIRST PRODUCTION CODE CHANGE IN SIX BATCHES** (reporting layer only — **nothing on the trading path**). Tag `deploy-18jul-report-classification` → `b3a960d`; PC == VM == `4d20a6c`; schema v44 unchanged. Report `docs/audit/daily_report_classification_fix_18jul2026.md`; memory `daily_report_classification_fix_18jul`.
> **📌 PERMANENT RULE (fourth in the series): NEVER CLASSIFY, COUNT, FILTER OR GROUP BY FREE TEXT WHEN A STRUCTURED STATUS/ENUM EXISTS.** Rendering the text is correct and expected; **BRANCHING on its contents is the defect.** `daily_report.py:464` did `"CAPITAL" in rejection_reason.upper()` — and the CONCENTRATION reason embeds **`capital_qty=`** — so the row read **"Rejected (Capital)"** for rejections that were **100% CONCENTRATION**, while concentration (which binds on 100% of trades) was named nowhere the operator could see. `:549` grouped by that same string, which embeds the symbol and three arm values ⇒ **190 near-unique breakdown lines for 7,655 rejections instead of 9.** Joins *CONFIGURED IS NOT COVERED*, *WIRED IS NOT REACHABLE* and *LIVE vs LATENT*.
> **THE FIX:** all signal-status classification now runs through **ONE** new module **`reports/signal_status.py`** (`bucket` · `is_rejected` · `has_explicit_disposition` · `is_dedup_duplicate` · `is_sizing_rejection` · `sizing_constraint` · `family`). Rule #4 anti-duplication: `daily_trade_review._signal_bucket` **already had the right pattern**, so it was **PROMOTED into the shared module** and that file now delegates (`_signal_bucket = sig_status.bucket`) — exactly one implementation, asserted by a test. A source guard fails if free-text classification returns to `reports/` (scoped to CODE, not comments).
> **⚠️ PREMISE CORRECTED — do NOT repeat a "3,098 → 0" framing:** the old count was **MISLABELLED, not miscounted**. All 3,098 sizing rejections in the corpus are CONCENTRATION, `REJECTED_SIZING_CAPITAL` = 0, and no non-sizing reason contains "capital" ⇒ the substring selected the right ROWS for the wrong REASON. The fix makes it right **by construction** and **names the binding constraint** for the first time. **The sweep found two MORE of the same class, both genuinely wrong:** `:465` counted only `"REJECTED" in status`, so **2,688** signals carrying `DROPPED_*`/`SKIPPED_*`/`QUEUE_FULL`/`PLACEMENT_FAILED`/`TIMEOUT` were reported as *silently dead*; `:459` conflated the dedup `DUPLICATE` with `REJECTED_DUPLICATE_SYMBOL`.
> **VERIFIED ON REAL DATA (a COPY, never the live DB):** 429 diffs, **all in `0_EOD_Dashboard`**, other six sheets byte-identical, 41 labels (P&L, win rate, drawdown, system health) unchanged. Breakdown **190 → 9 lines**; `Silent Dead` **139 → 0**; `After Dedup/Excluded` **7802 → 7814**. **The report GENERATES on the VM from the deployed code.** **ROLLBACK: `git revert b3a960d` — one commit, one step; this path writes no state.**
> **⚪ RECORDED, NOT FIXED (Rama's call):** `:537` "CRITICAL Count" counts routine `KILL_AUTO_CLEARED` and double-counts with `:540` "Kill Switch Events"; `:536` "ERROR Count" matches no event type. Same pattern, but **ambiguous intent rather than demonstrable misclassification** — not changed on the eve of a live boot. Also `build_taxonomy_map()` (`:1493`) ignores `--config-dir` (pre-existing; harmless under cron).

> ♻️🏁🔝 **18-Jul-2026 — Q9 BATCH 5 DEPLOYED: POST-RESTART CAPITAL RESTORATION IS WIRED. ⭐ Q9 IS COMPLETE** (bar the coverage-matrix metadata back-fill). Tag `deploy-18jul-q9-batch5` → `a9758b3`; PC == VM == `53193e4`; schema v44 unchanged. Report `docs/audit/q9_batch5_post_restart_capital_18jul2026.md`; memory `q9_batch5_post_restart_18jul`. **TEST-ONLY — no production file, no behaviour change.** **No stop-and-report: all four failure modes clean.**
> **THE RESTORE PATH, for the runbook:** `kill_switch.clear_stale_state(today)` (`main.py:1768`) → seed (**PAPER** `paper_capital` `:2243` / **LIVE** `broker.get_margins().net − fund_manager.today_realized_pnl_carryover()` `:2255`, M-C1) → `initialize()` `:2259` (**H-4 guard**: a 2nd call is WARNING + no-op) → **`rehydrate_from_open_trades()` `:2286`** — Phase 1 per-open-trade RESERVE/COMMIT replay (`:1685`, **OPEN/PARTIAL only**) · Phase 2 today's `RELEASE_USED` P&L carryover (`:1703`) · Phase 3 invariant ONCE (`:1718`) → `CapitalStateInconsistent` ⇒ **exit 3, fail-closed** → paper adapter re-sync (FIX-156, `:2299`) → **synchronous `order_reconciler.reconcile_once()` `:3207` against BROKER TRUTH.**
> **📌 THE TWO ANSWERS WORTH REMEMBERING:** **(1) the day's realized P&L SURVIVES A RESTART BY CONSTRUCTION** — `FIX-051` removed the in-memory `_daily_pnl`; `get_daily_realized_net_pnl` recomputes from `fm_ledger` on every read and **both** daily-loss halves read it that way (`fund_manager.py:1279` gate, `:1507` snapshot) ⇒ **a restart does NOT hand the system a fresh loss budget**, and the limit still enforces on the restored value. **(2) kill state survives via `KillSwitch.__init__` → `_load_state_from_store()` (`kill_switch.py:250`, KS3) — NOT via `clear_stale_state`,** which returns early when the in-memory `_state` is INACTIVE, exactly what a fresh process has. Same-day kills persist; prior-day auto-clear (Rama's 20-Jun headless decision). **⚠️ A `PENDING_FILL` reservation is deliberately NOT restored** — the DB cannot know whether the broker filled it, so capital returns to available and the startup reconcile adopts-or-fails it against the broker. That is correct, and consistent with **NEVER act on DB state alone**.
> **⚠️⚠️ MONDAY 20-Jul 08:15 EXERCISES ONLY THE TRIVIAL RESTORE CASE** — flat book + fresh day ⇒ Phase 1 replays 0 trades and Phase 2 carries 0 P&L rows, so **rehydrate is a NO-OP**. It proves the boot does not crash on the restore path and nothing more. **Still UNPROVEN in production afterwards:** Phase 1 replay of a real open position · Phase 2 P&L carryover · the M-C1 live-seed cancellation · same-day-kill survival — all need a **MID-DAY restart with live state**.
> **⚠️ COVERAGE BOUNDARY:** production runs `main.py --mode live`, but the `wired_system` fixture is `paper_mode=True` ⇒ **the LIVE capital seed (M-C1) has NO integration coverage**, only unit (`tests/unit/test_mc1_live_seed_rehydrate.py`). Removing that subtraction would silently inflate live reservable capital on a warm restart; it is now **pinned structurally**.
> **📌 PERMANENT RULE (third in the series):** **LIVE vs LATENT decides stop-and-report** — judge a finding by **REACHABILITY**, not by whether it matches the trigger phrase. LIVE (can fire under current production config) ⇒ STOP. LATENT (cannot fire today, but the config holds the trigger or one wiring change arms it) ⇒ CONTINUE, document prominently, **pin it with a test that fails the moment it becomes reachable**, and escalate. Joins *CONFIGURED IS NOT COVERED* and *WIRED IS NOT REACHABLE*.

> 📏⭐🔝 **18-Jul-2026 — Q9 BATCH 4 DEPLOYED: SIZING FLOORS/CAPS ARE WIRED, AND ONLY 4 OF 15 SIZING GUARDS CAN BIND IN PRODUCTION.** Tag `deploy-18jul-q9-batch4` → `f177c60`; PC == VM == `755435e`; schema v44 unchanged. Report `docs/audit/q9_batch4_sizing_floors_caps_18jul2026.md`; memory `q9_batch4_sizing_reachability_18jul`. **TEST-ONLY — no production file, no behaviour change.**
> **📌📌 THREE PERMANENT RULES RECORDED HERE:**
> **(1) "WIRED IS NOT REACHABLE"** — the sibling of Q9's *configured is not covered*. A guard can be correctly implemented, correctly wired and bite-proven in a fixture **and still never bind under production config**, because an earlier guard always clamps first. A green wired test then says nothing about whether it protects real capital. **REACHABILITY is now a permanent field in the Q9 coverage matrix.** Concretely: concentration is the strict unique minimum on **298/298** sized trades (`qty_by_risk <= qty_by_concentration` in **0** rows), so `qty_by_risk`, `qty_by_capital`, `POSITION_VALUE_CAP`, `BELOW_MIN`, the lot-skew guard, lot rounding and the FLAT ceiling are all **dead by algebra or config**.
> **(2) A REGRESSION BASELINE TAKEN IN A BARE `git worktree` IS INFLATED — AND INFLATION IS THE MASKING DIRECTION.** `config/instruments.csv` (75,749 B) is **git-ignored**, so a fresh worktree lacks it → `main.py:1949` cannot build the InstrumentCache → **`main.py:2014` `assert instrument_cache is not None`** trips → **~30 boot-path `test_main.py` + preflight/T4 tests fail spuriously** (proven causally: the test fails in **isolation in 0.56s**, passes in the main tree, and copying that one file in makes it pass; full base re-run went **42F → 15F**). A genuine new failure can then coincidentally match one of those ~30 and be waved through as pre-existing. **⇒ PREFER `git checkout <base> -- <files>` IN THE MAIN TREE** (it preserves git-ignored runtime data), or seed the worktree with the ignored files before trusting its numbers. **`git stash` remains FORBIDDEN**, and base+mine must be captured in the **SAME TIME WINDOW**. *This retires the "~30-test flake band" as an uncharacterised observation.*
> **(3) A FAILURE-SET COMPARISON ACROSS TWO SHELLS IS CORRUPTED BY ENCODING, NOT CODE — AND THE ARTIFACT INFLATES THE DELTA.** PowerShell `Set-Content -Encoding utf8` writes a **UTF-8 BOM + CRLF**; bash `sort`/`comm` write **LF, no BOM**. Diff a PowerShell-written failure list against a bash-written one and **every line mismatches on invisible bytes** → `comm -23` reported **32 phantom "new" failures** (this batch's regression gate) that were byte-for-byte the SAME test IDs. **⭐ THE TELL: identical COUNTS and identical IDs but a non-empty `comm` delta ⇒ suspect encoding, NOT a regression** — do NOT investigate the phantoms as new failures; **re-extract BOTH sides in the SAME shell**, or normalise before comparing (strip BOM + `\r`: `sed 's/\r$//'` / `dos2unix`). Sibling of (2): an **environment artifact masquerading as a code difference**, and inflation is again the dangerous direction — imaginary new failures waved through, or a genuine one lost among the phantoms. *This is the exact trap the next PowerShell↔bash failure-set compare will hit.*
> **⚠️ THREE FINDINGS RECORDED, NOT FIXED (evidence for D1 — nothing acted on):** **(a)** `PerformanceAllocator` is **never instantiated** anywhere and `perf_weights` is **never passed** to `SignalProcessor` ⇒ `perf_weight` ≡ **1.0** (298/298 trades) ⇒ `dynamic_by_winrate` / `min_multiplier` / `max_multiplier` are **read by nothing on the sizing path**. **(b)** `capital/position_sizer.py:506`'s `min(tiered, raw_qty*2)` ceiling permits **2× the tightest clamp arm** (20% of capital against a 10% cap; the 40% value cap cannot catch it; the stored `binding_constraint` still reads `'concentration'`) — **LATENT, not live**, only because of (a); a test now **fails the moment the allocator is wired**. **(c)** `reports/daily_report.py:464` reports **3,098 "capital" rejections when the true count is 0** — all are CONCENTRATION, matched because the reason string embeds `capital_qty=`; `:549` further fragments that one class into **246** lines.

> 📉🔬🔝 **18-Jul-2026 — Q10 REGIME THESIS VALIDATION: BLOCKED on the Kite token, but the VERDICT IS ALREADY DETERMINABLE — "NOT DETERMINABLE at n=23", and the backfill CANNOT change it.** Report `docs/audit/regime_thesis_validation_18jul2026.md`; memory `regime_thesis_validation_18jul`. **NOTHING RAN:** no backfill, no code, no flag, no schema; all DB access `mode=ro`. **🔴 §2 STOP — RAMA ACTION OWED: refresh the Kite token.** VM `data_store/session/zerodha_token.json` **absent**; session-dir mtime `Jul 18 05:00` = the documented 05:00 delete cron (`cron_marks/token_cleanup.done` 18-Jul 05:00); **the 08:15 TOTP refresh does NOT run on a non-trading day** (latest preflight marks 17-Jul); the PC copy is dated **21-Jun 12:47** ⇒ ~4 weeks stale (Kite tokens expire the next morning). ⇒ `kite.historical_data` unusable. **`scripts/auto_refresh_token.py` was deliberately NOT run** — it handles live credentials + the TOTP seed, and §2 reserves the refresh for Rama; no workaround attempted. **⭐⭐ THE REAL FINDING — the blocker was expected to be the missing index data; IT ISN'T. The sample ceiling is the TRADE side, not the index side, so backfilling CANNOT rescue the statistics.** The controlled question needs trading DAYS per bucket, and the closed book has 23 days: **LONG/INTRADAY 108 trades / 21 days**, **LONG/POSITIONAL 33 / 15**, **SHORT/INTRADAY 14 / 11** ⇒ **~7 / ~5 / ~3.7 days per bucket across 3 buckets — every cell single-digit BEFORE the analysis begins** (below the n<10 unreliable bar; the SHORT cell anecdotal). **⚠️ THE BETA TRAP (why the controls decide it):** the book is **91% LONG** (141/155) with the bleed in **intraday longs** (108, **−25.57R**, 36.1% win) ⇒ with a 91%-long book *"Bull mornings did better"* **IS the null — market beta, not a ranking edge**; only the WITHIN-cell test (same strategy+direction) separates them, and that is exactly the test with ~7/~5/~4 days per bucket. **The control that makes the question meaningful is the control that makes it statistically empty here.** **📏 POWER, grounded in observed variance** (LONG/INTRADAY daily P&L: 21 days, mean **−5.42**/day, **σ=13.54**; `n_per_group = 2σ²(1.96+0.84)²/Δ²`, 80% power α=0.05): **LARGE (1.0σ) → 16 days/bucket = 47 trading days ≈ 2.2 months** · MODERATE (0.5σ) → 63/bucket = 188 days ≈ 9 months · SMALL (0.25σ) → ~36 months ⇒ **only a LARGE effect is detectable on a realistic horizon; if the edge is subtle this book cannot prove it in any useful timeframe** (a Rama decision, not a computable one). **✅ PIPELINE PROVEN ON REAL DATA — only breadth is missing:** 2026-07-16 NIFTY 256265, 375 1-min candles (09:15:00→15:29:00), the **09:15–09:59 window = 45 bars**, open 24142.10 → close 24128.10 ⇒ **morning move −0.058%**; terciles + cross-tabs are then mechanical. **Trades side FULLY COMPUTED already** (needs no index data) and **independently reproduces BK-1**: LONG/INTRADAY 108/−113.8/36.1%/−25.57R · LONG/POSITIONAL 33/+10.4/+4.50R · SHORT/INTRADAY 14/+28.6/64.3%/+3.87R · TOTAL 155/−74.8. **📅 The 23 days needing backfill** (authoritative, from the closed book): 15–19 Jun · 22–25 Jun · 29–30 Jun · 1–3 Jul · 6–10 Jul · 13–16 Jul (26-Jun absent = no trades; 17-Jul = 0 trades, the S4 outage). **Coverage today: NIFTY = 1 day only (16-Jul, 375 rows) ⇒ 22 of 23 missing.** **One command when the token is live** (proven, data-only, idempotent `INSERT OR IGNORE`, `_fetch_indices` first + fail-safe): `PYTHONPATH=. python3 scripts/fetch_daily_candles.py --backfill --from 2026-06-15 --to 2026-07-16` — fresh `analytics.db` backup first, then verify 23 days landed / 0 stock rows changed / integrity ok. **The tercile % bands are the ONE deliverable that genuinely needs the backfill** (n=1 observed move ⇒ bands were NOT invented). **RECOMMENDATION:** run the backfill anyway when the token is live (cheap, proven, it is the Phase 3 calibration asset and **starts the clock**), but **do NOT build Phase 1 expecting this test to bless it** — the thesis is **unvalidated and not validatable on this book for ~2+ months, and only if the effect is large**; if built anyway, build it explicitly as a **hypothesis under measurement** (shadow-only, logged daily, verdict deferred). **⚠️🔴 STANDING WARNING: do NOT flip `regime.enabled`** while the V3/F1 shadow soak accumulates (the V3 chain consumes regime via `gate_extreme` + `regime_fraction` = **8/40 Context** ⇒ flipping mid-soak changes the score distribution and makes pre/post rows non-comparable). **📌 RECORDED FOR THE FUTURE PHASE 1 DESIGN (ChatGPT's recommendation, NOT built):** the opening-regime calc should be a **configurable PLUG-IN inside the existing `regime/` module** — configurable opening window (default 09:15–09:59), tunable thresholds, pluggable additional regime algorithms, and **VERSIONED regime logic** so variants can be compared on the same logged history (**versioning matters most given the finding above** — if the verdict needs months of data, each logged row must record which algorithm version produced it). Docs-only commit, **UNPUSHED**.

> 📊🔎🔝 **18-Jul-2026 — REGIME PHASE 1 SCOPED (READ-ONLY investigation; NOTHING built/enabled/pushed).** Report `docs/audit/regime_phase1_investigation_18jul2026.md`; memory `regime_phase1_investigation_18jul`. **⭐⭐ THREE PREMISE CORRECTIONS.** **(1) The existing `direction` axis answers a DIFFERENT QUESTION than Rama's ask** — it is a multi-month **DAILY** trend classifier (EMA50 vs EMA200 + slope + swing HH/HL + ADX over **daily** candles, `regime/engine.py:142-188`) that **refuses to compute below 201 daily candles** (`engine.py:92`, `:109-115`); Rama wants the **NIFTY 09:15–09:59 opening move**. **There is NO window config at all** — the only intraday knobs are `intraday_interval`/`intraday_lookback_days` (`config_loader.py:899-900`), and the intraday series feeds **only** `day_type`, which consumes the WHOLE session-so-far and deliberately **WAITS OUT the open** (`session_progress ≥ 0.2`, `engine.py:237`). ⇒ **Phase 1 = ADD A NEW ordinal opening-move axis INSIDE the existing module** (never a parallel one) + window/band config (`RegimeConfig` `config_loader.py:884` + `system_config.yaml:361`, both `extra="forbid"` ⇒ change together) + an append-only daily log. **Not a flag-flip, not a ground-up build.** **(2) The engine NEVER read Phase 0's `candles` table** — it reads the **LIVE Kite historical API**: `engine._fetch`(`:321-326`) → `OhlcFetcher.fetch_by_token`(`sr_detector/fetch.py:85-101`) → `self._fetch_fn(token, from_dt, now, interval)` + `Candle.from_kite`(`fetch.py:125-126`), where `_fetch_fn = _make_sr_fetch_fn(_md_kite, rate_limiter)` (`main.py:2828`, wired `:2859-2864`). ⇒ **Phase 0's stored candles are the BACKTEST/CALIBRATION asset (exactly what the Q10 test needs), NOT the engine's feed; the engine was never blocked on Phase 0.** Do NOT "make the regime read stored candles" — the live path stays on the API. **(3) 🔴 SOMETHING IS WIRED TO ITS OUTPUT — soak-baseline risk (NOT an order-path risk).** The V3 chain (`v3_chain_mode: "shadow"`, DEPLOYED/LIVE) consumes regime **twice** — `gate_extreme(sig.regime_state)` (`v3_chain/runner.py:211`) and **`regime_fraction(...)` = 8 of 40 Context points** (`runner.py:225`, impl `:300-330`) — as does the PB-01 would-be scorer (`pb01_runner.py:153-154`, `:198`, `:214`). All **LOG-ONLY** (shadow never rejects/delays an order; `gate_extreme` fails **OPEN**, `screening/hard_gate.py:337-345`). **⚠️ BUT today `regime_runner` is None ⇒ `regime_fraction` returns `(None, unavailable=True)`; flipping `regime.enabled` mid-soak would change the shadow score distribution being accumulated for the enforce decision and make pre/post-flip rows non-comparable ⇒ PHASE 1 MUST NOT FLIP `regime.enabled`.** **Q2 gate:** `regime.enabled: false` (`system_config.yaml:362`; default False `config_loader.py:895`), comment "VERIFY live historical access on the VM before flipping" (`:364`) — Phase 0 proved `historical_data` for token 256265 at **1-minute × 1 day**, but the engine needs **`day` × 400 days ≥201 candles**, a DIFFERENT call: **strongly implied, NOT proven** (one command closes it; only matters if the 3-axis engine is ever enabled). **Q5 — ALREADY ORDINAL and Phase 2's map ALREADY EXISTS:** the direction axis is a **vote count** (`engine.py:162-179`) and `models.py:37` states *"Ordinal FIRST — a calibrated % is a later, data-driven step (do NOT fake precision)"* = Rama+ChatGPT's decision already implemented; **`regime_pref_direction {BULL:1.0, SIDEWAYS:0.5, BEAR:0.0}`** at `w_regime_preference: 8.0`/40 (`config_loader.py:1086-1091`, `:1058`) **IS a Bull/Flat/Bear tilt** (V3/PB-01-scoped today). **Q6 fail-safe CONFIRMED** (3 layers + fail-open consumer; 13 tests `tests/unit/test_market_regime.py:133-263`). **Q9 sink:** NOT `regime_state.json` (overwritten every cycle, `runner.py:89-98` ⇒ latest only, **no history**) — recommend append-only `data_store/regime/regime_daily.jsonl` (matching `forward_shadow_*.jsonl`/`pb01_would_be.jsonl`/`allocator/regret.jsonl`), written by a **post-close cron sibling of the 15:40 `fetch_daily_candles`** ⇒ no migration, off the live process. **⭐ Q10 CORE-ASSUMPTION TEST = FEASIBLE, data NOT there yet:** trades side READY (**155 filled / 23 trading days, 15-Jun→16-Jul**, per-day net P&L), but **`candles` holds NIFTY 256265 for EXACTLY ONE DAY (16-Jul, 375 rows)** — all 10 index tokens likewise. Prerequisite = a ~23-trading-day backfill via the **already-proven** `scripts/fetch_daily_candles.py --backfill --from --to` (`:9`, `:132-135`; `_fetch_indices` runs first + fail-safe `:65`/`:153-158`) — **data-only, no migration**. Caveats: **n=23** (3 buckets ⇒ single-digit cells) · stock candles start 19-Jun vs trades 15-Jun · **confounded by scanner mix** (BK-1: 91% long, losses in intraday longs) ⇒ control for strategy/direction · tests association, not the counterfactual. **RECOMMENDED SCOPE: DO Q10 FIRST** (data-only + a query, proven tooling — it tests the thesis Phases 1–3 rest on **before** any code), **then** the new axis + append-only log, **without** flipping the flag. Docs-only commit, **UNPUSHED**.

> 🚨✅🚀🔝 **DEPLOYED 18-Jul-2026 ~11:2x IST (off-market, system DOWN): MISSING/INVALID `direction` NOW FIRES A LOUD TELEGRAM+EMAIL ALERT AT BOOT — a forgotten `direction:` can no longer silently fail to trade. PC == origin == VM bare == code tag `deploy-18jul-missing-direction-alert` → `34fd6a5`.** Report `docs/audit/missing_direction_alert_done_18jul2026.md`; memory `missing_direction_alert_18jul`. **§2 FINDING (the behaviour that decided the fix) — path (A) FAIL-BOOT, not silent-skip:** `load_all_strategies` (`strategies/loader.py:67-69`) raises `ConfigSchemaError` on the FIRST invalid YAML ("no partial loads" — it does NOT skip/continue), but the boot's REAL gate is the **preflight**: `run_all_startup_checks` (`main.py:1887`) → `check_strategy_configs` (`startup_checks.py:1266`) CATCHES it → `blocking_failures += "invalid_strategy_configs"` (`:1616-1618`) → `main.py` `_log.critical` + **`return 3`** — **BEFORE the `notifier` is even constructed (`main.py:2048`)**. So today a missing/invalid `direction` aborted the boot with **NO Telegram, NO email**, and one log line naming only the FIRST bad file. **FIX (path A: alert, then KEEP failing the boot):** `strategies/loader.py::scan_strategy_errors(dir)` — pure Layer-2, collects **ALL** bad files in one pass, **never raises**, field-first reasons from pydantic `.errors()` via `ConfigSchemaError.__cause__` (`missing required field 'direction'` · `direction: direction must be LONG or SHORT, got 'FOO'` · `unknown field 'x' not permitted`); `main.py::_alert_invalid_strategy_configs()` sends **ONE consolidated CRITICAL** naming every file+reason, **reusing the strategy-registry/cron-officer path** (`TelegramNotifier.from_env` + `write_critical_sentinel`) — **no parallel notifier**; Telegram uses `write_sentinel=False` so the helper owns the SINGLE email sentinel (**no duplicate email**, and the email still fires if the Telegram token is unset). **FAIL-SAFE** (every send wrapped; the alert can never crash or mask the abort) and **mode-agnostic** (paper==live). Wired into the abort branch guarded, before `store.close()`+`return 3` — **the abort itself is byte-identical**. Covers **ANY** validation failure, not just `direction`. **No schema change** (v44 == `EXPECTED_SCHEMA_VERSION` ⇒ no migration); integrity/FK clean; backup `pre_deploy_missing_direction_alert_20260718.db` (verified `quick_check=ok`, v44, 361 trades). **⭐ PROVEN ON THE VM WITH THE DEPLOYED CODE (real config untouched): the real 16 YAMLs scan `[]` = SILENT (no false alarm); a temp copy with `direction` deleted/`BUYSELL` yields exactly `missing required field 'direction'` + `direction must be LONG or SHORT, got 'BUYSELL'`.** RED-on-old proven on a base-HEAD **worktree** (never `git stash`): 6 scan tests fail `cannot import name 'scan_strategy_errors'`, 5 alert tests ERROR on `ImportError`. Regression **11F/4918P vs base 41F/4877P ⇒ ZERO attributable** (`comm -13` empty). Exercised at the next boot's strategy load (**Mon 20-Jul 08:15**); silent while the config is valid. Rollback = revert `34fd6a5` (schema-free).

> 🧭✅🚀🔝 **DEPLOYED 18-Jul-2026 ~00:2x IST (off-market, system DOWN): STRATEGY-DIRECTION REGISTRY — auto-detect/notify/confirm/DIRECTION_CONFLICT + consumer routing. UNBLOCKS regime Phase 1. PC == origin == VM bare == `44fdbcb`; code tag `deploy-18jul-strategy-registry` → `c2f82ff`.** Report `docs/audit/strategy_direction_registry_done_17jul2026.md`; memory `strategy_direction_registry_17jul`. `StrategyConfig.direction` (`strategies/schema.py:58`, enum-validated) stays the ONE authoritative source; this adds a **registry** that mirrors it, never overrides. **THREE separate fields** in `config/strategy_direction_registry.yaml` (keyed on the stable `StrategyConfig.name`): `direction` LONG|SHORT · `registration_status` PENDING|CONFIRMED · `health` OK|DIRECTION_CONFLICT. **`scripts/strategy_registry_officer.py`** (daily 16:22 Mon-Fri, monitored) mirrors `cron_officer._auto_discover`(:536): enumerate loaded YAMLs ∪ distinct `signals.scanner/strategy` → register UNSEEN PENDING (direction from config) → **notify Telegram+email** (reuses `TelegramNotifier` + critical-sentinel email; no parallel notifier) → PENDING→CONFIRMED on first filled trade (silent); the daily job OWNS writes; no-change run silent; **zero code for future strategy additions**. **⭐ FIX-182 premise correction:** ORPHAN_ADOPTION does NOT adopt orphans as strategy-attributed trades (human orders, `trade_id=None`), so the conflict check is a **daily realized-vs-declared DB scan** (any FILLED `trades.direction` ≠ declared ⇒ health=DIRECTION_CONFLICT, declared value KEPT, notify) — additive, no live trade-path hook; config-path trades can't diverge so a fire is a real anomaly. **Consumer routing (behaviour-neutral):** RETIRED the ONE name-parse (GUI `strategy_tower._family_of` `_long`/`_short`) → strips the token matching the structured `basic.direction`; ROUTED `eod_squareoff` summary grouping from a trade-vote to a canonical map (built by the caller so `_format_summary_body` stays PURE; default None → the vote → byte-identical). Allocator/regime/reports already canonical (audited). **No schema migration** (YAML registry; v44). Backup `pre_deploy_strat_registry_20260718_002539.db`; crontab AUTO-INSTALLED (canonical==generate(registry)). **PROVEN on real VM data (`--dry-run`): new=[] · confirmed=[the 11 BK-1-traded strategies] · conflict=[] · total=16.** RED-on-old GUI 3F + eod 3F on base; regression 44F/4863P zero-attributable (+1 = `test_daily_trade_review` weekend-skip on an untouched file — the run crossed into Sat). First live run Mon 20-Jul 16:22.

> 🧭🔎 **17-Jul-2026 — STRATEGY-DIRECTION CLASSIFICATION INVESTIGATED (READ-ONLY; `082f610`, pushed with this deploy; the registry above is its build).** Report `docs/audit/strategy_direction_investigation_17jul2026.md`; memory `strategy_direction_investigation_17jul`. Foundational for regime Phase 1. **⭐ A strategy→direction mapping ALREADY EXISTS and is STRUCTURED — `StrategyConfig.direction` (`strategies/schema.py:58`, required + enum-validated LONG|SHORT), NOT name-derived.** The Chartink webhook carries **NO side** and the `signals` table has **no side column**; direction ORIGINATES at that config field (`signal_processor.py:778/:812`) and propagates to `orders.transaction_type` + `trades.direction`. **EMPIRICAL: declared config direction == actual `trades.direction` for all 11 traded strategies; no strategy has both sides ⇒ ONE authoritative source, NO conflict ⇒ the ChatGPT addendum's "multiple conflicting sources" STOP was NOT triggered.** **⚠️ Nuance:** the outcome is *computed from* the declaration, so an outcome-based classifier reproduces the config field and cannot itself catch a config mislabel — true independence exists only for **`ORPHAN_ADOPTION`** broker trades (`order_reconciler.py:21`), where DIRECTION_CONFLICT earns its keep. **Q4 pattern to mirror:** `cron_officer._auto_discover` (`:536`) flags crontab jobs not in the registry → notify. **Q5 name fragility is NARROW** (GUI `strategy_tower._family_of :32-38` grouping only; the trading path never parses the name). **Q6 build (recommend, not built):** persist ONE `strategy_direction` registry, auto-detect new strategies (loaded YAMLs ∪ new `scanner_name`) + notify (Telegram+email), route regime/ranking/allocator/analytics/reports to it, retire the name-parse, DIRECTION_CONFLICT on divergence, robust to renames. Signal-path-adjacent → Web Claude → ChatGPT → implement.

> 📊✅🚀 **DEPLOYED 17-Jul-2026 ~22:5x IST (off-market, system DOWN): REGIME-RANKING PHASE 0 — NIFTY + sector index candles now ingested + stored. DATA ONLY (no regime computed, no weighting, no trading-decision change). PC == origin == VM bare == `c8a2e6c`; code tag `deploy-17jul-regime-phase0` → `4e2df39`.** Report `docs/audit/regime_phase0_index_ingestion_17jul2026.md`; memory `regime_phase0_17jul`. **Root cause of BK-1's empty index data:** index tokens were never in EITHER candle path — the live tick feed subscribes only the stock universe (`instrument_cache.token_map()`), and `fetch_daily_candles.py` fetched only *traded* symbols. **Fix (reuse, not a parallel pipeline):** extended the batch cron — `config/index_universe.yaml` (data-driven: NIFTY 50 + 8 sector indices + INDIA VIX; resolved to Kite tokens at fetch time) + `_load_index_universe()`/`_fetch_indices()` fetch each index's 1-min candles via the SAME `kite.historical_data` and store via the SAME `_insert_into_candles_db`, called first in `_fetch_single_day` (before + independent of the stock path). **No schema migration** — `candles` (analytics.db) already has `volume DEFAULT 0`; indices carry no volume. **FAIL-SAFE by construction** (missing config / unknown token / failed fetch → logs + continues, never raises → can't break stock ingestion or the boot). **Stock ingestion byte-identical** (only file deletion = one log string; stock fetch/CSV/insert untouched; index rows are dead keys in the daily report's symbol-keyed candle map). **PROVEN end-to-end (deployed backfill of 16-Jul):** 10/10 indices × 375 candles → NIFTY 256265 **0→375 rows** (09:15→15:29, sane OHLC, volume=0); stock tokens 285→295 (+10), **0 stock rows changed**. Going-forward the 15:40 cron ingests indices daily. Backups `pre_deploy_regime_p0_20260717_225742.db` (+`_analytics_`). Regression 42F/4850P zero-attributable. **⚠️ Phase 1+ (ordinal regime compute → dynamic strategy-weighting: PREFERENCE not permission, never disables a strategy, shadow-first) = SIGNAL-PATH careful-loop, NOT built.**

> 📊🔎 **17-Jul-2026 — BK-1 LONG/SHORT + PER-SCANNER ANALYSIS (READ-ONLY data package for D2; commit `bc35a62`, now pushed with the Phase 0 deploy).** Report `docs/audit/bk1_long_short_scanner_analysis_17jul2026.md`; memory `bk1_long_short_scanner_17jul`. **⭐ The "58% short" premise is INVERTED — the closed book (155 trades, 15-Jun→16-Jul, 23 trading days) is 91% LONG (141) / 9% SHORT (14).** No DB population is 58% short; the long dominance is a scanner-mix artifact (long scanners fire ~4×) amplified by short orders filling at **19% vs 49%**. **The loss is concentrated in INTRADAY LONGs (−0.239R, n=108); positional longs (+0.136R, n=33) and the 14 shorts (+0.298R, too small to trust) were net positive — the shorts PROP the book up.** Ranked keep/kill DATA (not a verdict): biggest drain `vwap_bounce_long` (−12.1R, **n=35, the only reliable loser**), then `gap_fade_long`/`gap_go_long`/`open_low_breakout_long`; best `positional_sector_rotation` (+4.46R, n=19). **⭐ NONE is cost-killed — every net-negative scanner is negative on GROSS too ⇒ negative-EDGE, cost tuning can't save them** (only `first_pullback_long` is cost-marginal). **CANNOT answer: the regime confound — the DB has NO index/NIFTY candles** (token 256265 = 0 rows), so July's long losses (worst weeks W27-W28) can't be pinned to a market downturn vs bad entries; but winning & losing long scanners overlap in time ⇒ scanner-quality is real. Every stat carries its n; 7 of 11 scanners are n<10. Feeds Rama's D2 + M-S4 (dead scorer, gated on D2).

> ✅🔒🚀🔝 **DEPLOYED 17-Jul-2026 ~21:1x IST (off-market, target DOWN): P1 — `GET /health` now honours `require_hmac` (+ P2/P3/P4 test-infra). PC == origin == VM bare == `2b10b5e`; code tag `deploy-17jul-p1-health-hmac` → `695aa84`** (delta tag..HEAD = markdown only). Report `docs/audit/p1_health_require_hmac_done_17jul2026.md`; memory `p1_health_require_hmac_17jul`. **The fix mirrors `/webhook`'s `elif self._require_hmac`:** under `require_hmac=True` a token-only `/health` now 401s (was 200 + `kill_switch_active` + `queue_depth` — the AB-910 §1.7 oracle); a valid HMAC still 200s; the **unauthenticated** 401 is byte-identical under both settings (the S4 boot-seam guard, `TestS4SeamUnchangedByP1`). Uses the `_require_hmac` the ctor already resolved — no new config/idiom; 401 body stays uniform (a distinct message would leak posture). **INERT until Rama flips `require_hmac`** (still `false`) — the deploy is behaviour-neutral; the point is the flip no longer opens the `/health` oracle. **RED-on-old proven twice** (isolated + full-suite: base=44 fails incl. my own fix-test, mine=43 → ZERO env-failure delta). **P2** `wired_system_authenticated` fixture (S4 test uses it, not a hand-built receiver) · **P3** route-map dashboard guard invariant (37 routes, incl. `static` + `/logout`) · **P4** coverage comments. Backup `data_store/backups/pre_deploy_p1_health_hmac_20260717_211747.db`; schema v44 no-migration; integrity/FK clean; `trading-system` correctly down → loads at the Mon 20-Jul 08:15 boot. Rollback: `git revert 6e2a826` (schema-free). **STRUCTURAL (not done — Rama LOOP): the receiver implements auth TWICE per-route and drifted; the dashboard does it ONCE globally (`app.py:169`) and can't — a single-global-auth refactor is the real preventative, P1 independent of it.**

> 🔎🔴 **17-Jul-2026 — FIXTURE-BLINDNESS SCOPED (READ-ONLY, nothing changed): the premise was STALE, and the search found a RUNTIME-PROVEN latent bug (now FIXED by P1 above).** Report `docs/audit/fixture_blindness_investigation_17jul2026.md`; memory `fixture_blindness_17jul`. **STEER: neither "small fix" nor "wide sweep" — of 11 security-gated surfaces, 10 have their secured branch TESTED.** The instruction's premise ("every wired fixture is `secret_token=None` ⇒ the 401 cannot occur in any test") is **FALSE**: `test_fix134_backpressure.py:151` `_make_receiver_with_secret()` drives `/health` auth-ON (6 tests) and `test_webhook_receiver.py:288-341` covers HMAC 401/200. The correctly-scoped original claim was about the **wired/integration** suite only. **The S4 fix already CLOSED its own seam** — unit `test_startup_checks.py:768` (401→reachable) + `:789` (5xx/404/429 still unreachable) + **wired** `test_hardening_scenarios.py:298-331` (real secret → real 401 → real `check_webhook_endpoint`). **🔴 THE ONE EMPTY CELL = `require_hmac=True` × `GET /health`, and the branch is ABSENT from prod code:** `webhook_receiver.py:290-304` never reads `_require_hmac` — its ONLY request-time site is `:469` on `/webhook`. **PROVEN by probe (control = /webhook → 401, so it could have been red): `GET /health?token=<secret>` under `require_hmac=True` returns 200 + `kill_switch_active` + `queue_depth`** — the exact oracle AB-910 §1.7 closed. **LATENT** (`require_hmac:false` today) — **it ARMS the moment Rama flips `require_hmac`, an OPEN Rama-action.** Same shape as S4: two green correct suites, bug in the cell neither owns. Root cause = auth implemented TWICE per-route and drifted (the dashboard implements it ONCE globally at `app.py:169` and cannot drift). **BUILD: P1** `/health` honours `require_hmac` (~3 lines + 2 tests, LOOP-class) · **P2** auth-ON wired fixture (`tests/integration/conftest.py:332` is the only auth-off default with no ON variant — the S4 test hand-builds its receiver, which is the tell) · **P3** route-map-driven dashboard guard test · **P4** comments. **NOT a sweep — no auth-blind components exist.**

> ⏸️🔴 **16-Jul-2026 ~10:54 IST — SYSTEM INTENTIONALLY PAUSED (planned pause for fix/review; NOT a crash).** `SOFT_KILL` active (operator; reason "planned pause for pending fix/review work — no trading issue") → `trading-system.service` **HALTED** (`startup_scenario=HALT` → exit-4, the designed halt-on-active-kill / Audit #18; `RestartPreventExitStatus=3 4` → stable, no flap). **Book verified FLAT at the broker.** First live boot on `c9fb298` was VERIFIED CLEAN (schema v44, no-migration, canary 08:20 ALL GREEN incl ✅ email — F0 SMTP holds). Pre-market kill window missed (task@09:56) → ran live from 10:00, took 3 trades all closed cleanly by the engine (ACI SL-out · HUHTAMAKI TGT-win · PRAVEG). **KEY:** no live halt/flatten API — KillSwitch is in-memory (loaded at boot); a soft-kill needs a restart, and an active kill at boot HALTS the service (down), NOT running-but-halted. **RESUME = `deploy/resume.sh` or auto-clears 08:15 tomorrow.** Live tree `/home/ubuntu/systems/trading-system`. Report `docs/audit/morning_verify_16jul2026.md`; memory `morning_verify_16jul`. Finding (not fixed): alert-watcher respawn loop (pre-existing, non-trading).
# Last updated: 2026-07-17 — **🚨✅ LIVENESS ALARM DEPLOYED (`6a4c092`, tag `deploy-17jul-liveness`) — `trading-system` CAN NO LONGER DIE SILENTLY.** New `scripts/liveness_probe.py` + cron **`*/5 9-15 * * 1-5`** (`market_day_only: true`, `cadence: intraday`, `monitored: false` — the `capture_metrics` precedent): during **[09:00, 16:00) on a trading day**, if the unit is not active and the operator did not park it ⇒ **ONE CRITICAL** (Telegram; CRITICAL-sentinel F1 fallback — the canary's exact delivery contract). **⭐ THE INVESTIGATION CORRECTED MY OWN S4 REPORT: the canary NEVER watched `trading-system` at all** — `check_service_respawn` defaults to **`alert-watcher.service`** (`monitoring_canary.py:220`), `check_dashboard` to **`gui-dashboard`** (`:124`), and `run_canary` mentions `trading-system` **nowhere** ⇒ the `{"nrestarts": 0}` was **alert-watcher's** count. Not "it misjudged trading-system" — **nothing had EVER watched this unit's liveness** (pinned by `test_old_canary_would_not_have_caught_it`). Restart-counting could not have caught it regardless: `Restart=on-failure` + a **clean exit 0** ⇒ `NRestarts` stays **0** = the *healthy* value. **WINDOW [09:00,16:00): the upper bound is CONSERVATIVE** (`_eod_self_exit_due` returns `(False,-1)` **without querying** before `window_end` ⇒ the service NEVER self-exits before the configured stop time; ⚠️ **25-Jul-2026 that stop moved 16:00→17:35 and is now `trading_hours.service_window_end`, so 16:00 is no longer equal to it — merely never after it. The 16:00–17:35 tail is UNWATCHED; extending the probe needs its cron `*/5 09-15` AND `_LIVENESS_END` moved together — a triggered follow-up, `service_window_configurable_25jul2026.md` §6**) and **the lower bound is deliberately NOT `SERVICE_WINDOW_START` (08:00)** — 08:00 is when it MAY start, not when it MUST be up (08:15 token → ~08:30 premarket) ⇒ alarming at 08:00 would be a **guaranteed daily false alarm**. **SILENT on:** NSE holiday (the S1 calendar authority, **fail-open** — same asymmetry S1 used: a calendar error costs one false alarm, never a silenced death) · operator **SOFT_KILL/HARD_KILL** park (the 16-Jul "planned pause" shape) · outside the window · unit active. **⭐ SILENCE IS UNBUYABLE:** an unreadable `kill_switch_state` → `UNKNOWN`, deliberately **NOT** in the suppression set; an unreadable `systemctl` **ALARMS** rather than going quiet. **⚠️ A deliberate `systemctl stop` is NOT distinguishable from a silent death** (both are a clean exit 0 — today's outage WAS one) ⇒ **it alarms, honestly**; bounded, because the operator already has the documented way to signal intent (park it with a SOFT_KILL). **DEDUP on the unit's `InactiveEnterTimestamp`** ⇒ ~84 probes → **1 alarm**; a *distinct* later death alarms **again**; a missing timestamp degrades to **≤1/day**. **⭐ The probe deliberately does NOT import `main.py`** — it must not depend on the health of what it monitors, or it dies exactly when the system breaks; the window constants are duplicated and **`test_window_end_matches_main` guards the drift**. **⭐ THE S4 FIXTURE LESSON APPLIED:** the matrix tests inject a runner — the very pattern that hid S4 — so **`test_default_runner_shells_out_to_systemctl`** pins the production default at the REAL boundary (`subprocess.run`), asserting the argv names `trading-system.service` and that the output is genuinely **parsed**, not defaulted. **Mutation-checked: a probe that can never alarm turns 7 tests RED; restored → 21 pass.** **✅ PROVEN ON THE REAL BOX (the system is still down): real systemctl + real calendar + real kill-state, `now`→09:00 ⇒ `DOWN`, since the REAL `Fri 2026-07-17 08:16:09 IST`; 09:05 ⇒ `ALREADY_ALARMED`; real-now ⇒ `OUTSIDE_WINDOW`. It would have caught today at 09:00 — an hour before the 10:00 entry window.** Regression **11F/4875P/4skip rc=1** (out-of-window set; 4854+21=4875), **0 attributable PROVEN** (failure set byte-identical to the pre-liveness run + none of the failing suites reference `cron_registry`/the crontab). **⚠️ THIS DEPLOY CHANGES THE CRONTAB** (48→49): registry == canonical == live all agree on the VM; `--selftest` 48/48. Monitoring-only (reads systemd/calendar/kill-state) — **no capital/kill/order/signal runtime touched**; parity free (0 mode refs). Report `docs/audit/liveness_alarm_17jul2026.md`. ‖ **🔴 S4 HALTED THE SYSTEM AT BOOT — FOUND + FIXED (`e393b3f`). A LIVE SELF-INFLICTED OUTAGE: 0 trades on a live trading day** (13-Jul=9 · 14-Jul=9 · 15-Jul=7 · 16-Jul=3 · **17-Jul=0**; 17-Jul is NOT a holiday). **S4 (`84cee3e`, deployed ~02:00) put `/health` behind the webhook secret (AB-910 §1.7) — correct — but `main.py:3238`'s post-start self-check GETs that same `/health` UNAUTHENTICATED** (unchanged since `bcf03b5`) ⇒ **401** ⇒ `startup_checks.py:797` counted only **2xx** as reachable ⇒ `main.py:3242` **`_shutdown_event.set()`** ⇒ the system shut itself down at **08:16:09** with a **clean exit 0** (looks like a normal stop; nothing restarts it). **FIX: a 401 PROVES Flask is listening — the only thing the check exists to prove** (its own docstring: *"to confirm Flask is listening"*) ⇒ `reachable = 2xx **or 401**`; **5xx/404/429/no-answer still fail the boot** (pinned by a guard test) and **S4's security property is fully preserved** (`/health` still denies anonymous callers). **Rejected:** token-in-URL (`:3241` LOGS the URL ⇒ re-opens the token-at-rest leak the sweep just closed) · loopback-exempt (behind Tailscale **every** caller is 127.0.0.1 — the S3 lesson ⇒ silently disables the auth). **⭐ WHY NO TEST CAUGHT IT — BOTH SIDES WERE TESTED, CORRECT, AND GREEN**: S4's tests assert `/health` 401s anonymous callers ✅ and the boot check's assert 2xx→reachable ✅ — **the defect lived in the SEAM neither owned**. **It is structurally invisible: EVERY wired fixture builds the receiver with `secret_token=None`** (`conftest.py:332`) ⇒ S4's `if receiver._secret:` branch **never executes in the whole suite** ⇒ the 401 that took prod down **cannot occur in any test**. New **wired** test (`test_hardening_scenarios.py::TestS4AuthenticatedHealthStillBootsWired`) is the ONE place built the way prod is CONFIGURED. **⚠️ Found by VERIFYING A PREMISE, not by an alarm** — the instruction said "System DOWN today" (true, but the author meant the planned pause). **Follow-ups → loop: the `secret_token=None` fixture blindness is SYSTEMIC · `_shutdown_event` on one non-2xx is a very sharp edge (a whole trading day, exit 0) · "booted then immediately shut down" raised no distinct alarm.** Report `docs/audit/s4_boot_outage_17jul2026.md`. ‖ **✅ P3-s14 FIXED (`a9a686e`) — release + retryable, ONE coordinated commit.** The `except sqlite3.IntegrityError`-only INSERT guard now has an `except Exception` BESIDE it that releases the dedup TTLCache claim **and** the in-flight claim and returns **STORE_ERROR**, folded into the `:650` 503 predicate via `_RETRYABLE_STATUSES = {QUEUE_FULL, STORE_ERROR}` ⇒ Chartink retries and the same signal re-enters cleanly; catching (not raising) also keeps the per-symbol loop alive ⇒ **the rest of the batch is no longer abandoned**. Mirrors M-S2's QUEUE_FULL decision exactly. **No double-entry re-opened** (`_claim_in_flight` + `UNIQUE(fingerprint,fingerprint_date)` remain the controls; the cache is a fast-path optimisation). **STORE_ERROR is RESPONSE-ONLY — never reaches `signals.status`** (its INSERT is what failed) ⇒ **no schema change**, `schema.sql:60-66` CHECK untouched, `daily_trade_review.py:717` reconciliation unaffected. **⭐ 2 PREMISES WERE WRONG (proven by the RED run): (1) pre-fix the response is 500, NOT 200** — the 200 is the TRAP a release-only fix would create, not old behaviour (old code escapes to `_handle_webhook:427`); **(2) an IMMEDIATE retry is bounced as IN_PROCESS, not DUPLICATE** — `_claim_in_flight` (`:822`) is checked BEFORE the dedup cache (`:829`) and both claims leak (IN_PROCESS ≤120s → DUPLICATE → 300s; the ~300s bound still holds). **⚠️ THE OPS FINGERPRINT IS RETIRED: `response_code=500` no longer marks this bug** (the 0/89,794 evidence base) — 503 is now shared with QUEUE_FULL ⇒ **alert on the CRITICAL `webhook_receiver: signal store FAILED` log line instead** (kept CRITICAL deliberately: catching must not make a real incident quieter). RED-on-old proven on the TRUE pre-change file; **the `fetch_one` (`:875`) inside the IntegrityError handler has the SAME shape and is NOT fixed here — recorded for the loop** (a sibling `except` cannot catch it; far narrower). Report `docs/audit/p3s14_done_17jul2026.md`. ‖ **P3-s14 INVESTIGATED (read-only, nothing fixed)**: `webhook_receiver._process_signal` guards its signals INSERT with `except sqlite3.IntegrityError` **ONLY** (`:858`), but claims the dedup cache at `:824` **before** it (`:811` also claims in-flight) ⇒ **any non-IntegrityError (realistically OperationalError: disk-full / a >30s writer stall) escapes with BOTH claims held** ⇒ that `(symbol, scanner)` is DUPLICATE-bounced ⇒ **silently dropped**. **STEER: the claim is SEPARATE, not transactional** (in-memory `TTLCache` `:197` vs a SQLite txn that "rolls back on any exception") ⇒ **the fix must EXPLICITLY release**; **claim-before + release** beats claim-after-insert (the idiom already exists twice at `:874-876`/`:913-915`, and double-entry is independently blocked by the in-flight claim + `UNIQUE(fingerprint,fingerprint_date)` `schema.sql:82-83`). **⚠️ THE TRAP: releasing alone is NOT enough** — a caught error returning a normal status makes the batch **200** (`:650`) ⇒ **Chartink never retries** ⇒ a bounded 300s loss becomes PERMANENT. Must pair with a **retryable 5xx**, mirroring M-S2's QUEUE_FULL→503. **Blast radius bounded: one (symbol,scanner), ≤300s** (dedup TTL; in-flight ≤120s via the FIX-011 sweeper) — **but the raise also abandons the REST of the payload batch** (`:637` is unguarded inside the per-symbol loop; they recover on retry). **✅ NEVER FIRED: 0 × HTTP 500 in 89,794 audited POSTs (12-Jun→16-Jul)** — WAL + `busy_timeout=30000` + `BEGIN IMMEDIATE` make it rare. **No test anywhere injects a non-IntegrityError INSERT failure.** Parity free (0 mode refs in the receiver). Report `docs/audit/p3s14_investigation_17jul2026.md`. ‖ **BATCH-3 (X7 + X5) — the batch-safe pile is now EMPTY.** `b842c7c` **X7**: `slippage_recorder._enrich_order` keyed `orders` on the INTERNAL `ord_<hex>` id, but that table's PK is the **broker** order id (`order_manager.py:290-291` — "internal_order_id is NOT stored") ⇒ the lookup matched **nothing, ever**: `parent_trade_id` NULL on **262/262** live rows (so `db_reader.py:1397`'s per-trade drill-down returned 0 rows for every trade), plus `order_type`/`qty`/`strategy_name`/`signal_id`/`order_timestamp` NULL 262/262 and `is_partial`=0 262/262 — and `leg` **WRONG, not missing**: `leg or "ENTRY"` relabelled all 262 rows ENTRY when live had 155 ENTRY + 79 SL + 50 TGT + 21 EOD. Now keyed on `ev.broker_order_id`; unresolved leg → `UNKNOWN`, never a fabricated ENTRY. Observability-only (the async recorder that "can never block trade execution"); the ORDER PATH IS NOT TOUCHED (`_WatchEntry` has no trade_id — declined per the escalation valve). `e2f34a1` **X5**: single-instance lock is now **OS-owned** (`fcntl.flock` / `msvcrt`), released by the KERNEL on any death ⇒ a stale lock is impossible **by construction**. **⭐ Its premise was BACKWARDS**: `SO_REUSEADDR` does NOT permit a second bind on Linux (measured: EADDRINUSE with AND without it; it succeeds only on the Windows dev box) and the PID layer refused second starts before the socket anyway — **P1 was never broken**. The REAL prod bug was the opposite: a **recycled PID** made `_pid_is_alive()` refuse a legitimate start, and since `main()` exits 1 and the unit's `RestartPreventExitStatus` is `3 4`, exit 1 is RESTARTED ⇒ **restart loop = silent total outage**. Lock file no longer unlinked (flock's inode/unlink race). Both properties tested cross-process on **both** platforms (Linux old 4F/15P → new 19P; Windows 19P). **⛔ X3 (retire the duplicate `daily_report`) DEFERRED on its own mandatory precondition — the survivor does NOT cover it**: `daily_trade_review.py:2112` itself emits `{"section":"Telegram","coverage":"0%","note":"not in DB → W1"}`, has **0 hits** for `candle`, and no Capital sheet; `daily_report` ran SUCCESS 14/15/16-Jul (not dead); `fetch_daily_candles.py` exists solely to feed it (cascade). Retire only after W1 + a Candles/Capital decision. Report `docs/audit/batch3_done_17jul2026.md`. ‖ prev 2026-07-16 — **15-Jul COMBINED DEPLOY ✅ DEPLOYED 16-Jul ~00:1x IST off-market (VM==bare==`c9fb298`, tag→`1645bd6` pushed, schema v44 no-migration, F0 SMTP restored + canary ✅ email, Phase-B prune 113,377 cleared→32,623 signals with trades 355 UNCHANGED + FK clean, retention 90d; report docs/audit/deploy_done_15jul2026.md). PRIOR (superseded): 15-Jul COMBINED DEPLOY INTEGRATED to local `main` (VM==bare `2dc69d5`; tag `deploy-15jul-combined`). Branch A (3 fixes: eod_cleanup FK-safe children-first prune + config 90d retention · generate_screened_csv M-SC2b read-only conn · fm_ledger `id`→`ledger_id`) + Branch B (monitoring: F1 alert_watcher no-crash+telegram-fallback · F2 exec-vs-functional · F3 cron-drift markers · F4 hygiene · canary · deploy-gate) MERGED. Overlap `generate_screened_stocks_csv.py` resolved — A read-path + B `_cron_main` functional BOTH present (verified). Schema v44 unchanged; full COMBINED regression GREEN (5052 pass / 12 fail = the known PC-env set ONLY, baseline-proven); the F2 stale-mock regression (`test_fix135_fno_ban::TestCronMainHeartbeat`) FIXED test-only via `create_autospec(record_heartbeat)` + a signature-lock/discovery-guard prevention pair (`tests/unit/test_cron_heartbeat_contract.py`); EOD dry-run clean on a DB COPY (FK-check clean); deploy_assert rc=0 + live integrity_check ok + foreign_key_check empty; annotated tag `deploy-15jul-combined`@`1645bd6` (rollback point). NEW engineering pattern = **Interface Change Checklist** in `docs/monitoring_prevention_checklist.md` (on ANY function-signature change: update callers · wrappers · mocks · autospec · contract test · run discovery guard). Rama: push off-market → verify deploy → Phase-B prune. Runbook `docs/audit/integration_deploy_15jul2026.md`. ⏰ F0 (Rama): restore `ALERT_SMTP_PASSWORD`. ‖ prev 2026-07-15 — **15-Jul MONITORING HARDENING BUILT (branch `monitoring-hardening-15jul`, off `main` `0a77c92`, UNPUSHED; schema-free, test-first, paper==live). Closes the two silent-failure CLASSES from the health audit. F1 `c405c30` alert_watcher: no crash-loop on SMTP auth failure + TELEGRAM FALLBACK for sentinels + backoff + `alert_watcher_degraded.json` degraded marker (CLASS 2). F2 `795a417` EXECUTION vs FUNCTIONAL status — functional encoded in the cron_heartbeat `message` col (`[func=...]`, NO schema change), applied to generate_screened_csv, surfaced by the Cron Officer incl. the F1 email-degraded line (CLASS 1). F3 `d960760` check_cron_drift consults MARKERS for exit_code_file jobs (kills the daily false 'no heartbeat' alarm). F4 `5311fe6`/`e57fdac` purge stale `telegram_ban_until` + date-embed alert_watcher.log. CANARY `11520b4` `scripts/monitoring_canary.py` (daily 08:20) proves email/telegram/sentinel/dashboard, quiet-when-healthy/loud-when-broken. DEPLOY-GATE `66cb182` `scripts/deploy_assert.py` blocks only on BLOCKER class (DB/schema/trading), monitoring=WARN. PREVENTION CHECKLIST (7 items, every new cron/service) = `docs/monitoring_prevention_checklist.md`. Phase-0 paradox resolved (2 535-clusters; genuine deliveries 19-Jun–14-Jul; credential died 15-Jul 03:20). Report `docs/audit/monitoring_hardening_15jul2026.md`; memory `monitoring_hardening_15jul`. ⏰ F0 (Rama) = restore ALERT_SMTP_PASSWORD (stops the loop + resumes email). NOT pushed. ‖ prev 2026-07-15 — **15-Jul WHOLE-SYSTEM HEALTH AUDIT (read-only, `3b9ccd1`, 16 subsystems, TWO verdicts). A) TRADING = PASS (7 trades within caps · book flat · DB integrity clean [`quick_check`/`foreign_key_check`/WAL=0] · backups/token OK · 0 crashes). B) MONITORING TRUTH = FAIL: 🔴 SMTP EMAIL DEAD (Gmail `535 BadCredentials` / expired `ALERT_SMTP_PASSWORD`) → 15-Jul EOD emails never delivered + 8/8 critical sentinels UNDELIVERED while heartbeats say SUCCESS (Layer-9); `alert_watcher` in an exit-2 restart-loop (email-only, no telegram fallback, log 14MB & growing); `check_cron_drift` false-alarms 4 marker-jobs. TELEGRAM (direct) WORKS. EOD-email root cause = Layer 5/8 SMTP auth (broke after 14-Jul 21:20, external credential expiry). NO BLOCKER (all monitoring/observability) → DEPLOY REC: PROCEED with the 3-fix branch; open a monitoring-hardening cycle (⏰ P0 = RESTORE THE SMTP CREDENTIAL — urgent, Rama). Report `docs/audit/system_health_audit_15jul2026.md`; memory `system_health_audit_15jul`. ‖ prev 2026-07-15 — **15-Jul SESSION-1 (first post-deploy, supervised) RAN CLEAN — read-only reconstruction + follow-up. VM rode through Rama's local power outage (PC-independent; token auto-refreshed VM-side 08:15); boot clean/NO migration (schema v44); book FLAT & reconciled; 12 trades ≈ baseline; ALL rejects mapped; prediction HELD. FOLLOW-UP FINDINGS (READ-ONLY — none fixed; feed the next fix cycle): 🔴 `eod_cleanup` FK prune BROKEN since the 14-Jul deploy — P10 `2e61fad` broadened the fingerprint DELETE to `GLOB 'REJECTED*'`; 94,436 old REJECTED_* signals are FK-referenced by `screener_results` and `StateStore` forces `foreign_keys=ON` → `DELETE FROM signals` rolls back atomically. **M-K3 "cron writes run FK-off" REFUTED for this path.** 146k signals / 108k prune-stuck / +4.3k per session / unbounded → non-safety, PULL FORWARD (not emergency). 🟠 `generate_screened_csv` = pre-existing `store.transaction(readonly=)` TypeError (FIX-039 14-May), SEPARATE from `522da32`'s DB-path fix → **M-SC2 RE-GRADED CLOSED→PARTIAL** (+M-SC2b). shadow-mismatch vs eod_verify = KNOWN label-gap (P1 VERIFIED vs eod_verify PENDING; `broker_pnl=0.0` every session; variance today Rs 0.99 = same known Rs 8.14 pattern) → soak evidence, NO fix, P1 stays shadow. M-S5 deploy-watch RECALIBRATED (doc-only; 323 SHADOW_INNING in-baseline 122–840; guard CORRECT, unchanged). 18:15 forward-shadow cron FIRED (rc0). PB-01 NOT wired (spec ready: `:5000/webhook/pb01_breakout_retest?token=`, EOD, must fire before 16:00). Docs `docs/audit/{day_reconstruction,followup_investigation}_15jul2026.md`; memory `day_reconstruction_15jul`+`followup_investigation_15jul`. Deploy UNCHANGED = `2dc69d5`; local main 4 docs commits ahead (UNPUSHED).** ‖ prev 2026-07-14 — **Q4+Q5+P11 bundle + PB-01 SHADOW flip DEPLOYED `277d63e`** (`bb9b1e7..277d63e`, 35 commits, off-market, service DOWN; PC==VM==bare): v3_chain_mode=shadow · watchlist.enabled=true (PB-01 EOD capture + next-morning entry stage, would-be JSONL only) · PB-01 strategy enabled=false (fail-closed) · delivery TRIPLE-LOCKED · min_pass=60 · sizing untouched · schema v44 (v44==v44 no-migration). Next 08:15 boot loads it; 15-Jul = supervised session. **🔴 `trades.sector` is NULL on 100% of rows → `StateStore.sector_exposure()` always returns 0 for the resting book → the 40% sector cap has NEVER fired; gate-8 TOCTOU hardening is INCOMPLETE (resting-book term structurally 0). Harmless today (~₹198/pos « ₹3,957 cap); D1 (raise `max_concentration_pct`) is BLOCKED until `trades.sector` is populated at insert.** Evidence `docs/audit/deployment_evidence_pb01_shadow_14jul2026.md`; memory `pb01_shadow_deploy_14jul`.  ‖ prev 2026-07-03 by Claude Code (Fable) — OFF-MARKET BATCH **DEPLOYED** (~17:25 IST Fri, off-market; deploy_preflight PASS market-CLOSED): `main 9becf8c → a9d44e0 → 7b1c92d`, two pushes, post-receive "Deployment complete" ×2, **crontab byte-identical (zero drift)**, **schema stays v41 (batch is code-only)**. **Push #1 `a9d44e0`** = C-1 completion ff `678c592` (carries **A-2 `fd09a38`** BrokerTimeoutError→no-retry [3 catches verified on the deployed VM tree] + `aa02f9f` api_key-scrub/.gitignore + audit/design/runbook docs) + **B-1 `f5fd4d9`** `--no-ff` (daily-loss unrealized-MTM; **SHADOW — `daily_loss_include_unrealized: false` verified on VM**). **Push #2 `7b1c92d`** = GUI `gui-deploy-03jul`@`33e9223` `--no-ff` (ops_dashboard G2a–G2c, exactly the 79 gated files). **P1 `4817032` (schema v42) DELIBERATELY NOT in this batch** (Rama's tonight-plan = code-only/no-schema) — still unpushed on `fix-p1-eod-broker-reconcile-02jul`; its @15:58 cron NOT installed; the §5.2/§7 shadow/cutover clock has NOT started. Trader left INACTIVE (had EOD-self-exited) → **A-2+B-1 activate at the Mon 06-Jul 08:15 boot** (validate per deploy-plan §4.1 minus the P1 items). **GUI live the same night:** VM venv I4 PASS (no kiteconnect), overlay PC-created (§2a production block appended pre-scp) → scp + chmod 600 + §2b name-only 3+5 PASS, unit `gui-dashboard` enabled+ACTIVE, smoke 302→/login, **binds 127.0.0.1:8500 ONLY**, API-noauth→401; E3 Tailscale (Rama-interactive) pending → then soak S1–S8 from Mon 06-Jul. Both DBs backed up pre-push (`data_store/*.pre-deploy-2026-07-03`). Rollback: revert the merge commits → 9becf8c; B-1 one-flag fallback stays false; GUI = disable unit. **DEFERRED POINTERS LANDED (tree was frozen pre-push):** **D-1** close_trade double-release race CONFIRMED still live post-A1/E1 + design ready (atomic close + trade_id-latched RELEASE_USED, NO schema; guard `orders/order_manager.py:533-42` vs unguarded UPDATE `:587-616`; realistic trigger = CHECK1 + exit-fill processing the SAME broker close; build after batch soak, pairs w/ B-2) — memory `d1_close_trade_race_design_03jul`; **B-2** = periodic unrealized soft-kill (B-1's remaining 4(b) gap) with **W10 bundled INTO B-2** — memory `b1_unrealized_mtm_design_03jul`; **7 low-sev audit verdicts:** C-3/C-4/F-1/E-4/A-3 = FIX (planned Commit A infra + Commit B trading-path), E-2 = ACCEPT (20-Jun HEADLESS-GUARANTEE locked decision), B-2 = accept-with-note — memory `audit_lowsev_verify_03jul`. memory `deploy_a2_b1_gui_03jul`. [prev: PHASE C REPORT CUTOVER **DEPLOYED** (~21:37 IST off-market → `main 01e07b7`, ff `f6de000..01e07b7`, post-receive AUTO-INSTALLED crontab; v41 migration applied; manual-run gate PASSED — 7-sheet 1MB workbook, net=3.24 ties to Phase B, heartbeat SUCCESS; **daily_review DELETED** [git rm module+test+registry entry per Rama's directive; git history=rollback; the prepared enabled-skip was reverted as unneeded], daily_review outputs `reports/daily/` deleted, daily_report kept for bake-in; W13 intact [shadow_tracker on, innings 110]): the redesigned DB-pure `reports/daily_trade_review.py` replaces the old EOD reports. Cron is REPO-MANAGED (`config/cron_registry.yaml` → `scripts/generate_crontab.py --generate` → canonical `deploy/cron/trading-system.cron` → post-receive AUTO-INSTALLS on push). CHANGES: `daily_trade_review` job ADDED @ **16:07 Mon-Fri** (monitored → heartbeat; env `python`/PYTHONPATH; runs AFTER excursion-reconstruction 15:50 + eod_verify 15:55 + wal_checkpoint 16:00 + daily_report 16:05, before trade_journal 16:10). `daily_review` **RETIRED** (`enabled:false`, entry KEPT for a 1-flag rollback — its `reports/daily/` output has NO downstream consumer, so safe). `daily_report` **KEPT in parallel** for a ~3–5-day bake-in (Phase-1 confirmed it runs cleanly in prod — 1.2 MB xlsx/day + heartbeats SUCCESS = a real safety-net; `scripts/system_manager.py` soft-WARN-checks its xlsx). `main()` `--date` now DEFAULTS to today IST (cron needs no date-substitution, like the retired generators) + records `cron_heartbeat("daily_trade_review")` SUCCESS/FAILED. **`core/cron_registry.py::expected_heartbeat_jobs` now SKIPS disabled jobs** (a retired job can never run so must never be "expected" → NO false MISSED from the Cron Officer — the fix that makes an `enabled:false` retirement clean). `scripts/system_manager.py` EOD check ADDED for `daily_trade_review_report_<day>.xlsx` (daily_report check kept for bake-in). The Cron Officer + drift-check auto-adapt (they derive expectations FROM the registry; `content_drift`/`_auto_discover` are live-vs-generate(registry) — NO stored expected-list/hash to edit). Canonical regenerated BY THE TOOL (byte-identical to generate(registry), pure ASCII+LF → post-receive `diff -q` gate holds → auto-install). **W13 GUARDRAIL (Phase-C MUST honour): do NOT set `shadow_tracker.enabled=false` and do NOT drop the `innings` table** (110 rows live; gates live re-entries + read by daily_report; the Multi-Inning report VIEW is DEFERRED — data persists, backfillable via `state_store.get_inning_summary_by_date`). Tests: `test_daily_trade_review.py` **57** (+entrypoint default-date+heartbeat), `test_cron_registry.py::test_phase_c_cutover_expectations`, 147 cron-machinery green. Deploy + first-run checklist + rollback: **`docs/phase_c_cutover_runbook.md`**. Phase-1 investigation stop-gate: ONE consumer found (system_manager's soft daily_report.xlsx WARN-check) → handled by the bake-in, nothing else reads the old outputs, NOT a blocker. memory `phase_c_cutover_01jul`. [prev: DASHBOARD SHEET (FINAL sheet of `reports/daily_trade_review.py`, placed FIRST; report-redesign, STAGED — NOT pushed; DB-pure, parallel, NO schema/cron/trading-code) — summarizes the six sealed sheets. **Summary-agrees-with-detail is the contract**: every headline derives from the SAME sources the detail sheets use (already-built records/srecords/sdata/slipdata/rmeta) → CANNOT disagree. Deterministic (rule-based) highlights ONLY — no AI (LLM narrative stays Tier-3). 7 sections: (1) RECONCILIATION BANNER (reads Recon OVERALL; FAIL is the headline ABOVE profitability), (2) DATA COVERAGE (per section %/status + W-task: Signals 100%/82%-dropped→W9, Orders 100%, Recon 100%-internal/broker-PENDING→W2/W3, Config post-W0/W0.1, Strategies 100%, Slippage price_band/tier→W12, MFE/MAE _computed_%→W6, Telegram 0%→W1), (3) YESTERDAY-vs-TODAY deltas (prior trading day data-driven `MAX(date)<today`), (4) TRADING SUMMARY (signals·traded/placed/filled·long/short·closure breakdown), (5) PROFITABILITY (gross·charges·net·ROI·profit-factor·win-rate·avg/max win/loss·slip cost+%), (6) CAPITAL (opening·peak·closing·drift·max-concurrent [sweep-line]·utilization%), (7) DETERMINISTIC HIGHLIGHTS (~6 flags: recon≠PASS, high-slippage>25%+driver, long-vs-short skew, biggest winner/loser, low-sample count, pending-capture count). **BUILD-GATE PASSED (all cross-checks, real 30-Jun VM backup)**: net ₹3.24==Orders TOTALS · banner==Recon OVERALL · long 17==Strategies T3 · closure breakdown==Orders · slip ₹19.65/249%==Slippage · MFE/MAE 55.6%==independent · yesterday ₹29.34/16==independent prior-day · Δ==today−prior · final tab order==[Dashboard,Reconciliation,Orders,Signals,Strategies,Slippage,Config]. **TAB REORDER**: build-all-then-render-in-tab-order; sheets RENAMED (dropped numeric prefixes). 61 tests (+4 Dashboard). `docs/report_data_contract.md` Dashboard rows appended. **★★ TIER-1 REPORT COMPLETE (all 7 sheets built + cross-validated); NEXT = Phase B (run new generator PARALLEL to old, compare) then Phase C (retire daily_review.py + point cron at new generator).** memory `dashboard_sheet_build_01jul`. [prev: SLIPPAGE SHEET (sheet 6 of `reports/daily_trade_review.py`, report-redesign, STAGED — NOT pushed; DB-pure, parallel, NO schema/cron/trading-code) — the LAST analytics sheet (absorbs the parked Slippage-Phase-2 reports). Source = `trade_slippage_log` (per-trade roll-up from `orders/slippage_recorder`) + Orders `records` (gross P&L + entry tolerance). **DECOMPOSITION = 3 legs** (verified vs the system's own `calc_rr_damage_pct`): total slip cost/trade = `(entry_slippage_rs + sl_slippage_rs − tgt_slippage_rs) × qty` — ENTRY adverse(+), SL-exit adverse(+), TGT-exit favourable(−); components SUM to total by construction. **NO 4th leg** — SL/TGT ARE the exits; EOD/manual-exit slippage NOT recorded (flagged). **TIER FINDING**: `slippage.tiers` (liquid/mid/small bps) is resolved at RUNTIME by `broker/slippage_engine.tier_for(symbol)` via InstrumentCache (a PAPER-fill model), NOT persisted per trade → no DB-pure tier. Block 2 buckets by the RECORDED `price_band`; configured tier bps (from W0 config_snapshots) shown as reference; **W12** = persist the resolved tier per trade. 6 blocks: SUMMARY+DECOMPOSITION (total · ENTRY/SL/TGT totals · slippage-as-%-of-gross-P&L), BAND ANALYSIS, STRATEGY-WISE, STOCK-WISE, WORST-20 (by slip cost: symbol·strategy·entry slip·cost·excess-over-tolerance), 10-DAY TREND. Aggregates at FULL precision, rounds once (decomposition sums to total EXACTLY — no rounding leakage). **BUILD-GATE PASSED (all 3, real 30-Jun VM backup)**: (a) total ₹19.65 == Σ per-trade recomputed independently; (b) ENTRY(−0.13)+SL(0.20)+TGT(19.59)=19.65 == total (no leakage), each == independent; (c) band '200-300' recomputed matches. Honest signal: slip cost = 249% of the day's ₹7.89 gross (CGCL's adverse TGT_HIT-at-loss exit dominates). 47 tests (+2 Slippage). `docs/report_data_contract.md` Slippage rows appended. **ALL data/analytics sheets DONE (Orders/Signals/Reconciliation/Config/Strategies/Slippage); only the DASHBOARD remains.** memory `slippage_sheet_build_01jul`. [prev: STRATEGIES SHEET (sheet 5 of `reports/daily_trade_review.py`, report-redesign, STAGED — NOT pushed; DB-pure, parallel, NO schema/cron/trading-code) — the FIRST analytics sheet, aggregating the PROVEN `trades` truth layer (reuses the day's Orders `records` + Signals `srecords`; **`strategy_metrics` INTENTIONALLY NOT used** — thin/derived/CLOSED-only, lacks signals/gross/ROI/direction/bucket/RR). win=net>0, loss=net<0 (system's own `compute_strategy_metrics` def). 5 spaced BASIS-labelled tables: **T1 PERFORMANCE** (per strategy: signals(reached-storage)·trades·wins·losses·win%·loss%·gross·net·ROI%[=Σnet/Σcapital]·capital·max_win·max_loss; signal-only strategies shown), **T2 TIME-BUCKET** (30-min; trades by ENTRY time, signals by receipt time; empties omitted), **T3 LONG-vs-SHORT**, **T4 RR** (planned SL%/TGT% = abs(entry−sys_sl)/entry & abs(sys_tgt−entry)/entry; actual from filled legs; avg RR = `trade_slippage_log.actual_rr`), **T5 RANKING** (TRAILING N sessions, default 20, by entry date — NOT that-day-only). **T5 COMPOSITE (transparent, on-sheet)**: `(0.40·win%_norm+0.40·netROI%_norm+0.20·avgRR_norm)×confidence`, `confidence=min(trades_n/20,1)`, norm=min-max across strategies (0.5 if all equal); rank desc. DELIBERATELY stops a low-sample strategy topping a high-sample one (a 1-trade/100% strat scores raw~1.0 but ×0.05 conf → composite~0.05, rank #7–9; an 8-trade/50% strat tops #1). Window shrinks to actual distinct trade-sessions (11 on the 30-Jun DB), stated on-sheet. **BUILD-GATE PASSED (both parts, real 30-Jun VM backup)**: (a) T1 aggregates recomputed independently per strategy + raw-SQL cross-check on the 2 top-trade strategies (`DATE(created_at)`) — all match; (b) composite sanity — low-sample down-weighted, 8-trade/50% tops, components shown top+bottom; T3 LONG net ₹3.24 over 18 trades == Orders TOTALS. 45 tests in `tests/unit/test_daily_trade_review.py` (+4 Strategies). `docs/report_data_contract.md` Strategies rows appended. memory `strategies_sheet_build_01jul`. [prev: CONFIG SHEET (sheet 4 of `reports/daily_trade_review.py`, report-redesign, STAGED — NOT pushed; DB-pure, parallel, NO schema/cron/trading-code) — the FIRST consumer of W0 `config_snapshots`. A sectioned two-column key/value view of the day's resolved `AppConfig`, read ONLY from `config_snapshots.config_json` (latest row by `snapshot_ts`); nested values (leverage_map, scoring weights, broker-cost rates, slippage tiers) as indented SUB-ROWS; NO-snapshot dates → honest `— pending W0` placeholder (never blank). Header = `Config snapshot for <date>` + Snapshot ID · Captured-at · Mode · Account · trade_type · hash. 6 sections → VERIFIED paths (real `load_all()` dump): SYSTEM (`$.system.{trade_type,force_intraday_only,delivery_enabled}` + `.trading_hours.*` + `.capital.leverage_map.*`; **max_capital NOT in config** → runtime-balance label), RISK (`$.system.risk.*` + `.position_sizing.{max_position_value_pct,max_concentration_pct,risk_per_trade_pct}`), SCORING (`$.scoring.{min_pass_score,thresholds}` + `.steps.*` weights), SLIPPAGE (`$.slippage.{default_tier,tiers.*.slippage_bps}` + `$.system.entry_gate.*` — tier-based bps, NOT a price-slab→allowed table), BROKER COSTS (`$.broker_costs.zerodha.*` equity), STRATEGY (`— pending W0.1` — per-strategy config NOT in config_json; loads via StrategyLoader). **BUILD-GATE PASSED**: fresh v41 DB + real `load_all()` snapshot → value spot-check ALL PASS (min_pass_score=60 · daily_loss_limit_pct=0.03 · leverage INTRADAY=5.0 · entry_end=15:00, +4 more) + all 4 sheets render + NO-snapshot placeholder proven. HOUSEKEEPING: corrected the Reconciliation CAPITAL note (code + contract) — `RESET_PNL` is a BY-DESIGN daily EOD reset, NOT "pollution"; the real reason `get_daily_realized_net_pnl` is avoided = **W10** (double-subtracts costs). Backlog recorded: **W0.1** (strategy-config snapshot writer), **W10** (get_daily_realized_net_pnl double-cost fix), **W11** (NULL-exit CLOSED_MANUAL trades lack net_pnl → can FAIL CAPITAL honestly). 41 tests in `tests/unit/test_daily_trade_review.py` (+3 Config); `docs/report_data_contract.md` Config-sheet rows appended. memory `config_sheet_build_01jul`. [prev: ★ SAFETY FIX: naked-position EMERGENCY-EXIT tag bug (P0, branch `fix-emergency-exit-tag-01jul`, NOT pushed — Rama pushes off-market; touches the order path, off-market only). Found while investigating a T2-halt request: the live system was in a stuck `SOFT_KILL` set by the reconciler's CHECK9 MISSING_EXITS (naked BANSALWIRE) whose **EMERGENCY MARKET EXIT was itself REJECTED**: `orders/order_reconciler.py:2133` passed `tag=trade_id` (`trd_`+32hex = 36 chars) → Zerodha "Invalid tags: max allowed tag length is 20" → the naked-position **last line of defense failed to place** (today masked because the SL had already filled — BANSALWIRE closed SL_HIT 2s later). **ROOT-CAUSE FIX (right layer, not a point patch):** a defensive truncation GUARD at THE single order-submission chokepoint — `broker/zerodha_adapter.py::place_order` does `if tag: tag = truncate_tag_for_broker(tag)` (≤16) right before `kite.place_order`, so **NO caller can EVER submit an over-length tag** (also covers the 17-18-char `ks_hard_kill_*` sweep tags near the limit). Plus the call-site fix at `:2133`. Investigation: `:2133` was the ONLY direct un-truncated LONG (>20) `adapter.place_order`; the 4 `order_placer.py` `tag=trade_id` sites (2569/2737/3031/3369) are SAFE (truncated downstream by the order protocols `:193/294/542/108/189`); GTT/modify submit no tag. Matching is by `order_id` not tag → no traceability lost. Paper never saw it (paper path doesn't submit to Kite — parity confirmed). **PROOF:** `tests/unit/test_emergency_exit_tag_fix.py` (6 tests; a Zerodha-mimicking kite rejects tag>20) — proven to FAIL on old code (3 bug-exercising tests) and PASS on the fix; regression sweep 254 green (adapter/ids/reconciler). **STEP 4 (the stuck SOFT_KILL):** `kill_switch.clear_stale_state()` auto-clears ANY prior-day kill regardless of type ("HEADLESS GUARANTEE") → **tomorrow's 08:15 boot AUTO-CLEARS today's kill; NO manual `--resume` needed** (leave halted today; same-day `auto_clear_scheduled_kill` won't clear an emergency reason). SECONDARY (flagged, not built): a rejected LAST-line-of-defense exit currently only SOFT_KILLs — consider retry/HARD_KILL escalation. memory `t2_halt_investigation_reconciler_tag_bug_01jul`. [prev: RECONCILIATION SHEET (sheet 3 of `reports/daily_trade_review.py`, report-redesign, STAGED — NOT pushed; DB-pure, parallel, NO schema/cron/trading-code) — the INTEGRITY BACKBONE. 5 blocks (Identity | LHS | RHS | STATUS | Verified-At | Detail) + an OVERALL VERDICT the Dashboard banner reads: (1) SIGNAL-STORAGE `stored=qual+rej+skip+other` Δ=0 [+funnel context; **unmapped status→FAIL**], (2) ORDER `qualified=order_placed_ok+placement_failed`, (3) TRADE `placed=filled+cancelled+rej/failed+pending` [unmapped→FAIL], (4) CAPITAL `opening+realized=closing` via a REAL cross-source check **`Σ fm_ledger.RELEASE_USED.pnl_delta` == `Σ trades.net_pnl`** within ₹1 (**NB: `get_daily_realized_net_pnl` NOT used** — a `RESET_PNL` housekeeping row pollutes it → 0.0 vs trades ₹3.24; `RELEASE_USED.pnl_delta` is the clean trade-close realized; RMS-close `costs=0.0` divergence FAILs honestly, not hidden), (5) BROKER **PENDING_CAPTURE** (broker P&L/positions/margin not persisted → W2/W3, never FAIL). OVERALL = FAIL if any FAIL / else "PASS — N pending capture" / else PASS. Also **re-labelled the Signals totals block FUNNEL-FIRST** (Received webhook 46,337 → storage 8,477/18.3% → Qualified 18 → Traded 18; Rejected/Skipped under the storage line). **BUILD-GATE PASSED (both parts):** (a) 30-Jun render — all 4 computable blocks match independent DB (stored 8477 · order 18=16+2 · trade 18=filled 10+rej/fail 8 · capital ₹3.24==₹3.24), OVERALL "PASS — 1 pending capture"; (b) **FAIL-INJECTION** — +₹100 to a closed trade's `net_pnl` on a COPY DB flipped CAPITAL PASS→FAIL (ledger 3.24 vs trades 103.24) AND OVERALL→FAIL, then removed (live untouched) — proves the FAIL path fires. Partition blocks (1,3) are FAIL-able (explicit status buckets, NO silent catch-all → an unmapped/new status makes Δ≠0). 3 reconciliation unit tests (38 total in `tests/unit/test_daily_trade_review.py`). memory `orders_sheet_forensic_master_01jul`. [prev: SIGNALS SHEET (sheet 2 of `reports/daily_trade_review.py`, report-redesign, STAGED — NOT pushed; DB-pure, parallel, NO schema/cron/trading-code). ONE row per signal that REACHED STORAGE — the HONEST denominator (a stop-gate finding). **Denominator finding:** duplicates are dropped at the webhook TTLCache/fingerprint dedup BEFORE any INSERT (`signals/webhook_receiver.py` returns `status='DUPLICATE'` with no row); score/secondary/capital REJECTS ARE stored (a `QUEUED` row updated to `REJECTED_*`). 30-Jun: **46,337 received (webhook_audit aggregate) → only 8,477 (18%) reach storage; 37,860 (82%) dropped pre-storage** (dupes + invalid/out-of-window/backpressure; no per-signal detail → **W9**). So the design identity `Received=Qualified+Rejected+Duplicate` CANNOT hold per-signal — a per-signal sheet is FORCED onto stored signals (the only per-signal data). Basis = "signals that reached storage"; totals block has TWO levels — webhook aggregate CONTEXT + storage partition `Received(stored)=Qualified+Rejected+Skipped/Other` **Δ=0** (no fake Δ=0, no fabricated dup rows). 21 cols, freeze header row (`A18`), rejected→amber / duplicate→grey / qualified→green; `Stage Reached` derived (RECEIVED→SCORED→SECONDARY_FILTER→CAPITAL_CHECK→ORDERABLE→ORDER_CREATED). **BUILD-GATE PASSED**: 30-Jun render 8,477 rows; totals-validation ALL PASS (46337 / 8477 / Qual 18 / Rej 8166 / Skip 293, Δ=0) vs independent DB counts; cross-sheet integrity = the 18 Qualified (ORDER_CREATED) signals == the 18 Orders trades. 13 Signals unit tests (35 total in `tests/unit/test_daily_trade_review.py`). W9 = persist per-signal drops (raw receiver log) to make the denominator per-signal complete. memory `orders_sheet_forensic_master_01jul`. [prev: ORDERS SHEET (forensic trade master; report-redesign sheet 1 of a NEW DB-pure generator `reports/daily_trade_review.py`, STAGED — NOT pushed; PARALLEL to daily_report.py/daily_review.py [NEITHER edited nor re-cronned]; NO schema / NO cron / NO trading-code). ONE row per trade placed (69 cols, 9 colour-banded groups A–I; 3-row header, freeze `F4` through Symbol, Excel OUTLINE groups B–I collapsible, TOTALS row, SL→red/TGT→green row tint + Net/slip-over/mismatch conditional fmt). **DB-ONLY** (StateStore date getters + direct `fetch_all`; reuses `reports/style_constants.py` palette; the 4 cell helpers mirrored locally to stay decoupled). **3 flags resolved** (investigated vs real schema/code): FLAG-1 exit-trigger timestamp **DEFINITIVELY ABSENT** (no column distinct from the fill; `orders.filled_at`≈`trades.exit_time`; a resting SL/TGT's `placed_at` is at entry) → Exit Trigger + Exit Delay render `— pending W5`; **Entry Delay confirmable** via `trades.order_to_fill_ms`. FLAG-2 closure-type **RESOLVED HONESTLY via a `reconciliation_log` join** (refined same-day after a semantics review) — `classify_closure()` keeps the precedence order but the RECON_CLOSE trigger is NARROWED to be truthful: `MANUAL_CLOSE` is the reconciler *DETECTING* an external close (`mark_trade_manually_closed`), NOT a repair, so it is **NOT** RECON_CLOSE. The daily-EOD squareoff (15:17) writes NO positive per-trade marker (finalized by the reconciler as `MANUAL_CLOSE`; `eod_squareoff_log` is per-day) so daily-EOD / operator-manual / RMS are genuinely indistinguishable → honest label **SYSTEM_CLOSE** (not RECON_CLOSE, no time-guess). Only `STUCK_EXITING` (reconciler-INITIATED finalize) = **RECON_CLOSE** (rare); `%ORPHAN%`/`SYSTEM_OVERSELL` = ORPHAN_RECOVERY; a POSITIVE EOD marker (`exit_reason='EOD_SQUAREOFF'`/legacy `*EOD*`) = EOD_SQUAREOFF (a co-occurring `MANUAL_CLOSE` = bookkeeping); `RECON_EOD_CLOSE` only if BOTH. Permanent fix logged **W8** (per-trade `closure_source` at close time). Values now: SL/TGT/EOD_SQUAREOFF/SYSTEM_CLOSE/RECON_CLOSE/RECON_EOD_CLOSE/ORPHAN_RECOVERY/UNKNOWN/—. Normal SL/TGT carry NO recon row → correctly fall through. FLAG-3 freshness: **S&R 2-state derivable** (`sr_detector_results.actual_result` NULL=detect/NOT=backfill); **MFE/MAE provenance ABSENT** (no source tag on `trade_excursions`; `excursion_reconstruction_runs`=aggregate-counts-only) → honest composite + `— pending W6`. Plus `— pending W2` broker-margin, `— pending W7` exchange-order-id (no column). **BUILD-GATE PASSED**: rendered a REAL date off the 1-Jul VM nightly backup (30-Jun, 18 trades, mode LIVE, TOTALS net ₹3.24) + **10-trade forensic validation ALL PASS** (entry/exit px+times, gross/charges/Σcosts==charges/net, slippage, ROI, broker-oid, closure incl. NRBBEARING+ARVIND — post-refinement = **SYSTEM_CLOSE** (honest; ARVIND is the 15:17 daily-EOD squareoff, NOT over-claimed as RECON_CLOSE or time-guessed EOD); re-validated 10/10 PASS with an independent honest oracle). 18 unit tests (`tests/unit/test_daily_trade_review.py`); classifier + helpers + synthetic-DB end-to-end. Data note: reconciler-finalized closes (incl. daily-EOD) surface as RECON_CLOSE; CGCL 30-Jun `TGT_HIT`@loss rendered faithfully (forensic master reflects stored truth). `docs/report_data_contract.md` Orders rows appended. memory `orders_sheet_forensic_master_01jul`. [prev: W0 CONFIG_SNAPSHOTS (daily-report redesign FOUNDATION, STAGED — NOT pushed; NO cron / NO flags / NO trading-code): the report's Config sheet + any historical re-run must read the config AS IT WAS on date X, DB-purely (system writes DB, report reads DB — never YAML). **Investigation stop-gate = CLEAN**: config IS centralized as ONE Pydantic `AppConfig` via `core/config_loader.load_all()` (main.py:1485), resolved ONCE per startup (no hot-reload; CL5/CL6), and NO full-config snapshotting exists (only per-FILE hash change-detection — `utils/startup_checks.check_config_hash` + `session.last_config_hash` = `json.dumps(file_hashes)`, a DISTINCT mechanism). **BUILD**: schema **v40→v41 PURE ADDITION** `config_snapshots` (`core/schema.sql` TABLE 46: snapshot_id/snapshot_date/snapshot_ts/account_id/mode/trade_type/config_hash/config_json + `idx_config_snapshots_date`; NO MIGRATION_TABLES entry — same path as v37/v39/v40). Writer `core/config_snapshotter.py::snapshot_config` serializes `AppConfig.model_dump(mode="json")` → STABLE sorted JSON → sha256 `config_hash`; **IDEMPOTENT per (snapshot_date, config_hash)** (same-config restart = no-op skip; same-day config change = NEW row, timestamps order them). Wired in `main.py` (~L1824) AFTER mode is FINAL (post interactive flip) + AFTER `--status`/`--dry-run`/failed-startup return → fires in BOTH paper+live (mode is a COLUMN → parity via single call site), non-fatal (never blocks startup). Strategies **NOT** included (separate `StrategyLoader` domain, loaded late @main.py:2292; per-strategy params = `NEEDS_CAPTURE` follow-up). **SECRET-SAFE by CL5** (resolved config holds env-var NAMES only, never values; the one plaintext `alerts.smtp.password` is empty in prod; writer logs only row-id + hash-prefix). Version pins bumped: `state_store.EXPECTED_SCHEMA_VERSION=41`, schema trailing INSERT `'41'`, migrations v36→v41 pure-addition note; test pins de-brittled (test_control_tower_phase1a `>=40`/`==EXPECTED_SCHEMA_VERSION`) + bumped (test_sr_detector_observer + test_sr_v2_monitor `==41`). NEW `docs/report_data_contract.md` (permanent SYSTEM-writer↔REPORT contract; Config rows seeded from the Phase-0 audit). **Verified on REAL repo config**: 1 row, 12,378-byte VALID JSON (all 8 sub-configs + file_hashes; trading_hours/capital/risk/scoring/etc. all present), re-run = None (skipped), count stays 1. **4126 pass / 13 skip / 0 fail** full unit suite. memory `w0_config_snapshots_01jul`. [prev: SR_DETECTOR_BACKFILL CRON WIRED (COMMITTED 30-Jun, branch `wire-sr-detector-backfill-cron-30jun`, NOT pushed — Rama pushes off-market; cron-registry ONLY — NO schema / NO flags / NO trading-code; shadow-data job). The EOD S&R shadow outcome backfill (`scripts/sr_detector_backfill.py`, DB-only `sr_detector_results ← trades` by signal_id, authored 27-Jun) was **NEVER registered** in the cron framework → outcomes stayed NULL until hand-run (the root cause of the 29-Jun all-NULL `sr_detector_results` outcomes, backfilled by hand 30-Jun = 13/13). Registered in `config/cron_registry.yaml` as `sr_detector_backfill` @ **15:58 Mon-Fri** — placed AFTER `reconstruct_excursions` (15:50) + the critical `eod_verify` (15:55) trade-finalization gate, before the 16:00 reporting wave (trades terminal by square-off ~15:17; `net_pnl` set in-process by EOD). Verification = **marker-based** (`detection_method: exit_code_file` + `marker_name: sr_detector_backfill`, matching the `preflight_phase_*` class) because the script emits **NO heartbeat** → the Officer reads `cron_marks/sr_detector_backfill.done`, never a false RAN_UNVERIFIED. `env_wrapper: python_nopath` (script self-inserts ROOT; no PYTHONPATH needed). Canonical regenerated BY THE TOOL (43→**44 command-lines**): `--selftest` 44/44 byte-round-trip, `generate()` deterministic, `--gate` canonical==generate(registry) (post-receive `diff -q` precondition holds → crontab auto-installs on Rama's next push). [prev: T5 ENTRY_END ALIGNMENT (DEPLOYED 29-Jun; CONFIG-ONLY — one value, parity automatic, NO schema, restart-to-reload): `config/system_config.yaml` `trading_hours.entry_end` **15:15 → 15:00** to match the per-strategy reality (all 15 strategies already set `entry_end_time=15:00`; pre-check confirmed NONE exceed 15:00 so zero current strategy is clamped). **Zero trading-behaviour change** (T5 investigation: latest entry EVER 14:57, 0 entries ≥15:00 across 50 closed trades / 15–29 Jun). Purpose: kill the Config Auditor `G1_entry_end_near_squareoff` finding at SOURCE (15:00 is 17 min before squareoff 15:17, was 2 min) + harden the global backstop (a future strategy that omits its own `entry_end_time` now inherits 15:00, not 15:15) + config self-consistency. **`eod_entry_cutoff` stays 15:15** (FIX-073, separate order-deadline mechanism — explicitly OUT of scope; they no longer coincide). Committed so `checkout -f` can't revert it. Value-lock test updated to pin 15:00; `test_entry_end_near_squareoff_warns` now asserts the G1 LOGIC via explicit 15:15 + that the live 15:00 does NOT warn; 4 new `test_market_windows.py` T5 tests (global 15:00 cap, default-15:15 strategy now capped by global, missing-entry_end fallback to 15:00, existing-15:00 unchanged). 115 + 171 preflight tests green. **T5 followup DONE (29-Jun, source-consistency, NO restart):** `strategies/schema.py:114` `entry_end_time` default **15:15→15:00** + comment fixed to "Matches trading_hours.entry_end = 15:00". INERT (all 15 strategies override + the global 15:00 clamps even a missing override; default verified=15:00, all 15 validate, distinct entry_end_time=['15:00']) → committed for source correctness, rides the next natural boot (Tue 08:15), no standalone restart. test_market_windows docstring de-staled. **Followup-2 SWEEP DONE (29-Jun, NO restart):** `strategies/schema.py:113` `entry_start_time` default **09:20→10:00** + comment fixed ("Matches trading_hours.entry_start = 10:00"). A full schema.py sweep found NO OTHER "must match system_config.yaml" stale defaults (`min_score=0` "use global from scoring_weights" + `sl_gap_buffer_pct=0.0` "disabled" are intentional sentinels, not drift; no non-inert/applied drift). Both entry-time defaults INERT (all 15 strategies override 09:25/15:00 + the global 10:00/15:00 binds even a missing override; verified defaults=10:00/15:00, 15/15 validate, no value-lock test pinned the schema defaults). **schema.py config-drift class FULLY RECONCILED.** [prev: T2 BACKUP RETENTION (DEPLOYED 29-Jun; cron-job ONLY — NO trading-code/parity change, NO schema, NO restart): NEW `scripts/backup_retention.py` = a category-aware keep-N reaper replacing the old 02:00 `find -mtime +7 -delete` (which was pattern-scoped to the DAILY backups only → the `pre_*` deploy/ad-hoc backups grew UNBOUNDED, 7 files/~794 MB). Keep newest **pre_*=20 / trading_system-*.db=14 / analytics-*.db=14**, ordered by mtime. **SAFETY (a deletion job):** DRY-RUN BY DEFAULT (--apply required; cron uses --apply); NEVER-DELETE-NEWEST (always retains newest N); SCOPED globs only — realpath-asserted inside data_store/backups, symlinks refused, any file matching NO category refused+logged (never wildcard/recursive); SANITY CAP (>10 deletes ABORTS + Telegram, override `--max-delete N`); idempotent (stateless keep-N); locked/missing files skipped (retry next run). Monthly category DROPPED (no producer). Audit = LOG-ONLY (one structured line, no table) + `record_heartbeat("backup_retention")` so the Control Tower sees it. Cron registry `backup_retention` repointed (shell `find`→python `scripts/backup_retention.py --apply`, monitored:true); canonical regenerated (still 43 lines, deterministic, 24 registry tests green). VM dry-run @current counts (pre_*=7/daily=8/analytics=8, all under keep-N) = 0 deletions. No retention gap (keep-14 daily is MORE generous than the old 7-day find). 10 tests `tests/unit/test_backup_retention.py` (keep-N + newest-preserved, dry-run vs apply, sanity-cap abort+override, unmatched/symlink refusal, locked/missing-file). [prev: T1 DISK-METRIC REPAIR (DEPLOYED 29-Jun; collector-cron ONLY — NO trading-code/parity change, NO schema, NO restart): `scripts/capture_metrics_baseline.py::_get_disk_used_pct()` now uses stdlib `shutil.disk_usage(_ROOT)` (round used/total*100,2) instead of `psutil.disk_usage()` — psutil is ABSENT from the VM venv, so the old path returned the −1.0 ImportError sentinel for the metric's whole life (582/582 `system_metrics` rows since 19-Jun in analytics.db; daily `_stats` filtered <0 → 0.0). ONLY the disk fn changed; cpu/mem/fds keep their psutil −1.0 fallbacks (separate ticket, intentionally untouched); db/log sizes (pathlib) unchanged. The repaired metric would have REVIVED `_check_drift`'s disk branch (hist_disk now truthy) → that branch is now GATED OFF (`if False and …` + comment: disk alerting is owned by `disk_monitor.py`, the hourly shutil authority). Blast radius was a CLEANLINESS fix only — `disk_monitor.py` (real disk-full guard: shutil + warning/critical thresholds + cleanup + Telegram) and the Control Tower (size_logger fresh-shutil → control_tower_trends) were always INSULATED; no disk-full condition went undetected. Historical −1.0 rows left as-is (no backfill). 27 tests `tests/unit/test_capture_metrics_baseline.py` (disk valid %; db/log unchanged regression; disk-drift inert). Lands on the next */5 collector run during market hours. [prev: T4 TIMEZONE OPERATOR TOOLS (DEPLOYED 29-Jun; operator-tooling ONLY — NO trading-code/parity change, NO restart, the service imports none of these): root cause of the 29-Jun "13:47 IST when it was 19:22" mistake = Git Bash/MSYS2 has NO IANA zoneinfo ⇒ `TZ='Asia/Kolkata' date` silently returns UTC (off 5:30). App + VM were NEVER at risk (`core/time_authority.now_ist()` = fixed +5:30 `timezone`, zoneinfo-free, no DST in India; VM NTP-synced). LEAN fix = 3 standalone operator scripts: `scripts/ist_now.sh` (authoritative IST via now_ist(), never `date`), `scripts/check_tz.sh` (loud self-check — FAILS on this MSYS2 PC BY DESIGN, proving the guard; diagnostic only), `scripts/deploy_preflight.py` (run BEFORE any market-window-gated action: anchors on `date -u` UTC + the VM — PC↔VM UTC agreement ≤120s + VM-AUTHORITATIVE holiday-aware `is_market_open` gate that refuses a mid-session deploy; the C guard is surfaced for visibility but NEVER hard-blocks since this PC always fails it). **RULE: never `TZ='Asia/Kolkata' date` in Git Bash → use `scripts/ist_now.sh` / `now_ist()` / `date -u` (UTC) / `TZ='IST-5:30' date`.** 9 tests `tests/unit/test_t4_deploy_preflight.py` (evaluate() pure logic + bash-script behaviour). NO schema, NO cron, NO config-outside-scope. SKIPPED (out of scope): the 8-objective framework, a Control Tower timezone_integrity check, a new PC→VM channel. [prev: TELEGRAM ALERT UX (DEPLOYED post-close 29-Jun; rebased onto Control Tower; NO DB schema; cosmetic alert-text only, paper+live parity via the shared notifier body): (1) Direction (LONG/SHORT) is the FIRST body line on INTRADAY SIGNAL (`signals/signal_processor.py::_emit_signal_alert`) + ORDER PLACED (`orders/order_placer.py::_format_order_placed_body`, NEW pure staticmethod extracted from `place()`). (2) EOD DAILY SUMMARY redesign (`orders/eod_squareoff.py::_format_summary_body`, NEW pure staticmethod extracted from `_send_daily_summary`; the empty-trades branch is unchanged): Best/Worst now carry `DIRECTION | exit_reason`; NEW `🟢 Long Trades`/`🔴 Short Trades` split (counts + W/L + P&L from REAL trade directions, totals the overall W/L); per-strategy `🟢`/`🔴` (long first) + per-trade detail sub-line `STOCK - DIR - ₹entry - ₹exit (SL|TGT|EOD|MAN|TO|CB) - ±₹pnl`. Helpers `_disp_dir`/`_exit_tag`/`_summary_entry_px`. P&L/win-rate/AvgR/capital/SmartTGT formats UNCHANGED. Rama-approved render; validated offline vs REAL 25-Jun (Long −₹9.08 / Short +₹8.75 — confirmed 4-LONG/1-SHORT). Tests `tests/unit/test_alert_direction_eod_29jun.py` (11); full run w/ test_eod_squareoff+test_signal_processor+test_fix129 = 151 passed / 0 regressions. Branch `telegram-direction-eod-redesign-29jun` rebased onto Control Tower (only the 2 doc headers conflicted; the 3 code files applied clean). [prev: CONTROL TOWER Phase 1 COMPLETE (1a→1d, DEPLOYED 29-Jun): a READ-ONLY ops aggregator (`ops/control_tower/`) over the existing monitors — it never blocks/acts on trading. **1a** schema **v39→v40** (+5 PURE-ADDITION tables control_tower_findings/runs/trends/status/freshness) + the security `last_run.json` status write + a daily size-logger (FRESH `shutil.disk_usage`, not the broken `system_metrics.disk_used_pct`). **1b** the aggregator (`run_aggregation`) — 4 read-only adapters (security last_run.json / cron heartbeats+markers / `config_auditor` BLOCK·WARN / `excursion_reconstruction_runs`) + a per-stage data-FRESHNESS engine (candles/signals/orders/executions/reconstruction/eod; NA-before-due ⇒ never a false finding) + disk/backup-growth findings, each UPSERTed on a (category,resource_name,reason) dedup identity. **1c** findings lifecycle (auto-resolve with a RAN-category guard so a check that errored can't falsely resolve its findings + re-occurrence reopen), a weighted health score (C100/H50/M20/L5 → HEALTHY≤50/WARNING≤150/ATTENTION≤300/else CRITICAL; OPEN-only, ACK-suppressed), a per-run_date status roll-up (+`last_successful_run`), the reporter (noise-controlled Telegram DELTA — CRITICAL always-unless-ACKED, HIGH only if NEW/reopened, MED/LOW/INFO NEVER — via the REAL `TelegramNotifier` + a pull HTML/CSV report `data_store/control_tower/report_<date>.*`), and the `ct list|ack|unack` ACK CLI. **1d** ONE daily runner `ops/control_tower/runner.py` (cron **17:05 Mon-Fri**, SUPERSEDES the standalone size-logger): size-logger FIRST (seeds today's trend size cols) → aggregator (detection→lifecycle→health→status→reporter); **SKIPS non-trading days** with a SKIPPED heartbeat (honours no-alerts-on-non-trading-days — else the freshness engine false-flags the legitimately-absent EOD/candle stages on a weekday holiday; `--force` bypasses for previews); a `self_job` param excludes the IN-FLIGHT runner from its OWN cron freshness check (its completion heartbeat is written post-run; check_cron_drift @18:00 is the external monitor). Registry job `control_tower` (monitored:true, marker+heartbeat); canonical regenerated (43 command-lines, `--selftest` byte-round-trip green). Plus the daily_report OBSERVABILITY source-fix: `reports/daily_report.py` now records `cron_heartbeat("daily_report")` on a successful xlsx write (closes the 23-Jun false "daily_report missed" MEDIUM at the source). Severity maps live reviewable in `severity.py` (security WARNING→MEDIUM = dynamic-IP login fatigue; genuine threats already emit CRITICAL). PARITY: runner + Telegram are mode-neutral (read-only DB aggregation, no broker session). Tests `tests/unit/test_control_tower_phase1{a,b,c,d}.py`. Deferred T1/T2/T3 (NOT in Phase 1): disk-metric repair, a retention job, weekly advisory scanners (the Sun-18:10 slot). [prev 28-Jun: MFE/MAE Option B (DEPLOYED 28-Jun): root-caused the 3+day-empty `trade_excursions` — `candles.ts` (space/naive, from the 15:40 EOD `fetch_daily_candles`) vs `trades.*_time` (ISO-T +05:30) were compared LEXICALLY in `StateStore.compute_trade_excursions` → ' '<'T' at idx10 matched ZERO candles → None → no insert (silent DEBUG swallow); the compute also ran on the hot INTRADAY-EXIT path, before candles exist. FIX (permanent, paper+live parity, NO realtime tracker / NO candle-format change / `_persist_candle` untouched): (1) DATETIME-SAFE compare — parse both sides to tz-aware IST (`_parse_ist_dt`), pre-filter the indexed generated `date` col, storage format UNCHANGED, **direction math VERBATIM**; (2) moved OFF the exit path (removed the dead compute + silent swallow from `order_placer._handle_exit_fill`) into NEW post-EOD `scripts/reconstruct_excursions.py` (PID lock-file + cron_heartbeat + `ensure_candles` fetch-if-missing via the token-file kite = paper+live parity + `INSERT OR REPLACE` idempotent/resume-safe; modes `--daily/--date/--all-closed/--dry-run`); (3) OUTCOME TAXONOMY WRITTEN / WOULD_FETCH (dry-run-only preview) / SKIPPED_UNRECONSTRUCTABLE (NULL-exit OR sub-minute<1m-candle) / FAILED (transient → CRITICAL sentinel), HARD GUARD `written+would_fetch+skipped+failed==examined`, + `excursion_reconstruction_runs` audit table (schema **v38→v39**, PURE addition, proven 38→39 on the 158 MB copy). New cron `reconstruct_excursions --daily` **15:50 Mon-Fri** (gated on the new `fetch_daily_candles` success marker, self-heals via ensure_candles). **MFE/MAE SIGNED convention** (mfe may be NEGATIVE if never-favourable / mae positive if never-adverse — full information, NOT floored; 1-min reconstruction so sub-minute trades get no row). Validated on a live-DB COPY (branch never touched live): 25-Jun DYCL LONG + BANDHANBNK SHORT mfe/mae EXACT vs manual candles, idempotent (5→5); 3A `--all-closed` dry-run reconciliation = 46 (22 would_write + 19 would_fetch[17-18 Jun] + 5 skipped[3 NULL-exit SULA/GICRE/AGARIND + 2 sub-minute RCF 15.6s/NOCIL 2.35s] + 0 failed). Full unit suite green (only 2 fails were v38→v39 version-pins, updated). DEPLOYED to main 28-Jun (branch `mfe-mae-reconstruction-28jun`); schema applied via a CONTROLLED one-shot StateStore open in the Sunday non-market window (NOT the Mon 08:15 auto-boot). 3B/3C historical backfill DEFERRED → next trading day + live token (expect written=41 / skipped=5 / failed=0 / `trade_excursions`=41). [prev 27-Jun: SNR-V2 Phase B (Structure-Aware Exit, stacked on Phase A): NEW `orders/structure_exit_manager.py` (`StructureExitManager`) subscribes CandleStore 1m closes and, for each OPEN **MIS LIMIT_TRIPLE** trade, reads the same warmed ZoneCache (LONG→`.support`, SHORT→`.resistance`, HIGH-confidence only): **ACTION B (break, evaluated first)** — a 1m CLOSE beyond the nearest band + a STRONG close (LONG support break = strong LOWER close via `retest_confirm._strong_close(is_long=False)`; SHORT resistance break = strong UPPER) → EXIT in the MANDATORY order `status=EXITING` → cancel resting SL+TGT → reverse-aware marketable-LIMIT flatten (MARKET fallback), inheriting the reconciler/G5b EXITING-exclusion so no spurious SL re-arm; **ACTION A (else, trail)** — move the SL to `support.band_low×(1−sl_buffer_pct/100)` (LONG) / `resistance.band_high×(1+…)` (SHORT), tick-snapped, with TWO real **pre-modify** guards (ONLY-TIGHTEN + WRONG-SIDE vs LTP — root-cause, NOT broker-rejection reliance; mirrors `smart_tgt_manager`), LIMIT moved with the trigger (`calc_sl_limit_price`), debounced to **at most one move per (trade, candle)**. Zone selection keys on the candle EXTREME (high/low) not close, so the breaking candle still selects the zone it broke. CO (SL in bracket) + CNC (SL in OCO GTT) EXCLUDED; **SINGLE SL OWNER** — do NOT co-enable `strategy.trailing_sl_enabled` (BreakevenManager stays dormant). Shares the ZoneCache/ZoneWarmer infra (built when EITHER retest OR structure-exit is on). **NO schema change** (stateless: EXITING covers in-flight exits durably; the per-candle debounce map is in-memory and re-derives on boot). ONE default-off master flag `system.structure_exit.structure_exit_enabled` (off ⇒ not constructed, no subscribe ⇒ byte-identical; paper==live parity proven against the real adapter split). 23 new tests (`tests/unit/test_structure_exit_manager.py`); full unit suite green / 0 NEW regressions. branch `snr-v2-phaseB-27jun` (on Phase A), NOT pushed; PAUSED for review. [prev 27-Jun: SNR-V2 Phase A (WAIT_FOR_RETEST entry side, stacked on V1) — now SYMMETRIC LONG+SHORT: a LONG entry inside a HIGH **resistance** zone (or a SHORT entry inside a HIGH **support** zone) is DIVERTED **before** placement (in `signal_processor._process_one`, after side, before sizing/reserve — holds NO capital) into a restart-safe `RetestMonitor` (`screening/retest_monitor.py`) that polls 1m and runs ONE PURE direction-parameterised state machine (`sr_detector/retest_confirm.py`: LONG breakout-above→retest-touch→reclaim-strong-UPPER-close→CONFIRMED / SHORT breakdown-below→retest-touch→rejection-strong-LOWER-close→CONFIRMED; timeout or wrong-way max_away→REJECT [LONG break_down / SHORT reclaim_up]); on CONFIRM `signal_processor.continue_from_retest` (sibling of continue_from_gate; NO 60s expiry) reserves + places a **MARKET** entry (side from `parked.direction`) with a STRUCTURE SL on the far side of the band (LONG below band_low / SHORT above band_high) + R:R-preserving TGT (direction-aware `_derive_target`). New `sr_detector/{zone_cache,zone_warmer,zone_builder(DRY-shared with V1),retest_confirm}.py`; ZoneCache is the SYNC hot-path read (miss→fall through), caches BOTH resistance+support, warmed by a daemon ZoneWarmer enqueued on signal arrival. MARKET route = `entry_order_type` (default LIMIT → all existing callers byte-identical) threaded `place→engine→protocol→adapter` (adapter already supports MARKET; paper synth fills at LTP = parity). Schema **v37→v38**: +`retest_state` (pure add) + signals.status CHECK widened `GLOB 'RETEST_*'` (signals rebuilt, MIGRATION_TABLES[38]; FK-safe; .backup-tested v37→v38 CLEAN). ONE default-off flag `system.sr_detector.wait_for_retest_enabled` covers BOTH sides (off ⇒ nothing constructed ⇒ byte-identical; paper+live parity). Config keys made SIDE-NEUTRAL pre-merge (one knob each side): `near_resistance_buffer_pct`→`near_zone_buffer_pct`, `reclaim_strong_close_frac`→`confirm_strong_close_frac` (+ RetestParams field). NO schema change (direction already persisted in retest_state.direction + ParkedCandidate). Phase B (SL-reposition/structure-break exit) PARKED. 55 SR-V2 tests (every LONG branch unchanged + full SHORT mirror); full unit suite green / 0 NEW regressions (31 pre-existing PC-env: test_main 23 + TOTP/NTP 8, identical to V1). branch `snr-v2-phaseA-27jun` (on V1), NOT pushed; schema AWAITING Rama nod. [prev 27-Jun: SNR-DETECTOR-V1 (shadow support/resistance detector): NEW pure package `sr_detector/` (fetch→pivots→zones→confluence→flags→shadow-log) wired as an ASYNC NON-GATING observer that runs ONLY after a successful `place()` in `signals/signal_processor` (BOTH `_process_one` + `continue_from_gate`); enqueues + returns immediately on a single background worker — never blocks/delays/raises into placement. +`sr_detector_results` table (schema **v37**, pure addition, .backup-tested v36→v37 clean) + DAO. ONE default-off master flag `system.sr_detector.enabled` (paper+live parity; disabled ⇒ detector not constructed = zero pipeline change). The rate-limited historical fetch is a closure in `main.py` over (market-data kite handle + `rate_limiter.acquire("historical")`) — adapter UNTOUCHED; `sr_detector/` imports only core/+stdlib. EOD outcome backfill `scripts/sr_detector_backfill.py`; corp-action spot-check `scripts/sr_corp_action_spotcheck.py` (VM/live). SHADOW-only: no reject/veto/SL/TGT/STM, no entry change. 37 new tests; full unit suite 3869 pass / 0 regressions (31 pre-existing PC-env fails unchanged: test_main 23 + TOTP/NTP ~8). branch `snr-detector-v1-27jun`, NOT pushed; schema AWAITING Rama nod before live apply. [prev 26-Jun: SLICE2.5-PHASE-4 (completes Slice 2.5 build side): the trade_type reject-by-intent gate ALREADY existed (Slice 2 strategy_will_trade LAYER 1×2, both signal_processor paths, pre-sizing); Phase 4 = CONFIRM + split a DISTINCT "TRADE_TYPE" reject label (→ REJECTED_TRADE_TYPE) via a machine-readable Verdict.cause (control.py; gate maps cause==TRADE_TYPE→label, others stay STRATEGY_CONTROL; reason strings + accept/reject logic UNCHANGED) + go-live scenario tests. Dormant (INTRADAY+force=true→coerced→accept→split never fires). Option B (declared-intent gating) PARKED. No schema. DEPLOYED to main 37b3db3 26-Jun ~13:16 in a one-time authorized batch with Phase 3 + the FIX-183 deploy-log (standing Rama-owns rule restored); restart self-exited 0 at the Muharram holiday guard, real boot Mon 29-Jun 08:15; verified broker-session-free PASS — fully DORMANT (delivery_enabled=false, conditional_allocation_enabled=false, trade_type=INTRADAY, force_intraday_only=true → fixed 70/30, delivery caps inert, trade_type gate no-op, Auditor 0 BLOCK). The ENTIRE Slice 2.5 build side is now live + dormant; remaining before activation = T2 + C1-watch (Monday). [prev: 2026-06-26 FIX-183 orphan-GTT ADOPTION DEPLOYED af4b784; 2026-06-25 SLICE2.5-P2 durable CNC overnight-GTT schema v36 DEPLOYED bad0aad]]]]]]]]]]]]]]]]]]]]]]]
# ⚠️ READ THIS BEFORE TOUCHING ANYTHING ⚠️

This is the single authoritative path/ops reference. It complements (does not
replace) the design/runbook docs in `docs/` — see "Related Docs" at the bottom.
For a one-screen quick reference, see [`/PATHS.md`](../PATHS.md).

## V3 SHARED-ENGINE — Common Utilities substrate (Phase-0 map + Step-1 build, 11-Jul-2026)

Phase-0 gap analysis: `docs/v3/V3_PHASE0_INVESTIGATION_GAP_ANALYSIS.md` (Common Utils + 03.01–03.09, ADAPT/NEW vs `main`@`8116b74`). The V3 "Common Utilities" tier is a **LOGICAL grouping of EXISTING pure helpers (reused as-is, NOT moved/rewritten)** plus two genuinely-new pure helpers built in Step 1. No existing helper was relocated (avoids churn/regressions).

**Substrate = REUSE (do NOT rebuild or duplicate):**
| Helper | Path | What |
|---|---|---|
| Price/tick/SL/TGT/clamp math | `orders/price_math.py` | pure Decimal math (`round_to_tick`, `calc_sl_price`, `calc_tgt_price`, `marketable_limit_price`, `clamp_exit_into_band`) |
| Shared OHLCV candle type | `sr_detector/models.py::Candle` | the ONE candle value object (frozen; `ts,open,high,low,close,volume`; `from_kite`) — reused everywhere, no parallel type |
| Swing-pivot detection | `sr_detector/pivots.py::find_swing_pivots` | pure local-extreme swings |
| Live 1m candle builder | `data/candle_store.py::CandleStore` | stateful LTP→OHLC (not pure; has clock thread) |
| IST clock | `core/time_authority.py` | `now_ist`/`today_ist`/`ist_timezone` |
| Sessions/holidays | `core/market_windows.py::MarketWindows` | injected-config trading-day/session/EOD authority (layer-1, stdlib-only) |
| Logging | `core/logger.py` | JSON-lines logging facade |
| Config reader | `core/config_loader.py::load_all` | single-source `AppConfig` (extra="forbid") |

**NEW (Step 1 build — `core/candle_math.py`, a domain-agnostic layer-1 pure module):**
- `true_range_series(candles) -> list[float]` + `atr(candles, period, method="wilder"|"sma") -> float|None` — Wilder's smoothing (default) or SMA of true ranges; the degenerate first TR (no prev close) is excluded, so the data requirement is `period+1` candles → fewer returns **None** (never fabricated). `period` is a parameter (no baked-in default). Fills the Phase-0 gap: ATR previously only consumed pre-computed; ATR-based SL/TGT was unimplemented (FIXED_PCT fallback).
- `resample(base_candles, target_tf, session_bounds) -> ResampleResult` — base→higher timeframe (int minutes, or `"day"`). Open=first/High=max/Low=min/Close=last/Volume=sum; ts=bucket START; buckets anchored to each date's **session open via injected `session_bounds`** (never hardcodes 09:15; no cross-day merge; pre-open/after-hours excluded). Honest flags on `ResampleResult`: `empty_bucket_starts` (gaps, not fabricated) + `partial_bucket_starts`/`partial_last` (session-tail bucket, e.g. NSE 375-min day's 15-min tail on a 30m grid — emitted from data present, never padded).
- **Placement rationale (R2/R3):** put in `core/candle_math.py` (domain-agnostic, NOT in `price_math.py` [order math] or `sr_detector/` [S&R-specific]). `atr`/`true_range_series` reference no project module. `resample` is the only Candle constructor and imports the shared `Candle` via a **function-local** `from sr_detector.models import Candle` — mirroring the established `# local import avoids cycle` pattern in `core/market_windows.py` — so `core.candle_math` has **no import-time dependency on `sr_detector`** (verified: importing `core.candle_math` does not load `sr_detector`; layer inversion avoided).
- **Wiring: NONE yet.** Step 1 is build + unit-test only; nothing in the live trading/scoring/sizing/S&R path consumes these. Later modules (03.01 S&R, 03.02 Regime, 03.06 Sizing) wire them in and own the config defaults. **No existing file modified; no live behaviour change; paper==live by construction (pure, no mode branch).**
- **Tests:** `tests/unit/test_candle_math.py` (31 = 26 + 5 `session_vwap`: hand-computed TR/ATR Wilder+SMA + VWAP, insufficient→None, determinism, invalid-param raises; resample 1m→5m / 5m→30m OHLCV, session alignment, partial-tail flag, empty-bucket flag, no cross-day merge, pre/post-hours exclusion, daily, tz preservation, MarketWindows-derived bounds). sr_detector regression 64 pass; main suite collects 4448 clean. **BUILT-but-UNPUSHED** (Rama pushes off-market) — see UNPUSHED_PENDING_DEPLOY_LEDGER. memory `v3_step1_common_utils_11jul`. (Step 2 added `session_vwap`.)

## V3 SHARED-ENGINE — 03.01 S&R Detection ADAPT (Step 2, 11-Jul-2026, SHADOW, default-OFF)

ADAPT of the existing `sr_detector/` observer (no parallel module). Adds Layer-A VWAP/ORB anchors, confirms Layer-B 30m/1h structural swings with role labels, exposes `confidence_class` (default ANCHOR_ONLY), and a manual-marking level EXPORT. **Stays shadow/observer-only — gates NO capital.** All net-new behaviour is gated by ONE default-OFF flag so the live shadow row + the 64 existing sr_detector tests are byte-identical.

- **VWAP + ORB anchors (Task 1):** `sr_detector/zone_builder.py` — `intraday_anchors(fine_candles, on_date, session_open, session_close, orb_window_minutes)` = VWAP (via new `core.candle_math.session_vwap`) + ORB high/low (max/min over `[session_open, +orb_window)`); `build_anchor_payload` merges these with the EXISTING PDH/PDL/PDC (`prior_day_levels`) + `round_number_levels`. Session bounds are INJECTED from `MarketWindows` (main.py) — never hardcoded. Today-only fine fetch via new `OhlcFetcher.fetch_interval(symbol, interval, lookback_days)` (`fetch_timeframes` unchanged).
- **Structural TF policy (Task 2):** `sr_detector/models.py::TF_ROLE` = {30minute→PRIMARY, 60minute→MAJOR, day→DAILY, 15/5minute→MINOR/MICRO}; `zone_builder.build_swings_payload(scored_zones)` groups the existing confluence-scored swings by role. Structural swings already computed on the fetched 30m/60m (confirmed) — this labels them for validation.
- **confidence_class (Task 3):** `SRAnalysis.confidence_class` (additive field) = `CONF_ANCHOR_ONLY` (default) | `CONF_ANCHOR_PLUS_VALIDATED_SWINGS`. **Never flips to VALIDATED in this step** — the flip is a later, human-gated action after the manual-marking validation passes. The 03.03 R:R gate will use ANCHOR-only levels for live capital until then.
- **Level export (Task 4):** `scripts/sr_level_export.py` — READ-ONLY; dumps anchors + swings (with TF/role + confidence) per symbol×date to a flat CSV Rama compares against his manual chart marks. Pure core (`flatten_analysis`/`rows_to_csv`/`export_levels`, injected analyze fn) is unit-tested; `main()` wires a read-only market-data kite + detector (anchors on) per as-of date. `python scripts/sr_level_export.py --symbols RELIANCE,TCS --dates 2026-07-10`.
- **Config (`core/config_loader.py` + `system_config.yaml`):** `sr_detector.intraday_anchors_enabled` (default **false** — master gate for the anchor fetch/emission), `anchor_intraday_interval` (5minute, validated intraday-only), `anchor_lookback_days` (1), `orb_window_minutes` (15). `enabled` stays true (V1 shadow armed). Flip `intraday_anchors_enabled` true AFTER the export validation passes.
- **Persistence:** the anchor/swing/confidence_class payload folds into the EXISTING `confluence_evidence` JSON column (`insert_sr_detector_result` reads only `_SR_INSERT_COLS`, ignores extras) — **NO schema change**. When the gate is off, the row is byte-identical to before.
- **Wiring:** `sr_detector/detector.py::analyze` (gated enrichment + `_build_anchors`), `_write` (gated evidence fold-in); `build_sr_detector`/`SRDetector` gain optional `session_open`/`session_close`; main.py passes `market_windows.market_open/close`. `intended_entry` is the round-number/nearest reference.
- **R4 regression:** the 64 sr_detector tests PASS UNCHANGED; **clean-tree vs dirty-tree suspect-file failures are IDENTICAL** (proved via stash-diff) → zero new failures; the 41 full-suite failures are the pre-existing PC-env baseline (test_main/fix061/phase17, green on VM). Changed-area set 148 pass (sr_detector 64 + sr_v2 37 + candle_math 31 + sr_anchors 12 + sr_level_export 4). Paper==live (pure, no mode branch). **BUILT-but-UNPUSHED.** Tests `tests/unit/test_sr_anchors.py` (12) + `test_sr_level_export.py` (4). memory `v3_step2_sr_detection_11jul`.

## V3 SHARED-ENGINE — 03.02 Market Regime engine (Step 3, 11-Jul-2026, NEW, SHADOW, default-OFF)

Pure NEW package `regime/` (Phase-0 grep negative confirmed). Emits a THREE-AXIS index-level regime (direction / volatility / day-type), each with an ordinal confidence → preference multiplier, plus a positive-confirmation-only extreme flag. **SHADOW — gates NOTHING** (no order/score/size/kill-switch); fail-safe by construction (a broken regime NEVER halts the book). Reuses Common Utilities + the existing OHLC fetch; no duplicated ATR/candle/session/calendar/fetch logic.

- **Index data (TASK 0):** the NIFTY index is **NOT in the instrument cache** (`instruments.csv` has no index rows; token 256265 lives only in test fixtures). Reused the existing rate-limited fetch closure via new **`OhlcFetcher.fetch_by_token(token, interval, lookback_days)`** (skips the symbol→token cache) with the index token+symbol from **config** (`regime.index_token` 256265 / `index_symbol` "NIFTY 50"). NOT a new data path — the same `_make_sr_fetch_fn` closure. **⚠️ VM must verify live index historical-data access before flipping `regime.enabled`; fail-safe covers absence (→ UNKNOWN).**
- **New pure indicators (`core/candle_math.py`):** `ema_series`/`ema` (SMA-seeded), `adx` (Wilder, reuses `true_range_series`; needs ≥2·period+1 candles → None). Live with `atr`/`session_vwap` in the substrate (reused, not duplicated).
- **Axes (`regime/engine.py`):** DIRECTION (BULL/BEAR/SIDEWAYS) = price vs EMA50/EMA200 + EMA slope + HH/HL via `find_swing_pivots` + ADX, confidence from sub-signal agreement (≥4 & trending → HIGH). VOLATILITY (HIGH/NORMAL/LOW) = cur ATR / baseline TR ratio vs config bands. DAY-TYPE (TREND_DAY/RANGE_DAY/UNDETERMINED) = intraday range/ATR + ADX + one-directionality + position-in-range; **early session → UNDETERMINED LOW** (session_progress from MarketWindows). Confidence rises with session progress.
- **Ordinal confidence → multiplier (`regime/models.py`):** HIGH 1.0 · MEDIUM 0.5 · LOW 0.2 · TRANSITION 0.0 (`CONFIDENCE_MULTIPLIER`). `RegimeState.preference_multiplier` = the direction axis (headline); each axis carries its own. Ordinal FIRST (no faked % precision).
- **Extreme flag (TASK 5):** TRUE **only** on a CONFIRMED halt/circuit from the injected `exchange_status_fn` (positive confirmation). A **MISSING feed → FALSE + CRITICAL log** (absence ≠ halt). **Emitted + logged but NOT wired to stop trading** (03.03 consumes it later).
- **Fail-safe (TASK 6):** missing/insufficient index candles → `unknown_state` (status UNKNOWN, every axis neutral, multiplier 0 — the book trades normally, NOT a stop). Per-axis error → most-uncertain value at TRANSITION. Any exception in `compute()` → UNKNOWN + neutral (never raises).
- **Shadow runner (`regime/runner.py`):** `MarketRegimeShadowRunner` computes once per cycle during market hours, logs, and persists the latest to `data_store/regime/regime_state.json` (best-effort; for later prior_day_regime). Default-off.
- **Config:** `system_config.yaml regime:` block + `core/config_loader.py::RegimeConfig` (`enabled: false` default; index token/symbol, EMA/ADX/ATR periods, vol ratios, thresholds; validators: intraday interval + ema_fast<ema_slow). Registered on SystemConfig.
- **Wiring (`main.py`, default-OFF):** built + started only when `regime.enabled` (shares `_sr_fetch_fn`; own OhlcFetcher; `exchange_status_fn=None`); stopped in `_shutdown`. Inert by default → no live behaviour change.
- **R4 regression:** regime is NEW (no callers). Existing touches: `main.py` (dormant block + shutdown hook), `core/config_loader.py` (+RegimeConfig), `sr_detector/fetch.py` (+fetch_by_token, `fetch_timeframes`/`fetch_interval` unchanged), `core/candle_math.py` (+ema/adx), `config/system_config.yaml` (+regime block). **test_main clean-tree vs dirty-tree failures IDENTICAL (26==26, stash-diff) → zero new failures.** Spec tests `tests/unit/test_market_regime.py` (T1–T8 + fail-safe + determinism + runner, 13) + candle_math +9 (ema/adx). Paper==live (pure, injected collaborators, no mode branch). **BUILT-but-UNPUSHED.** memory `v3_step3_market_regime_11jul`.

## V3 SHARED-ENGINE — 03.03 Hard-Gate + 03.04 Scorer re-scale (Step 4, 11-Jul-2026, PLAN ONLY — no code)

Design doc: **`docs/v3/V3_STEP4_HARDGATE_SCORER_PLAN.md`** (P1-P8). **No code/config/behaviour changed; ledger untouched.** Plan for the coupled change: extract `circuit_check`+`signal_age` (`step_executor` steps 9&10) into a NEW pre-scoring `screening/hard_gate.py` at the SINGLE live seam `secondary_screener.screen()` (only caller `signal_processor.py:778`; built once `main.py:2447-2455`); `step_executor`→8 steps (`scoring_weights.yaml` Σ80). **Re-scale is THRESHOLD-ONLY** — re-weight to Σ100 is scale-invariant under the proportional scorer (`total=Σachieved/Σweights_present×100`), so recalibrate thresholds instead: analytic min_pass 60→50 / medium 65→56 / high 80→75 (age≤30s survivor reference; circuit=10 constant for survivors, age_c∈{5,10}; map `new=1.25·old−12.5−1.25·age_c`). **Exact parity IMPOSSIBLE** (signal_age was folded into the OLD pass decision) → residual = the age 30-60s band (intended, freshness now a binary gate) + at-circuit-but-passed→now-gated (≈0). **Parity proof = deterministic OFFLINE recompute from the persisted `screener_results.step_results_json`** (classify UNCHANGED/FLIP/TIER-SHIFT/NOW-GATED; accept iff flips ⊆ explained) + live shadow-compare. Whole change behind default-OFF `scoring.v3_hardgate_mode: off|shadow|enforce` (off = byte-identical). Gate module scopes rules LIVE (circuit/proximity/freshness/liquidity) vs PLAYBOOK-shadow (confirmation/pullback/R:R/strong-HTF/extreme; must-have fail-CLOSED, must-not-have fail-OPEN; R:R ANCHOR-only). Consumers of `screener_results.score`: reports (read from config snapshot → auto-follow; `min_pass_score=60` build-gate test needs update), GUI (display only), config_auditor (only B4 rule → unaffected). OPEN Qs for ChatGPT: final thresholds (fresh vs conservative), accept residual, freshness cutoff (60/90/600s ambiguity), re-scale per-strategy `min_score` overrides. memory `v3_step4_hardgate_scorer_plan_11jul`. NEXT = review → Step-4b implements.

**STEP 4b IMPLEMENTED (11-Jul, behind default-OFF flag; STOPPED AT OFF).** NEW `screening/hard_gate.py::HardGate` (at-circuit + circuit-proximity[RELOCATED shared `circuit_proximity_reason`; `secondary_screener._circuit_proximity_reason` delegates] + freshness 60s; liquidity NO-OP) invoked in `secondary_screener.screen()`. **`scoring.v3_hardgate_mode: off|shadow|enforce` (default `"off"` = BYTE-IDENTICAL** 10-step path; quoted — YAML reads bare off as False). THRESHOLD-ONLY re-scale: `step_executor.run_all(exclude_steps=…)` (8 steps Σ80 in v3), shared `rescaled_total`/`tier_for`/`rescale_min_score` in hard_gate.py (quality_scorer UNCHANGED), v3 thresholds are SEPARATE keys `v3_min_pass 50 / v3_medium 56 / v3_high 75` (OLD 60/80/65 untouched → reports/GUI/tests unaffected). ENFORCE = gate-first + 8-step + v3 thresholds + no age-defense; SHADOW = live follows OLD + logs OLD-vs-NEW. A7: all 15 `config/strategies/*.yaml min_score`=0 (re-scale inert). A9: high>med>min invariant in `config_loader.ScoringConfig` model_validator + `config_auditor` B5. Offline parity tool `scripts/v3_hardgate_parity_recompute.py` (from `screener_results.step_results_json`; UNCHANGED/FLIP_PASS/FLIP_FAIL/TIER_SHIFT/NOW_GATED + explained-residual + data-fit) — synthetic-tested; **BINDING real-data run PENDING on a VM backup** (0 local rows). G1 off byte-identical ✓; G3 full-suite 4459 pass/41 pre-existing PC-env fail, test_main stash-diff 26==26 → zero new; tests `test_hard_gate.py`(18)+`test_v3_hardgate_parity.py`(6). paper==live. **BUILT-but-UNPUSHED; no live behaviour changed.** memory `v3_step4b_hardgate_scorer_impl_11jul`. NEXT (Rama/off-market after VM parity sign-off): push → shadow soak → enforce.

**G2 BINDING PARITY DONE (12-Jul) — PARITY_OK.** Artifact `docs/v3/V3_STEP4B_PARITY_ARTIFACT.md`. Ran `scripts/v3_hardgate_parity_recompute.py` over **114,807 real screener_results rows** (slim read-only extract from VM backup `trading_system-2026-07-11.db`, 12-Jun→10-Jul; live DB never touched). **@50/56/75 (data-fit selected exactly this): UNCHANGED 114,693 (99.90%) · FLIP_PASS 104 (age-band, A2-accepted) · FLIP_FAIL 10 (rounding-boundary, 0.009%) · TIER_SHIFT 0 · NOW_GATED 0 · UNEXPLAINED 0 → PARITY_OK.** Harness cross-check: recomputed OLD total == stored score on all scored rows (0 mismatch). **2 tool bugs root-fixed:** column is `step_results` not `step_results_json`; RECOMPUTE OLD at current 60/80/65 (stored status used the 55-era min_pass [06→10-Jul] → 23k false flips cut to 10). New tests locked both. Discrepancy note: the script is dev-tree-only (built-but-unpushed) — not on the VM where ChatGPT looked; local dev DB has 0 rows. **Flag STILL off; nothing flipped/pushed.** memory `v3_step4b_parity_artifact_12jul`. NEXT: ChatGPT/Web Claude sign-off → push → shadow → enforce.

## V3 SHARED-ENGINE — 03.06 Risk & Position-Sizing (Step 5, 12-Jul-2026, VERIFY + inert scaffold; LIVE byte-identical)

Gap-map + review doc: **`docs/v3/V3_STEP5_RISK_SIZING_GAPMAP.md`**. 03.06 is essentially COMPLETE in the existing `capital/position_sizer.py` (risk-based `min(risk,cap,conc)` from `sl_distance=abs(entry-sl)`, tier ON/OFF HIGH1.0/MED0.7/LOW0.5, 40% position-value cap, 10% per-trade conc), `capital/risk_engine.py` (10 gates incl. delivery COUNT caps 3/5, DAILY_LOSS 3%, consec-4), `capital/fund_manager.py` (intraday70/positional30 absolute, no cross-bucket borrow). **SL-distance seam = the sizer's `sl_price` param** — V3 03.03's S&R `computed_sl` feeds it later with NO sizer change; LIVE SL stays FIXED_PCT. **ONLY change = inert delivery-sizing scaffold** (T3): optional `position_sizing.delivery_risk_per_trade_pct` + `delivery_max_position_value_pct` (`PositionSizingConfig` default None; `system_config.yaml` null) → `position_sizer.calculate` uses `eff_*` = delivery value ONLY when `bucket=="positional"` AND set, else global; wired `main.py`. **Byte-identical** (default None→global; live bucket never positional under force_intraday_only). Delivery NOT activated; no risk_engine/fund_manager code change. **T5 flagged for architectural review (NOT changed):** (a-i) `kill_switch=None`→KILL_SWITCH skip fail-open → rec boot-time fail-fast in main.py; (a-ii) sector-lookup-fail→UNKNOWN+admit fail-open → keep (UNKNOWN bucket is conservative on the 40% cap); (b) wire `core/candle_math.atr` into LIVE SL → DEFER (changes live sizes; V3-shadow path + own parity proof). G1 byte-identity `tests/unit/test_position_sizer_delivery_scaffold.py` (4); G2 test_main stash-diff 26==26 → zero new. paper==live. **BUILT-but-UNPUSHED.** memory `v3_step5_risk_sizing_12jul`. NEXT = Step 6 (03.05 Portfolio Allocator — NEW ranked-admission core).

## V3 SHARED-ENGINE — 03.05 Portfolio Allocator (Step 6, 12-Jul-2026, PLAN ONLY — no code)

Design doc: **`docs/v3/V3_STEP6_PORTFOLIO_ALLOCATOR_PLAN.md`** (P1-P10, for Web Claude + ChatGPT + Rama). **No code/config/behaviour changed; ledger untouched.** Plan for the biggest NEW build — replace **FCFS/concurrent admission** (dispatcher drains queue → 5-worker pool, each signal runs the whole pipeline independently, NO candidate set assembled/ranked; "Priority rank #1 of 1" `signal_processor.py:439` is a placeholder) with **batch-over-a-short-candle-window → rank by score → greedy admit within portfolio rules → atomic reserve via EXISTING primitives**. **Single seam** = the PREPARE→ADMIT boundary inside `_process_one` (`signal_processor.py`): PREPARE (pre-flight→strategy→screen→size, ends `:904`) stays 5-worker-concurrent; ADMIT (the `portfolio_lock` block `:919-960`: `_enforce_strategy_position_cap`→`risk.approve`→`fm.reserve`→RESERVED→place) is extracted so both the fused OFF path and the batch worker invoke it. **SITS ON** (reimplements none): `fm.reserve`(`fund_manager.py:473`), `portfolio_lock`(RLock `:382`), `count_live_reservations[_for_strategy]`(`:1424`/`:1440`), `risk.approve` 10 gates(`risk_engine.py:177`), `_enforce_strategy_position_cap`(`:589`). **`allocator_mode: off|shadow|enforce` default `off` = BYTE-IDENTICAL** (fused path; buffer never entered). **shadow** = non-blocking observer emits a scored-candidate copy at `:904`, computes the would-be ranked set vs live FCFS + logs REGRET (never reserves/places; live timing+orders unchanged; counterfactual is directional-approximate — flagged). **enforce** = single admission worker ranks `(score DESC, tier, triggered_at, signal_id)` + greedy-admits the batch under ONE portfolio_lock (counts/capital auto reservation-aware via the primitives; only NEW running state = the shared-concentration tally + one-per-symbol/long-short sets). **NEW shared concentration cap** = portfolio-wide deployed-margin ceiling (`deployed+cand.margin ≤ max_portfolio_deployment_pct×total`, default INERT null/1.0), COMPOSES with (doesn't duplicate) the existing per-sector 0.40 cap; do NOT silently rewrite gate 8. **Idempotency:** buffered candidate is already-dequeued + still holds the receiver's atomic `_claim_in_flight(symbol)` (`webhook_receiver.py:695`, released at pipeline end) + unique `signal_id` PK; `trades.signal_id` is NOT DB-unique (behavioral only) → **v1 does NOT carry candidates across windows** (admit-or-reject within the window; claim key = signal_id + the symbol claim; gate_state precedent for any future cross-window carry). **No-late-entry:** window ≪ 60s expiry + placement fresh-LTP re-anchor (FIX-067 `:858-882`) → no stale entry. **Migration scope:** SHADOW observes ALL (15 scanners + V3) for portfolio-wide regret; ENFORCE v1 governs V3-path only (scanners keep FCFS, read as prior exposure), full-portfolio = v2. **Cutover:** recommend off-market flip for first enforce (hot-path structural change), then remove the fused FCFS branch (R5, no permanent dual path). Resume paths `continue_from_gate`(`:~1460`)/`continue_from_retest`(`:1794`) stay OUT of the batch in v1. OPEN Qs (P10): window length · enforce scope · shared-cap definition/default · shadow regret metric + counterfactual approximation · residual SECTOR_EXPOSURE TOCTOU (gate 8 DB-only, not reservation-aware — flag, don't change silently) · long/short rule · admission-worker open-bell throughput · candle-source (reuse `CandleStore.register_on_candle_close` vs wall-clock timer). memory `v3_step6_portfolio_allocator_plan_12jul`. NEXT = Web Claude + ChatGPT review → Step-6b implements (default-OFF flag → shadow regret-compare → off-market enforce → remove FCFS).

**STEP 6b IMPLEMENTED (12-Jul, behind default-OFF flag; STOPPED AT OFF).** Applies the plan with ratified answers A1–A8. NEW `PortfolioAllocatorConfig` (`config_loader`, `extra="forbid"`) + `system.portfolio_allocator` + `system_config.yaml` block; **`allocator_mode: off|shadow|enforce` default `"off"`**, all knobs INERT (`enforce_scope v3_only`, `max_portfolio_deployment_pct null`, `long_short_skew_max null`, wall-clock window 60/2). **One-seam ADMIT extraction:** the `_process_one` ADMIT+place block extracted VERBATIM into **`_admit_and_place(self, ac, *, …)`** (the SINGLE callable both the fused path and the enforce worker use — R2 no fork), operating on a mutable **`_AdmitCtx`** (`reservation_id/requeued/in_flight_incremented/placed`) so the caller's except+finally observe the lifecycle on every exit path (incl. the queue.Full leak-guard). `_process_one` = PREPARE (unchanged) → routing hook → `try _admit_and_place(ac) finally: sync flags` (outer except/finally UNCHANGED). **`if self._allocator is not None:` hook — OFF (allocator None, not constructed in main.py) skips it entirely → FCFS BYTE-IDENTICAL.** shadow = `observe(copy)` fire-and-forget (guarded) then fall through to the fused admit; enforce+in-scope = `submit`+`handed_off` (worker owns the claim, A8); enforce-out-of-scope = fall through (A2). NEW `allocation/` pkg: `models`/`ranker`(A7 deterministic)/`window_buffer`/`portfolio_allocator` (mode/observe/submit; wall-clock window worker; `_simulate` greedy w/ slots+capital+**inert** concentration/skew/one-per-symbol; `_compute_regret` 3 metrics crowd_out/starvation/score_weighted_regret; enforce `_run_enforce_batch` under ONE portfolio_lock reusing `admit_prepared`). signal_processor adds `set_allocator`/`_build_candidate`/`admit_prepared`(reuses `_admit_and_place`, returns `ac.placed`)/`reject_prepared`. main.py builds the allocator ONLY when mode≠off (OFF → allocator=None). **gate-8 sector-TOCTOU NOT touched (separate task).** **G-OFF:** test_signal_processor 77 pass UNCHANGED post-extraction + shadow-admits-identically test + full-suite stash-diff (zero-new). **G-SHADOW-READY:** regret computed w/ admit-fn+reserve NEVER called in shadow. **G-REG:** 308 pass (sp/config/risk/fm/allocator); tests `test_portfolio_allocator.py`(25). paper==live. **BUILT-but-UNPUSHED; STOPPED AT OFF — no live behaviour changed.** memory `v3_step6b_portfolio_allocator_impl_12jul`. NEXT (Rama/off-market after review): push → off→shadow regret soak (N sessions) → (review + off-market cutover) enforce → remove FCFS. Next module = Step 7 (03.07 Entry).

## V3 SHARED-ENGINE — 03.07 Entry Engine (Step 7, 12-Jul-2026, VERIFY — ZERO code change)

Gap-map: **`docs/v3/V3_STEP7_ENTRY_ENGINE_GAPMAP.md`**. 03.07 is **COMPLETE + RAMCOIND-battle-tested** in `orders/` → **no genuine gap → NO code change** (do not churn proven code). **LIVE entry BYTE-IDENTICAL by construction (0 `orders/*.py` files modified by any V3 step).** Every 03.07 requirement PRESENT (cited): bounded LIMIT entry (`order_protocol_limit.execute`; MARKET only for off-by-default retest-confirm; emergency = marketable-LIMIT capped 1%, no raw MARKET; SL = stop-limit P0); slippage tolerance `_compute_slippage_tolerance` min(SL_dist×0.22, Rs5)+band+override-hierarchy Symbol>Strategy>Band>Global + FIX-128 1% abort; **TGT recalc from fill** (FIX-013) with **SL signal-fixed**; **software OCO** 1 SL+1 TGT (SL-first `place_exits`; exit-fill `close_trade` double-close-guard → `_cancel_oco_siblings`; tgt_retry never a 2nd); naked → `SLUnplaceableError`→CRITICAL→`_emergency_market_exit`(FIX-148)→hard_kill; idempotency A-2 timeout→UNKNOWN_IN_FLIGHT no-retry→reconciler owns (authoritative broker book); partial-fill sized to `filled_qty`; MIS coercion + CNC-GTT (delivery OFF); circuit exit-clamp `clamp_exit_into_band`. **T2 SL seam** = the existing `sl_price` param (order path derives NO SL) — V3 03.03 `computed_sl` feeds it at the signal_processor seam on the V3 path LATER, no order-path change; LIVE SL stays FIXED_PCT. **T4 flagged (NOT changed):** ATR-into-LIVE-SL (Step-5 T5b carry — LARGE money-path blast radius, belongs to the V3 playbook step + own parity proof); MARKET-retest path off-by-default. **G1** byte-identical (0 orders/ files touched). **G2 RAMCOIND MANDATORY: 209 pass** (`test_ramcoind_dup_exit_fix`+`test_ramcoind_oversell_prevented`+`test_order_placer`+`test_tgt_retry`+`test_fix190_exit_safety`+`test_fix148_broker_gaps`; 4-layer intact). **G3** no code change → zero new failures. paper==live. memory `v3_step7_entry_engine_12jul`. **VERIFY-only; nothing to push except the gap-map doc.** NEXT = review → Step 8 (03.08 Trade Management — highest-frequency SL modifier, RAMCOIND-critical).

## V3 SHARED-ENGINE — 03.08 Trade Management (Step 8, 12-Jul-2026, VERIFY — ZERO code change)

Gap-map + T1 SL-owner finding: **`docs/v3/V3_STEP8_TRADE_MGMT_GAPMAP.md`**. 03.08 COMPLETE + RAMCOIND-battle-tested → **NO code change**. **LIVE trade-mgmt BYTE-IDENTICAL (0 `orders/*.py` modified by any V3 step).** **T1 SINGLE-SL-OWNER (headline):** ownership protocol-partitioned at registration (`order_placer._handle_entry_fill`) — **CO_PLUS_TGT→SmartTgtManager** (`:2095`, `smart_tgt.enabled`), **LIMIT_TRIPLE+`trailing_sl_enabled`→BreakevenManager** (`:2067`), **LIMIT_TRIPLE+`structure_exit_enabled`→StructureExitManager** (`main.py:2789`). **CO vs LIMIT_TRIPLE = AIRTIGHT BY CONSTRUCTION** (one protocol per trade; never overlap) → **LIVE SL owner = SmartTgt ONLY** (all live intraday = CO_PLUS_TGT; the 2 LIMIT_TRIPLE strategies = positional/DELIVERY dormant). **The one non-airtight seam = breakeven ∩ structure-exit on the same LIMIT_TRIPLE trade** — CONVENTION comment (`main.py:2788`), not construction; **TRIPLY-INERT today** (structure_exit_enabled false · 0 trailing_sl_enabled · LIMIT_TRIPLE DELIVERY-dormant). Not two-SLs (one static leg) but a modify-race/transient-no-SL-during-flatten hazard if co-enabled → **FLAGGED (T4-1), NOT fixed.** **T2:** all managers **modify-in-place** (`adapter.modify_order` on broker order id; NO cancel-replace for an SL move → no two-SL/zero-SL); **MONOTONIC** (SmartTgt `_compute_new_sl`→None + `_modify_co_sl` re-guard `:566/568`; Structure only-tighten `:23`; Breakeven forward-only-once); ghost-SL-safe (state after broker confirm); modify-fail→old SL stands+CRITICAL@3 (FIX-142)+next-candle retry; partial→`filled_qty` (OP-NS1/2/3). Breakeven=milestone SL-advance (60→BE/80→40%) not scale-out (working-differently-but-fine). **T4-1 rec:** boot-time fail-fast / config-auditor rule rejecting `structure_exit_enabled`+`trailing_sl_enabled` co-enable (turns `:2788` comment into a guard; both off→no live change). **G1** byte-identical; **G2 RAMCOIND 154 pass** (dup-exit+oversell+3 managers+tgt_retry; 4-layer intact); **G3 monotonic** test-enforced (`test_long_price_retraces_sl_not_lowered`/`test_short_sl_never_raised`/`test_action_a_only_tighten_skips_when_not_higher`); **G4** zero new. paper==live. memory `v3_step8_trade_mgmt_12jul`. **VERIFY-only.** NEXT = review → Step 9 (03.09 Exit — terminal exits, OCO sibling cancel, 15:15/15:17 squareoff+delivery exemption, SYSTEM_OVERSELL, kills).

## V3 SHARED-ENGINE — 03.09 Exit Engine (Step 9, 12-Jul-2026, VERIFY — ZERO code change) ★ SHARED ENGINE 03.01–03.09 COMPLETE

Gap-map + delivery-exemption ledger: **`docs/v3/V3_STEP9_EXIT_ENGINE_GAPMAP.md`**. 03.09 COMPLETE + RAMCOIND-battle-tested → **NO code change**. **LIVE exits BYTE-IDENTICAL (0 exit-path files `orders/*.py`/`kill_switch.py`/`market_windows.py` modified by any V3 step).** Ratified: 15:15=entry cutoff ONLY (`market_windows.is_past_eod_entry_cutoff:177`, NO flatten); 15:17=EOD squareoff MIS/CO. **T1 all PRESENT:** terminal exit `_handle_exit_fill` close_trade(dbl-close)→`_cancel_oco_siblings`(`order_placer:4346`); 15:17 `eod_squareoff` two-pass; **CNC EXEMPT** `p.product in ("MIS","CO")` (`eod_squareoff:1023` FIX-015 +`:1390`+EOD6, test `test_open_delivery_positions_not_touched`); SYSTEM_OVERSELL `_detect/_flatten`(`order_reconciler:1557/1604`)→CHECK2 L3→CRITICAL+flatten; scheduled-vs-emergency `auto_clear_scheduled_kill`(`kill_switch:250`) + **HARD never auto-clears**(`:256`) + `clear_stale_state`(`:199`); HARD cancel-FIRST `_cancel_trade_resting_exits`(`:971` FIX-190)→marketable-LIMIT flatten(FIX-181)→`_exit_all_trades_indestructible`(retry≤2h FIX-180); orphan/fail = two-pass+write-ahead+reconciler recovery. **T2 DELIVERY-EXEMPTION LEDGER (Slice 2.5 input):** EXISTS(protect a carried CNC) = 15:17 filter + reconciler delivery-exclusion(`:785-797`) + FIX-183 GTT adoption + FIX-008 bootstrap-WARN + **CncGttMonitor WIRED**(`main.py:2338/2364`, P1+P2 tests pass) + GTT/holdings ungated; PARKED(active delivery TRADING) = CNC ENTRY off (delivery_enabled false/force_intraday_only/trade_type INTRADAY) + T2-canary DDPI overnight UNPROVEN(`854112b`) + delivery sizing knobs(Step-5 inert) + next-day-carry live proof. **Protective exemptions EXIST+tested; active lifecycle PARKED.** **T4:** no new exit flag; carries Step-8 SL-owner + Step-5/7 ATR flags; Slice 2.5 activation = Rama's parked call. **G1** byte-identical; **G2 RAMCOIND 241 pass** (dup-exit+oversell+eod+kill+reconciler+cnc-P1/P2); **G3** CNC-not-squared-off test-enforced + honest ledger; **G4** zero new. paper==live. memory `v3_step9_exit_engine_12jul`. **★ SHARED ENGINE 03.01–03.09 COMPLETE** (Common Utils+S&R+Regime+HardGate/scorer+Sizing+Allocator+Entry+TradeMgmt+Exit). NEXT = review → next phase: intraday pipeline plumbing → stateful watchlist + next-morning entry → PB-01 shadow.

## V3 SHARED-ENGINE — Step 10b PB-01 Watchlist INCREMENT 2 COMPLETE + Side-task A Taxonomy (12-Jul-2026, post-power-outage)

Branch `v3-step10a-chain-12jul` (BUILT-but-UNPUSHED; **SHADOW / ANALYSIS-ONLY — a PB-01 candidate can NEVER place a real order**). The stateful FRONT-END that finishes Step 10b. **T3** `v3_chain/pb01_entry.py::Pb01EntryStage` — a 09:20-11:00 daemon poll (analog of `retest_monitor.py`) over today's PENDING `pb01_watchlist` rows. **T0c decision = OhlcFetcher poll** (`fetch_interval("5minute", lookback_days=1)`), NOT LiveFeed+CandleStore: `CandleStore.on_tick` sums cumulative `volume_traded` across ticks → not a correct per-candle traded volume, which the G-CONFIRM volume test needs; and watchlist symbols aren't in the live subscription. `truncate_to_asof(candles,"5minute",now)` is the **G-FORMING-CANDLE** chokepoint (drops the still-forming bar). Gap→SKIPPED_GAP; walk closed candles chronologically, FIRST terminal wins (INVALIDATED = a 5m close < LEVEL−hold_buffer; CONSUMED = G-PULLBACK then G-CONFIRM); 11:00→EXPIRED_WINDOW. **Restart-safe by RE-DERIVATION** — the row status IS the durable state machine; session_low/lowest_close re-derived from candles-since-open each poll (nothing intermediate to persist/lose); anti-rehydration loads ONLY today. ATR30 + baseline_5m_volume are YESTERDAY-derived (truncated to the session open), memoized per (symbol,trading_date) → deterministic + one fetch. **T6** `v3_chain/pb01_runner.py::Pb01WouldBeRunner` — on_confirm builds ONE would-be record + writes JSONL (`data_store/v3/pb01_would_be.jsonl`), nothing else. **G-NO-ORDER by CONSTRUCTION** (no fund_manager/placer/broker/reserve reference exists in the class — test asserts structurally + behaviourally). Reuses the pure helpers (NO 2nd scorer/gate module): `gate_rr/htf/extreme` + `gate_confirm/pullback` recorded, `compose_score` + Playbook-40 factors, and the SHARED `regime_fraction`/`sr_target_fraction` **extracted** from `v3_chain/runner.py` to module functions (10a byte-identical). PB-01 SL = min(retest_low,LEVEL)−sl_buffer×ATR30 (deterministic); TGT = nearest 30m/1h resistance above → none ⇒ **G-RR FAILS = the Kalyan skip**. NO-LOOKAHEAD: all structural TFs truncated to `conf.as_of` (the confirmation candle CLOSE). Score = Playbook-40 FULL + native Context (regime/sr_target/htf); step-based sector/momentum/execution are ABSENT (a watchlist candidate never ran the intraday screener — recorded honestly). **Main wiring** (`main.py`): capture worker + entry stage + would-be runner built ONLY when `watchlist.enabled` (default false → byte-identical); ONE OhlcFetcher over the shared `_sr_fetch_fn` closure (cache_ttl 0 → 5m fresh; static inputs memoized); `webhook_receiver.set_eod_capture()` late-binds (same pattern as `signal_processor.set_v3_chain`); reverse-order shutdown. **T7** `scripts/v3_shadow_soak_report.py --pb01` (read-only): capture/entry OUTCOME distribution + the retest **CONFIRMATION RATE** (CONSUMED/terminal — the key unknown) + would-be verdict/gate/R:R/score; would-be EXPECTANCY flagged PENDING an outcome replay (a shadow candidate has no real trade → P&L must be simulated from POST-confirmation candles; needs candle access, NOT a read-only join → the honest next step, deliberately not faked). **Side-task A (taxonomy, metadata+display only):** `StrategyConfig.pipeline`(INTRADAY|DELIVERY) + `horizon`(SAME_DAY|NEXT_DAY|BTST|**SWING**) REQUIRED enums (validated at load); all 16 YAMLs tagged — 12 INTRADAY·SAME_DAY, PB-01 INTRADAY·NEXT_DAY, **3 positional_* = DELIVERY·SWING (VERIFIED multi-day from their YAML descriptions, NOT BTST)**; `strategies/taxonomy.py` reusable helper; surfaced in `scripts/strategy_status.py` (HTML + Telegram + plaintext Category) + EOD `reports/daily_report.py` sheet-6 (Pipeline/Horizon cols). Inert — no routing/execution/capital change; the prerequisite for a future two-pipeline split. **Acceptance:** G-NO-ORDER + G-OFF + G-NO-REHYDRATION + G-FORMING-CANDLE + G-NO-INTERFERENCE + G-REG all proven; **full-suite ID-level clean-vs-dirty stash-diff = ZERO new failures (41==41)** + RAMCOIND dup-exit 24 + **418 pass**; fixed 2 stale `==42`→`==43` schema-version asserts the capture-increment v43 bump missed. **O4/side-task B CONFIRMED already-satisfied** (the 10a observe hook enriches all 15 live strategies with G-RR in shadow → `--v3` G-KALYAN join ready). memory `v3_step10b_increment2_complete_12jul`. **STILL OWED separately:** Monday SESSION-1 scorer+allocator sanity + the NIFTY-index probe. NEXT = Web Claude + ChatGPT review → off-market push.

## V3 SHARED-ENGINE — Pipeline Plumbing (Step 10, 12-Jul-2026, PLAN ONLY — no code)

Design doc: **`docs/v3/V3_STEP10_PIPELINE_PLUMBING_PLAN.md`** (TASK-0 inventory + P1-P10, for Web Claude + ChatGPT + Rama). **No code/config/VM change; deploy ledger UNCHANGED (nothing built).** Baseline `main`@`c1ad82e` (VM `v3_hardgate_mode: shadow` + `allocator_mode: shadow`, else OFF). Runs in parallel with the scorer + allocator shadow soaks. Plan for how a signal flows END-TO-END through the V3 chain (regime→S&R→hard gates→score→allocator→sizing→would-be entry) in **SHADOW** — a would-be trade, NO real order — threaded through the EXISTING pipeline via ONE flag-selected seam (R2).

**TASK-0 HEADLINE (wired vs missing, as of `c1ad82e`):** Steps 1-9 built every V3 module as an **independent, local shadow** — each observes/logs its OWN decision and gates nothing. **MISSING is the CHAIN, not more modules:** (a) **decision content** — no **playbook registry / PB-01** (no `v3_playbook` field on `StrategyConfig`, `strategies/schema.py:35` — verified absent), the **playbook-scope hard gates** (confirmation/valid-pullback/S&R-R:R/strong-HTF/extreme) are **NAMED in `hard_gate.py:6-8` docstring but have ZERO code** (stubs), and the **3-layer score (Playbook 40 / Context 40 / Execution 20) is NOT built** — only the 8-step "Execution seed" re-scale exists (`hard_gate.py:171` `rescaled_total`; `quality_scorer.py` UNCHANGED); (b) **consumption wiring** — **regime** exposes `runner.latest` (`regime/runner.py:40`) but nothing consumes it ("gates NOTHING", `:6-7`); **S&R** is observer-only (`confidence_class=ANCHOR_ONLY`, observed `signal_processor.py:517`), nothing consumes levels for a decision; (c) **allocator scope empty** — shadow observes the EXISTING FCFS flow, not a V3 set (`enforce_scope: v3_only` + `v3_scope_fn=lambda s: getattr(s,"v3_playbook",False)` `main.py:2732` → always False, "none exist yet" `system_config.yaml:358`); (d) **PB-01 front-end MISSING** — no stateful overnight watchlist / next-morning 5-min entry (`EntryGate` is same-day, cleared at EOD by FIX-046). **BUILT + ready:** the SEAM (`_process_one` sizing→allocator-hook boundary `signal_processor.py:924-965`), the candidate primitive (`_build_candidate`→`ScoredCandidate`+`AdmitPayload` `:1328`/`allocation/models.py`), sizing (`sl_price` param seam open), entry/trade-mgmt/exit (03.07-09 complete). **One-liner:** pipes built + OFF; water (playbook/gates/3-layer-score) not; regime/S&R drip to the floor.

**SEAM DECISION (P2):** a V3 branch **INSIDE `_process_one`, flag-selected** — realized as (i) additive **enrichment** of the candidate already flowing through the live path (attach regime + S&R + gate + 3-layer-score fields) and (ii) a **flag-gated STOP-and-emit** for `v3_playbook` candidates (write a would-be-trade JSONL instead of `_admit_and_place`). Reuses screen/derive/size VERBATIM. The allocator hook at `:926` is the PROVEN template; S&R read = the SNR-V2 diverter's synchronous ZoneCache pattern (`:840`, never a hot-path fetch). **Rejected:** (b) separate stream consumer = re-implements the pipeline (R2-forbidden duplicate path); (c) replay harness = can't produce a live would-be to compare (a complement — the offline recompute tools already exist). PB-01's overnight watchlist is a new signal **SOURCE** (emits into the SAME `_process_one`, like `webhook_receiver`), NOT a new pipeline.

**NEW flags planned (all default-OFF → byte-identical; proven via test_signal_processor stash-diff + test_main 26==26):** `StrategyConfig.v3_playbook: bool=False`; master `v3_chain_mode: off|shadow` (binary — no `enforce` yet, avoids a dead hot-path branch); `watchlist.enabled: false`. **Would-be output** = `data_store/v3/would_be_trades.jsonl` (NO schema change, mirrors allocator `regret.jsonl`), compared via a NEW `v3_shadow_soak_report --v3` mode. **PB-01 rides INTRADAY** (next-morning 5-min retest, same-day exit under `force_intraday_only`; overnight part is analysis WATCHLIST — no CNC/GTT/capital carry) → **delivery stays OFF** (double-locks + Option-A 12/3 split untouched; true position-carry = Slice 2.5, out of scope). **PB-01 promotion evidence is its OWN would-be track record** (via existing `shadow_tracker`), NOT OLD-vs-NEW (no live twin) — honesty flagged.

**SEQUENCING (P8):** 1 playbook substrate (`v3_playbook` field + registry + PB-01) → 2 consumption wiring (regime + synchronous S&R read into a flag-gated V3 branch) → 3 3-layer score (Playbook 40 + Context 40 on the Execution seed →20) → 4 playbook hard gates (log-only on the V3 path) → 5 unified would-be record + emission + `shadow_tracker` simulate + `--v3` soak → 6 stateful watchlist + next-morning 5-min entry (NEW front-end; persistence must NOT rehydrate stale, the FIX-046 class) → 7 allocator scope (tag PB-01) → 8 soak. **Steps 2-5 provable on EXISTING live signals first** (a webhook signal exercises the enrichment); watchlist (6) added after the plumbing is proven, then PB-01 rides it. **OPEN Qs (P10) for Web Claude/ChatGPT/Rama:** Q1 PB-01 gate rules+thresholds (the decision content); Q2 3-layer sub-weights + seed→20 rescale; Q3 strong-HTF source (S&R TF_ROLE swings? regime?); Q4 regime as multiplier vs gate-input; Q5 watchlist retest level+window; Q6 flag granularity (per-playbook once >1). memory `v3_step10_pipeline_plumbing_plan_12jul`. NEXT = Web Claude + ChatGPT + Rama review → separate build instruction authorizes the build (per P8 sequencing).

**STEP 10a IMPLEMENTED (12-Jul, behind default-OFF `v3_chain_mode`; STOPPED AT OFF; the CHAIN on EXISTING signals — no PB-01, no watchlist, no playbook layer).** Ratified via the **V3 DECISION CONTENT SPECIFICATION v1.0** + the **NO-LOOKAHEAD addendum**. **TASK 0 (decided, no round-trip): ASYNC** — the ZoneCache/ZoneWarmer are NOT constructed today (`wait_for_retest_enabled:false` + `structure_exit_enabled:false`, `main.py:2635`), so a synchronous S&R read would cold-miss and fail-closed on nearly every signal → the chain emits a guarded fire-and-forget copy on the hot path and computes the verdict on a BACKGROUND worker (shadow places no order → latency is irrelevant to the verdict; the live path is never slowed). NEW `v3_chain/` pkg (`models`/`truncate`/`score`/`runner`) + generic gates in the EXISTING `screening/hard_gate.py` (`gate_rr`/`gate_htf`/`gate_extreme`; no second gate module) + `V3ChainConfig` (`config_loader`, extra=forbid) + `system.v3_chain` block + `StrategyConfig.v3_playbook:bool=False`. **`v3_chain_mode: off|shadow` default `"off"` = BYTE-IDENTICAL** (runner NOT constructed → `signal_processor` gets `v3_chain=None` → the hot-path hook `signal_processor.py:~924` is one skipped flag check). shadow = `observe()` fire-and-forget → worker: fetch day/60m/30m via its OWN OhlcFetcher over the SAME rate-limited closure (REUSE, isolated cache) → **truncate every series to bars CLOSED STRICTLY BEFORE `as_of` (NO-LOOKAHEAD, `v3_chain/truncate.py`)** → build S&R via the shared `scored_zones_from_candles`/`split_zones` → G-RR (side-aware nearest support/resistance, must-have, missing→FAIL) + G-HTF (60m EMA20+swings, fail-OPEN) + G-EXTREME (regime.extreme_flag, fail-OPEN) → 3-layer score COMPOSER (Context 40 + Execution 20; Playbook=null NOT renormalized; confluence groups {rsi_range,vwap_position}+{1h EMA,1h swings} combined by MEAN so correlated factors can't double-count) → would-be JSONL `data_store/v3/would_be.jsonl` (V3 vs LIVE SL/TGT/RR + gates + score + `sr_sync_hit` + as-of `last_candle_close_ts`). LOG-ONLY: the verdict NEVER rejects/delays/alters a live order. regime read O(1) from `runner.latest` AS OF signal time (None when regime OFF → the chain runs correctly with regime off). Soak: `scripts/v3_shadow_soak_report.py --v3` = the **G-KALYAN join** (would-be R:R verdict × REAL trade P&L → did the S&R R:R gate reject our winners or our losers?). **Acceptance:** G-OFF byte-identical (test_signal_processor 77 unchanged + test_main clean-vs-dirty 4==4 stash-diff → zero-new); G-NO-INTERFERENCE (a shadow chain that RAISES on observe → live outcome UNCHANGED + hook fires, test); G-HOTPATH (no hot-path fetch; O(1) regime; fire-and-forget guarded); G-CONFLUENCE (grouped pair = one weight, test); anti-lookahead V2 (a future candle does NOT alter the verdict, test); RAMCOIND 264 + full-suite 4516 collect clean. Tests `tests/unit/test_v3_chain.py` (20). paper==live. **BUILT-but-UNPUSHED; STOPPED AT OFF — no live behaviour changed.** memory `v3_step10a_chain_impl_12jul`. NEXT = Web Claude + ChatGPT review → (Rama/off-market) push → shadow soak (the Kalyan measurement) → Step 10b (PB-01 + playbook gates + Playbook score layer + overnight watchlist).

## GUI Project (Frontend) — Phase G0 investigation COMPLETE (2026-07-02)
> Rama's pending decision #8 (build a Frontend GUI) is moving. **Phase G0 = backend investigation only** (zero code changes) is DONE. **Report:** [`docs/gui_project/G0_BACKEND_INVESTIGATION_REPORT.md`](gui_project/G0_BACKEND_INVESTIGATION_REPORT.md) — 8 sections, every claim cited `file:line`, produced from 5 parallel read-only investigations + direct verification of the control (§3) & security (§6) sections.
> **Locked v1 decisions (Rama, 02-Jul):** same Oracle VM · browser-from-anywhere, HTTPS-only, single-user, auth-required · runs while PC off · desktop-first + mobile-responsive · control depth = view + safe controls (pause/resume, enable/disable, kill pending orders, manual square-off, emergency halt, restart) · ~20 view modules.
> **5 design-critical findings (facts, not design):** (1) TWO existing Flask+Waitress HTTP servers as daemon-threads inside `main.py` (webhook :5000 authed · healthcheck/metrics **:8080 UNAUTHENTICATED**, exposes P&L/capital/kill-switch) — both `0.0.0.0`, plain HTTP, **NO TLS/reverse proxy**; up only while the trader runs. (2) **Control plane barely exists** — only clear-kill-switch (CLI) + `systemctl restart` are operator surfaces; pause/enable-disable/cancel-orders/square-off/trigger-halt have **NO programmatic entry point**, strategy/config edits need **restart (no hot reload)**, **no inbound Telegram**; all existing paths are paper==live. (3) **No cross-process push** (EventBus is in-process only) → GUI polls SQLite (WAL + 30 s busy_timeout ⇒ read-only reader safe) or the in-process `/metrics`; **live LTP not persisted** → needs its own broker session. (4) DB = 43 main + 3 analytics tables; **no `positions`/`holdings` table** (status-filtered `trades` / CNC via `gtt_state`); data exists for nearly all view modules. (5) The **18:00–08:00 time-lock is copy-gate (scp/sftp/rsync) only — it does NOT block nighttime browser access**; `security_monitor`'s checks never inspect listening ports (9 security checks + a 10th, added 26-Jul, that is deliberately NOT a security check — see `security-watcher.service` below); a GUI writing under `config/`/`​.env` trips auditd, writes to `data_store/logs/reports` do not.
> **[REQUIRES LIVE VM]** (not run from PC this pass): `systemctl is-active`, `ss -tlnp`, live fail2ban/auditctl rules, RAM/CPU/disk headroom (documented floor 2 vCPU / 2 GB / 20 GB; psutil absent ⇒ CPU/RAM not persisted). memory `gui_g0_backend_investigation_02jul`.
>
> **G2a — SKELETON + DASHBOARD (M1) BUILT (03-Jul, branch `gui-g2a-03jul`, NOT merged; PC-only, NO VM deploy).** Folder **`ops_dashboard/`** (renamed from gui_project per G1). **ISOLATED by mechanism** (test-gated I7): zero production imports (AST gate green), SQLite opened **read-only** (`mode=ro` + ATTACH analytics ro + busy_timeout 30s; write raises), **no kiteconnect in its venv**, binds **127.0.0.1:8500 loopback only**. Stack = Flask+Waitress+pyotp+PyYAML (own `.venv`, git-ignored). Auth = single-user pbkdf2+TOTP+5-fail/15-min lockout, HttpOnly+SameSite=Strict cookie (Secure config-gated → on under TLS in G2c). **M1 Dashboard**: 13-stage pipeline (big colored cards, pure `derive_color`), Daily Capacity Monitor (8 primary quotas: daily-trades/open-pos/daily-loss/intraday-capital/consec-losses/queue/delivery×2 — Limit·Used·Remaining), Strategy panel, service-health strip (`systemctl is-active`→"unavailable" on Win), events feed, trader-down banner (`:8080` unreachable⇒DOWN, never error). 4 JSON APIs (`/api/dashboard|pipeline|capacity|strategy_panel`), all `login_required`. **Verified:** 102 tests pass on BOTH v41+v42 synthetic fixtures; V4 funnel exact (100/85/10dup/5rej/70/60); real Waitser server `/`→302→`/login`, `/api/*` no-auth→401, 0.0.0.0 not bound. STEP-0 capacity inventory (30 limit-keys, 8 ambiguity decisions) → `ops_dashboard/docs/G2a_capacity_inventory.md`. `mode` is a data attribute, never a branch (parity). **G2a ACCEPTED by Web Claude review** (D1–D8 approved permanent; Q1=Tailscale, Q2=:8080 untouched/read-only). memory `gui_g2a_dashboard_03jul`.
>
> **G2b-1 — STRATEGY CONTROL TOWER + TRADING MODULES + CAPACITY COMPLETION (03-Jul, branch `gui-g2b1-03jul`, NOT merged; PC-only).** STEP-0 attribution investigation → `ops_dashboard/docs/G2b1_strategy_attribution.md`: **scan_webhook_map is 1:1 today (15↔15), 1:N structurally impossible** (each scanner entry names ONE strategy, dup scanners = startup failure) → per-strategy webhook attribution EXACT (incl. N:1 sums); unmapped scanners → **"scanner-level (shared)"** labeled bucket, never fabricated. Post-insert expiry = `REJECTED_EXPIRED` (processor 60s-age gate); success path QUEUED→PROCESSING→RESERVED→PROCESSED. Decision rules R1–R6 (attribution / two-level SIGNALS / expired / **ROI = net ÷ Σ margin_reserved today** / order buckets / **last-failure = max(FAILED order, losing close)**) documented for Web-Claude review. **Built:** `/api/strategies`(+`/<name>`) via `services/strategy_tower.py` (7 Rama groups BASIC/SIGNALS/PROCESSING/TRADING/PERFORMANCE/RISK/HEALTH + 4 ranking views net-P&L/win-rate/expectancy/success-rate, staleness-colored HEALTH; replaces strategy_panel) · M2–M5 `/api/{signals,orders,positions,holdings}` + screens (filters; denominator strip keeps the ~82% pre-insert drop visible; superseded-chain badges; positions pin the G0 open-set + inning# + unrealized="—"/G4; holdings = gtt_state mirror w/ broker-authority banner + designed empty state) · capacity completed: **6 groups (Orders/Positions/Capital/Risk/Strategy/System-guards) covering all 30 inventory keys** (DB-readable actuals incl. worst-symbol/sector exposure, largest-position, entry-burst window, 429s-proxy; in-memory counters = config-only rows, D7 holds; window rows INSIDE/OUTSIDE). **167 tests pass on v41+v42** (V3 rankings hand-checked exact; V5 shared-label asserted; 15-second-goal UAT test; silent + all-losing + N:1 + expired + superseded seeds). INSTALL.md now locks the G2c Secure-cookie flip (Tailscale). **G2b-1 ACCEPTED** (R1–R6 permanent). memory `gui_g2b1_tower_trading_capacity_03jul`.
>
> **G2b-2 — SCORECARD + M7–M21 (03-Jul, branch `gui-g2b2-03jul`, NOT merged) — the READ-ONLY DASHBOARD IS COMPLETE.** §1: deterministic **strategy scorecard** (`services/strategy_score.py`, pure, thresholds in gui_config: RED silent-while-expected / failed≥3 / consec-losses≥3 / net≤−₹500 · YELLOW silence>90m-in-window / winrate<40%@≥3 / failed≥1 / capacity≥90% · disabled=GRAY never RED, reasons shown) + **silence tiers** (last-signal age 🟢≤30m/🟡≤120m/🔴>120m-or-none, entry-window-gated, plain outside) + mini funnel + failure strip (R/C/O/D/E) + Best/Worst dashboard widget + honest capital view ("global bucket" — no invented per-strategy caps). §2–4 screens: **M7 Risk** (big-numbers; D2 source + W10 footnote) · **M8 Capital** (fm_ledger append-only view) · **M9 Exposure** (top-N bars) · **M10 P&L** (hand-rolled SVG equity curve from RELEASE_USED; single-mode note — trades aren't mode-tagged) · **M11 Services** (registry-joined cron expected-vs-actual + CT findings) · **M12 VM** (psutil gap stated, never fabricated) · **M13 Logs** (whitelisted dir, block-tail ≤2000 lines, traversal→403, download disabled) · **M14 Audit** (6-source merged feed) · **M15 Alerts** (telegram + failed-tail + sentinels; outbound-only note) · **M16 Slippage** (breach = entry slip > min(0.22×SL-dist, ₹5 cap), entry-leg-only documented) · **M17 Execution** (latency buckets) · **M18 Statistics** (LONG/SHORT permanent split, honest zeros; MFE/MAE) · **M19 Reports** (download **gated OFF** — Q3 pending) · **M20 Config** (8 exact groups, **drift banner** vs yesterday's hash + changed keys, last-change date) · **M21 Controls stub** (locked G3 list). **233 tests pass on v41+v42** — scorecard truth tables (V3), drift banner (V4), 10MB tail-perf never-full-read (V5), all 22 pages route (V6), isolation I7 over every new file. Run (Rama's PC pass): `cd ops_dashboard && .venv/Scripts/python -m backend.app` → `http://127.0.0.1:8500`. **G2b-2 ACCEPTED** (judgement calls a–d permanent). memory `gui_g2b2_complete_03jul`.
>
> **G2b-3 — UI POLISH A1–A9 (03-Jul, branch `gui-g2b3-03jul`, NOT merged; frontend-only, APIs frozen).** Web Claude's AlgoPilot verdict implemented: **hero KPI row** (P&L Today + SVG sparkline · Capital Used % · Active Positions · Daily Loss Used %, all from existing /api/capacity+/api/pnl numbers) · bars on ALL quota rows · **inline-SVG icon sidebar** (22 items, active accent border, icon-only ≤900px; no icon-font/CDN) · **mode pill** PAPER-green/LIVE-red (display-only, NOT a toggle — X1 mode switching stays out permanently) · **kill chip** INACTIVE-dim/SOFT-amber/HARD-red with reason+since, **blink bound ONLY to HARD_KILL** (halt banner same: amber SOFT static, red+pulse HARD) · mono numerals (system stack) · **palette fully tokenized** (semantic aliases + tint tokens; zero rgba literals left) · sev-chips in audit/alerts/logs/execution · **closed-trades-today table** on P&L (additive `/api/pnl.closed_trades`; `trades.exit_price`+`charges` schema-confirmed). Rejected: control buttons/mode toggle (G3-only/never); parked R1–R5. **238 tests v41+v42** (+V4 CDN grep gate in test_isolation — vendored .min.js exempt; V3 seeded-HARD_KILL blink test; additive contract). **G2b-3 ACCEPTED; Q3 LOCKED = downloads stay FALSE (revisit post-soak).** memory `gui_g2b3_ui_polish_03jul`.
>
> **G2c — VM DEPLOY (03-Jul ~17:25 IST: DEPLOYED — merged `--no-ff` to main `7b1c92d` [tip `33e9223`] right after the A-2/B-1 batch `a9d44e0` per ordering (a); VM unit `gui-dashboard` enabled+ACTIVE, 127.0.0.1:8500 only, smoke 302→/login + API-noauth 401, venv I4 PASS, overlay scp'd+600 §2b 3+5; REMAINING: E3 Tailscale (Rama) → soak S1–S8 from Mon 06-Jul → E4 reboot drill.)** Historical record of the pre-push state follows. STEP 0 live-VM gates: **G0a PASS big — 11 GiB RAM (11 GiB available) / 2 vCPU / 83 GB root free** (docs' 2 GB floor stale); Python 3.12.3; fail2ban sshd-only 0 banned; auditd 11 watches + 3 copy_attempt (matches G0); G0b listeners `:22/:53(lo)` + **`:111 rpcbind 0.0.0.0` = pre-existing FINDING (stock portmapper, not GUI; parked to VM-hardening with Q2)**; :5000/:8080 absent pre-boot (in-process). STEP 1: GUI chain **rebased --onto origin/main(9becf8c)** off the fix-p1 stack → `gui-deploy-03jul`; **HARD GATE PASS** (diff = ops_dashboard/** + docs/gui_project/** + SYSTEM_MAP + PATHS only); **240 tests green**. **STOP:** push held — entanglement with the verified security-batch runbook (assumes main==9becf8c: C-1 `--ff-only` + boot-gate HEAD check). Both orderings proven content-clean (merge-tree sims); **recommendation = batch first (tonight/weekend, runbook untouched), GUI push immediately after** — zero calendar cost, soak starts Mon 06-Jul either way. **Deploy-prep committed (853cad2):** `deployment/gui-dashboard.service` (manual install; NO BindsTo — GUI survives trader restarts) + **`gui_config.local.yaml` overlay** (git-ignored, deep-merged; VM paths + auth secrets survive every future `checkout -f`) + INSTALL.md = living VM runbook (venv/I4, overlay w/ `session_cookie_secure: true`, **credentials PC-created pre-GO → scp transfer §2a/2b (03-Jul; VM re-run only for rotation/lost-phone)**, Tailscale serve, reboot drill, rollback). Rama-loop queued: ordering call · auth setup · `tailscale up` · MagicDNS+HTTPS · reboot timing · soak (S1–S4, ≥3 market days). Future backlog F1–F9 recorded in mempalace (roadmap only). memory `gui_g2c_deploy_prep_03jul`.
>
> **G2 READINESS VERDICT + G3.0 CONTROL-PLANE FACTS (03-Jul, market-hours read-only audit).** Formal deploy-bundle gate → **`ops_dashboard/docs/G2_READINESS_VERDICT.md`**: fresh **240 passed**, route audit (29 routes, POST = /login+/logout ONLY), zero SQL-mutations/broker-imports, controls = static page with NO backend module, download-OFF + Secure-cookie locks confirmed, diff gate 4 buckets (76→78 files after this docs commit), credentials.xlsx .gitignore **already in the batch (`aa02f9f`)** — verdict: **GUI READY + DISPLAY-ONLY CONFIRMED**. G3 design fact-base → **`ops_dashboard/docs/G3_0_CONTROL_PLANE_INVESTIGATION.md`** (facts only, cited): **kill-switch is in-memory-sole-authority while running (DB row read ONCE at construction — external DB writes NEVER take effect live and get overwritten)**; `resume.sh` stop→clear→start sequencing is load-bearing (18-Jun instance-lock collision); config/strategy **hot reload DOES NOT EXIST** (load-once docstrings); operator strategy-pause DOES NOT EXIST but the governor's in-memory `_paused_today` auto-pause does; :5000/:8080 Flask apps are in-process (loopback-admin capability fact); unit uses KillSignal=SIGINT + RestartPreventExitStatus=3 4; ubuntu = full NOPASSWD sudo; auditd watches sudoers/units/config (record-only). Q7 options table per control (Halt/Clear/Restart/Pause/Resume) with latency/restart/race/parity/footprint. memory `gui_readiness_g3_0_03jul`.
>
> **E3 — TAILSCALE LIVE ACCESS (03-Jul ~22:15 IST, off-market; access-layer only, trading stack UNTOUCHED).** GUI dashboard now reachable over the tailnet. **VM host `trading-system` · Tailscale machine `trading-system` · tailnet `tail1cdc6d.ts.net` · IP `100.74.84.44`.** **URL → https://trading-system.tail1cdc6d.ts.net** (`tailscale serve`, **tailnet-only, NOT funnel** → `127.0.0.1:8500`). Diagram: `browser/phone (Rama's tailnet) → HTTPS → trading-system.tail1cdc6d.ts.net (100.74.84.44:443, tailscaled) → loopback → gui-dashboard 127.0.0.1:8500 (read-only)`. **CLI-CHANGE (recorded):** old positional `tailscale serve --bg https / http://127.0.0.1:8500` was **REMOVED** by current Tailscale; the working form is `tailscale serve --bg http://127.0.0.1:8500` (one-arg = HTTPS:443/ → target) — INSTALL.md §5 updated. **Listener proof:** 8500 stays loopback-only; `:443` bound to `100.74.84.44` (+ tailnet IPv6), **zero `0.0.0.0` public listener added** (pre-existing `rpcbind 0.0.0.0:111` is unrelated, flagged for later hygiene). **Night access:** 22:15 IST curl → **HTTP 302 → /login** (time-lock is copy-gate-only, does not block access). Recovery/rebuild (new device / lost phone / lost TOTP / SSH-tunnel fallback) documented in `ops_dashboard/deployment/INSTALL.md` §5. **E4 reboot drill (03-Jul ~22:21, off-market): CLEAN PASS** — `sudo systemctl reboot` → new boot `2026-07-03 22:20:54` (back ~20s); ALL units returned to baseline: `gui-dashboard` ACTIVE, `token-watcher` active, `alert-watcher`/`security-watcher` activating (their steady baseline), `cron-watchdog` inactive/static (timer), `tailscaled` active; **`trading-system` stayed INACTIVE (correct — token-watcher only starts it in the 08:00–16:00 window on a fresh token; next boot Mon 06-Jul 08:15)**; `tailscale serve` **persisted** (still proxying), 8500 loopback-only, :443 tailnet-IP-only, URL → HTTP 302 /login. No findings. memory `gui_g2c_tailscale_live_reboot_03jul`.
>
> **G5 — OPS DASHBOARD REDESIGN · PHASE A: INVESTIGATION + MAPPING COMPLETE (04-Jul, READ-ONLY; zero code/schema/route/config edits; soak PAUSED for this).** Full mapping of the **22 redesign specs** (read from the **PC** copy `D:\Projects\trading-system\gui\*.txt` — no VM copy needed; temp `gui/` deletable on Rama's word once G5 lands) vs the live `ops_dashboard`. **Report → `ops_dashboard/docs/G5_REDESIGN_PHASE_A.md`** (5 sections + exec summary, every claim cited `file:line` / `spec NN §`). **Target menu = 5 groups** (Dashboard · Trading[Strategies/Signals/Orders/Positions/Holdings] · Analytics[Strategy Ranking/Strategy Health/Scanner Attribution/Trade Explorer/P&L/Slippage/Execution] · Operations[Live Activity/System Health/Configuration/Controls] · Investigation[Audit/Trade Logs/System Logs]); **Recent Events + Alerts = MERGED into screens, not menus** (frontend redistribution of existing `/api/alerts`+`recent_events` — no new backend). **Headline (22 target = 21 menu + Capital&Risk):** ~9 reuse-as-is (menu/UX only, additive) · ~4 new-frontend over existing data · ~9 new-backend/blocked; **NO new DB schema required for any buildable screen** — redesign is ~90% frontend + additive read-only SQL joins over the ~23 tables already read. **Preserve-first held — NONE forced-rename** (all 24 endpoints preserved+extended; 6 new screens add new routes). **Top walls:** (1) **Controls (spec 16) = architecture wall** — wants write-actions (toggles/limit-edits/pause) but GUI is read-only by mechanism (`test_isolation.py`) + NO control-plane exists (G3.0: kill-switch in-memory, config needs restart) → **G3 track, not Phase B/C**; only display-only subset safe. (2) **Positions/Holdings live-broker LTP/MTM/holdings = G4 wall** (no broker session read-only; `/api/positions` already renders `—`/"G4"); Holdings' nearest future source `eod_broker_reconciliation` (P1/v42) is **UNPUSHED/absent from live `core/schema.sql`**. (3) **238-case contract gate pins exact shapes/counts** (6 units, 13 stages, 8 capacity rows, 6 groups, 4 rankings) → redesign must be **additive** (keep 13 pipeline stages; Dashboard spec's 12 is a visual subset). **Schema verified:** System Score = `screener_results.score` (join signal_id); scanner→trade P&L reachable via `signals.scanner`←`trades.signal_id`; `signals.webhook_payload` = raw Chartink JSON; `orders.order_id` IS broker-assigned; no distinct "Signal Score" column; broker RAW response NOT persisted. **Placement flags (report, don't act):** Capital & Risk (spec 07) is Phase-1 + Alerts-merge-target but **missing from the target menu tree** → needs a home (suggest Operations); **Reports** screen has no target home (replaced by per-screen XLSX export). **Q3/XLSX:** ~20 screens want filtered-table export → collides with `reports_download_enabled=FALSE` + copy-gate → ONE flag-gated shared component, Rama decides. **Git HELD** (no branch/commit/push per no-push posture + in-flight tree changes) — ready for `gui-g5-phaseA-04jul`. Next = Web Claude reviews → Phase B (design); no coding until Rama approves the mapping. memory `gui_g5_phaseA_04jul`.
>
> **G5 — PHASE B: DESIGN COMPLETE (04-Jul, DESIGN ONLY; zero code/schema/route/config edits).** Report → **`ops_dashboard/docs/G5_REDESIGN_PHASE_B.md`** (sections **A–K**: 22-screen design + component architecture + shared-component inventory + navigation + API-impact matrix + reader/service matrix + frontend/backend sequences + test-impact + rollout + itemized Phase-C backlog). Bound by Rama's **10 LOCKED DECISIONS** (redesign-in-place · preserve-first[labels-may-rebrand-AlgoCore, internal-names-stay] · Broker→System→Delta honest-unavailable-never-faked · Controls=read-only-summary(A)+G3-placeholder-no-writes(B) · **Capital&Risk=ONE screen under Operations** composing /risk+/capital+/exposure+/capacity · Reports endpoint UNTOUCHED · XLSX=shared component+2 flags DEFAULT OFF · **single System Score=`screener_results.score`, drop "Signal Score"** · Positions/Holdings two-state AVAILABLE/UNAVAILABLE · Events+Alerts merged-into-screens). **THE design invariant:** contract tests are **equality**-pinned (`rankings`==4, `service_health`==6, `stages`==13, capacity `rows`==8/`groups`==6) → **all new data lands in NEW response fields, never mutating existing keys/lengths** (new ranking modes→`rankings_extra`) → all 238 cases stay green while every screen gains data. **Headlines:** 13 shared components (9 NEW: DataTable/FilterBar/KpiRow/PeriodSelector/EventsAlertsPanel/TwoStatePanel/ExportButton/ScoreChip/LifecycleTimeline/LeaderboardMedals; NavShell/FunnelPipeline/StatusChip extended) · API = **5 preserve / 12 extend-additive / 6-7 NEW** endpoints (`/api/{scanner-attribution,trades,trade-story/<id>,analytics/pnl,activity,strategy-ranking,strategy-health}`) · **NO new schema** (new readers `scanner_attribution`[scanner→trade P&L join via `signals.scanner`←`trades.signal_id`]/`trade_story`[per-trade assembly, feeds Trade-Explorer+Trade-Logs+all lifecycle timelines]/`pnl_analytics`[multi-period]/`activity_feed`[merged Live feed]/`broker_reconcile_reader`[graceful, lights up if P1 ships] all map to existing tables). **Rollout G5a-e** (each own branch off deployed tag `7b1c92d`, soak resumable): G5a shared-foundation+nav+rebrand+global-table+ExportButton-flag-OFF(L) · G5b 7 reuse screens+additive backend(M) · G5c 5 NEW analytics screens+new services(L) · G5d two-state Positions/Holdings+Live/Logs+Controls-A(M) · G5e deploy+resume soak(S); §K = 29-item ordered backlog. **Honest-unavailable G-1..G-6 designed as explicit TwoStatePanel states, never faked.** **Rama-decision gates (NOT auto-built):** `table_export_enabled` DEFAULT OFF (Q3) + **schema-pause PROPOSALS P-1..P-4** (per-stage timings / change-author+auth audit / ship P1 broker-reconcile / GUI-readable unrealized-MTM table — need Rama's explicit trigger). Parity: `mode` displayed attribute everywhere, no mode branch. Git HELD (no branch/commit/push); ready for `gui-g5-phaseB-04jul`. Next = Web Claude reviews → issues Phase C sub-phase files ONE at a time (G5a first); no coding until Rama approves Phase B. memory `gui_g5_phaseB_04jul`.
>
> **G5 — PHASE C · G5a: SHARED FOUNDATION + NAVIGATION + COMPONENT FRAMEWORK BUILT (04-Jul; first CODE sub-phase; frontend-only + ONE additive backend context-processor; NO schema/trading/broker/endpoint/reader/service change — git-proven 0 files touched in `backend/api,readers,services` + `core/schema.sql`).** **279 tests green (both v41+v42)** = 238 baseline + new G5a tests; base HEAD `80be23a`; deployed VM app (`7b1c92d`) UNTOUCHED → soak resumes there; Git HELD (no branch/commit/push; formalize on `gui-g5a-04jul`). **Nav = 5 groups** in `base.html` — Dashboard(landing `/`) · Trading(Strategies/Signals/Orders/Positions/Holdings) · Analytics(Strategy Ranking/Health/Scanner Attribution/Trade Explorer/P&L = dimmed `nv-soon` placeholders + Slippage/Execution links) · Operations(Live Activity/System Health/Capital&Risk = soon + Configuration/Controls links) · Investigation(Audit link, Trade Logs soon, System Logs link). **Not-yet-built screens render as dimmed `nv-soon` placeholders (no href → no 404).** Standalone screens dropped from the sidebar (risk/capital/exposure/capacity/pnl/services/vm/statistics/reports/alerts) keep their routes+endpoints UNTOUCHED (all still 200 by direct URL; `test_all_nav_pages_render` proves it). **13 shared components** (Alpine-over-JSON): macros `frontend/templates/components.html` (`data_table`/`filter_bar`/`kpi_row`+`kpi_card`/`period_selector`/`events_alerts_panel`[L10]/`two_state_panel`[L3/L9]/`export_button`[L7]/`score_chip`[L8 System Score only]/`medal`/`status_chip`/`funnel_pipeline_card`/`lifecycle_timeline`) + mixins `frontend/static/components.js` (`tableMixin`/`filterMixin`/`periodMixin`, loaded sync before Alpine) + styles `style.css` `/* == G5a component framework == */` + contracts `ops_dashboard/docs/COMPONENTS.md`. `gui_flags` (`table_export_enabled`/`reports_download_enabled`, DEFAULT OFF) injected via an `app.py` context processor → ExportButton renders DISABLED (Q3). **AlgoCore rebrand = LABEL layer ONLY** (base/login/dashboard brand+titles; internal name `ops_dashboard`, modules, routes, config keys UNCHANGED; git shows no R/D). **Additive-only invariant HELD** (`test_pinned_contract_counts_unmoved` + contract tests): service_health==6 / pipeline stages==13 / capacity rows==8 & groups==6 / rankings=={4 keys} all unmoved; new data → NEW fields only. New tests `tests/test_nav.py` + `tests/test_g5a_components.py`; isolation I7 (AST + CDN gate) auto-covers new files. Deviation: JS sort/pagination BEHAVIOUR not unit-tested (no JS runner in-stack) → Rama's interactive browser pass. Reconciliation: G5a-T5 named "Alerts" a mobile nav item but L10 merges Alerts → honored L10 (Alerts not a sidebar item; `/alerts` still 200; surfaces via EventsAlertsPanel in G5b). Discipline: no half-converted screens — each screen keeps its current table until its own sub-phase. Next = Web Claude reviews component contracts + preservation proof → G5b (reuse screens). memory `gui_g5a_foundation_04jul`.
>
> **G5 — PHASE C · G5b: REUSE SCREENS BUILT (04-Jul; 7 screens on G5a components + additive-only backend; NO schema/trading/broker change).** **306 tests green (v41+v42)** = 279 baseline + 27 G5b; base `80be23a`; deployed VM `7b1c92d` untouched; Git HELD (branch `gui-g5b-04jul`). **api/ touched = ONLY `analytics.py` (slippage +keys) + `risk_capital.py` (exposure +by_direction)** — 0 endpoint removed/renamed, 0 schema, 0 renames/deletes (git-proven); **Reports UNTOUCHED**. **Screens:** Configuration/System-Logs/Audit (pure-frontend reflow onto `kpi_row`/`data_table`/`filter_bar` + honest **G-5** "author/user not captured", "resolution not tracked"; endpoints unchanged) · Slippage (kept breach model; NEW per_scanner/per_symbol/price_buckets/trend panels; `period_selector` emit-only, honest "Week/Month arrive G5c") · Strategies (NEW dense `data_table` view toggle + `family`/`sl_tgt_hits`/`profit_factor` in cards) · Dashboard (NEW "Today's Trading Summary" `kpi_row` clickable deep-links + Profit Factor + merged Alerts banner L10; **pinned summary-bar + 13-stage pipeline + chip-blink==1 UNMOVED**) · **NEW `capital_risk.html`** (route `/capital-risk` added to `app.py:_PAGES`; nav placeholder→link; L5 consolidation composing the PRESERVED `/api/risk`+`/capital`+`/exposure`+`/capacity` into 4 zones; long/short/net exposure; **MTM = `two_state_panel` "Pending Broker Source (G4)"** honest G-1; Recent Risk Events via `events_alerts_panel` L10; **old /risk /capital /exposure /capacity still 200 by direct URL**). **Additive backend (read-only mode=ro, EXISTING tables, NO schema, exact-verified on seed):** `db_reader.scanner_for_trades` (shared scanner→trade join `trades.signal_id→signals.scanner`, reused by G5c) · `strategy_sltgt_hits` · `exposure_by_direction` (long 40000/short 0/net 40000) · `profit_factor_today` (0.89) · `slippage_trend_today`. `/api/slippage`+=per_scanner/per_symbol/price_buckets/trend · `/api/exposure`+=by_direction · `/api/dashboard`.counters+=profit_factor · `strategy_tower` per-row+=`family`/`sl_tgt_hits`/`performance.profit_factor` (2.0) — **funnel/failures equality-dicts + rankings(4 keys) UNMOVED**. Fixture: conftest.py +2 YESTERDAY-dated linking signals (sig_trd_c1/c4) so the scanner-join resolves; yesterday-dated ⇒ invisible to all today-scoped signal counts. ExportButton OFF everywhere; parity (mode = displayed attribute, no branch); no half-converted screens. tests `test_g5b_reuse.py`. Next = Web Claude reviews → G5c (NEW analytics: Strategy Ranking/Health, Scanner Attribution, Trade Explorer, P&L Analytics). memory `gui_g5b_reuse_screens_04jul`.
>
> **G5 — PHASE C · G5c: 5 NEW ANALYTICS SCREENS BUILT (04-Jul; additive — new routes/endpoints/readers; existing endpoints UNTOUCHED; NO schema/trading/broker).** **333 tests green (v41+v42)** = 306 baseline + 27 G5c; base `80be23a`; deployed VM `7b1c92d` untouched; Git HELD (`gui-g5c-04jul`). **0 core/trading/schema files changed, 0 renames/deletes, 0 Reports lines, `/api/pnl` (today-scoped) untouched** (git-proven). **Multi-period foundation:** `freshness.resolve_period(period,from,to,now)` → IST (from,to): today · **week**=trailing-7d incl today · **month**=trailing-30d · custom (trailing windows, deterministic — flagged for review). Period-scoped readers wrap existing today-logic without mutating it: `db_reader.closed_trades_range`(spine)/`trades_in_range`/`signals_scanner_funnel_range`/`strategy_signal_counts_range`/`webhook_by_scanner_range`/`screener_scores`(System Score L8)/`trade_story_parts` — all EXISTING tables, mode=ro, NO schema. **Service `analytics_period.py`** documented formulas: win_rate=100×wins/decided · **expectancy**=(win%×avgWin)−(loss%×|avgLoss|) · **profit_factor**=Σwin/Σ|loss| · roi=100×net/Σmargin · **opportunity_quality(scanner,0-100)**=100×accept_rate×trade_conversion×win_rate (factor=0 when its denominator is 0 — no evidence, never fabricated) · **health_state** precedence Disabled(enabled False)→Silent(silence RED)→Warning(badge RED)→Quiet(badge YELLOW|silence YELLOW)→Healthy(badge GREEN); health_score 90/70/40/20/null. **6 NEW endpoints** (blueprint `api/analytics2.py`, registered; login_required): `/api/{strategy-ranking,strategy-health,scanner-attribution,trades,trade-story/<id>,analytics/pnl}` — all `?period&from&to`; `/api/analytics/pnl` is DISTINCT from the today-scoped `/api/pnl`. **Screens** (routes in `app.py:_PAGES`; 5 Analytics nav placeholders → live): Strategy Ranking (mode toggle Net/ROI/Win%/PF/Trades re-sorts same dataset + medals) · Strategy Health (5-state chips + health score + period signal-trend + merged Warnings/Alerts L10) · Scanner Attribution (funnel Signals→Accepted→Orders→Trades + quality + medals; unmapped scanner → "scanner-level (shared)" honest R1) · Trade Explorer (filter_bar+data_table+trade-story `lifecycle_timeline`; Validation/Risk/Capital timings = honest **"not captured (G-2)"**; raw Chartink payload from `signals.webhook_payload`) · P&L Analytics (period totals + per strategy/scanner/direction + realized equity curve + **MTM `two_state_panel` "Pending Broker Source (G4)"** G-1). **Fixture:** conftest +`screener_results` (System Score) +3 multi-day gap_fade_long closed trades (trd_w1/w2 YDAY, trd_m1 TENDAYS) + signals — ALL older than today's 14:50 win ⇒ loss-streak assertions UNMOVED; dated ⇒ invisible to today-scoped counts. Verified: closed today=4/week=6/month=7; ranking gap_fade_long net 100(today)/150(week); scanner gap_fade_long quality 1.7; trade-story trd_c1 score 72. Additive-only HELD (pinned 6/13/8/6/4 + HARD_KILL blink unmoved); ExportButton OFF; ScoreChip single-score (grep clean); parity (no mode branch). tests `test_g5c_analytics.py`; `test_nav.py` `_SOON` updated → (Live Activity, System Health, Trade Logs) remaining G5d placeholders. Next = Web Claude reviews formulas + preservation → G5d (two-state Positions/Holdings · Live Activity · Trade Logs · Controls read-only summary). memory `gui_g5c_analytics_04jul`.
>
> **G5 — PHASE C · G5d: OPERATIONS/INVESTIGATION + FINAL NAV BUILT (04-Jul; additive; NO schema/trading/broker; G5 BUILD COMPLETE — only G5e deploy remains).** **354 tests green (v41+v42)** = 333 + 21 G5d; base `80be23a`; deployed VM `7b1c92d` untouched; Git HELD (`gui-g5d-04jul`). **0 core/trading/schema files, 0 renames/deletes, 0 Reports lines** (git-proven). **TWO-STATE CONTRACT (permanent, L3/L9 — broker data honestly UNAVAILABLE, never faked):** **Positions** (`positions.html`, route preserved) SYSTEM side full (date/time/strategy/**scanner**/symbol/dir/qty/entry/SL/TGT/risk/margin/age/inning) + `two_state_panel` "Live LTP/MTM/Unrealized/Current-RR — Pending Broker Source (G4)"; MTM cell "—". `/api/positions` additive: `unavailable`{fields:[ltp,mtm,unrealized,current_rr],reason} + per-row `scanner` (existing keys frozen, unrealized still "—"). **Holdings** (`holdings.html`, route preserved, broker-first) SYSTEM = enriched `gtt_state` (holdings_list +LEFT JOIN trades → date/strategy/symbol/qty/avg, additive) + `two_state_panel` "Broker Qty/Delta/Reconciliation Status — Pending Broker Source (G4/P1)"; broker/delta/recon cells "—" (P1 unpushed → activates when P1 ships). **Controls = READ-ONLY summary** (`controls.html`; NEW `/api/controls-summary`; **ZERO write path grep-proven — no form/button/POST**; 8 fields Trade-Type/Strategies-Enabled/Telegram[honest config-only]/Max-Trades/Positions/Concentration/Daily-Loss/Kill-Switch; G3-future = disabled placeholder, L4). **Trade Logs** (NEW `/trade-logs`, `/api/trade-logs`; `trades_in_range`+`scanner_for_trades`+`screener_scores`+NEW `db_reader.recon_actions_for_trades`; raw log-line drill reuses `/api/logs?id=` log_reader ref_id UNTOUCHED; distinct from System Logs). **Live Activity** (NEW `/live-activity`, `/api/activity`, L10; `services/operations.build_activity` merges 7 sources signals/orders/trades/system_events/fm_ledger/kill via existing readers; types+per-min-pulse+deep-links; no standalone Alerts, /alerts still 200). Blueprint `api/operations.py` + service `services/operations.py`. **FINAL 5-group nav COMPLETE (no nv-soon):** Dashboard · Trading(5) · Analytics(7) · Operations(Live Activity/**System Health→carried /services**/Capital&Risk/Config/Controls) · Investigation(Audit/Trade Logs/System Logs). **System Health = CARRIED** (nav→existing /services; full services+vm consolidation never assigned a sub-phase — deferred, flagged). **Carried-not-redesigned** (pre-G5 templates, render+200): Signals, Orders, Execution Analytics, Services(=System Health), VM. Non-menu direct-URL 200: /risk /capital /exposure /capacity /pnl /vm /statistics /reports /alerts. Additive-only HELD (pinned 6/13/8/6/4 + HARD_KILL chip-blink==1 unmoved); ExportButton OFF; ScoreChip single-score (grep clean); parity (no mode branch). Verified: controls 4/5 enabled, trade-logs week 10 rows (trd_c3 1 recon action), activity ≥5 source types newest-first. tests `test_g5d_operations.py`; `test_nav.py` menu-complete update. **The G5 redesign BUILD is complete across G5a-G5d; G5e = VM deploy + resume soak (the only remaining sub-phase).** memory `gui_g5d_operations_04jul`.
>
> **G5 — PHASE C · G5e: DEPLOYMENT + VALIDATION PLAN AUTHORED (04-Jul; PLANNING ARTIFACT ONLY — no deploy/merge/push/restart/production change; Git HELD, doc written to working tree).** Doc `ops_dashboard/docs/G5e_DEPLOYMENT_PLAN.md` (sections **A–I**: deployment checklist · rollback · soak-resume · smoke · cutover readiness · risk · go/no-go · validation matrix · report-back format). **Ground truth (read-only):** `origin/main == HEAD == 80be23a` (carries deployed G2 ops_dashboard); **rollback baseline `7b1c92d`** (pre-G5 GUI merge — its `ops_dashboard/` tree is functionally identical to `80be23a`, only `deployment/INSTALL.md` doc differs) → plan recommends tag `gui-pre-g5-rollback@80be23a` in cutover-prep. **G5 build = UNCOMMITTED working tree** (NO `gui-g5*` branch exists yet — `git branch -a` verified); **44 `ops_dashboard/**` files** (22 modified + 22 untracked). **MERGE-GATE (critical, run FIRST):** create `gui-g5-deploy` off `origin/main`, stage ONLY `ops_dashboard/`, then name-only diff must be `^ops_dashboard/` ONLY — **STOP** on any `core/ orders/ capital/ strategies/ broker/ signals/ main.py config/ schema` path. **Must EXCLUDE from the deploy branch** (non-G5 working-tree noise): `PATHS.md`, `docs/SYSTEM_MAP.md` (both mixed w/ telegram/chartink edits), `docs/audit/{c2_chartink,telegram_token_shadow}...md`, `gui/` (PC source specs). **Deploy = REFRESH not fresh-install:** off-market push (never 15:30–17:05 IST; `deploy_preflight.py` market-open gate) → post-receive checks out `ops_dashboard/` → restart **`gui-dashboard` ONLY** (trading stack/6 units/DBs/cron untouched) → verify overlay `gui_config.local.yaml` survived (`chmod 600` + `secure:true`) → Tailscale serve persists (no re-auth) → curl `127.0.0.1:8500` → 302 /login. **Rollback = GUI-only blast radius** (single `git revert -m 1` + push + GUI restart; zero schema to unwind). **Smoke = 21 menu screens (5 groups) + 9 off-menu direct-URL routes + contract checks** (13 pipeline stages · ExportButton OFF · two-state honesty · Controls no-write · ScoreChip single). **Soak = resume PAUSED S1–S8 on the G5 build + new-screen validation (Trade Explorer/P&L Analytics/Live Activity/two-state Positions·Holdings/Controls); ≥3 clean market days = PASS.** **GO only if ALL:** 354 tests green on deploy branch (clean env — PC has 32 known env-only failures) · merge-gate diff clean · smoke pass · **Rama's browser sign-off (the outstanding NOT-RUN human gate, before promotion)** · rollback tag confirmed · off-market window. **Q3/XLSX stays OFF at cutover.** Zero execution performed. memory `gui_g5e_deploy_plan_04jul`.
>
> **G5 — PRE-CUTOVER LOCAL VALIDATION DONE (04-Jul; local branch+commit+run+evidence ONLY — no deploy/merge/push/VM-restart).** Evidence `ops_dashboard/docs/G5_SIGNOFF_EVIDENCE.md`. Local deploy branch **`gui-g5-deploy` @ `91cc207`** off `origin/main`(`80be23a`), **NOT pushed**; merge-gate CLEAN = committed diff `origin/main..HEAD` **45 files, all `ops_dashboard/**`** (5 non-G5 dirty paths PATHS.md/SYSTEM_MAP.md/docs/audit×2/gui left UNSTAGED). **Clean-env (throwaway `.venv_validate`, deleted after): `pytest -q` → 354 passed / 0 failed** (I4 kiteconnect-absent PASS; 0 env-only in this isolated suite — the ~32 known PC failures are the MAIN suite). **28/28 gate tests PASS**: pinned 6/13/8/6/4 · HARD_KILL blink==1 · two-state honesty · single System Score · export OFF · isolation I1–I7 incl. **CDN/no-network gate** (`test_d_no_cdn_or_network_refs_in_frontend`). **Route sweep: 0 404 / 0 500** — live unauth 61 routes (30 page→302, 30 api→401, /login→200) + authed test-client 60×2=120 all 200. **STEP 5 browser sign-off = PENDING (Rama's binding gate; no autonomous login — overlay stores password HASH only).** **VERDICT: CONDITIONAL GO** — all machine-verifiable gates green; recommend GO contingent on Rama's browser pass; NO-GO if any STEP-5 visual/content item fails. Recommendation only — merge/push/deploy/soak later off-market on Rama's GO. memory `gui_g5_local_validation_04jul`.
>
> **G5 — DEPLOYED TO VM (04-Jul, off-market, GUI-only). LIVE at https://trading-system.tail1cdc6d.ts.net.** Merge `b4aea6f` (`gui-g5-deploy`@`91cc207` merged `--no-ff` → origin/main; VM bare-repo main HEAD == `b4aea6f`; deployed `base.html` now has the Investigation nav group). Pre-G5 tip `80be23a`; **rollback tag `gui-pre-g5-rollback`→`80be23a`** (== live G2, functionally == `7b1c92d`; GUI-only rollback = revert merge/redeploy tag → restart gui-dashboard). **BINDING GATE (clean VM venv):** `pytest` 354/0 (I4 kiteconnect-absent; 0 env-only on clean venv; 28/28 pinned 6/13/8/6/4 + HARD_KILL blink + isolation + CDN). **Merge-gate CLEAN at push AND merge** (`ops_dashboard/**` only; post-receive is main-only so the branch push didn't deploy; crontab reinstall was a no-op). **Deploy:** overlay survived checkout -f (600, secure:true, real DB path); ONLY gui-dashboard restarted → active; **trading stack byte-untouched** (trading-system inactive, token-watcher active); Tailscale persisted. **SMOKE:** 61-route sweep 0×404/0×500; no "Signal Score"; controls 0 write-controls; "Pending Broker Source" present; export OFF. **REAL-DATA (authed test-client on REAL read-only DB vs SQL — ALL MATCH):** P&L month closed 81/net 43.67 · Strategy Ranking positional_sector_rotation 12/31.99 · Scanner Attribution accepted 19572 · Trade Explorer 228 · Trade Story assembles from real joins · Dashboard(Sun) trades_today 0 · Positions/Holdings broker side honestly `unavailable` (no fake numbers) · Controls read_only:true · Pipeline 13 stages. Latest real trading day = 2026-07-03. **G5.1 UI/UX backlog seeded** (scanner quality_score≈0 uniform on real data; large-funnel formatting; None-on-non-trading-day rendering; empty-states; 228-row table layout) — NOT deploy blockers. NEXT = Rama full browser review on real data → G5.1 polish list; soak S1–S8 resumes Mon 06-Jul 08:15 per the locked contract. memory `gui_g5e_executed_04jul`.

> ## ✅ CURRENT STATUS (2026-06-19) — RESUMED LIVE; FIX-190 CLOSED
> **Resumed live 13:24 IST** (HARD_KILL force-cleared via `deploy/resume.sh --force`,
> root cause fixed by Bug C/D; morning trades reconciled to CLOSED_MANUAL, orphans
> cancelled, capital consistent). Running LIVE with `live_test_mode` (now **max_open=4
> / 6-per-day, PERMANENT** — active bug-hunting with small qty on ₹10k).
> **FIX-190 live incident at ~10:00 IST.** First real trading after the Kite IP
> allowlist was fixed: a Chartink spike fired 5 entries in ~5s; THELEELA's target
> exceeded the upper-circuit band → a single recoverable TGT rejection cascaded
> into a full **HARD_KILL that flattened every open position and left orphan
> SL/TGT orders**, double-selling THELEELA into a naked short. Rama manually
> recovered (net loss ₹2.87).
>
> **Stage-4 ALL fixes LANDED (committed+pushed; unit + replay tested):** C
> (TGT-only no HARD_KILL), D (circuit-band clamp), A (reverse-aware flatten — no
> oversell), E (cancel resting exits — no orphans), F (no duplicate G5b SL), G
> (entry throttle: 20s gap / 3-per-60s + per-symbol 5-min cooldown), H (`live_test_mode`: live caps now **max_open=4 /
> 6-per-day, permanent**), I (in-session drift tolerance), **B** (status semantics
> correct via FIX-181 + observability metrics on `/metrics`: signals_processed /
> entries_placed / entries_throttled / entries_rejected — **but the daily-cap RACE
> was NOT closed**; see the 2026-06-19 "Bug B" Changelog entry for the
> reservation-aware DAILY_TRADES fix that closes the 18-Jun 8-vs-5 overshoot).
> Incident replay green
> (`tests/integration/test_fix190_incident_replay.py`). **Paper mode SKIPPED per
> Rama** (stay LIVE with tiny ₹10k for active bug-hunting). Service is LIVE with
> `live_test_mode=true` (4 positions / 6 trades-per-day, permanent). See memory
> `fix_190_incident`. (FIX-189 dash-cron / market-window / EOD-self-exit separate.)

> **GUI redesign, one-screen-at-a-time (started 08-Jul-2026):** Screen 01
> (Login) **DONE** — branch `gui-redesign-login-08jul`@`3537632`, off
> `main`@`271d24f`, LOCAL/UNPUSHED. Presentation-only, single file
> (`ops_dashboard/frontend/templates/login.html` — self-contained, since
> `/static/*` requires auth per the `_LOGIN_EXEMPT` guard in `backend/app.py`,
> so no separate hero asset file could be served without touching that
> guard). Gap analysis: `ops_dashboard/docs/redesign/01_login_gap.md` —
> corrects the task premise (the deployed login had **no hero pane at all**;
> `gui/01. Login-Screen.png` is a target mockup, never previously
> implemented in this repo, confirmed via full git-history search). Added:
> 40/55 hero/card split (mobile ≤780px hides hero, card centered), inline
> SVG chip+heraldic-eagle hero (pins on all 4 sides, neon-blue glow, no
> external file/CDN), field placeholders, footer shield icon, two-tone
> title, Inter/IBM Plex Sans stack (matches `style.css` G5a convention).
> Zero backend/route/API/auth changes (confirmed via diff — only
> `login.html` touched). 354/354 tests pass incl. CDN/isolation gates.
> Awaiting Rama's browser sign-off before Screen 02 (Dashboard) starts.

## Pre-Work Checklist
- [ ] Read this file (and `PATHS.md`).
- [ ] Identify exactly which files/paths you will touch.
- [ ] Confirm no duplicate/canonical version exists for your target (see "Known Duplicates / Issues").
- [ ] Remember: **deploy ≠ restart**. `git push` updates the VM working tree but does NOT
      restart `trading-system.service`. A manual restart is required to activate code.
- [ ] Config edits change the file hash → may surface in startup checks; edit the canonical file only.
- [ ] After work: **update this file** (and the Changelog) if any path/file/cron/service changed.

---

## VM Paths  (host 161.118.187.249, user `ubuntu`, tz Asia/Kolkata / IST)

### Project Root
`/home/ubuntu/systems/trading-system/`  ← the running working tree (git-checked-out by deploy hook)

### Shared venv (OUTSIDE the project)
`/home/ubuntu/systems/venv/`  — Python **3.12.3**. Always invoke as
`PYTHONPATH=. /home/ubuntu/systems/venv/bin/python ...`.
Key pkgs: kiteconnect 5.1.0, pydantic 2.13.0, Flask 3.1.3, openpyxl 3.1.5, requests 2.33.1, pytest 9.0.3.

### Agent CLIs (`~/tools/`, OUTSIDE the project)
| Path | What | Used by |
|---|---|---|
| `~/tools/antigravity/agy` | **Antigravity CLI** (`agy`, ~164 MB binary). Google-AI-Pro OAuth (`~/.gemini/`), NOT an API key. Engine behind the AI-ops crons. Governed by `AGENTS.md` ("Cross-Tool Agent Charter") when run in the project dir. | `scripts/gemini_*.py` (premarket brief, log review, trade coach, data integrity, weekly patterns); `.env` `GEMINI_BIN` |
| `~/tools/gemini/` | Node-based **Gemini CLI** (package.json + node_modules: @google, keytar, node-pty). | secondary AI tooling |
| `~/tools/claude/` | **Claude Code CLI** dir. `cron.log` = output of the 4×/day heartbeat (`/usr/bin/claude -p "random 8-char string"` @ 05:30/10:31/15:32/20:33). | claude heartbeat cron |

> ✅ **The claude heartbeat cron is GOVERNED** (23-Jun): the 4 cron lines now `cd /home/ubuntu/tools/claude`
> first, loading `~/tools/claude/AGENTS.md` (charter) + `~/tools/claude/.claude/settings.json` (hard
> `permissions.deny`: `Write/Edit(**/*.py)`, `Read/Write/Edit(**/*.db)` + `Bash(sqlite3:*)`,
> `Bash(systemctl|sudo|service:*)`, `Write/Edit(~/systems/**)`, read of the trading `.env`, `git push`).
> Mirrors AGY's prohibitions (no `.py` writes, no DB, no systemctl). **Verified 23-Jun:** heartbeat still
> works; a `.py` write AND a `.db` read were both blocked (the `.db`-read block — normally default-allowed —
> proves `settings.json` is loaded). Guardrail files are **VM-local** (outside the repo); crontab backup at
> `~/tools/claude/crontab.backup.*`. ⚠️ These 4 heartbeat lines live in the **live crontab only** — NOT in
> `deploy/cron/trading-system.cron`; a future canonical crontab reinstall would drop them (reconcile if you want them permanent).

### Deploy (git push, NOT scp)
- Bare repo: `/home/ubuntu/trading-system.git/` with `hooks/post-receive`.
- Hook does: `git --work-tree=/home/ubuntu/systems/trading-system --git-dir=… checkout -f <branch>`.
- It does **NOT** restart the service. (See PC Paths → SCP Deployment Map.)
- **Self-maintaining cron (ARMED 23-Jun):** `post-receive` (1336 B, from `deploy/hooks/post-receive`) now ALSO regenerates the crontab from the deployed `cron_registry.yaml` and **auto-installs** it *iff* `generate(registry) == deploy/cron/trading-system.cron` (else WARNs + skips — self-protecting). Replaced the old 329-B checkout-only hook. The **pre-receive** equality guard (`deploy/hooks/pre-receive`) is **DEFERRED — NOT installed** (deliberate, 23-Jun); `pre-commit` (`deploy/hooks/pre-commit`, local-clone-only) now ALSO runs a **C-1 secret scan** (`deploy/hooks/secret_scan.py`, 02-Jul) as its FIRST step — blocks committing a real credential / a real `.env` / a non-placeholder `.env.example` (11 tests `tests/unit/test_secret_scan.py`; install: `cp deploy/hooks/pre-commit .git/hooks/ && chmod +x`). See memory `cron_framework_armed_23jun` / `c1_secret_remediation_02jul` + Changelog 23-Jun.

### ⚠️ ARCHITECTURAL RULE — SCHEMA MIGRATIONS RUN ON DB-OPEN, NOT "AT BOOT" (14-Jul-2026)

⚠️ **SCHEMA VERSION SOURCE — ROLLBACK TRAP (24-Jul-2026):** the schema version lives in **`schema_meta`** (a `(key, value)` table): `SELECT value FROM schema_meta WHERE key='schema_version'` → **`44`**. **`PRAGMA user_version` is `0`** and is NOT the schema version — an operator doing a rollback who reads `user_version` (the natural reflex) will see `0` and wrongly conclude the DB is unversioned / version zero. **Read `schema_meta`, never the pragma.** (Verified live 24-Jul: `schema_meta.value='44'`, `PRAGMA user_version=0`.)
`StateStore.__init__` → `_initialize_schema()` reads the on-disk `schema_version` and, if it is **older** than the code's `EXPECTED_SCHEMA_VERSION`, runs `run_migrations()` (which **rebuilds** affected tables) then `executescript(schema.sql)`. **This happens whenever ANY process constructs a `StateStore` against the live DB with newer code** — a cron, a monitor, a report, or a research script — **whichever opens it first after a schema-changing push.** The 08:15 `main.py` boot is only the *first opener on a normal day*, NOT the migration mechanism.
- **Proof (13-Jul):** v42→v43 migrated at **19:30** (`cron_watchdog`); v43→v44 at **23:41** (`forward_shadow_record`). **Neither fired at boot.** Both were additive + off-market → harmless by luck of timing, not design.
- **RISK:** a schema pushed such that a **during-market** StateStore-opener runs first → migration (a live-table rebuild) runs **09:00–15:30 with `main.py` running on the DB**. The `cron_officer` cron opens the live DB at **09:20 + hourly** = the realistic mid-session trigger. The off-market-push convention is the *only* thing preventing this today; it is not enforced.
- **NOT triggers (by mechanism, not intent):** the GUI (`?mode=ro` — a ro conn can't `executescript`), `capture_metrics_baseline` (`*/5 09-15`, raw `db_connect`, no StateStore), `db_backup` (`sqlite3 .backup` CLI).
- **COROLLARY:** a "read-only verification" against the LIVE DB with newer code is **NOT read-only** — opening the StateStore migrates it. Always verify with `--db <copy>`.
- **Full inventory + a PROPOSED (not implemented) migration guard** (market-hours refusal / `--allow-migrate` / deploy preflight): `docs/audit/migration_on_open_rule_14jul2026.md`. Every future schema deploy MUST identify which process realistically opens the DB first and reason about WHEN. **Guard = PROPOSAL, PAUSED for review.**

### ⚠️ ENGINEERING RULE — A TEST HARNESS MUST NEVER HOLD A WRITABLE HANDLE ON A LIVE DB (18-Jul-2026)
**A destructive / crash-test harness must NEVER hold a writable handle on a live database. ALL writable DB access goes through ONE guard that fails CLOSED — no override flag, no environment escape hatch. Resetting a live system is an OPERATOR tool in `scripts/` with a backup + confirmation gate, NEVER part of a test harness.**
- **Why it exists:** `tests/crash_test/ct_utils.py` hardcoded the live DB and `get_db_connection()` defaulted to **writable**; `reports/crash_test/cleanup_log.jsonl` records real live writes on **05-Jun and 07-Jun 2026** ("Kill switch cleared (1 rows updated)", "Released 37 orphan reservations", a `hard_cleanup` that cancelled trades/orders and reset capital). Five further modules bypassed the guard entirely with raw `sqlite3.connect()` on a hardcoded live path.
- **The guard:** `ct_utils.assert_not_live_db()` → raises `LiveDatabaseRefused`. Compares canonical `realpath` **plus `os.path.samefile()`** (the only thing that catches a **hardlink**, which `realpath` cannot resolve) and refuses the `-wal`/`-shm` sidecars. Proven against absolute/relative/`..`/symlink/hardlink/env-override routes. **Read-only live access stays allowed** (`get_db_connection(readonly=True)`, `mode=ro`) — that is the harness's legitimate diagnostic use.
- **Enforced automatically, not by memory** — `tests/crash_test/test_ct_guard_invariant.py` (AST scan of every harness module): **(A)** no raw `sqlite3.connect()` outside `ct_utils.py`; **(B)** no live-DB path literal outside `ct_utils.py` (*you cannot open what you may not name*); **(C)** `StateStore(db_path=…)` never receives `LIVE_DB_PATH`. **Proven to bite:** a planted bypass makes A+B fail; removing it restores green. Allow-list is 3 files, each justified in-line.
- **Corollary (composes with the migration-on-open rule above):** a harness passing the live path to `StateStore` is doubly dangerous — it opens **writable** *and* can migrate the schema on open.
- Detail: `docs/audit/ct_harness_safety_18jul2026.md` · `docs/audit/ct_guard_invariant_and_secwatcher_18jul2026.md`. **⚠️ OPEN (Rama):** `cleanup.py --live` now REFUSES — does live-reset return as a `scripts/` operator tool with backup + confirmation?

### Config Files  (`config/`)

⚠️ **LATENT CONFIG BEHAVIOUR — relaxing `entry_start` auto-activates 16 strategies (recorded 24-Jul, MEASURED):** **16** strategy files in `config/strategies/` declare `entry_start_time: "09:25"` (all uniform — verified by grep), while the global `trading_hours.entry_start` gates entries to `10:00` (launch-phase conservative; the 04-Jul audit called this divergence "cosmetic"). The global gate is the ONLY thing holding them back: **if `entry_start` is relaxed toward 09:20 — an OPEN decision on Rama's board — all 16 begin entering ~35 min earlier with NO strategy edit and no announcement.** A latent behaviour change sitting in config, directly relevant to the `entry_start` decision — NOT a defect. (The G5 config-auditor check deliberately stays quiet on this gated divergence — see `core/config_auditor.py` G5.)
| File | Purpose | Read by |
|---|---|---|
| `system_config.yaml` | Master config (trading_hours, capital, risk, position_sizing, leverage_map, alerts.telegram, order_reconciler, eod_squareoff, webhook, …). **C-2 (02-Jul):** `webhook.per_ip_{rate_limit_enabled,burst,refill_per_sec}` token-bucket per-IP limit (429); `WEBHOOK_SECRET` now required in BOTH modes (`main.required_startup_secrets`); bind stays `0.0.0.0`/`require_hmac:false` pending Phase-3 network control (IP-allowlist vs reverse-proxy) — memory `c2_webhook_lockdown_02jul`. **C-2 prep (03-Jul, read-only):** Chartink source-IP investigation over 15 trading days (58,507 hits) → **1 static IP (23.106.53.213, 99.99% of hits, unbroken daily coverage 12-Jun→03-Jul)**; 2 single-hit same-/24 outliers + Leaseweb Singapore (AS59253) ASN for all 3; verdict = static-so-far with insufficient data to rule out occasional pool-IP variation. Facts only, no A-vs-B call — full findings `docs/audit/c2_chartink_ip_investigation_03jul2026.md`, memory `c2_chartink_ip_investigation_03jul`. **WEBHOOK_SECRET ROTATED 03-Jul (VM-only, value never displayed)** — old token exposed in chat twice → permanently invalidated; single shared token (sole source `EnvironmentFile=.env`, no unit/drop-in override, consumed `main.py:2479`, validated `webhook_receiver.py:429`); old-rejected/new-accepted verified value-blind; loads Mon 08:15 boot; **Rama must update `?token=` in all 15 Chartink scanner URLs**; **MONDAY-OPEN: confirm 1st real Chartink signal accepted**; memory `webhook_token_rotated_03jul`. ✅ **TELEGRAM SHADOW FIXED (03-Jul ~23:40):** deleted the `telegram.conf` drop-in (held `TELEGRAM_BOT_TOKEN`+`CHANNEL_*` `Environment=` overrides; the token was REVOKED/401, so the main process had been running a dead token while cron used valid `.env`). Now `.env` (EnvironmentFile) is the **SINGLE source** for all telegram keys; `daemon-reload` done, verified **no `TELEGRAM_*` override** in `systemctl show -p Environment`, `.env` token re-confirmed VALID, dead-token backup shredded. Parity by construction (single unit, mode = runtime arg). **Activates Mon 08:15 boot** (main process inactive off-market; no restart tonight). **MONDAY-OPEN: confirm a main-process telegram alert delivers post-boot.** Root-cause findings `docs/audit/telegram_token_shadow_investigation_03jul2026.md`; memory `telegram_token_shadow_FIXED_03jul` + `telegram_token_shadow_investigation_03jul` | `core/config_loader.py` → SystemConfig |
| `broker_costs.yaml` | Brokerage/STT/charges model | `broker/cost_calculator.py` |
| `broker_limits.yaml` | Rate-limit buckets (order/quote/historical/margins) + 429 backoff | `broker/rate_limiter.py` |
| `slippage_model.yaml` | Slippage assumptions | `broker/slippage_engine.py` |
| `scoring_weights.yaml` | Quality scorer weights | `screening/quality_scorer.py` |
| `scan_webhook_map.yaml` | Chartink scan → strategy mapping | `signals/webhook_receiver.py` |
| `chartink_scanners.yaml` | Scanner definitions | screening/signals |
| `nse_holidays_2026.yaml` | Trading-holiday calendar | `core/market_windows.py` |
| `cron_registry.yaml` | **Cron SINGLE EXECUTABLE source of truth** (~41 lines; TASK #3 + 23-Jun framework) → `scripts/generate_crontab.py --generate` emits `deploy/cron/trading-system.cron` (ASCII+LF, deterministic) | `core/cron_registry.py` → `cron_officer`, `check_cron_drift`; `scripts/generate_crontab.py` |
| `symbol_aliases.yaml` | Symbol normalization | instrument resolution |
| `accounts.csv` | Account registry (LFL836 primary, paper_capital, env-var names) | `core/account_registry.py` |
| `accounts_multi_example.csv` | Template/example (NOT loaded) | — |
| `instruments.csv` | Instrument master (lot sizes, tokens) | `core/instrument_cache.py` |
| `config/reference_data/` | NSE reference data | reference lookups |
| `config/strategies/` | Per-strategy YAML configs | `strategies/loader.py` |
| `.env` (root, 0600-ish, NOT in git) | Secrets: ZERODHA_* — incl. `ZERODHA_USER_ID`/`ZERODHA_PASSWORD` + per-account `ZERODHA_TOTP_<acct>` (e.g. `ZERODHA_TOTP_LFL836`) for headless TOTP login (FIX-187); `GEMINI_BIN` (path to the `agy` CLI — agy auths via OAuth tokens in `~/.gemini/`, NOT an API key), WEBHOOK_SECRET, etc. | systemd `EnvironmentFile` + cron `set -a && . ./.env && set +a` |
| `.env.example` | Template for `.env` | — |

> Telegram bot token / chat IDs are also injected via the systemd drop-in
> (`/etc/systemd/system/trading-system.service.d/*.conf`). **Secrets live in `.env` and the
> drop-in only — never commit them.**

> **Email alerts (alert-watcher SMTP)** — `system_config.yaml` → `alerts.smtp`: Gmail
> `smtp.gmail.com:587` TLS, `username`/`from_address`/`to_addresses` = `ramakrishnan031@gmail.com`,
> password via `password_env: ALERT_SMTP_PASSWORD` (set in `.env`, not committed). `alert_watcher.py`
> emails CRITICAL sentinels with subject `[LFL836] <SEVERITY> — <title>` (digest when >3 pending).

### Python Modules (role per package)
| Package | Role | Key modules |
|---|---|---|
| `core/` | Infra: config, DB, events, time, IDs | config_loader, cron_registry, state_store, db_connect, events, logger, time_authority, market_windows, instrument_cache, account_registry, migrations, constants, schema.sql |
| `broker/` | Broker integration + polling | zerodha_adapter, angelone_adapter, order_monitor (2s fill poll), order_state_machine, rate_limiter, cost_calculator, slippage_engine, product_resolver, token_monitor, clock_skew_probe |
| `capital/` | Capital, risk, kill-switch | fund_manager, position_sizer, risk_engine, kill_switch, drift_handler, invariant, performance_allocator, shadow_engine, strategy_governor |
| `orders/` | Order lifecycle | order_placer, order_reconciler (15s; + SLICE2.5-P2 startup/15-min CNC-GTT monitor hook + delivery-trade exclusion), order_manager, eod_squareoff, smart_tgt_manager, tgt_retry_manager (30s TGT re-place), breakeven_manager, **structure_exit_manager** (SNR-V2 Phase B: structure-aware exit — on each 1m close, trail the SL below/above the nearest HIGH support/resistance [tighten-only + wrong-side guard, once/candle] + flatten on a CONFIRMED break [EXITING→cancel SL+TGT→reverse-aware marketable-LIMIT]; MIS LIMIT_TRIPLE only; SINGLE SL OWNER — no co-enable of trailing_sl_enabled; default-off `structure_exit_enabled`; subscribes CandleStore 1m closes), sl_breach_monitor, entry_engine, full_entry_engine, order_protocol_co, order_protocol_limit, price_math, shadow_tracker, cnc_gtt (CNC OCO-GTT placer + durable gtt_state persist/hydrate), **cnc_gtt_monitor** (overnight-GTT reconcile + GTT_EXIT + K6 ladder; ⚠️ **+ `_announce_paper_carry_blind_spot` (26-Jul-2026)** — PAPER-ONLY by a mode check: paper's positions/holdings/GTTs are in-memory and die with the nightly restart while `gtt_state` survives, so the morning after a paper carry the ladder emits a clean `GTT_EXIT` — P&L booked at today's LTP, capital released — for a position never held. It announces that BEFORE the misleading exit; it does NOT change what the reconcile then does. The carry is a **live-only** proof: `docs/audit/paper_overnight_carry_26jul2026.md`) |
| `signals/` | Ingestion | webhook_receiver, signal_processor (+ SNR-DETECTOR-V1 `_sr_observe` seam after a successful place(), both paths), entry_throttle (Bug G: global min-gap/burst + per-symbol cooldown) |
| `sr_detector/` | **SNR-DETECTOR-V1** shadow S&R detector + **SNR-V2 Phase A** WAIT_FOR_RETEST (PURE pkg: core/+stdlib only; default-off `wait_for_retest_enabled`) | V1: models, fetch, pivots, zones, confluence, flags, detector (bg worker → `sr_detector_results`). V2: `zone_builder` (DRY zone-build shared with V1), `zone_cache` (sync hot-path read), `zone_warmer` (daemon, caches resistance+support), `retest_confirm` (pure, direction-parameterised state machine — symmetric LONG+SHORT). The retest monitor/diverter (`RetestDiverter`: LONG→resistance / SHORT→support) live in `screening/retest_monitor.py`. **SNR-V2 Phase B** reuses the SAME warmed ZoneCache (`orders/structure_exit_manager.py` reads `.support` for an open LONG / `.resistance` for an open SHORT) — built/warmed when EITHER `wait_for_retest_enabled` OR `structure_exit_enabled` is on |
| `screening/` | Signal screening/scoring | entry_gate, quality_scorer, secondary_screener, step_executor |
| `data/` | Market data | live_feed (WS ticks), candle_store |
| `alerts/` | Alerting | telegram_notifier, critical (sentinels) |
| `strategies/` | Strategy config | loader, schema |
| `utils/` | Utilities/preflight | startup_checks, holiday_guard, instance_lock, cron_heartbeat |
| `reports/` | Reporting | daily_report, daily_review, style_constants, **daily_trade_review** (NEW DB-pure forensic generator — sheet 1 = ORDERS [one row/trade], sheet 2 = SIGNALS [one row/stored-signal, honest storage-basis denominator], sheet 3 = RECONCILIATION [integrity backbone, per-identity PASS/FAIL/PENDING + OVERALL verdict, FAIL-injection proven], sheet 4 = CONFIG [sectioned key/value of the day's resolved AppConfig from W0 config_snapshots; — pending W0 if no snapshot for the date], sheet 5 = STRATEGIES [5 tables from the trades truth layer — per-strategy/time-bucket/direction/RR daily + a trailing-window confidence-weighted composite ranking], sheet 6 = SLIPPAGE [6 blocks from trade_slippage_log — summary+3-leg decomposition, band("tier") analysis, strategy/stock-wise, worst-20, 10-day trend], + DASHBOARD [FINAL sheet placed FIRST — summarizes all six: reconciliation banner, coverage panel, yesterday-vs-today deltas, trading/profitability/capital, deterministic highlights; summary-agrees-with-detail]. **FINAL TAB ORDER: Dashboard·Reconciliation·Orders·Signals·Strategies·Slippage·Config (numeric prefixes dropped). TIER-1 report COMPLETE — 7 sheets.** Runs PARALLEL, does not touch daily_report/daily_review) |
| `scripts/` | ops/cron scripts | incl. `cron_officer.py` (TASK #3 briefing/eod/check-change) + `cron_report_render.py` (rich HTML/Telegram render, pure), `system_manager.py` (TASK #5 EOD deep cross-check, 18:45), 6 `gemini_*.py` (AI ops), auto_refresh_token, premarket_healthcheck, reconcile_positions/pnl, eod_cleanup/verify, etc. **T4 timezone operator tools (29-Jun, standalone — service imports none):** `ist_now.sh` (authoritative IST via `now_ist()`), `check_tz.sh` (loud self-check vs the broken `TZ='Asia/Kolkata' date`), `deploy_preflight.py` (pre-deploy: `date -u` PC↔VM agreement ≤120s + VM-authoritative market-open gate). |
| `ops/control_tower/` | **VM Operations Control Tower** (Phase 1) — READ-ONLY aggregator over the existing monitors (never blocks/acts on trading) | `runner` (the daily 17:05 entry: size-logger then aggregator; self_job-aware; skips non-trading days), `aggregator` (4 adapters + freshness + disk; `run_aggregation`), `reporter` (Telegram delta + pull HTML/CSV), `health`, `status`, `freshness`, `disk`, `size_logger`, `severity` (native→tower maps), `db` (findings DAO), `model`, `cli` (`ct list/ack/unack`) |
| `tests/` | 305 test files | unit/, integration/, crash_test/ |
| `main.py` (root) | App entrypoint | launched as `main.py --mode live` |

### Database  (`data_store/`)
- `trading_system.db` (~174 MB on VM) — **MAIN** DB (schema **v41**; **v41 added `config_snapshots`** — W0 daily-report redesign foundation: the FULL resolved runtime config per date (system writes at startup via `core/config_snapshotter.py`; the report reads only this table, never YAML), pure addition; **v40 added the 5 `control_tower_*` tables** (findings/runs/trends/status/freshness) — VM Ops Control Tower Phase 1a, pure addition; **v39 added `excursion_reconstruction_runs`** — MFE/MAE reconstruction audit, pure addition; **v38 added `retest_state`** — SNR-V2 Phase A restart-safe WAIT_FOR_RETEST parking (pure addition) + widened `signals.status` CHECK with `GLOB 'RETEST_*'` (signals table rebuilt via MIGRATION_TABLES[38]); **v37 added `sr_detector_results`** — SNR-DETECTOR-V1 shadow S&R log (one row per placed candidate; actual_*/win_loss/pnl EOD-backfilled), pure addition; **v36 added `gtt_state`** — durable one-OCO-GTT-per-CNC-trade source of truth, SLICE2.5-P2, pure addition; v35 added trades.tgt_risk_reward_applied/exits_verified; v33 preflight_*; v32 tolerance_*; v30 added trades.needs_tgt_retry/…; v31
  added the slippage-intelligence layer: order_execution_log + trade_slippage_log + market_execution_context;
  **v32 added trades + order_execution_log `tolerance_fraction_used` / `tolerance_source` for the Phase 3a
  slippage tolerance override hierarchy**). trades, orders, signals, fm_ledger, etc.
- `analytics.db` (~44 KB) — analytics split (v28); **ATTACHed** to the main DB at runtime.
- **Raw sqlite access MUST use `core.db_connect.connect`** (it sets up the ATTACH); plain `sqlite3`
  works only for read-only SELECTs against the main file.
- `data_store/session/zerodha_token.json` — broker session token (wiped 05:00 daily by cron).
- `data_store/backups/` — nightly `.backup` snapshots (7-day retention).
- `data_store/candles/` — candle artifacts.
- `data_store/critical_alert_*.flag` — CRITICAL sentinels written by `alerts/critical.py`,
  consumed by `alert-watcher.service` (VM) → renamed to `.delivered`. **Local-only/gitignored**
  (`data_store/` is ignored). **Retention (23-Jun):** the `sentinel_retention` cron (02:05 daily)
  deletes `critical_alert_*.delivered` older than 7 days and **NEVER** touches `*.flag` (pending) —
  email is the authoritative record, so there is no archive. The PC dev tree has no watcher, so test
  runs that write real sentinels accumulate `.flag` there; **root-caused 23-Jun** by pointing
  `test_fix132_email_fallback.py`'s `sentinel_dir` at a tmp dir (was `"data_store"`). VM stays clean
  (0 `.flag`, watcher healthy); PC's 42 test flags removed 23-Jun (445 earlier on 19-Jun).

### Logs  (`logs/`)
Daily-dated files: `system_YYYY-MM-DD.log`, `debug_*.log`, `reconciler_*.log`, `trades_*.log`,
plus per-cron `cron-*.log` / `*.log`. Plain `FileHandler` (not Rotating); a cron deletes `*.log`
older than 30 days (00:00). `failed_alerts.log` = Telegram delivery-failure JSON-lines.

### Reports & Outputs  (`reports/`)
`reports/daily/`, `reports/daily_review/`, `reports/flow_trace/`, `reports/log_review/`,
`reports/watchman/`, `reports/weekly_patterns/`, `reports/output/`, `reports/crash_test/`,
`reports/system_manager/` (TASK #5 `<date>.txt` EOD reports). NB (audit 19-Jun): daily
report is `reports/output/daily_report_<date>.xlsx`; most other reports are `.md` (not `.xlsx`);
`reports/daily/` + `reports/daily_review/` are currently empty.

### Tools / External
- `gemini` CLI at `/usr/local/bin/gemini` — drives the 6 `scripts/gemini_*.py` AI-ops jobs
  (a.k.a. "Antigravity/AGY" automation) + `trading-watchman.service`.
- `sqlite3` CLI for DB ops/backups.

---

## PC Paths  (developer machine, Windows)

### Local Project
`D:\Projects\trading-system`  (this git repo; mirrors the VM working tree).

### SCP Deployment Map
**There is no SCP.** Deployment is **git push**:
```
PC:  git push origin main            # origin = vm = trading-vm:~/trading-system.git
VM:  bare repo post-receive hook  →  git checkout -f  →  /home/ubuntu/systems/trading-system
     (service is NOT restarted automatically — restart manually to activate code)
```
- Git remotes (PC): `origin` **and** `vm` both point to `trading-vm:~/trading-system.git` (duplicate; see Issues).
- SSH host alias `trading-vm` (key `trading_vm_secure`, passwordless).

### SATS — Static Analysis · 🗑️ TOOLING REMOVED 29-Aug-2026 (scripts archived)
🔴 **`sats/` NO LONGER EXISTS.** The two isolated tool venvs — `semgrep-env` (373 MB,
Semgrep 1.167.0) and `bandit-env` (43 MB, Bandit 1.9.4), 415 MB and 10,152 files
between them — were deleted on 29-Aug-2026. Created 22-Jun-2026, run once, untouched
for 68 days. `sats/` was git-ignored throughout (`.gitignore:92`, added the same day by
`d79c186`), so it **never deployed**, `git log -- sats/` was **empty**, and its removal
changed nothing on the VM and nothing tracked.

**KEPT — the 27 KB work product, now tracked** in `docs/archive/sats_20260622/`:
| What | File |
|---|---|
| Scan scripts | `scan_bandit.bat`, `scan_semgrep.bat` |
| The only scan ever produced | `semgrep_20260622_203318.txt` — 22 findings, the 22-Jun triage |
| Semgrep baseline | `semgrep_baseline.txt` — pinned `65439ff` |

- ⚠️ **THE BASELINE IS STALE, AND THAT MAKES A NAIVE RE-RUN MISLEADING.** `scan_semgrep.bat`
  adds `--baseline-commit 65439ff` (2026-06-22) when `semgrep_baseline.txt` is present, so it
  reports only findings **NEW since that commit**. Every change after 22-Jun is outside that
  window. **Re-pin or delete the baseline before trusting any future scan** — an unchanged
  baseline would report a near-empty diff and read as "clean" when nothing was really examined.
- ⚠️ **Bandit appears never to have run** — a 43 MB venv was installed for it, but the archive
  holds only a *semgrep* report. No `bandit_*.txt` was ever produced.
- **To reinstate:** `pip install bandit semgrep` into fresh venvs, restore the two `.bat`s from
  the archive, and repoint their hardcoded `sats\{bandit,semgrep}-env\Scripts\` exe paths. The
  deleted 415 MB was entirely reconstructible from PyPI — that is why it was not kept.
- The scripts' own mechanics are preserved verbatim in the archived `.bat`s: repo-root scan,
  `exclude venv,sats,.git`, `PYTHONUTF8=1` (without it Semgrep/Bandit **crash** writing the
  report — `chcp 65001` fixes only the console, not Python's cp1252 file writes), rulesets
  `p/python` + `p/security-audit`, Bandit `-x` needing **absolute** paths.
- 💭 `_PYC_SCAN_EXCLUDED_DIRS = ("venv", "sats")` in `scripts/system_manager.py` still lists
  `"sats"`. It is **STALE but INERT** — the excluded directory no longer exists, so the entry
  silences nothing, exactly as the `venv` entry is already inert on the VM. Left deliberately:
  removing it edits a live monitoring check for no behavioural gain.

---

## Cron Jobs  (live `crontab -l` == `deploy/cron/trading-system.cron` == `generate(registry)` — **44 command-lines as of 30-Jun** (was 43 @29-Jun; +`sr_detector_backfill` 15:58; prior +`reconstruct_excursions` 15:50 +`control_tower` 17:05; regenerate the sha after a registry change); **source of truth: `config/cron_registry.yaml`** — TASK #3 + 23-Jun framework. The table below is an illustrative summary — the canonical/registry are authoritative.)
All market jobs run `cd … && set -a && . ./.env && set +a && PYTHONPATH=. venv/bin/python <script> >> logs/<log>`.
To change cron: edit `config/cron_registry.yaml` → `scripts/generate_crontab.py --generate --out deploy/cron/trading-system.cron` → commit + push (the `post-receive` hook **auto-installs** on the VM; or manually `crontab deploy/cron/trading-system.cron`). `--gate` proves zero-drops (+ only `sentinel_retention`); `--selftest` proves byte round-trip.

> ⚠️ **FIX-189 (2026-06-19) — cron MUST run under bash.** cron's default `/bin/sh`
> is **dash**, whose `.` (source) builtin will NOT load a relative path without a
> slash → a bare `. .env` fails with `sh: .: .env: not found` and **every job dies
> before Python** (no token refresh, no heartbeats, no per-cron logs). The
> canonical file therefore carries TWO guards: a leading **`SHELL=/bin/bash`** line
> AND **`. ./.env`** (the `./` makes even dash source from CWD). Keep both on any
> regeneration. `tests/unit/test_cron_registry.py::TestFix189CronShell` enforces it.

> ⚠️ **ENV-EXPORT FIX (2026-06-19) — source MUST be `set -a && . ./.env && set +a`.**
> `.env` uses bare `VAR=value` (no `export`), so a plain `. ./.env` sets shell
> variables the child `python` does **NOT** inherit → every cron job ran WITHOUT
> its `.env` secrets (`ZERODHA_API_KEY_LFL836`, `TELEGRAM_*`).
> FIX-189 made jobs *run*; this made them get their secrets (the 19-Jun 15:45
> reconcile_positions broker-creds + "Telegram env not set" were both this).
> `set -a` (allexport) exports everything sourced. Keep the wrapper on every
> Python job line. `tests/unit/test_cron_registry.py::TestCronEnvExport` enforces it.

| Time (IST) | Days | Script | Purpose |
|---|---|---|---|
| 00:00 | daily | `find logs -mtime +30 -delete` | log cleanup |
| 01:00 | daily | sqlite3 `.backup` trading_system.db | nightly DB backup |
| 01:05 | daily | sqlite3 `.backup` analytics.db | analytics DB backup (TASK #3) |
| 02:00 | daily | `backup_retention.py --apply` | **backup retention** (T2: category-aware keep-N — pre_*=20 / trading_system-*=14 / analytics-*=14; dry-run default, --apply in cron; replaces the old daily-only `find -mtime +7`; monitored+heartbeat) |
| 02:05 | daily | `find data_store -name 'critical_alert_*.delivered' -mtime +7 -delete` | **sentinel_retention** (DELIVERED alerts >7d, NEVER `.flag`; 23-Jun +1) |
| 02:30 | daily | `db_retention.py` (Sun: `--vacuum`) | DB row prune (TASK #3) |
| 09:20 | daily | `cron_officer.py --briefing` | Cron Officer morning briefing (was 04:55; live since 21-Jun reinstall) |
| 05:00 | daily | `rm session/zerodha_token.json` | force fresh login |
| 08:15 | Mon-Fri | `auto_refresh_token.py` | Headless TOTP token refresh (FIX-187; no manual OTP) |
| 08:30 / 09:14 / 09:15 | Mon-Fri | `scripts.preflight.orchestrator --phase A/B/C` | Pre-flight readiness, 3 phases (replaced premarket_healthcheck) |
| 08:35 | Mon-Fri | `fetch_fno_ban.py` | F&O ban list |
| 08:55 | Mon-Fri | `gemini_premarket_brief.py` | AI premarket brief |
| 09:00 | Mon-Fri | `refresh_instruments.py --account LFL836` | instrument master |
| */5 09–15 | Mon-Fri | `capture_metrics_baseline.py` | metrics capture |
| 15:40 | Mon-Fri | `fetch_daily_candles.py` | OHLCV fetch |
| 15:45 | Mon-Fri | `reconcile_positions.py` | broker↔local positions |
| 15:50 | Mon-Fri | `eod_cleanup.py` | stale signals/orders cleanup |
| 15:55 | Mon-Fri | `eod_verify.py` | EOD verification |
| 15:58 | Mon-Fri | `sr_detector_backfill.py` | **S&R shadow outcome backfill** (DB-only: `sr_detector_results ← trades` by signal_id; runs after `eod_verify` so trades are terminal; marker-verified `detection_method: exit_code_file`; 30-Jun) |
| 16:00 | Mon-Fri | `reports/daily_review.py` | EOD review |
| 16:00 | Mon-Fri | `wal_checkpoint.py` | WAL checkpoint |
| 16:01 | Mon-Fri | `generate_screened_stocks_csv.py` | screened CSV |
| 16:05 | Mon-Fri | `-m reports.daily_report` | daily xlsx report |
| 16:10 | Mon-Fri | `trade_journal.py` | trade journal |
| 16:15 | Mon-Fri | `compute_strategy_metrics.py` | strategy metrics |
| 16:16 | Mon-Fri | `capture_metrics_baseline.py --summarize` | metrics summary |
| 16:20 | Mon-Fri | `gemini_log_review.py` | AI log review |
| 16:40 | Mon-Fri | `gemini_trade_coach.py` | AI trade coaching |
| 17:00 | Mon-Fri | `gemini_data_integrity_check.py` | candle integrity |
| 17:05 | Mon-Fri | `ops/control_tower/runner.py` | **Control Tower** daily roll-up (size→aggregate→findings→health→status→pull-report + Telegram delta; skips non-trading days). Supersedes the standalone size-logger |
| 18:00 | Sun | `gemini_weekly_patterns.py` | weekly patterns |
| 18:00 | Mon-Fri | `check_cron_drift.py` | cron heartbeat drift (registry-driven) |
| 18:50 | Mon-Fri | `cron_officer.py --eod-summary` | Cron Officer EOD report (was 18:30; floor 18:45 +5min so it captures system_manager_eod; live since 21-Jun reinstall) |
| 18:45 | Mon-Fri | `system_manager.py` | System Manager EOD deep cross-check (TASK #5) |
| 03:00 | 1st of month | `backup_restore_drill.py --quiet` | restore drill |
| hourly | every | `disk_monitor.py` | disk space |

> ✅ **Self-maintaining (23-Jun framework):** live `crontab -l` == canonical == `generate(registry)`
> (43 command-lines as of 29-Jun; regenerate the sha after a registry change). `scripts/generate_crontab.py` is the deterministic
> generator; `scripts/check_cron_drift.py` (18:00) is the bidirectional content-pass drift check; the
> **post-receive** hook auto-installs on every push (the **pre-receive** equality guard is DEFERRED).
> Crontab backups at `~/tools/claude/crontab.backup.*` (+ legacy `data_store/crontab_backups/`).

## Systemd Services  (`/etc/systemd/system/`)
| Service | ExecStart | Purpose | Notes |
|---|---|---|---|
| `trading-system.service` | `venv/bin/python main.py --mode live` | Main app (live mode) | `Restart=on-failure`, `RestartSec=10`, **`RestartPreventExitStatus=3 4`** (4=HALT/SOFT_KILL no-restart, added 18-Jun); `EnvironmentFile=.env` + drop-in (Telegram secrets). **FIX-189 (19-Jun): main() exits 0 outside the broad service window [08:00–16:00 IST]** (startup guard; bypass: `--status`/`--dry-run`/`--interactive`/`--resume` or `TS_IGNORE_MARKET_WINDOW=1`) **and an `eod-self-exit` thread exits 0 once past 16:00 IST AND flat** (`count_active_positions()==0`) so it never idles overnight; never exits while a position is open. |
| `token-watcher.service` | `bash deploy/token_watcher.sh` | Auto-start app on fresh token | active |
| `alert-watcher.service` | `python scripts/alert_watcher.py --loop` | Consume CRITICAL sentinel flags (email digest / Telegram fallback) | **enabled, delivering.** **16-Jul-2026: switched `--once`+`Restart=always` → long-lived `--loop` daemon + `Restart=on-failure` + `StartLimitBurst=5`/`StartLimitIntervalSec=300` + `TimeoutStopSec=35` (PREPARED in repo — deploy OFF-MARKET, unpushed at time of writing).** Was a periodic-oneshot respawning ~every 10s (101,570+ restarts); now ONE process polling `alerts.watcher_interval_sec` (default 60s), SIGINT-clean shutdown, single-instance pidfile lock held for its lifetime. **Rising `NRestarts` / `SubState=auto-restart` is now a RESPAWN SYMPTOM, no longer normal** — the monitoring canary's `respawn` probe (`check_service_respawn`) flags it NOT-healthy. F1 (Telegram fallback + `alert_watcher_degraded.json` marker) means a dead SMTP never exits non-zero, so `on-failure` fires only on a real crash (e.g. config error). Report `docs/audit/alertwatcher_loop_fix_16jul2026.md`. |
| `trading-watchman.service` | (gemini watchman) | AI log monitor during market hours | `Wants=` by trading-system |
| `security-watcher.service` | `python scripts/security_monitor.py --watch` | **VM Security Manager Phase 1+2** — auth.log + file-integrity monitor (9 checks incl. Phase-2 copy-switch + copy-bypass) **+ 1 non-security check (26-Jul-2026): `check_nse_holiday_calendar`** — CRITICAL while a REQUIRED `config/nse_holidays_<year>.yaml` is absent (current year always; next year from 15-Dec), silent the moment it exists. ⭐ It is hosted HERE, not on the boot path, because this is the only always-on process — on 1-Jan the trading service is the thing that is dead, so a boot-path check cannot warn you the boot died — and because `_dedup`'s presence ledger already turns a persistent condition into ONE CRITICAL then a 6h/24h/7d ladder. ⏳ It escalates ONCE near the deadline: the key carries a PHASE from a closed 3-value set (`boot-dead`/`notice`/`final`), so the presence ledger treats the escalation as the different condition it is and re-alerts at full severity — bounded at ≤3 CRITICALs per file per year by the set's size (a DATE in the key was MEASURED at 17). Whole 15→31-Dec window = **7 alerts, 2 CRITICAL**. Knobs: `holiday_calendar_alert` / `holiday_calendar_lead_days` (16 ⇒ 15-Dec) / `holiday_calendar_final_days` (3 ⇒ 28-Dec, must stay < lead_days) in `config/security.yaml`. [LFL836] alerts | **enabled+active (19-Jun)**. `Type=simple`+`Restart=always`+`RestartSec=60` → periodic (~60s); model = alert-watcher. Reads `/var/log/auth.log` (ubuntu ∈ `adm`). Config `config/security.yaml` (standalone — NOT system_config.yaml, which is `extra="forbid"`). State `data_store/security_state.json`. Alert-ONLY (never blocks). **SSH baseline:** the allowed key fingerprint(s) = `expected_key_fingerprint` in `config/security.yaml` (committed default) OR the durable operator override `data_store/security/ssh_key_baseline.json` (overlaid by `apply_operator_ssh_baseline`, wins when present, NOT git-tracked so it survives a deploy's `checkout -f`). **After ANY legitimate SSH key rotation, run `python scripts/approve_ssh_keys.py --apply`** — it shows the live keys + diff, writes the override, re-seeds state, and ALWAYS Telegrams (a re-baseline can never be silent). 28-Jun-2026: baseline DrHT9…→uDRN8… for Rama's LEGITIMATE rotation (ED25519 `oracle-vm-2026`, Airtel/Tamil-Nadu IP 223.237.190.224) — NOT a compromise. |
| `cron-watchdog.timer`→`.service` | `venv/bin/python scripts/cron_watchdog.py` (oneshot) | **Tier-2 watch-the-watcher (ARMED 23-Jun)** — asserts `cron_officer_eod` + `check_cron_drift` both heartbeated today, else a CRITICAL sentinel via the **cron-INDEPENDENT** path (alert-watcher emails) | systemd (NOT cron) so it can't fail the way a dead crond / broken shared-env cron would. Fires **19:30 IST daily** (`Persistent=true`); first run **Wed 24-Jun 19:30**. `enabled`+`active`. From `deploy/systemd/cron-watchdog.{service,timer}`. |

> ## 🔌🕳️ **THE BOOT CHAIN — WHAT ACTUALLY STARTS `trading-system.service` (traced 05-Aug-2026)**
> ⛔⛔ **NOTHING IN CRON STARTS THE SERVICE.** The 08:15 cron entry (`auto_refresh_token`) writes
> **only** the token file. The starter is **`token-watcher.service`** → `deploy/token_watcher.sh`,
> polling **every 30 s** (`SLEEP_SEC=30`, set in the unit's `Environment=` and defaulted identically
> in the script) and gated by `within_service_window()` — **`hour ≥ 8 and < 16` IST** (FIX-189).
> The unit is `WantedBy=multi-user.target` (VM boot only) with `Restart=on-failure` +
> `RestartPreventExitStatus=3 4`, so **a clean exit 0 never self-restarts** — which is why the
> ~17:35 self-exit ends the trading day and nothing revives it until the next morning's token.
> ⭐ **THE CHAIN IS: `08:15 cron → token file → (≤30 s poll) → token-watcher → trading-system.service`.**
> Expect `active` anywhere in **08:15:02–08:15:32, roughly uniformly** — ⛔ **not "~08:15:30"**, which
> is the pessimistic END of the poll window; quoting the end as the expectation makes every fast
> start look anomalous. *(05-Aug measured: token `08:15:01.910`, service `active 08:15:05`.)*
>
> 🔴🔴 **THE CONSEQUENCE, AND IT IS THE REASON THIS BLOCK EXISTS: A FAILED TOKEN REFRESH IS SILENT.**
> **No boot. No error. No alert. No CRITICAL.** ⇒ ⛔ **"no order today" and "the service never
> started" are INDISTINGUISHABLE unless the token file is checked FIRST, and no later evidence
> separates them.**
> ⚠️ **`logs/cron-auto-token.log` IS NOT THE SUBSTITUTE** — it writes only on abnormality (measured:
> no write in 8 days while the cron ran every weekday), so **silence there is SUCCESS and an
> unchanged log is not proof of a refresh.**
> ⭐ **THE GATE IS THE FILE:** `data_store/session/zerodha_token.json` — present, and its `date`
> equal to today. **Check it before any other read, every trading morning.**
> 📌 **Registered as a single point of failure with no alarm on it.** ⛔ Not fixed — recorded.
> Sibling of the 16:00–17:35 unwatched-liveness tail above: both are **absence-of-signal** holes,
> where healthy and dead emit the same thing — nothing.
> ⛔ **G3 — WHAT THIS BLOCK DELIBERATELY DOES NOT REPEAT** (it was already here, and was checked
> before writing): the unit's existence and `ExecStart` (services table above) · the exit-code-aware
> back-off and the same-day/prior-day HALT handling (`Known Duplicates / Issues` #2, FIX-188) · the
> 08:00–16:00 window and the "stays INACTIVE at a reboot outside it" reboot-drill note (E4, 03-Jul).
> **What is new here is the CAUSAL CHAIN stated as such, and the SILENT-FAILURE consequence.**

> **VM security tooling (Phase 1, 19-Jun)** — also installed at OS level (NOT via git):
> **fail2ban** (`/etc/fail2ban/jail.local` from `deploy/security/jail.local`; sshd jail, `ignoreself`,
> reads auth.log, no static IP allow-list — Rama's IPs are dynamic) and **auditd**
> (`/etc/audit/rules.d/trading-security.rules` from `deploy/security/`; watches `.env`/`config/`/
> systemd units/`authorized_keys`/`sudoers` — query `sudo ausearch -k <key>`). All three services
> `enabled` (reboot-survive). **SSH hardening #2 APPLIED (19-Jun): `PermitRootLogin no`** via
> drop-in `/etc/ssh/sshd_config.d/99-trading-security.conf` (`deploy/security/sshd_config.d/`;
> validated `sshd -t` → `reload ssh`; ubuntu access unaffected — no root keys exist). Idle timeout
> (#1) intentionally NOT set (long `tail -f` sessions); `MaxSessions` left at 10. Phase 2 (copy
> protection) **BUILT 20-Jun** (code+config+tests in git; OS-level activation staged — see the
> Phase 2 note below); Phase 3 (EOD integration) **DONE 20-Jun** (Changelog). See memory `vm_security_phase1` /
> `vm_security_phase2`.

> **VM security tooling (Phase 2 — copy protection, 20-Jun, code in git; NOT yet activated on the
> VM)** — controls **VM→PC** file copying. `scripts/copy_gate.py` = the policy engine (priority:
> **TIME-LOCK 18:00–08:00 IST absolute** › OFF-switch › session-cap › 15-min token) + token store
> (`data_store/security/copy_token.json`) + JSON-lines audit (`data_store/security/copy_audit.log`).
> `scripts/request_copy.py` (`request-copy <reason>`) mints a 15-min token (audited + Telegram).
> `deploy/security/bin/copy-guard` is symlinked as `/usr/local/bin/{scp,sftp,rsync}` and consults the
> gate before exec'ing the real binary — **hard-blocks VM-INITIATED copies** (fail-safe: blocks if the
> gate can't be consulted). `security_monitor.py` gained two checks: **(8)** `copy_protection` ON→OFF
> transition → CRITICAL, **(9)** auditd `copy_attempt` bypass (a raw outbound scp/sftp/rsync with no
> token) → CRITICAL. auditd `copy_attempt` execve rules added to `trading-security.rules`.
> **Limitation:** a **PC-INITIATED pull** (PC is the client, VM's sshd serves it) cannot be
> hard-blocked from the VM side without risky `ForceCommand`/subsystem changes — it is **detect+alert
> only**. **HARD-BLOCK ACTIVE on the VM (20-Jun 14:00):** `install_copy_protection.sh` symlinked
> `/usr/local/bin/{scp,sftp,rsync}` → `copy-guard` and `/usr/local/bin/request-copy`. `/usr/local/bin`
> precedes `/usr/bin` in PATH, so a VM-initiated `scp`/`sftp`/`rsync` hits the gate: **blocked without a
> token, allowed with one** (verified live: no-token → DENIED exit 1 real-scp-never-ran; `request-copy`
> → token → copy succeeds). git push (ssh) + interactive ssh are NOT wrapped (verified intact). **New
> workflow to copy VM→PC:** `request-copy '<reason>'` (15-min token), then `scp`/`rsync`; blocked
> 18:00–08:00 (hard time-lock, no override) / without a token / when >2 SSH sessions. **Rollback:**
> `bash deploy/security/install_copy_protection.sh --uninstall` (removes the symlinks; real `/usr/bin`
> binaries untouched).

> 🛡️🔎 **Q8 VM-SECURITY FORENSICS (14-Jul-2026, read-only) — NO BREACH.** Report
> `docs/audit/q8_vm_security_breach_forensics_14jul2026.md`; memory `q8_vm_security_forensics_14jul`.
> Durable facts: **(1) the "18:00–08:00 time-lock" is `copy_gate.py` (VM→PC file copies) — NOT an SSH
> gate. SSH has NO time/network restriction** (24/7 `0.0.0.0:22`, key-only: `PasswordAuthentication no`
> + `PermitRootLogin no` + fail2ban). ⚠️ **Never add an 18:00–08:00 SSH lock — that IS Rama's off-market
> deploy window** (a time-locked SSH would lock the operator out). (2) 2,246 SSH logins are 100%
> publickey/`ubuntu`/Airtel-India; the only foreign IP (`45.198.224.120`, Stockholm bot) failed 327×,
> **0 success**. (3) The Jul-13 09:16 `authorized_keys` change to `BRi6…` (vs baseline `uDRN8…`) was
> **Rama's own un-baselined rotation** (bash_history nano-edits + bracketing session) that
> `security-watcher` correctly alerted on → **run `scripts/approve_ssh_keys.py --apply` to re-baseline
> and reset the tripwire.** (4) **`ubuntu ≡ root`** here (sole login user + `NOPASSWD:ALL`) → leverage is
> keeping intruders off `ubuntu` (SSH→Tailscale-only + key passphrase), NOT `.env` file-mode. Controls
> PROPOSE-not-apply; recovery path for any SSH change = **OCI serial/VNC console**.

> 🎯🛡️✅ **BUY-DAY FILTER #2b — THE RECONCILER'S THIRD SELL-UNDER-KILL SITE — `<BUILT>` 02-Aug `6495baa`, ⛔ NOT DEPLOYED.** `order_reconciler._check2_inflight_orphan` sells under an ACTIVE HARD_KILL and was product-blind; it now reads the **same `EMERGENCY_FLATTEN_PRODUCTS`**: CNC **SPARED** loud (`check_name=INFLIGHT_ORPHAN_SPARED_DELIVERY`, recorded as resolved NOWHERE ⇒ **re-alerts every cycle by design**), NULL/unknown **FLATTEN + CRITICAL via the existing shared emitter** (no second definition), MIS/CO unchanged. **0 status literals, 0 `holdings()` ⇒ Q4 ordering rule intact, D-8 still blocked.** ⚠️ Per-product-row masking here is UPSTREAM + pre-existing (the cycle snapshot is symbol-keyed, `:863`) — the kill_switch sweep still flattens the masked MIS row. Regression NEW-set EMPTY. ⛔⛔ **#2c CLOSED `<STOPPED AT STEP-1>` (02-Aug, docs-only, NO CODE) — a validated REDESIGN trigger, and it SUPERSEDES the old "intent is hardcoded INTRADAY" disclosure, which understated it.** ~~a CO position would exit under an MIS intent~~ → Step-1 measured: `intent` **IS** the live Kite `product` field; ⛔ **Audit 3.1 — a CO position CANNOT be squared by a reverse order at all** (broker rejects + ₹50+GST auto-squareoff; correct path = `cancel_order(entry_broker_id, variety="co")`, which `eod_squareoff` already does); this path **cannot reach a parent order id** (`bp` is a POSITION row; `trade_id` is the only affordance); **🔴 today `product="MIS"` is ACCEPTED, does NOT net, and OPENS A NAKED MIS SHORT while the CO position survives**; LATENT only — **double dormancy** (CO never used 805/805 X6 · `force_intraday_only: true` coerces non-INTRADAY back in `place_order`). **⭐⭐ STANDING RULE: PAPER CANNOT VALIDATE PRODUCT SEMANTICS — paper nets by SYMBOL, live Kite nets per (symbol,product) ⇒ a paper drill of any product-semantics change is vacuously GREEN.** ⛔ Never "fix" by mapping the intent; redesign = **#2c-R**, not started. 🔴 R4 = deploy slot only. Record: `docs/audit/reconciler_product_filter_build_02aug2026.md`.

> 💀🚫✅ **#2c-R — ORPHAN-CO REFUSE-AND-ESCALATE — `<BUILT>` 02-Aug `42db913`(code+15 tests)+`bb25fa6`, ⛔ NOT DEPLOYED.** The approved redesign (Option 1) of the site above. Under an ACTIVE HARD_KILL the reconciler's CHECK2 orphan path now **REFUSES a CO position instead of selling it**: **NO order placed**, CRITICAL naming the reason, disposition **`INFLIGHT_ORPHAN_REFUSED_CO`**, **`success=False`** (correct-but-INCOMPLETE — the CNC spare is a correct FINAL state and stays `True`). **WHY:** Audit 3.1 — a CO position cannot be squared by a reverse order at all, the correct path is `cancel_order(…, variety="co")`, and this path **has no parent bracket id**; the old code sold MIS = ACCEPTED, does NOT net, **naked MIS short**. ⛔⛔ **CO IS a MEMBER of the shared `EMERGENCY_FLATTEN_PRODUCTS` (ONE name, FIVE readers) ⇒ site-LOCAL branch placed BEFORE the membership test; the constant was NOT narrowed** — `core/constants.py` byte-untouched, as are kill_switch/eod_squareoff/adapter/resolver. **0 status literals · 0 `holdings()` ⇒ Q4 ordering intact, D-8 blocked.** RED-on-old 11F/4P · regression NEW-set EMPTY · revert = ZERO BYTES vs `0a9e13a`. ⚠️ **THE ALERT IS BOUNDED (new measurement): CHECK6's FIX-B marks a `PENDING_FILL` trade FAILED + releases capital on the 3rd consecutive cycle ⇒ ~3 CRITICALs, then the position routes to `_check2_orphan_adoption` (`HUMAN_ORDER`, once-a-day) while STILL LIVE at the broker — and #2b's CNC spare inherits the same ceiling.** ⭐⭐ **STEP-1b, NOT PATCHED, NEEDS ITS OWN CARD: `kill_switch` has the SAME CO defect at ALL THREE sell sites (`:1629`/`:1708`/`:1789`) — `variety` appears NOWHERE in that file.** ✅ `eod_squareoff` is CLEAN = the REFERENCE implementation. 🅿️ Option 2 PARKED (trigger: CO intentionally enabled OR reliable parent-order lookup; owner = the CO protocol surface, ⛔ not the reconciler). ⛔ CO doubly dormant ⇒ `<VERIFIED LIVE>` unreachable. Record: `docs/audit/reconciler_product_filter_build_02aug2026.md` §R1–R11.

> 🌳✅📋 **LEDGER #10 — DEPLOYED-TREE-vs-HEAD VERIFICATION — `<BUILT — NOT DEPLOYED>` 03-Aug-2026.** ⭐ Closes the **VERIFICATION** half of IA-P10-01 — *"the invariant every phase's premise rested on, held by ritual"* — by implementing **the audit's own recommendation**: a **12th `system_manager` check**, read-only, nightly artifact, **no schema**. `scripts/system_manager.py::deployed_tree_check` (+ `_resolve_deploy_git_dir`), wired into `run()`. It turns **ten manual SHA verifications** into an artifact. **Two halves:** tracked drift via `git diff --stat HEAD`, **and untracked `.py`** via `ls-files --others` — the second matters because ⛔ **`checkout -f` LEAVES UNTRACKED FILES IN PLACE** (KNOWN), so a stray `.py` is importable while sitting in **no commit**; check 11 (`stray_pyc_check`) covers only the `.pyc` slice of that. **Resolution order: `~/trading-system.git` (the VM's BARE repo — the deployed work tree has no `.git` of its own) → `<root>/.git` (PC) → DEGRADE with a warning.** ⭐⭐ **STEP 1's REAL FINDING — THE DESCRIBED TWO-LINER IS WRONG AS DESCRIBED, and it would have failed LOUDLY on its first healthy night: `git diff HEAD` needs an index. MEASURED (a) `GIT_INDEX_FILE`→a non-existent path = a VALID but EMPTY index ⇒ git reports every tracked file deleted — `1250 files changed, 344936 deletions(-)` ON A PROVABLY CLEAN TREE; (b) a ZERO-BYTE file = a CORRUPT index, a different failure (`fatal: index file smaller than expected`); (c) the REAL index = correct output but a MONITORING JOB WRITING THE DEPLOY REPO'S INDEX.** ⇒ **the correct form COPIES the real index and diffs against the COPY** — proven three ways (clean ⇒ empty · planted one-line change ⇒ `README.md | 1 +` · real `.git/index` mtime UNCHANGED), and **both wrong forms are pinned by test** so the copy step can never be silently simplified away. ⛔⛔ **SAFETY — IT NEVER SETS `soft_kill_reason`.** `system_manager` **can trip tomorrow's SOFT_KILL** (`generate_full_report` → `trigger_soft_kill`, `:1144-1145`), which made this **kill-adjacent, not additive**; `soft_kill_reason` is **opt-in per check**, so the property holds **BY CONSTRUCTION** and a test fails if anyone adds one. **Report-don't-block is the correct side of the discriminator** — the failure class (partial checkout, stray file, hook drift) is **ENVIRONMENT-caused** ⇒ DEGRADE+ALARM. **Every failure path degrades to a WARNING, never a violation** (no git dir · no index yet · rc≠0 · 30 s timeout · OSError/ValueError) — *a check that cannot run must not look like a check that found something.* ✅ **11 new tests, ALL RED ON OLD** (base worktree `f1972ec`, test file md5-identical both sides, `instruments.csv` copied in); ⭐ **they build REAL git repos rather than mocking `subprocess`** — a mocked seam asserts only what you told it, the exact failure practices **§V4** was re-earned on THIS SAME MORNING. ✅ **Composition smoke test against the live PC tree correctly reported its own uncommitted drift AND the untracked new test file**, `soft_kill_reason=None`, index mtime unchanged. Regression **NEW-set EMPTY**, +11 collected = exactly the new tests. ⛔ **THIS CLOSES VERIFICATION, NOT ENFORCEMENT** — nothing here *prevents* a divergent tree, it makes divergence **visible nightly**; enforcement remains open and is not proposed. ⛔ **Not `<VERIFIED LIVE>`** — its first real execution is on the VM against the real bare-repo layout, and no PC test can stand in for that. ⚠️ **It answers *"does the tree match HEAD"*, NOT *"is the running PROCESS that code"*** — a service started before a push keeps its old code in memory; different question, not claimed. Record: `docs/audit/ledger10_deployed_tree_check_03aug2026.md`.

> 🔁⛔📐 **LEDGER #3 — DESIGN REGISTERED 03-Aug-2026 `<REGISTERED — NOTHING AUTHORISED>` (DOCS-ONLY).** Record: `docs/audit/ledger3_design_registration_03aug2026.md` · index: `MASTER_PENDING` §B.1 row 3. ⛔ **No code written, no design started, #3 remains fully GATED** — registered so implementation later starts from the register, not a chat transcript (§0 triggers 1·3·4). ⛔⛔ **R-3, THE PART THAT BINDS FUTURE WORK: ANY CHECK6 REDESIGN MUST EXPLICITLY STATE — FOR #2b AND #2c-R BY NAME — WHETHER ALERT COUNT, ESCALATION BEHAVIOUR OR REFUSAL SEMANTICS CHANGE.** CHECK6's 3-cycle FIX-B is the **only** thing bounding **#2b**'s CNC spare (`6495baa`) and **#2c-R**'s CO refusal (`42db913`) to ~3 CRITICALs rather than an unbounded stream, and ⭐ **neither item's code mentions CHECK6** — the bound is an emergent property of cycle ordering (CHECK6 runs **after** CHECK2), so a CHECK6 author retuning that counter has **no local signal** that two shipped items depend on it ⇒ **both regress silently.** ⭐ **ENDPOINT FRAMING ACCEPTED: IA-P5-02's cancel-race and the CHECK6 route are TWO INDEPENDENT PATHS TO THE SAME ENDPOINT** — the system's own position filed as `HUMAN_ORDER` ⇒ ⛔ **fixing either alone leaves the endpoint reachable by the other; any design treating them as two separate bugs is wrong BY CONSTRUCTION.** ⭐ **DIRECTION TO TEST AT STEP 1 — ⛔ NOT a settled mechanism: separate RELEASE-THE-RESERVATION from DISOWN-THE-POSITION**, today collapsed into one action; ⛔ **whether they are separable in this system is a measurement NOBODY HAS MADE.** **THE SPLIT: #3a `orders.qty_filled` ships ALONE · #3b the `HUMAN_ORDER` endpoint covers BOTH paths together** — ⛔ do not bundle 3a into 3b (they share a row, not a fix); ⛔ do not split the paths inside 3b. ⚠️ **the audit's "~2 lines" is an ESTIMATE, NOT a measurement** — every item this campaign touched grew on contact. ⭐ Width (M1): **405 of 805** `orders` rows are the wrongly-zeroed population (`qty_filled>0` on **0/405**, `filled_at` on **405/405**); the other **400 are CANCELLED and correctly zero.** **R-4 RULED — FIX-FORWARD BY DEFAULT:** backfilling the 405 only on operational evidence they are consumed by CURRENT production logic, and then as a **separate, explicitly approved activity** (it writes the LIVE DB; ⛔ **a wrong backfill is indistinguishable from correct data afterwards**). **QUEUE ORDER UNCHANGED — #8b Step 2 → #2d → #3**, asked and answered; ⚠️ **measured while registering: that order had never been written down anywhere** (repo-wide `*.md`+`*.txt`), and ⚠️ **`#2d` still has NO register row** (it lives only in practices §G3 + `:290-293`, `PATHS.md:112`, and §R7 of the reconciler record) — stated, not fixed, since a row would move the 231. 📜 **PROVENANCE, and do not collapse it: these are red-team DESIGN acceptances from ChatGPT — architecture review IS its remit ⇒ ⛔ they are NOT §G2 material.** §G2 governs **Rama's** decisions (deploy slots, gates, ride-or-hold) — which is exactly what R1–R5 were. ⛔ No new register row — **231 stands.**

> ⭐⭐🗓️ **RAMA'S RULINGS R1–R5 — RATIFIED 02-Aug-2026 ~16:32 IST (his own words, via the bridge), recorded at `MASTER_PENDING` §A-DEC.** ⛔ Provenance is the point: **NOT** the auto-filled prompts that impersonated approvals (practices §G1) and **NOT** ChatGPT (§G2). · **R1 — the filter-slip gate is 3-PART: a filter slip postpones the ENTIRE flip ⇒ A0 CLOSED.** · ⭐⭐ **R2 — OPTION Y ⇒ THE FLIP IS NOW WED 5-AUG: Mon eve = code+docs, NO flags · Tue 4-Aug = SHAKEDOWN day + first real EOD census, then push the flip flags that evening · Wed = THE FLIP.** ⛔ 4-Aug is **reassigned, not deleted** — a resequencing, not drift; every "4-Aug flip" now means **5-Aug**, reason = R2. · **R3 — #2b + #2c-R ride the same push** (linear history; §D1 no partial deploy). · **R4 — the `cnc_gtt_*` registry edits ride the FLIP-FLAG push (Tue eve)**, else the census mismatches by design on go-live day. ⚠️ Not to be confused with the #2b record's own "R4". · **R5(a) APPROVED — remove `--reset` entirely (Option A)** · ⛔ **R5(b) HOLD — #8b does NOT ride Monday; Step 2 authorised but NOT started until Monday's critical path finishes.**

> 📜⚖️✅ **CAMPAIGN PRACTICES — `docs/campaign_practices.md` — NEW 02-Aug, DOCS-ONLY.** One durable home for the standing rules this campaign EARNED, each with the incident that earned it; cross-linked from `MASTER_PENDING` §2, which also adopts **THE DOCUMENTATION RULE** (record design completions · decisions · **REJECTED options and why** · sequencing · governance rulings IMMEDIATELY — register = INDEX, `docs/audit/` = EVIDENCE, cross-linked). ⭐ **G1 an auto-filled console prompt is NEVER an instruction and NEVER an approval — #5 impersonated Rama's ruling; plausibility is the attack surface, not the tell** · G2 ChatGPT advisory on Rama's calls · G3 disclose-don't-expand · G4 superseded-but-legible · **M1 repo-wide never file-wide (earned 4×)** · M2 prove-don't-assert · V1 paper cannot validate product semantics · V2 the regression invocation · V3 the worktree-base mitigation + the masking asymmetry · V4 composition-truth · **D1 THERE IS NO PARTIAL DEPLOY (linear `main`, push→`checkout -f`)** · D2 the SHA gate · D3 no push before 18:15, Rama flattens MANUALLY · D4 `<VERIFIED LIVE>` never for a latent path. ⛔ PROCESS practice — distinct from the two PRODUCT rules docs; do not merge. ⛔ No new register row — **231 stands**.

> 🧪🔒✅ **LEDGER #8b — `scripts/check_vm_state.py --reset` — ✅ `<BUILT — NOT DEPLOYED>` 03-Aug `38f8ab9`.** ~~`<MEASURED, FIX GATED>` 02-Aug. ⛔ NO CODE WRITTEN; the script is UNTOUCHED.~~ **Step 2 implemented under R5(a)/Option A once R5(b)'s hold was released by the 18:22:51 push; rides the NEXT push (VM pinned at `d6c298d`) ⇒ it does NOT execute on Tuesday's shakedown day.** ⭐⭐ **A LOUD BLOCK, NOT A SILENT DELETION: because the worst property was PRINTING SUCCESS WHILE CHANGING NOTHING, deleting the branch alone would have kept that trap** (type `--reset` → kill state prints → **exit 0** → read as success). Now **exit 2 on stderr**, naming both sanctioned paths, **before the DB is opened**, matching **any argv position** (the old code checked only `argv[1]`). ✅ **NEW PATH `tests/unit/test_check_vm_state_reset_removed.py` (5 tests); RED-ON-OLD 4F/1P in a worktree at `d6c298d` — T4 green on old code BY DESIGN (diagnostics-survived guard).** ⛔ **`--cleanup-*` untouched and still gated; the allowlist is NOT narrowed (§C.4 R12).** Step-1 measurement that earned it: The executable twin of #8. ⭐ **Worst property: it prints `Kill switch RESET to INACTIVE` while changing NOTHING on a running service** (`is_active()` reads memory, `:487-491`) — **and `_persist_state` is `INSERT OR REPLACE`, so the edit can be silently overwritten back to killed.** Also: no `system_events` audit · stale `triggered_at` (**audit-integrity only**, no boot harm) · **HARD_KILL `--force` gate bypassed** · no dry-run · **zero tests**. ✅ **No automation caller · NO runtime coupling (zero importers, unreachable from `main.py`)** — the ride-decision fact. ✅✅ **Second-copy question CLOSED/RULED OUT by Rama's 02-Aug VM check (`~/check_vm_state.py` does not exist) ⇒ a repo-only fix IS permanent** — which **dissolves the stronger half of the hold argument**; hold now rests only on *no urgency + Monday's stack is large*. **REC: Option A (remove `--reset`) + HOLD.** ⛔ Read-only diagnostics must survive. ⛔⛔ **DISCLOSED NOT FIXED: `--cleanup-pending` (`:102-119`) mass-CANCELs every `PENDING_FILL` trade in the LIVE DB, no capital release, no audit — the state CHECK2/CHECK6 arbitrate ⇒ can strand reservations; cross-referenced to debt-ledger #3. Sibling `--cleanup-orders` (`:122-148`). Neither has a sanctioned equivalent.** ⭐ **GOVERNANCE (to ledger #11 + §C.4 R12, NOT #8b): the assistant allowlist pre-authorises an AI agent to run these against the LIVE DB without prompting; removing the flag does NOT narrow it, and 11 entries are stale.** One ledger item with #8 — **231 stands.** Record: `docs/audit/ledger8b_check_vm_state_step1_02aug2026.md`.

> 📕🛟✅ **LEDGER #8 — RAW-DB KILL-CLEAR PURGED FROM THE RECOVERY DOCS — `<BUILT>` 02-Aug, DOCS-ONLY (0 `.py`), ⛔ NOT DEPLOYED until Monday's push.** ⭐ **IA-XDOCS-01 scoped ONE doc; the sweep found THREE** (`03_daily_operations_runbook.md:132` · `RUNBOOK.md:47` · `disaster_recovery.md:164`), all prescribing a raw `UPDATE kill_switch_state` + restart — **all fixed in ONE commit**, old text struck-through-but-legible. **SANCTIONED PATH now stated everywhere: LIVE/VM `sudo bash deploy/resume.sh` (`--force` = HARD_KILL) · PAPER/PC `python scripts/clear_kill_switch.py` (⚠️ `resume.sh` is VM-only ⇒ parity split stated).** **Measured WHY:** raw `UPDATE` has **no effect on a running service** (`is_active()` reads memory, `:487-491`), leaves `triggered_at`/`triggered_by` stale with **no `system_events` audit**, and **bypasses the HARD_KILL `--force` gate**. ⛔⛔ **The restart step is the real hazard — a persisted kill at boot = EXIT 4 (`main.py:1932`) + `RestartPreventExitStatus=3 4` ⇒ systemd will NOT restart it.** HARD_KILL halts always; SOFT_KILL halts same-day. ⭐ **"Database Locked" was the worse half** (unconditional restart; after 15:15 the daily breaker SOFT_KILL makes that the COMMON case) — it now checks kill state first. ⚠️ **NEW DISCLOSURE, NOT fixed (CODE): `scripts/check_vm_state.py --reset` does the same raw clear.** ⛔ No `<VERIFIED LIVE>` for a runbook until used in a real incident. No new row — **231 stands**.

> 📌➕ **#2c-R ADDENDUM (02-Aug, DOCS + ONE COMMENT ONLY).** ⭐ **CHECK6 REGISTERED TO DEBT-LEDGER #3** (IA-P5-02 family): #3 now also owns **capital-released-while-the-position-is-live** — FIX-B marks a `PENDING_FILL` trade FAILED + releases its reservation at **3 cycles** while the position is STILL LIVE at the broker, then it routes to `_check2_orphan_adoption` and can be filed **`HUMAN_ORDER`**: the cancel-race endpoint by a **second, independent path**. **Importance RAISED**; ⛔ **ordering UNCHANGED, no new row — 231 stands.** Forward pointers now in **BOTH** the #2b and #2c-R records. 🔴 **Post-flip a CNC holding spared by #2b follows this path** — **latent-on-latent** (needs a HARD_KILL **and** an in-flight entry **and** a fill; HARD_KILL has never fired) ⇒ **documented, NOT flip-blocking.** ⛔ No CHECK6 code touched. ⭐ **`core/constants.py` COMMENT CORRECTED — it said FOUR readers of `EMERGENCY_FLATTEN_PRODUCTS`, there are FIVE** (kill_switch ×2 · eod_squareoff ×2 · order_reconciler; it predated #2b) ⇒ it understated blast radius. **Comment-only PROVEN by AST identity (docstrings stripped) + frozenset value asserted `{MIS,CO}` + 39 constant-reading tests green.** ⚠️ Now also records that **membership ≠ "will be sold" at site 5.**

> 🎯🛡️ **BUY-DAY FILTER (ledger #2 / Q4) — `<BUILT>` 01-Aug night `43043a2`, ⛔ NOT DEPLOYED.** HARD_KILL now flattens **intraday only**: both emergency sites read `core.constants.EMERGENCY_FLATTEN_PRODUCTS` {MIS,CO} (single source, shared with both scheduled EOD passes); **CNC spared loud; NULL/NRML/unknown flatten + CRITICAL**; per-product-row hazard test-pinned. 🔴 OWED: reconciler CHECK2 third-site ruling (#2b vs carry-pilot blocker) · Mon PC kill drill. Record: `docs/audit/buyday_filter_build_01aug2026.md`.

> 🔬🛠️ **EFFECT-TELEMETRY (ledger #1) — `<BUILT>` 01-Aug `132e571`, ⛔ NOT DEPLOYED.** `core/effect_telemetry.py` + **`config/expected_managers.yaml` (the C2/C6 single source of truth — ⛔ a new main.py manager ctor REQUIRES a registry entry in the SAME diff)**; 42/43 units counted at frozen effect-points; **B2 assertion at end-of-boot (paper fail-fast · live CRITICAL+continue); EOD census emits at `_shutdown()` entry (main.py:1236)** — the 16:05 report cron is out-of-process. Contract + build record in `docs/audit/effect_*_01aug2026.md`. ✅ B-2 `4959111`: the order_placer STOP RESOLVED (place() + `placer.emergency_exit` tripwire; 70 entries). 🔴 OPEN: the deploy slot · MON pre-deploy PC-paper composition boot (B2 must PASS clean) + calm regression confirm (record §6a) · the 4-Aug `cnc_gtt_*` flip-rider.

> 📋⭐ **PENDING SOURCE-OF-TRUTH — CURRENT (01-Aug-2026): `docs/MASTER_PENDING_01-Aug-2026.md`.**
> The consolidated WORKING register, four bands, **231 items reconciled** (132 pre-audit + the
> integrity audit's **95 finding IDs** + 4 new): **§A live-trading 6 · §B the XE.3 debt-ledger
> running order 96 · §C still-open 105 · §D settled 24.** ⛔ **`MASTER_PENDING_28-Jul-2026.txt` and
> `MASTER_PENDING_30-Jul-2026.md` are SUPERSEDED as the working register but RETAINED as the cited
> authorities — DO NOT DELETE EITHER, and where they disagree with the 01-Aug file THEY WIN.**
> ⭐ The debt ledger is a **running order, not an authorisation** — no fix work is authorised by the
> audit. 🔴 **§A0 carries the ONE decision owed Rama before Mon 3-Aug's close** (does a buy-day-filter
> slip postpone only the carry pilot, or all of 4-Aug). The lines below are the SUPERSEDED 14-Jul
> pointer, kept for lineage.

> 🧾🔍 **PENDING SOURCE-OF-TRUTH (14-Jul-2026) — 🔁 SUPERSEDED, see above:** the 10-Jul master pending
> list was reconciled against HEAD → **`docs/audit/pending_reconciliation_14jul2026.md` was then the
> authoritative pending register** (superseded `MASTER_PENDING_TRUE_FINAL_10JUL.txt`, a Downloads file not in-repo).
> Durable facts it establishes: **Audit-B Phases 9 (Operations) + 10 (Security) were NEVER completed**
> (`docs/audit/audit_05jul2026.md` ends mid-Phase-9, "Batch 5 running") → pending, not clean.
> **M-S4 is PARTIAL** — substrate (schema-v44 `daily_symbol_stats`, `candle_math.rsi`, forward-shadow
> recorder) shipped default-OFF, but the live bug persists (`screening/secondary_screener.py:407
> "atr": None` → screening steps 1&3 still 0.0). **The stale duplicate `deploy/post-receive` still
> carries the wrong `/home/ubuntu/trading-system` path** — the canonical `deploy/hooks/post-receive`
> AND the LIVE VM hook are correct (`/home/ubuntu/systems/trading-system`), so deploys land; the dup
> is a record-don't-fix footgun. memory `pending_reconciliation_14jul`.
> **✅ RESOLVED 17-Jul (batch-2, AB-910 §2.4): `deploy/post-receive` is DELETED** — no longer
> record-don't-fix. Dead-proof: md5 `bd950b7…` live VM hook == `deploy/hooks/post-receive` (1336 B),
> while the dup was `604d2b3…` (3629 B); no caller anywhere (`install_vm_services.sh` never
> referenced it, despite the dup's own header claiming it did). Recover from git if ever needed:
> `git show 57f28a1:deploy/post-receive`. **⚠️ NEW finding it exposed → LOOP:** the dup was the ONLY
> copy of FIX-065's market-hours push guard. The canonical hook and the LIVE VM hook have NO such
> guard (grep=0) — **the guard has never been live**, so "no push during market hours" is manual
> discipline only. `tests/unit/test_fix065_market_hours_guard.py`'s 12 tests assert against a mock
> hook they define inline, so they pass regardless and are vacuous. Decide: implement in
> `deploy/hooks/post-receive` (deploy-path behaviour change) or retire FIX-065 + its tests.
>
> **✅ E4 + W10 FIXED 17-Jul (branch `e4-w10-pnl-contract`, LOCAL/UNPUSHED — DEPLOY AWAITS RAMA'S
> EXPLICIT RISK-POSTURE SIGN-OFF).** Report → `docs/audit/e4_w10_done_17jul2026.md`. THE CONTRACT
> IS NOW: **`fm_ledger.pnl_delta` is NET; `costs` is observability-only and is NEVER re-subtracted.**
> Reader `state_store.py` → `SUM(pnl_delta)`; the 3 backstop paths take the shared mode-agnostic
> `CostCalculator` (via `broker/cost_calculator.round_trip_costs_or_zero`, fail-OPEN so a cost-calc
> failure can never block a capital release) and book real costs; `record_manual_close_financials`
> / `record_gtt_close_financials` gained `gross_pnl` so those trade rows keep
> `gross_pnl − charges == net_pnl`; `release_used` now persists `trade_id`. `reset_daily_pnl` and
> rehydrate/M-C1 were deliberately NOT changed — both are in lockstep by construction and are
> pinned by test. **⚠️ The daily-loss limit now fires on the TRUE net ⇒ it trips LATER than the
> old double-counted figure on normal exits.** The historical gap is NOT backfillable (the 36 rows'
> real costs were never computed). The read-only investigation that established all this ↓
>
> **🔴 E4 INVESTIGATED read-only 17-Jul → `docs/audit/e4_investigation_17jul2026.md`. E4 IS W10 —
> ONE bug (a `pnl_delta` contract mismatch), and fixing either half ALONE is a regression.**
> WRITER `fund_manager.py:1228/1251` stores `pnl_delta = gross − costs` (**NET**); READER
> `state_store.py:2447` `get_daily_realized_net_pnl` computes `SUM(pnl_delta) − SUM(costs)` and
> its docstring wrongly claims `pnl_delta` is gross ⇒ **costs subtracted twice**. The NORMAL exit
> path **already passes real costs** (`order_placer.py:2410 costs=charges` via
> `CostCalculator.round_trip_breakdown`), so E4's `costs=0.0` is scoped to **3 backstop paths**
> (`order_reconciler.py:1109`, `:1901`, `cnc_gtt_monitor.py:450`) — which pass 0.0 because
> **neither class holds a CostCalculator at all**. Live split: **119 rows costs≠0 (loss
> OVERSTATED) vs 36 rows costs=0 (loss UNDERSTATED)**; 16-Jul the control saw **−4.01 vs a true
> −2.62**. **BOTH daily-loss halves are fed by the same reader** (post-close `fund_manager.py:1279`
> + pre-trade `risk_engine.py:25` RE7 via `get_snapshot()` `:1507`); `reset_daily_pnl:1603` reads
> it too and must move in lockstep. The GUI/reports already route around it (**D2, approved
> permanent** — `capacity.py:15`), so `pnl_delta`-is-NET is the established contract. Available
> capital (`:1231/1257/1259`) uses `pnl` **once** ⇒ correct on the normal path, over-credited on
> the 3 backstop paths. Parity is free: `CostCalculator` is built at `main.py:1786` **before** the
> paper/live branch. ⚠️ A fix removes today's accidental conservatism ⇒ **the loss limit will trip
> LATER than it does now on normal exits** — a live risk-posture change, state it to Rama.
> Prior art: `audit_05jul2026.md:572` (W10 "confirmed unfixed"), `pending_reconciliation_14jul2026.md:73`
> (W10 OPEN, 0 fix commits). **Investigation only — the FIX is the ✅ block above.**
>
> **🔄 SUPERSEDED 17-Jul (sweep S7): the post-receive md5 is now `b716673…`, NOT `bd950b7…`.**
> *(Corrected same-session: an earlier line here — and S7's commit message `5f89ec5`, which is
> immutable — recorded `e493dc5…`. That was measured after the first of TWO edits to the file;
> the second edit moved the md5 again and the record was not re-measured. `b716673…` is the real
> value, verified identical across the PC working copy, the git blob, and the armed VM hook — the
> file is `eol: lf` normalised, so the checksum is platform-stable. Recording a stale checksum for
> the live hook is precisely the trap S7 exists to close, which is why it is corrected rather than
> quietly left: **always re-measure AFTER the last edit.**)*
> The header was corrected (it falsely claimed "NOT the currently-installed hook"; a real push
> that same day printed `crontab AUTO-INSTALLED from canonical`, which only the armed Option-A
> version does — runtime proof it IS live). Comment-only: executable lines are byte-identical to
> `bd950b7…`. **The armed VM hook was re-armed to match in the same change** — repo == live is the
> identity proof and must be re-established after ANY edit (`cp deploy/hooks/post-receive
> ~/trading-system.git/hooks/post-receive`), since nothing auto-installs the hook.
> **FIX-065 update (sweep S6): the guard belongs in `deploy/hooks/pre-receive`, NOT post-receive** —
> git ignores post-receive's exit code (refs are already updated), so a guard there would skip the
> checkout while the bare ref moved, leaving bare≠tree (a half-deploy) instead of a clean rejection.

Drop-in dir: `trading-system.service.d/` (holds Telegram env vars — secrets).

---

## ⚠️ MANDATORY — Broker-Truth Verification BEFORE Every Manual Position Intervention (16-Jul-2026)

**NEVER flatten / square-off / hedge from DB state alone. The DB status LAGS broker truth.**
On 16-Jul the DB read a position (ACI) as OPEN for minutes *after* its stop-loss had already
filled at the broker; a DB-driven "flatten the open position" would have SOLD a share we no
longer held → **a naked short**. Zerodha *regular* orders also have **no broker-side OCO** (the
engine enforces software-OCO for `LIMIT_TRIPLE`), so any manual action **races the exit-manager**
unless the engine is stopped first. Every manual intervention on positions MUST follow, in order:

1. **STOP the trading engine** (`sudo systemctl stop trading-system.service`, or a SOFT_KILL then
   restart — see below). Do not skip: a running engine will race your manual order.
2. **VERIFY the engine is fully stopped** (`systemctl is-active trading-system` → `inactive`; no
   `main.py` in `ps`). A half-stopped engine still manages exits.
3. **READ BROKER positions** — `kite.positions()` / the Kite web terminal is the **source of
   truth**, NOT `trades`/`positions` in the DB.
4. **COMPARE broker vs DB** and resolve every mismatch. If they disagree, **the broker wins.**
   (Per symbol: buys == sells ⇒ flat, regardless of what the DB row says.)
5. **FLATTEN ONLY broker-open positions** (net qty ≠ 0 at the broker). Never place an order for a
   position that is flat at the broker even if the DB shows it open.
6. **VERIFY zero positions** at the broker after flattening (`kite.positions()` net == 0).
7. **VERIFY zero pending orders** at the broker (`kite.orders()` — cancel any resting SL/TGT legs;
   regular orders have no OCO, so an orphan leg can fill later).
8. **RESTART only when intended** (engine boot re-reads the kill switch; an active SOFT_KILL boots
   to `startup_scenario=HALT` → exit-4, designed no-restart — see the kill-switch note below).

**Corollary — no live halt/flatten API:** the KillSwitch is loaded **at boot only** (in-memory), so
an intra-session stop needs **stop → set-kill → start → HALT (exit-4)**; there is no running-process
"flatten now" endpoint (recorded as a future architecture decision, not built). See `deploy/resume.sh`
/ `scripts/clear_kill_switch.py` to resume. This checklist is the **"broker-truth before every manual
intervention"** rule — memory `alertwatcher_loop_fix_16jul`.

---

## Known Duplicates / Issues  (audit 2026-06-18; cleanup applied same day)
1. ~~Live crontab ≠ `deploy/cron/trading-system.cron`~~ — **RESOLVED 2026-06-18 (TASK #3)**: the
   registry `config/cron_registry.yaml` is now the source of truth; the canonical file was regenerated
   and **installed** (`crontab deploy/cron/trading-system.cron`) — live == file (33 lines). The
   previously-missing analytics.db backup (01:05) + `db_retention` (02:30) were added; `auto_refresh_token`
   moved 08:00→08:15. Old crontab backed up to `data_store/crontab_backups/`.
2. **[PARTIAL] `trading-system.service` — emergency-kill recovery + headless** — crash-loop FIXED
   (`RestartPreventExitStatus=3 4`). FIX-188 (18-Jun) additionally stopped **token-watcher** from
   hammer-restarting an exit-4 HALT every 30s (now exit-code aware: same-day HALT → back off + 1
   alert/day; prior-day HALT → one clean start so it auto-recovers) and broadened `/health` (token +
   kill_switch, 200/503). The emergency SOFT_KILL was cleared (operator `--resume`) and the service
   **handed off to systemd — now `active`, `NRestarts=0`**. Still OPEN (EXTERNAL): the Kite dev-console
   **IP allowlist** for the VM IP (place_order 403 root cause; see `kite_ip_allowlist_dependency`,
   FIX-185) — required for live order placement. **Resume cleanly with `deploy/resume.sh`** (stop →
   `scripts/clear_kill_switch.py` clears the kill in the DB without the instance lock → start under
   systemd) — FIX-188b; do NOT use a standalone `main.py --resume` (it competes for instance-lock port 5001).
   **X5 (17-Jul):** the lock is now OS-owned (`fcntl.flock` on the lock file) as well as the port, so a
   standalone `main.py` is refused by BOTH layers — the guidance above is unchanged, only harder to defeat.
   `clear_kill_switch.py` takes no instance lock, so `deploy/resume.sh` is unaffected. The lock file
   (`/tmp/trading-system.lock`) is deliberately NEVER unlinked: the lock lives on the inode via an open fd,
   and unlinking a path a live instance holds would let the next start lock a fresh inode and run alongside
   it. Its PID is informational — a stale/recycled PID no longer blocks a boot (that was the real X5 bug).
3. ~~`alert-watcher.service` SMTP delivery~~ — **RESOLVED 2026-06-18 (verified)**: valid Gmail App
   Password set in `.env`; the 16 pending sentinels delivered (`Digest delivered: 16 alerts → .delivered`;
   `.flag`=0, `.failed`=0; digest emailed to `ramakrishnan031@gmail.com`, subject
   `[LFL836] CRITICAL — DIGEST: 16 alerts`). No auth errors after the valid password.
4. ~~Two git remotes on PC~~ — **RESOLVED 2026-06-18** (`vm` remote removed; `origin` remains).
5. 🧟 **MAINTAINED-BUT-NEVER-WIRED — `orders/breakeven_manager.py` (E3, 25-Jul-2026): the code looks alive and is not.** `BreakevenManager` was created 31-May (FIX-132a Item 8) and then edited by **five** later fix cycles (FIX-148 · FIX-179 · FIX-181 · the 23-Jun tick-snap · the 14-Jul M-X2 batch) — six commits of upkeep — while **`git log -S "BreakevenManager" -- main.py` returns NOTHING for the entire repo history**: main.py has never constructed one, so `order_placer`'s optional `breakeven_manager` is always `None` and `register_trade()` is never called. Consequence for the next reader: **its edit history is not evidence that it runs**, and a defect inside it cannot be triaged from commit activity — E2 (25-Jul) found its SL lookup selecting `broker_order_id`, a column `orders` does not have, which had been dead since day one behind a mock that answered on that key. **Before treating any module here as live, grep `main.py` for its constructor.** Sibling shape: `smart_tgt_manager`'s `volume_dependent_trails` block is an explicit `pass` placeholder behind a `false` config flag.

## PENDING CLEANUP
✅ **All previously-listed items were actioned on 2026-06-18** (see Changelog): root `trading.db`
deleted, 56 stale `critical_alert_*.flag` removed, dated notes archived to `docs/archive/`,
`deploy_audit_fixes.ps1` removed, duplicate `vm` git remote removed. **No items currently pending.**
New open operational issues are tracked under "Known Issues" above (crontab divergence, crash-loop,
inactive alert-watcher).

---

## Related Docs (do not duplicate these — cross-reference)
- 🚫📋 **Operator PLANNING docs are NOT repo artifacts.** The master pending register
  (`MASTER_PENDING_REGISTER_*.txt`) + decision sheets (`*_DECISION_SHEET_*.txt`) are **Rama's own
  external working files** — git-excluded via `.git/info/exclude` (local, never pushed), NEVER
  `git add`-ed. The tracked ANALYSIS lineage (the census/audit reports) DOES live here, under
  `docs/audit/` (e.g. `pending_reconciliation_14jul2026.md`) — those stay tracked. Convention memory
  `feedback_operator_planning_docs_external`.
- 🏦 **F1 sector cap (16-Jul, branch `f1-trades-sector-observe-16jul`, UNPUSHED):** `trades.sector` is
  now populated at INSERT (`order_placer._resolve_trade_sector` ← `InstrumentCache.sector`, the same
  source gate-8 uses; `"UNKNOWN"`-bucketed, frozen). Gate-8 gains `risk.sector_cap_mode` (**default
  `observe`** = log `WOULD_REJECT`, does NOT reject → behaviour-neutral deploy). The observe→enforce
  flip that ACTIVATES the live 40% sector concentration cap is OFF-MARKET + Rama-gated (observe soak
  ≥1 session first). Report `docs/audit/f1_trades_sector_16jul2026.md`; memory `f1_trades_sector_16jul`.
  16-Jul follow-up: BUG A (`risk_engine.py:123` `sector_for`) was a stale class-DOCSTRING example
  (never executable) → fixed to `.sector`; BUG B (`signal_processor._sector_for` always UNKNOWN)
  DEFERRED-inert (its `V3Signal`/`ScoredCandidate` sector fields are set-but-never-read); Q6 repo-wide
  stale-resolver sweep CLEAN (only BUG A+B). Canonical resolver everywhere = `InstrumentCache.sector`.
- ✅🔀🚀 **16-Jul COMBINED DEPLOY (alert-watcher + F1) — DEPLOYED 16-Jul ~18:5x IST off-market.
  VM==bare==`11abebb`** (= tag `deploy-16jul-alertwatcher-f1`→`1d5337d` + 5 docs-only files;
  `git diff --name-only 1d5337d..11abebb` = markdown ONLY ⇒ deployed CODE == the regression-green tag.
  Rama approved the deviation from the instruction's expected `f68d15d`, stale by 2 docs commits).
  Config overlap auto-merged (AlertsConfig respawn_* + RiskConfig sector fields); combined regression
  4700 pass / 10 known-PC-env (zero-new); deploy_assert rc=0 · integrity/FK clean · **schema v44,
  no migration**. Behaviour-neutral: gate-8 `sector_cap_mode=observe` (log-only) · alert-watcher is
  monitoring-only. **alert-watcher: `activating`/auto-restart NRestarts=104,567 → `active`/`running`
  NRestarts=0**; single instance PROVEN via the unit cgroup (`TasksCurrent=1`) — note a raw
  `pgrep -fc alert_watcher.py` returns 2 because the ssh shell self-matches its own command line;
  use `ps -eo args | grep -c '[a]lert_watcher.py'`. Canary ALL GREEN incl. the new `respawn` probe
  (quiet when healthy — `_deliver_report` returns early on `overall_ok`). **OWED: 24h resource soak
  (baseline 16-Jul 18:47:50 PID 1318632 — RSS 37,548kb · VSZ 50,264kb · 1 thread · 4 fd · 0 db-fd;
  ANY sustained growth → rollback) · F1 observe soak ≥1 session (starts Fri 17-Jul) → evidence →
  Rama-gated enforce flip.** M-C4 (`6c77525`) deliberately NOT in this deploy. Report
  `docs/audit/deploy_done_16jul2026.md`; runbook `docs/audit/consolidation_16jul2026.md`;
  memory `consolidation_16jul`.
- ✅🏁🚀 **M-C CAPITAL-SAFETY CLUSTER — DEPLOYED 16-Jul-2026 ~22:1x IST (off-market). VM==bare==`06a61cb`;
  the deployed CODE == the drilled tag `deploy-16jul-mc-cluster`→`341cc57`** (`git diff --name-only
  deploy-16jul-mc-cluster..HEAD` = markdown ONLY. Local main at run time was `06a61cb`, NOT the SHA written
  in the consolidation report — **the TAG is the code identity; re-derive the SHA at deploy time**, the same
  trap that fired on the F1 deploy this morning). **Schema v44 unchanged, NO migration** (deployed
  `EXPECTED_SCHEMA_VERSION=44` == live v44 ⇒ the 08:15 boot skips migration); integrity/FK clean; services
  healthy; **all 4 fixes verified in the CHECKED-OUT tree — M-C4 by AST** (`record_api_failure`'s
  `with self._lock` block contains ZERO `soft_kill` calls; the call is at line 750, outside). Backup
  `data_store/backups/pre_deploy_mc_cluster.db`. Combined regression 4763 pass / 11 known-PC-env (zero-new,
  no divergence); sandbox hard_kill drill 35/35, no invariant violation.
  **⏰ GOES LIVE AT THE 17-Jul 08:15 BOOT** — `trading-system.service` is halted on the planned operator
  SOFT_KILL (dated 16-Jul ⇒ a PRIOR-DAY kill ⇒ `main.py:1607 clear_stale_state` auto-clears it);
  **NO restart owed, no operator action.**
  **⚠️ M-C8 rewrote the EMERGENCY-EXIT path and the drill was MOCK-broker only ⇒ THE FIRST LIVE HARD_KILL IS
  THE REAL TEST.** Watch: `HARD_KILL flatten dispatched to worker thread` + `flatten worker finished — all N
  attempted position(s) flat` = good; `finished with N UNEXITED trade(s)` / `flatten worker CRASHED` /
  `SHUTDOWN WITH FLATTEN STILL RUNNING` / `could NOT start the flatten worker` = each means MANUAL
  broker-truth verification (never flatten from DB state). Report `docs/audit/deploy_done_mc_cluster_16jul2026.md`;
  memory `mc_cluster_consolidation_16jul`. Rollback: L1 revert a fix · L2 revert a merge (`c0c9376`/`8bc685a`/
  `341cc57`) · L3 reset main to `16437ae`; all schema-free.
  — **HISTORICAL (pre-deploy detail):** M-C4 `6c77525` (`mc4-killswitch-lock-16jul`) · M-C8 `8f9ce0d`
  (`mc8-async-hardkill-16jul`) · **M-C5 + M-C6 + the test_main logger-leak fix `mc5-mc6-testmain-16jul`
  (3 independent commits: `f87e587`/`0847134`/`6ca1bbe`; report `docs/audit/mc5_mc6_testmain_16jul2026.md`,
  memory `mc5_mc6_testmain_16jul`).**
  **M-C5** — `FundManager.commit_adopted_entry` evaluates its `_commit_exists` guard INSIDE `self._lock` but
  commits OUTSIDE it (it must: `commit_to_used` takes the lock itself + defers BL-4 hard_kill to after
  release). Two NON-GATING callers both pass the guard ⇒ both commit; **the harm is NOT capital —
  `_apply_commit:1999` pops the reservation, so the loser raises ValueError and BL-4 fires a SPURIOUS
  hard_kill.** Not reachable today (both prod callers gate atomically: `order_reconciler:3603`
  `adopt_recovery_trade_to_open`, `:3660` `mark_recovery_trade_exiting`). **Fix = 3 gates:** caller's atomic
  transition (primary) + `fm_ledger` COMMIT row (DURABLE, covers a later cycle post-restart) + **NEW in-memory
  CAS claim `_commit_claims` (CONCURRENT, covers the window the durable guard structurally cannot see)** —
  test-and-set under the lock ALREADY held, released in a `finally`, **NO lock across the commit I/O** (the
  M-4/M-C4 anti-pattern). NOT part of the 3-balance invariant.
  **M-C6** — `position_sizer.py` `max(1, floor(raw_qty × effective_mult))` turned a ZERO multiplier into
  **1 lot** (capital on a signal the model sized to nothing). Now `effective_mult <= 0` →
  `constraint="ZERO_MULTIPLIER"` skip; **FIX-133's floor is PRESERVED for positive multipliers** (its real job).
  `<=0` needs no tolerance (`perf_weight` clamped ≥0 ⇒ no tiny FP negatives; `-1.0*0.0 == -0.0` caught).
  **⚠️ `PositionSizingTierConfig` types HIGH/MEDIUM/LOW as BARE FLOATS with NO `ge=0` bound ⇒ a negative tier
  multiplier loads cleanly from config.** BEHAVIOUR-NEUTRAL today (`performance_allocator` clamps
  `min_weight=0.5` PA3/PA8 + `signal_processor:893/1827` defaults unknown strategies to 1.0 ⇒
  `effective_mult >= 0.25`) — **it is the hard PREREQUISITE for ever lowering `min_weight`.**
  **⚠️ M-C6 INVERTS a documented FIX-133 decision** (`test_perf_weight_zero_floor_at_one` + the module
  docstring recorded "perf_weight=0 -> floor at 1" as INTENDED — that test encoded the defect; renamed +
  corrected, siblings untouched). **⏰ NEXT: consolidate the 3 branches → fresh combined regression → SANDBOX
  hard_kill DRILL → off-market deploy. Do NOT disturb the deployed `11abebb`.**
- 🧪⚠️ **`tests/unit/test_main.py` leaked a MagicMock into `main._log` — FIXED 16-Jul (`f87e587`, test-only).**
  `main._main_locked` declares `global _log` (`main.py:1538`) and rebinds it (`_log = get_logger("main")`,
  `:1626`); test_main patches `main.get_logger`→MagicMock and the patch restores `get_logger` but **NOT
  `_log`** ⇒ after test_main ran, `main._log` stayed a MagicMock for the whole session and **ANY later test
  asserting on main's logging was SILENTLY VACUOUS** (passing while testing nothing — that is how M-C8's
  test_d3 was caught). Fix = an **autouse module fixture** saving/restoring `main._log` (autouse because there
  are **4** separate `patch.multiple("main", ...)` sites); guard `test_zz_main_log_is_not_left_as_a_mock` is
  deliberately **LAST in the file** — pytest runs tests in definition order, and last is the only position
  where an entry-invariant assertion means anything.
- 🗂️⭐ **BATCH-vs-LOOP CLASSIFICATION of every open register item (16/17-Jul, read-only PROPOSAL —
  awaiting Web Claude's approval; NOTHING executed).** `docs/audit/batch_classification_16jul2026.md`;
  memory `batch_classification_16jul`. **27 BATCH-SAFE** (docs 6 · repo-hygiene 1 · logging 5 · tests 3 ·
  verify-only 6 · trivial-code 6) · **+3 batch-safe-by-risk but TOO LARGE ⇒ own run** (AB-910 Audit-B
  Ph9/10 · Q9 integration coverage · BK-1 LONG-strategy review) · **~70+ LOOP-REQUIRED** · **8 AMBIGUOUS
  defaulted to LOOP** (X1 `ops_dashboard/backend/auth.py:62-64` **`verify_topt` fail-open 2FA** = the most
  security-severe item on the list; X2 `eod_verify.py:60-68` wrong columns ⇒ a fix makes a dormant P&L
  check LIVE; X6 `cron_heartbeat.py:157-162` holiday degrade changes whether jobs RUN on a holiday).
  **📌 THE 16-Jul REGISTER IS STALE — verified at the deployed head: B1 (trades.sector), B4
  (alert-watcher `--loop`) and M-C4/C5/C6/C8 are ALL CLOSED by the two 16-Jul deploys ⇒ B2/M-S4 is now the
  TOP open engineering item** (`screening/secondary_screener.py:407/408/410` still `"atr"/"rsi"/
  "prev_close": None` ⇒ 25/100 selection points are a constant 0.0 in LIVE). Rule: LOOP if it touches
  capital/kill/order/schema/sizing/regime/**any live trading decision**; **unsure → LOOP**. Group-E
  boundary: verification-only items produce a FINDING and STOP — **any implied fix EXITS the batch to LOOP**.
- 🩺🔒 **Capital-safety cluster M-C4/C5/C6/C8 — read-only investigation (16-Jul).** M-C4 (auto-trip holds the
  kill-switch RLock through soft_kill's publish+Telegram send, `kill_switch.py:649-663`) + M-C8 (hard_kill's
  2h retry loop runs sync on the fill/commit thread, `:550/1204-1290`) are OPEN+REACHABLE = the fix targets;
  M-C5 mitigated (atomic reconciler caller gate), M-C6 latent (allocator min_weight 0.5). Report
  `docs/audit/mc_cluster_investigation_16jul2026.md`; memory `mc_cluster_investigation_16jul`. Fixed nothing.
  **M-C4 FIXED (16-Jul, LOCK SCOPE ONLY): `record_api_failure` counts+decides under the lock and calls
  `soft_kill` OUTSIDE it, so the lock is never held across the publish/Telegram send** (RED-on-old:
  concurrent `is_active` blocked 20.02s). `6c77525` — report `docs/audit/mc4_killswitch_lock_16jul2026.md`,
  memory `mc4_killswitch_lock_16jul`.
  **M-C8 deeper investigation (16-Jul, `63dbb38`, read-only — `docs/audit/mc8_investigation_16jul2026.md`,
  memory `mc8_investigation_16jul`):** no prod caller uses hard_kill's `CancellationReport` ⇒ the async
  redesign can fire-and-return; prod always takes the adapter path (`main.py:1983 set_adapter`) while the
  sync-contract tests use the legacy `cancel_fn` path ⇒ a clean seam. **⚠️ OPERATIONAL FACT (latent today):
  `StateStore.count_active_positions()` counts ONLY `OPEN`/`PARTIAL`/`PENDING_FILL` — `EXITING` is BLIND
  (`state_store.py:639-651`) — and the HARD_KILL flatten marks trades `EXITING` EARLY, so the eod-self-exit
  daemon (`main.py:964-978` / `:1012-1035`, due iff active==0 past the window end) can set the shutdown
  event while trades are still inside the 2h exit-retry loop.** Any async flatten MUST use a non-daemon
  worker + join/drain in `_shutdown` + a flatten-in-progress gate — never `count_active_positions()`.
  (Note: the investigation's seam claim "all existing tests pass unchanged" was later DISPROVEN — see the
  M-C8 fix entry. All four M-C fixes are now done; the cluster entry above is authoritative.)
- 🧨✅ **M-C8 FIXED — the hard_kill flatten now runs on a WORKER, not the caller's thread (16-Jul,
  MERGED into the M-C cluster; `capital/kill_switch.py` + `main.py`; report
  `docs/audit/mc8_async_hardkill_16jul2026.md`, memory `mc8_async_hardkill_16jul`).**
  **`hard_kill` FIRES-AND-RETURNS on the adapter path** (production — `main.py:1983` always `set_adapter`);
  the kill STATE is still tripped synchronously first (`is_active` blocks orders immediately — that did NOT
  go async). The **LEGACY `cancel_fn` path stays SYNCHRONOUS** and `_exit_all_trades_indestructible` stays a
  sync internal method — **that split is LOAD-BEARING** (it is what keeps the 86 pre-existing kill-switch
  tests passing untouched). Do not "tidy" it. **⚠️ CORRECTION: the design's claim "no existing test calls
  `hard_kill()` with an adapter set / no test rewritten" was FALSE** —
  `test_p0_live_day1_fixes::TestBugC_KillSwitchExit` did exactly that and broke; those 3 now call
  `_exit_all_trades_indestructible()` directly like their siblings (assertions verbatim). **Only the FULL
  suite caught it.** A test that drives `hard_kill()` with an adapter gets a DISPATCH, not a result:
  `drain_flatten()` first, then assert.
  **Flatten state machine `IDLE→RUNNING→DRAINING→COMPLETE`** under a **DEDICATED lock, never the KS4 RLock**
  (reusing it would re-create M-C4 in a worse place: a 2h join under the lock that gates `is_active`);
  `COMPLETE` is set in a `finally` — a worker dying at RUNNING would make `is_flatten_in_progress()` answer
  True forever and hang the eod-self-exit gate all night. **SINGLE-FLIGHT on the adapter path** (a repeat/
  cross-thread `hard_kill` no-ops; the running loop already re-derives from broker truth every retry) —
  **KS6's "re-runs cancellation" is now PATH-SPECIFIC: preserved for legacy, single-flight for the adapter.**
  **⚠️ THE EXITING-BLIND RACE IS NOW CLOSED:** `main._eod_self_exit_due` gates on
  `KillSwitch.is_flatten_in_progress` (NOT `count_active_positions`), fail-safe (gate raises → stay up), and
  `_shutdown` drains the non-daemon worker **BEFORE any teardown** (the worker writes through `store`, and
  `store.close()` is at the bottom). **Shutdown split:** internal eod-exit waits (bounded by the flatten's own
  2h deadline); **external SIGTERM gets a bounded 15s grace** (`_FLATTEN_DRAIN_GRACE_SEC`) because
  `trading-system.service` sets `TimeoutStopSec=30` and systemd SIGKILLs regardless → CRITICAL "positions may
  remain open" on overrun. **DB writes are CONDITIONAL** (no global lock; broker truth stays authoritative):
  `trades→EXITING` conditioned on the live set, `orders→CANCELLED` on NOT-terminal, both sharing one module
  constant with the SELECTs that feed them. **📌 Two schema facts found while proving the tests RED-on-old:
  (1) `schema.sql:278 trg_trades_terminal_status_guard` ALREADY ABORTs transitions out of CLOSED/
  CLOSED_MANUAL/FAILED/CANCELLED/REJECTED* — so the terminal trades case was never exposed; the conditional's
  real gain there is `UNKNOWN_IN_FLIGHT` (the A-2 "don't know if it filled" state, NOT terminal, NOT guarded)
  plus turning a trigger-ABORT-logged-as-CRITICAL into a quiet no-op. (2) There is NO trigger on `orders` at
  all ⇒ the `CANCELLED`-over-`COMPLETE` clobber (a FILLED SL recorded as cancelled) was ENTIRELY unguarded —
  the conditional is the only thing preventing it.** Also hardened in self-review: a **thread-start failure
  runs the flatten INLINE** rather than wedging the state at RUNNING or dropping the flatten (an unflattened
  book beats a blocked caller), and **`drain_flatten` keys off the STATE not the thread handle** so an inline
  flatten cannot read as "nothing in flight". FIX-180/181/190/H-4/H-5 preserved by construction (the worker
  runs the same method); **27 new tests (9 proven RED-on-old by reverting each behaviour)**, 86 pre-existing
  kill-switch tests unchanged, `test_main` zero-new. **Deploys OFF-MARKET with the M-C cluster — emergency
  path ⇒ fresh combined regression + a sandbox hard_kill drill first.**
- `docs/CONFIG_GUIDE.md` — **Rama-facing config reference (TASK #8)**: every setting in plain
  language, effective-values table, override precedence, common scenarios, safety warnings
- `docs/system_manuals/*.docx` — **Word-format manuals for Rama** (`trading_System_v2_runbook.docx`,
  `config_file_guide.docx`, `chartink_mounted_strategies.docx`). ⚠️ **GITIGNORED** (binary;
  `.gitignore` → `docs/system_manuals/*.docx`) — so `git push` does **NOT** carry them. Sync
  out-of-band: `scp docs/system_manuals/*.docx trading-vm:~/systems/trading-system/docs/system_manuals/`.
  Re-scp after any edit. VM copies verified byte-identical + valid OOXML (19-Jun).
- `docs/01_system_architecture.md` — architecture
- `docs/03_daily_operations_runbook.md` / `docs/RUNBOOK.md` — daily ops
- `docs/04_db_schema_reference.md` — DB schema (v28)
- `docs/05_incident_response.md`, `docs/disaster_recovery.md` — incidents
- `docs/06_deployment_guide.md` — deployment detail
- `docs/design/a1_e1_orphan_fix_design_02jul2026.md` — **DESIGN (no code yet)** for the A-1+E-1 naked-orphan root-cause fix: tag-correlation recovery (adapter `get_all_orders()` surfaces the broker tag the reconciler currently drops → correlate a timed-out/crashed ENTRY back to its trade by `truncate(trade_id)` → adopt+protect / flatten-under-kill instead of FAILED→naked; capital released only on confirmed broker-absence; one adoption path; RAMCOIND/CHECK9/G5b preserved; no schema). **IMPLEMENTATION IN PROGRESS** (branch `fix-a1e1-orphan-recovery-02jul`, unpushed): Phase 0 GREEN→recompute; Phase A DONE (`99bd4c4` adapter `get_all_orders()`+paper tag, 83 tests green, pure-addition); **Phases B-E [correlate+adopt / rewrite unknown-in-flight / order_monitor reroute / wire+tests] REMAIN — naked bug still live until complete.** memory `a1_e1_orphan_fix_design_02jul` + `a1_e1_orphan_fix_impl_02jul`.
- `docs/design/a2_timeout_retry_design_02jul2026.md` — **IMPLEMENTED (committed, unpushed)** for audit A-2: `BrokerTimeoutError` was retried at `signal_processor.py` (combined with the retry-safe `BrokerRateLimitError`) → pipeline re-runs → **second `place_order` = duplicate/2× exposure**; masked only by the entry throttle (a rate-limiter, not idempotency). order_placer already defers (`UNKNOWN_IN_FLIGHT` + queue, `:1291-1323`) and the A-1/E-1 reconciler (`_recover_in_flight_entries`, 15 s poll) already OWNS the timed-out entry. **Fix (all 3 placement paths — `_process_one`/`continue_from_gate`/`continue_from_retest`): a dedicated `except BrokerTimeoutError` that does NOT re-queue, does NOT release the reservation (recovery owns it — singular ownership), marks the signal `TIMEOUT` (an already-CHECK-allowed, already-report-mapped status → NO schema/report change; NOT a new `PLACEMENT_UNKNOWN`, which the `signals.status` CHECK constraint would reject), frees the lock, returns. `BrokerRateLimitError` KEEPS its retry (pre-submission).** Trade stays `UNKNOWN_IN_FLIGHT`; paper/live parity; RAMCOIND/CHECK9/G5b/FIX-068 preserved; no order_placer/reconciler/schema/config/cron change. 6 A-2 tests + 408 regression green. Memory `a2_timeout_retry_impl_02jul`. **Deploy AFTER the 03-Jul 08:15 A-1/E-1 boot confirms clean.**
- **B-1 daily-loss unrealized-MTM — IMPLEMENTED** (branch `fix-b1-daily-loss-mtm-02jul` @ `f5fd4d9`, off main, unpushed, **SHADOW default**). The pre-trade daily-loss (3%) gate `risk_engine.py:497-512` summed `get_total_unrealized_mtm()` but its writers had **zero production callers** → realized-only. Now: `order_reconciler._refresh_unrealized_mtm()` (15s) computes `(ltp − avg_fill) × qty × sign` from `broker_adapter.get_quote` (parity-clean; paper = real Kite quotes) → `update_unrealized_mtm` → **SET-BASED prune** (no per-close hooks); `fund_manager` freshness (`get_unrealized_mtm_status`, `_unrealized_mtm` separate dict, invariant untouched); gate reads (total, is_fresh) and enforces realized+unrealized ONLY when `daily_loss_include_unrealized=true` **and** fresh, else realized-only + WARN. Default `false` = shadow (logs `would_reject_with_unrealized`, zero behaviour change → deploy-safe). 20 tests + full suite green. Design `docs/design/b1_daily_loss_unrealized_mtm_design_02jul2026.md`; memory `b1_daily_loss_unrealized_mtm_impl_02jul`.
- **P1 broker-authoritative EOD reconcile — IMPLEMENTED** (branch `fix-p1-eod-broker-reconcile-02jul` @ `4817032`, off main, unpushed, SHADOW default). `scripts/eod_broker_reconcile.py` @15:58, standalone creds (`reconcile_positions._resolve_credentials`) → runs even if the process was DOWN at 15:17. Queries broker positions/orders/margins/day-realized-P&L, compares to local, emits per-dimension + overall **VERIFIED / ISSUES / UNVERIFIED**. REQUIRED dims = positions · orders · broker DAY-REALIZED P&L (any unavailable ⇒ UNVERIFIED, no partial false-pass). MARGIN supplemental + reliability-gated to `[09:00,15:45]` (broker funds unreliable post-close) → NOT_CHECKED @15:58, never blocks VERIFIED (tolerance mirrors G3 `max(₹50,|total|×10%)+₹5000`). LEDGER = local fm_ledger invariant (deep standalone cross-check = shadow follow-up). Subsumes `reconcile_pnl` (correct `system_pnl`/`broker_pnl` cols + real creds → variance computes); CONSUMES `reconcile_positions` (15:45 non-OK ⇒ can't VERIFY → no contradiction). DETECT+VERIFY+ALERT only (auto-fix=P3); NEVER mutates trading state. Paper = LOCAL SELF_CONSISTENCY (labeled, never broker-authoritative). Schema **v42** (+`eod_broker_reconciliation` pure-add). SHADOW-first flag `eod_reconcile.authoritative=false` (default → runs alongside eod_verify, records `mismatch`, alerts INFO, gates nothing → deploy-safe); flip true → authoritative + retire eod_verify. 15 tests + full suite ZERO new failures. Design `docs/design/p1_eod_broker_reconcile_build_design_02jul2026.md`; memory `p1_eod_broker_reconcile_impl_02jul`.
- `docs/audit/audit_remediation_status_02jul2026.md` — **AUDIT RECONCILIATION (read-only)**: the 02-Jul 1 CRIT + 4 HIGH mapped vs deployed `9becf8c`. **A-1 CLOSED/deployed**; A-2 built-unpushed (`fd09a38`, boot-gated); C-1 live-exposure closed (rotation + deployed scrub) with residual `aa02f9f` unpushed + purge-runbook-only; **B-1 OPEN** (daily-loss gate's `unrealized_mtm` is dead — writers `update_unrealized_mtm`/`remove_unrealized_mtm` have no production callers, `risk_engine.py:497-510`); C-2 PARTIAL (auth secret+rate-limit deployed; `bind_host:0.0.0.0`+`require_hmac:false` network exposure still open). **No headline HIGH is a P1/P2 deliverable** (P1 = W3 + the eod_verify false-VERIFY; P2 = E-4 liveness). **B-1 = fix BEFORE/WITH P1** (independent, small); C-2 network = AFTER P1. Push queue = `fix-c1-completion-02jul` (stacks A-2 `fd09a38` + C-1 `aa02f9f`); `fix-t2-import-02jul` parked. memory `audit_remediation_status_02jul`.
- `docs/design/p1_eod_broker_sync_assessment_02jul2026.md` — **ASSESSMENT (read-only, no code)** for the P1 EOD broker-sync/observer build. Key findings: NO artifact ever named "EOD-broker-sync" was closed (capability is spread across `reconcile_positions.py`@15:45 [detect+alert only, paper fabricates OK, gates nothing] · 15s `order_reconciler` [broker-authoritative but PROCESS-DEPENDENT] · `eod_squareoff`@15:17 [in-process] · Control Tower@17:05 [freshness observer, no broker session]). **`scripts/eod_verify.py`@15:55 FALSE-VERIFYs** — it makes ZERO broker calls (local `OPEN/PARTIAL` trades + `PENDING` orders only, `:44-81`) so it emits "EOD VERIFIED: all clear" even with a naked broker position; its P&L arm is doubly dead (column bug `system_net_pnl`/`broker_net_pnl` vs real `system_pnl`/`broker_pnl` `schema.sql:783-785` → swallowed OperationalError → variance stuck 0.0; AND `reconcile_pnl.py` is UNSCHEDULED so its source table is never written). No EOD P&L OR capital broker reconcile; capital is LOCAL-authoritative (`sync_from_broker` only at startup + a 09:15 one-shot); reconciler-sourced capital drift (G3/CHECK5) is detect-only and NEVER kills. Build seq: P1 standalone process-independent broker-authoritative EOD reconcile (positions+orders+P&L+capital, REAL verify, broker-down⇒UNVERIFIED never false-pass) → P2 observer/alerting + `/health` liveness (E-4) → P3 in-session edges. memory `p1_eod_broker_sync_assessment_02jul`.
- `docs/audit/` — point-in-time audits (cross-reference, don't duplicate). Latest:
  **`docs/audit/system_security_audit_02jul2026.md`** (02-Jul full security & integrity audit,
  investigate-only) — 1 CRITICAL (**C-1: live creds committed in `.env.example`**) + 4 HIGH (A-1
  timeout→naked [FIXED, deployed `9becf8c`] · A-2 timeout-retry double-entry [FIXED, committed
  `fd09a38`] · B-1 daily-loss unrealized-MTM term is dead code · C-2 webhook 0.0.0.0+no-HMAC
  [locked down]). memory `system_audit_02jul`.
  **C-1 exposure INVENTORY (02-Jul, read-only):** `docs/audit/c1_credential_exposure_inventory_02jul2026.md`
  — remote is INTERNAL-ONLY (`trading-vm:~/trading-system.git` bare repo, NO public forge); real `.env`
  NEVER tracked; the ONLY real-secret commit is **`9f58848`** (`.env.example`, 10 vars — scrubbed by
  `7bc3367`), plus the **LFL836 api_key** hardcoded in 3 scripts at HEAD
  (`scripts/{fetch_daily_candles.py:30, gemini_data_integrity_check.py:45, reconstruct_excursions.py:61}`).
  Post the 02-Jul rotation: **6 exposed secrets DEAD**, **2 api_keys ACTIVE but non-authenticating alone**.
  Prevention LIVE (placeholder `.env.example`, gitignored `.env`, secret-scan hook). Outstanding (Rama's
  ops call): optional rotate the 2 api_keys + clean the 3 scripts; defense-in-depth history-purge (plan in
  memory `c1_secret_remediation_02jul`) — do before any public push. Prior: `system_flow_review_15jun2026.md`,
  `system_audit_14jun2026.md`, `db_schema_review_15jun2026.md`.

---

## Circuit-band placeability gate (NOCIL fix, 23-Jun-2026)
**Invariant:** a resting protective exit (SL stop / TGT limit) recalc'd from the fill must land on
the **correct side of the entry fill** *and* inside the day's circuit band — never a wrong-side,
instantly-marketable order. (NOCIL 22-Jun: a LONG TGT recalc'd to 197.12 was clamped DOWN to
`upper×0.98 = 187.00`, **below** the 189.78 fill → instant SELL, 2.35 s scratch mislabeled TGT_HIT.)

- **Primitive (single chokepoint):** `orders/price_math.py` → `clamp_exit_into_band(price, *, leg,
  direction, entry_fill, upper_circuit, lower_circuit, …) → ClampResult(price, was_clamped,
  placeable, reason)`. Frozen result; pure (no I/O). Replaces the old side-agnostic
  `clamp_to_circuit_band`. Margin = `DEFAULT_CIRCUIT_MARGIN_PCT` (0.02) — the **single source** shared
  with the pre-fill rule. Called from **exactly 3 sites, all in `orders/order_protocol_limit.py`**
  (SL + TGT in `place_exits`, TGT in `place_tgt_only`); a no-bypass test enforces this.
- **Leg asymmetry (caller branches on `placeable=False`):** **TGT** unplaceable → hold (SL-only;
  `tgt_placed=False` → FIX-190 Bug-C → `mark_needs_tgt_retry` → TGTRetryManager). **SL** unplaceable →
  position cannot be stopped → `raise SLUnplaceableError` (`core/exceptions.py`) → order_placer's
  existing unprotected-position escalation (`_emergency_market_exit` + `_fire_hard_kill_for_unprotected_position`).
  `entry_fill` (= settled `avg_fill_price`) is threaded fill→`place_deferred_exits`→`place_exits`.
- **HARD_KILL rationale (updated 2026-07-22, behaviour unchanged):** the
  `_fire_hard_kill_for_unprotected_position` escalation above no longer protects a *naked* position —
  since **FIX-148 (03-Jun)** the emergency flatten runs FIRST, so at kill time the position is normally
  already flat. The kill is **retained (Decision 7, Rama, 22-Jul)** on a different rationale — an
  **anomaly circuit-breaker**: an exit-placement rejection ⇒ halt the trading day (every live firing
  15/16/19-Jun was a multi-position storm), NOT naked-position protection (FIX-148 + the reverse-aware
  kill-flatten sweep already handle that). Contract: **OP-NS5** (`orders/order_placer.py` header).
  Forensics: `docs/audit/exit_rejection_hard_kill_forensics_22jul2026.md`.
- **Pre-fill reject (framing-b, both legs):** `screening/secondary_screener.py` rejects an entry that
  sits at/beyond the exit-clamp ceiling (`entry ≥ upper×(1−m)` LONG / `≤ lower×(1+m)` SHORT, and the
  symmetric SL-side) → `REJECTED_CIRCUIT_PROXIMITY`. Fast-disable lever:
  `entry_gate.circuit_proximity_reject_enabled` (default **true**). The post-fill gate is **not** flagged.
- **Documented un-gated exceptions (never clamp → no wrong-side risk; test-enforced):** CO-TGT
  (`order_protocol_co.py` — broker-managed CO SL backstops it) and the G5b recovery SL
  (`order_reconciler.py` — LTP-guarded). Both fail loud (broker reject), never a silent wrong-side fill.
- **Parity:** all shared entry/exit code, no mode branch → Paper + Live together. **No schema change.**

## MIS Learned Blocklist (source-free, 30-Jun) — NO_FILL Mechanism B fix
Fixes the broker MIS-block no-fills (Zerodha refuses MIS/intraday on non-MIS-list
stocks; `force_intraday_only` can't fall back to CNC → ~19 trades/9 symbols wasted,
0 fills ever). **Step 0 proved NO clean MIS source exists** (no API; RMS-dynamic
intraday; fragile/inconsistent Google Sheets) → so **NO daily list, NO `mis_tradable`
table, NO cron, NO schema, NO external source.** Instead: observe the broker's own 400.

| What | Location |
|---|---|
| Store + detector | `core/mis_blocklist.py` — `is_mis_block_rejection(exc)` (matches "mis orders are currently blocked") + `MisLearnedBlocklist` (persisted JSON `data_store/mis_blocklist.json` `{SYMBOL: last_blocked_date}`; thread-safe, atomic write, restart-safe, corrupt→fail-open) |
| RECORD (always-on) | `orders/order_placer.py::_handle_placement_failure` appends ONE guarded `record_block(symbol)` on a MIS-block 400; **every existing step unchanged**. Flag-independent so the list warms up while dormant |
| RE-TEST TTL | blocked iff in store AND `(today − last) < ttl_days` (default 5 calendar days). Expiry → allowed to re-test; fresh 400 refreshes; a trade self-corrects. Avoids permanent false-drop |
| DROP (flag-gated) | `screening/secondary_screener.py::screen()` step-0 hook → `REJECTED_NOT_MIS_TRADABLE` (signals.status GLOB 'REJECTED*' — no schema). Only when resolved product == MIS (future-proof via injected `resolve_product`). Logs every would-drop |
| Flag | `config/system_config.yaml` `mis_filter` (`core/config_loader.py` MisFilterConfig): `enabled:false` (default → DORMANT/byte-identical), `shadow:true` (log-only when enabled), `ttl_days:5`. Rollout OFF→enabled+shadow→shadow:false |
| Wiring / tests | `main.py` ONE shared instance → OrderPlacer (record) + SecondaryScreener (read). `tests/unit/test_mis_blocklist.py` (22). Parity: recording fires only on the live 400 (paper never 400s) |

> STAGED on branch `mis-tradability-filter-30jun`; NOT pushed. Daily-list path investigated + rejected in Step 0 (no clean source). Detail: memory `mis_learned_blocklist_30jun`.

## Control Tower (`ops/control_tower/`) — VM Operations monitoring (Phase 1, 29-Jun)
A READ-ONLY aggregator that reads the EXISTING monitors and adds data-freshness +
disk/backup checks. It NEVER blocks or acts on trading.

- **Daily run:** `ops/control_tower/runner.py` at **17:05 Mon-Fri** (cron job
  `control_tower`; supersedes the standalone size-logger). Order: size-logger
  (today's trend SIZE cols FIRST) → aggregator (detection → findings lifecycle →
  health → status roll-up → reporter). **Skips non-trading days** with a SKIPPED
  heartbeat (`--force` runs anyway, e.g. a preview). Records
  `cron_heartbeat("control_tower")`; `check_cron_drift` (18:00) is the external
  monitor (the runner is `self_job`-aware so it never flags its own in-flight run).
- **Adapters (4, read-only):** security `data_store/security/last_run.json`
  (written by `security_monitor`) · cron heartbeats + `cron_marks/*.done` · the
  Config Auditor (`audit_app_config`) · `excursion_reconstruction_runs`.
- **Freshness** (`freshness.py`): per stage candles(15:40)/signals/orders/
  executions/reconstruction(15:50)/eod(16:00) → OK | OVERDUE (>15-min grace) |
  MISSING (after cutoff) | NA (not yet due → never a finding).
- **Severity maps** (`severity.py`, the single reviewable place): security
  CRITICAL→CRITICAL, **WARNING→MEDIUM** (dynamic-IP login fatigue — genuine
  threats already emit CRITICAL), INFO→INFO; config_auditor BLOCK→CRITICAL,
  WARN→HIGH, INFO/PASS→INFO.
- **Health score** (`health.py`, OPEN findings only, ACK-suppressed):
  C=100 / H=50 / M=20 / L=5 / INFO=1 → ≤50 HEALTHY, ≤150 WARNING, ≤300 ATTENTION,
  else CRITICAL.
- **Disk thresholds** (`disk.py`): usage HIGH ≥85% / CRITICAL ≥95%; backup-growth
  ≥500MB, log-growth ≥300MB vs the prior trend row (history-gated).
- **Telegram (noise-controlled DELTA, `reporter.py`):** pushes ONLY a CRITICAL
  (unless ACKNOWLEDGED) or a NEW/reopened HIGH; MEDIUM/LOW/INFO never push; a clean
  run is SILENT (the heartbeat + `last_successful_run` prove it ran). Uses the REAL
  `TelegramNotifier.from_env`.
- **Pull report (never pushed):** `data_store/control_tower/report_<date>.{html,csv}`
  — findings (OPEN/ACK/RESOLVED) + freshness + 7-day trends + largest folders/files.
- **Operator ACK CLI** (`ops/control_tower/cli.py`): `ct list` (OPEN+ACK) ·
  `ct ack <id>` (suppress from the health score + the push) · `ct unack <id>`
  (reopen). Run `PYTHONPATH=. venv/bin/python ops/control_tower/cli.py list|ack|unack [id]`.
- **Tables (schema v40, pure addition):** `control_tower_findings` (dedup identity
  category/resource_name/reason; OPEN→ACKNOWLEDGED→RESOLVED) · `_runs` · `_trends`
  (per date: sizes + health) · `_status` (per run_date: per-domain statuses +
  overall band + `last_successful_run`) · `_freshness` (per stage).
- **daily_report observability:** `reports/daily_report.py` records
  `cron_heartbeat("daily_report")` on a successful xlsx write (Phase 1d source-fix
  for the 23-Jun false "daily_report missed" MEDIUM).
- Deferred T1/T2/T3 (NOT Phase 1): disk-metric repair, a retention job, weekly
  advisory scanners (the Sun-18:10 slot). Detail: memory `control_tower_phase1a_29jun`.

## Deferred items board (30-Jun) — ONLY TWO open
Everything else is closed / deployed / dormant / awaiting normal production evidence.
| # | Item | Status | Reopen gate | Continuation |
|---|---|---|---|---|
| 1 | **S&R V1 calibration** | DEFERRED — collecting (V1 shadow LIVE; Phase A/B dormant) | **DATA-gated**: ~50 FILLED + ~8–10 BIR-FILLED W/L (checkpoint ≈14-Jul; verdict ~3–4 wk) | zone-accuracy review (watch TSFINV too-wide-zone) + BIR W/L → PASS→Phase A per `SR_V2_MERGE_VALIDATION_RUNBOOK` / RECALIBRATE→zone params only. Backfill cron `sr_detector_backfill`@15:58 feeds it nightly. Memory `sr_v1_calibration_deferred_30jun` |
| 2 | **Delivery Slice 2.5 (T2)** | **DEFERRED (02-Jul attempt — live NEVER run; proof script bit-rotted)** | **off-market whole-script repair first** (never run since the API evolved) | Pre-checks PASSED 02-Jul (deploy both fixes live HEAD 59f83b5; delivery-lock gate GREEN — T2 adapter `delivery_enabled=True`, `force_intraday_only` off the direct path, reconciler CHECK2 non-destructive for the untracked CNC). Drift in `t2_cnc_gtt_realtest.py`: L71 `core.`→`broker.order_state_machine`, L83 `RateLimiter(cfg.broker_limits.rate_limits)`→`RateLimiter(cfg.broker_limits)`, L319 `store=None`→wire store (else NO `gtt_state` row = lifecycle unvalidatable). REPAIR whole script + full static audit of construction/live-order path vs main.py (a dry-run can't exercise the live path) → tests → clean dry-run → commit → Rama pushes OFF-MARKET → run COMPLETE T2 (mid-session, not open 30 min / not after 14:45, flat book). Parked branch `fix-t2-import-02jul`@`b826ae0` (L71 only, unpushed, superseded by the full repair). Memory `t2_attempt_02jul` / `delivery_slice25_status_30jun` |

## Delivery (Slice 2.5) — ~~STATUS: BUILT / DORMANT / AWAITING_T2_VALIDATION (30-Jun)~~
## ~~STATUS UPDATED 05-Aug-2026: `<DEPLOYED — ENTRY PATH VERIFIED LIVE 05-Aug; EXIT PATH UNVERIFIED>`~~
## ⭐⭐ STATUS **UPDATED 05-Aug-2026 23:0x: `<ROUND TRIP VERIFIED LIVE 05-Aug; CLEAN SHUTDOWN WITH POSITION HELD VERIFIED 05-Aug; T+1 CARRY UNVERIFIED>`**

> ⭐ **WHAT THE SECOND CLAUSE MEANS, PRECISELY — and it is narrower than it sounds.** At **22:46:36**
> a clean `systemctl stop` ran with a real CNC delivery position held: `_shutdown()` completed,
> **squared off nothing, cancelled nothing, released no capital, closed no position**, emitted the
> census (`mismatches=0`) and exited **0**. The safety argument had been read from source all
> evening; **that is the reading that held in production.** Record: `docs/audit/STOP_PROCEDURE_05-Aug-2026.md` §(i).
> ⛔ **IT DOES *NOT* MEAN THE EXIT PATH IS VERIFIED.** No GTT fired, nothing was sold, and
> **`acted 24` on `cnc_gtt_monitor` counts rows EXAMINED, not actions TAKEN** — so the GTT question
> remains **(d) undetermined** until `get_gtts` is instrumented.
> 🔴 **T+1 CARRY IS THURSDAY'S** — scored at `docs/audit/THURSDAY_CONTINGENCY_06-Aug-2026.md`
> → **BRANCH A CONTINUED, step 3**, whose expectation and baseline were written **before** the fact.

> ### 🔄 CURRENCY — read this before the 30-Jun text below, which is **superseded, not deleted**
> ⛔⛔ **THE THREE LOCKS ARE OFF AND DELIVERY HAS TRADED.** Flags pushed **04-Aug 19:08:09** (`0197923`)
> and first **LOADED at the 05-Aug 08:15 boot**: `force_intraday_only: true→false` ·
> **`trade_type: INTRADAY→BOTH`** · `delivery_enabled: false→true`. *(⛔ **NOT**
> `conditional_allocation_enabled` — measured a behavioural no-op under `BOTH`; it remains `false` and
> Rama's call on it remains open.)* The boot published `delivery_lock.status → cnc_orders_possible:
> true` and `strategy_control.summary → will_trade_count: 15`, including all three positional
> strategies.
>
> ⛔⛔ **USE THE COMPOUND LABEL. DO NOT WRITE "LIVE".** A filled CNC position proves **screening ·
> sizing · affordability · placement · FILL** — the whole ENTRY path. ⛔ **The GTT TRIGGER, T+1
> HANDLING and the CARRY are UNPROVEN**, and the 05-Aug evening operator card
> (`docs/audit/EVENING_OPERATOR_CARD_05-Aug-2026.md`) exists precisely because of that. **`will_trade`
> is a permission, not an order; a fill is not an exit.**
>
> ⚠️ **OPERATOR-REPORTED, ⛔ NOT MEASURED — and no count or value is written here on purpose.** The
> held-position figures have never been read from the DB or the broker by this bridge; the PC's DB copy
> is two days stale and no VM read has occurred. ⛔ **Do not copy a position count or a rupee value
> into this map from conversation.** Tonight's card is what measures it.
>
> ⭐ **WHY THE STRUCK TEXT STAYS LEGIBLE HERE, WHEN THE OPERATOR CARD FORBIDS STRIKETHROUGH — the two
> rules are different and this is the distinction:** **§G4 (supersede-but-keep-legible) governs
> REGISTER-CLASS documents, which are read for history. NOBODY EXECUTES `SYSTEM_MAP.md`, so legible
> history is safe here.** An operator CHECKLIST is the opposite: a struck-out command is a command
> someone might still run, so that document is revised cleanly and its history lives in a revision
> table. ⛔ **Do not apply the checklist rule to this file, or the map loses its history.**
>
> 🔴 **WHY THIS WAS CORRECTED THE SAME DAY, recorded so the urgency is understood:** `campaign_practices.md`
> **§M8** (written 05-Aug) makes reading this file **mandatory pre-reading for every future session**.
> ⇒ **A rule that mandates reading a document raises the cost of that document being wrong.** Before
> M8 a stale entry here was a trap someone might walk into; after M8 it is a trap **every session walks
> into first, by rule.** Shipping M8 while leaving this section stale would have armed the exact
> failure M8 exists to prevent — with the campaign's own authority behind it.
>
> ⛔ **SCOPE OF THIS EDIT, stated so the sweep is falsifiable:** only **CURRENT-STATE** delivery claims
> were changed — this section and the INDEX line at the top. **Width:** a sweep of this file for
> `DORMANT|never exercised|delivery_enabled=false|trade_type=INTRADAY|force_intraday_only=true|gtt_state = 0 rows`
> returns hits at **`:36`** (INDEX — fixed), **this section** (fixed), and **~14 lines in the
> Changelog and dated banners** (`:162`, `:1213`, `:1244`, `:1255-1271`, `:1289`) — ⛔ **those are
> DATED HISTORY and were deliberately NOT touched: they were true when written, and rewriting them
> would destroy the record.** *(Other `dormant` hits — `:1132` MIS blocklist, `:1183-1186` S&R
> phases — are unrelated to delivery and out of scope.)*

~~The CNC/delivery lifecycle is **built + deployed to main but DORMANT and never exercised**
(`gtt_state` = 0 rows ever; flags OFF: `delivery_enabled=false`, `conditional_allocation_enabled=false`,
`trade_type=INTRADAY`, `force_intraday_only=true`).~~ Architecture validated (30-Jun read-only assessment),
~~**NOT production-proven**~~ **ENTRY path now production-proven; EXIT path not.** Components: P1 GTT placer (`orders/cnc_gtt.py`), P2 durable `gtt_state` v36 +
monitor (`orders/cnc_gtt_monitor.py`) wired into the reconciler, FIX-183 orphan-GTT adoption, P3 delivery
caps + conditional capital (`risk_engine`/`fund_manager`), P4 trade_type gate. Square-off paths exempt CNC
in LIVE (`eod_squareoff` MIS/CO-only; `order_monitor` 15:15 delegates to EOD; reconciler skips ACTIVE-gtt_state trades).
**Next gate = the T2 live round-trip proof** (`scripts/t2_cnc_gtt_realtest.py`, Rama-supervised, market hours,
REAL money — direct-API, does NOT flip live flags). **REOPEN: 02-Jul ATTEMPTED → DEFERRED** (proof script bit-rotted; needs an off-market whole-script repair — see deferred-board row 2 above + memory `t2_attempt_02jul`. The original 1-Jul reopen prereq was the
ensure-flat safety fix to the proof script — the `finally` must square the held position, not just delete
the GTT). Then: optional paper rehearsal → supervised ~11:30 IST run → verify PASS → harden/defer the §4
edges → paper-carry → supervised live-carry pilot → mark CLOSED.

> ### ⭐ THE SEQUENCE ABOVE IS **SUPERSEDED AND KEPT** (05-Aug, §G4) — ⛔ do not read it as the current plan
> **`Next gate = T2` is DISCHARGED:** T2 was **armed live 29-Jul** (5 CNC held overnight, broker == system
> on every field) and its basket is **fully closed** (holdings 0 / GTTs 0 / positions 0, re-confirmed
> 04-Aug). The flags then flipped 04-Aug and delivery traded 05-Aug. ⇒ **the ladder above ran; what
> remains of it is the CARRY PILOT, which is separately gated and NOT authorised.**
> ⛔⛔ **AND `scripts/t2_cnc_gtt_realtest.py` PLACES REAL ORDERS WITH REAL MONEY** (its own header says
> so; `place_order` BUY/SELL + `delete_gtt`). **It is NOT a GTT lister and must never be put on an
> operator card as one.** ⚠️ **There is no read-only GTT-listing script in this repo** — width:
> `git ls-files | grep -i gtt` → 10 files, one script, and that is it.
>
> ### 🔴 THE RESIDUAL RISKS ARE NOW **LIVE**, NOT "BEFORE GO-LIVE" — and one of them is tonight's gate
> **(1) ⭐⭐ THE GTT-LINKAGE-LOSS RECONCILER EDGE IS THE 05-Aug EVENING GATE.** *"Carried CNC mis-closed
> if both `gtt_state` row + broker GTT vanish"* — **named here on 30-Jun, five weeks before it mattered.**
> Measured 05-Aug: CHECK1's skip is keyed on an **`ACTIVE` `gtt_state` row whose `trade_id` equals the
> trade's** (`order_reconciler.py:855,858,871-872`), so a broker-side GTT with no matching row does
> **not** skip; on T+1 the holding leaves `positions()` and CHECK1 fires MANUAL_CLOSE, **cancelling
> broker orders and releasing capital on shares still held.** ⛔ **It does NOT place a sell.** Full
> measurement: `docs/audit/check1_product_skip_step1_05aug2026.md`.
> **(2)** the `conditional_allocation_enabled` foot-gun — **measured a behavioural no-op under
> `trade_type: BOTH`**, so the 70%-stranded premise assumes a delivery-ONLY book, which is not what is
> deployed. **Rama's call remains open; it is not urgent.**
> **(3)** paper-mode EOD mis-square and **(4)** DDPI broker-account-only: **unchanged.** ⚠️ **(3) is the
> V1 rule in miniature — paper nets by SYMBOL, live Kite per (symbol, product) ⇒ a paper drill of any
> product-keyed behaviour is vacuously green.**

**Residual risks ~~before go-live~~ (see the block above — they are now LIVE):** config foot-gun
(enable delivery without `conditional_allocation_enabled=true` → 70% capital stranded), GTT-linkage-loss
reconciler edge (carried CNC mis-closed if both gtt_state row + broker GTT vanish), paper-mode EOD mis-square
(paper adapter mislabels CNC as MIS), DDPI is broker-account-only. Detail: memory `delivery_slice25_status_30jun`.

## Changelog
- 2026-07-28 — Claude Code (Opus 5) — **REGISTER CONSOLIDATION: SEVEN OPERATOR FILES → ONE, AND FOR THE FIRST TIME IT IS TRACKED IN THE REPO — `docs/MASTER_PENDING_28-Jul-2026.txt` (`d5b9d8c`, local, unpushed until Thu 30-Jul).** Replaces `MASTER_PENDING_REGISTER_FINAL_16-Jul` · `REVISED_25-Jul` · `REVISED_25-Jul_EVENING` · `REVISED_27-Jul` · `REVISED_27-Jul_EVENING` · `TUESDAY_28-JUL_CARD` · `DO_NOT_DELETE_READ_FIRST_27-Jul`. **DESIGNED AS AN INDEX PLUS THE ITEMS THAT LIVE NOWHERE ELSE** — for anything `docs/decisions/00_INDEX.md`, `ACTIONS_not_decisions.md`, the deploy calendar or the unpushed ledger already own, it gives one line and a pointer; ⛔ a second copy of a register is a second thing to keep in sync, which is what two days had just been spent removing. Every cross-reference is resolved INLINE, because after the originals are deleted **a pointer to a deleted file looks like a source and is worse than no pointer**. ⭐ **THE LOCATION WAS FOUND, NOT INVENTED:** `docs/` already tracked `T2_RUNBOOK_29-JUL.txt`, `DEPLOY_CALENDAR_28-JUL_TO_04-AUG.txt` and `MONDAY_27-JUL_CARD.txt`, so the prior "operator docs are NEVER a repo artifact" rule had already been overtaken by practice; Rama's 28-Jul instruction made it explicit. 🔑 **THE COMMIT WAS BLOCKED BY `.git/info/exclude:29`, NOT `.gitignore`** — a grep of `.gitignore` returns nothing and is simply the wrong file; `git check-ignore -v <path>` names the real rule. Unblocked by a **NEGATION on the canonical path** (`!docs/MASTER_PENDING_*.txt`) rather than by loosening the glob, and both directions were verified: the repo copy is trackable while a stray `MASTER_PENDING_*.txt` at the root and every `*_CARD.txt` remain excluded — so the 25-Jul rename-gap that rule was broadened to close **stays closed**. ⚠️ One-time unblock only: once a file is tracked, git ignores no longer apply to it. **RECONCILIATION, with its own precision stated rather than implied: M = 118 CARRIED (exact — every item enumerated by ID in the file) · K ≈ 91 CLOSED with evidence · J = 211 MERGED-AS-DUPLICATE, of ~420 named-item appearances.** ⛔ **J is DERIVED (N−M−K), not independently counted, so any imprecision in N or K lands in it — it must not be quoted as a measurement**; the guarantee the section actually provides is M plus an explicit not-carried list with a reason per omission (spent cards, spent Monday blocks, per-edition "what changed" narratives, the numbered C-1..C-44 correction series whose load-bearing members are restated as X1-X15). ⭐⭐ **THE JOB WAS NOT BOOKKEEPING — IT SURFACED A LIVE PRODUCTION GAP THAT EXISTED ONLY IN A FILE ABOUT TO BE DELETED: `scripts/liveness_probe.py:108` sets `_LIVENESS_END = 16:00` while `config/system_config.yaml:59` now sets `service_window_end: "17:35"` ⇒ THE SERVICE RUNS 95 MINUTES LONGER THAN ANYTHING WATCHES IT, EVERY TRADING DAY.** A silent death at 16:20 produces an empty next-day watchlist indistinguishable from "no breakouts"; the manual ~17:10 alive check on the evening cards **is** the compensating control for exactly this. ⭐ The in-code comment ("≤ the configured service stop, drift-guarded by the tests") is TRUE and still permits the gap — 16:00 ≤ 17:35 passes the guard, because **the guard prevents the probe outliving the service, not the reverse**. Registered as §4 G1; ⚠️ not a one-line cron edit (the cron window and `_LIVENESS_END` must move together). **FOUR MORE CORRECTIONS FOUND BY RE-VERIFYING RATHER THAN TRANSCRIBING:** (1) `one_trade_per_symbol_direction_per_day: true` and `mis_filter → SHADOW` (`b7f5eef`) both read as SHIPPED in the 27-Jul evening register but are **on `fix-symdir-27jul` and ABSENT from `main`** — `<BUILT>`, not `<DEPLOYED>`; (2) that same register **contradicts itself on the date** — its §8 says the rule is live "FRI 31-Jul 08:15" while stating in the same sentence that it ships on the Friday *evening* slot, which cannot both be true; its own §9 and the deploy calendar say **Mon 3-Aug**, which is correct, and the error had already propagated into the 28-Jul desk note; (3) **B2/M-S4 (`secondary_screener.py:407 "atr"` / `:408 "rsi"` / `:410 "prev_close"` all None) is UNCHANGED since 16-Jul** — 25 of 100 scorer points are still a constant 0.0; ⚠️⚠️ **and on B1 the register and `PATHS.md` DISAGREED AND BOTH WERE WRONG — the truth is a third thing, MEASURED from production: `trades.sector` is NULL on 361 rows (15-Jun→16-Jul), `'UNKNOWN'` on 81 (20-Jul→28-Jul) and a real value on EXACTLY ONE trade ever (`PSU_BANK`, 20-Jul). ⇒ the 16-Jul claim "NULL at INSERT" is obsolete (the write path shipped ~17-Jul) AND `PATHS.md`'s "B1 CLOSED" is wrong (only the write path closed); what is broken is the RESOLUTION, answering UNKNOWN for essentially everything. ⛔ AND THE ERROR DIRECTION INVERTED, which is the part that matters for D1/sizing: under NULL the cap was silently PERMISSIVE (`sector_exposure()` filters `AND sector = ?` ⇒ summed nothing while reporting PASS); under a single `UNKNOWN` bucket every position collapses into ONE sector, so the same query sums the WHOLE BOOK — conservative, not permissive. Stated as an implication of the data, NOT as verified gate behaviour: `sector_exposure()`'s handling of 'UNKNOWN' was deliberately NOT read tonight (no code work in a freeze), and it MUST be read before D1 moves.** Register §4 G3; (4) the **"V3 DECISION CONTENT SPECIFICATION v1.0" still does not exist**, verified by searching for the document itself rather than its citations, while `screening/hard_gate.py`, `v3_chain/pb01_entry.py`, this map and one audit report all cite it as the authority for PB-01's gates. ✅ **AND ONE 16-Jul UNKNOWN IS RESOLVED, NOT CARRIED:** its Appendix C listed "D4 — the 4th standing Rama decision, text lives only in the external 14-Jul .txt". **D4 is the EXIT POLICY, and it is CLOSED** — `docs/decisions/05_d4_exit_policy.md` is in the repo and the thread closed 24-Jul on measured numbers (8 policies, 115 trades, best −0.076R vs current −0.100R, none reaching breakeven). ⛔ The seven originals were **NOT deleted** — Rama deletes them himself after reading the replacement; he has been told explicitly that it is self-contained and they are safe to delete.
- 2026-07-28 — Claude Code (Opus 5) — **OPERATOR AVAILABILITY RECORDED ON THE DEPLOY CALENDAR FOR ALL FOUR DAYS: Rama is UNAVAILABLE 11:00-17:00 IST on Wed 29-Jul, Thu 30-Jul, Fri 31-Jul and Sat 01-Aug.** ⚠️ **It lands on both market-hours events the T2 sequence is built around.** **THE ARM (Wed) COLLIDES:** the runbook's §1 P3 wants **AIM 13:00**, acceptable 12:30-14:00, ABORT outside **10:30-14:30**, with P6 needing ~30 uninterrupted minutes — so the aim *and the entire acceptable band* sit inside the unavailable window, leaving **10:30-11:00 as the only slot inside both the runbook's own bound and his availability**, which is exactly the 30 minutes P6 requires with **no slack for §3's mandatory by-hand broker verification**. ⚠️ **COST, FROM THE RUNBOOK'S OWN FIGURES:** the 13:00 anchor is *half* the void reduction (band −10/+10 plus a 13:00 anchor ⇒ ~2.8% void; the band alone at the **measured 11:30 anchor** ⇒ ~9.0%); 10:30 is **earlier than the measured anchor**, so void risk is **≥9% and is NOT measured at a 10:30 anchor — no honest number exists for it and none was invented**. A void costs one day. ⭐ One thing improves rather than degrades: P3 wants 13:00 partly so a rejection "leaves room to retry", and at 10:30 there is more. **THE CLOSE (Thu) DOES NOT COLLIDE** — §7 gives it no clock time at all, only "market hours only", so 09:15-11:00 works, and early is if anything better since a SELL rejection means DDPI is unauthorised and restarts the pair. Options recorded **without a recommendation, because the tradeoff is Rama's**: arm 10:30-11:00 · carve ~40 min out of the unavailable window (the only option preserving ~2.8%) · or slip, which is **not free here** — never a Friday arm ⇒ Mon 3-Aug is owned by the symbol+direction rule going live ⇒ realistically Tue 4-Aug, colliding with the flag-flip gate. ⭐⭐ **THE UNATTENDED-WINDOW RISK WAS ANSWERED FROM SOURCE, NOT FROM THE RUNBOOK'S PROSE:** the CNC position's protection is the **broker-side OCO GTT alone**, which lives at Zerodha independent of our service, process, DB, the VM and the laptop ⇒ **being unattended changes nothing about it** (max loss ~₹10, one IDEA share); the 15:15 circuit-breaker and EOD squareoff **do fire unattended** — MEASURED 28-Jul 15:15:01, fully automatic — but are **structurally incapable** of touching the share, since `orders/eod_squareoff.py:1064-1072` filters `p.product in ("MIS","CO")` under the module's stated EOD6 invariant "DELIVERY (CNC) positions NOT touched", which is precisely what keeps the test valid because an EOD square would void it; and **`scripts/liveness_probe.py` is DETECTION, NOT PROTECTION** — it reads `systemctl show` read-only and calls `deliver_alarm()` for one Telegram CRITICAL with a sentinel fallback, with **no restart, no start and no corrective action anywhere in it**, and it watches the unit rather than the position. ⇒ **NET: no added money risk to T2; the real exposures are a Wednesday-afternoon GTT trigger silently VOIDING the arm unknown until 17:00, and the ordinary intraday book — where a service death at 11:30 raises one CRITICAL nobody can act on for ~5.5 h.** That second exposure exists every day and is not created by T2; it is simply un-actionable on these four days. ⚠️ And it compounds with the liveness gap in the entry above: between 16:00 and 17:35 there is no alert to be un-actionable about.
- 2026-07-28 — Claude Code (Opus 5) — **DEPLOYED 18:46:19 IST: `52ead46..ce08668`, 24 commits, `main` now 0 ahead of `origin/main`. The stray-`.pyc` detector is `<BUILT>` → 🏷️ DEPLOYED — ⛔ NOT "VERIFIED LIVE"; its first production run is WED 29-JUL 18:45.** The push was **gated, not assumed**: CHECK 3 clean (`inactive (dead)` · `Result=success` · `ExecMainStatus=0` · exit **17:35:04** · `NRestarts=0` · `ActiveEnterTimestamp` still `08:15:15` = one continuous run) + **ZERO orphaned reservations** + book flat. ⭐⭐ **THE ORDER WAS REVERSED ON THE NIGHT AND THE REASON IS THE REUSABLE PART: a power-down moved the clock past 18:25, so the 18:45 EOD job was allowed to run FIRST, on the OLD code, as a deliberate BASELINE.** 27-Jul's EOD report never appeared (the v45 schema-refusal night), so *"is the EOD job broken at all?"* was a genuinely open question — and pushing first would have left **two candidate causes and one observation**, the exact confound the deploy calendar exists to prevent. ✅ **RESULT: `reports/system_manager/2026-07-28.txt` PRESENT (4,242 B, 18:45), heartbeat `system_manager_eod SUCCESS 18:45:05`, 10 full sections, `check_failed`=0 ⇒ THE EOD JOB WORKS, and 27-Jul's absence is explained by the schema-refusal night ALONE.** That closes it as a cause, rather than leaving a second suspect alive. ✅ **The control held too: ZERO `STRAY` lines in that report** — the detector was not deployed yet, so its *absence* was the control and it was absent. Job confirmed FINISHED (not merely started) before the push replaced its own source file. **VERIFIED THREE WAYS, each falsifiable:** (a) PC `ce08668` == VM bare `ce08668`; (b) **md5 `35012cbb7e7b53cc35c4545d4e645cb4` (re-measured live at 18:43, i.e. the baseline was real, not remembered) → `a57af797b2624399b54b80b444c245da`**, the value pre-registered in the operator card, with `stray_pyc_check` occurrences **0 → 2**; (c) deploy reflog shows **BOTH** halves at `2026-07-28 18:46:19 +0530` — `push` **and** `checkout: moving from main to main`, the latter being the event that actually writes the tree — with the prior `52ead46 @ 09:15:25` pair intact, confirming `gc.reflogExpire=never` (set ~15:3x, before any push) is holding. ⚠️ **`post-receive` reported *"crontab AUTO-INSTALLED from canonical"* — VERIFIED A NO-OP** (110 lines; the `45 18 * * 1-5` EOD entry byte-identical pre- and post-push; zero `deploy/` files in the diff). Checked rather than assumed, because a hook that rewrites cron on every deploy is exactly the kind of side effect that gets discovered later. ⚠️ **ONE PROBE OF MINE ERRED AND THE CORRECTION IS WORTH KEEPING: `git -C ~/systems/trading-system rev-parse` fails with *"not a git repository"* — BY DESIGN.** The deployed tree is a **checkout target** of the bare repo and has no `.git`; that is precisely why the card specifies **md5, "not a date"**, as the tree-level check. Do not read that error as a deploy failure. **DAY EVIDENCE (CHECK 3, run 18:41):** error census **9, all 9 CLASSIFIED not counted** — 1 CRITICAL 08:15:16 startup kill-switch notice, 4 ERROR = 2 `slippage_exceeded` × 2 loggers (INFOBEAN 0.44>0.42, HEXT 2.40>1.47 — the guard *refusing an entry*), 3 CRITICAL 15:15:01 routine circuit-breaker SOFT_KILL triple, 1 ERROR 17:35:03 `kiteconnect.ticker "Connection closed: None - None"` one second before the 17:35:04 exit and paired with the `live_feed` disconnect WARNING (websocket teardown at shutdown). ⭐⭐ **The card's rule "any new CRITICAL at all is NOT clean" would have raised a FALSE ALARM, and was corrected in place rather than argued around:** its "5-and-explained" baseline was measured at **10:20**, before both the 15:15 kill and the 17:35 shutdown, so **a full-day census can never be 5**. Classified by a **falsifiable test, not assertion** — `circuit_breaker_force_close_15:15` appears **4× on every one of 22/23/24/27/28-Jul**, making it a daily designed property; `Connection closed: None - None` also appears on 23- and 27-Jul. **Full-day comparables: 22-Jul=6 · 23-Jul=11 · 24-Jul=12 · 27-Jul=13 · 28-Jul=9** — today is *lower than each of the three most recent trading days*. Exact and widened regexes both returned 9 ⇒ nothing hid behind a whitespace variant. **CAPITAL:** orphaned-reservation query **ZERO ROWS**, and reconciled independently — **25 RESERVE = 21 RELEASE + 4 COMMIT**, every reservation terminated, including the card's own 14:12 SWIGGY false-positive `3cdce65f04bc44b4`, which is now closed exactly as predicted; `RESET_PNL` present (1) ⇒ the routine 15:15 kill did **not** forfeit it, matching 24-Jul. **ORDERS:** 7 FAILED ↔ 7 `entry_cancelled_zero_fill`, each ~60-70 s after its trade, 1:1 in time order; the 2 REJECTED are exactly the 2 slippage rejections (no order placed ⇒ correctly no zero-fill line). ⚠️ *Width stated: matched by count and time-order, **not** by symbol — the zero-fill log line carries no symbol field.* ⇒ **Wednesday GO/NO-GO, runbook case (b) — "does any unexplained noise touch the ORDER or CAPITAL path?" → NO ⇒ ✅ PROCEED** (schema reads 45, not 44). ⚠️ **OPEN OBSERVATION carried, NOT closed: PB-01 25 → 12.** It touches neither path so it does not gate — but *"does not gate" is not "explained"*. ✅ Also checked and **not** a finding: `watchman.md`/`flow_trace.md` MISSING appears in the 22-, 23- and 24-Jul reports too. **DISCHARGED:** the 24 commits · `8331fb0` detector · fix061 test repair · `1b03a64` recovery docs · **`45e0133`+`d2426d2`, which were recorded "UNPUSHED BY DESIGN → Thursday" and in fact went tonight**. ⏳ **THU 30-JUL still owes:** `fix-tests-27jul` · `fix-boot-27jul` · `214a878` · `249317d`; `fix-symdir-27jul` → 31-Jul; **§7.1 pre-push gate (#16a inert on the VM) must be RE-RUN on the night.** ⭐ **NOT a change-freeze breach:** the freeze forbids changes *on Wednesday*; this landed Tuesday. The detector's Wednesday run is contained — `system_manager.py:1093-1102` wraps every check in try/except, so a raiser logs `check_failed`, becomes a WARNING section, and the report still builds; it can never trip a soft-kill. A flat book was preferable, not required.
- 2026-07-28 — Claude Code (Opus 5) — **TEST-COVERAGE SWEEP + FIX-061 REPAIR (test-side only; ⛔ no production code touched). "KNOWN PC-ENV FAILURES" IS A LABEL, NOT A DIAGNOSIS — and swept as a CLASS it is wrong for ALL THIRTEEN.** Regression = 13F/5,362P/4skip; ⭐ **all 13 fail in 2.54 seconds** — no network call, no missing binary, no Windows-path problem. They fail on mock wiring, a stale fixture, a self-inflicted orphan process, or a real contract violation. ⭐⭐ **THE GENUINELY ENVIRONMENTAL CASES ARE THE *SKIPS*, NOT THE FAILURES** (`test_zerodha_login` ×2 `skipif` win · `test_backup_retention` symlinks · `test_gui_secret_key` POSIX modes — each names its environmental fact and skips cleanly) ⇒ **the label borrowed the skips' legitimacy for a population that has none**, which is how `test_instance_lock` stayed unexamined 9 days and FIX-061's hard-kill coverage 22. **Ranked by what each leaves uncovered, not by count:** (1) `test_order_placer_fix061` ×4 — **the LTP-retry → emergency-exit → HARD_KILL path, the same machinery as K1**; (2) `test_fix181` inflight-orphan flatten asserts `LIMIT` while production emits **`MARKET`** under HARD_KILL — **⚠️ UNKNOWN, not guessed**: stale test or a real kill-path divergence; (3) `test_main::TestContinueFromGate` ×3 — entry gate → placement; (4) `test_closure_source_contract` — ⭐ **not a failure at all, the test is WORKING**: it reports `scripts/backfill_closure_source_w8.py:92` restating `OWN_SL/OWN_TGT/OWN_EOD` instead of importing `core/closure_source.py:43-45`, **unfixed on every branch checked**, and that script wrote 35 rows on 28-Jul; (5) `test_fix077_flask_max_content_length` — `int(Mock)` at `webhook_receiver.py:212`; (6) `test_instance_lock` ×2 — the 120 s orphan `Popen`, **fix already queued on `fix-tests-27jul`**; (7) `test_main::test_paper_mode_does_not_require_webhook_secret` — ⚠️ UNKNOWN. **✅ RANK 1 FIXED THE SAME DAY, TEST-SIDE:** root cause was a **stale 15-Jun fixture, not a stale test** — H-3 (`a254a82`, 05-Jul) added `trade = store.get_trade_for_tgt_retry(trade_id)` then `trade["status"]`, and `store` is `self._om._store` on a bare `Mock`, so all four died **inside production code at `order_placer.py:3513`** (`TypeError: 'Mock' object is not subscriptable`) before reaching a single assertion. Repaired by giving the store mock the real row shape (the query's own SELECT columns; `state_store.py:1543` returns an `sqlite3.Row`). ⛔⛔ **THE LOAD-BEARING PART IS `get_sl_order_for_trade` → `None`, AND IT IS EASY TO GET BACKWARDS** — at `order_placer.py:3524` a **non-None** SL sends execution down the "an SL already exists, place the TGT only" branch and **returns**, so `place_deferred_exits` is never reached and neither is the `hard_kill` these tests assert; a bare `Mock` is non-None, which is how the default silently took the wrong branch. ⭐ **I hit this myself mid-repair and briefly had three tests failing on "hard_kill not called" — which would have been reported as a production defect on the kill path had I not traced the branch.** **RESULT: 9 passed** ⇒ *the production path was fine and had merely gone unwatched — **no defect**.* ⭐ **Not vacuous:** returning an SL row instead of `None` makes three of them go red on `hard_kill`, so the assertions demonstrably can fail. Neighbours 182 passed (the one failure is `test_fix181`, already failing beforehand). ⚠️ **WIDTH, per the standing rule applied to my own sweep:** the 13 come from `pytest tests/unit tests/integration` = 308+14 = **322 files**; ⛔ **`tests/crash_test/` (8) and `tests/core/` (1) are NOT in that command — 9 test files never run in the gate at all.** ⭐ *A skipped test and a failing test hide the same thing; an **uncollected** test hides more, because it appears in no count.* Widening the gate is its own item, deliberately NOT done mid-sequence (it would move the baseline). Full ranked table: `docs/decisions/ACTIONS_not_decisions.md`.
- 2026-07-28 — Claude Code (Opus 5) — **CAPITAL-DRIFT KILL LADDER — INVESTIGATION ONLY, ⛔ NO VALUE CHANGED (yaml/defaults/fixtures untouched; `git diff HEAD -- config/ capital/ core/ orders/ signals/ main.py` empty; thresholds still 250/1,000/2,500 and `consecutive_cycles_before_escalate: 3`).** ✅ **DECISION #11 IS CLOSED (Rama, 28-Jul 15:04): Option A — absolute, VALUES UNCHANGED (250/1,000/2,500, cycles 3), NO code change, NO boot slot.** An intermediate ₹1,500 soft-kill figure proposed earlier the same day was **WITHDRAWN, not deferred** (recorded, not deleted — a withdrawn figure that vanishes gets re-proposed), and the "earliest slot is after 4-Aug" line it implied is **struck**: there is no slot because there is no change. Reopen only on a material capital change, the ladder actually firing, or a drift distribution becoming obtainable. ⭐⭐ **THREE FINDINGS MADE UNDERNEATH IT SURVIVE THE CLOSURE and are registered as K1–K3 in `docs/decisions/ACTIONS_not_decisions.md`** — ⛔ a closed decision must not bury them. **K1: an EMERGENCY kill + a SAME-DAY restart ⇒ `StartupScenario.HALT`, exit 4, no boot without `--resume`, and in that window no exit management and no 15:15/EOD squareoff** — and it is a **CLASS, not a drift item** (26 non-test `soft_kill`/`hard_kill` call sites vs exactly **two** literals in `SCHEDULED_KILL_REASONS`, so every other path is an emergency kill). ⭐ **But it is MONITORED, which is the difference between a bounded and an open-ended hazard:** systemd neither retries nor loops (`RestartPreventExitStatus=3 4`, verified in `deploy/systemd/trading-system.service:35` **and** on the VM), and `liveness_probe` (cron `*/5 9-15 * * 1-5`, verified in the live crontab) raises **ONE CRITICAL within ~5 minutes** for a not-active unit during [09:00,16:00) on a trading day. ⛔ **Recovery is the weak link: `deploy/resume.sh` works but is named in NO incident document, and both docs that touch this are WRONG** — `docs/RUNBOOK.md:101` describes a *"Restart loop (exit code 4)"* symptom that **cannot occur** (the unit exits ONCE and stays **`failed`** — journal `Failed with result 'exit-code'`, measured 21-Jul-2026 11:37:53; there is no `SuccessExitStatus`, so exit 4 is a failure exit. ⭐ `inactive (dead)` is the HEALTHY nightly state after a clean exit 0 — **`failed` is the HALT**; an earlier draft of this entry wrongly said `inactive`) with a cause that was **fixed** (a stale/prior-day kill auto-clears since 20-Jun; the real cause is a SAME-DAY kill), and `docs/05_incident_response.md:40` says *"Manual resume: restart the service"*, **which is exactly what fails with exit 4**. A two-line doc correction with zero deploy consequence; deliberately NOT done in a register-only session. **K2:** the 06-Jul ₹10,000 events remain unresolved (real discrepancy vs seeding artefact — both answers matter). **K3:** the observability gap below. Decision file `docs/decisions/11_absolute_kill_ladder.md` → **CLOSED**. **WHAT THE CODE ACTUALLY DOES (read from source, not the config comments — it matched Rama's description on both rungs):** `capital/drift_handler.py::_compute_tier` buckets |delta| into NOISE/LOG_ONLY(≥250)/SOFT(≥1,000)/HARD(≥2,500); SOFT calls `kill_switch.soft_kill()` which **blocks new ENTRIES and genuinely ALLOWS EXITS** — enforcement is `is_active(intent)`, where `exit` is blocked **only** on HARD — and HARD calls `hard_kill()`, which blocks all orders **and dispatches an async FLATTEN** (cancels resting orders, exits `OPEN`/`PARTIAL`/`PENDING_FILL`, 2 h max retry, per-trade escalation alert), so **HARD does not abandon positions**; its `exit` block exists because the kill switch's own indestructible flatten owns the close. ⛔⛔ **THREE CORRECTIONS TO WIDELY-HELD ASSUMPTIONS.** (1) **ESCALATION IS SINGLE-SAMPLE** — `consecutive_cycles_before_escalate: 3` gates **only** the LOG_ONLY→SOFT_ESCALATED promotion; `TIER_SOFT`/`TIER_HARD` **reset** the counter and dispatch immediately, so ONE ≥₹1,000 sample soft-kills and ONE ≥₹2,500 sample hard-kills. (2) Its wall-clock is **≈45 s** via `fund_manager_self_check` (reconciler CHECK7, `poll_interval_sec: 15`, runs every cycle at `order_reconciler.py:1002`) and **UNREACHABLE** via `fund_manager` — `sync_from_broker` has exactly **ONE** production caller, `main.py:995`, a **one-shot at 09:15**, so that source fires once per process lifetime and the in-memory counter dies at the 17:35 self-exit. (3) The persisted-kill HALT is **bounded to a SAME-DAY restart**, not overnight: a drift reason is not in `SCHEDULED_KILL_REASONS` so it is an EMERGENCY kill and a same-day restart hits `StartupScenario.HALT` (**exit 4**, no exit management, no 15:15/EOD squareoff until `--resume`) — but `clear_stale_state()` is a **HEADLESS GUARANTEE** (Rama, 20-Jun) clearing **every** prior-day kill regardless of type. ⭐⭐ **MEASURED READ-ONLY, WITH THE SEARCH WIDTH PROVEN FIRST (22 files `system_2026-06-29`→`2026-07-28`, 1,539,561 lines, no `.gz`/archive = the whole on-disk history): THE LADDER HAS NEVER FIRED.** Total `drift_handler` lines = **2**; escalating-source events = **ZERO**; no log_only/soft/hard trip; no kill `triggered_by=drift_handler`; `HARD_KILL ACTIVATED` = 0 (the 19 `SOFT_KILL ACTIVATED` are the scheduled 15:15/EOD kills). **The only drift ever observed at hard magnitude came from a source the ladder deliberately ignores** — 06-Jul ×2, `source=order_reconciler`, tier=HARD, **delta ₹10,000** (4× HARD, 200× the ₹50 reconciler tolerance), logged at **INFO** by design because `order_reconciler` is not in `_ESCALATING_SOURCES`; ⚠️ its `expected=0.00` is the "~zero expected, possible publisher bug" shape and looks like a **seeding artefact**, so it must not be cited as evidence of real ₹10k drift. ⛔ **AND THE LIMIT, STATED NOT GLOSSED: a realised-drift DISTRIBUTION IS NOT OBTAINABLE from these logs** — `TIER_NOISE` logs at **DEBUG** and production runs at INFO, so every drift below ₹250 is invisible; min/median/p95 need a code change (raise the noise tier or persist samples) plus time. `kill_switch_state` is a **single-row CURRENT-STATE table, not a history**, so it cannot answer "has soft ever tripped" either. ⇒ decision #11's original "cheap and unblocked" framing was **wrong**: the *trip* question was cheap, the *distribution* question is not, and any value chosen now is chosen against an unmeasured background on a path that has never fired. ⭐ Both halves of this were found by applying the standing rule — **an absence is only established by a check wide enough to have found the thing**: a grep for `"capital drift detected"` returned 0 while `"tier":"HARD"` returned 2, because the JSON key is `msg`, not `message`.
- 2026-07-28 — Claude Code (Opus 5) — **STRAY-`.pyc` DETECTOR** — ⚠️ **STATUS SUPERSEDED BY THE ENTRY AT THE TOP OF THIS CHANGELOG: it was `<BUILT>`/uncommitted when written, then committed as `8331fb0` and 🏷️ DEPLOYED at 18:46:19 the same evening (`ce08668`); the "LOCAL/UNCOMMITTED, NOT PUSHED, Thursday 30-Jul gate" wording below is HISTORY, not a live instruction.** ⛔ It is DEPLOYED, not VERIFIED LIVE — first production run WED 29-JUL 18:45. **MONITORING-ONLY: it can never change trading behaviour.** An **11th** isolated check in the existing `scripts/system_manager.py` (`stray_pyc_check`, 18:45 Mon-Fri EOD job) — the check count has run 8 (19-Jun) → 10 (security + slippage) → **11**. ⛔ **No new file, no new cron, no new report path**; the EOD report gains one section. **THE PROPERTY, MEASURED NOT ASSUMED (PEP 3147):** `pkg/__pycache__/x.pyc` with no `x.py` is **NOT importable** (a cache keyed to an existing source) — Type A, a search nuisance; `pkg/x.pyc` with no `x.py` **IS importable** (the legacy sourceless layout) — Type B, the real hazard. So the trigger is not "a `.pyc` exists" but "a `.pyc` sits where a `.py` would". **WHY IT EXISTS:** `post-receive` deploys with `git checkout -f`, which overwrites tracked files but **never removes untracked ones**, and all bytecode is untracked — production proves the residue survives: `tests/unit/test_daily_review.py` was deleted in `01e07b7` yet its bytecode has sat in the deployed tree for 2+ months (inert, Type A — but the same mechanism would preserve an importable Type B indefinitely). **SEVERITY IS CONDITIONAL ON THE PROPERTY, NOT THE OCCURRENCE COUNT** (a "warn once, escalate on the second" ladder would let the dangerous case sit at WARNING for a whole night): sourceless ⇒ `violation()` ⇒ report goes CRITICAL; beside its `.py` ⇒ `warn()` (inert residue). ⛔ **Never sets `soft_kill_reason`** — a stray build artefact must not be able to stop tomorrow's trading; asserted by test, not merely intended. **Exclusions are config** (`_PYC_SCAN_EXCLUDED_DIRS = ("venv","sats")`) with a comment saying what an entry means; ⛔ nothing added speculatively — measured the same day, there are **ZERO `.pyc` outside `__pycache__` anywhere** (PC tree, deployed tree, and inside both venvs), so the entries exist for scan cost/ownership, never to silence a known false positive. ⭐⭐ **PROVEN-TO-BITE (the point of the item — its trigger has measured zero occurrences, exactly the "configured but never covered" shape this project keeps getting caught by):** 6 tests appended to the **existing** `tests/unit/test_system_manager.py`, every plant inside `tmp_path` so nothing is ever written into the repo and **nothing is ever planted in the deployed tree** — sourceless ⇒ CRITICAL · beside-source ⇒ WARNING not CRITICAL · excluded path ⇒ SILENT · Type A orphan ⇒ SILENT · clean tree ⇒ SILENT · the real repo currently clean. **3 code plants all went RED on exactly their own branch** (severity downgrade · `__pycache__` filter dropped → 2,259 violations, which also proved the real-repo test non-vacuous · exclusions emptied); restore verified **byte-identical by md5 + length**. ⛔⛔ **`git clean -xdf` MUST NEVER BE RUN IN THE DEPLOYED TREE** — `data_store/` (live DB **and** its backups), `logs/` and `.env` are all untracked or ignored there; the prohibition is written in the **code comment**, where somebody reaching for the quick fix is standing, not only in a report. ⛔ **CORRECTION carried into the docstring:** `.gitignore:46` is `*.py[cod]`, so a Type B is **ignored** and `git status` shows nothing — **git is blind to this class on the PC as well as the VM** (verified via `git check-ignore`; the earlier "no bare `*.pyc` rule, so the PC would see it" claim was false, now fixed in `docs/audit/pyc_orphan_sweep_28jul2026.md` §4). ⇒ **this check is the only detector on either machine.** **Regression — GATE PASSED, both runs MEASURED:** BASE `b5c11ae` **13F/5356P/4skip** (13:53→14:08) vs AFTER **13F/5362P/4skip** (14:09→14:23) — **`comm` EMPTY BOTH WAYS** (no new failure, none silently fixed), **+6 = exactly the 6 new tests** (collection confirms 6 `stray_pyc` node IDs). **Predicted before the run and met exactly.** ⭐ A fresh BASE was taken in the same session rather than trusting a remembered count — and in the **same time window**, so neither run crosses the 18:15 startup-test flip that changes the known-failure set; the BASE run's `test_instance_lock` orphan child (a 120s unreaped `Popen` that holds a machine-global lock into the *next* invocation) was confirmed reaped before AFTER started, so its 2 failures are genuinely common-mode and not an artefact of running the suites back to back. Both runs in the same window (13:53→14:08, 14:09→14:24) so neither crosses the 18:15 startup-test flip; the BASE orphan-lock child was confirmed reaped before AFTER. **Parity:** the check is mode-agnostic (walks a tree; no paper/live branch). ⛔ **NOT a boot variable** — `system_manager` runs at EOD and `main.py` never imports it. Registered on the deploy calendar §7.5 with Friday's boot inventory re-stated (live boot variables unchanged at ONE). Related and **explicitly NOT built**: the deployed-tree-vs-HEAD integrity check (calendar §7.4, gated post-4-Aug).
- 2026-07-10 — Claude Code (Opus 4.8) — **trade_type/intent SEGREGATION fix (Option A) BUILT + COMMITTED (LOCAL/UNPUSHED, market-hours-safe build; push OFF-MARKET) — declared intent PRESERVED; the loader's load-time DELIVERY→INTRADAY rewrite REMOVED; resolver segregates on the DECLARED intent; product-coercion RELOCATED to the broker chokepoint. DELIBERATE BEHAVIOR CHANGE: default config now 12 WILL / 3 WON'T (the 3 positional_* DELIVERY strategies DORMANT, no longer repurposed as intraday) — was 15/0.** Root cause: `strategies/loader.py` rewrote every DELIVERY strategy's `intent`→INTRADAY at load whenever `force_intraday_only=true` (the permanent default), so `strategies.control.strategy_will_trade`'s trade_type×intent gate (LAYER 1×2) + FORCE_BREAKER (LAYER 0) never saw a raw DELIVERY intent → the 3 positional strategies fell through to WILL TRADE (as MIS), defeating trade_type's OWN documented semantics (`system_config.yaml`/`config_loader.py`: "trade_type INTRADAY = only intent==INTRADAY strategies trade"). NOT a safety bug (no CNC leakage — double-locked; positions were MIS by enforcement). **Fix (Option A, Rama+ChatGPT approved — un-parks what Phase-4 named "Option B"):** (1) `loader.py` DROPS the destructive `model_copy(intent→INTRADAY)` (declared intent preserved for all 15; a per-strategy INFO dormancy log replaces the WARNING overwrite); (2) resolver UNCHANGED but its FORCE_BREAKER (force ⇒ DELIVERY dormant) + TRADE_TYPE branches are now LIVE — a DELIVERY strategy is WON'T TRADE under the breaker / trade_type=INTRADAY, so it never reaches sizing/placement; (3) **P0 MIS-only RE-VALIDATED — product-coercion RELOCATED:** `zerodha_adapter.place_order` gains `force_intraday_only` and coerces ANY non-INTRADAY intent → INTRADAY (product MIS) before resolve — the SECOND, independent MIS lock, composing with the `delivery_lock` (`delivery_enabled=false` refuses CNC); `main.py` wires `force_intraday_only=app_config.system.force_intraday_only` into the adapter; (4) `scripts/strategy_status.py` DE-DUP: removed the `_effective_intent` rewrite-simulation (resolver gates on declared intent directly), footnote redesigned to flag DELIVERY strategies as DORMANT; (5) control/loader/strategy_status docstrings updated (no logic change). `enabled` enforcement (control.py:81) UNTOUCHED. **Parity:** one loader + pure resolver (no mode branch) → paper==live; adapter coercion runs both modes (default OFF). **Tests (RED→GREEN + mandatory P0 double-lock):** T1 `test_slice2_strategy_control` real-config per-mode counts INTRADAY **12/3** (was 15/0) · DELIVERY 3/12 · BOTH 15/0; T4 `test_zerodha_adapter` Option A DOUBLE-LOCK proven INDEPENDENTLY (LOCK1 coercion force⇒MIS with delivery_enabled=True; LOCK2 delivery_lock refuses CNC with force=False); T5 declared intent preserved (`test_p0_live_day1_fixes` rewritten); T3 table==gate. **305 pass** across slice2/phase4/p0/loader/product-resolver/signal-processor-gate/zerodha-adapter/cnc-gtt-delivery-lock/cron-officer; the 4 `test_main.py` fails = pre-existing PC-env baseline (verified identical on a clean-main stash). **Rollback:** single loader/adapter/config revert; `delivery_enabled=false` throughout (no live-delivery risk either way). NOT pushed/deployed. **PATHS.md `Default state` line (15 WILL) now stale — update on deploy.** memory `trade_type_intent_segregation_investigation_10jul` (→BUILT).
- 2026-07-09 — Claude Code (Opus 4.8) — **Wave-6 M-C3 FIXED → 🏁 WAVE-6 COMPLETE (branch `fix-mc3-per-bucket-invariant-09jul` `bff5511`, top of the M-O2→M-C7→M-C3 stack, off main `9815786`, LOCAL/UNPUSHED, deploy together off-market) — per-bucket non-negativity guard on used + reserved.** Root cause (audit M-C3): `fund_manager._check_invariant`'s per-bucket guard (the pre-audit "H-1" block) fired ONLY when a bucket's AVAILABLE went negative → a wrong-bucket release (or any future bug) driving a bucket's USED or RESERVED negative while its avail stayed ≥0 balanced the global books and passed SILENTLY (the per-bucket no-borrow guarantee drifted undetected). **Fix (detection only, NO schema change):** broaden each bucket's trigger from `avail<-tol` to `avail<-tol OR used<-tol OR reserved<-tol` (both buckets), reusing the SAME `assert_capital_invariant`→`CapitalInvariantViolation`→hard_kill path + tolerance (1.0₹). **NON-NEGATIVITY only, NOT a per-bucket EQUALITY check** — PnL legitimately shifts the per-bucket split (avail+reserved+used ≠ total×pct) so an equality check would false-fire; used/reserved can NEVER legitimately be <0. Because `assert_capital_invariant` is invoked ONLY when a partition is already negative, its INV6 field-guard raises BEFORE the equality check is reached → a legitimate PnL-shifted split never trips it (a false `CapitalInvariantViolation`→unnecessary hard_kill — this no-false-fire property is the crux). **Composition:** complements M-C7 (which fixed the leverage-decrease over-release, a CAUSE of negative used) — M-C3 is the DETECTION net for ANY per-bucket negativity; does NOT touch release_used/M-C7/M-O2; terminal-guard unaffected. **Parity:** one mode-agnostic invariant path (paper+live). **Tests** `tests/unit/test_mc3_per_bucket_invariant.py` (REAL StateStore+FundManager+fm_ledger): T1 neg-USED / T2 neg-RESERVED / T3 both buckets — all GLOBALLY BALANCED so only the per-bucket guard catches (RED→GREEN proven: silent against the stashed pre-fix), **T4 NO-FALSE-POSITIVE** (profit/loss closes, warm restart, M-O2 partial, M-C7 release, multi-bucket, normal cycle — none raise), T5 tolerance boundary. **450 pass** across fund_manager/invariant/mc7/mo2/mc1/reconciler/order_placer/cnc_gtt/terminal-guard/a1e1/daily-loss/e2-capital/phase3-delivery/crash capital-invariant+double-release+fund-manager-edges; 0 regressions (0 false fire in the corpus). **🏁 Wave-6 COMPLETE: M-O1 already-fixed (959badf) · M-O2 · M-C7 · M-C3 all built + stacked + unpushed (deploy together).** memory `mc3_mc7_capital_invariant_recompute_09jul`.
- 2026-07-09 — Claude Code (Opus 4.8) — **Wave-6 M-C7 FIXED (branch `fix-mc7-release-committed-margin-09jul` `117ed8d`, STACKED on the M-O2 branch, off main `9815786`, LOCAL/UNPUSHED, deploy together off-market) — release_used reverses the PERSISTED committed margin instead of recomputing from the current leverage_map.** Root cause (audit M-C7): `release_used` freed `required_margin(qty, entry_price, intent, CURRENT leverage_map)` (M2), but `commit_to_used` persisted the commit-time margin (M1) in the COMMIT `fm_ledger` row and `rehydrate` REPLAYS that same M1 → a leverage-config edit across a restart with a carried position left a permanent residual `M1−M2` in `used`, SILENT (the mis-released amount lands in avail so the global invariant still balances; L2<L1 → `used` goes NEGATIVE / avail inflated → over-trade risk). **Fix (NO schema change):** `core/state_store.py::get_entry_commit_margin(trade_id)` returns `(committed_M1, committed_qty)` summed over the trade's COMMIT `fm_ledger` row(s) (`trade_id`→`trades.reservation_id`→ledger; committed_qty parsed from the COMMIT `reason` "fill: qty=<n>…"); `capital/fund_manager.py::release_used` gains `trade_id` and frees `_committed_release_margin() = committed_M1 × exit_qty/committed_qty` (denominator = ORIGINAL committed_qty → sequential partials sum to M1; a full close frees exactly M1 regardless of leverage). Falls back to the legacy recompute ONLY when the committed row is unresolvable (no trade_id / missing COMMIT / unparseable) — logged, never silent; release never raises. **PnL booking UNCHANGED** (gross_pnl still (exit−entry)×exit_qty). The 4 callers thread `trade_id`: `order_placer:2343` (normal exit), `cnc_gtt_monitor:447` (delivery), reconciler `CHECK1:1102`, `CHECK4/M-O2:1893`. **Composition:** M-O2 partial inherits the proportional reverse; M-C1's rehydrate seeds `used=M1` and release reverses that SAME M1 → used→0, the `_total`-only seed (total==broker.net) untouched (no re-double-count); terminal-guard unaffected (release writes no `trades.status`); does NOT touch M-C3 (per-bucket invariant, separate next). **Parity:** one mode-agnostic path (release_used/rehydrate/leverage_map identical in paper+live). **Tests** `tests/unit/test_mc7_release_committed_margin.py` (REAL StateStore + two FundManagers on one store to simulate a restart + real fm_ledger): T1 GREEN (leverage-edit restart → used→0) + **T1 RED** (`trade_id=None` recompute fallback reproduces the exact `M1−M2` drift — self-contained RED→GREEN), T2 same-leverage no-regression, T3 partial proportional across a leverage change, T4 multi-bucket, T5 M-C1 total==broker.net preserved. `_MockFundManager` in `test_order_placer` updated for the `trade_id` kwarg. **469 pass** across fund_manager/mc1/mo2/reconciler/order_placer/cnc_gtt/terminal-guard/a1e1-recovery/daily-loss/double-release/emergency-exit; 0 regressions. **M-C3 (per-bucket invariant gap) remains the separate next build.** memory `mc3_mc7_capital_invariant_recompute_09jul`.
- 2026-07-09 — Claude Code (Opus 4.8) — **Wave-6 M-O2 FIXED (branch `fix-mo2-check4-partial-capital-09jul` `0b48e87`, off main `9815786`, LOCAL/UNPUSHED, push OFF-MARKET) — reconciler CHECK4 partial external close now releases capital + books PnL for the closed portion.** Root cause (audit `full_system_audit_04july2026.md` M-O2, confirmed STILL LIVE at HEAD): `orders/order_reconciler.py::_check4_partial_close` (routed when `0<broker_qty<local_qty`) updated `qty_filled` + cancelled stale SL/TGT + alerted but NEVER called `fm.release_used` → the closed portion's margin stayed locked in `used` all session (reservable understated → sizing starved) and its realized PnL never entered `daily_realized`, leaving the FM7 post-close daily-loss gate blind to a partial stop-out. CHECK1 (full close) does the accounting; CHECK4 skipped it (the asymmetry). Only `dd4f3f4`/`959badf` touched the reconciler post-audit (neither CHECK4); no M-O2 test existed. **Fix (mirror CHECK1 proportionally, NO schema change):** `closed_qty = local_qty − broker_qty`; `_resolve_exit_price` (broker trades()→LTP→entry proxy) then `release_used(exit_qty=closed_qty, costs=0.0)` → frees proportional margin + books the closed portion's PnL into `fm_ledger`/`daily_realized` (`get_daily_realized_net_pnl` sums `fm_ledger.pnl_delta`, state_store:2368 → the FM7 gate now sees it). **Exactly-once via a qty_filled CAS** (`SET qty_filled=broker_qty WHERE trade_id=? AND qty_filled=local_qty`, release iff `rowcount==1`; CAS-then-release so a post-claim release failure UNDER-releases conservatively, never double-releases) → a re-detected same-delta cycle / a crash between release and the qty write cannot double-release; sequential DIFFERENT partials each match their own current qty. **Edge:** `broker_qty==0` (a present-but-zero position that CHECK1's `bp is None` misses) routes to `_check1_manual_close` → finalizes + releases the remainder, not left OPEN at qty 0. **Trade STAYS OPEN/PARTIAL** — no `mark_trade_manually_closed`/`close_trade` CAS → no terminal write → terminal-state guard silent; no terminal `net_pnl` on a partial (finalizes at full close; `fm_ledger.pnl_delta` is the daily-loss/reservable source of truth). **DELIBERATE DEVIATION from the build note:** `PositionClosed` is NOT published on a partial — its 3 subscribers treat it as a FULL close (paper `zerodha_adapter._on_external_position_closed` does `_paper_positions.pop(symbol)` = strands the remainder + cascades a double-release; `slippage_recorder` writes a completed-trade roll-up on a still-OPEN trade; `shadow_tracker` opens an inning); observability kept via the enriched CHECK4 log; `release_used` alone satisfies M-O2. **Parity:** one shared mode-agnostic path; exit price = broker trades() live / entry proxy paper — identical to CHECK1's existing full-close behavior (pre-existing, not a new divergence). **Tests** `tests/unit/test_mo2_check4_partial_capital.py` (REAL StateStore+FundManager+reconciler): T1 partial release+PnL+trade-OPEN · T2 LONG/SHORT PnL sign · T3 partial loss reaches the FM7 daily-loss gate (breach fires) · T4 idempotent (no double-release, CAS) · T5 broker_qty==0 finalizes via CHECK1 · T6 sequential partials each release once. **RED→GREEN proven** (git-stash the fix → all 7 fail). Existing `test_order_reconciler::test_check4_partial_close` updated for the new release_used call. **256 pass** across reconciler/fund_manager/daily-loss/terminal-guard/a1e1-recovery-matrix/double-release + emergency-exit-chain + M-O1; 0 regressions. Residual (flagged): trades.net_pnl not written on a partial (reporting nuance). Did NOT start M-C3/M-C7. memory `mo2_check4_partial_close_capital_09jul`.
- 2026-07-08 — Claude Code (Opus 4.8) — **P1 RE-INTEGRATED onto the fail-fast main — branch `wave4-p1-v3` @ `a62ece5` (LOCAL/UNPUSHED, NOT merged/deployed; SHADOW default `eod_reconcile.authoritative:false`). v42 table + terminal-guard trigger + schema-version fail-fast ALL coexist.** Re-cherry-picked P1 (orig `3ef7c35`/`wave4-p1-v2`, based on the pre-PUSH-1 `271d24f`) onto current main `7338e08` (PUSH-1 infra+A-3 + fail-fast `0b816dc`). Cherry-pick CLEAN; `core/state_store.py` auto-merged — the fail-fast guard in `_initialize_schema` and P1's reconcile methods / EXPECTED bump are disjoint hunks. **Coexistence verified:** `schema.sql` has BOTH `trg_trades_terminal_status_guard` (v41, Wave-5) AND `eod_broker_reconciliation` (v42, `:1487`), `schema_version=42`; the fail-fast guard is INTACT with `EXPECTED_SCHEMA_VERSION=42` → v41→v42 migrates up, v42==v42 boots, v43+ raises; `upsert/get_eod_broker_reconciliation` present; config (`eod_reconcile:` + `EodReconcileConfig`) & cron additive; `main.py` (M-C1 seed) untouched by P1. **Validation:** full suite **11 fail / 4373 pass / 15 skip** — the 11 are the IDENTICAL pre-existing PC-env set (fix061×4 / fix181 / test_main×4 / interactive_startup / phase17); +18 pass vs the `9bcc1eb` baseline = P1's 15 + the 3 fail-fast tests → **0 regression**. **v42 migration × fail-fast (throwaway COPY DBs):** FORWARD a genuine v41 DB → v42 code migrates (table created, trigger coexists, integrity ok, idempotent), guard silent; ROLLBACK a real v42 DB + v41-EXPECTED code RAISES + no downgrade (config-flag-only rollback confirmed). Schema-version asserts consistent at 42; state_store signatures re-verified vs current main. **Parity:** `eod_broker_reconcile` paper SELF_CONSISTENCY intact (P1 tests green). Web Claude issues the coordinated PUSH-2 (merge + SHADOW deploy). memory `p1_reintegrated_failfast_main_08jul` (supersedes `p1_reintegrated_271d24f_08jul`).
- 2026-07-08 — Claude Code (Opus 4.8) — **Schema-version FAIL-FAST BUILT (commit `0b816dc`, main, LOCAL/UNPUSHED — ships before/with the P1 v42 deploy). Fresh-audit §B.1; behaviour-only, stays v41.** Root cause: `core/state_store.py::_initialize_schema` migrate gate is `old_version < EXPECTED` and the following `executescript()` is UNCONDITIONAL (re-creates the terminal-guard trigger + re-stamps `schema_version` every boot), so a DB stamped NEWER than the running code (a v42 P1 DB opened by v41 code after a code-revert) SKIPPED migration, then executescript **silently stamped the version DOWN to 41** and booted clean — a silent downgrade that stranded the newer schema's tables and passed the `:371` check (stored version already rewritten). **Fix:** immediately after `old_version` is read and BEFORE executescript, `if old_version is not None and old_version > EXPECTED_SCHEMA_VERSION: raise SchemaVersionMismatch(...)` — stored version left INTACT for diagnosis. The two legal paths preserved EXACTLY (`< EXPECTED` migrates up, `== EXPECTED` boots); trigger / migrations / version-bump untouched; brand-new DB (None) unaffected. **Consequence: P1/v42 rollback must be the `eod_reconcile.authoritative:false` config flag, NOT a code-revert** (corrects the earlier "old code refuses a v42 DB" assumption — it did NOT; it silently downgraded). **Parity:** mode-agnostic single path (both modes open the DB through it). **Tests** `tests/unit/test_schema_version_failfast.py` (3, schema-backed — real StateStore + real schema.sql, no mocks): T1 newer→RAISES + version NOT mutated (RED→GREEN proven: RED = DID-NOT-RAISE + downgraded), T2 older→migrates-up to EXPECTED, T3 equal/fresh→boots + terminal-guard trigger present (brick check). 166 green (new + migrations + state_store + terminal-guard), 0 regressions. NOT pushed (coordinated with P1 = PUSH-2). memory `schema_version_failfast_08jul`.
- 2026-07-08 — Claude Code (Opus 4.8) — **PUSH-1 PC=VM sync DEPLOYED + VERIFIED (main `271d24f`→`dd4f3f4`→`9bcc1eb`, pushed + restarted + spot-checked live, off-market ~19:58 IST).** Consolidated the two 08-Jul LOCAL builds onto main and deployed: **(dd4f3f4) infra low-sev batch C-3/C-4/F-1/E-4** — C-3 `scripts/healthcheck_server.py` default bind `0.0.0.0`→**`127.0.0.1`** (+ `/health` scrub: `account_id`→`account_present` bool, raw exceptions genericised); C-4 `scripts/zerodha_login.py::save_token` token file now `os.open(0o600)`+chmod 0600 + session dir 0700; F-1 `main.py:1761` disk floor now reads `app_config.system.logging.min_free_disk_gb` (was silently the 1.0 fallback — nested `LoggingConfig` under `extra="forbid"` SystemConfig); E-4 daemon liveness on `/health` — `is_alive()` on OrderMonitor/OrderReconciler/EodSquareoff (3 poll threads GATE health→503) + `live_feed` reported NON-gating. **(9bcc1eb) A-3 entry-path kill TOCTOU** — `orders/order_placer.py:1229` second cheap in-memory `is_active("entry")` re-check at the top of the 429-retry loop before each `_engine.execute()` (AFTER the pre-submit network I/O, INSIDE loop, raised OUTSIDE try), reject marker `"kill_switch_active_last_mile_presubmit"`, reuses OP-LM1's fail-path (PENDING→FAILED+release), OP-LM1 early-out kept @944; shrinks the SOFT_KILL leak window from seconds to µs; additive-only, no schema. **Deploy (resumed after an unplanned PC power loss — Part B suite re-run clean):** Part B regression gate on `9bcc1eb` = 11F/4355P/15S == baseline's 11 pre-existing PC-env fails, **0 new** (+16P/+2S = the 18 tests the 2 commits added). `git push origin main` `271d24f..9bcc1eb` (post-receive crontab AUTO-INSTALLED = idempotent no-op); `sudo systemctl restart trading-system.service` → fresh `ExecMainStartTimestamp 19:58:57 IST`, clean self-exit 0 at the service-window guard (`Result=success`/`ExecMainStatus=0`/`NRestarts=0`, 0 ERROR/CRITICAL) — full component/schema init runs at the next in-window 08:15 boot. **schema_version STILL 41** (live `schema_meta`=41; P1 correctly absent — `wave4-p1-v2` untouched on its own branch). All 4 deployed-code spot-checks confirmed (C-3 bind `:267` / E-4 `daemon_liveness_provider` `:66,:112` + is_alive `eod_squareoff:280`/`order_reconciler:397`/`order_monitor:519` / F-1 `main.py:1761` / A-3 `order_placer.py:1229`+`:1231`). **C-4 one-time chmod applied** to the live token: `data_store/session/zerodha_token.json` 0664→0600, session dir 0775→0700. PC=VM at git level. memory `push1_pc_vm_sync_08jul` (+ `infra_lowsev_c3c4f1e4_08jul` / `a3_kill_recheck_investigation_08jul`).
- 2026-07-07 — Claude Code (Opus 4.8) — **Wave-5 M-C1 BUILT (commit `0fba665`, main, UNPUSHED, NOT deployed) — LIVE rehydrate seed now excludes today's realized PnL → 🏁 WAVE-5 COMPLETE (H-7 ✓ · FIX-067/M-S1 ✓ · terminal-guard+D-1 ✓ · M-C1 ✓).** Audit M-C1 (`docs/audit/full_system_audit_04july2026.md:113`). **Premise was INVERTED** (the build instruction said "paper rehydrate diverges → mirror live"; investigation STOP-reported that the audit+code say the OPPOSITE) → Web Claude confirmed the corrected direction, built it. **Root cause:** on a LIVE mid-day warm restart the broker's net margin (the live startup seed, `main.py:2007`) ALREADY reflects today's realized PnL, but `fund_manager` rehydrate Phase 2 re-applies that same PnL (fm_ledger `RELEASE_USED` carryover) → the live capital seed **double-counts** it (inflated reservable capital until the next `sync_from_broker`, which then records a phantom `−today_pnl` drift = spurious alert). Invisible at 08:15 cold boots (0 closed trades). **PAPER was already correct** (its static `paper_capital` seed excludes today's PnL) → this fixes the buggy side to match the correct side. **Fix (paper UNTOUCHED):** `capital/fund_manager.py` new `today_realized_pnl_carryover(start=None)` → Σ of today's `RELEASE_USED` `pnl_delta` via a NEW shared `_today_release_used_pnl_rows(start)` helper that **rehydrate Phase 2 now ALSO uses** (was an inline query) → the seed subtraction and the Phase-2 re-addition are GUARANTEED the same rows + sign (can't drift); read-only, callable before `initialize`. `main.py:2007` (LIVE branch only): `_startup_capital = broker_adapter.get_margins().net − fund_manager.today_realized_pnl_carryover()`. Paper seed (`:2005`) + Phase 2 + the FIX-156 paper adapter re-sync (`:2049 if is_paper`) all UNCHANGED. **Identity:** `broker.net − Σ = (base + today_pnl) − today_pnl = base` → the fixed live seed reduces EXACTLY to paper's static PnL-excluded base (any sign/count) → `seed + Phase 2 == broker.net` by construction (no double-count, no phantom sync drift); cold boot Σ=0 → seed = broker.net (unchanged). **Parity: RESTORED** by fixing the buggy LIVE side to match the already-correct PAPER side. **Tests** `tests/unit/test_mc1_live_seed_rehydrate.py` (7; REAL StateStore+FundManager+fm_ledger): T1 core RED (`BASE+2×pnl`)→GREEN (`broker.net`) + T1a loss-day sign + T1b multi-trade + T1c multi-bucket FULL-SNAPSHOT parity to the paper reconstruction (per-bucket attribution intact) · T2 paper untouched (both ways) · T3 cold-boot no-op · T4 sync-drift RED (`−pnl` phantom)→GREEN (zero). **239 green** across capital/rehydrate/sync (`test_fund_manager` incl. the refactored Phase 2) + cold-start + invariant + Wave-1 emergency-exit + Group-A HARD_KILL + H-12; **0 regressions**. No schema/path/cron/service change. **🏁 Wave-5 CLOSED — all 13 Audit-A HIGHs addressed (+ M-C1 MEDIUM); next per the approved scope = Wave-5 closure + fresh-audit recommendation.** memory `mc1_rehydrate_investigation_07jul` (→BUILT).
- 2026-07-07 — Claude Code (Opus 4.8) — **Wave-5 terminal-state write guard + D-1 close_trade CAS BUILT (local, UNPUSHED, NOT deployed) — Audit-B Phase-4 rec #5. Wave-5 status: H-7 ✓ · FIX-067/M-S1 ✓ · terminal-state guard + D-1 ✓; PENDING = M-C1 (H-6 remains, delivery-gated).** Root cause: `trades.status` CHECK (schema.sql:158-161) validates the value SET, not TRANSITIONS → raw UPDATEs could reopen a terminal row (CLOSED→OPEN/EXITING/…) after capital was released; the invariant was convention-only (audit §4.4/§4.8 rec #5). **HYBRID fix** (accepted design `docs/design/terminal_state_write_guard_design_07jul2026.md`): **(1) data-layer keystone** — `core/schema.sql` `CREATE TRIGGER IF NOT EXISTS trg_trades_terminal_status_guard BEFORE UPDATE OF status ON trades WHEN OLD.status IN ('CLOSED','CLOSED_MANUAL','FAILED','CANCELLED') OR GLOB 'REJECTED*' … CASE WHEN NEW.status<>OLD.status THEN RAISE(ABORT)` — forbids CHANGING status once terminal, catching EVERY writer incl. future raw SQL. **Terminal-LOCK, NOT a full matrix** → every legal edge (H-2 EXITING→CLOSED, the recovery flows, EXITING→OPEN revert) originates NON-terminal and is untouched; `UPDATE OF status` + `WHEN NEW<>OLD` let the exit-financial backfills (no `status` in SET — `record_manual_close_financials`/`record_gtt_close_financials`) and idempotent same-status writes pass. **BEHAVIOUR-ONLY pure add — NO schema_version bump (stays v41):** `executescript(schema_sql)` re-applies it idempotently every boot (state_store.py:367) AFTER any migration rebuild → survives a future `trades` 12-step rebuild; NO `MIGRATION_TABLES` entry. **(2) close_trade CAS (=D-1):** `orders/order_manager.py::close_trade` replaced the non-atomic read-then-check-then-write pre-check (a TOCTOU that let two finalizers both release capital) with an atomic `UPDATE … WHERE trade_id=? AND status IN ('OPEN','PARTIAL','EXITING')` + `rowcount`: winner (1) closes+releases ONCE, loser (0) raises the SAME `ValueError` the caller already maps to "already closed → skip release" (order_placer.py:2249) — double-release closed, H-2 preserved, trigger never tripped on this path (WHERE excludes terminal). **(3) all 9 raw writers (U1-U9: update_trade_status/record_entry_fill/order_placer:3685/reconciler:1961/eod:830/structure_exit:354/kill_switch:962+1275) verified to already catch broad `Exception`** → the trigger's `sqlite3.IntegrityError` abort degrades gracefully (log+continue) at every site — NO except-broadening needed. RAISE(ABORT)→`sqlite3.IntegrityError` (not ValueError). **Parity:** single mode-agnostic path (all writes → one `trades` table via order_placer→order_manager→state_store; `mode` is a column tag, paper differs only at the adapter fill-sim) → guard covers PAPER+LIVE by construction (tests parametrize both). **Tests** `tests/unit/test_terminal_state_write_guard.py` (47; REAL StateStore+trigger+OrderManager, mode-parametrized): **A** illegal-rejected matrix (terminal×writers, IntegrityError) + a headline **RED→GREEN** that DROPs the trigger to reproduce the exact reopen vulnerability + a raw-module-SQL writer + close_trade-is-ValueError-not-trigger; **B** every legal edge ALLOWED (★H-2 EXITING→CLOSED, EXITING→OPEN revert, EXITING→CLOSED_MANUAL, entry+recovery edges, ★financial-backfill-on-a-terminal-row, idempotent same-status); **C** D-1 race — sequential single-release + a REAL 2-thread barrier race (exactly one winner, BEGIN IMMEDIATE serialises). 3 `test_order_manager.py` assertions updated to the new CAS message (behaviour identical: reject + no overwrite). **626 green across new+affected+adjacent** (order_manager/state_store 90 · order_placer/reconciler/kill_switch/eod/structure_exit/H-2/H-7/FIX-067 362 · a1e1/daily_review/cnc_gtt/ramcoind 127 · new 47); **0 regressions**. No PATHS/cron/service change. memory `terminal_state_write_guard_design_07jul` (→BUILT).
- 2026-07-07 — Claude Code (Opus 4.8) — **Wave-5 FIX-067/M-S1 FIXED (commit `3f4587f`, main, UNPUSHED) — momentum fresh-LTP now re-anchors the FULL placement basis (entry+SL+TGT+qty+reservation); + the D1 plumbing root + the retest twin. Wave-5 status: H-7 ✓ · FIX-067/M-S1 ✓; PENDING = terminal-state guard · M-C1 (H-6 delivery-gated).** Completes the 16-May partial `d37ad62` (entry-only fresh quote). **Two coupled defects on the momentum path (`pullback_wait_enabled=false`):** **D1 (plumbing, the real root — the fresh branch was INERT):** `signal_processor._process_one` called `self._quote_fn(symbol)` with a BARE STRING and read `.get("last_price")`, but `broker_adapter.get_quote(symbols: list[str]) -> dict[str, Quote]` (frozen dataclass, attr `.last_price`, keyed by bare symbol, `NSE:` stripped) — the string was iterated per-char and `Quote` has no `.get` → None → SILENT fallback to the stale webhook price in BOTH live+paper (FIX-067 undelivered since 16-May). Same twin latent in `_retest_entry_estimate:2018` (retest MARKET sizing silently used the reclaim-level fallback, never the live LTP). Direct precedent: order_placer FIX-180. **D2 (logic, audit M-S1):** even with a fresh LTP, `fresh_entry_price, _ = _derive_prices(...)` DISCARDED the recomputed SL → SL/TGT/sizing/reservation stayed anchored to the STALE trigger price (SL distance drifts out of bounds, R:R skews, reserved capital ≠ order value, adverse move can put a LONG's stale SL ABOVE the fresh entry). **Why no live risk-distortion until now:** D1 made the branch inert → entry AND sl both stale-derived → internally consistent; the M-S1 divergence was LATENT (armed only if D1 were fixed alone). **Fix (Q4=COMPLETE re-anchor per Rama):** fetch the live LTP ONCE, AFTER the retest diverter (its routing zone-check keeps its trigger basis — unchanged) and BEFORE sizing, then re-anchor the ENTIRE basis — `entry_price, sl_price = _derive_prices(live_ltp,…)` and (downstream, because they derive from these) `_derive_target`, `sizer.calculate`, and `fm.reserve` all consume the fresh entry/SL. **H-7 preserved:** the per-strategy-cap + `approve` + `reserve` section inside `portfolio_lock` is UNTOUCHED and stays atomic (count-based, orthogonal to price; the network fetch stays OUTSIDE the lock) — build-gate cleared, no STOP. **No new abort** (Q3): the pre-existing FIX-128 slippage guard owns `delta>SL_budget` and now sizes its tolerance off the CORRECT fresh SL (`_slippage_decision(_scfg, signal_trigger_price, sl_price,…)`@order_placer:1012); `trigger_price` preserved for it. Retest twin fixed to the same LIST convention (Q2: D1-only — it returns an estimate, derives no SL). **Parity:** single shared path; `_make_paper_quote_provider` returns the same `dict[str, Quote]` keyed by original symbol with a real Kite LTP → identical re-anchor both modes. **Tests** `tests/unit/test_fix067_ms1_fresh_anchor.py` (REAL `_process_one`+`_derive_prices`/`_derive_target`; only quote/placer/sizer/fm are RECORDING doubles): T1 D1 list-convention exercised · T2 fresh SL+TGT (M-S1 core) · T3 sizing inputs + reservation price on the fresh anchor · T4 H-7 portfolio_lock section preserved · T5 retest twin returns the live LTP · T6 paper-provider parity · +pullback-skip guard. RED→GREEN proven by running against HEAD `2023948` (6 fail, only the invariant pullback-guard passes) then the fix (7 pass). `test_sr_v2_continue.py` 3 stubs moved off the old `_quote_fn` shape → `dict[str, Quote]` (D1 fallout). Also removed the 3rd `fresh_entry_price` ref in `_sr_observe` (would NameError→PLACEMENT_FAILED). **320 green across affected suites (signal_processor 77 / order_placer / slippage×3 / H-7 / SL-gap / retest / new M-S1 7) + 63 adjacent (entry_gate/strategy-config/windows); 0 regressions.** Residual (NOTE, Q4-bounded): reservation/qty use fresh-anchored inputs so reserved==order value; the only residual is the FIX-128 tolerance ref shifting by ≤ the move (bounded, flat-% + hard-ceiling backstopped). No schema/path/cron/service change. memory `fix067_ms1_investigation_07jul` (→BUILT).
- 2026-07-07 — Claude Code (Opus 4.8) — **Wave-5 H-7 FIXED (commit `2023948`, main, UNPUSHED) — the per-strategy position cap is now ATOMIC inside `portfolio_lock` + deduped 3->1. Last Audit-A HIGH in scope.** Root cause (audit H-7): the per-strategy concurrent-position cap was checked OUTSIDE `fm.portfolio_lock`, counted only DB OPEN/PARTIAL (no in-flight/reservation comp), and was triplicated across `_process_one`/`continue_from_gate`/`continue_from_retest` → a Chartink burst (1 webhook → N symbols → up to worker_count workers on ONE strategy) let all read the same stale count and pass, blowing a cap of N. **Fix (mirrors the global FIX-185 OPEN_POSITIONS):** ONE `signal_processor._enforce_strategy_position_cap(strategy_name, obj)` called INSIDE portfolio_lock immediately before `reserve()` in all 3 paths (3 outside-lock copies removed); authoritative count = strategy OPEN/PARTIAL (DB) + live fm reservations, floored by DB active (incl. PENDING_FILL), +1 candidate, reject if `>cap`. `capital/fund_manager.py`: `_Reservation` carries `strategy`; `reserve()`/`_apply_reserve` thread it; new `count_live_reservations_for_strategy()`; read under the existing RLock that `portfolio_lock` exposes → check+reserve is ONE atomic critical section, no nested/second lock. `reserve()` tagged `strategy=strategy_name` at all 3 sites. **Parity:** single shared path, no paper-specific copy. **Tests** `tests/unit/test_h7_strategy_cap_toctou.py` (real StateStore+FundManager+lock/reserve): a 6-thread burst @ cap 2 — RED the reproduced old outside-lock pattern admits >cap, GREEN the new inside-lock method admits exactly cap; + no-deadlock + dedup(source) + reservation-counts + scoped-count; the lagging doubles (`_MockFundManager`/`_FM`/`_Store`) raised to the new interface; **252 green, 0 regressions**. Wave-5 status: H-7 ✓; PENDING (not this task) = terminal-state guard · FIX-067/M-S1 · M-C1; H-6 remains (delivery-gated). memory `h7_strategy_cap_toctou_07jul`.
- 2026-07-07 — Claude Code (Opus 4.8) — **T2 CNC-GTT proof script REPAIRED off main `eb75731` — branch `fix-t2-repair-07jul` (`ef442ab`, LOCAL/UNPUSHED, NO live run/deploy/push). 3 drifts + throwaway-DB store wiring; `--dry-run` clean PC+VM.** `scripts/t2_cnc_gtt_realtest.py` rebuilt fresh (supersedes the parked partial `b826ae0` = drift #1 only, discard). **Drifts:** #1 L71 `core.order_state_machine`→`broker.order_state_machine` (main.py:46); #2 L83 `RateLimiter(cfg.broker_limits.rate_limits)`→`RateLimiter(cfg.broker_limits)` (BrokerLimitsConfig has no `.rate_limits`; main.py:1640); #3 `CncGttPlacer` built without `store=` (→None→in-memory→no `gtt_state` row) now wired to a throwaway StateStore. **Isolation (never the live `trading_system.db`):** fresh `StateStore` at `data_store/t2_proof_<ts>/t2_proof.db` in its OWN subdir so the ATTACHed `analytics.db` sibling is isolated too (`db_connect.analytics_path_for`=parent/analytics.db would else hit the LIVE `data_store/analytics.db`); `_assert_isolated()` refuses any live-DB path. **FK-chain seed (spec gap found):** the 03-Jul spec's "one trades row" missed `trades.signal_id`'s NOT-NULL FK to `signals` (schema.sql:241) → seed `signals`→`trades` (trade_id='t2') or the trades INSERT itself FK-fails. `CncGttPlacer(store=store)`+`hydrate_from_store()`; `run_single_session` now verifies the durable `gtt_state` row (P2 criterion)+CANCELs on cleanup; `--dry-run` places a PAPER OCO GTT through the store-wired placer → proves a `gtt_state` row lands in the throwaway DB → verify → cleanup (paper `get_quote` fed a synthetic `quote_provider`). **Validation (no live run):** py_compile clean; import-smoke (`_build_live_adapter` paper=True builds — drifts #1/#2 gone); `--dry-run` EXIT=0 on PC + VM (throwaway `t2_proof.db`@v41 got the row, live `trading_system.db` gtt_state 0→0, analytics sibling isolated, no real order). Standalone operator script (never imported by the trader). **Next: supervised live run** (Rama, after reviewing the clean dry-run) → then commit-merge + COMPLETE T2. memory `t2_repair_built_07jul` (supersedes the same-day `t2_repair_verify_07jul` NOT-READY finding).
- 2026-07-07 — Claude Code (Opus 4.8) — **Wave-3 + config-55 RUNTIME-CONFIRMED at the 08:15 in-window session → Wave-3 (H-12/13/11/8/9) FULLY CLOSED; pre-s1b2 rollback anchor SHREDDED (no secret-bearing artifacts remain).** Deferred runtime confirmation done (the off-market 06-Jul 20:59 restart only proved IMPORT-clean, exiting at the `main.py:1488` market-window guard before DB-open). Today's token-watcher-started **08:15:13 IST in-window session got PAST the guard → full init**: `trading-system.service` active/running, `Result=success`/`ExecMainStatus=0`/`NRestarts=0`; config+strategies loaded; kill-switch **new-day auto-cleared** (prior SOFT_KILL `circuit_breaker_force_close_15:15` from 06-Jul → clean 07-Jul, Audit Issue #18); NO import/init traceback/halt. **Scrubbed DB opened clean at v41** (`schema_meta`=41 unchanged — P1 undeployed; `PRAGMA integrity_check`=ok; live scrub intact: `signals` 103,730 rows / 0 32-hex token runs in `webhook_payload`). **config-55 in effect** — `min_pass_score:55` on disk (`scoring_weights.yaml:22`), fresh 08:15 boot loaded it; runtime proof = the 06-Jul intraday transition (warm restart ~11:19 flipped 60→55: last `REJECTED_SCORE_≥55` at 11:19:14, then 12:00–15:00 = 0×≥55 rejects / only ≤54 → screener honors 55). **STEP 2 (on GO):** `shred -uz` the last secret-bearing artifact `/home/ubuntu/db-backups/pre-s1b2-20260706-1759.db` (277,549,056 B, Jul-6 17:59) + `-shm`/`-wal` → dir empty, CONFIRMED GONE; post-shred `/home/ubuntu` `.db` sweep = no `pre-s1b2`/`pre_deploy…trading_system.db` remains (only clean `*_analytics.db` kept). PC=VM `9709a46`. Next: Wave-4 eod_verify honest-fix track (awaiting Rama); P1 on the `wave4-p1` rebase track, undeployed. memory `wave3_config55_runtime_closure_07jul`.
- 2026-07-07 — Claude Code (Opus 4.8) — **Wave-4 P1 INTEGRATION VALIDATED — branch `wave4-p1` (LOCAL, UNPUSHED, NOT merged, NOT deployed, SHADOW). Wave-4 status: eod_verify honest-fix ✓ · P1 integration validated (undeployed) · reconcile_positions/reconcile_pnl separate.** Cherry-picked P1 `4817032` onto current main **`eb75731`** (runbook said `9709a46` but main had advanced by the benign eod_verify commit — no schema change, still v41; reconciled to base at current main so the integration includes eod_verify, its future consumer). Cherry-pick `c54f688`: config **auto-merged additively** (0 deletions — `system_config.yaml` +`eod_reconcile:` block, `config_loader.py` +`EodReconcileConfig`; `load_all()` loads both P1+main). **state_store signatures ALL verified** vs current main (46 commits of drift, no mismatch): `get_all_open_trades`/`get_pending_intraday_orders`/`get_today_closed_pnl`/`get_eod_verification_status`/`upsert_pnl_reconciliation` + P1's new `upsert_eod_broker_reconciliation`. **Schema v41→v42 migration validated on a fresh TEST DB** (never live): 43→44 tables (+`eod_broker_reconciliation` only, CREATE IF NOT EXISTS), integrity ok, idempotent, writer/reader roundtrip, no data loss; assertion sweep clean (no hardcoded-41 straggler). **199 tests green** (P1 15 + config_snapshotter/sr_detector/sr_v2/control_tower/migrations + state_store + cron_registry + eod_verify_honest). **Cron consistent** (`generate(registry)==committed` canonical, P1 `58 15 * * 1-5` present) → no sha regen. **PARITY:** `eod_broker_reconcile.py` byte-identical to source → paper SELF_CONSISTENCY unchanged, test passes. main untouched `eb75731`. Next (LATER): P1 SHADOW deploy after the 08:15 Wave-3 confirmation. memory `p1_wave4_integration_validated_07jul`.
- 2026-07-07 — Claude Code (Opus 4.8) — **Wave-4 eod_verify HONEST-FIX (commit `eb75731`, main, UNPUSHED) — closes Audit-B Phase-8 false-verify HIGH + M-SC1. Wave-4 status: eod_verify honest-fix ✓; P1 on rebase track (undeployed); reconcile_positions/reconcile_pnl separate.** Root cause: `scripts/eod_verify.py` P&L leg queried NON-EXISTENT columns `ABS(system_net_pnl - broker_net_pnl)` (real `pnl_reconciliation` cols = `broker_pnl`/`system_pnl`/`variance`, schema.sql:780) inside `except Exception: pass` → OperationalError swallowed → `pnl_variance=0.0` → **false VERIFIED**; plus `pnl_reconciliation` has 0 rows (no feeder) → even a working query defaulted 0.0 → P&L never checked (11/11 historical rows VERIFIED/var 0.0); and `main()` always `return 0` (M-SC1). **Fix (no schema change):** `_evaluate_pnl()` reads the REAL columns (error/missing→logs+`NOT_CHECKED`, never silent 0.0); honest `eod_verification.status` roll-up (TEXT) = **ISSUES_FOUND** (open/pending/P&L DIVERGENCE) | **PENDING** (live, clean, P&L NOT_CHECKED — no feeder) | **VERIFIED** (live matched OR paper N/A) — never a false VERIFIED; **exit 2 on ISSUES_FOUND** / 0 on VERIFIED+PENDING. **Parity:** mode-aware ONE path (`--mode`/`TRADING_MODE`, default live); PAPER→P&L N/A (never false FAIL/broker-VERIFIED). Did NOT wire eod_verify↔P1 (later, after P1 soaks). Tests `test_eod_verify_honest.py` (REAL StateStore/schema): live/no-feeder→PENDING, real-columns→Rs250 divergence→ISSUES_FOUND, paper N/A, exit 2/0, genuine VERIFIED preserved; RED proven vs HEAD (clean→VERIFIED, Rs250 divergence swallowed→VERIFIED, ISSUES exit 0); 2 fix137 facade tests updated. **176 green** (eod_verify+fix137+eod_squareoff+cron+daily review; 0 regressions). memory `eod_verify_honest_fix_07jul`.
- 2026-07-06 — Claude Code (Opus 4.8) — **Wave-4 P1 `eod_broker_reconcile` INVESTIGATION (READ-ONLY; no fix/deploy) — verdict NEEDS-WORK (design GO-quality, deploy mechanics need a rebase).** Located: SHADOW feeder `scripts/eod_broker_reconcile.py` (458 lines) on branch `fix-p1-eod-broker-reconcile-02jul` (`4817032`, 02-Jul), **ABSENT at main `9709a46`** (only gui-g2* inherited it). **Isolated** (standalone @15:58 cron, DETECT+VERIFY+ALERT-ONLY, never mutates trades/exit/capital). Broker positions/day-P&L/orders/margins vs local; REQUIRED-dim-unavailable→UNVERIFIED (no false VERIFIED); margin reliability-gated→NOT_CHECKED @15:58 (FIX-189). **Paper CORRECTLY handled** = SELF_CONSISTENCY (`mode=PAPER`, never false-FAIL, never broker-authoritative). **Schema: PURE-ADD `eod_broker_reconciliation` (table 47), bumps v41→v42 coupled with `state_store EXPECTED_SCHEMA_VERSION=42`** — forward-clean/idempotent on the current scrubbed v41 DB; version-guard = post-deploy rollback via config flag (`authoritative:false`) NOT code-revert (old EXPECTED=41 refuses a v42 DB). **Cron entry present in the TRACKED canonical crontab** (`58 15 * * 1-5`) → push auto-installs, no manual step. **15 unit tests** (pure verdict matrix). SHADOW default (`eod_reconcile.authoritative:false`) = log-only, deploy-safe. **NEEDS-WORK to deploy** (branch 46 commits behind main): rebase → resolve additive conflicts in `config/system_config.yaml` + `core/config_loader.py` (schema/state_store/cron merge clean) + regenerate canonical-cron sha + re-run suite sweeping ALL schema-version asserts 41→42. memory `p1_eod_broker_reconcile_wave4_investigation_06jul`.
- 2026-07-06 — Claude Code (Opus 4.8) — **Wave-3 + config-55 DEPLOYED & IMPORT-clean startup verified (PC=VM `9709a46`); scrubbed-DB-open + anchor removal DEFERRED to the 08:15 session.** Rama pushed the 8 commits; `checkout -f` ran. VERIFIED: local==origin==bare HEAD `9709a46` (clean); **`config/scoring_weights.yaml min_pass_score`=55 SURVIVED the deploy** (headline revert-risk closed — committed `8a3e0b7` carried it); all 5 Wave-3 fixes present on disk (H-12 paper signed qty / H-13 `_is_token_expiry`+latch-reset / H-11 `_TOKEN_PATH` / H-8 `strategy` gate / H-9 candle off-by-one + `exchange_timestamp` param removed). **Startup:** genuine restart 20:59:39 IST → `Result=success`/`ExecMainStatus=0`; all 5 modules IMPORTED cleanly (reached `main()` + printed the guard + exit 0 — an import error would traceback first); no import/init/DB error. **KEY CORRECTION** to the prior runbook's assumption: the FIX-189 service-window guard (`main.py:1488` `return 0`) runs BEFORE `_main_locked`→`load_all` (config, `:1519`) and the DB open, so an OFF-MARKET start does NOT open the scrubbed DB or parse the 55 config in a running context — both first occur at **tomorrow's 08:15 in-window session** (token-watcher start). **pre-s1b2 rollback anchor `db-backups/pre-s1b2-20260706-1759.db` HELD (NOT shredded)** — the "DB opens cleanly" precondition for removal isn't met off-market; irreversible delete deferred until 08:15 confirms the app opens the scrubbed DB. (Scrubbed DB was integrity-verified at scrub time — GATE 5 + independent re-verify — so risk is low; hold is defense-in-depth.) memory `s1b2_history_scrub_06jul` / `min_score_floor_55_06jul`.
- 2026-07-06 — Claude Code (Opus 4.8) — **Pre-Wave-3-deploy PC↔VM sync — 2 commits (main, UNPUSHED — Rama pushes). Clears all pending PC state so the deploy carries the right config + code with NO revert.** **`8a3e0b7` config(screening): min-score `min_pass_score` 60→55 COMMITTED** — it was applied VM-live 06-Jul but uncommitted, and the deploy hook's `git checkout -f main` would OVERWRITE the working tree with the committed value → without this the Wave-3 deploy would revert 55→60 and re-break entries. VERIFIED: VM live config = **55**, git-HEAD `58ff1e7` = **60** → this makes git agree (deploy-safe). **`87c6afa` docs sync** — PATHS.md + this SYSTEM_MAP changelog (Wave-3/S-1B.2/min-score notes) + 4 untracked `docs/audit/*.md` (audit_05jul, full_system_audit_04july, c2_chartink_ip_investigation_03jul, telegram_token_shadow_investigation_03jul) + `ops_dashboard/docs/G5_SIGNOFF_EVIDENCE.md` + **`.gitignore` adds `gui/`** (23 GUI design-spec `.txt`/108K, consumed by the deployed G5 redesign; Audit-B Phase-1 untracked-debt flag). Secret-scanned clean (no raw token/secret in any committed file; audit docs reference tokens redacted). PC now **ahead of origin by 7** (5 Wave-3 + 2 sync), working tree clean. **PC=VM at git level pending Rama `git push origin main`.** memory `min_score_floor_55_06jul` (now committed).
- 2026-07-06 — Claude Code (Opus 4.8) — **Wave-3 H-9 FIXED (commit `beb8278`, main, UNPUSHED) — candle off-by-one + DELETE dead late-tick landmine + synthetic-only guard. WAVE-3 COMPLETE (H-12✓ H-13✓ H-11✓ H-8✓ H-9✓).** One fix = one commit (`data/candle_store.py` + new `tests/unit/test_h9_candle_offbyone_landmine.py` + `tests/unit/test_candle_store.py` [2 dead discard tests removed]). Two defects on the candle data every strategy consumes: (a) `_close_candles` stamped `_last_closed_window = window_being_closed` (the window that STARTS now, e.g. 10:01) not the one that just CLOSED (10:00); (b) a FIX-049 `exchange_timestamp` late-tick discard that was DEAD (live dispatcher `main.py:2157-2159` passes `ts` but never `exchange_timestamp`) and a LANDMINE — with (a), wiring it would make every current-minute tick satisfy `tick_window<=last_closed` → discarded → store 100% synthetic (+ latent naive/aware TypeError). **DECISION=DELETE** (not wire): `exchange_timestamp` only reliable in KiteTicker `full` mode (`live_feed.py:289`); discard is 100% dead so removal = zero runtime effect + closes the landmine by construction; wiring risks dropping real ticks with no proven prod problem; clock-driven receive-time windowing correct since go-live. **Fix:** `_last_closed_window = window_being_closed - timedelta(interval)` (off-by-one); removed `exchange_timestamp` param+discard from `on_tick`+`_Accumulator.update` (real ticks can't be dropped as "late"); `ts` kept but documented INFORMATIONAL-ONLY (main.py UNCHANGED); added synthetic-only guard (WARN after 3/every-30 consecutive tick-less closes → starved feed is LOUD not silently synthetic). **Consumer scan:** live consumer = `smart_tgt_manager.get_candles` (OHLC); nothing reads `_last_closed_window` but the deleted discard → off-by-one had no runtime consumer. **Parity:** single CandleStore (`main.py:2131`), one tick path, no paper duplicate. **Tests** (REAL aggregator; now_ist patched only for the boundary assertion): off-by-one last_closed=10:00 not 10:01 (RED before), real ticks→real non-synthetic candle + all accepted, synthetic-guard WARNs after 3 + resets, consumer OHLC. 2 FIX-049 discard tests removed (tested removed behavior). RED proven by revert. **125 green** (H-9 + candle_store + smart_tgt_manager + secondary_screener; 0 regressions). Residual: `ts` unused-but-documented (future cleanup, ripples main.py/tests — left to minimize money-path blast radius). memory `h9_candle_offbyone_landmine_06jul`. **Mempalace restructured (durable):** MEMORY.md split → lean active index (7.5KB) + `memory/MEMORY_ARCHIVE_2026H1.md` (older/superseded pointers; no data loss, 208 links preserved) to stop recurring lossy compaction.
- 2026-07-06 — Claude Code (Opus 4.8) — **Wave-3 H-8 FIXED (commit `d2c19d9`, main, UNPUSHED) — ShadowTracker FIX-013 TGT recalc now reads the real `strategy` column (was dead `strategy_name`); closes the LAST active dead-column bug. Wave-3 status: H-12✓ · H-13✓ · H-11✓ · H-8✓ · H-9 PENDING.** One fix = one commit (`orders/shadow_tracker.py` + new `tests/unit/test_h8_shadow_tracker_strategy_column.py`). Root cause (dead-column class, sibling of H-1 + kill_switch:977): `shadow_tracker._on_position_closed:317` keyed the inning-1 TGT recalc on `trade["strategy_name"]`, but the trades column is `strategy` (`schema.sql:124`; row is `SELECT * FROM trades`). Guarded by `"strategy_name" in trade.keys()` it failed SILENTLY (always `""`, not OperationalError) → the RISK_REWARD recalc branch never ran → inning-1 `tgt_price` persisted as the theoretical DB target not the fill-recalc'd one (wrong inning analytics on entry slippage). Fix: `trade["strategy"]` (the same fn already reads `trade["strategy"]` at :402/:624; :601-603 selects `strategy`); no new except/fallback. Analytics-only (shadow/observer path); consumer contract unchanged beyond the branch now running. Parity: single shared instance (`main.py:2137`), tick+event driven, no paper duplicate. Test drives REAL `_on_position_closed`+`calc_tgt_price`+REAL StateStore/schema (reusing the shadow_tracker harness): RR strategy recalcs inning-1 tgt 2600→**2630**, FIXED_PCT control keeps 2600; RED proven by revert. **55 green** (H-8 + full shadow_tracker suite; 0 regressions). H-6's CNC dead-column stays delivery-gated (out of scope). No schema/path/cron/service change. memory `h8_shadow_tracker_strategy_column_06jul`.
- 2026-07-06 — Claude Code (Opus 4.8) — **Wave-3 H-11 FIXED (commit `f7282f0`, main, UNPUSHED) — `main._invalidate_token` now targets the REAL token path → FIX-062 auth-restart-loop guard re-armed. Wave-3 status: H-12✓ · H-13✓ · H-11✓ · H-8/H-9 PENDING.** One fix = one commit (`main.py` + new `tests/unit/test_h11_invalidate_token_path.py` + `tests/unit/test_fix062_token_invalidation.py` path-corrected). Root cause: `_invalidate_token()` used `Path("zerodha_token.json")` (process CWD) while the token lives/reads at **`data_store/session/zerodha_token.json`** everywhere (live-startup loader `is_token_valid`/`load_token` @ `main.py:1800/1806`, `_load` @ `:314/:399/:1637`, `scripts/auto_refresh_token.py`, PATHS.md). On a BrokerAuthError `_shutdown`→`_invalidate_token` logged "not found, skipping" and left the dead token → the guard was inert (service could loop on a revoked token). **Guard flow (armed):** reason contains `"BrokerAuthError"` → `_on_critical_failure:570` → `_mark_auth_failed` sets `_broker_auth_failed` → `_shutdown:1107` → `_invalidate_token` renames real token → next non-interactive live boot `is_token_valid`==False (`:1800`) → "Token missing or expired" → **`return 6`** fail-fast (systemd StartLimit stops the loop past that = unit config). **Fix:** module consts `_TOKEN_PATH=data_store/session/zerodha_token.json` + `_TOKEN_INVALID_PATH=.with_suffix(".invalid")`, `_invalidate_token` uses them (same atomic `os.rename`); loader/writers unchanged. No prior shared const (sites inline the literal) → new const unifies only `_invalidate_token`; loader sites still inline = residual duplication (PATHS.md authoritative). **Parity:** mode-agnostic (paper has no real token, never sets the flag) — single path, no duplicate. **Consumer scan:** writer (login/auto_refresh) + loader already use this path; invalidation renames only on auth-fail shutdown → no clash; `_broker_auth_failed` has no other consumer. **Tests** (REAL `_invalidate_token`/`_shutdown` + REAL `zerodha_login.is_token_valid`/`load_token` on a genuine temp-path token; only `_shutdown` collaborators mocked): T1 invalidate renames real token→loader can't load (RED before) · T2 auth-fail shutdown→`is_token_valid` False→next boot fails fast (RED before) · T3 valid token round-trips when flag unset; the pre-existing FIX-062 fixtures encoded the WRONG CWD path (audit "test mocks the broken thing" pattern) → corrected. RED proven by revert-to-HEAD ("not found, skipping"; real token survived). **58 green** (H-11 + FIX-062 + H-13 + FIX-128 + auth_recovery + clock_skew + preflight_security; 0 regressions). No schema/path/cron/service change (path is unchanged from the loader's — this makes `_invalidate_token` agree with it). memory `h11_invalidate_token_path_06jul`.
- 2026-07-06 — Claude Code (Opus 4.8) — **Wave-3 H-13 FIXED (commit `e965589`, main, UNPUSHED) — TokenMonitor expiry latch RESETS on a valid check + classifies transient-vs-expiry; restores a dead token-expiry safety net. Wave-3 status: H-12✓ · H-13✓ · H-11/H-8/H-9 PENDING.** One fix = one commit (`broker/token_monitor.py` + new `tests/unit/test_h13_token_monitor_relatch.py` ONLY). Root cause: `check_now()` (`token_monitor.py:130-141`) treated ANY exception from `profile_fn` (raw `_kite.profile`, `main.py:2453`) as expiry → CRITICAL + SOFT_KILL via `_handle_expiry`, which set `_expiry_fired=True` and **never cleared it** → (1) a transient network blip fired a FALSE expiry + soft_kill and LATCHED; (2) once latched a later GENUINE expiry no-op'd (`:152-153` early-return) → no alert/soft_kill → entries kept firing on a dead token until restart. **Fix:** (a) reset `_expiry_fired=False` on a SUCCESSFUL check (latch now once-per-EPISODE, re-arms); (b) new `_is_token_expiry(exc)` — fire ONLY on a genuine token-invalid signal (kiteconnect `TokenException` by type-name OR `auth_recovery.classify_broker_auth_error=="TOKEN_EXPIRED"`); transient (`NetworkException`/`DataException`/timeout→`AUTH_UNKNOWN`, or `IP_NOT_ALLOWLISTED`=token valid) logs a WARNING, no latch/soft_kill. Once-per-episode suppression preserved. **State scan:** `_expiry_fired` has NO external consumer (only `:76/152/154`); reset can't dup-alert (only resets when token confirmed valid) and masks no real expiry. **Parity:** single monitor, paper `check_now` no-op, one live instance — no paper duplicate. **Tests** (REAL check_now/_is_token_expiry/_handle_expiry/latch + classifier; only the profile_fn boundary simulated with real kiteconnect exceptions): classifier unit · T1 expiry alerts once (episode suppression) · **T2 re-arm→2nd alert (RED before)** · **T3 transient no-latch, later real expiry alerts (RED before)**; RED proven by revert-to-HEAD. **109 green** (H-13 + fix128 + auth_recovery + fix062 + kill_switch + preflight_security + exceptions; 0 regressions). **Residual:** a SUSTAINED non-token `AUTH_UNKNOWN` failure stays silent (no distinct degraded/transient alert in TokenMonitor — FUTURE item, not added to keep scope minimal). No schema/path/cron/service change. memory `h13_token_monitor_relatch_06jul`.
- 2026-07-06 — Claude Code (Opus 4.8) — **Wave-3 H-12 FIXED (commit `18b0a74`, main, UNPUSHED) — paper `get_positions` now returns SIGNED net qty → paper flatten-direction parity restored. Wave-3 status: H-12✓ · H-13/H-11/H-8/H-9 PENDING.** One fix = one commit (`broker/zerodha_adapter.py` + new `tests/unit/test_h12_paper_positions_signed.py` ONLY). Root cause: the paper branch (`zerodha_adapter.py:1077`) returned `qty=abs(info["qty"])` while live (`:1105-1115`) returns kite's signed `"net"` quantity; the paper book itself already tracks a signed net (`_synth_fill:2114-2136` `new_qty = old_qty ± qty`), so a paper SHORT netted <0 in the book but surfaced >0 through get_positions — `abs()` the sole sign-strip. **Fix:** `qty=info["qty"]` (return the book's signed net; live + consumers unchanged; `side` already sign-derived). **Consumer scan = 0 workarounds:** magnitude-only consumers (`abs()`/`!=0`/`==0` at kill_switch:874/1181, reconciler:813/1224/2239/2292, eod:1011/1379/1407/1533, structure_exit:631) are byte-identical; the SIGN-for-direction consumers (`position_helpers.broker_net_qty:38`→`determine_close_direction`, kill_switch HARD_KILL sweep `:1159`, `order_reconciler._flatten_broker_position:1739`, `eod_squareoff._place_marketable_limit_exit:1450`) were BROKEN in paper (short read +qty → SELL → **doubled the short**, the THELEELA oversell FIX-190 prevents) and are now CORRECTED. `cnc_gtt_monitor:355` filters `product!="CNC"` = no-op in paper (all-MIS). **Tests** drive REAL `place_order`→`_synth_fill`→book then assert REAL `get_positions`+`determine_close_direction` (no mocks in the assertion path): T1 long +100 · **T2 short −60 (RED→GREEN)** · T3 flat=no-row/net 0 · T4 partial 100−40=+60 · **T5 dcd paper short→("BUY",60)==live-signed stub (RED→GREEN)**; RED proven by one-line revert (T2+T5 → `('SELL',60)`), restored. **248 green** (H-12 + paper-adapter + Wave-1 `test_emergency_exit_chain` + Group-A `test_hard_kill_flatten_chain` + fix190/kill_switch/reconciler/eod/structure_exit/ramcoind consumers; 0 regressions). **Parity:** paper now matches live's signed convention (Wave-1/2 assumed it). **Payoff (future, not run today):** Wave-1 emergency-exit + Wave-2 HARD_KILL reverse-flatten can now be paper-DRILLED for direction. No schema/path/cron/service change. memory `h12_paper_positions_signed_06jul`.
- 2026-07-06 — Claude Code (Opus 4.8) — **S-1B.2 COMPLETE (off-market ~18:11 IST) — historical webhook-secret scrub + backup purge; GATES 1-7 ALL PASS. S-1B done.** Final at-rest cleanup of the leaked webhook secret in `signals.webhook_payload`. **Self-gate/G3:** service `inactive`, 17:58 IST (off-market), TZ IST → GO. **G4:** backup `/home/ubuntu/db-backups/pre-s1b2-20260706-1759.db` (277MB), integrity=ok, counts match live. **G5 LIVE scrub:** ran the already-proven `s1b2_migrate.py --commit` (regexes b+c verbatim from `58ff1e7`; part-a omitted — no raw secret needed) against live `data_store/trading_system.db` → matched/updated **95,431** rows, **token=0**, total **103,730 unchanged**, sample 300/300 JSON-valid + fields intact, 0 residual 32-hex, integrity=ok, committed + `wal_checkpoint(TRUNCATE)`; independent fresh-connection re-verify confirmed token=0 / 0 hex-runs / no `-wal` / audit fields (stocks/trigger_prices/triggered_at/scan_name/scan_url) intact. **G6:** seeded fresh SCRUBBED `data_store/backups/trading_system-2026-07-06-postscrub.db` (token=0), then `shred -uz` of **21** secret-bearing DBs — 7 `pre_*` main snapshots + 14 `trading_system-*.db` dailies (guarded by a per-file `token=` re-check) — **kept 16 clean `analytics*` backups** (no `signals` table); also shredded root `trading_system.db.pre-deploy-2026-07-03` (245M). **G7:** `shred -uz .env.pre-rotation-02jul.bak`; final sweep = text-grep repo+VM (0 raw `token=<hex32+>`; local PC repo 0) + whole-`/home/ubuntu` `.db` sweep — **which CAUGHT a recon-missed artifact** `/home/ubuntu/backups/pre_deploy_02jul_securitybatch/trading_system.db` (token=85,765, outside `data_store/`) → shredded (its clean `analytics.db`+crontab kept). **Remaining by design:** ONLY the GATE-4 rollback anchor `pre-s1b2-20260706-1759.db` holds (DEAD) secrets — retained as the corruption-recovery path; **recommend shred after the Mon 08:15 boot confirms the scrubbed DB is healthy.** Both historical secrets already DEAD (401 at GATE D). No path/cron/service/code change (pure at-rest data op; mode-agnostic DB). DID NOT start Wave-3/H-12 (runbook STOP). memory `s1b2_history_scrub_06jul`; PATHS.md DB-backups line updated.
- 2026-07-06 — Claude Code (Opus 4.8) — **CONFIG: min_pass_score 60→55 (all strategies) — LIVE edit + restart (Rama-requested, market hours).** `config/scoring_weights.yaml` `min_pass_score: 60→55` is the SINGLE authoritative score floor for all 16 strategies: every `config/strategies/*.yaml` has `min_score: 0`, so `screening/secondary_screener.py:251-256` computes `effective_min = strategy.min_score if >0 else score_result.min_pass_score` → all fall back to the global. No per-strategy override (the 26-Jun "YAMLs=60" note is stale), no paper/live fork, no hardcoded 60 in the screener. **Reason:** 0 trades today; 494/1,032 of today's signals (48%; 423 at exactly 59) were `REJECTED_SCORE` in the 55–59 band just under the old floor. **Applied VM-live only** (backup `/home/ubuntu/scoring_weights.yaml.bak-premin55-1106`, YAML-validated) + `systemctl restart` (WARM, clean, `run_all_startup_checks OK`, waitress :5000 up, config-hash change detected, kill-switch clear, ~12s webhook downtime; 0 open positions at restart). **VERIFIED live:** post-restart max `REJECTED_SCORE=54` (was 59), 55–59 band rejects=0, score-59 signals now `PASSED`; binding gates now entry-throttle(20s)/concentration/circuit/position-cap(5). Downstream: 1 OPEN (PICCADIL — ENTRY COMPLETE + SL + TGT live, protected) + several FAILED = `entry_cancelled_zero_fill` (reservations released; no naked/leak). **⚠️ VM edit EPHEMERAL — working tree is not a git repo (`checkout -f` on deploy); local repo `config/scoring_weights.yaml` pre-staged to 55 UNCOMMITTED → commit + push OFF-MARKET to persist, else the next deploy reverts to 60. Keep at 55 until Rama says otherwise.** memory `min_score_floor_55_06jul`.
- 2026-07-06 — Claude Code (Opus 4.8) — **S-1A CLOSED — GATE D PASS (read-only live verify on Monday's real first signals; NO changes, no rollback).** All three staged controls confirmed after the 08:15 natural start (service `active (running)`, bare-repo HEAD `58ff1e7`, `/health` ok, Fri SOFT_KILL auto-cleared new-day). **GATE B allowlist + GATE C rotation:** today's `webhook_audit` = **86× 200 + 309× 403, ZERO 401**, ALL from `23.106.53.213` — rows present from the Chartink IP prove the `/32` allowlist permits Chartink (a netfilter block = zero rows) and the 200s prove the NEW `WEBHOOK_SECRET` authenticates; **0× 401 ⇒ Chartink migrated off the old token** (passive proof, stronger than the active old-token curl which was correctly NOT run during market hours — any non-matching token → 401 at `webhook_receiver.py:431`). **The 403 window is NORMAL, not a token issue:** 403 has only two sources (`:442` kill-switch / `:457` outside-entry-window); kill-switch inactive (0 logged webhook rejections) ⇒ all 309 403s = "Outside entry window" (`trading_hours.entry_start: "10:00"` LAUNCH-PHASE gate); last 403 `09:59:15` → first 200 `10:00:16` straddles 10:00 exactly. **Operational rule: only 401 = webhook auth/token failure; 403 = kill-switch OR pre-entry-window, NOT the token.** **GATE A/S-1B.1 redact live:** today's 113 signals = **113 `<REDACTED>` / 0 `token=`**; the `webhook_url` echo (the leak vector) is redacted `<REDACTED>`; secret not at rest going-forward. **Processing healthy:** 113 ingested→parsed→pipelined to legit verdicts (`REJECTED_SIZING_CAPITAL` ×70 on the Rs 10k account, `REJECTED_SCORE_*` <60, `REJECTED_CIRCUIT_PROXIMITY` ×5); **0 ERROR / 0 Traceback** (the 1 CRITICAL = benign 08:15 startup kill-switch, auto-cleared). Ingress is mode-agnostic (service `--mode live`). **STOP/GO = GO; S-1A CLOSED.** Remaining (NOT started): **S-1B.2** (scrub 95,431 historical `signals.webhook_payload` rows + regenerate backups + delete the stale `.env.pre-rotation-02jul.bak` older secret) + **TLS** (deferred). memory `s1a_webhook_allowlist_rotation_05jul`.
- 2026-07-05 — Claude Code (Opus 4.8) — **S-1A (/32 webhook allowlist + token rotation) — GATE A GO (re-verified); GATE B next; NOTHING APPLIED yet.** Off-market runbook; read-only reads + authored exact mutating commands, all gated on Rama's approval. **GATE A = GO (re-verify):** the push landed — bare-repo HEAD = `58ff1e7` (S-1B.1), running `webhook_receiver.py` has `_sanitize_payload_for_storage` (grep 2) + persist wired at line 591; service clean-`inactive`/`Result=success`/exit 0 (market-closed, NOT halted — acceptable Sunday state); not restarted after push but the redact runs on next natural start. Rotation (GATE C) UNBLOCKED. (Initially FAILED: `58ff1e7` was unpushed, HEAD `b4aea6f`; Rama then pushed all unpushed main commits incl. H-10 `ee9f993`.) **GATE B = DONE (05-Jul, Rama applied):** webhook `:5000` restricted to `23.106.53.213/32` (Leaseweb-SG, sole Chartink source) — runtime + persisted at `/etc/iptables/rules.v4` L43 (`iptables -R INPUT 6 -s 23.106.53.213/32 … --dport 5000 -j ACCEPT` + `netfilter-persistent save`); #6 before catch-all REJECT #7; SSH :22 intact; snapshots `~/rules.v4.bak-05jul`+`~/iptables-runtime-05jul.txt` kept. **GATE C (token rotation) = STAGED for Monday (05-Jul):** `.env WEBHOOK_SECRET` rotated (OLD sha256 `b7a4…b9ca`→NEW `484c…21b43`, 64-hex; hashes only, raw never stored); redact still `58ff1e7`; NO Sunday restart (weekend no-token → new secret loads at Monday's natural start); Chartink URL updated by Rama (his URL-token hash must == the new). C.4 sweep: `.env` = sole `EnvironmentFile` (rotated); all code consumers read `os.environ.get("WEBHOOK_SECRET")` (auto-consistent); systemd single env-file, no drop-in shadow; no cron token; `.env.example` placeholder; NO raw token literal in repo/docs. **⚠️ `.env.pre-rotation-02jul.bak` (VM) holds an even-older dead secret (`1dedac…`) — not loaded, delete in S-1B.2.** **GATE D = Monday first-signal e2e (old token→401, new→200, payload `<REDACTED>`).** Sunday caveat: service likely won't stay active(running) (no weekend token → clean exit at market guard = EXPECTED, distinct from the HALT-guard exit 3/4). **FIREWALL FINDING (corrects the runbook's nft assumption):** box is **iptables-nft**, persisted `/etc/iptables/rules.v4` via **netfilter-persistent** (enabled+active); `nftables.service` DISABLED, `/etc/nftables.conf` UNUSED → modify with `iptables` + `netfilter-persistent save`, NOT nft. INPUT policy ACCEPT with catch-all `REJECT icmp-host-prohibited` at rule #7; the `:5000` blanket-accept is rule **#6** (rules.v4 line 29). **GATE B allowlist (authored):** single in-place replace `sudo iptables -R INPUT 6 -s 23.106.53.213/32 -p tcp -m tcp --dport 5000 -j ACCEPT` → `sudo netfilter-persistent save` (23.106.53.213 = Leaseweb-SG, the SOLE Chartink source across 38,046 POSTs, stable); rollback `iptables -R INPUT 6 -p tcp -m tcp --dport 5000 -j ACCEPT` / `iptables-restore < ~/iptables-runtime-05jul.txt`. Parity: mode-agnostic ingress (one WEBHOOK_SECRET, both modes). PATHS.md updated with the firewall-persist fact. **S-1 sequence: S-1B.1 redact✓ (committed, unpushed) → S-1A AUTHORED/blocked-at-GATE-A → S-1B.2 scrub (pending).** memory `s1a_webhook_allowlist_rotation_05jul`.
- 2026-07-05 — Claude Code (Opus 4.8) — **S-1B.1 FIXED (commit `58ff1e7`, main, UNPUSHED) — webhook secret redacted going-forward from `signals.webhook_payload`.** Root cause: `signals/webhook_receiver.py` persists the full raw body to `signals.webhook_payload`, and Chartink echoes the configured URL — incl. `?token=<SECRET>` — inside the body's `webhook_url` field → secret plaintext at rest (was 95,431/95,431 rows). **Fix (storage-only):** new `_sanitize_payload_for_storage()` computed ONCE before the per-symbol loop and passed as `webhook_payload` (was inline `raw_body.decode(...)`); belt-and-suspenders on the STORED string only — (a) replace literal `self._secret` anywhere, (b) regex `token=[^&"'\s]+`→`token=<REDACTED>`, (c) blank the `webhook_url` JSON value; `import re` added. **Auth (`:410-432`) + parsing UNCHANGED** — the parsed `body` (stocks/trigger_prices/triggered_at/scan_name, extracted before the loop) is never passed to the sanitizer; malformed→400 / auth-fail→401 return before `_process_signal` (no other persist site); audit fields preserved. **Parity:** mode-agnostic single persist path, no paper duplicate. **Tests** (`tests/unit/test_webhook_receiver.py`, real StateStore + Flask test_client): core RED→GREEN (`_secret_never_persists_to_webhook_payload`, proven via revert) + parsing-unchanged + auth-unchanged (200/401) + secret-outside-webhook_url; full `test_webhook_receiver.py`+`test_c2_webhook_lockdown.py` = **69 pass** (pre-commit secret-scanner blocked a credential-named test const → renamed `_S1B1_FIXTURE` + `dummy`-placeholder value). **S-1 SEQUENCE:** S-1B.1 redact-going-forward ✓ → **S-1A** (nginx+TLS+Flask→127.0.0.1+close public :5000 + `/32` Leaseweb-SG allowlist `23.106.53.213` + token rotation) PENDING → **S-1B.2** (scrub 95,431 historical rows + backups) PENDING. Residual: token still travels in the URL (Chartink-inherent → S-1A); history/backups still hold the old secret until S-1B.2. memory `s1b1_webhook_payload_redaction_05jul`.
- 2026-07-05 — Claude Code (Opus 4.8) — **AUDIT-B PHASES 9 (OPERATIONS) + 10 (SECURITY) COMPLETED (read-only, NO fixes) — appended to `docs/audit/audit_05jul2026.md` which had ended mid-Phase-9. NO INDICATOR OF COMPROMISE (no escalation).** IOC sweep clean: single authorized SSH key = approved baseline `SHA256:uDRN8B…`; key-only auth (`passwordauthentication no`/`permitrootlogin no`); all Accepted logins from Indian-mobile ISP ranges (Jio/Airtel — the documented Rama rotating-IP pattern); all outbound = Tailscale/Oracle-email/OCI-metadata; root+system cron stock-only; no SUID in home; recent /etc changes benign; auditd active; copy-guard wrappers installed. **SSH-incident root cause:** the 18:00–08:00 "time-lock" (`config/security.yaml:82` copy_protection) is a **VM→PC copy-egress** control, explicitly **NOT an SSH gate** (`security.yaml:8` "nothing here blocks access — key-only SSH is the gate") — it structurally cannot block an SSH session; the "foreign IP at 04:18" was a new/rotating mobile IP admitted by the correct key-only gate; >2-sessions/new-IP are ALERT-only. **PHASE 9 OPEN ITEMS:** O-1 [MED] alert/security-watcher run as exit-and-restart poll loops (NRestarts 15,676/2,730, ExecMainStatus=0 clean — NOT crash; is-active→`activating` false-positive; Wave-0 confirm); O-2 [MED] post-receive hook = `checkout -f`+crontab-install ONLY (no `systemctl restart`, no `daemon-reload`, no venv/pip sync, no FIX-065 market-hours guard); O-3 [MED] live installed-vs-repo unit drift (`trading-watchman.service` lacks the antigravity PATH the repo added; `trading-system.service` comment-only — HALT guard `RestartPreventExitStatus=3 4` present in installed unit); O-4 [MED] cron observability split (17 `cron_marks/.done` vs 28 `cron_heartbeat` DB jobs; neither covers all 45 live jobs = the 22-vs-30 Officer delta); O-5 [HIGH, confirms Phase-3] no off-site backup (3.4G on same /dev/sda1; disk 16%); O-6 [LOW] no logrotate.d for app logs, single-name logs grow unbounded (`alert_watcher.log` 8MB) though a `log_cleanup` cron exists; O-7 [LOW] recurring unactioned Officer config warnings (`max_position_value 2500 > daily_loss 300`). **PHASE 10 OPEN ITEMS:** S-1 [MED-HIGH, most urgent NEW] webhook **:5000 open to 0.0.0.0** at nftables (`tcp dport 5000 … accept`, live) + `require_hmac:false` + `?token=` query auth — C-2 Option-B (reverse-proxy/HMAC/IP-allowlist) NOT deployed; S-3 [MED] public SSH :22 on 0.0.0.0 (contradicts "Tailscale-only") → recommend Tailscale-only SSH; S-4 [LOW] `rpcbind:111` binds 0.0.0.0+enabled but **nftables REJECTS it externally** (de-escalated — not internet-reachable); S-5 [LOW] sshd x11/tcp-forwarding on; S-6 [LOW] security-watcher false-positive sudo-alert on `/usr/sbin/sshd`; S-7 [INFO] no pip-audit installed (off-box follow-up). Roll-up: 1 MED-HIGH + 6 MED + 6 LOW + 1 HIGH-confirm; no parity issues. memory `audit_phase9_10_ops_security_05jul`.
- 2026-07-05 — Claude Code (Opus 4.8) — **Wave 2 / H-10 FIXED (commit `ee9f993`, main, UNPUSHED) — `place()` result=None backstop → WAVE 2 COMPLETE (Group A + Group B done).** The FIX-072 16388 (insufficient-margin) retry does `retried_16388 = True; continue`, borrowing an iteration of the **SHARED** 429 attempt budget. When the FIRST 16388 lands on the FINAL loop attempt, `continue` steps past `range()`'s last index → `execute()` never re-runs, no rejection handler fires, and `result` stays **None**. The post-loop `if not result.success` (order_placer.py:1347) then dereferenced None → **AttributeError (not a BrokerError)** → `_handle_placement_failure` never ran → trade stuck **PENDING** + fund-manager reservation **leaked** for the session (A-1/E-1 prepass mitigated only by accident). **Fix (a, mandatory):** a `if result is None:` backstop before the deref routes the None case through the SAME `_handle_placement_failure` every other placement error uses (release reservation + mark FAILED), then raises a proper **BrokerError**; no AttributeError path remains. 16388 == order REJECTED → no open position → failing without the best-effort fresh-margin retry is **safe** (no naked exposure). **Option (b) verdict: NO defect** — the shared 429/16388 attempt budget is fine; the 16388 retry is best-effort and its starvation on the final-attempt edge produces a safe, correct rejection once (a) is in place; **budget left unchanged, (b) NOT implemented** (per the no-speculative rule). **Parity:** `place()` is one shared method (paper vs live differ only in the adapter); no paper-specific duplicate; one fix both modes. **Tests** (`tests/unit/test_order_placer.py::TestBl19PlacerRateLimitRetry`, reusing the BL-19 mock-engine harness): `test_h10_final_attempt_16388_none_result_backstop` (the bug; RED = AttributeError @:1347 + stuck PENDING + leaked reservation → GREEN = BrokerError + FAILED + reservation released; verified by one revert cycle), `test_h10_nonfinal_16388_retry_still_succeeds` (legitimate retry unbroken), `test_h10_429_backoff_unaffected` (regression guard). Full `test_order_placer.py` + `test_emergency_exit_chain.py` (Wave-1) + `test_hard_kill_flatten_chain.py` (Group-A) = **128 pass, 0 cross-path regression**. Ref `docs/audit/full_system_audit_04july2026.md` (H-10); memory `h10_place_none_backstop_fixed_05jul`. **WAVE 2 COMPLETE** — Group A (P1/977✓ H-4✓ H-5✓ + e2e) + Group B (H-10✓). Deferred out: H-6 (CNC-orphan un-flattened while delivery-off — surfaced+escalated, not silent), H-12 (paper get_positions qty-sign — Wave 3), H-11 (`_invalidate_token` wrong path). Await Web Claude Wave-2 closure + fresh-audit rec.
- 2026-07-05 — Claude Code (Opus 4.8) — **Wave 2 Group-A VALIDATION: HARD_KILL flatten VALIDATED end-to-end (commit `af9e0a4`, tests-only, UNPUSHED). Result: PASS — 977 + H-4 + H-5 compose. GROUP-A CLOSED.** New `tests/integration/test_hard_kill_flatten_chain.py` drives the REAL `KillSwitch._exit_all_trades_indestructible` (first pass + orphan sweep + retry) and asserts SIX together on one flatten (a TRACKED long with resting SL+TGT whose first exit is an A-2 BrokerTimeout after a partial fill 100→40, + an untracked CNC orphan): (1) 977 — resting SL+TGT cancelled BEFORE the flatten; (2/4) H-4 — retry fires residual SELL 40 not stale SELL 100 (no oversell); (3/5) H-5 — CNC orphan swept as DELIVERY not INTRADAY (no naked MIS short); (6) delivery-off — the CNC exit is REFUSED (SLICE2.5-P1 lock) → surfaced → escalated via `_alert_exit_failed`, not swallowed, no MIS substitute. REAL: KillSwitch + the whole `_exit_all_trades_indestructible` + 977 cancel + determine_close_direction + StateStore(real schema) + EventBus; SIMULATED: the broker adapter (ordered place/cancel, signed positions, A-2 timeout on the tracked first exit, CNC DELIVERY refusal) + `_alert_exit_failed` spy; deterministic clock (mocked now_ist + time.sleep-advances + shrunk retry deadline). **Load-bearing:** verified by one revert cycle on the H-4 keystone (the integration test FAILED at assertion 2 with the re-derivation reverted, then restored via `git checkout`; commit stayed tests-only). H-12-independent (signed positions). Optional delivery-ENABLED happy-path DEFERRED. Full affected suite **58 pass**. Memory `groupa_hardkill_validated_05jul`. **GROUP-A CLOSED.** Out of Group A: **H-10 (Group B, next)**; H-12 blocks a PAPER DRILL of the HARD_KILL direction (Wave 3); the H-6 CNC-orphan-un-flattened-while-delivery-off interaction (surfaced+escalated, not silent).
- 2026-07-05 — Claude Code (Opus 4.8) — **Wave 2 / H-5 FIXED (commit `57dfbe2`, main, UNPUSHED) — HARD_KILL sweep now product-aware.** The HARD_KILL broker-position sweep (`_exit_all_trades_indestructible` FIX-181 orphan sweep) hardcoded `intent="INTRADAY"`. Kite nets per product → an orphan **CNC** position swept with an **MIS** exit does NOT offset it → the CNC stays AND a fresh **naked MIS short** is created. The first pass is product-aware (Bug C); the sweep reintroduced the hardcode. **Fix (mirror the first pass):** read `product` from the Position payload → map via `core.constants.PRODUCT_TO_INTENT` (MIS→INTRADAY, CNC/NRML→DELIVERY) → pass that intent to place_order + carry it in the failed_trades entry (so the H-4 retry re-fires under the right product); absent product → INTRADAY. **Parity:** single shared sweep, one fix both modes; H-12 (qty-sign) is a separate axis, untouched. **Delivery-gate interaction (REPORTED, NOT fixed — H-6):** post-fix a CNC orphan is swept as DELIVERY → the adapter's SLICE2.5-P1 lock (zerodha_adapter.py:535) REFUSES it while delivery_enabled=false → the sweep SURFACES it (CRITICAL + retry + `_alert_exit_failed` escalation), not swallowed; the first pass already behaves identically. So H-5 turns a SILENT naked MIS short into a SURFACED+escalated refused CNC exit (strictly better). **Test:** `tests/unit/test_h5_killswitch_sweep_product.py` (4, REAL sweep + recording adapter) — CNC→DELIVERY + NRML→DELIVERY RED→GREEN, MIS/absent→INTRADAY pass; kill_switch **56 pass**. Ref `docs/audit/full_system_audit_04july2026.md` (H-5); memory `h5_killswitch_sweep_product_fixed_05jul`. **Group-A: 977✓ H-4✓ H-5✓ — closes ONLY after the Group-A end-to-end VALIDATION (next).** H-10 = Group B; H-12 still blocks a paper drill of the HARD_KILL direction (Wave 3).
- 2026-07-05 — Claude Code (Opus 4.8) — **Wave 2 / H-4 FIXED (commit `5f116c0`, main, UNPUSHED) — HARD_KILL retry re-derives (side,qty) per attempt.** The HARD_KILL flatten's retry loop (`KillSwitch._exit_all_trades_indestructible`) re-fired the STALE full qty captured at first-pass time, gated only by a binary `_is_position_flat` check. If the first exit raised AFTER transmission (BrokerTimeoutError — the A-2 ambiguity class) and PARTIALLY filled, the position is non-flat → the retry re-fired the full qty → **oversell → new naked reverse**. The first pass is broker-net-aware (`determine_close_direction`); the retry was not. **Fix (mirror the first pass):** re-derive `(close_side, close_qty)` via `determine_close_direction` from a FRESH signed broker-position read on EVERY retry; fire exactly that residual. `(None,0)` flat SUBSUMES the binary `_is_position_flat` gate (now unused — left in place); broker-error fallback to the captured (exit_side, qty) preserves err-toward-flattening; no extra broker call. **Parity:** single shared method, one fix both modes. **H-12 clarification (NOT in scope):** determine_close_direction consumes broker_net_qty (SIGNED) — LIVE correct; PAPER strips the sign (H-12) so a SHORT's DIRECTION is wrong, but that's PRE-EXISTING (the first pass already uses it), H-12's fix (Wave 3), not introduced here; H-4 makes both passes consistent. **Test:** `tests/unit/test_h4_killswitch_retry_rederive.py` (3, drives the REAL `_exit_all_trades_indestructible` + a recording adapter injecting a correctly-SIGNED residual, time.sleep patched) — T1 LONG→SELL 40 not 100, T2 SHORT→BUY 40 (RED→GREEN), T3 flat→no fire; kill_switch **52 pass**. Ref `docs/audit/full_system_audit_04july2026.md` (H-4); memory `h4_killswitch_retry_rederive_fixed_05jul`. **Group-A STILL OPEN: 977✓ H-4✓ — H-5 (CNC sweep hardcodes intent=INTRADAY → naked MIS short) + the Group-A validation remain; H-5 next.** H-12 still blocks a paper drill of the HARD_KILL direction (Wave 3).
- 2026-07-05 — Claude Code (Opus 4.8) — **Wave 2 / P1 FIXED (commit `7c4106b`, main, UNPUSHED) — kill_switch:977 HARD_KILL dead-column (the H-1 twin).** `KillSwitch._cancel_trade_resting_exits` (capital/kill_switch.py:977, called by `_exit_all_trades_indestructible` :1091) SELECTed the non-existent column `orders.broker_order_id` (real PK is `order_id`, broker-assigned, schema.sql:271). sqlite raised OperationalError every call → the except swallowed it → a **HARD_KILL flatten NEVER cancelled the trade's resting SL/TGT at the broker** → they survived + could fill against a now-flat book (**naked reverse**; the AEROENTER-orphan class). **Fix (mirror H-1 / structure_exit_manager):** `SELECT order_id, leg` + cancel & finalize each leg by `order_id` (order_id IS the broker id; the old `boid`/`oid` split collapses to one column); no new except/fallback. `:977`/`:994` are the ONLY dead-column SQL on this path (`:1126/1128/1175/1245` are Python `.broker_order_id` attribute access, legitimate). **Parity:** single method, single caller, mode-agnostic query — HARD_KILL fires in both PAPER+LIVE through this method, no paper duplicate; one fix both. **Test:** `tests/unit/test_p1_killswitch_cancel_resting.py` (2, REUSES the H-1 `real_schema_store` harness) — RED against broker_order_id (nothing cancelled) → GREEN against order_id (SL_LIVE+TGT_LIVE cancelled); kill_switch module **49 pass**. Ref `docs/audit/full_system_audit_04july2026.md` (kill_switch:977, H-1 twin); memory `p1_killswitch_dead_column_fixed_05jul`. **Group-A HARD_KILL flatten STILL OPEN** — H-4 (retry double-sell) + H-5 (CNC sweep → naked MIS short) + the Group-A validation remain; **H-4 next**. H-12 (paper get_positions strips qty sign) still blocks a paper drill of the HARD_KILL reverse-flatten direction (Wave 3).
- 2026-07-05 — Claude Code (Opus 4.8) — **Wave 1 VALIDATION: emergency-exit chain VALIDATED end-to-end (commit `d8795fe`, tests-only, UNPUSHED). Result: PASS — the four fixes compose.** New `tests/integration/test_emergency_exit_chain.py` drives the REAL `OrderPlacer._emergency_market_exit` (LONG OPEN trade + resting SL/TGT) and asserts on ONE flatten: (H-1) resting SL/TGT cancelled at broker BEFORE the flatten; (cap) flatten is a FIX-181 marketable LIMIT not raw MARKET; (H-2) the fill closes the EXITING trade immediately with `release_used` once at REAL non-zero costs + `PositionClosed` (not the ~30-min costs=0.0 fallback); (H-3) a stale retry on the now-closed trade places NO second SL. REAL: OrderPlacer / OrderManager / engine+protocols / StateStore(real schema) / CostCalculator(broker_costs.yaml) / EventBus + the close/cancel/retry/cap methods; SIMULATED (legit seams): the broker adapter (ordered place/cancel + signed LONG position + live LTP) + the fund manager (records release_used). **Load-bearing:** verified by one revert cycle on H-2 (the integration test FAILED at the close assertion with the guard reverted, then restored via `git checkout`; commit stayed tests-only). **H-12-independent** (direction from an injected correctly-signed LONG → SELL; a PAPER DRILL of the SHORT reverse-flatten still needs H-12, Wave 3). Optional reconciler-flatten scenario DEFERRED (M-O1 unit-tested separately). Full affected suite **230 passed**. Nuance (not a defect): the emergency-exit cap is `get_quote_raw`-fed → {} in the real paper adapter → MARKET in paper (harmless); the test supplies a live LTP to validate the LIMIT branch. Memory `wave1_chain_validated_e2e_05jul`.
- 2026-07-05 — Claude Code (Opus 4.8) — **Wave 1 / M-O1 FIXED (commit `959badf`, main, UNPUSHED) — EMERGENCY-EXIT CHAIN CLOSED (H-1✓ H-2✓ H-3✓ M-O1✓).** `_flatten_broker_position` looked up the LTP with a DOUBLE-prefixed quote key: `_quote_fn`=`adapter.get_quote` takes BARE symbols, internally prepends `NSE:` (zerodha_adapter.py:1567) + returns bare-keyed Quotes (:1581); the flatten passed `[f"NSE:{symbol}"]` → get_quote queried `NSE:NSE:SYM` → miss → ltp always None → every kill-switch/inflight-orphan flatten fell back to a **RAW MARKET** order → the **FIX-181 marketable-limit slippage cap was silently inoperative in BOTH modes**. **Fix (2 tokens):** the bare-symbol idiom (`self._quote_fn([symbol])` / `raw.get(symbol)`, mirroring :1329/:2859); no None-guard added; `symbol` is bare (passed straight to `place_order(symbol=)`, which handles NSE: internally). **Parity:** get_quote is mode-agnostic (paper delegates to the injected quote_provider, same bare-symbol contract; live queries kite), single shared method — one fix both. **Test:** `tests/unit/test_mo1_flatten_quote_key.py` (2) long→SELL + short→BUY, RED (order_type MARKET) → GREEN (order_type LIMIT at the marketable-limit price); `order_reconciler` **88 pass**. Ref `docs/audit/full_system_audit_04july2026.md` (M-O1); memory `mo1_fixed_flatten_quote_key_05jul`. **CHAIN NOW CLOSED** — an emergency flatten cancels resting SL/TGT pre-flatten (H-1), fires a FIX-181 capped LIMIT not raw MARKET (M-O1), completes the full close+release immediately with real costs so capital frees and the loss hits both daily-loss gates (H-2), and no stale retry fires a duplicate SL / naked reverse (H-3). Still open (out of scope): twin dead-column `kill_switch.py:977` (HARD_KILL has the same H-1-class bug); H-4/H-5 HARD_KILL correctness (Wave 2); H-12 paper get_positions qty-sign (Wave 3) — blocks a paper drill of the reverse-flatten direction; H-3 TOCTOU (local-table SL check) residual.
- 2026-07-05 — Claude Code (Opus 4.8) — **Wave 1 / H-3 FIXED (commit `a254a82`, main, UNPUSHED — Rama pushes off-market).** `_retry_limit_triple_exits` fired from a STALE snapshot — it re-read nothing and `_pending_exit_retry` entries never expired. After an LTP-validation failure G5b places a recovery SL within ~15-30s; a later tick then fired the stale retry → trade still OPEN = a SECOND SL (duplicate-SL/**RAMCOIND** oversell: a stop-hit inside the one-cycle dedupe window double-fills), trade already CLOSED = SL+TGT on a flat position = **naked reverse**. **Fix (mirror `retry_tgt_for_trade`, reuse its helpers — no divergent check):** re-read via `store.get_trade_for_tgt_retry` + bail unless OPEN/PARTIAL (drop the entry); if a non-terminal SL exists (`store.get_sl_order_for_trade`) skip the SL leg + place only the missing TGT via `retry_tgt_for_trade` (idempotent, self-guarding); OPEN+no-SL still places SL+TGT (the legit retry is preserved — the guard must not over-bail); expire entries older than `_EXIT_RETRY_TTL_SEC`=**180s** (new `_ExitRetryParams.enqueued_at` now_ist stamp — 180s ≈ 6× the ~30s G5b recovery bound, ≪ the 15-min reconciler cycle, and a legit retry fires on the first LTP tick within it). **Parity:** single shared path (`_on_ltp_tick_for_retry`→`_retry_limit_triple_exits`; paper synth + live both dispatch here, only the adapter differs) — no paper duplicate, one fix both. **Residual (out of scope):** `get_sl_order_for_trade` is LOCAL-table based (~40ms TOCTOU vs broker truth for a NEAR-SIMULTANEOUS SL) — not an H-3 issue (the G5b SL lands ~15-30s before the retry), and the RAMCOIND 4-layer reconciler/G5b defense is the compensating backstop; not re-architected. **Test:** `tests/unit/test_h3_exit_retry_guard.py` (4, real StateStore over the real schema + real guards) — T1 closed→bail + T2 open+SL→skip-SL RED→GREEN, T3 open+no-SL→place (no over-bail), T4 stale→drop; `order_placer` **127 pass**. **Emergency-exit chain: H-1✓ H-2✓ H-3✓ — only M-O1 (NSE:NSE: double-prefix) remains.** Twin dead-column `kill_switch.py:977` still open. Ref `docs/audit/full_system_audit_04july2026.md` (H-3); memory `h3_fixed_exit_retry_guard_05jul`.
- 2026-07-05 — Claude Code (Opus 4.8) — **Wave 1 / H-2 FIXED (commit `5549fc7`, main, UNPUSHED — Rama pushes off-market).** An emergency-exit fill for a trade in the transitional EXITING state could never cleanly close it: `_emergency_market_exit` marks EXITING, then the fill hits `OrderPlacer._handle_exit_fill` → `OrderManager.close_trade`, whose guard accepted only OPEN/PARTIAL and RAISED for EXITING; `_handle_exit_fill` caught it as `exit_fill_already_closed` and early-returned → skipped `_cancel_oco_siblings`, `release_used`, the `PositionClosed` publish, and cost/PnL recording. The trade was finalized only ~30 min later by the reconciler's `_check_stuck_exiting` fallback with **costs=0.0** (for that window: capital locked, realized loss invisible to **both** daily-loss mechanisms + dropped from the B-1 unrealized set, resting siblings live) — the designed-in outcome on **every** emergency exit that fills. **Fix (option a):** `close_trade` guard `("OPEN","PARTIAL")` → `("OPEN","PARTIAL","EXITING")` — EXITING is TRANSITIONAL (core/schema.sql:155 — "before the fill handler / reconciler closes the row"), so the fill is meant to close it. Chosen over (b) because `close_trade` has a **single** production caller (`_handle_exit_fill`) so the guard is unshared (one token + a comment, no handler restructure). Truly-terminal states (CLOSED/CLOSED_MANUAL/FAILED/CANCELLED/REJECTED*/PENDING*) still raise → **double-close protection preserved**; the reconciler's EXITING-exclusion (**THELEELA** double-sell guard) is untouched; `_handle_exit_fill` itself is unchanged (it already runs the full close+release once close_trade succeeds). **Parity:** the emergency-exit fill path is mode-agnostic (paper synth + live both dispatch to the same `_handle_exit_fill`/`close_trade`; only the adapter differs) — no paper-specific duplicate, one fix covers both. **Test:** `tests/unit/test_h2_exiting_close_release.py` (2, REUSING the H-1 `real_schema_store` harness + a real OrderManager + the real `_handle_exit_fill`) — Test 1 RED (stays EXITING, no release) → GREEN (CLOSED + release + OCO + `PositionClosed` + real charges 25.0, NOT 0.0); Test 2 (CLOSED duplicate fill early-returns) passes both ways; `order_placer` + `order_manager` **133 pass**. **Emergency-exit chain: H-1✓ H-2✓ — H-3 + M-O1 STILL OPEN** (not closed until all four land); twin dead-column `kill_switch.py:977` still open. Ref `docs/audit/full_system_audit_04july2026.md` (H-2); memory `h2_fixed_exiting_close_05jul`.
- 2026-07-05 — Claude Code (Opus 4.8) — **Wave 1 / H-1 FIXED (commit `168e70d`, main, UNPUSHED — Rama pushes off-market) + reusable schema-backed test harness.** `OrderPlacer._cancel_trade_resting_exits` (orders/order_placer.py:3614) SELECTed a non-existent column `orders.broker_order_id`; the real orders PK is `order_id` (the broker-assigned id, core/schema.sql:271) and no migration adds broker_order_id, so sqlite raised OperationalError on **every** call and the `except` swallowed it as a WARNING → **FIX-190 Bug E** (cancel a trade's resting SL/TGT at the broker BEFORE an emergency flatten) never ran → resting legs could survive the flatten and fill against a now-flat book (**naked reverse position**). **Fix:** `SELECT order_id` (+ read `r["order_id"]`) — permanent, error ELIMINATED not caught; order_id IS the broker id passed to `adapter.cancel_order`. **Parity:** single shared code path in `_emergency_market_exit` (mode-agnostic, same orders table) — NO paper/live/shadow duplicate of the query. **New reusable infra:** `tests/conftest.py` → `build_real_schema_db()` / `real_schema_db` / `real_schema_store` / `RealSchemaStore` — executes the REAL `core/schema.sql` into an in-memory DB so a query naming a non-existent column fails exactly as in production (never a mock schema / _MockStore; later waves extend it). Test `tests/unit/test_h1_cancel_resting_exits.py` (2 — RED against broker_order_id, GREEN against order_id); `test_order_placer.py` **123 pass**, 0 regressions. **Emergency-exit chain (H-1 / H-2 / H-3 / M-O1) IN PROGRESS — NOT closed until all four land** (H-1 alone does not remove the emergency-exit risk). **OUT-OF-SCOPE surface flagged (not fixed here):** `capital/kill_switch.py:977` has the SAME dead `broker_order_id` in its own `_cancel_trade_resting_exits` (HARD_KILL path) — a separate finding; `orders/structure_exit_manager.py:365` is already correct (uses order_id). Deploy caveat (Wave 0a): the live post-receive hook does `checkout -f` only, **no service restart** — trading-system is inactive now so the next start picks up the fix; a running session would need a manual `systemctl restart`. Ref `docs/audit/full_system_audit_04july2026.md` (H-1); memory `h1_fixed_dead_column_05jul`.
- 2026-07-02 — Claude Code (Opus 4.8) — **A-1 + E-1 naked-orphan ROOT-CAUSE FIX (tag-correlation recovery) — COMPLETE on branch `fix-a1e1-orphan-recovery-02jul` (UNPUSHED; off-market deploy+restart after Rama's review).** Kills the highest-severity bug: an ENTRY that reached the broker but whose local `orders` row never persisted (A-1 place-timeout / E-1 crash-mid-place) was marked **FAILED + capital released WITHOUT confirming broker absence**, then disowned as a human order when it filled → a **naked, unmonitored position** until the 15:17 EOD sweep. **ROOT ENABLER:** the broker `tag` (`truncate_tag_for_broker(trade_id)`) was written on every order but never read. **FIX (one recovery path, both modes):** (1) `broker/zerodha_adapter.get_all_orders()` — ALL of today's orders (any status) WITH the `tag` + `product` (live `kite.orders()`; paper = `_paper_fills` oracle, tag+product retained across the synth-fill merge). (2) `order_reconciler.correlate_entry_by_tag()` (PURE) — recompute the tag, filter to the ENTRY leg by side → MATCH / ABSENT / AMBIGUOUS (48-bit-collision-safe via symbol+qty narrowing). (3) `order_reconciler._recover_in_flight_entries()` runs as a **PREPASS at the top of every reconcile cycle** (startup synchronous `reconcile_once()` + the 15-min poll) — BEFORE CHECK1 and the G5b loop — so an adopted-OPEN trade gets its recovery SL (G5b) + TGT retry **in the SAME cycle**, never naked for a poll interval. BOTH feeds (timeout `UNKNOWN_IN_FLIGHT` queue + crash orphaned-`PENDING`) flow through ONE `_adopt_or_fail`: **terminal+filled→ADOPT** (backfill fill fields + ENTRY row COMPLETE, set OPEN, commit capital, flag TGT retry — even a partial-then-CANCEL is protected, never disowned); **terminal+zero-fill→FAILED+release** (the ONLY evidence-based release); **non-terminal (resting)→DEFER** (reserve retained, re-correlated next cycle); **ABSENT→FAILED+release only after a 3-poll propagation budget** (broker-reachable); **broker unreachable→DEFER the whole set** (never blind FAILED); **AMBIGUOUS→CRITICAL alert + defer** (never mis-adopt/release); **HARD_KILL + matched fill→FLATTEN** (commit→EXITING→oversell-guarded emergency close, finalized by `_check_stuck_exiting`→CHECK1; never resumed). (4) `broker/order_monitor._cleanup_orphaned_pending_trades` is now **DETECT-ONLY** (WARNING log; NO blind FAILED / NO orphan-callback release) — it defers to the reconciler recovery (which runs first at startup). **CAPITAL (crash-aware, exactly-once):** new `fund_manager.commit_adopted_entry` / `restore_adopted_reservation` / `release_adopted_reservation` — a crashed entry's reservation (rehydrate replays OPEN/PARTIAL only, so it's absent) is reconstructed in-memory from the durable `fm_ledger` RESERVE row, then committed/released; the three-balance invariant holds and the ledger stays consistent (RESERVE→COMMIT, or RESERVE→RELEASE, no duplicate/double). `state_store.adopt_recovery_trade_to_open` extended to backfill the fill fields atomically + new `mark_recovery_trade_exiting`. **RAMCOIND L1-L4 / CHECK9 (FACET-1 race-aware + FACET-2 oversell) / G5b all PRESERVED** (adoption reuses the existing guarded exit protocol; entry_time backfilled to `created_at` so G5b's settling window doesn't defer the recovery SL). **NO schema change.** Old `_check_unknown_in_flight` (timeout-only, blind FAILED after 3 polls, keyed on a broker_order_id a timed-out entry never learned) is REMOVED. Files: `broker/zerodha_adapter.py`, `orders/order_reconciler.py`, `broker/order_monitor.py`, `capital/fund_manager.py`, `core/state_store.py`. Tests: NEW `tests/unit/test_a1e1_recovery_matrix.py` (7 capital-correctness-proof cases + 13-row recovery matrix incl. same-cycle-G5b integration + paper-parity), `tests/unit/test_a1e1_orphan_recovery.py` (B.1 units), `tests/unit/test_fix068_timeout_recovery.py` retargeted (order-placer side kept; obsolete reconciler-method tests removed), 2 `test_order_monitor` FIX-071-Part-B tests updated to the detect-and-defer contract. Affected-suite regression green. Design: `docs/design/a1_e1_orphan_fix_design_02jul2026.md`; memory `a1_e1_orphan_fix_impl_02jul`.
- 2026-06-29 — Claude Code (Opus) — **CONTROL TOWER Phase 1d (CRON WIRING + daily_report OBSERVABILITY + DEPLOY) — completes Phase 1.**
  The capstone of the VM Operations Control Tower (1a foundation + 1b aggregator/freshness + 1c health/status/reporter/ACK CLI). **(1) ONE daily runner** `ops/control_tower/runner.py` (cron `control_tower` at **17:05 Mon-Fri**, env_wrapper `python_nopath`, marker+heartbeat): runs the size-logger FIRST (`size_logger.run` — seeds today's `control_tower_trends` SIZE cols, field-scoped UPSERT so the aggregator's health write doesn't clobber) THEN `aggregator.run_aggregation` (detection → findings lifecycle → health → status roll-up → reporter with the REAL `TelegramNotifier` + the pull report at `data_store/control_tower/report_<date>.*`). It **supersedes** the standalone Phase-1a size-logger cron (the registry entry `control_tower_size_logger` is REPLACED by `control_tower` → sizes are logged once). **(2) Non-trading-day skip:** `runner.main()` calls `skip_if_non_trading_day` (SKIPPED heartbeat, early return) so on a weekday NSE holiday the freshness engine doesn't false-flag the legitimately-absent EOD/candle stages as MISSING (which would push false HIGH Telegram alerts) — honours the foundation "no real alerts on non-trading days" rule; `--force` bypasses for previews. **(3) Self-reference guard:** a new `self_job` param on `run_aggregation` → `read_cron(exclude=…)` skips the `control_tower` job itself (its completion heartbeat + `.done` marker are written only AFTER the run, so it would otherwise flag its own in-flight run as "missed"); `check_cron_drift` (18:00) is its external monitor. Default empty exclude ⇒ the standalone aggregator + existing 1b tests are byte-identical. **(4) daily_report OBSERVABILITY source-fix:** `reports/daily_report.py::main` now records `cron_heartbeat("daily_report", SUCCESS|FAILED)` around the xlsx write (helper `record_daily_report_heartbeat`) — fixes the recurring false "daily_report missed" MEDIUM (the 23-Jun gap) at the SOURCE (the job produced the xlsx but emitted no heartbeat/marker), not by weakening detection. **Cron:** `config/cron_registry.yaml` entry renamed/repointed (monitored:true, `command: ops/control_tower/runner.py`, `marker_name: control_tower`); `deploy/cron/trading-system.cron` regenerated (43 command-lines; `scripts/generate_crontab.py --selftest` byte-round-trip GREEN). **PARITY:** the runner + Telegram are mode-neutral (read-only DB aggregation, no broker session). **No DB schema change** (v40 ships in 1a). Tests: NEW `tests/unit/test_control_tower_phase1d.py` (8: runner size-then-aggregate + report written; read_cron self-exclusion with/without; runner doesn't flag itself; registry runner entry + old size-logger removed; non-trading-day skip; `--force` runs + SUCCESS heartbeat; daily_report helper + main() heartbeat). control_tower(1a-1d) + cron_registry/officer + report suites GREEN (81 + report tests). **DEPLOYED to main `19de58a` 29-Jun ~18:42 IST (post-close):** main was unchanged at `033ae58` (the Telegram-enhancements branch did NOT auto-deploy — still its own branch awaiting Rama) ⇒ a CLEAN fast-forward (no rebase). Backed up the live DB (`pre_control_tower_deploy_20260629_184203.db`, 181MB / integrity ok / v39); push → post-receive checkout + crontab **AUTO-INSTALLED** (the 4 claude-heartbeat lines are NOW in the canonical and SURVIVED the install — the "install drops them" note in the Cron section is STALE); controlled v39→v40 via a one-shot StateStore open → **schema_version=40 + all 5 control_tower tables**; `systemctl restart` → clean **exit 0** on the post-close market-window guard (no crash). Verified: Config Auditor **0 BLOCK** (1 WARN = the entry_end-near-eod nuance), **sr_detector.enabled=true** (survived `checkout -f`), live `crontab -l` == canonical (drift-clean), control_tower 17:05 line live. **Post-deploy manual runner run:** overall **WARNING** / health 71 / freshness ALL OK / REAL VM sizes (disk 12.55% / backup 1717MB / log 691MB / db 173.5MB) / report at `data_store/control_tower/report_2026-06-29.{html,csv}` / **REAL Telegram FIRED for the config HIGH** (qualifying=1; the MEDIUM daily_report + INFO security stayed silent). The config HIGH was ACKED (`ct ack 3`, reversible). The MEDIUM "daily_report missed" is EXPECTED tonight (today's daily_report pre-dates the heartbeat fix) and resolves on tomorrow's 16:05 run. **Phase 1 COMPLETE.** **Deferred T1/T2/T3:** disk-metric repair, a retention job, weekly advisory scanners (the Sun-18:10 slot). Docs: this section + the Control Tower manual subsection + `PATHS.md`. See memory `control_tower_phase1a_29jun`.
- 2026-06-26 — Claude Code (Opus) — **SLICE2.5-PHASE-4: trade_type reject-by-intent gate — CONFIRM + observability label split (the last Slice 2.5 build; no schema; dormant).**
  **Investigation finding:** the gate Phase 4 set out to add ALREADY EXISTS and is live — Slice 2's `strategies/control.strategy_will_trade` LAYER 1×2 (`:82-94`) already rejects a strategy whose EFFECTIVE (post-coercion) intent ≠ `trade_type` (INTRADAY→reject DELIVERY, DELIVERY→reject INTRADAY, BOTH→accept), wired into BOTH `signal_processor` paths (`_process_one` ~:625, `continue_from_gate` ~:1330) BEFORE sizing/risk/reserve, metric'd via `entries_rejected`, dormant under the current config. So Phase 4 is **Option A = confirm + harden, NOT a new gate** (Option B — declared/pre-coercion-intent gating — was REJECTED: it would stop the 3 live `positional_*` strategies; parked as a strategy decision). **The one live-path touch — an observability label split:** a trade_type×intent mismatch now rejects with a DISTINCT label `TRADE_TYPE` (→ signal status `REJECTED_TRADE_TYPE` + its own `_stats["rejected"]["TRADE_TYPE"]` tally + the same `entries_rejected` metric / `_PipelineReject` machinery), so a delivery go-live can SEE trade_type rejects apart from other strategy-control rejects. Mechanism: a machine-readable **`Verdict.cause`** field on `strategies/control.py` (`CAUSE_OK`/`CAUSE_DISABLED`/`CAUSE_FORCE_BREAKER`/`CAUSE_TRADE_TYPE`, default OK), set at each return site; the gate maps `cause==CAUSE_TRADE_TYPE → "TRADE_TYPE"` and **everything else (disabled switch, force-breaker) keeps `"STRATEGY_CONTROL"`** — keyed on the cause, **never on the message string**. **Reason strings UNCHANGED; the gate's accept/reject logic UNCHANGED; the loader coercion + strategy `enabled` flags UNTOUCHED.** Identical mapping at both gate sites. **Dormancy:** under the current config (`trade_type=INTRADAY` + `force_intraday_only=true`) every strategy is coerced to INTRADAY at load → the gate accepts all → 0 rejects → the split never fires → ZERO Monday behaviour change. **Parity** (shared pure gate module). **No DB schema.** Tests: `tests/unit/test_phase4_trade_type_gate.py` (9 — cause-per-layer + reason-unchanged guard; INTRADAY/DELIVERY/BOTH force=false accept/reject matrix; **★ dormancy no-op**; **★ signal_processor label split** `REJECTED_TRADE_TYPE`; **★ label-bleed regression** disabled→`REJECTED_STRATEGY_CONTROL`; contradictory `DELIVERY`+`force=true` → Config Auditor group-A BLOCK, confirmed not re-implemented). Updated ONE Slice-2 assertion (`test_master_intraday_blocks_delivery_at_process_one` now expects `REJECTED_TRADE_TYPE` — the intended relabel) + extended `test_signal_processor._make_proc` with optional `trade_type`/`force_intraday_only`. Full PC unit suite **3863 passed / 12 skipped / 0 regressions** (3854 baseline + 9). **DEPLOYED to main `37b3db3` 26-Jun ~13:16 IST** (one-time authorized batch push+restart with Phase 3 + the FIX-183 deploy-log `0286b73`; standing Rama-owns rule RESTORED after). Push → post-receive clean + crontab auto-installed (no hook errors). The 13:16 restart **self-exited 0/SUCCESS at the Muharram holiday guard** (before StateStore init), same as the FIX-183 restart — **real boot = Mon 29-Jun 08:15**. Verified broker-session-free **PASS — fully DORMANT**: load_all() + Auditor 0 BLOCK, Phase-3 (`resolve_bucket_allocation`/`count_*_delivery_*`/risk_engine caps) + Phase-4 (`Verdict.cause` + TRADE_TYPE mapping) present, and `delivery_enabled=false` / `conditional_allocation_enabled=false` / `trade_type=INTRADAY` / `force_intraday_only=true` → fixed 70/30 + caps inert + gate no-op. **This completes the Slice 2.5 BUILD side;** remaining before `delivery_enabled=true`: T2 (market-hours real-API/TPIN) + C1-watch (Monday). See memory `slice25_phase4_trade_type_gate_26jun`.
- 2026-06-26 — Claude Code (Opus) — **SLICE2.5-PHASE-3: delivery (CNC) count caps + conditional capital allocation — the "capital" delivery difference; both LOCKED but INERT (no schema; delivery_enabled stays false; flag default false).**
  Two features built to stay correctly dormant until deliberately activated. **FEATURE A — separate delivery count caps:** new `risk.max_open_delivery_positions` (3) + `max_daily_delivery_trades` (5) (`core/config_loader.py` RiskConfig + positive-int validator + `system_config.yaml`); two product-keyed count methods in `core/state_store.py` — `count_open_delivery_positions()` (OPEN/PARTIAL/PENDING_FILL) + `count_daily_delivery_trades(date_iso)` (today, executed statuses), both `JOIN orders ON leg='ENTRY' AND product='CNC'` with `COUNT(DISTINCT trade_id)`; enforced in `capital/risk_engine.py::_run_checks` where **OPEN_POSITIONS + DAILY_TRADES branch on `sizing_result.bucket=="positional"`** (delivery entry → its delivery-scoped cap; ELSE → the EXISTING global reservation-aware check, **byte-for-byte unchanged** — preserved verbatim, just moved into the `else`). The delivery counts are read **lazily** in `approve()` only when `bucket=="positional"`, so ZERO extra DB queries hit the intraday hot path (and always while coerced). CONSECUTIVE_LOSSES stays SHARED. **Keying** (from the coercion clarification): the current entry keys on `bucket=="positional"` (the proven 1:1 proxy for "this entry will be CNC"; the resolved product string isn't available pre-trade), the COUNT keys on the persisted ENTRY `product='CNC'`. **A5 — INTENTIONAL asymmetric coupling** (documented in code): the intraday/global branch is left unchanged, so it still counts ALL positions (incl. delivery) → delivery DOES count toward an intraday entry's cap, while intraday does NOT count toward the delivery cap; chosen to keep the LIVE intraday cap byte-identical (zero regression on the money path) — at current capital, capital binds long before these counts, so the coupling is academic. **FEATURE B — conditional capital allocation:** new `capital.conditional_allocation_enabled` (**default false**) + a pure helper `capital/fund_manager.resolve_bucket_allocation(...)` returning the effective `(intraday_pct, positional_pct)` — flag OFF → the fixed config split (byte-for-byte unchanged), flag ON → only-intraday `(1.0, 0.0)` / only-delivery `(0.0, 1.0)` / both → the config split / neither → `(1.0, 0.0)` safe idle (always sums to 1.0, FM12 holds). `main.py` computes `delivery_active`/`intraday_active` (the same predicate as the `delivery_lock` boot log) and passes the EFFECTIVE pcts into the FundManager ctor + logs `capital.bucket_allocation`; **FundManager itself is UNCHANGED** (it only receives the final pcts; `initialize()` splits broker_balance by them). The no-borrow/reject is already enforced by `reserve()` (it consults ONLY the intent's bucket) — delivery 0% ⇒ every delivery reserve rejects "Insufficient positional capital", never borrowing intraday, for free. **SHARED/untouched:** `max_concentration_pct` (position_sizer, total-capital-relative), SECTOR_EXPOSURE, `max_position_value_pct`, and the entire intraday cap path. **DORMANCY:** flag default false → 70/30 unchanged; `force_intraday_only=true` coerces every strategy to intent=INTRADAY (loader, early) → bucket=intraday → the positional cap branch is never hit AND `count_open_delivery_positions()==0` → provably inert. Monday's boot changes NOTHING live. **Parity** (risk_engine/fund_manager/state_store shared, no mode branch). **No DB schema.** Tests: `tests/unit/test_phase3_delivery_caps_conditional_capital.py` (17 — allocation 7 cases + dormancy proof + FM12 sum; delivery count methods CNC-only; OPEN/DAILY delivery-cap rejects; under-cap approves; **★ inertness** (intraday bucket ignores the delivery cap; count==0 with only MIS); **★ MIS regression** (global cap + message unchanged); no-borrow reject path). Full PC unit suite **3854 passed / 12 skipped / 0 regressions** (3837 baseline + 17). **DEPLOYED to main `37b3db3` 26-Jun ~13:16 IST** (one-time authorized batch push+restart with Phase 4 + the FIX-183 deploy-log; standing Rama-owns rule RESTORED after). The 13:16 restart self-exited 0 at the Muharram holiday guard (real boot Mon 29-Jun 08:15); verified broker-session-free **PASS — DORMANT**: `conditional_allocation_enabled=false` → the effective split is the fixed 70/30 (resolve_bucket_allocation returns the config pcts), and `force_intraday_only=true` → every entry is intraday-bucket → the delivery cap branch is never hit + `count_open_delivery_positions()==0`. delivery_enabled stays **false**. See memory `slice25_phase3_delivery_caps_conditional_capital_26jun`.
- 2026-06-26 — Claude Code (Opus) — **FIX-183: orphan-GTT ADOPTION / reconstruction — closes the C2.1 gap (a live broker GTT with no `gtt_state` row was never reconstructed; no schema; delivery_enabled stays false).**
  Completes the locked SLICE2.5-P2 principle "**broker GTT = authority, `gtt_state` = self-healing mirror**": a LIVE broker GTT (status `active`) with NO `gtt_state` row is now ADOPTED instead of left orphaned. **The gap (C2.1):** a carried CNC delivery holding lives in `holdings()`, NOT `positions()`, so the reconciler's `bp is None` → without an ACTIVE `gtt_state` row to exclude it, CHECK1 would mis-mark the trade `CLOSED_MANUAL` (and G5b/CHECK9 place a spurious SL). A row-less GTT (persist-failure at placement, or a GTT predating the table) therefore had no self-healing path. **STEP 1 — `cnc_gtt_monitor.adopt_orphan_gtts()`:** for each broker GTT — skip unless `status=='active'`; skip if a `gtt_state` row already exists for its `gtt_id` (`get_gtt_state_by_id`); else **RECONSTRUCT** every column except `trade_id` from the broker GTT (`_reconstruct_from_broker_gtt`: symbol/exit_side/qty from the legs, **SL/TGT split by trigger MAGNITUDE** not array index → robust to broker leg reordering; unparseable/empty-legs → WARN, leave alone) and **CORRELATE** `trade_id` product-aware (`_delivery_open_trades_by_symbol` via `get_all_open_trades`, which carries the ENTRY-leg product through its LEFT JOIN — bare `get_trades_by_status_and_symbol` lacks product). Outcomes: **exactly 1** open CNC trade with no existing ACTIVE row → ADOPT (`insert_gtt_state`, reusing the durable write path); **0** trades → can't adopt (FK forbids; leave the live GTT ALONE) + WARN; **>1** (only reachable via manual entry — `DUPLICATE_SYMBOL` blocks it otherwise) → WARN, no adopt; **1 but already has an ACTIVE row** → WARN, no adopt (a 2nd ACTIVE row would trip M2). **Adoption ONLY ever INSERTs a row or WARNs — it NEVER deletes a live GTT.** Unadoptable WARNs are de-duped **once per gtt_id per process** (`_adopt_warned`, mirrors FIX-182's once-per-symbol discipline) so the every-cycle prepass can't spam (esp. benign human GTTs that match the 0-trades case); alerts carry `source_module="cnc_gtt_adoption"`. **STEP 2 — placement (the timing fix):** a NARROW prepass `order_reconciler._run_gtt_adoption_prepass()` runs at the **top of `_reconcile()`**, BEFORE the delivery-exclusion build + CHECK1, fail-safe (no-op when no monitor wired; never raises). Because CHECK1 lives inside `reconcile_once`, adopting at its top guarantees **adopt-before-CHECK1 in BOTH paths** (startup `reconcile_once()` at `start()`, and the 15-min poll's `reconcile_once()` which runs before the monitor's own `reconcile()`) — WITHOUT reordering `_maybe_run_cnc_monitor` or any MIS-path check (minimal regression). Cost: `get_gtts()` once per ~15s reconcile cycle (LIVE) — the "margins" rate bucket (burst 8, 8/s) makes this trivial. **Parity:** paper `get_gtts()` returns the identical dict shape → adoption runs end-to-end in paper. **No DB schema** (`gtt_state` already exists from P2 → no `.backup` step). Tests: `tests/unit/test_cnc_gtt_adoption_fix183.py` (12, paper injected-state) incl. **the C2.1 regression** (row-less carried GTT → reconcile_once → trade stays OPEN + gains an ACTIVE row, NOT CLOSED_MANUAL; plus a no-monitor contrast that DOES go CLOSED_MANUAL = proof of the gap) and **the MIS regression** (intraday trade through reconcile_once with the prepass present → CHECK1 unchanged, no `gtt_state` row created). Full PC unit suite **3837 passed / 12 skipped / 0 regressions**; touched suites (cnc_gtt × 4 + order_reconciler) 113 pass; integration smoke 11 pass. **delivery_enabled stays false** (durability only — one of the go-live gates before true, alongside T2). **DEPLOYED to main `af4b784` 26-Jun ~11:11 IST** (one-time authorized push+restart; standing Rama-owns rule RESTORED after): ff-merged `fix-183-gtt-adoption-26jun` onto main (also carried the pending P2 docs-log `75d4eca`); push → post-receive clean + crontab auto-installed (no hook errors). The 11:11 restart **self-exited 0/SUCCESS at the HOLIDAY guard** (26-Jun = Muharram, NSE closed → banner in 786ms, before StateStore) — like the P1/P2 after-hours boots; **real boot = Mon 29-Jun 08:15**. Verified independently broker-session-free **5/5 PASS**: FIX-183 symbols present, `load_all()` clean (Config Auditor 0 BLOCK), `delivery_enabled=False`, **live schema_version=36 + gtt_state (14 cols)** (P2 migration already applied). See memory `fix_183_gtt_adoption_26jun`.
- 2026-06-25 — Claude Code (Opus) — **SLICE2.5-P2: durable CNC overnight-GTT state + reconcile + GTT_EXIT + 15-min monitor (Phase 2 of the Delivery arc; schema v36; delivery_enabled=false — durability + safety only, no activation).**
  P1 kept the one-GTT-per-trade map IN MEMORY (lost on restart) and had no reconcile / leak monitor. P2 makes it durable + self-healing. **STEP 0 — schema v36:** new **TABLE 37 `gtt_state`** (gtt_id INTEGER PK = broker trigger id; trade_id FK→trades; exit_side/qty/sl_trigger/sl_limit/tgt_trigger/tgt_limit; status `ACTIVE|TRIGGERED|CANCELLED|EXPIRED|REJECTED|CLEANED` CHECK; `needs_review` Y2 latch; last_verified_at; 2 indexes) — PURE ADDITION (CREATE IF NOT EXISTS, no MIGRATION_TABLES rebuild, same path as v31/v33); `EXPECTED_SCHEMA_VERSION 35→36`. **Verified on a `.backup` of the live 161 MB DB: v35→v36, 0 tables rebuilt, all rowcounts intact, FK/integrity/WAL clean.** Y6: a trade may accrue MANY rows over its life (each recreate = new gtt_id = new row = history); the M2 one-GTT invariant is on `status='ACTIVE'`. **STEP 1 (adapter wiring):** `broker/zerodha_adapter` `get_gtt`/`get_gtts`/`delete_gtt`/`get_holdings` (live=Kite, paper=in-memory `_paper_gtts`/`_paper_holdings` + `seed_paper_holding`; paper gtt ids now **NUMERIC** `_PAPER_GTT_ID_BASE` since gtt_id is INTEGER PK). **STEP 1.5 (R2 guard split):** `delivery_enabled` gates ONLY the CNC **entry** (`place_order`); GTT ops are NOT gated — protection survives disablement (with delivery off there are no new entries, so a GTT op only ever touches a pre-existing holding). **STEP 2 (persist):** `CncGttPlacer` gains a store — `place_for_fill` writes/updates the `gtt_state` row (best-effort; broker GTT is the authority), `hydrate_from_store()` rebuilds the hot cache on boot, `forget()` for recreate; `core/state_store` gtt_state DAO. **STEP 3+3.5 — new `orders/cnc_gtt_monitor.py` `CncGttMonitor.reconcile()`:** per ACTIVE row gather (get_gtts + holdings + same-day CNC positions) → M1 (qty) + M2 (one ACTIVE) → **K6 ladder** [healthy→touch / **GTT_EXIT** (triggered+flat → finalise CLOSED + release delivery-bucket capital + publish PositionClosed SYSTEM-OWNED + CLEANED, idempotent via the OPEN→CLOSED gate so a double-observe never double-releases) / **F6** (triggered+still-holding → CRITICAL re-protect remaining) / **recreate** (missing+qty-match in-hours, Y3 fresh LTP) / **Y1 pre-open queue** (missing pre-open → queued, drained on the first in-hours cycle before the gather) / **qty-mismatch** (CRITICAL ONCE via Y2 needs_review + cancel wrong-qty GTT + NO recreate + NO soft-kill + intraday unaffected) / orphan-active-flat (delete+finalise) / **>1 ACTIVE → SOFT-KILL**] + **Y4** broker-gather failure → defer+alert (never crash / never treat no-data as flat). **STEP 4 (wiring):** `order_reconciler` startup [4a] + 15-min in-hours cadence [4b] (first-in-hours fires immediately for the Y1 drain) — reuses the daemon, no new scheduler; **delivery trades (ACTIVE gtt_state) EXCLUDED from CHECK1/3/4/5 + CHECK2 + CHECK9/G5b/duplicate** (a carried CNC holding lives in holdings() not positions() → CHECK1 would wrongly CLOSED_MANUAL it and G5b would place a spurious SL). **STEP 5 (leak/cap/exempt 8b):** orphan sweep = forensic-log FIRST then delete a LEAKED system GTT (still ACTIVE at broker, row non-ACTIVE); human/external GTT (no row) NEVER deleted (FIX-182); 50-cap WARNING/CRITICAL; confirmed+tested `sweep_stale_orders` touches ONLY the `orders` table — a `gtt_state` GTT is a live protective leg exempt from every cancel-sweep (delete_gtt is the sole remover). **STEP 6 (alerting):** `CncGttMonitor` alerts via `telegram_notifier.send(... source_module="cnc_gtt_monitor")`, WARNING (info/50-cap) vs CRITICAL (qty-mismatch/soft-kill/F6/re-protect-fail; CRITICAL writes a sentinel → email fallback), Y2 de-dup. **Parity:** the only live/paper difference is the broker I/O boundary; the reconcile + GTT_EXIT + ladder run end-to-end in paper (injected-state tests). **Tests:** `test_cnc_gtt_slice25_p2.py` (5) + `test_cnc_gtt_monitor.py` (13) + `test_cnc_gtt_step4_wiring.py` (4) + P1 asserts updated for numeric gids. Full PC unit suite **3793 passed / 32 pre-existing env-fails (test_main waitress + TOTP/NTP/interactive — green on VM) / 0 regressions**. Branch `slice25-p2-gtt-durability-25jun` ff-merged onto main → **DEPLOYED to main `bad0aad` 25-Jun ~22:23 IST** (one-time authorized push+restart; standing Rama-owns rule RESTORED after). Push → post-receive clean + crontab auto-installed (no hook errors); the 22:23 restart **exited 0/SUCCESS on the market-window guard** (post-close, by design — 813ms, before StateStore init), so **schema v36 applies at the Fri 08:15 in-window boot** (live DB still v35 tonight; `.backup` proved v35→v36 safe + deployed schema.sql/`EXPECTED_SCHEMA_VERSION` verified =36). Broker-session-free verify **15/15 PASS** (delivery_enabled=false, gtt_sl_off=0.03, Config Auditor 0 BLOCKs, all P2 modules import). **delivery_enabled stays false** — go-live gates before true remain: T2 · FIX-183 · C1-watch. See memory `slice25_p2_gtt_durability_25jun`.
- 2026-06-25 — Claude Code (Opus) — **DEPLOYED to main (one-time authorized push+restart): BUILD 2 + RAMCOIND fix + SLICE2.5-P1 all live.** `main` ff-merged `c55d5ff..598f4dc` (BUILD 2 `eb373c4` → RAMCOIND `6e8ca6a`/`25bcc95` → P1 `598f4dc`); pushed → post-receive deployed cleanly (crontab canonical, no hook errors). The 16:40 restart **exited 0/SUCCESS** = main.py's market-window guard (post-16:00, by design — NOT a crash). Verified independently (probe, broker-session-free): new config loads (`delivery_enabled=false`, `gtt_sl_limit_offset_pct=0.03`), **Config Sanity Auditor runs + passes (0 blocks)**, new modules import (`cnc_gtt`/`config_auditor`/preflight `config_sanity`; adapter `place_gtt`). System flat (0 open trades). Full live boot = the normal Fri 08:15 token-watcher auto-start. `delivery_enabled=false` → nothing trades CNC. One-time override; future pushes/restarts revert to Rama-owned.
- 2026-06-25 — Claude Code (Opus) — **SLICE2.5-P1: GTT-backed CNC overnight protection (Phase 1 of the Delivery arc; no DB schema; delivery_enabled=false — nothing trades CNC).**
  The CNC entry path was built but dormant (force_intraday_only rewrites DELIVERY→INTRADAY); a CNC position's day-validity LIMIT_TRIPLE SL/TGT legs expire at EOD → naked overnight, and no GTT existed (audit G1/G2). P1 replaces those day legs (CNC ONLY) with ONE broker-side two-leg **OCO GTT** placed right after the entry fill, so protection runs entry→exit and survives a VM outage. **Build (all tagged SLICE2.5-P1):** `system.delivery_enabled` (bool, default FALSE — master capability lock) + `capital.gtt_sl_limit_offset_pct` (0.03 DEEP protective SL floor, distinct from the 0.5% intraday offset); `orders/price_math.calc_gtt_limit_price` (dedicated GTT leg-limit helper — does NOT overload the intraday path); `broker/zerodha_adapter` `place_gtt`/`modify_gtt` (live → `kite.place_gtt`/`modify_gtt` OCO two-leg SELL CNC LIMIT; **paper → mock `PAPER_GTT_*` id + identical recorded params** = parity) + the **master lock at the broker boundary** (`place_order`/`place_gtt` refuse CNC when delivery_enabled=false; MIS/CO unaffected); `orders/cnc_gtt.CncGttPlacer` (tick-valid OCO legs reusing the EXACT sl_price + actual_tgt_price FIX-013; SL limit = trigger−3%, TGT limit = trigger−small; C8 straddle/min-distance validation; **ONE GTT per trade** via an in-memory map + `modify_gtt` on a later partial fill); ONE **centralized gate** in `full_entry_engine.place_deferred_exits` (`intent=="DELIVERY"` → `CncGttPlacer` → `ExitLegsResult(is_gtt=True, gtt_id)`; all 3 order_placer call sites inherit it) + `order_placer._finalize_cnc_gtt` (logs gtt_id + records a verified exit; NO day-leg persist / `_fill_map` OCO / day-leg after-check). **INTRADAY/MIS path byte-for-byte unchanged.** main.py wires it + logs `delivery_lock.status` at boot. **No DB schema** (gtt_id LOGGED only — durable carry-state persistence + a GTT leak/health/CA monitor are Phase 2). **Parity:** the only live/paper difference is the broker boundary. Tests: `tests/unit/test_cnc_gtt_slice25_p1.py` (T1, 9 paper). **T2** `scripts/t2_cnc_gtt_realtest.py` — the MARKET-HOURS real-API blocker (real CNC buy + real OCO GTT + real CNC sell with NO CDSL TPIN; + an `--arm/--close-overnight` variant); **delivery_enabled=true is considered ONLY after T2 passes.** Branch `slice25-p1-cnc-gtt-25jun` (stacked on BUILD2+RAMCOIND; pushes as its own change after they land). See memory `slice25_p1_cnc_gtt_25jun`.
- 2026-06-25 — Claude Code (Opus) — **RAMCOIND duplicate-exit fix — 4-layer permanent + parity fix for the G5b duplicate-SL race (highest-criticality exit/reconciler path; no schema).**
  Root cause (25-Jun RAMCOIND −1 over-sell): `orders/order_reconciler.py` `_g5b_crash_recovery_sl` placed a 2nd SL via a direct `adapter.place_order` (RC18, OUTSIDE the LIMIT_TRIPLE software OCO), guarded by the LOCAL `orders` table which `order_monitor.track` populates ~40ms AFTER the broker placement — a TOCTOU race (guard checked 12ms before the row landed). Both SLs shared the trigger, so on the stop-hit BOTH filled → +1 closed the long and the duplicate over-sold into a −1 naked short, which CHECK2 then disowned as a "human order". Recurring (IRFC 17-Jun, NIACL 22-Jun, RAMCOIND 25-Jun — all LONG only because LONGs are 93% of fills); harmful only on an SL-exit (RAMCOIND was the first). Exit-path inventory confirmed G5b is the UNIQUE out-of-OCO duplicate source. **4 layers (all shared paper+live, no mode branch):** **LAYER 2 (keystone)** new reconciler invariant `_check_duplicate_exits` every cycle — EXACTLY ONE live SL per OPEN/PARTIAL LIMIT_TRIPLE trade (CO excluded — its SL is in the broker CO bracket; detected via the `variety='co'` entry) + ONE live TGT (both protocols); >1 → cancel the non-canonical extra, KEEP the earliest-placed (`get_orders_for_trade` ORDER BY placed_at = the OCO/trailed leg), never drop to zero (re-verify + re-place via G5b if the cancel races a fill) → `DUPLICATE_SL`/`DUPLICATE_TGT` reconciliation_log + alert. Would alone have cancelled the RAMCOIND dup ~12 min before its 10:12:49 stop-hit. **LAYER 1** `_g5b_crash_recovery_sl` now has, before placing: a **10s settling window** (skip recovery for a trade whose persisted `entry_time` is < 10s ago — exits still being placed; mode-agnostic, deterministic) + an **authoritative check** `_already_has_live_sl` (broker order book via the existing `_broker_orders_fn` — an SL = exit-side order with `trigger_price>0`; `_fill_map` fallback when unwired) — race-proof, replaces trusting the lagging table. **LAYER 3** `_check2_orphan_adoption` recognises a SYSTEM over-sell (a recently-closed trade on the symbol, OPPOSITE side, ≈exit price, |qty|≤traded) → CRITICAL + AUTO-FLATTEN the residual (covering MARKET, once/day-guarded); a NAKED untracked position (no protective stop at the broker) → WARNING (never the old silent INFO); a protected human position stays silent (FIX-182 unchanged). **LAYER 4** `order_placer._verify_exits_placed` flags `>1` live SL/TGT (`DUPLICATE_SL/TGT`, the cheap same-path placement-time guard). **No DB schema** (existing `orders`/`reconciliation_log`; SYSTEM_OVERSELL is an additive event). **Direction-symmetric** — every layer is direction-agnostic (G5b places a BUY-stop SL for shorts the same way; L2 filters on `leg` not side; L3 mirrors short over-buy → +qty residual → SELL cover); proven by SHORT-side mirror tests. Tests: `tests/unit/test_ramcoind_dup_exit_fix.py` (24, incl. SHORT L1/L2/L3 mirrors) + acceptance gate `tests/crash_test/test_ramcoind_oversell_prevented.py` (6, paper+live: LONG over-sell + SHORT over-buy both end FLAT, never ±1). NOTE: LONG underperformance is NOT this bug (42/42 LONG exits placed correct distinct SL/TGT — separate parked review). Branch `ramcoind-duplicate-exit-fix-25jun`. **Deploy post-15:30 close (exit/reconciler path — off-market only); activates next restart.** See memory `ramcoind_duplicate_sl_incident_25jun`.
- 2026-06-25 — Claude Code (Opus) — **BUILD 2: Config Sanity Auditor — the enforcement capstone of the Config Authority work (no schema; validate+alert only).**
  ONE rule engine `core/config_auditor.py` (`audit()` → `ConfigAuditReport` of per-group `AuditFinding`s, each PASS/INFO/WARN/BLOCK), TWO callers (single source of truth — no rule lives twice). **Check groups:** **(A) Contradictions** — the BUILD 1 #10 `force_intraday_only`+`trade_type=DELIVERY` is **re-homed here** (BLOCK, message still carries "CONTRADICTORY CONFIG") + A2 trade_type-domain (BLOCK, belt-and-suspenders) + A3 "0 strategies would trade" (WARN, strategy-aware via `strategy_will_trade`). **(B) Single-source regression guards** (WARN) — flags a deleted key reappearing in raw YAML (`capital.daily_loss_limit`, `position_sizing.max_position_value_rs`, `risk.live_test_*`, scoring `tier_multipliers`); reads raw YAML because `extra="forbid"` already strips them from the model. **(C) Capital-relative sanity** (WARN) — pct ranges + the ladder `max_concentration_pct < max_position_value_pct` (the bug-guard must be LOOSER than routine concentration, else it binds first) + cumulative-risk-vs-daily-loss (the migrated BUILD 1 cross-check). **(D) Active-override listing** (INFO/WARN) — T3 visibility of every non-empty slippage override (reuses `orders.order_placer.validate_slippage_overrides` for typo/extreme WARNs). **(E) Launch-phase reminders** (INFO) — surfaces `[LAUNCH-PHASE]`-tagged params (`entry_start` 10:00) with a review note. **(F) Stale-default guard** (WARN) — introspects component ctor defaults (`PositionSizer.max_position_value_pct`=0.40, `FundManager.daily_loss_limit_pct`=0.03) vs config intent (BUILD 1 aligned them; catches a future YAML-vs-code drift). **(G) Cross-field sanity** (WARN) — the migrated entry-window/leverage/min-tick checks + per-strategy-window-in-envelope. **STARTUP** (`core/config_loader.py` `SystemConfig._cross_field_sanity_checks`) now delegates to the auditor (`audit_system_config`, groups `ACG`), logs WARNs and **fails fast on BLOCK** (unchanged boot behaviour + boot-log; B/D/E/F need pre-flight context). **PRE-FLIGHT** new group `scripts/preflight/checks/config_sanity.py` (7 rows A–G, group "Config Sanity") in **Phase A 08:30** → the consolidated **09:20 email + Telegram**; ONE auditor run memoised on the shared `CheckContext` feeds all 7 rows. **Parity:** the auditor is pure with no mode branch → paper + live identical (test-enforced). **No DB schema.** Tests: `tests/unit/test_config_auditor.py` (37: every group BLOCK/WARN/PASS, startup gate, pre-flight integration, parity). Full PC unit suite **3770 passed / 0 failed**. Sample 09:20 section: `Config Sanity (7/7 ok)` — A–F ✅, G ⚠️ (entry_end within 15min of squareoff, the lone pre-existing warn). Branch `build2-config-sanity-auditor-25jun`. **Deploy post-15:30 close; activates Fri 08:15 restart.** Completes the Config Authority arc (audit → authority map → framework → resolution → BUILD 1 cleanup → BUILD 2 enforcement). See memory `build2_config_sanity_auditor_25jun`.
- 2026-06-24 — Claude Code (Opus) — **BUILD 1: config authority fixes — single daily-loss source + capital-relative position cap + dead-config cleanup (12 audited conflicts; trading-path; no schema).**
  Resolves the 12 config conflicts from the Phase 1+2 audit (all LOCKED by Rama). **#1 SINGLE DAILY-LOSS SOURCE:** deleted the absolute `capital.daily_loss_limit` (was ₹300) — `risk.daily_loss_limit_pct` (3%) is now the SOLE authority. The FundManager post-close **realized** breach (`capital/fund_manager.py` `release_used`, `_daily_loss_limit_pct`) derives ₹ = `daily_loss_limit_pct × current capital` (`self._total`), the SAME pct the pre-trade RiskEngine MTM gate uses (`main.py:1828` feeds `app_config.system.risk.daily_loss_limit_pct` to BOTH). Basis split preserved (pre-trade = realized+unrealized MTM; post-close = realized). Scales: ₹10k→₹300, ₹1L→₹3000. **#2 CAPITAL-RELATIVE POSITION CAP:** `position_sizing.max_position_value_rs` (₹2500) → `max_position_value_pct` (0.40). `PositionSizer.calculate` computes `cap = 0.40 × total_capital` (the snapshot it already fetches) and keeps the **REJECT** (`POSITION_VALUE_CAP`, catastrophic-loss/bug-guard, NOT a clamp). Removes the ₹25k scaling cliff (a ₹30k position at ₹1L now passes; routine sizing still bound by concentration 10% + risk 1%). ₹10k→₹4000, ₹1L→₹40000. **#10 STARTUP-BLOCKING GUARD:** `force_intraday_only=true` + `trade_type=DELIVERY` (rewrites all→INTRADAY then gates out INTRADAY = 0 strategies trade) now raises a `ValidationError` in `SystemConfig._cross_field_sanity_checks` → **refuses to boot** (fail fast). **#3/#4/#11 DEAD CONFIG DELETED:** `live_test_mode`/`live_test_max_*` (were == base caps 5/10 → the `main.py` swap was a no-op; base caps now sole authority both modes) + `scoring_weights.yaml` `tier_multipliers` (sizer reads `system_config` 0.70, never this 0.75) + per-strategy `max_risk_pct` (dead in live; `replay_signals` falls back to 0.01). **#A.4 ALIGNED STALE DEFAULTS:** FundManager 10000→0.03, PositionSizer 50000→0.40, RiskEngine docstring 10/20/4/0.05→5/10/5/0.03. **#12 BOOT INVARIANT:** comment + `assert _startup_reconcile_done` so `reconcile_once()` provably precedes `signal_processor.start()`/webhook (phantom capital corrected before any trade; proven no-trade-in-window). **#5/#6/#7/#8 DOC-ONLY** + a PERMANENT-vs-LAUNCH-PHASE tagging convention in `system_config.yaml` (feeds BUILD 2). **AUDITORS (beyond the 12, forced by the deletions):** `scripts/system_manager.py` EOD + `scripts/preflight/checks/config_integrity.py` recompute the pct→₹ thresholds from a new `StateStore.get_day_opening_capital()` (fm_ledger INIT row, **OPENING** basis, `accounts.csv` paper_capital **bootstrap fallback**, no broker call → parity-safe); preflight `LiveTestModeCapsCheck`→`CapsConfigDriftCheck` (guards base caps). **No DB schema** (config + Pydantic field changes only). **Parity:** all on shared paper+live paths. Tests: full suite green except 1 pre-existing time-of-day market-window artifact (`test_interactive_startup`, exits 0 outside 08:00–16:00 IST — confirmed pre-existing on clean HEAD); added #10 + repurposed-preflight + auditor-capital-basis tests. Branch `build1-config-authority-fixes-24jun`. Activates next restart (08:15). BUILD 2 (Config Sanity Auditor) follows. See memory `build1_config_authority_fixes_24jun`.
- 2026-06-24 — Claude Code (Opus) — **Hygiene pack: date-coupled test fixed + 9 merged branches pruned (deployed post-15:30 close).**
  **(1) Date-coupled test FIXED:** `test_system_manager.py::test_report_integrity_missing_and_present` passed ONLY on 19-Jun — it wrote `system_2026-06-19.log` (mtime = run day) but called `report_integrity_check(day="2026-06-19")`, whose intentional anti-staleness guard warns when a file's mtime-day ≠ `day`; on any other day → "stale" not ✅. **Test bug, not a function bug** — fixed the test to derive `date.today()` (the function is correct, untouched). This was the lone remaining pytest failure; suite now fully green. **(2) tzdata PC-env failures:** ALREADY FINE (resolved by the 22-Jun tzdata install; `test_main`/`test_fix129`/`test_fix135` 122/122 green) — no action. **(3) Branch prune:** deleted the **9 fully-merged** session branches (local + origin) — `fix-fno-ban-endpoint-22jun`, `aftercheck-circuit-cap-fix-24jun`, `cron-officer-email-leak-fix-24jun`, `governor-severity-and-briefing-debounce-24jun`, `part-c-rr-1.5-all-strategies-24jun`, `preflight-activation`, `slice1-rr-fix-aftercheck-22jun`, `slice2-strategy-control-24jun`, `tgt-retry-postmortem-24jun` (each re-verified via `git branch --merged main` + `git branch -d` safety); kept unmerged `preflight-check-21jun`. **(4) CT crash-tests CT114/127/130/132/133/135:** PARKED, NOT run — destructive operator-run scenarios (`scenario_runner.py`, `shell=True` on the real repo: CT114 `fallocate`s the disk, CT132 `rm`s a config, **CT133 `sudo systemctl stop trading-system`**, CT135 cleanup-mid-trading); they are NOT pytest tests and don't gate the suite. Off-hours sandbox-VM drill only — never on the live box. **No restart** (test fix + branch prune). Deployed after the 15:30 IST close per Rama's gate (live trades protected). Branch `hygiene-pack-24jun`. See memory `hygiene_pack_24jun`. Lesson (`tasks/lessons.md`): don't date-couple tests — derive/inject the date.
- 2026-06-24 — Claude Code (Opus) — **Slice 2: 3-layer strategy control + status table (trading-path entry gate; no schema).**
  Adds a config-driven strategy control system above the entry path. **LAYER 1** `system_config.trade_type` (INTRADAY|DELIVERY|BOTH, default INTRADAY; `core/config_loader.py` SystemConfig + validator). **LAYER 3** `strategy.enabled` (`strategies/schema.py` `enabled: bool = True`; added `enabled: true` to all 15 YAMLs). **LAYER 0** `force_intraday_only` unchanged (load-time intent→INTRADAY rewrite). **Single resolver** `strategies/control.py::strategy_will_trade(strategy, *, trade_type, force_intraday_only) -> Verdict` is the SOLE authority — drives BOTH the entry gate AND the status table so they can't disagree. **Gate (trading-path, 2 sites):** `signals/signal_processor.py` `_process_one` (~:614) + `continue_from_gate` (~:1305), after strategy resolution / before the per-strategy window check; `will_trade=False → _PipelineReject("STRATEGY_CONTROL")` BEFORE sizing. SignalProcessor gained `trade_type`+`force_intraday_only` ctor args (wired from `app_config.system` in `main.py`, which also logs a one-line `strategy_control.summary` at boot). **Product wiring UNCHANGED — the gate only REJECTS; survivors place via the existing intent→product_resolver path** → INVARIANT: default (trade_type INTRADAY + force_intraday_only true) places MIS for every will-trade strategy, never CNC. **DECISION (Rama):** ship **ALL 15 enabled:true** = TRUE zero behaviour change — found the 3 `positional_*` "delivery" strategies are trading LIVE as intraday today (force rewrites them; `positional_sector_rotation` was 24-Jun's most active, +₹8.93), so disabling them (the brief's original plan) would STOP live strategies, not be zero-change; the switch exists for future use; real delivery (CNC) deferred to Slice 2.5. **Default = 15 WILL TRADE / 0 WON'T TRADE.** **Status table** (`scripts/strategy_status.py`): full 15-row Gmail-safe HTML table (pills) + compact Telegram (names in backtick code-spans → MarkdownV2-safe) folded into the Cron Officer 09:20 briefing (new `CronReport.strategy_status`); **Type column = TRUE declared intent** (DELIVERY for positional_*, from raw `validate_strategy` pre-rewrite), **Verdict = effective post-rewrite intent (==gate)**; footnote flags delivery-as-intraday; malformed YAML → CONFIG ERROR row. **No DB schema** (config + code + display only). Tests: `tests/unit/test_slice2_strategy_control.py` (28: truth table, gate both sites, status table verdict==gate, schema, INVARIANT, regression, CNC-paper) + fixed a **Part-C regression** (`test_positional_tgt_not_equal_entry_price` hardcoded R:R 2 → now RR-aware; Part-C skipped the full suite + missed it). Branch `slice2-strategy-control-24jun`. Activates next restart. See memory `slice2_strategy_control_24jun`.
- 2026-06-24 — Claude Code (Opus) — **Part C: all 15 strategies standardized to R:R 1.5 (config-only).**
  Rama's standardization — `tgt_risk_reward: 1.5` in every `config/strategies/*.yaml`. Before: `gap_fade_long`/`gap_fade_short` already 1.5, `gap_go_long`/`gap_go_short` 2.5→1.5, the other 11 2.0→1.5 (**13 files changed, 1 line each**; `tgt_method` stays `"RISK_REWARD"` in all 15). **No schema, no code** — the R:R MECHANISM was fixed + proven in Slice 1 (22-Jun): `tgt_risk_reward` is frozen at placement into `trades.tgt_risk_reward_applied` and re-read at fill, so the broker TGT uses each strategy's own R:R (NULL→2.0 fallback won't fire now). Verified: grep (15×1.5), `StrategyLoader.load_all_strategies` loads all 15 cleanly (pydantic `>0` validator passes, each parsed `tgt_risk_reward=1.5`), 176 real-config-loading tests green (all `2.0`/`2.5` test refs are self-contained fixtures, not real-config assertions). Takes effect on the **next restart** (running app holds old YAMLs until 08:15 tomorrow). To retune one later: edit its `tgt_risk_reward` → commit → push → restart (now reaches the broker). `docs/CONFIG_GUIDE.md` §11 noted; `.docx` manual needs a manual re-SCP. Branch `part-c-rr-1.5-all-strategies-24jun`. See memory `part_c_rr_standardized_1.5_24jun`.
- 2026-06-24 — Claude Code (Opus) — **Two alerting fixes: strategy_governor lost-alert (missing severity) + 09:20 briefing false-CRITICAL debounce.**
  **FIX 1 (`capital/strategy_governor.py:149`):** `_pause_strategy` called `notifier.send(title=, body=, source_module=)` with NO `severity` (a REQUIRED keyword-only arg) → every strategy-circuit-breaker trip raised `TypeError: ...missing ... 'severity'`, swallowed by the surrounding try/except → the "STRATEGY PAUSED" alert was **silently lost** (today's only 2 TypeErrors; the Issue-3 side-finding). It was the **only** `.send(` caller missing severity (grepped all sites). Fixed `severity="WARNING"` (convention: `kill_switch` HALT=CRITICAL; a single-strategy daily-loss pause = degraded-but-operating=WARNING, like `breakeven_manager`). Never surfaced earlier: the trip path was rarely hit live + the existing test used a bare `MagicMock` notifier (accepts any args → hid the bug). Tests: `_StrictNotifier` (enforces the real keyword-only signature) + regression `test_pause_alert_dispatches_with_required_severity` (empty `calls` on old code) + severity assert on the MagicMock test. **FIX 2 (`scripts/cron_officer.py` + `core/cron_registry.py`):** the 09:20 Cron Officer briefing computed a daily **false CRITICAL** from a heartbeat-commit race — `now_time = officer.morning_briefing_time` is a FIXED 09:20 (not wall-clock), and `_classify_job` marked a heartbeat_db job MISSED the instant `now_time >= due_time` with no row, so a job firing at the 09:20 cron cluster (e.g. `capture_metrics` */5) whose heartbeat hadn't committed → transient MISSED → CRITICAL (recompute = INFO). **Chose debounce over stagger** (root-cause + timing-agnostic + no self-maintaining-crontab churn): added `OfficerConfig.miss_grace_minutes` (default 2, also in the yaml `officer:` block) + a `grace_minutes` param on `_classify_job` — a heartbeat job due within `grace` minutes of the snapshot with no row → **PENDING** (just fired), not MISSED; `build_report` passes it. A genuine miss (due > grace ago) still → MISSED → CRITICAL (test-proven). **The `officer:`-block change does NOT alter the generated crontab** (generation reads `jobs:` only; verified byte-identical → 4-way sha intact, post-receive auto-install stays a no-op). Applies to the EOD report too (harmless — EOD already fires +5 min after the last job). Tests +6 in `test_cron_officer.py`. **No schema; alerting-only — FIX 1 activates next restart, FIX 2 next 09:20 cron.** Branch `governor-severity-and-briefing-debounce-24jun`. See memory `governor_severity_briefing_debounce_24jun`.
- 2026-06-24 — Claude Code (Opus) — **SL/TGT after-check is now circuit-clamp-aware (stops false-flagging a legitimately band-clamped exit).**
  Slice-1's after-check (`order_placer._verify_exits_placed`) flagged PACEDIGITK (Tue 23-Jun) `exits_verified=0` "TGT 216.15 != intended 220.41" — the **only `exits_verified=0` trade in the whole DB**. Mechanism: the fill-recalc TGT (220.41) sat above PACEDIGITK's upper circuit, so `place_exits`→`clamp_exit_into_band` legitimately clamped the placed TGT down to the band ceiling 216.15 and placed it there; the after-check then compared placed (216.15, clamped) vs intended (220.41, pre-clamp) → false mismatch (`_exit_price_mismatch` was even designed to catch clamps). **Fix (root cause, no schema):** the clamp ceiling was already in memory — `ExitLegsResult.tgt_price`/`.sl_trigger_price` hold the clamped price — so added `sl_clamped`/`tgt_clamped` bools to `ExitLegsResult` (defaults False, set in `LimitTripleProtocol.place_exits` from `sl_res`/`tgt_res.was_clamped`) and new `sl_clamp_price`/`tgt_clamp_price` params on `_verify_exits_placed`. New `_explained_by_clamp(placed, clamp_price)` = clamp recorded AND `placed ≈ band ceiling` (same tick tolerance): a price differing from intended is excused as a NOTE ("TGT/SL clamped to circuit band X (intended Y)") with `exits_verified=1` and **no alert** ONLY when fully explained by the band; a placed price matching NEITHER intended NOR band (or no clamp) still flags **CRITICAL(SL)/WARN(TGT)** — discriminator, NOT blind suppression (the band reference is the clamp's computed output, independent of the read-back order row). **Covers SL + TGT** (a LONG's SL can be clamped UP off the lower circuit → also was false-flaggable). Wired at both LIMIT_TRIPLE after-check sites; CO path unchanged (CO-TGT never clamps, CO-SL broker-managed). **Also fixed a latent bug:** the TGT-retry after-check site passed `legs=legs` to `_verify_exits_placed`, which has no `legs` param (a TypeError masked only because that retry path was dead until 23-Jun — Issue 3). **Forensics:** clamps are rare (0/0/1/1/0 over 18–24 Jun); PACEDIGITK is the sole after-check-era case. PACEDIGITK-shaped fixture re-classifies `0→1` (historical row left as the pre-fix record; live proof = next clamped trade). **Parity:** shared path, no mode branch → paper + live identical. Tests **+8** in `test_slice1_rr_aftercheck.py` (clamp-valid TGT+SL, discriminator placed≠band, real mismatch unchanged, missing-SL-still-CRITICAL-with-TGT-clamp, PACEDIGITK 0→1, parity, end-to-end via a circuit-capable adapter). **No schema change; alert/observability path only — activates next restart.** Branch `aftercheck-circuit-cap-fix-24jun`. See memory `aftercheck_circuit_cap_fix_24jun`.
- 2026-06-24 — Claude Code (Opus) — **tgt_retry_manager crash-loop post-mortem: confirm fix + add signature-lock, crash-loop alert, /health liveness (no harm Mon/Tue).**
  `orders/tgt_retry_manager.py` (the 30s TGT re-place safety daemon) error-looped every cycle Mon 22-Jun (**840**) + Tue 23-Jun (**230**) — `TypeError: is_within_market_hours() missing 2 required positional arguments` at `:161` (`is_within_market_hours(now)`, 1 arg). The `_loop` try/except caught it each time (process never died) → **840 silent ERRORs/day, ZERO alerts, the retry net dead for two live days.** **Root cause = BORN-BROKEN, not a latent regression:** `is_within_market_hours` was *created* by FIX-169 F18 (`c303e04`, 13-Jun) with the 3-arg signature `(now_t, open_t, close_t)` — it never had a 1-arg form; `tgt_retry_manager` was written 6 days later (`6ee5b7e`, 19-Jun) calling it with 1 arg → wrong from its first commit, never worked. Invisible until first run (deploy≠restart + FIX-189 market-window exit → loop didn't run until Mon 08:15). **Fix was INCIDENTAL** — rode along as P2 inside the NOCIL clamp fix (`8812026`, 23-Jun 02:06), restarted **23-Jun 12:28** (FIX-191 resume; last cycle_error 12:28:14, fixed `started` 12:28:29); today 0 crashes, confirmed. **HARM = NONE:** no trade has EVER been flagged `needs_tgt_retry` (`needs_tgt_retry=1 OR tgt_retry_count>0` empty all-time) + zero Bug-C SL-only events Mon/Tue → the candidate query (which the crash preceded) would have returned empty regardless. **PACEDIGITK** (Tue, `exits_verified=0`) is SEPARATE (Issue-2 circuit-cap): it filled, BOTH `sl_placed`+`tgt_placed` fired (TGT WAS placed), `needs_tgt_retry=0`, closed `SL_HIT`; `exits_verified=0` = `exits_verify_mismatch` after a circuit `exit_price_clamped_to_band`. **Why no test caught the born-broken bug:** the one manager test that enables the guard (`test_manager_skips_outside_market_hours`) **mocks** `is_within_market_hours` → arg-count masked. **ADDED (regression guard):** (1) signature-lock test (`inspect.signature` must be `(now_t, open_t, close_t)`, binds the 3-arg call, rejects the 1-arg call); (2) real-call regression tests (in/off-hours `run_once()` with the REAL un-mocked guard — reproduces the crash, now green); (3) **crash-loop self-detection** in `_loop` — ≥`crash_alert_threshold` (3 → ~90s) consecutive failures fire ONE throttled CRITICAL via the notifier (sentinel→email + Telegram), re-alert ≤1/h, INFO on recovery → "dead 2 days" becomes "1 email in 90s"; (4) **`health_snapshot()` on `/health`** (`tgt_retry_provider` threaded `main.py`→`scripts/healthcheck_server.py`): `ok=False`/503 when `dead` (never-started/thread-gone) or `crash_loop`, `disabled`→ok — and pre-flight **Phase B (`engine.py`) already curls `:8080/health`**, so a boot-dead daemon now auto-surfaces as `failing ['tgt_retry']` (no engine.py change). **Monitoring before = none** (in-proc thread, not a cron, no probe); **after = self-alert + /health 503 + Phase B**. Side-finding (separate, NOT fixed): today's 2 unrelated TypeErrors = `strategy_governor` calling `TelegramNotifier.send()` without `severity` (a lost circuit-breaker ALERT, not a trade action) — one-line fix queued. Tests +11 (`test_tgt_retry` +7, `test_healthcheck_server` +4). **No schema change. Alerting/observability only — restart activates the crash-detection + /health field.** Branch `tgt-retry-postmortem-24jun`. See memory `tgt_retry_crashloop_postmortem_24jun`.
- 2026-06-24 — Claude Code (Opus) — **Cron Officer post-ban CRITICAL briefing: malformed-email leak fixed + full job list in email.**
  First post-ban day (`telegram_ban_until` 2026-06-23 lapsed) the 09:20 briefing — CRITICAL via `_compute_severity` — routed through `cron_officer._send_telegram_md` → `TelegramNotifier.send(severity=CRITICAL)`, whose `_handle_critical` **unconditionally** wrote a bare `write_critical_sentinel` (no subject/html, body = raw Telegram MarkdownV2). `alert_watcher` SMTP'd it → an ugly raw-MarkdownV2 email (the "`24\-Jun…+11 more`" blob) IN ADDITION to the Telegram message. During the ban this was masked (ban branch emailed clean HTML + skipped Telegram); first post-ban day = first leak. **Root-cause fix (Option A, backward-compatible):** `TelegramNotifier.send`/`_handle_critical` gained `write_sentinel: bool = True` — default keeps the TG5 sentinel→email for EVERY existing CRITICAL caller (main/kill_switch/reconciler/eod_squareoff/token_monitor/order_placer — all verified unchanged); `False` suppresses BOTH the sentinel and the Telegram-failure email fallback. `cron_officer._send_telegram_md` now passes `write_sentinel=False` (Telegram can no longer emit a bare sentinel), and `deliver_report` writes ONE clean HTML email (`render_briefing_html`, subject `[LFL836] …`) for the post-ban CRITICAL case (`email_backup = is_eod or ban or severity=="CRITICAL"`). Net (Rama's call): post-ban CRITICAL briefing = clean Telegram **+** clean HTML email backup; post-ban INFO/WARN = Telegram only; ban window + EOD unchanged. **Full job list:** the "+N more" cap lives ONLY in `render_briefing_telegram` (compact, phone) — the HTML/plaintext email renderers never truncated, so routing the email through `render_briefing_html` restores the full ~39-job list automatically; added a "📧 Full list → email" Telegram footer when CRITICAL. **Part 3 (why today was CRITICAL):** transient false-positive — recomputed `build_report` is INFO (today's 19 heartbeats all SUCCESS, watcher fresh, preflight READY); NOT `daily_report` (PENDING_REDESIGN never escalates), NOT a real persistent miss — most consistent with a sub-second heartbeat-visibility race at the 09:20:00 cron boundary; self-cleared (debounce = separate follow-up). Tests: +5 (write_sentinel suppress ×2 + signature-lock updated; post-ban clean-HTML / INFO-telegram-only / 39-job no-truncation); full suite **3720 pass** (1 pre-existing PC-env `test_system_manager` fail). Branch `cron-officer-email-leak-fix-24jun`. Alerting-only — no schema, no restart (next 09:20 cron uses it). See memory `cron_officer_email_leak_fix_24jun`.
- 2026-06-24 — Claude Code (Opus) — **Preflight live_test_mode_caps envelope → 5/10 + test/prod sentinel isolation.**
  **TASK 1 (commit `9919652`):** the `live_test_mode_caps` CRITICAL preflight check (`scripts/preflight/checks/config_integrity.py` → `LIVE_TEST_EXPECTED_MAX_OPEN`/`_MAX_ENTRIES`) expected the OLD **4/6**, but the caps were deliberately RAISED to **5/10** on the 23-Jun FIX-191 resume → the 08:30 preflight would fire a false "caps drift" CRITICAL daily. Updated the expected envelope to 5/10 (it's the drift-guard's expected value, NOT the live source — live reads `risk.*` from system_config.yaml). Preflight is **alert-only** (`run_on_demand_if_missed` never blocks startup) so trading was never gated. `test_preflight_groups` caps tests updated. **TASK 2 (commit `72cdfd5`):** running the full suite on the LIVE VM emailed test-written CRITICALs — the alert-watcher consumes any `critical_alert_*.flag` in the real `data_store`, and tests exercising the sentinel path without a tmp dir (`test_fix142` gemini failure path; `test_preflight` CRITICALs via `deliver.py:77`) wrote into it. New `tests/conftest.py` autouse fixture `_isolate_real_sentinels` redirects any `write_critical_sentinel` whose `sentinel_dir` resolves to `<project>/data_store` (incl. the bare `"data_store"` default) → a per-test tmp sandbox; explicit tmp dirs pass through; patches the source + module-level importers. **Production alert path unchanged.** Regression test `tests/unit/test_sentinel_isolation.py`; 250 sentinel-writer tests pass with zero real flags. 3 leaked `.delivered` moved to `/tmp/leaked_test_sentinels_24jun/`. See memory `live_test_mode_permanent` + `test-sentinel-isolation-24jun`.
- 2026-06-23 — Claude Code (Opus) — **Tick-size fail-safe snap (trading path) + gemini_log_review hardening (tooling).**
  **PART 1 — tick (commits `478673f` + `1313a08`, pushed, NO restart → activates 03:30):** `broker/zerodha_adapter._snap_order_to_tick` **failed OPEN** — a missing/zero `tick_size` (symbol absent from `instruments.csv`, e.g. HDFCSILVER/SILVERBETA) returned the price **un-rounded** → Zerodha "enter price in multiple of tick size" rejection, losing valid silver/ETF signals. Fix at the **single adapter chokepoint** (no new chokepoint): (a) new `_resolve_tick()` → `DEFAULT_TICK` (0.05) fallback on cache-None / `InstrumentNotFoundError` / tick≤0 + throttled per-symbol WARN `snap_to_tick.missing_tick_size` (FIX-170 visibility); `_snap_order_to_tick` routes through it → **never un-rounded** (SL round-past-trigger direction unchanged); (b) `modify_order` gained optional `symbol` + snaps price/trigger **before the paper branch** (parity), threaded from `breakeven_manager`/`smart_tgt_manager` (both already pre-snapped → idempotent; this centralises it); (c) **de-dup** — removed the redundant `order_placer._round_to_tick` (float-floor, fail-open) + its 3 entry calls, gated on a green no-bypass test (`_tick_for` kept for the emergency-exit marketable-LIMIT). No-bypass audit: `place_order` + `modify_order` are the ONLY two broker-submission primitives; AngelOne adapter is dormant (unsnapped — needs the same if ever live). Tests `tests/unit/test_tick_failsafe_snap.py` (13) + 2 updated FIX-181 fail-open tests; 373/325 green. **FIX-170 (instrument-universe gap) = separate follow-up** — the 0.05 fallback + alert is the bridge. **PART 2 — gemini (commit `8c80a15`, cron tooling, next run):** `_extract_warning_plus` read the OLDEST-500 W/E/C lines with no restart cutoff → stale pre-restart errors surfaced as "current" (today's tgt_retry false alarm) + a heavy morning could consume the cap before the post-restart window. Added `_process_start_ts` (cutoff = last `main.py:1388` "Trading System v… starting" marker) + restart-aware skip (lexicographic +05:30 ts; whole-file fallback) + `_emit_review_failure_alert` (full-failure-ONLY CRITICAL sentinel → alert-watcher email + Telegram WARN + "REVIEW FAILED" stub; degraded-but-completed stays quiet). Tests `tests/unit/test_gemini_log_review_hardening.py` (6). See memory `tick_snap_failopen_23jun` + `gemini_log_review_windowing_23jun`.
- 2026-06-23 — Claude Code (Opus) — **Self-maintaining cron framework ARMED (registry = executable source of truth → auto-install + watchdog; pre-receive DEFERRED).**
  Armed the BUILD 1–6 framework (commits →`600e345`, pushed; VM bare HEAD `79c70e8` after the step-4 test commit). `config/cron_registry.yaml` is the **single executable source of truth**; `scripts/generate_crontab.py --generate` deterministically emits `deploy/cron/trading-system.cron` (ASCII+LF; fixed VM-path constants, no host lookup → identical output anywhere). **Equality model:** live `crontab -l` == canonical == `generate(registry)` — verified **four-way sha256 `1469f905…b9a23`** (PC committed / PC generated / VM deployed / VM live); **41 command-lines** (40 + new `sentinel_retention` 02:05). **Pre-arm gates G1–G4 all green** (G1 `--gate` zero-drops + only-`sentinel_retention`; G2 canonical==generate; G3 hooks/watchdog exec + valid shebangs; G4 canonical ASCII+LF), re-proven **natively on the VM** at push/reconcile. **ARMED:** **post-receive** (`~/trading-system.git/hooks/post-receive`, 1336 B) regenerates from the deployed registry + auto-installs the crontab *iff* `generate==canonical` (else WARN + skip — self-protecting); replaced the old 329-B checkout-only hook; proven by empty push `79c70e8` → zero drift. **cron-watchdog** systemd timer (`/etc/systemd/system/cron-watchdog.{service,timer}`) fires **19:30 IST daily** (`Persistent=true`), asserts `cron_officer_eod` + `check_cron_drift` both heartbeated today → CRITICAL sentinel via the **cron-INDEPENDENT** path if missing; **first run Wed 24-Jun 19:30**. **DEFERRED by choice (NOT installed):** **pre-receive** equality guard (`deploy/hooks/pre-receive`) — would hard-reject a push whose canonical != `generate(registry)` (+ blast-radius bound; override `[cron-canonical-override]`); arm later via dry-run (`CRON_GUARD_DRYRUN=1` injected into the *installed* copy) → enforce; break-glass `rm` the hook. Until armed, post-receive still keeps a bad canonical off the live crontab — it just won't reject the push. **pre-commit** (`deploy/hooks/pre-commit`) = local-clone-only. NB today's `check_cron_drift` heartbeat is absent (today's 18:00 ran on pre-deploy code; the deployed `check_cron_drift.py:197-198` self-heartbeats) — benign, self-corrects 24-Jun; do NOT manually trigger the watchdog today. See memory `cron_framework_armed_23jun`.
- 2026-06-23 — Claude Code (Opus) — **Sentinel retention cron + test-pollution root-cause fix.**
  `data_store/critical_alert_*` had no retention (slow unbounded growth). Added daily **sentinel_retention**
  (02:05): `find data_store -maxdepth 1 -name 'critical_alert_*.delivered' -mtime +7 -delete` — deletes
  DELIVERED alerts >7d, **NEVER** `*.flag`; marker `cron_marks/sentinel_retention.done`; registered in
  `cron_registry.yaml` + canonical `deploy/cron/trading-system.cron` (mirrors `backup_retention`). Email is
  the authoritative record, so `.delivered` are just local processed-markers (no archive). **Assessment
  (23-Jun, VM clock confirmed 23-Jun, NTP-synced):** VM = 61 `.delivered` (104 KB, 18–23 Jun), **0 `.flag`**
  (alert-watcher healthy → no delivery bug); PC dev tree = 42 `.flag` = test-suite artifacts
  (`test_fix132_email_fallback.py` wrote real sentinels via `sentinel_dir="data_store"`), deleted (gitignored
  noise). **Root cause fixed:** `test_fix132` now uses a throwaway tmp dir for `sentinel_dir` +
  `failed_alerts_log_path` (7/7 green, 0 sentinels written to `data_store/`). `data_store/` gitignored (0
  tracked sentinels). Two commits (cron / test fix). NOT pushed — cron activates on next push +
  `crontab deploy/cron/trading-system.cron`. (VM one-time archive skipped — all <7d; cron ages them out.)
- 2026-06-23 — Claude Code (Opus) — **FIX-191: API-failure breaker is connectivity-only (false SOFT_KILL halt) + halt alert WARN→CRITICAL + resume caps + Telegram restored.**
  **Incident (live):** at 10:07 a SOFT_KILL auto-tripped on "3 consecutive API failures" and `webhook_receiver`
  403'd EVERY signal for ~2h (150 webhooks/hr, **0 accepted**) — yet the broker was fine. Root cause: the
  consecutive-API-failure breaker (FIX-069 intent = transient connectivity outage) counted **business
  rejections**. `OrderRejectedError extends BrokerError`, and `signal_processor` calls `record_api_failure(be)`
  on ANY `BrokerError` at placement → 1 broker MIS-block reject + **2 client-side slippage-guard aborts**
  (`order_placer` raises `OrderRejectedError` BEFORE any broker call) = 3-in-a-row → trip. Only the 2 morning
  fills (GARUDA, PPLPHARMA, both SL) ever placed; the rest of the day was a **false** halt (consecutive-losses
  was NOT the cause — only 1 loss had closed at trip time). **Fix (permanent, single chokepoint):**
  `KillSwitch.record_api_failure` now **WHITELISTS** transient types — counts ONLY `BrokerTimeoutError` /
  `BrokerRateLimitError`; `OrderRejectedError` (broker reject OR slippage abort), `SLUnplaceableError`,
  `ProductNotSupportedError`, generic `BrokerError` no longer count; `BrokerAuthError` still excluded (FIX-185).
  No mode branch (paper+live parity). **Halt-alert severity:** `soft_kill()` notified at `severity="WARN"`,
  which **drops silently on a Telegram send failure with no email fallback** → today's halt reached Rama through
  ZERO channels. Raised the SOFT_KILL halt notification **WARN→CRITICAL** (routing only; kill stays SOFT_KILL;
  uses the existing CRITICAL email-fallback path). Scoped — routine WARN alerts unchanged, `hard_kill` has no
  notifier send, IP-403/exit alerts already CRITICAL. **Resume caps (parity, 10/5/5):** `max_daily_trades 20→10`,
  `live_test_max_open_positions 4→5`, `live_test_max_entries_per_day 6→10`, `max_consecutive_losses 2→5`.
  **Telegram restored** — root cause = **stale bot token** (all 277 historical `failed_alerts.log` "failed to
  deliver to ['-100…']" were the dead token, not the chat_id; 0 failures today). NOTE: the `telegram_alerts` DB
  table is **vestigial** — the notifier never writes it (sends via HTTP + sentinels + `failed_alerts.log`), so
  an empty `telegram_alerts` is NOT evidence of a Telegram problem. Tests: +2
  (`test_fix191_order_rejected_not_counted`, `test_fix191b_soft_kill_halt_alert_critical_emails`); 34/34
  kill_switch + 7/7 FIX-132 email-fallback green. Commits `7ff24b2` (FIX-191+caps) + `6bed838` (WARN→CRITICAL).
  **Deployed + resumed 12:28** (bare HEAD `6bed838`; kill cleared via `clear_kill_switch.py`; caps 5/10/5
  confirmed in the running process; signals flowing). Still open: F&O/MIS ban pre-screening (TVTODAY reached the
  broker — its reject no longer trips the kill post-FIX-191, but the pre-screen gap remains a separate item).
- 2026-06-23 — Claude Code (VS Code) — **NOCIL circuit-clamp fix: placeability gate + leg-asymmetric handling + dual pre-fill reject + P2 (tgt_retry).**
  Root cause (22-Jun NOCIL): the FIX-190 Bug-D circuit clamp was **side-agnostic** — a LONG TGT recalc'd
  above the upper circuit was clamped DOWN to `upper×0.98 = 187.00`, **below** the 189.78 fill, producing an
  instantly-marketable SELL (2.35 s scratch, net −₹0.56, mislabeled TGT_HIT). Forensics: **1/77 trades**
  (NOCIL only; mechanism system-wide for LIMIT_TRIPLE, trigger geometric — entry within ~2% of a circuit).
  The wrong-side guard already existed on the TGT-**retry** path (Guard 3 + a post-clamp re-check, commit
  `6ee5b7e`) but was **never** retrofitted to the primary `place_exits` site (clamp added earlier in
  `b4cd434`) — an accidental per-call-site omission; the retry guard had **never fired in prod** (the dead
  `is_within_market_hours` TypeError, fixed here as P2). **Fix (permanent, primitive layer):**
  (1) `clamp_to_circuit_band` → `clamp_exit_into_band(... leg, direction, entry_fill ...) → ClampResult`
  (frozen; adds the leg/direction polarity check vs the fill; the **single chokepoint**, see the section
  above); (2) the 3 LIMIT_TRIPLE sites consume `.placeable` — **TGT** unplaceable → SL-only/hold (Bug-C →
  TGTRetryManager), **SL** unplaceable → `SLUnplaceableError` → emergency-close + hard_kill (traced,
  reuses existing escalation; `_is_ltp_validation_error` returns False for it); `entry_fill=avg_fill_price`
  threaded through the engine; (3) pre-fill **circuit-proximity reject** in `secondary_screener`
  (framing-b BOTH legs, `entry_gate.circuit_proximity_reject_enabled`, default true — 0 over-rejection on
  the 77-trade history); (4) **de-dup** = removed the now-redundant retry-path Guard-3 wrong-side clause +
  post-clamp place-then-cancel re-check (kept the `tgt_price<=0` computation guard). **P2:**
  `tgt_retry_manager._run_once_locked` now calls `is_within_market_hours(now.time(), MARKET_OPEN,
  MARKET_CLOSE)` (was 1-arg → TypeError every cycle, silently disabling the retry sweep). **Scope = Option
  2:** gate the 3 LIMIT_TRIPLE clamp sites; CO-TGT + G5b-SL are evidence-justified, **test-enforced**
  never-clamp exceptions (no path silently un-guarded). **No schema change.** Parity: shared code, no mode
  branch. Tests: +new `tests/unit/test_nocil_clamp_fix.py` (E.1–E.9) + extensions; touched suites **293
  passing**; full sweep **3640 passed / 3 pre-existing env failures** (`test_interactive_startup`,
  `test_state_store::…fix156`, `test_system_manager` — confirmed identical on pre-change HEAD `d79c186`,
  all time/date-of-day dependent, modules untouched). 4 commits (de-dup isolated). Activates next VM
  restart; `clamp_exit_into_band` / `ClampResult` / `SLUnplaceableError` locations in PATHS.md.
- 2026-06-22 — Claude Code (VS Code) — **SATS first-scan triage + fixes (Semgrep: 23 findings).**
  Triaged all 23 Semgrep (`p/python` + `p/security-audit`) findings: **22 confirmed false positives**
  (logger-credential-leak rules firing on booleans / env-var NAMES / masked `totp[:3]***` / account ids /
  the keyword "token" meaning *instrument_token*; dynamic-urllib on hardcoded/config/localhost URLs;
  Telegram `exc` cannot leak the bot token — `TelegramNotifier._post_with_retry` swallows all `requests`
  exceptions) and **1 real fix**: `scripts/preflight/checks/recovery.py` `_ensure_dir` chmod
  **0o755 → 0o700** (owner-only) on the `logs/` + `data_store/cron_marks/` dirs (least privilege;
  single-user VM) — commit **ab8b12e**. The file-oriented `insecure-file-permissions` rule still flags the
  *directory* chmod (suggests 0o644, which would strip the traversal bit) → a documented one-line
  `# nosemgrep` (Rama-approved) sits directly above the call. **Scan-script fixes (PC-only, gitignored):**
  both `.bat`s now set `PYTHONUTF8=1` (Semgrep/Bandit crashed writing the report under Windows cp1252);
  `scan_semgrep.bat` gained an optional **baseline** via `sats\semgrep_baseline.txt` (pinned `65439ff`)
  so future scans surface only NEW findings (the 22 reviewed FPs suppressed). Post-fix full scan = **22**,
  recovery.py = **0**. No `# nosemgrep` scattered on the 22 FPs (baseline handles them). See PC Paths →
  SATS + memory `sats_triage_22jun` / `sats_tooling`.
- 2026-06-22 — Claude Code (VS Code) — **SATS static-analysis scan scripts created (PC-only, manual).**
  New `sats\scripts\scan_bandit.bat` + `scan_semgrep.bat` and `sats\reports\` (Bandit 1.9.4 / Semgrep
  1.167.0 in the pre-existing isolated venvs under `sats\`, which is git-ignored — tools never deploy to
  the VM). On-demand only (no hooks/automation): each scans the repo root, **excludes `venv,sats,.git`**,
  writes a timestamped txt report to `sats\reports\` and echoes it (`chcp 65001`, locale-independent
  PowerShell timestamp, auto-creates `reports\`, tool run ONCE via `-o`/`--output` then `type`). Semgrep
  rulesets `p/python` + `p/security-audit` (login-free; first run downloads, cached after); Bandit `-x`
  uses absolute paths (`manager.py` matches each token as glob **and** substring). See PC Paths → SATS +
  memory `sats_tooling`. Tool venvs left untouched.
- 2026-06-22 — Claude Code — **Slice 1: fill-time R:R fix (all 4 recalc sites) + SL/TGT after-check (schema v35).**
  Deployed to `main` (commit a730c63); activates on the VM's next restart (Tue 23-Jun 08:30), when the
  live DB migrates **v34→v35**. **Bug:** the deferred-exit TGT recalc used order_placer's hardcoded
  `self._rr_ratio` (default **2.0**, never overridden in `main.py:2063-2087`) instead of the originating
  strategy's `tgt_risk_reward`; deferred exits are the ONLY TGT that reaches the broker (placement defers,
  OP-NS1), so the broker TGT was **always 2.0-based** — gap_fade (1.5) / gap_go (2.5) traded at the wrong
  R:R; FIX-013's "preserve R:R" was unmet. **Fix (A):** strategy R:R frozen at placement
  (`trades.tgt_risk_reward_applied`, carried on the ENTRY `_FillEntry`), read at fill via
  `_resolve_fill_rr()` at **all 4** recalc sites — the 3 audited + the **TGT-retry path
  (`order_placer.py` ~line 2462) the audit missed** (same 2.0 bug); NULL/recovered (pre-v35) →
  fall back to `self._rr_ratio` 2.0 **+ WARNING**. **After-check (B):** `_verify_exits_placed()` reads
  the persisted SL/TGT order rows (ground truth; parity-safe — paper+live persist the same rows) and
  checks price + qty vs intended; **missing/wrong SL = CRITICAL**, **TGT-only = WARN**, **alert-only**
  (no auto-cancel this slice); verdict in `trades.exits_verified` / `exits_verify_detail` (READ-BACK,
  unlike the write-only v34 sizing cols); CO SL is broker-managed (bracket) → TGT-only check.
  **Schema v35:** trades += `tgt_risk_reward_applied` / `exits_verified` / `exits_verify_detail`
  (rebuild-trades migration, v34 pattern; `EXPECTED_SCHEMA_VERSION` 34→35); **DB-copy gated** on the real
  live DB (34→35, 77 trades preserved, integrity ok, idempotent). 16 new tests
  (`tests/unit/test_slice1_rr_aftercheck.py`); full suite = pre-existing baseline, zero new. **Sequencing
  (Option 2):** strategy YAMLs kept at current values (gap_fade 1.5 / gap_go 2.5 / rest 2.0) for one day so
  Tuesday's live trades prove the broker honours EACH strategy's distinct R:R; **all 15 → R:R 1.5 lands
  Wed 24-Jun** after the proof. Branch `slice1-rr-fix-aftercheck-22jun`.
- 2026-06-22 — Claude Code — **`fetch_fno_ban` endpoint fix + severity downgrade (fail-open for EQ).**
  Monday 08:35 the job emailed a CRITICAL: the old JSON endpoint
  `nseindia.com/api/live-analysis-banned` is dead (**404**). New source = NSE Clearing's daily CSV
  `https://nsearchives.nseindia.com/content/fo/fo_secban.csv` (browser UA + Accept headers; verified live
  → today 1 ban: KAYNES). Parser rewritten **JSON→CSV** (`parse_secban_csv`: header date `DD-MMM-YYYY`→ISO,
  `<serial>,<SYMBOL>` rows; HTML-block-page guard; no-ban = header only). **Finding:** `is_symbol_fno_banned`
  has **no runtime consumer** (only its own test) — the ban list never gated EQ (or anything), so the old
  "F&O signals BLOCKED" + fail-closed sentinel was misleading. Failure now **WARN not CRITICAL** and
  **fail-open**: on 404/timeout/HTML/stale-date → `log.warning` + Telegram WARN (no `critical_alert_*.flag`,
  **no email**) + heartbeat + **exit 0** (Cron Officer shows done, not failed/missed). Stale CSV (date≠today)
  is WARN + not stored. `fno_ban.fail_closed` default **true→false** (kept as a knob for a future F&O era;
  ON still only ever gates F&O via the sentinel, never EQ); removed dead `min_expected_fields`. Config
  `system_config.yaml` + `FnoBanConfig` updated. Tests rewritten (CSV parser + main() WARN/fail-open/
  heartbeat/exit-0). No crontab change (same 08:35 schedule + job name). Branch `fix-fno-ban-endpoint-22jun`.
- 2026-06-21 — Claude Code — **Diary #4: tier-multiplier ON/OFF position-sizing switch (Option δ flat-Rs).**
  `config/system_config.yaml → position_sizing.enabled` (default **true = ON**, byte-unchanged
  score-tier × perf sizing). **OFF** = flat Rs/order (`flat_value_rs`, Pydantic-validated > 0):
  flat is ONE MORE ceiling atop risk/capital/concentration (those still bind), score + perf NOT
  applied, < 1 lot → BELOW_MIN skip. Switch lives in `capital/position_sizer.py calculate()` above
  any paper/live split (parity clean). **Schema v34**: `trades` += 10 sizing-audit columns
  (tier_multiplier_mode / tier_weight_applied / perf_weight_applied / flat_value_rs_used /
  qty_by_{risk,capital,concentration,flat} / binding_constraint / actual_position_value_rs);
  chosen over order_execution_log (per-leg, fill-time) — one decision per trade, mirrors the
  tolerance_source precedent. `breakdown` plumbed signal_processor → order_placer.place() →
  create_trade; v34 = trades 12-step rebuild (MIGRATION_TABLES[34]), DB-copy-tested on the real
  live DB (32→34 idempotent, data preserved, integrity ok). Mode surfaced in pre-flight Phase A
  config display + Cron Officer EOD badge. Toggle = edit `enabled` + `systemctl restart`. Monday
  22-Jun ships default ON = **zero behavioural change** (VM-verified enabled=True/flat=None).
  ~25 tests; full suite 3560 green. Commits 4c0ba43 + 3382e55 + 57d4ead. Memory
  `diary_4_tier_multiplier_switch_21jun`.
- 2026-06-21 — Claude Code — **Pre-flight check system BUILT + ACTIVATED (Monday 22-Jun = first live proof).**
  New `scripts/preflight/` — a 3-phase daily pre-market readiness check: **Phase A 08:30**
  (infra, app down), **Phase B 09:14** (engine readiness via `:8080/health`+`/metrics`+DB,
  app up), **Phase C 09:15→09:20** (passive signal-warmup watch, `--watch-sec 285`). **55
  checks / 10 groups**; **ALERT-ONLY** (exit 0 always — Rama is the gate, never auto-blocks);
  **auto-fix only on a DB/OS SAFE whitelist** (in-memory app state = alert-only). Sentinel
  `data_store/preflight/today.json`; email on CRITICAL/phase-C + Telegram outside the ban
  (reuses `write_critical_sentinel` content_type=html + `cron_officer._send_telegram_md`).
  **Cron Officer morning briefing now embeds a pre-flight banner** (reads the sentinel;
  NOT_RUN/stale → briefing CRITICAL). **premarket_healthcheck SUBSUMED by Phase A** (5
  checks ported + parity-tested; removed from cron+registry; script kept w/ deprecation
  header, delete after Mon+Tue proof). **Schema v33** (+preflight_runs / preflight_check_results
  / preflight_autofix_log; pure additions, no MIGRATION_TABLES; DB-copy tested on the real
  64MB live DB: 32→33 idempotent, data preserved, integrity ok; live DB migrates at the
  Monday restart). `main.py` startup hook runs Phase A/B on-demand if a 06:00-09:20 restart
  missed the cron slot. Cron reinstalled (`crontab -l | diff`=0, 3 preflight lines live, 13
  markers); snapshots in `data_store/cron_audit/crontab_{pre,post}_install_preflight_21-Jun-2026.txt`.
  ~150 preflight tests; 116 cron tests unchanged. Commits 165c845 (schema) + d1355e8 (cron+hook)
  + the preflight feature commits. Memory `preflight_system_21jun`. NB Monday is the live proof;
  full SYSTEM_MAP cron-table rows + CONFIG_GUIDE update land post-proof.
- 2026-06-21 — Claude Code — **Cron Officer revision ACTIVATED — crontab reinstalled (the held step done).**
  Rama signed off the HTML samples (clean EOD / morning briefing / Fri-19-Jun dry-run) + Bug A/B/C
  dispositions. **Pre-activation safety:** `bash -n deploy/cron/trading-system.cron` clean; marker-tail
  safety proven — a broken/unwritable `data_store/cron_marks` dir does NOT alter the job's captured exit
  (the job runs first, `rc=$?` captures it, the `; mkdir -p … ; echo > …done` tail is append-only and
  after `;`); the 3 backup lines (db_backup/analytics_backup/backup_retention) preserve the exit code with
  the marker write strictly AFTER completion. VM snapshots saved:
  `data_store/cron_audit/crontab_{pre,post}_install_21-Jun-2026.txt` (named for the actual install date —
  the runbook said 20-Jun, but activation slipped to Sun 21-Jun). **Reinstall:**
  `crontab deploy/cron/trading-system.cron` (VM file md5 == local `3c6871a8…`) → **`crontab -l | diff` = 0**,
  **10** exit-code marker lines present, new times live (**briefing 09:20 / EOD 18:50 / system_manager
  18:45**), old **04:55/18:30 removed**, `cron_marks/` pre-created. Also untracked the accidental
  `reports/system_manager/2026-06-19.txt` sweep + gitignored `reports/{system_manager,cron_officer}/`
  (commit **f8edc4a**, pushed → VM tree redeployed). **Sunday 21-Jun (holiday)** suppresses the real
  morning/EOD reports (expected) — only passive markers fire (disk_monitor hourly, gemini_weekly_patterns
  18:00). **Monday 22-Jun = first live proof** (09:20 briefing email + 18:50 EOD email; Telegram still
  banned → `[LFL836-BAN]` subject; **Tue 23-Jun** Telegram auto-resumes and the prefix drops). Registry +
  officer code were already in 3dd72b0. Memory `cron_officer_revision_20jun` updated → ACTIVATED.
- 2026-06-20 — Claude Code — **Cron Officer revision — Phase 2-6 (full visibility + Bug A/B fixes + rich
  email/Telegram).** Commit 3dd72b0 (pushed; **crontab reinstall + docs PENDING Rama's dry-run sign-off**).
  EXTENDS `config/cron_registry.yaml` (no fork). **Phase-1 finding:** the 7 "missed" on 19-Jun were **3
  permanent bugs + 4 environmental** (FIX-189 morning, already fixed); registry was already 33 jobs, "20"
  was the EOD *expected* subset. **Bug A** registry key `gemini_data_integrity` → **`gemini_data_integrity_check`**
  (matches the heartbeat the script records — 0 heartbeats under the old key ever; cron file needed no
  change). **Bug B** `cron_officer_eod` records a `STARTED` heartbeat BEFORE building the report → stops
  self-reporting missed. **Bug C** (`daily_report` has no heartbeat) DEFERRED → shown **⏸ Pending** (never
  CRITICAL; lands with the xlsx redesign). **Schema:** `category` DERIVED from `cadence` (single source of
  truth, no 33-job duplication) + `heartbeat_required`/`detection_method`/`excluded_reason` (defaults
  derived) + `OfficerConfig` (`officer:` block: day-window 00:00-23:59, morning **09:20**, EOD floor 18:45
  +5min = **18:50**, `telegram_ban_until: 2026-06-23` auto-clears). `build_report` classifies **EVERY** job
  due today by detection method: `heartbeat_db`, or `exit_code_file` (a marker the cron line appends —
  `; rc=$?; … > data_store/cron_marks/<name>.done`; missing → **NO_SIGNAL**, never a false CRITICAL), or
  `none`. **Rich delivery** (`scripts/cron_report_render.py`, pure/tested): HTML email (Gmail-safe inline-CSS
  tables — severity banner, stat cards, progress bar, status pills, per-task table, change-log, excluded,
  footer) + plain mirror + Telegram MarkdownV2 (`escape_md_v2`). `alerts/critical.py` + `alert_watcher._build_email`
  extended for `content_type=text/html` → multipart/alternative (fail-fast w/o `plain_fallback`) + verbatim
  `subject` — **backward-compatible** (no content_type → plain, unchanged). EOD always emails; morning
  briefing emails during the ban, else Telegrams; subject carries severity emoji + **`[LFL836-BAN]`** in the
  ban window. New CLI `--force-dry-run` / `--as-of-date`. **Verified** (dry-run on the real VM DB, as-of Fri
  19-Jun): **17 done / 3 real-missed / 1 ⏸ pending** (vs old "12/20, 7 missed"); HTML samples in
  `reports/cron_officer/`. +20 tests; 116 cron/alert green; legacy `build_eod_summary`/`build_briefing` kept.
  See memory `cron_officer_revision_20jun`. NB **ACTIVATED 2026-06-21** — the live crontab now == the
  canonical file (09:20/18:50; `crontab -l | diff`=0); see the 2026-06-21 entry above.
- 2026-06-20 — Claude Code — **Slippage overrides ship FULLY EMPTY (commit 8fd54a8, follow-up to 2ce54ab).**
  Rama's call: set `by_price_band` to `{}` too (was the lone example `0-100: 0.18`) → every entry resolves to
  the global **0.22** baseline everywhere for clean data collection first; add per-symbol/strategy/band
  overrides later from real evidence. Verified live on the VM (`load_all` → all maps `{}`). The v30→v32
  migration was confirmed clean on a copy of the real DB (63 trades preserved). Docs/test updated.
- 2026-06-20 — Claude Code — **Slippage tolerance override hierarchy — Phase 3a (MANUAL, schema v32).**
  Lets Rama set per-symbol / per-strategy / per-price-band entry-slippage tolerances NOW (from trading
  knowledge), without waiting for the Phase-3b auto-recommender. New `entry_gate.slippage_control.overrides`
  block (`enabled` + `by_price_band` / `by_strategy` / `by_symbol` maps, **all ship EMPTY → pure global
  0.22 baseline everywhere** for clean data collection first). The effective `sl_fraction` is resolved **MOST-SPECIFIC-WINS: Symbol > Strategy > Price
  Band > Global** by the pure `resolve_slippage_fraction()` (orders/order_placer.py), threaded into
  `_slippage_decision` via a new `fraction_override` arg → the override drives the pre-order abort. Resolved
  up-front in `place()` (band from `signal_trigger_price`/`entry_price` via `get_price_band`) so it both
  enforces the guard AND is recorded. **Transparency:** `tolerance_source` (e.g. `symbol:IDEA` /
  `strategy:gap_fade` / `band:0-100` / `global`) + the fraction are logged on every entry
  (`entry_slippage_observed` + `slippage_guard_exceeded` + the Telegram abort) and **persisted**:
  **schema v32** adds `tolerance_fraction_used` (REAL) + `tolerance_source` (TEXT) to **trades** (written by
  `OrderManager.create_trade`) and **order_execution_log** (the async `slippage_recorder` copies them from
  the parent trade onto the execution row). `MIGRATION_TABLES[32] = [trades, order_execution_log]` rebuilds
  both (new cols → NULL); `EXPECTED_SCHEMA_VERSION` 31→32. **Validation:** config hard-rejects fractions
  outside `(0,1]`; `validate_slippage_overrides()` warns at startup on extreme values (<0.05/>0.50) and on
  `by_symbol`/`by_strategy` keys that match no known instrument/strategy (typo → silently ignored). **System
  Manager EOD: 10th check** `SLIPPAGE OVERRIDES` (visibility-only — never a violation / never SOFT_KILL):
  active override counts + per-`tolerance_source` placed/rejected usage today. **Parity:** mode-agnostic,
  paper + live. Only `sl_fraction` mode consults overrides (flat_tiers/pct unchanged). Closes the loop:
  Phase 1 records WHAT happened, Phase 3a records WHICH rule applied → Phase 3b can later analyse
  effectiveness (join `trade_slippage_log.rr_damage_pct` on `trade_id`). **+v31→v32 migration verified on a
  built DB (DROP-COLUMN-simulated v31 → migrate → cols added, rows preserved, round-trips).** +27 tests
  (control 20 / recorder 1 / migration 1 + version-pin fixes); 367 affected tests green. CONFIG_GUIDE.md +
  slippage_intelligence.md updated. Activates next restart. Commit 2ce54ab.
- 2026-06-20 — Claude Code — **Slippage intelligence Phase 1 — raw data layer (schema v31).** Permanent
  execution-intelligence tables (RAW facts only; analytics computed on-demand in Phase-2 reports — NO
  aggregate/stale tables). **v31** adds 3 append-only tables to the MAIN DB: `order_execution_log` (per
  filled leg), `trade_slippage_log` (per trade, incl. **`rr_damage_pct`** = % of the planned risk budget
  execution ate — the key metric), `market_execution_context` (Priority-2, nullable bid/ask/spread).
  `EXPECTED_SCHEMA_VERSION` 30→31 (pure additions; `CREATE IF NOT EXISTS` + executescript creates them,
  no rebuild). New `orders/slippage_recorder.py` `SlippageRecorder` subscribes **ASYNC** to `OrderFilled`
  (→ order log + context) and `PositionClosed` (→ trade roll-up) — async dispatch means the EventBus
  logs but NEVER re-raises handler exceptions, so recording is **fully decoupled and can never block
  trade execution** (handlers also try/except; inserts best-effort/never-raise). Pure tested helpers:
  adverse/favourable slip (adverse=positive), `calc_rr_damage_pct` (Rama's example 20%), `get_price_band`,
  `build_trade_slippage_row`. Wired in main.py (construction wrapped). `slippage_bands` config (default
  0-100..1000+). **Parity:** records paper + live (events fire in both). **Verified live: v30→v31
  migration runs cleanly on a copy of the real DB** (3 tables created, 63 trades intact). +19 tests; 156
  migration/state_store/config tests pass. Queries + roadmap in `docs/slippage_intelligence.md`. Commit
  71d4186. Activates next restart (migration at StateStore init). Phase 2 (reports) + Phase 3 (adaptive
  engine: Global→Band→Strategy→Symbol) pending.
- 2026-06-20 — Claude Code — **Slippage tolerance → %-of-SL-distance (replaces today's flat-Rs tiers).**
  Calibration investigation (`slippage_calibration_data_20jun`) found **SL is fixed (signal-based) and
  TGT recalcs from the fill (FIX-013)** ⇒ entry slippage directly inflates the risk budget. Rama's model:
  cap slippage as a FRACTION of the SL distance. `entry_gate.slippage_control` (replaces `slippage_tiers`),
  mode-selectable: **sl_fraction (DEFAULT)** `tolerance = min(SL_dist × max_slippage_fraction(0.22),
  absolute_cap_rs(₹5))` — auto-scales with price AND strategy SL%; `flat_tiers` (today's) + `pct` kept for
  A/B; **`hard_max_slippage_rs(₹10)`** absolute ceiling in every mode; `also_apply_pct_check`
  belt-and-suspenders; disabled → legacy flat %. `SL_dist = |signal_trigger − sl_price|` (both available
  in `place()`; cap-only if SL missing). New `_compute_slippage_tolerance` / `_slippage_decision`
  (order_placer, pure/tested); guard uses them; the calibration log now records
  `sl_distance_rs`/`tolerance_rs`/**`fraction_of_sl_used`** per entry (tune 0.22 from real numbers).
  **Step 7.3 (LIMIT=signal+tolerance) deliberately NOT done** — it's a no-op when the guard passes (the
  `release_ltp` cap dominates), risks NON-FILLS when planned-entry ≠ trigger, and doesn't fix the CO/SL
  gap; the pre-order ABORT is the safe enforcement. Verified live (all 5 spec examples: THELEELA
  ₹3.10 > ₹2.12 ABORT, etc.). **FINDING:** LLOYDSENGG (₹0.43 = 26% of its 2% SL) ABORTS at 0.22 (spec
  Step 9.6 assumed flat_tiers) → raise `max_slippage_fraction` to ~0.27 to allow it. +18 tests; 137 pass.
  Commits a08901e + 3ddb774. Activates next restart.
- 2026-06-20 — Claude Code — **Tiered entry-slippage abort (per-price-band, in RUPEES).** [superseded
  same day by the %-of-SL model above; flat_tiers retained as a mode] Augments the
  flat `entry_gate.max_entry_slippage_pct` (1%) with a tiered Rs tolerance by price band, calibratable in
  config without code changes. The pre-order guard (`order_placer.place()`, the FIX-128 LTP-vs-trigger
  check) now aborts when `|LTP − signal_trigger|` exceeds the band's `max_slippage_rs`. Starter bands
  (Rama to calibrate over live days): **<100→₹1.00, 100-200→₹1.25, 200-500→₹2.00, >500→₹3.00** (lower
  bound EXCLUSIVE → 100.00 is in the 100-200 band). **THELEELA (trigger 481.50, slip ₹2.60) now ABORTS**
  (200-500 band tol ₹2.00; it passed the old flat 1% = ₹4.81). `also_apply_pct_check: true` keeps the
  flat % as belt-and-suspenders; `enabled: false` → flat % is the sole gate (backward-compatible). New
  `tier_slippage_tolerance_rs` (orders/price_math.py) + `_slippage_abort_reason` (orders/order_placer.py)
  + `EntrySlippageTiersConfig`/`SlippageTier` (config_loader, `extra="forbid"`) + the
  `entry_gate.slippage_tiers` block in system_config.yaml. Every entry logs `entry_slippage_observed`
  (Rs+pct) for calibration; an abort logs `slippage_guard_exceeded` + a WARNING (Telegram + email
  fallback). **Parity:** runs in `place()` for paper + live; config is mode-agnostic. Verified live
  (config loads; ₹2.60 → abort, ₹1.50/₹2.80-at-600 → allow). +18 tests. Commit d464f33. Activates next
  restart; abort-only (safe direction), editable. NB the system_config.yaml hash change → the
  security-watcher emits one WARNING "system_config changed" (expected for a config deploy).
- 2026-06-20 — Claude Code — **Copy approval is Telegram-INDEPENDENT (verified) + email fallback.**
  Rama's check: does VM→PC copy work with Telegram banned (IN, until 23-Jun)? **YES** — `request-copy`
  writes the token to a LOCAL file (+ audits `copy_audit.log`) BEFORE `send_alert`, and the gate
  decision is purely local (time-lock/switch/session/token); `send_alert` is best-effort and never
  blocks. Proven live (token granted + copy succeeded, Telegram down). One gap fixed:
  `copy_gate.send_alert` INFO "token issued" / WARNING denials were Telegram-only → now an **email
  fallback** writes a sentinel (→ alert-watcher email) for ANY tier when Telegram did NOT deliver
  (`result.sentinel_path`/`success`), with `context.severity` keeping the subject correctly labelled
  (INFO/WARNING) and NO email spam when Telegram is up (delivered → skip); CRITICAL still exactly one
  email. Security CRITICALs (bypass / protection-disabled, via `security_monitor._send`) already
  emailed. Verified live (deployed `send_alert` writes an INFO-labelled sentinel). +4 tests. Commit
  cd85994.
- 2026-06-20 — Claude Code — **Copy protection: HARD-BLOCK ACTIVATED** (was detection-only). Rama's
  go-ahead, done at 14:00 Sat (markets closed, service down — safest window). Pre-checks confirmed NO
  cron / post-receive hook / systemd unit invokes `scp`/`sftp`/`rsync`, real binaries present, PATH puts
  `/usr/local/bin` before `/usr/bin`. `bash deploy/security/install_copy_protection.sh` symlinked
  `/usr/local/bin/{scp,sftp,rsync}` → `deploy/security/bin/copy-guard` + `/usr/local/bin/request-copy`.
  **Verified live:** `which scp`→wrapper; no-token `scp` → `DENIED (NO_TOKEN)` exit 1, real scp never
  executed; `request-copy` → 15-min token → `scp` exit 0, copy succeeds (real binary reached); token
  revoked. **git push (this commit) + interactive ssh confirmed unaffected** (ssh is not wrapped).
  Fail-safe = block-on-error. Time-lock 18:00–08:00 absolute (unit-tested; not live-testable at 14:00).
  Audit: no-token denials are logged (not alerted — normal case); `request-copy` issuance + TIME_LOCK/
  SESSION denials alert; raw-scp bypass still → CRITICAL via auditd. **Rollback:**
  `bash deploy/security/install_copy_protection.sh --uninstall`. Memory `vm_security_phase2`.
- 2026-06-20 — Claude Code — **Kite IP-403: actionable alert (headless self-recovery, no halt).**
  Investigated the "morning manual resume" blamed on the IP-allowlist 403. Deep trace: **post-FIX-185 a
  pure IP-403 ALREADY self-recovers** — `place_order` 403 → `PermissionException` → `BrokerAuthError` →
  `signal_processor` rejects the signal (SP12) + `record_api_failure` ignores it (FIX-185, never trips);
  reads are NOT IP-gated so `order_monitor`/`order_reconciler` never hit their 3×-auth escalation and the
  token is never invalidated → the next signal after the IP is fixed just succeeds. **No halt, no
  restart, no token invalidation.** So the proposed `token-watcher` restart-retry + a token-invalidation
  guard were **moot**; the only real gap was the operator not being told WHAT to fix. Added that:
  new `broker/auth_recovery.py` (`classify_broker_auth_error` IP-vs-token-vs-unknown; `get_public_ip`;
  `build_ip403_alert_body`) + `KillSwitch.record_api_failure()` now fires **ONE CRITICAL alert/hour** on
  an IP-allowlist 403 with the VM's **current public IP + exact Kite steps** (via the injected notifier →
  its CRITICAL path writes the sentinel → **email**, the live channel while Telegram is banned). Throttled
  1/hr (None sentinel = first always fires), runs outside the lock, best-effort; **FIX-185 preserved**
  (still never trips). **Verified live:** `get_public_ip()` → `161.118.187.249`; classification + alert
  body render correctly. Parity-safe (mode-agnostic). +13 tests. Commit 7126034. (See
  `kite_ip_allowlist_dependency` — the Kite-console IP update remains Rama's external step; this just
  makes the system TELL him the IP.)
- 2026-06-20 — Claude Code — **Kill switch: HEADLESS prior-day auto-clear + `KILL_AUTO_CLEARED` audit.**
  Rama's decision: the system ALWAYS starts headless — EVERY prior-day kill auto-clears at next-day
  startup regardless of type (scheduled, emergency, HARD_KILL, loss-limit, System Manager EOD); the
  safety net shifts from "block startup" → EOD report analysis (Task B, later). Investigation
  (`softkill_investigation_20jun`) found this already worked: **`clear_stale_state()`** (FIX-127,
  `main.py:1439`) already clears ALL prior-day kills and **`auto_clear_scheduled_kill()`** (FIX-154,
  `main.py:1444`) clears same-day *scheduled* kills when flat. This commit **solidifies** the guarantee
  (explicit docstring) and adds the audit bridge: new `KillSwitch._record_cleared_kill()` writes a
  `system_events` row (`event_type=KILL_AUTO_CLEARED`; details JSON: previous_state / reason /
  triggered_by / triggered_at / classification `scheduled|emergency` / cleared_via) on every auto-clear.
  Same-day kills still persist within the day (the `triggered_date >= today` guard is unchanged →
  loss-limit/HARD_KILL stay active intraday). The System Manager "SOFT_KILL for tomorrow" is dated its
  RUN time (`now_ist`, never post-dated) so it clears as prior-day. **Monday 22-Jun: the live kill
  (`circuit_breaker_force_close_15:15` from Fri) auto-clears at 08:30 — NO `resume.sh` needed** (proven
  against a DB copy: → INACTIVE + audit row, classification=scheduled). NB the morning manual-resume
  Rama hits is a *same-day* emergency re-trigger (Kite IP-403 `BrokerAuthError` → HALT exit 4), NOT the
  scheduled kill — a separate token/IP issue. Parity-safe (kill switch is mode-agnostic). +4 tests; 42
  kill_switch tests pass. Commit a60868f.
- 2026-06-20 — Claude Code — **VM Security Manager Phase 3 (EOD integration) + CRITICAL email dedupe.**
  **(A) Double-email fix** — `security_monitor._send` + `copy_gate.send_alert` used to write a sentinel
  explicitly AND let `TelegramNotifier.send()` write one (TG5) → 2 emails per security CRITICAL. Now
  they capture `send()`'s `SendResult` and write their own ONLY when `send()` did not
  (`result.sentinel_path is None` — notifier missing / Telegram disabled via the master switch / send
  raised), so each CRITICAL = **exactly one email**, and email still fires when Telegram is unavailable
  (the sole channel while Telegram is banned to 22-Jun). **(B) System Manager 9th check**
  `security_check` (18:45, after tomorrow_readiness): (1) daily copy-audit summary from
  `data_store/security/copy_audit.log`; (2) a `COPY_BYPASS_DETECTED` / `COPY_PROTECTION_DISABLED` today
  is a **VIOLATION** → escalates the EOD report to CRITICAL (→ email) + exit 3, **but never trips the
  trading SOFT_KILL** (security ≠ trading-safety halt); (3) health: auditd `copy_attempt` rules loaded
  (`sudo -n auditctl -l`), `copy_protection.enabled`, audit-log writable, **security-watcher alive**
  (`security_state.json` freshness). `security_monitor` now persists freshly-alerted bypass/switch
  findings to `copy_audit.log` (single EOD source; post-dedup only → no per-pass bloat). **(C) Cron
  Officer** `--eod-summary` appends a **security-watcher liveness** line and escalates to CRITICAL if
  the watcher is stale/down (`build_eod_summary` signature left intact). **Verified live on the VM:**
  the 9th check renders 0 violations with `3 auditd rules / enabled / watcher alive (42s)`, and the
  Cron Officer prints the watcher line. +15 tests (`test_system_manager_security.py`,
  `test_cron_officer_security.py`, `_send` dedupe + copy-audit persistence in `test_security_monitor.py`).
  Commits 202b98f (A), bb351e0 (B+C). NB pre-existing `test_system_manager.py::test_report_integrity_*`
  fails on any day ≠ 2026-06-19 (date-hardcoded; verified via stash, not a regression). Memory
  `vm_security_phase2`.
- 2026-06-20 — Claude Code — **VM Security Manager Phase 2 (copy protection) — BUILT (code in git;
  OS activation staged).** Controls **VM→PC** copying. New `scripts/copy_gate.py` = policy engine
  (strict priority: **TIME-LOCK 18:00–08:00 IST is absolute** and overrides even the OFF switch / a
  live token › master switch OFF = unrestricted › session-cap › a valid 15-min token) + atomic token
  store (`data_store/security/copy_token.json`) + append-only JSON-lines audit
  (`data_store/security/copy_audit.log`). New `scripts/request_copy.py` (`request-copy <reason>`,
  `--status`, `--revoke`) mints a 15-min token, audited + Telegram. `deploy/security/bin/copy-guard`
  (symlinked as `/usr/local/bin/{scp,sftp,rsync}`) consults the gate before exec'ing the real
  binary — **hard-blocks VM-INITIATED copies**, fail-safe (blocks if the gate errors); nothing
  automated on the VM uses these clients, and git/ssh are unwrapped so no lockout. `security_monitor.py`
  gained **2 checks** (now 9): (8) `copy_protection` ON→OFF transition → CRITICAL (who/when), (9) auditd
  `copy_attempt` bypass — an outbound scp/sftp/rsync run with no token → CRITICAL (conservative: skips
  inbound `scp -t` sinks so PC→VM pushes don't false-fire). `copy_protection:` block added to the
  standalone `config/security.yaml` (NOT system_config.yaml); auditd execve rules added to
  `deploy/security/trading-security.rules`. **Known limitation:** a PC-INITIATED *pull* (PC is the
  client; VM sshd serves it) can't be hard-blocked from the VM without risky `ForceCommand`/subsystem
  changes on the live trading VM → **detect+alert only** (and modern scp's sftp-subsystem path may
  evade the execve rule; reliable pull-detection needs the deferred sftp `-l INFO` logging). 52
  unit tests green (`test_copy_gate.py` + extended `test_security_monitor.py`); CLIs smoke-tested
  end-to-end (deny→issue→allow→revoke lifecycle in the audit log). **DETECTION-ONLY ACTIVATED on the
  VM 20-Jun** (Rama's choice): auditd `copy_attempt` rules installed (`/etc/audit/rules.d/`,
  `augenrules --load`; the `b64` rule confirmed capturing `scp` execve on this **aarch64** VM) + the
  two monitor checks verified live (real `ausearch -i` parsed, 0 false-positives on a benign `scp`).
  **Blocking wrappers deliberately NOT symlinked** → Rama's `scp` workflow is unchanged; run
  `bash deploy/security/install_copy_protection.sh` later to add hard-block. **Follow-up commit
  7129b38** fixed two findings surfaced by the live aarch64 box: real `ausearch -i` uses a **2-digit
  year** (parser now accepts 2-/4-digit) and `ausearch` **blocks on stdin** (now `stdin=DEVNULL`,
  query uses the locale-proof `-ts recent`). Phase 3 (System Manager EOD integration — a 9th
  System-Manager security check reading the copy audit log) still pending. Memory `vm_security_phase2`.
- 2026-06-19 — Claude Code — **VM Security Manager Phase 1 (monitoring + alerts) — built + deployed.**
  Alert-ONLY (never blocks; key-only SSH is the gate). New `scripts/security_monitor.py` (7 isolated
  checks: new/changed authorized_keys, non-whitelisted sudo, failed-login spike, NEW successful-login
  IP, sensitive-file content-hash change, active-session count, root-probe spike), `config/security.yaml`
  (standalone — system_config.yaml is `extra="forbid"`), `deploy/systemd/security-watcher.service`
  (`Type=simple` periodic ~60s), `deploy/security/jail.local` (fail2ban) + `trading-security.rules`
  (auditd), 15 unit tests. **Live on VM:** installed fail2ban+auditd; auditd 10 file-watches loaded;
  fail2ban sshd jail running (pre-tested — 0 legit/management IPs in the bannable set; bans rare as
  the botnet is low-per-IP); security-watcher enabled+active; baseline captured; **SSH access verified
  intact at every step**. Whitelist path bug (fail2ban-client/auditctl bin↔sbin) found via the live
  first pass + fixed. NOT done: SSH hardening (needs Rama OK), copy-protection (P2), EOD integration
  (P3). Commits a701b0d→fcfa9a8. Memory `vm_security_phase1`.
- 2026-06-19 — Claude Code — **Cron env-export fix (ROOT CAUSE) + reconcile_positions creds (Item A)
  + per-episode drift logging (Item B).** Investigating the 15:45 `reconcile_positions` FAILED revealed
  a deeper root cause: cron jobs ran **without any `.env` secrets** — `.env` is bare `VAR=value` (no
  `export`), so `. ./.env && python` sets shell vars the child Python never inherits (proven on VM:
  all secrets MISSING under the cron pattern, SET with `set -a`). FIX-189 made jobs *run*; they still
  got no secrets. **Fix:** every Python cron line now sources via **`set -a && . ./.env && set +a`**
  (allexport) — `deploy/cron/trading-system.cron` (29 lines) + `config/cron_registry.yaml` header +
  `TestCronEnvExport`. Crontab **reinstalled** on VM (backup in `data_store/crontab_backups/`); probe
  confirms `ZERODHA_API_KEY_LFL836`/`TELEGRAM_*` now reach Python; all 5 broker-crons wrapped.
  **Item A:** `scripts/reconcile_positions.py` read never-set generic `ZERODHA_API_KEY`/`ACCESS_TOKEN`
  → now mirrors `refresh_instruments` (`_resolve_credentials`: per-account `api_key_env` +
  `access_token` from token JSON via `load_token`; `--account` default LFL836, generic fallback). Cron
  passes `--account LFL836`. **Verified end-to-end on VM: exit 0 + heartbeat SUCCESS** (was exit 1 /
  FAILED). **Item B:** `order_reconciler._g3_capital_drift` logged a `reconciliation_log` row every 15s
  cycle even while the alert was throttled (144 rows for 3 alerts on 19-Jun) → now per-EPISODE
  (`_drift_episode_active`): a row only on episode-start or a real alert; throttle unchanged. Tests:
  130 affected unit tests green (cron_registry 23, reconcile 25, order_reconciler 82). Commits bff0cad
  (env-export+A), 89c0c20 (B). Item A activates Monday 15:45; env-export already live; Item B next restart.
- 2026-06-19 — Claude Code — **Doc: `docs/system_manuals/*.docx` location recorded.** Three Word
  manuals (`trading_System_v2_runbook.docx`, `config_file_guide.docx`,
  `chartink_mounted_strategies.docx`) live in `docs/system_manuals/`. **Gitignored** (added
  `docs/system_manuals/*.docx` to `.gitignore`, commit 2424a21) → NOT carried by `git push`;
  synced to the VM out-of-band via `scp` (verified byte-identical + valid docx). Added to Related Docs.
- 2026-06-19 — Claude Code — **TASK #8: `docs/CONFIG_GUIDE.md`** — comprehensive Rama-facing
  config reference. Audited all config files (`system_config.yaml`, `scoring_weights.yaml`,
  `broker_costs/limits.yaml`, `slippage_model.yaml`, `scan_webhook_map.yaml`,
  `chartink_scanners.yaml`, `cron_registry.yaml`, `accounts.csv`, `strategies/*.yaml`, `.env`).
  12 sections + a "Currently Effective Values" quick table, an **Override Precedence** section
  (live_test_mode > base caps; dual daily-loss; smallest sizing cap wins; force_intraday_only),
  Common Scenarios with edit+restart commands, Dangerous-Changes warnings, Quick Commands.
  Key correction documented: **`mode` (paper/live) is the `--mode` CLI flag in the systemd
  ExecStart, NOT a YAML key** — and **config edits require a restart** to take effect.
  Cross-referenced under Related Docs. Docs-only.
- 2026-06-19 — Claude Code — **Task: standalone TGT retry mechanism** (closes the
  "TGT fails forever" gap left by FIX-190 Bug C — the 19-Jun THELEELA TGT that failed
  at 10:00:28 and was never retried). When a LIMIT_TRIPLE TGT can't be placed but the
  SL is live (Bug C SL-only), `order_placer._persist_sl_only_protected` now flags the
  trade (`trades.needs_tgt_retry=1`, new schema **v30** columns: needs_tgt_retry /
  tgt_retry_count / tgt_last_retry_at — auto-migrates v29→v30 on restart). New
  `orders/tgt_retry_manager.py` `TGTRetryManager` (30s daemon, started after smart_tgt,
  stopped in `_shutdown`) re-attempts the TGT on exponential backoff
  (**30/60/120/240/480s, give up after 5** → position stays SL-protected) via
  `OrderPlacer.retry_tgt_for_trade` → `FullEntryEngine.place_deferred_tgt_only` →
  `LimitTripleProtocol.place_tgt_only` (re-clamps to the CURRENT circuit band each try —
  Bug D; the band may relax). Guards: SL must still be standing (never place a naked
  TGT), no double-TGT (idempotent), never place an unprofitable TGT, skip while the kill
  switch is active or outside market hours. The placed TGT is registered in `_fill_map` +
  order_monitor so a fill triggers the software OCO (cancels the SL). Telegram INFO on
  success / WARNING on give-up. Config `tgt_retry:` (enabled/poll_interval_sec/max_attempts/
  backoff_base_sec; optional, defaults reproduce the schedule). State lives in the trades
  table → survives restart. 18 tests in `test_tgt_retry.py`; 454 affected-suite tests green
  (v29→v30 migration verified live). Activates on next restart. Files: `orders/tgt_retry_manager.py`
  (new), `orders/order_placer.py`, `orders/order_protocol_limit.py`, `orders/full_entry_engine.py`,
  `core/schema.sql`, `core/migrations.py`, `core/state_store.py`, `core/config_loader.py`,
  `config/system_config.yaml`, `main.py`.
- 2026-06-19 — Claude Code — **Task 4: reconciler resolves trades stuck in EXITING**
  (closes the `followup_reconciler_exiting_gap` exposed by the 19-Jun incident). A
  HARD_KILL / emergency flatten marks a trade EXITING before flattening (Bug A, FIX-190);
  if the process dies mid-exit the trade lingered in EXITING with locked capital + orphan
  SL/TGT, and CHECK1/G5b never touched it (they only see OPEN/PARTIAL/PENDING_FILL) — the
  morning incident needed a manual EXITING→OPEN flip. New `_check_stuck_exiting` runs each
  reconcile cycle (startup via `reconcile_once()` + every 15s) inside the broker-positions
  guard: for EXITING trades older than `reconciler.stuck_exiting_timeout_minutes` (default
  **30**), **flat at broker → CHECK1 finalize (CLOSED_MANUAL + release capital + cancel
  orphans)** — `mark_trade_manually_closed` now accepts EXITING; **still holding → revert
  EXITING→OPEN** (`revert_exiting_to_open`) + WARNING so SL/TGT/EOD/kill-switch resume
  management. Fresh EXITING (active exit) is left alone. CHECK2 guarded so a held EXITING
  position is not mis-adopted as an orphan. New `state_store.get_stuck_exiting_trades()` +
  `revert_exiting_to_open()`; EXITING→OPEN added to the crash-test state-machine validator;
  config key `stuck_exiting_timeout_minutes`. 6 new tests (incident replay flat→CLOSED_MANUAL,
  fresh-not-touched, stale-held→OPEN, + 3 store helpers). 323 touched-suite tests green
  (`test_main` waitress failures are a pre-existing local-venv gap, not a regression).
  Activates on next restart. Files: `orders/order_reconciler.py`, `core/state_store.py`,
  `core/config_loader.py`, `config/system_config.yaml`, `tests/crash_test/state_machine_validator.py`.
- 2026-06-19 — Claude Code — **Bug B metrics: `/metrics` broker quota gauges.** Added
  `broker_in_flight` / `broker_filled_today` / `broker_quota_used` / `broker_quota_max` /
  `broker_quota_available` to `SignalProcessor.get_runtime_metrics()` (merged into `/metrics`
  via the existing `metrics_provider`). They mirror the reservation-aware DAILY_TRADES gate
  exactly — `used = max(daily_count, settled_today + count_live_reservations())` — so live
  monitoring reflects the real cap decision, not a parallel tally. Fully guarded (best-effort;
  never raises/blocks). 2 tests. Files: `signals/signal_processor.py`. (NB: the `/metrics/prometheus`
  text endpoint is unchanged — it already omits the runtime counters.)
- 2026-06-19 — Claude Code — **Bug B: reservation-aware DAILY_TRADES cap** (extends
  FIX-185 to the daily limit). Audit found the prior "B already correct" was only
  half-true: FIX-181 fixed *what* counts (rejects excluded → retry) and FIX-190 added
  funnel metrics, but the `DAILY_TRADES` check was still a bare `count_trades_today`
  DB read with **no in-flight accounting** — the daily-cap twin of the position-cap
  TOCTOU race FIX-185 already closed. `approve()` reads `daily_count` inside
  `portfolio_lock`, but the candidate's `PENDING_FILL` trade row is inserted later by
  `order_placer.place()` **outside** the lock, so a burst all read the same pre-burst
  count and passed → overshoot (18-Jun 8-vs-5). Fix mirrors FIX-185 exactly:
  `effective_daily = max(daily_count, count_settled_trades_today() + count_live_reservations())`
  — `settled_today` = today's executed trades minus `PENDING_FILL`; `live_reservations`
  = the in-flight half (reserved-not-placed + `PENDING_FILL`); the two partition with
  no double-count (commit pops the reservation at fill), and `daily_count` (incl.
  `PENDING_FILL`) is the restart floor. `reserve()` runs inside the same lock, so the
  count is race-consistent. Rejections enter neither term → slot frees → next signal
  retries to reach max (the "5→3 undershoot" was never a counting bug — just no further
  signals). New `state_store.count_settled_trades_today()` + 6 unit tests (burst race,
  final-slot, rejection-frees-slot, restart floor, 18-Jun overshoot replay, settled-count
  partition). 346 tests green locally. Files: `capital/risk_engine.py`,
  `core/state_store.py`, `tests/unit/test_risk_engine.py`, `tests/unit/test_state_store.py`.
  Activates on next restart. (NB: `REJECTED` is NOT a valid `trades.status` — rejects are
  `FAILED`/`CANCELLED` or never get a trade row.)
- 2026-06-18 — VS Code Claude — Initial creation. Full VM+PC audit (structure, configs, modules,
  DB, logs, ~28 cron jobs, 4 systemd services, tools, deploy mechanism). Flagged: root `trading.db`
  (0-byte dead), crontab divergence, sentinel accumulation, duplicate git remote, crash-loop.
- 2026-06-18 — VS Code Claude — Cleanup: removed dead `trading.db`, deleted 56 stale `*.flag` files,
  moved dated notes (`CRON_FIX_2026_05_18.md`, `FIXES_DAILY_REPORT_2026_05_18.md`) to `docs/archive/`,
  removed `deploy_audit_fixes.ps1`, removed duplicate `vm` git remote. Discovered `alert-watcher.service`
  is INACTIVE (now tracked as open issue #3).
- 2026-06-18 — VS Code Claude — Infra fixes: (A) `alert-watcher.service` enabled + started (survives
  reboot) — delivery still blocked by placeholder SMTP config (issue #3). (B) systemd unit
  `RestartPreventExitStatus=3 4` (live + `deploy/systemd/trading-system.service`) so an exit-4 HALT no
  longer crash-loops — verified service settles to `failed` (NRestarts froze). (C) deleted 0-byte PC
  `trading.db`.
- 2026-06-18 — VS Code Claude — SMTP configured for alert-watcher: `alerts.smtp` → Gmail
  `ramakrishnan031@gmail.com` (send+receive), `ALERT_SMTP_PASSWORD` env; crisp account-tagged email
  subject `[LFL836] <SEVERITY> — <title>` (single + digest builders in `alert_watcher.py`). Transport
  verified (STARTTLS connects) but Gmail returns 535 BadCredentials — `ALERT_SMTP_PASSWORD` is not a
  valid 16-char App Password. Issue #3 BLOCKED on a valid Gmail App Password (Rama to set).
- 2026-06-18 — VS Code Claude — SMTP delivery VERIFIED after a valid Gmail App Password was set:
  16 pending sentinels delivered (`.flag`→`.delivered`), digest emailed. Issue #3 resolved.
- 2026-06-18 — Claude Code — FIX-187: `scripts/auto_refresh_token.py` rewritten to a fully
  headless TOTP login — eliminates the daily manual OTP step (08:00 Mon-Fri cron, unchanged).
  Reuses `zerodha_login.exchange_request_token`+`save_token` (token-format parity: passes
  `is_token_valid()` and carries `api_key` for the paper quote provider); `request_token` via the
  `connect/login?v=3` redirect chain; account-specific→generic credential resolution; Telegram +
  retry + cron-heartbeat. `pyotp==2.9.0` added to requirements and installed in the venv. **Verified
  live** (token valid + `kite.profile()` OK + heartbeat SUCCESS). TOTP secret is the per-account
  `ZERODHA_TOTP_<acct>` in `.env` (single source of truth; a stale/duplicate `ZERODHA_TOTP_SECRET`
  was removed). Commit ff0984b.
- 2026-06-18 — Claude Code — FIX-188: **headless fixes**. (A) `deploy/token_watcher.sh` is now
  exit-code aware (ExecMainStatus/ExecMainExitTimestamp): same-day exit 4 (HALT) / exit 3 → no restart
  + 1 Telegram/day + 5-min back-off; prior-day 4/3 → one clean start (auto-recovers overnight HALTs);
  exit 1/2 → restart w/ 3-per-hour backoff; clean exit-0 today → no post-EOD restart. Stops the 30s
  hammer-restart of a HALT-failed service. (B) `scripts/healthcheck_server.py` `/health` now reports
  `{db, token, kill_switch}` and returns 200/503 (reads the `kill_switch_state` table; also fixed the
  same latent wrong-table bug in `/metrics`). Verified live; service handed off to systemd (active).
  Commits 7a12cf2, 8cf8685.
- 2026-06-18 — Claude Code — FIX-188b: **systemd-friendly resume**. `scripts/clear_kill_switch.py`
  clears (resumes) the kill switch in the DB via `KillSwitch.resume()` WITHOUT acquiring the instance
  lock or starting the loop (`--dry-run`, `--force` for HARD_KILL); `deploy/resume.sh` wraps
  stop → clear → reset-failed → start under systemd. Closes the manual-vs-systemd `--resume` lock
  conflict (no more standalone `main.py --resume` competing for port 5001). 6 tests; live dry-run
  verified. Commit 9ddcddf.
- 2026-06-18 — Claude Code — TASK #3: **Cron Officer**. New `config/cron_registry.yaml` (single source
  of truth, 30 jobs) + `core/cron_registry.py` loader; `scripts/cron_officer.py` (`--briefing` 04:55,
  `--eod-summary` 18:30, `--check-change`); `check_cron_drift.py` now registry-driven (hardcoded list
  removed). Universal heartbeats via `HeartbeatTimer(alert=True)` (8 silent jobs instrumented) +
  `alerts/cron_alerts.py` per-job alert policy (reuses TelegramNotifier tiers; honors the master switch)
  + `skip_if_non_trading_day()` holiday guard. **Crontab regenerated + installed** (live == canonical,
  33 lines): `auto_refresh_token` 08:00→08:15; +`cron_officer` briefing/eod; +analytics.db backup (01:05);
  +`db_retention` (02:30 + Sun VACUUM, activated); backup-retention now covers analytics. Old crontab
  backed up to `data_store/crontab_backups/`. 40 new tests. Commits …→6fd0ed9.
- 2026-06-19 — Claude Code — **FIX-189: dash-cron + overnight-run + false-alert fixes.**
  Root cause of the 19-Jun morning incident: cron's default `/bin/sh` is dash, so
  the bare `. .env` in every market job failed (`sh: .: .env: not found`) — no token
  refresh (08:15), no heartbeats, no per-cron logs. FIX-187's "verified live" had only
  ever been exercised via a manual **bash** run, so the dash bug never surfaced.
  Fixes: **(P0-A)** `deploy/cron/trading-system.cron` now leads with `SHELL=/bin/bash`
  and sources via `. ./.env` (belt-and-suspenders); registry header documents the rule.
  **(P1-A)** `main.py` exits 0 outside the broad service window [08:00–16:00 IST] and
  `deploy/token_watcher.sh` only starts the service in-window — the service no longer
  runs overnight (the 18-Jun 23:22 start that produced the false 04:24 capital-drift and
  07:07 KiteTicker CRITICALs). **(P1-B)** `order_reconciler._g3_capital_drift` skips the
  alert when broker `net==0.0` outside market hours (overnight funds endpoint returns 0),
  and `live_feed._on_noreconnect` downgrades max-reconnect-exhausted to WARNING (no
  SOFT_KILL/CRITICAL) outside market hours — both still escalate normally in-session.
  **(P2)** token-watcher start is window-gated + already idempotent on `ActiveState`.
  16 new tests. Kite dev-console IP allowlist remains Rama's external step. Commits 2ac32ae,
  0f722f7, 5603013, 66380b6 — pushed+deployed; crontab installed; service recovered (`/health`
  HEALTHY).
- 2026-06-19 — Claude Code — **FIX-190 Stage 4: incident fixes** (commits 51f5e94→870bbc9).
  Root-cause cluster of the 10:00 cascade fixed: **C** TGT-only failure returns a partial
  (SL-protected) result instead of raising → no emergency-exit/HARD_KILL; **D** SL/TGT clamped
  into the circuit band before placing; **A** reverse-aware flatten (`broker/position_helpers`)
  in the HARD_KILL first pass + emergency exit (skip if flat, BUY to cover a short, mark EXITING)
  → no oversell; **E** cancel a trade's resting SL/TGT before any flatten → no orphans; **F** G5b
  skips recovery-SL when a live SL already exists → no duplicate SL. Guardrails: **H**
  `live_test_mode` (RiskConfig; live caps max_open=1 / 3-per-day), **I** in-session capital-drift
  tolerance = max(Rs, expected*10%). Defensive hardening of the new flatten helpers (never crash
  the indestructible loop). **G** entry throttle (signal_processor 20s / 3-per-60s, 575a1dc).
  **B** already correct (FIX-181 excludes rejects + FIX-185 reservation cap; only metrics deferred).
  Incident replay test (3af8310) green. Paper mode SKIPPED per Rama (stay LIVE, tiny capital).
  Service stays DOWN until Rama's explicit go-ahead → LIVE with live_test_mode.
- 2026-06-19 — Claude Code — Cleanup: removed **445** stale `critical_alert_*.flag` from the **PC**
  `data_store/` (31-May→18-Jun, mostly `source_module:test`). Local-only/gitignored — never tracked,
  no `.gitignore` change. VM already clean (alert-watcher consumes → `.delivered`).
- 2026-06-19 — Claude Code — **FIX-189 (P1-A completion): EOD window-end self-exit.** Found while
  verifying the assumed "EOD self-exit" that it did **not** exist (runtime loop only waits on the
  shutdown event; EOD squareoff just trips a scheduled SOFT_KILL; no timer/cron stops the process) —
  so a service started in-window ran all night (harmless after P1-B, but not clean). Added an
  `eod-self-exit` daemon thread (`_start_eod_self_exit_thread` / `_eod_self_exit_due`): once past
  the **configured** `trading_hours.service_window_end` (**17:35 IST**; a hardcoded 16:00 until
  25-Jul-2026 — ⭐ that constant had **two roles** and was split into `SERVICE_START_CUTOFF`
  (latest the service may **START**, now 18:15 = the schema's exclusive bound, tied to the 18:15
  forward-shadow cron) and the configured **stop**; binding them that way makes a crash-restart
  between the two — refused with exit 0, never retried by `Restart=on-failure` — impossible for
  any legal config value) **and** flat (`StateStore.count_active_positions()==0` over
  OPEN/PARTIAL/PENDING_FILL) it sets the shutdown event → `main()` exits 0 → systemd won't restart →
  token-watcher's exit-0-today path skips the restart → clean overnight + clean 08:30 start. **Never**
  exits while a position is open (stays up to manage residual positions; count error → stays up).
  Armed only for normal starts (skipped for `--interactive`/`--resume`/`TS_IGNORE_MARKET_WINDOW`).
  Parity-safe. 6 new tests. Commit d61ece9. Activates on next service restart (≈ next 08:30).
- 2026-06-19 — Claude Code — **TASK #5: System Manager EOD** (`scripts/system_manager.py`, 18:45
  Mon-Fri, after the Cron Officer EOD). Deep daily cross-check beyond job execution: 8 isolated
  checks (config-vs-actual w/ live_test_mode effective caps · order quality/slippage · report
  integrity on REAL paths · system health: PRAGMA integrity_check/heartbeats/disk/token/restarts ·
  strategy health · risk events: HARD_KILL via LOG since it's not in any table · vs-yesterday ·
  tomorrow readiness). Telegram + CRITICAL-sentinel→email + saved `reports/system_manager/<date>.txt`.
  Trips tomorrow's SOFT_KILL only on a real violation (cap breach / DB-integrity fail / HARD_KILL
  today). Registry + canonical crontab updated (FIX-189 `. ./.env`) + installed (registry==crontab).
  10 unit tests; validated on real VM data. First run Mon 22-Jun. Commits 8d37f4f→29e477e.
- 2026-06-19 — Claude Code — **live_test_mode → PERMANENT bug-hunting caps**: raised
  `risk.live_test_max_open_positions` 1→**4** and kept `live_test_max_entries_per_day`=**6**
  (commit 6b6979c). Rama's strategy — active bug-hunting on live with controlled exposure: 4
  concurrent positions exercises concurrency/race paths, small qty (1-2 shares) on ₹10k caps loss.
  No auto-disable (manual only). Effective live caps now 4/6 (override base 5/20). Activates on
  next restart (today's 16:00 EOD self-exit → Mon 08:30 auto-start). NOT temporary; no revert.
- 2026-06-19 — Claude Code — **Bug G entry throttle — full integration** (commit c0554c6). The
  global throttle (min-gap 20s + burst 3/60s) was already wired in FIX-190; this adds a clean
  `signals/entry_throttle.py` `EntryThrottle` (thread-safe, ATOMIC `admit(symbol)` check-and-record —
  no TOCTOU burst race) with a NEW **per-symbol cooldown** (`per_symbol_cooldown_sec: 300` — no
  re-entry of the same symbol within 5 min) and **per-reason /metrics** (entries_throttled_min_gap /
  _burst / _per_symbol / entries_admitted). Single chokepoint at both `place()` sites; DROP on
  throttle (no queuing). 13 unit tests incl. 19-Jun 5-in-5s burst replay (→1 admitted) + thread-safety
  (50 concurrent → exactly burst_max). Activates next restart.
- 2026-07-02 — Claude Code — **Off-market deploy plan for the A-2/C-1/B-1/P1 batch** (planning doc,
  `docs/ops/offmarket_deploy_plan_03jul2026.md`). Push AFTER the 03-Jul 08:15 boot of `9becf8c` proves
  clean, in an after-17:05/weekend window (never 15:30–17:05). One push: merge **C-1** (carries A-2 `fd09a38`)
  → **B-1** `f5fd4d9` → **P1** `4817032` into `main`, then `git push origin main` (post-receive installs the
  `@15:58` crontab). Schema **v41→v42** (P1 only, pure-add `eod_broker_reconciliation`; back up DB first).
  A-2+B-1 activate Mon 06-Jul 08:15 boot; P1 first shadow EOD Mon 06-Jul 15:58. Both shadow flags default OFF
  (`daily_loss_include_unrealized=false`, `eod_reconcile.authoritative=false`). Merges verified clean
  (B-1/P1 disjoint hunks in `system_config.yaml`+`config_loader.py`). Earliest B-1 enforce ≈ Mon 13-Jul;
  earliest P1 authoritative + eod_verify/reconcile_pnl retirement ≈ Mon 20-Jul (after ≥2 clean shadow weeks +
  ledger-dimension deepening + a demonstrated UNVERIFIED). No HIGH/CRIT open outside the batch except deferred
  C-2 network Phase-3. NO push in this pass.
