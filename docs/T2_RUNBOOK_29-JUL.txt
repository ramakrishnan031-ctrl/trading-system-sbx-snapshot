================================================================================
*** T2 --arm-overnight RUNBOOK — WEDNESDAY 29-JUL-2026 ***
*** REAL MONEY. REAL BROKER. A POSITION HELD OVERNIGHT. ***
================================================================================
Written 27-Jul-2026 Mon. Closes Thursday 30-Jul. Account LFL836.
GOAL: prove the DDPI/demat sell — the ONE thing a same-day square cannot prove.
✅ REVISED 27-Jul 22:3x — the band widen and the script repair are now BOTH
   APPLIED, as ONE operation, and the step-zero hash below is the new one.
================================================================================

⛔⛔⛔ STOP — WHAT IS TODAY'S DATE? ⛔⛔⛔

  *** THIS CARD IS FOR WEDNESDAY 29-JULY-2026. NO OTHER DAY. ***

  ⛔ *** DO NOT RUN THIS ON TUESDAY 28-JUL. *** Tuesday is the v45 schema
     migration — the day's SINGLE VARIABLE. Arming T2 on Tuesday would put a real
     CNC position into the one boot that has to stay readable, and if anything
     then went wrong *** nobody could tell which change caused it. *** A day lost
     to waiting costs one day. A confounded result costs the whole pair, twice.

  ⚠️ You will probably open this card on TUESDAY, while checking the migration.
     That is expected and fine. *** READING IT TUESDAY IS FINE. RUNNING IT IS
     NOT. *** Nothing in §2 or §7 should be typed until Wednesday.

  CHECK THE DATE, DO NOT TRUST THE FILENAME OR YOUR MEMORY OF THE WEEK:
      date "+%A %d-%b-%Y"        -> must print  Wednesday 29-Jul-2026
  ⛔ Anything else -> CLOSE THIS CARD. It is not today's card.

  ⭐ Filenames drift and get copied; this line is the date that governs.

================================================================================

⭐⭐ READ THIS FIRST — THE THREE FACTS THAT SHOULD SET YOUR NERVES CORRECTLY

  1. *** MAXIMUM LOSS OVERNIGHT = THE PRICE OF ONE SHARE (~Rs 10). *** One share
     of IDEA. Not one lot, not one position size. If the company vanished
     overnight the loss is about ten rupees.
  2. ⭐⭐ *** A GTT CANNOT FIRE WHILE YOU SLEEP. *** GTTs trigger on live price
     only, and the market is CLOSED 15:30 Wed -> 09:15 Thu. *** There is no
     overnight execution risk. *** The gap risk lands at the Thursday OPEN —
     when you are awake and holding this card.
  3. ⭐⭐ *** IF THE GTT IS REJECTED, NOTHING IS LEFT DANGLING. *** This is the
     one that separates "burned a day" from "I am holding something unprotected
     overnight", so it is up here and not buried in §1b.
     If `place_for_fill` raises, `intentional_hold` is STILL False, so the
     `finally` block runs ensure_flat and SQUARES the position. You end FLAT.
     ⇒ *** A REJECTED GTT COSTS THE DAY. IT DOES NOT COST YOU PROTECTION. ***
     ⚠️ This applies to a REJECTED placement only. A placement that SUCCEEDS and
        then fails VERIFICATION is the opposite case — see §3, which is the most
        important section on this card.

================================================================================
0. ⛔ STEP ZERO — BEFORE ANYTHING ELSE
================================================================================
  ssh trading-vm
  sha256sum /home/ubuntu/t2_proof_run/t2_cnc_gtt_realtest.py

  *** MUST print exactly: ***
  0a3c505c25e6518c7b77461965b512bb20f55765842d3b3bfb5fb46a0f582d36

  ⛔ *** ANY OTHER HASH -> STOP. DO NOT RUN IT. ***
  ⚠️ *** `--help` IS NOT A CHECK. *** Every version of this script has
     `--arm-overnight`. The flag is not the fingerprint. The hash is.

  ⭐⭐ *** THE BIT-ROT TRAP IS NOW REMOVED, NOT MERELY DOCUMENTED (27-Jul eve). ***
     Previously `~/t2_proof_run/` held the repaired copy while the deployed
     `scripts/` tree held a bit-rotted one (`23aca731...`), so running the wrong
     PATH ran the wrong CODE. The repair is now merged and both copies are the
     same widened script. *** Running the wrong path can no longer run the wrong
     code. *** The hash check stays anyway — it is what proves that is still true.

  ⛔ OLD HASHES, FOR RECOGNITION ONLY — if you see either of these you are on a
     stale copy and the 27-Jul sync did not reach it:
       a5779420...  repaired but NOT widened (band still -3%/+5%)
       23aca731...  the bit-rotted copy (3 signature drifts)

================================================================================
1. PRECONDITIONS — each one is an ABORT, not a warning
================================================================================
  P0  *** TODAY IS WEDNESDAY 29-JULY-2026. ***             ABORT if not.
        date "+%A %d-%b-%Y"      -> must print  Wednesday 29-Jul-2026
        ⛔ This is P0 and not P6 because it is the ONLY precondition that can be
           got wrong while every other check passes cleanly. The script will run
           perfectly on the wrong day. ⭐ The calendar is the constraint, not the
           script — nothing in the code knows which date the plan says.
        ⚠️ If the arm has SLIPPED to a later date, that is fine — but re-check P4
           (never a Friday arm) and re-read the P1 box, because "Tuesday's boot"
           then means "the most recent boot", not 28-Jul.

  P1  Tuesday 28-Jul v45 boot was observed CLEAN.        SEE THE BOX BELOW.
  P2  Service `active`, NRestarts=0, token refreshed.    ABORT if not.
        ssh trading-vm "systemctl is-active trading-system"
  P3  *** AIM 13:00 IST. *** Acceptable 12:30-14:00.      ABORT outside 10:30-14:30.
        (Script refuses outside 09:15-15:30 — that is its ONLY time gate; 13:00 is
         an OPERATOR choice, not something the script enforces, so nothing will
         stop you arming at the wrong time. The later anchor is HALF the -10/+10
         void reduction: a shorter remaining session is less time in which to
         touch a trigger. Not so late that a rejection leaves no room to retry.)
  P4  *** DATES VERIFIED against nse_holidays_2026.yaml (27-Jul): ***
        Wed 29-Jul TRADING · Thu 30-Jul TRADING · Fri 31-Jul TRADING.
        *** So 29->30 Jul is a ONE-NIGHT carry. Confirmed, not assumed. ***
        ⛔ NEVER a Friday arm (a Fri->Mon carry holds real stock three
           unsupervised days for no extra evidence). If the arm slips to Fri
           31-Jul, *** wait for Mon 3-Aug ***, do not arm on the Friday.
  P5  No unusual CRITICAL in this morning's alerts.      ABORT if there is.
  P6  You have ~30 uninterrupted minutes and can be at a screen 08:15 Thursday.
                                                        ABORT if not.

  ────────────────────────────────────────────────────────────────────────────
  ⭐⭐ P1 IN DETAIL — DECIDED NOW, SO YOU ARE NOT DECIDING IT AT 12:55 WEDNESDAY
  ────────────────────────────────────────────────────────────────────────────
  "Tuesday was clean" is not one condition, and the three failure shapes have
  DIFFERENT answers. Find yours, then act — do not re-derive this on the day.

  (a) *** MIGRATION FAILED — the service did not start Tuesday. ***
      ⛔ ABORT WEDNESDAY. Do not arm. The whole point of the Tuesday slot was to
      make v45 the single variable; arming on top of an unresolved migration
      means a Thursday problem has two candidate causes and proves nothing.
      ⇒ Fix v45 first. T2 slips to the next TRADING pair with a clean boot
        behind it. ⛔ NOT Fri 31-Jul — never a Friday arm. That means Mon 3-Aug
        at the earliest, and 3-Aug is itself blocked by the 2FA seed move, so
        realistically Tue 4-Aug. *** Slipping is CHEAP. Re-running the pair
        after a confounded result is not. ***

  (b) *** MIGRATION SUCCEEDED but the day was NOISY *** — alerts you have not
      explained, an unexpected kill, a reconciliation you do not understand.
      ⚠️ JUDGEMENT CALL, and the rule is: *** does the noise touch the ORDER or
      CAPITAL path? *** If yes -> abort, same as (a). If it is reporting,
      alerting, or a cron unrelated to orders -> PROCEED. T2 runs on its own
      isolated DB and its own adapter instance; a noisy report does not reach it.

  (c) *** MIGRATION SUCCEEDED, DAY WAS CLEAN, but you did not get to watch it. ***
      ⇒ PROCEED, after checking three things that take two minutes:
          ssh trading-vm "systemctl is-active trading-system"          -> active
          ssh trading-vm "sqlite3 -readonly ~/systems/trading-system/\
            data_store/trading_system.db 'select value from schema_meta;'" -> 45
          and no CRITICAL in Tuesday's mail you have not accounted for.
      *** Observation is preferable, but its ABSENCE is not a failure. Schema 45
      + service active + no unexplained CRITICAL IS the clean signal. ***

  ⚠️ AND THE ONE THAT IS NOT A JUDGEMENT CALL: if schema_meta still says *** 44 ***
     on Wednesday, the migration did NOT run. That is case (a). ABORT.

================================================================================
1b. ✅ THE BAND — WIDENED AND APPLIED 27-JUL. READ THIS BEFORE §2.
================================================================================
✅ *** DECIDED (Rama) AND NOW APPLIED IN THE SCRIPT: KEEP IDEA · BAND -10%/+10% ·
   ARM ~13:00. *** The card and the script agree; the hash in §0 is the proof.

*** THE GTT TRIGGERS NOW SIT -10% / +10%. *** (Was -3%/+5%.)

WHY. A same-day trigger VOIDS the arm — it becomes a day trade, so there is no
demat debit and nothing is proved — and it costs a market day. Measured on 735
real symbol-days of 1-minute candles (19-Jun -> 24-Jul 2026, 482 symbols): from an
11:30 anchor, 33.9% of symbol-days touch -3% or +5% before the close, and for
shares under Rs 150 it is *** 52.2% — roughly a coin flip. ***

  band -3%/+5%  ->  52.2% void         (what this card used to describe)
  band -10/+10  ->   ~9.0% void
  + arm ~13:00  ->   ~2.8% void        *** where we are now ***
  expected arm-days 2.09 -> 1.03

⭐ Band width dominates every other lever and costs NOTHING in exposure. None of
   the script's PASS assertions look at trigger LEVELS — they check the two-leg
   OCO shape, SELL+CNC+qty, and the durable gtt_state row. The structure is the
   proof; the levels are incidental. (Re-verified 27-Jul: no test references
   either offset.)

⚠️ *** IDEA IS NOT IN THE MEASURED SET AT ALL *** (no candles, no stats; the
   cheapest symbol measured is Rs 13.90). At ~Rs 10 it sits BELOW the sample
   floor, so 52.2% was the closest available proxy and probably optimistic —
   which is an argument for the wider band, not against it.

⭐ *** WHY -10 AND NOT WIDER — DO NOT "IMPROVE" THIS TO -20. *** Our validator has
   only a MINIMUM trigger distance (0.25%) and NO maximum, and the kiteconnect SDK
   does NO client-side validation of place_gtt — so any cap is enforced
   SERVER-SIDE at Zerodha and *** cannot be read from our source at all. ***
   *** An over-wide band would be REJECTED AT PLACEMENT and burn the day exactly
   as a same-day trigger would. *** -10/+10 is deliberately the SMALLEST band that
   solves the problem, not the safest-looking one. The reasoning is recorded here
   and in the script's own constant block so it is not later widened on a hunch.

⭐ *** AND IF IT IS REJECTED, NOTHING IS LEFT DANGLING *** — see fact 3 at the top
   of this card. A rejected GTT ends FLAT. (Still complete §3's by-hand check.)

================================================================================
2. THE ARM STEP
================================================================================
  ssh trading-vm
  cd /home/ubuntu/systems/trading-system
  set -a && . ./.env && set +a
  PYTHONPATH=. /home/ubuntu/systems/venv/bin/python \
    /home/ubuntu/t2_proof_run/t2_cnc_gtt_realtest.py \
    --arm-overnight --i-understand-this-places-a-real-cnc-order

  Defaults are correct: --symbol IDEA --qty 1 --account LFL836.

  *** A GOOD RESULT LOOKS LIKE: ***
    1) CNC BUY placed: <order_id> — confirming fill    ... status=COMPLETE
    2) OCO GTT placed: gtt_id=<GTT_ID> sl_trigger=... tgt_trigger=...
    3) get_gtt: type=two-leg legs=[('SELL','CNC',1), ('SELL','CNC',1)]
       verified: broker=True durable_gtt_state(P2)=True
    ARMED for the overnight TPIN test. Position HELD with GTT <GTT_ID>.
    exit code 0

  ✍️ *** WRITE THE GTT_ID DOWN NOW. *** You need it Thursday.
     GTT_ID = ______________________

================================================================================
3. ⚠️⚠️ IMMEDIATELY AFTER — THE MOST IMPORTANT LINES ON THIS CARD
================================================================================
*** A NON-ZERO EXIT DOES NOT MEAN NOTHING HAPPENED. IT MEANS THE OPPOSITE. ***

The script sets `intentional_hold = True` BEFORE it evaluates the result. So on a
failed verification it STILL holds the position AND STILL skips the GTT cleanup.
*** exit 1 = "verification failed AND you are carrying stock overnight." ***

⇒ *** CONFIRM BY HAND AT THE BROKER, WHATEVER THE SCRIPT SAID. *** Open Kite:

  [ ] Positions tab  -> IDEA, qty 1, product CNC          (Wednesday it is here)
  [ ] Orders -> GTT tab -> the GTT_ID above is ACTIVE,
                           two SELL legs, qty 1 each, product CNC
  [ ] Funds -> roughly Rs 10 less than before

  *** ALL THREE MUST BE TRUE BEFORE YOU WALK AWAY. ***

  If position EXISTS but GTT is MISSING -> you are holding UNPROTECTED.
     Either place an OCO GTT manually in Kite, or sell the share now
     (Positions -> IDEA -> Exit) and re-arm tomorrow. Do not leave it bare.
  If position is MISSING -> nothing is held; nothing to protect. Re-arm another
     day. (Read the log for why the BUY did not fill.)

================================================================================
4. OVERNIGHT — DO NOTHING
================================================================================
*** There is no overnight step. No monitoring, no check, no alert to watch. ***
The market is closed; the GTT cannot fire; our service is not running after
17:35 and does not manage this position anyway (§6). *** Go to sleep. ***

================================================================================
5. THURSDAY 08:15-08:25 — ONE EXPECTED WARNING
================================================================================
*** EXPECT exactly one WARNING alert, and it is CORRECT: ***

   "Orphan GTT — no open delivery trade (IDEA)"
   Active broker GTT <id> for IDEA has no gtt_state row and no OPEN/PARTIAL
   CNC trade to adopt onto; left alone for review.

*** THIS IS NOT A FAILURE. *** The live service's adoption prepass sees your GTT
(T2's database is isolated; the broker ACCOUNT is not). It is WARNING severity,
fires once, and by design adoption only ever INSERTs a row or WARNs — *** it can
never delete your GTT. *** A separate sweep is also forbidden from touching any
GTT with no gtt_state row.

⭐ *** THIS IS THE FIRST TIME THAT COMPONENT (FIX-183 adoption) HAS EVER RUN IN
   PRODUCTION *** — you are getting a free proof of it two stages early.

⚠️ *** ITS ABSENCE IS THE MORE INTERESTING RESULT. *** No warning means either
   the prepass did not run, or `get_gtts()` returned nothing — i.e. your GTT may
   be GONE. *** If there is no warning, go straight to §6.B and check the broker
   before anything else. ***

================================================================================
6. ⚠️⚠️ IF SOMETHING GOES WRONG — DECIDE NOW, NOT AT 08:20 THURSDAY
================================================================================

A. *** WHAT PROTECTS THE POSITION, AND WHAT DOES NOT ***
   PROTECTS:  the broker-side OCO GTT. It lives at Zerodha, independent of our
              service, our process, our database, the VM, and your laptop.
   DOES NOT:  our trading service (it never sees this position — no gtt_state
              row, no local trade); the throwaway DB; any alert we send.
   ⚠️ THE HONEST LIMIT: the SL leg is a stop-LIMIT, not a market order. Trigger
      now sits 10% below arm price and its limit a further 3% below that.
      COMPUTED 27-Jul at LTP 10.00: *** trigger 9.0, limit 8.7 = -13.0% of the
      arm price. A gap-open through about -13% leaves the share UNSOLD ***
      (still yours, still ~Rs 10). That is a Thursday-09:15 event, not a night
      event. ⭐ The widened band made this floor DEEPER, not shallower — it was
      -6.0% under the old -3%/+5% band.
   ⭐⭐ *** AND A GAP-OPEN DOES NOT INVALIDATE THE TEST — IT HELPS IT. *** An
      unsold share on Thursday is still a share in Holdings, so §7's close step
      (or a manual sell) still exercises the demat debit. *** The DDPI proof is
      UNAFFECTED. *** Position SURVIVING to Thursday is exactly what the test
      needs; a gap is a money event (bounded at ~Rs 10), never a test failure.
      *** The only outcome that voids the test is the position being SOLD on
      Wednesday *** — see the box below.

B. *** THE GTT IS GONE THURSDAY MORNING — WHAT FIRST ***
   1. Kite -> Holdings: is the share still there? (Thursday it is a HOLDING, not
      a position — T+1 settlement moved it.)
   2. Share present, GTT gone  -> you are unprotected but not in danger (Rs 10).
      *** Just run the close step (§7) — it sells and needs no GTT. ***
      Do not spend time reconstructing the GTT.
   3. Share gone AND GTT gone  -> something sold it. *** STOP. Do not place any
      order. *** Screenshot Kite Orders + Holdings, then investigate. This should
      not be possible and the evidence matters more than the ten rupees.

C. ⭐ *** CAN YOU JUST SELL IT BY HAND? YES. ***
   Kite -> Holdings -> IDEA -> Sell -> qty 1, product CNC, market/limit.
   · If DDPI is authorised it simply executes.
   · If it prompts for *** TPIN / CDSL authorisation ***, complete it (OTP flow)
     and the sale goes through.
   ⭐⭐ *** AND IF THAT PROMPT APPEARS, THAT *IS* THE TEST RESULT: DDPI IS NOT
      AUTHORISED. *** It is not a malfunction — it is the answer we came for.
      Record it, sell the share, and report. See §8.

D. *** THE SERVICE DOES SOMETHING UNEXPECTED AT 08:15 — DO YOU STOP IT? ***
   ⭐ *** STOPPING THE SERVICE COSTS THIS CNC POSITION NOTHING. *** The service
      does not protect it and cannot square it: CNC is exempt from the 15:17 EOD
      squareoff (it filters to MIS/CO only), there is no local trade row, and the
      protection is broker-side.
   ⚠️ *** WHAT STOPPING COSTS IS THE INTRADAY BOOK *** — it halts entries AND
      exit management AND the 15:17 squareoff for any MIS position.
   ⇒ *** DECISION RULE: if the intraday book is flat, stopping is free. If MIS
     positions are open, stopping is expensive — and the CNC is NEVER a reason
     to stop. ***
   The only halt (there is no halt.sh):
       ssh trading-vm sudo systemctl stop trading-system

================================================================================
7. THURSDAY — THE CLOSE STEP
================================================================================
  ssh trading-vm
  cd /home/ubuntu/systems/trading-system
  set -a && . ./.env && set +a
  PYTHONPATH=. /home/ubuntu/systems/venv/bin/python \
    /home/ubuntu/t2_proof_run/t2_cnc_gtt_realtest.py \
    --close-overnight <GTT_ID> --i-understand-this-places-a-real-cnc-order

  (Hash-check again first. Market hours only.)

  *** PASS = the CNC SELL polls COMPLETE with ZERO manual TPIN intervention,
      and the GTT is deleted only after that. *** -> DDPI PROVEN, piece #5 closed.

  *** FAIL = the SELL is REJECTED. *** The script KEEPS the GTT (protection) and
      flags manual handling — by design. This means DDPI/TPIN is unauthorised:
      authorise it at the broker, then RE-RUN THE WHOLE PAIR from §2 (§8).

================================================================================
8. WHAT ABORTS THE DAY, AND WHAT IT COSTS
================================================================================
  BUY rejected — nothing held                        RESUME. 1 day.
  BUY fills, GTT fails — script squares it, ends flat RESUME. 1 day.
  ⚠️ GTT TRIGGERS Wednesday afternoon (-10% or +10%)  *** ARM IS VOID *** — it
     became a day trade, no demat debit, nothing proved. RESUME. 1 day.
     (Now ~2.8% likely, not 52.2%. This is why §1 P3 says 13:00 — the late anchor
      and the wide band each do about half the work.)
  ⚠️ GTT REJECTED at placement (band too wide for the broker's
     server-side cap, which we cannot read from source)  RESUME. 1 day, and you
     end FLAT — the `finally` squares it. If this happens, the cap is real:
     record the broker's error text, and NARROW toward -5/+5 rather than
     abandoning the widen.
  Broker/token outage Wednesday                      RESUME. 1 day.
  Broker/token outage Thursday                       RESUME — GTT still protects,
     close Friday. *** The proof is NOT forfeited. ***
  ⛔ SELL REJECTED Thursday — DDPI unauthorised       *** RESTART the pair. ***
     2 days + however long broker authorisation takes. A same-day square proves
     nothing about demat, so there is no shortcut.

================================================================================
9. WHEN IT PASSES — REPORT
================================================================================
  GTT_ID · BUY order id · SELL order id · fill prices · total cost ·
  whether ANY TPIN prompt appeared (the headline) · the Thursday orphan WARNING
  present or absent.
  ⇒ Then piece #5 is closed and the next gate is the FLAG FLIP (Tue 4-Aug),
    which is the first genuinely irreversible step. Different card, different day.
================================================================================
END
================================================================================
