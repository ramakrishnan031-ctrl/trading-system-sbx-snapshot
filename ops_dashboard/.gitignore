# Isolated GUI venv — never committed (I4: no broker packages leak in either)
.venv/
__pycache__/
*.pyc
.pytest_cache/
# Local auth secrets may be written into gui_config.yaml by `--setup`; keep the
# committed copy free of real secrets (the committed file ships empty auth fields).
# VM deployment overlay (paths + auth secrets; chmod 600). checkout -f safe.
backend/config/gui_config.local.yaml

# Asset policy (all screens): production ships ONLY optimized assets (.web.webp / .webp / .svg).
# Raw editable masters (PNG/JPG/JPEG from image-gen) stay LOCAL-ONLY — never committed/deployed.
# Force-add with `git add -f` only if a raster is genuinely the final production asset.
assets/**/*.png
assets/**/*.jpg
assets/**/*.jpeg
