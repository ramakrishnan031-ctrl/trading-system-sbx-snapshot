# ops_dashboard — ISOLATED GUI venv requirements (pinned exact versions).
# Isolation rule I4: kiteconnect MUST NOT appear here (no broker interaction).
# Nothing from the production requirements.txt is inherited; this venv is
# independent. Keep this list minimal — only what the GUI needs.
Flask==3.1.3
Waitress==3.0.2
pyotp==2.9.0
PyYAML==6.0.3
pytest==8.3.4
# Screen-06/07 xlsx export (/api/export/positions, /api/export/trades).
# Pinned to the SAME version the main system already runs (requirements.txt:13,
# "DR5: daily_review .xlsx output") so the two venvs cannot drift apart.
# NOT a broker package -- I4 isolation is unaffected.
openpyxl==3.1.5
