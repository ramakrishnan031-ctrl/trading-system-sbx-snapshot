# PATHS — Quick Reference (Trading System v2)
# Full map + audit: docs/SYSTEM_MAP.md  ·  Last updated: 2026-08-07
#
# ⚠️ HONEST LABEL: this is no longer a "quick reference". It is ~120KB and grows with
# every deploy (75KB at the 04-Jul audit → ~120KB now). It works as a WHAT-CHANGED-
# RECENTLY log, read top-down — the newest banners are at the top and that is the point.
# It is NOT an orientation document; a new reader should not start here.
# (P4-7. The restructure — and P4-6's SYSTEM_MAP split — are open items: they need an
# agreed shape before content moves, since every report and memory entry points into
# these two files by section. Do not split either one silently.)

> ⚠️ Read `docs/SYSTEM_MAP.md` before any VM/system work. **Deploy ≠ restart.**

> 🟢🖥️🔝 **02-Sep-2026 23:39 IST — THE 22-SCREEN GUI CAMPAIGN IS MERGED AND PUSHED.** `origin/main` `39292d3` → **`686df1c`** (a MERGE, ⛔ not a rebase; parents `39292d3` + `0bbe127`; 121 GUI commits, 0 code collision). 🔬 **origin/main == VM bare == VM deployed work-tree**, all three measured independently. 🔬 **Zero new failures on BOTH axes** — trading gate **10F/6007P/5S** at A *and* C, GUI suite **1F/2171P** at B *and* C, and the failure **SETS are identical**, ⛔ not merely the counts. 🔬 Outside `ops_dashboard/` and `docs/` the diff is only `PATHS.md` + `UNPUSHED_LEDGER.md`; across `core/ capital/ orders/ signals/ screening/ strategies/ alerts/ broker/ config/ deploy/ tests/ main.py` it is **EMPTY** ⇒ ⭐ **the engine executes byte-identical code at tomorrow's 08:15 boot.** ⏸ 👤 **RAMA OWES `sudo systemctl restart gui-dashboard`** — 🔬 the post-receive hook contains **zero** `systemctl`/`restart`/`reload` references (measured on the LIVE VM hook, ⛔ not the repo copy) and the unit has been up since **06:19:53** with no file-watching ⇒ ⛔ **it is still serving the OLD templates.** 🔴 ⛔ **NO screen is `VERIFIED LIVE`** — every approval so far was a LOCAL render. ⛔ **F2-CORE `587b306` stays UNPUSHED** (`feat/f2-core-30aug`, on ⛔ no remote). 📄 `UNPUSHED_LEDGER.md` — *WINDOW CLOSED — 02-Sep-2026*

> 🖥️🎛️🔝 **01-Sep-2026 — S17 CONTROLS `<APPROVED>`; GUI TRACK NOW **19 GREEN / 2 QUALIFIED / 1 PENDING (S16 only)**. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED.** 🔴🔴 **THE CONTROL PLANE WAS DESIGNED 17-Aug AND NEVER BUILT** — 🔬 nothing on `:8600`, ⛔ no `CONTROL_SECRET`, ⛔ **the trading process serves NO HTTP AT ALL**; `control_client.py` is a client for a server that does not exist and is ⛔ not even deployed. ⇒ ⭐ **S17 is truthfully 100% VIEW, ⛔ not 95/5**, every control disabled WITH its reason. ⛔ **NO write path was created.** 👤 Rama REJECTED the first render — *"no overlap is not visual acceptance"* ⇒ 🔬 **2366px (2.19× viewport, 9 scattered rows) → 1540px, 4 clean bands, 0 overlaps.** 📄 `UNPUSHED_LEDGER.md` Entry 32

> 🖥️📦🔝 **01-Sep-2026 — S22 HOLDINGS `<APPROVED>`; GUI TRACK NOW **18 GREEN / 2 QUALIFIED / 2 PENDING**. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED.** 👤 Rama approved the render. 🔬 **2 defects fixed, ⛔ neither the KPI:** a filter now returns to page 1, and a **stale stamp shows its date** (`18-Aug 15:45:02`), which is ⭐ *why* `System Holdings 0` looked wrong beside `System Qty 1` — 🔬 two TIME BASES (open-now vs the 18-Aug record), ⛔ not an error; ⛔ **no number was changed to make panels agree**. ⚠️ Symbol stays **78px** ⇒ 🔬 `UTTAMSUGAR` truncates at a TRUE 1920 — 👤 a SEPARATE visual decision. ⛔ Deferred global table rule NOT applied (🔬 `style.css` **zero** diff). 📄 `UNPUSHED_LEDGER.md` Entry 31

> 🖥️🔎🔝 **01-Sep-2026 — S21 SCANNER ATTRIBUTION `<APPROVED>`; GUI TRACK NOW **17 GREEN / 2 QUALIFIED / 3 PENDING**. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED.** 👤 Rama approved ~16:0x IST; build `921969c`. ⚖️⚖️ **A DATED RULING OF RAMA'S WAS REVERSED, and that is the entry's point.** 📄 The template AND service both carried *“⛔⛔ NO SCANNER COLUMN IN THE MAIN TABLE (Rama, 16-Aug) — Scanner and Strategy are 1:1, 🔬 measured: 16 scanners onto 16 distinct strategies”*, with the identity surviving in SCANNER MAPPING. 👤 **The 01-Sep contract reverses it and answers that reasoning head-on:** *“Keep the word Scanner wherever it is meaningful in this screen; do not rename or remove the Scanner concept merely because it maps 1:1 to Strategy.”* ⚠⚠ **THE 1:1 MEASUREMENT STILL HOLDS — it was never wrong, only its CONCLUSION was overturned** ⇒ the cell reads the **SAME row's own `scanners` list**: 🔬 **16/16 rows `scanners == [strategy]`, zero divergence** — ⭐ one identity shown twice by request, ⛔ NOT a second dataset. ⭐ And what the old rule was REALLY protecting — that Scanner must never become a second independent dataset — is now guarded by **its own test** rather than by the column's absence; ⭐ that is the durable part, the column was only the means. 🔧 **THE TWO COLUMNS:** ⭐ **`#` is a SERIAL, ⛔ NOT the payload's `rank`** — the index of the CURRENT sort, so re-sorting renumbers 1..n (🔬 confirmed: sorting by Signals kept serials **1,2,3,4** while the SCANNERS beneath them changed); ⛔ `nosort`; ⭐ still draggable; ⭐ medals follow the top-3 POSITIONS; ⛔ `rank` untouched and still driving SCANNER RANKING · ⭐ **Trade Type needed NO new calculation** — already in the payload as the strategy's own YAML `intent` via the shared `strategy_meta` path S19/S20 use, ⛔ never inferred from the scanner NAME (the contract forbids it in as many words). ⚠⚠ **`COLS_KEY`→v2, ⛔ NOT cosmetic:** a stored v1 order lists the OLD thirteen keys and `initCols` **appends what is missing**, so a returning operator would have got Scanner and Trade Type at the **FAR RIGHT and no `#` at all** — ⭐ Screens 14 and 20 both paid for this, and **this is the third occurrence and the FIRST caught BEFORE shipping**. ⚠️ **ONE BACKEND FILE, and only because the change forced it:** `EXPORT_HEADER` is bound to the table labels **by an existing test**, so the export had to follow — 🔬 verified against a **real downloaded workbook** (header matches, three sheets survive). ⚠️ **THREE STALE RECORDS CORRECTED** — the footer note still said *“Strategy is the identity shown”*, the service docstring's HEADING still read *“NO SCANNER COLUMN”* above a body describing its own supersession, and the payload still published a `scanner_column` **GAP for a column that now exists**; ⭐ a stale explanation is worse than none — it teaches the wrong model. ⛔ **THE DEFERRED GLOBAL TABLE RULE WAS NOT APPLIED and its absence was MEASURED, ⛔ not merely skipped:** 🔬 `thead` `position: static`, wrap `max-height: none`, ⛔ no vertical scroll, all 16 rows in view; ⛔ no global selector touched, ⛔ no compensating visual change. ⭐ **DATA IS THE 15:54 VM RE-PULL, ⛔ not the 11:41 extract** — 👤 stale logs are forbidden where newer exist, and 🔬 the gap was large: **787 → 4,083 signals**, 2 → 3 closed trades. 🔬 **S21 142 passed**; ⭐ two superseded tests **rewritten, ⛔ not deleted** (the no-Scanner test became the **not-a-second-dataset** guard); full suite **2117 passed, 1 failed** = the `kiteconnect` environment artifact ⇒ ⛔ zero regressions. 🔴 ⛔ **NOT `VERIFIED LIVE`** — one real trading day. 📄 `UNPUSHED_LEDGER.md` **Entry 30**.

> 🖥️🩺🔝 **01-Sep-2026 — S20 STRATEGY HEALTH `<APPROVED>`; GUI TRACK NOW **16 GREEN / 2 QUALIFIED / 4 PENDING**. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED.** 👤 Rama approved ~14:2x IST; build `a2fe2e4`. ⭐ Two changes asked for — a serial column and S19's table interaction — and the investigation ran FIRST: ⭐ **both proved UI-ONLY**, 🔬 `git diff --name-only` returning **nothing** under `backend/`/`services/`/`readers/`/`api/` with every added selector scoped to `.sh-page` ⇒ the verdict is **measured**, ⛔ not asserted. ⭐ **WHAT ALREADY EXISTED** (so ⛔ nothing was rebuilt): the health score is the **real weighted composite** at exactly **Activity 30 / Signal Quality 25 / Acceptance Rate 20 / Trade Activity 15 / Errors 10** (`strategy_health.py:57-61`), ⛔ not a status lookup · **silent detection reads `gui_config.silence.yellow_max_min` = 120 min = 2 Hours, READ-TIME ONLY with ⛔ NOTHING WRITTEN** (:186-203) ⇒ ⭐ the read-only dashboard boundary holds · **“Scanner Offline” keeps the artwork's EXACT label** under the recorded 16-Aug ruling (:64, :550), **derived** ⛔ not invented · export `/api/export/strategy-health` · column drag already worked. 🔧 **(1) THE SERIAL COLUMN IS NOT DATA — that is the whole point.** It renders the **index of the CURRENTLY SORTED, FILTERED set** via `x-for="(r, i)"` ⇒ ⛔ never read from the row, ⛔ never stored, so re-sorting/filtering **renumbers 1..n on the spot**. 🔬 **VERIFIED:** sorting by health score kept serials **1,2,3,4** while the STRATEGIES beneath them changed; Disabled⇒`1`, Silent⇒`1..15`, unfiltered⇒`1..16`. ⛔ It carries `nosort` (sorting BY a row number would sort by the order the sort itself produces) ⭐ but stays **draggable**. ⚠⚠ **THE `COLS_KEY` BUMP TO v3 IS NOT COSMETIC** — a stored **v2** order lists the OLD eleven keys and `initCols` **appends anything missing**, so a returning operator would have found **`#` at the FAR RIGHT** instead of first: ⭐ precisely the **Screen-14 lesson the template's own v2 note already records**, the same trap one screen later. 🔧 **(2) THE FREEZE-PANE**, reusing S11/S18/S19 rather than a **fourth** scrolling implementation. ⭐ **FOURTEEN rows, ⛔ NOT S19's twelve** — this artwork draws 14 and says *“Showing 1 to 14 of 14”*. 🔬 **546px MEASURED AT SUB-PIXEL**: thead **32.00** + 14×**36.67** ⇒ row 14's bottom at **545.33** — ⭐ **S19's rounding lesson APPLIED rather than repeated** (there it showed 11 rows instead of 12). ⛔ `min-height: 350px` **KEPT** (the empty-day footprint, a different job). ⛔ **S20 ONLY** — 👤 the global table rule stays **deferred** until every screen is built. 🔬 **BROWSER-VERIFIED, ⛔ not from source:** 14 rows visible · wrap **546 client vs 619 scroll** · **the header's top does not move** across a full scroll, reaching **row 16** · drag works with the sticky header · the serial header **refuses to sort** · page **1485→1412**. ⚠️ **A WIDTH I DID NOT CLAIM:** the 1440px check ⛔ could not be done as a resize (the browser sits at **75% page zoom**, so the CSS viewport never changed) ⇒ ⭐ rather than report a width not really rendered, the **MECHANISM** was tested — squeezing the panel to **1400/1100/900px**, the table clamps to its **1258px** min-width and the **WRAP** scrolls horizontally while the **PAGE never overflows**, header sticky throughout. ⚠️ **TWO EXISTING TESTS REPAIRED, both left SHARPER:** `APPROVED_COLUMNS` now leads with `#`, and 🔴 the heading-fits-on-one-line test matched `.sh-page .sh-tbl th` **BY PREFIX**, silently grabbing the new `thead th` rule and asserting `nowrap` against it — ⭐ the match is now **exact**, ⛔ the property unchanged. 🔴 ⛔ **NOT `VERIFIED LIVE`** — approved on a genuinely **QUIET** real day (🔬 15 Silent / 1 Disabled, 787 signals, 6 trades) and ⭐ that is exactly what the screen shows; ⛔ no demo rows, ⛔ nothing fabricated. ⚠️ Still **NOT INSTRUMENTED**: **HEALTH TIMELINE** and **RECENT HEALTH EVENTS** — ⛔ the artwork's *“(Example)”* timeline was **NOT** turned into production history. ⛔ **Scanner not duplicated** — 🔬 exactly ONE occurrence, the approved **“Scanner Offline”** label. 🔬 **S20 102 passed** (100 + 2 guards), ⭐ **RED-CAPABILITY PROVEN BY THREE MUTATIONS**; full suite **2115 passed, 1 failed** = the `kiteconnect` artifact, ⭐ the count rising **2113→2115** by exactly the two guards ⇒ ⛔ zero regressions. 📄 `UNPUSHED_LEDGER.md` **Entry 29**.

> 🖥️🏆🔝 **01-Sep-2026 — S19 STRATEGY RANKING `<APPROVED>`; GUI TRACK NOW **15 GREEN / 2 QUALIFIED / 5 PENDING**. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED.** 👤 Rama approved ~13:4x IST; build `f1e97a9`. ⭐⭐ **THE BRIEF INVOKED THE PRE-BUILD REVIEW GATE** (S19 is business/signal-path adjacent) ⇒ the investigation ran FIRST and ⛔ no code was touched until it cleared — ⭐ **and it cleared as UI-ONLY**, which is the entry's point. 🔬 **`git diff --name-only` returned NOTHING under `backend/`, `services/`, `readers/`, `api/`** ⇒ the verdict is **measured**, ⛔ not asserted. ⭐ **WHAT ALREADY EXISTED:** the ranking engine is complete and ⛔ is NOT a parallel engine — `services/strategy_ranking.py` owns the composite score (`_score` :93) in the artwork's own **35/25/20/20** weights (:56), the trend (`_classify_trend` :142) against a **real previous equal-length window**, the six KPIs (:324) and five modes (:48), and 🔬 it reads `db_reader.closed_trades_range` :172 aggregating through `analytics_period._aggregate` :174 ⇒ ⭐ it **REUSES** the existing engine; ⛔ no duplicate, ⛔ no shadow calculation. Column drag already worked (`colDragMixin()` :313, `draggable` :136). ⚖️ **A DATED RULING FOUND AND RESPECTED:** the 16th header **TRADE TYPE** is a **recorded prior instruction** (strategy YAML `intent` via `strategy_meta.py`, ⛔ explicitly NOT the order product) ⇒ ⛔ left alone, ⛔ not reported as a deviation. 🔴 **THE ONLY GAP — THE TABLE CONTRACT:** 🔬 `.sr-tbl-wrap` carried `overflow-x` **alone** (`style.css:4486`) with **no height bound**, so it grew to fit every row and the **WHOLE PAGE** scrolled (**1353px vs a 1264px viewport**) to reach rows 13-16 of 16, and the header computed `position: static` ⇒ ⛔ nothing was frozen. ⭐ Now a **freeze-pane over a scrolling 12-row body**, ⭐ **reusing S11's `.exec-rank-scroll` (:2850) and S18's `.lav-feed` (:4081)** rather than writing a second scrolling implementation. 🔬🔬 **491px IS MEASURED AT SUB-PIXEL AND THE ROUNDING MATTERED:** thead **30.92** + 12×**38.33** ⇒ row 12's bottom at **490.92**; ⚠️ rounding to 31 and 38 gives **487**, which shows only **ELEVEN** rows — ⭐ on this table a 4px arithmetic error costs a whole row, so the figure comes from the rendered box, ⛔ never from integer arithmetic. ⛔ `max-height`, ⛔ never `height` (a quiet day must not open an empty region); ⛔ no pagination substituted, ⛔ no row truncated. ⭐ **VERIFIED IN THE BROWSER, ⛔ not from source:** 🔬 12 rows fully visible · wrap **491 client vs 645 scroll** · **the header's top does not move** across a full scroll, which reveals **ranks 13-16** · **column drag still works with the sticky header** (real HTML5 handlers driven: TRADES 3→5, sort fired, order restored) · page **1353 → 1199** · ⛔ no page-wide horizontal overflow. 🔴 ⛔ **NOT `VERIFIED LIVE`** — ⚠️ the default *Today* window holds only **2** completed trades and reads as dashes, so the approval render used **the screen's OWN Date Range filter**: *This Month* = **117 REAL trades across 13 of 16 strategies** (2026-08-03→09-01); ⛔ no demo rows, ⛔ no config pointer, ⛔ nothing fabricated, ⛔ default period unchanged in code. 🔬 **S19 75 passed** (74 + the one guard), ⭐ its **RED-CAPABILITY PROVEN BY MUTATION** (`max-height`→`height` red; dropping `position: sticky` red; both reverted); full suite **2113 passed, 1 failed** = the `kiteconnect` environment artifact, ⭐ the passed count rising **2112→2113** by exactly the guard added ⇒ ⛔ zero regressions. ⛔ Scanner absent (0 in text/classes/attributes). ⛔ S16/S17 untouched — 👤 held to be built LAST. ⛔ Paper/live parity untouched. 📄 `UNPUSHED_LEDGER.md` **Entry 28**.

> 🖥️📡🔝 **01-Sep-2026 — S18 LIVE ACTIVITY `<APPROVED>`; GUI TRACK NOW **14 GREEN / 2 QUALIFIED / 6 PENDING**. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED.** 👤 Rama approved ~13:0x IST; builds `f9d5fe3` + `f860f82`. 👤 **S16 and S17 are HELD by Rama's 01-Sep decision — they are built LAST**, so the queue is S19–S22 then S16–S17. 🔧 **PASS 1 — THE ARTWORK'S THREE BANDS:** the work area was two independent strips of four panels; the PNG draws band A as two strips (feed ǀ pipeline·strategy) and then bands B and C running **ACROSS BOTH** (x205→1240) ⇒ **FEED FILTERS** moved beside Winners/Losers and System Events, **CAPITAL UTILIZATION** beside ACTIVE POSITIONS, both spanning `1 / -1` at the artwork's own ratios. ⚠⚠ **AN EARLIER SESSION HAD BUILT FULL-WIDTH ROWS AND RECORDED THEM REJECTED** — a grid row is as tall as its tallest cell, so a short feed left a band beneath it — and ⭐ **that mechanism is ADDRESSED, ⛔ not ignored**: only `.lav-main` stretches and the feed's own scroll WINDOW takes the slack, so band A closes on **REAL ROWS becoming visible** (430→560px against 540 stored events); 🔬 feed 694, side 694, **difference 0**. 🔧 **PASS 2 — 🔴 THE RESIDUAL BAND, AND MY OWN WRONG EXPLANATION OVERTURNED.** A 150px band remained under WINNERS, and ⛔ I had called it unavoidable (*"stretching would only relocate the emptiness"*). ⭐⭐ **MEASURING THE ARTWORK PROVED THAT WRONG:** 🔬 a pixel scan of three blank columns, one inside each band-B panel (**x612 / x918 / x1235**), returns the **SAME** border rows for all three — top **y=586**, bottom **y=785** ⇒ the artwork's panels are **EXACTLY equal height (199px)**; band C the same, 797→969. ⇒ **the artwork STRETCHES its bands.** ⭐ The scale also proved the tall panel was never the fault: PNG left band x202-1247 = **1045px** vs our **1961px** = **×1.877**, so the artwork's 199px band scales to **373.4** and SYSTEM EVENTS measures **375** here. ⚠️ The `align-items: start` replaced came from a **BAND-A** lesson **over-generalised** to the bands. 🔧 Three changes, every figure read off the artwork: `align-items: stretch` on the bands · 🔬 WINNERS card footprint **174→218px** (artwork card y634-750 = 116 × 1.877; ⚠️ the first pass used 174 from an **inferred** 1.66 scale) · 🔬 FEED FILTERS tile footprint **149→240px** (artwork rows y629-686 + y701-757 = 128 × 1.877) — ⭐ our tiles were genuinely **undersized**. ⭐⭐ **IT FIXED THE GEOMETRY RATHER THAN HIDING IT**, the test 👤 Rama set: 🔬 the space BELOW the winners cards is **157** vs the artwork's **155.8**, and the card:panel ratio is **58% in both**; band B 375 vs 373.4, card 218 vs 217.7, tiles 240 vs 240.2, slack 135 vs 133.2 — all within ~2px. 🔬 **DEAD BAND 206px → 150px → 0px**; band B tops all 1006 / bottoms all 1381, band C 1397→1690, ⛔ no overflow, ⛔ no clipping. ⚠️ **NOT CLOSED and REPORTED, ⛔ not padded:** band C measures **293** vs the artwork's **322.8** — both panels equal and full so ⛔ no gap, and forcing it would need arbitrary padding. ⭐ **REAL DATA:** `signals`/`orders`/`trades`/`fm_ledger` pulled **READ-ONLY** from the live VM DB at **11:41:02** — 🔬 787 signals, 533 feed events, 88 ledger rows; ⛔ the 428 MB DB was never copied. 🔴 ⛔ **NOT `VERIFIED LIVE`** — 👤 at Rama's instruction ALERTS BANNER (1 critical + 2 warning) and ACTIVE POSITIONS (4 rows) carry **DEMO** data held **ENTIRELY out-of-repo**; ⚠️ 🔬 I caught one incoherence in my OWN demo first — it drove capital to **90.33%** because the figures resolve against the **REAL** ₹10,469.40 opening INIT row, ⛔ not the ₹18,469 I assumed; resized to the artwork's **42.17% / 57.83%**. 🔬 **S18 116 passed**; full suite **2112 passed, 1 failed** = the `kiteconnect` environment artifact. ⭐ **FOUR guards updated and still guards** — notably the artificial-height guard now reads a rule's **SUBJECT** rather than any ancestor, and asserts the bands **DO** stretch while band A is ⛔ still not a full-width row. ⛔ Scanner absent (0 in text, classes, attributes). ⚠️ 🏷️ **OPEN for Rama:** the feed renders **all 540 rows** client-side (`feed_page` is returned but unused), which defeated screenshot capture — the S14 strain again. 📄 `UNPUSHED_LEDGER.md` **Entry 27**.

> 🖥️🔍🔝 **01-Sep-2026 — S15 SYSTEM LOGS `<APPROVED>`; GUI TRACK NOW **13 GREEN / 2 QUALIFIED / 7 PENDING**. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED.** 👤 Rama approved ~11:2x IST; build `7ec180a`. ⭐ S15 was already built and already had the wide-page fix (`0ca38e2`); 👤 Rama returned it with three corrections, and ⭐⭐ **THE FIRST WAS NOT A MISSING FEATURE AT ALL BUT A LIVE DEFECT NO TEST COULD SEE.** 🔧 **(1)** 🔴 **EVENT TYPE and STATUS rendered as BLANK CELLS** — the `NOT INSTRUMENTED` markup was ALREADY THERE, but each `<template x-if>` held **TWO sibling spans**, and 🔬 **Alpine 3.14.1 builds an x-if branch with `content.cloneNode(true).firstElementChild`** ⇒ it renders **ONLY the first root and DISCARDS the rest**, so the gap span was **never created** and the survivor was `x-show`-hidden **exactly when the value was missing**. ⭐ The one thing this screen must never do, done **SILENTLY**: ⛔ no error, ⛔ no console warning, ⛔ no failing test. 🔬 **A/B on TWO live instances (`:8501` pristine vs `:8500` corrected): 20 of 20 cells BLANK → 0 of 20.** ⭐ **SWEPT THE CLASS, ⛔ not the instance** — all **33 templates** scanned; 🔬 **exactly two multi-root `x-if` blocks exist and both are these** ⇒ ⛔ no other screen affected, and a test now pins the rule. ⭐ Real values untouched: a Scheduler row still shows real `Status=Success`, `RECOV-7903` a real `Event Type=Recovery Completed`. 🔧 **(2)** **COMPONENT TIMELINE** rebuilt as the PNG's **HORIZONTAL four-node pictorial diagram** — a vertical `<ol>` was precisely what the TXT forbids (*"do not replace diagrams/pictorial elements with plain text"*); the TXT's arrow list is its ASCII rendering of the SAME four stages and **SOURCE AUTHORITY 1 (the PNG) is binding for composition**. ⛔ NO new backend, ⛔ no new data — same `detail.timeline`, same `measured` flag; the rail is a `::before` drawn from the previous node's centre so it is positioned **BY** the nodes and ⛔ cannot drift. ⛔ **The PNG's "Duration: 2 sec" has NO source and says NOT INSTRUMENTED** — 🔬 the measured stages share ONE timestamp, so subtracting them would have manufactured a `0 sec` dressed as a measurement. ⚠️ Two layout defects found by MEASURING: 🔬 `.slg-ni`'s `nowrap` made four labels overrun a **102px** column (label needs **123px**) and collide ⇒ nowrap lifted only inside the timeline (wrapped: **94px**); then row 3 moved to **the artwork's own ~1 : 1.34 : 1 : 1** ⇒ stage column **123px**. ⛔ No word shortened to a dash, ⛔ nothing below the 13px floor. 🔧 **(3)** **SEARCH + EVENT TYPES INTEGRATED into FILTERS, ⛔ not deleted** — 🔬 the **PNG places NEITHER as a panel** (its search is one toolbar box; Event Type is a dropdown) and the **TXT** asks to preserve the search ***capability***, ⛔ not a full-width block. All five approved fields move across unchanged, **Error Code still SHOWN and DISABLED** (no error-code scheme exists); the eleven counts compact to a chip strip keeping the distinction that matters — 🔬 `Service Restarted 0` is **instrumented and genuinely zero** while `Connection Lost NOT INSTRUMENTED` is **unmeasurable**, ⛔ and a 0 is never shown for the latter. ⛔ `.slg-rowx` **REMOVED, ⛔ not emptied**. 🔬 Page **2158px → 1941px (−217px)**; removed row **438px**, FILTERS **125px → 346px**. 🔧 **(4)** ⭐ The width fix of `0ca38e2` is **preserved exactly** — ⛔ no global CSS, every rule scoped to `.slg-page`. ⚠️ **(5) A REGRESSION FOUND ON ARRIVAL:** the S15 suite was **ALREADY RED at 8F / 72P** — 🔬 `0ca38e2` changed the root to `class="dash-page slg-page"` and the test helper hard-coded `'<div class="slg-page"'`, matching nothing, returning **-1** and taking **EIGHT tests** down, each reporting *"the page root is missing"* instead of the real change; the matcher is now **class-aware**. ⭐ **EVIDENCE:** the brief's HARD RULE honoured — today's dated `system_`/`reconciler_`/`trades_` logs (**28-Aug…01-Sep**) + `system_events`/`cron_heartbeat`/`reconciliation_log` pulled **READ-ONLY** from the live **428 MB** VM DB (⛔ the DB itself never copied, ⛔ nothing written on the VM outside `/tmp`). 🔬 **11,656 events · Info 11,565 (99.22%) · Warning 83 · Error 8 · Critical 0 · last event 10:49:24 TODAY** — ⭐ severity is genuinely Info-dominated and ⛔ NOT reshaped toward the PNG's 55%. ⛔ No Scanner anywhere. 🔬 **S15 82 passed, 0 failed** (from 8F/72P) with **+2 guards**; full suite **2112 passed, 1 failed** = `test_c_venv_has_no_kiteconnect`, ⭐ **proven environmental against a PRISTINE checkout of HEAD where it fails identically**. ⭐ RED-capability proven **by injecting a blank cell** (the probe caught it) and **by emitting a probe warning** (the console check captured it) ⇒ ⛔ neither zero was vacuous. 🔴 ⛔ **NOT `VERIFIED LIVE`** — a READ-ONLY VM EXTRACT via the git-ignored `gui_config.local.yaml`; ⛔ no repo edit, ⛔ no demo DB, ⛔ nothing to revert. 📄 `UNPUSHED_LEDGER.md` **Entry 26**.

> 🖥️🔍🔝 **30-Aug-2026 — S14 TRADE LOGS `<APPROVED>`; GUI TRACK NOW **12 GREEN / 2 QUALIFIED / 8 PENDING**. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED.** 👤 Rama approved ~22:4x IST; build `3885208`. ⭐ S14 was **ALREADY FULLY BUILT** — 12 panels, all bindings, 83 tests — and ⭐ **Scanner was ALREADY REMOVED and DEFENDED AT THREE LAYERS** (rendered page + JSON payload · the export header · **the SQL itself** — `signals.scanner` exists in the schema and is ⛔ deliberately UNREAD). ⭐ The separate **Scanner Attribution** nav link is correctly untouched. ⇒ a visual pass, ⛔ not a rebuild. 🔧 **(1)** ⭐ **THE TRADE REPLAY PANEL**, built into a 🔬 **MEASURED blank region**: `.tlg-rail` ran **543px past** `.tlg-row3`, leaving **1222×559 px** dead INSIDE `.tlg-left`. ⚠️ ⭐ That measurement drove the PLACEMENT: a full-page-width band below both columns would have left the hole exactly where it was and ⛔ only made the page taller. 🔬 Dead band **559px → 16px**; panel 1222×345. ⛔ **NO NEW BACKEND** — the 8 stages ARE `detail.timeline`, the same ones TRADE TIMELINE renders. ⭐⭐ **THE ANIMATION STEPS ONLY THROUGH `measured` STAGES** — that ONE rule makes a fabricated transition **structurally impossible**, ⛔ not merely discouraged, and delivers all three required behaviours with ⛔ no special-casing. 🔬 VERIFIED LIVE ON TWO TRADES: the completed one stepped Signal→Validation→Order→Fill→Position→Exit **SKIPPING Risk and Capital** (no timestamp); the zero-fill `TRD-2026-000807` marked **Order 10:58:34 TERMINAL**, Fill/Position **not reached**, Exit **pending / position still open**. ⭐ Four states drawn distinctly because they MEAN different things: recorded · NOT INSTRUMENTED (with the system's own reason) · not reached · replaying. 🔧 **(2)** Lower row **rebalanced** — AUTO-RECOVERY and RECENT ERRORS were both clipped while EXPORT held `.8fr` it does not use. ⚠️ ⭐ RECORDED HONESTLY: **the first attempt was WORSE than the defect** — wrapping the cells cleared the clip but turned every RECENT ERRORS row into THREE lines; 🔬 measured, reverted, width-change only. 🔧 **(3)** ⭐ An S13 test of mine fixed **at root**: its *touched-no-other-screen* scan ran to **EOF**, so the next screen's correctly-scoped block failed it (S14's `.tlg-page .tlg-row4` did exactly that) — ⭐ the WINDOW was wrong, ⛔ not the assertion. ⭐ **THE EVIDENCE SOURCE:** the brief names *VM > review*; 🔬 inspected `reports/log_review/eod_review_2026-08-28.md` and reproduced it — the payload's own counts land on the file EXACTLY: **Signal Received 4,686 · Signal Accepted 16 · Capital Rejected 58 · Order Created 16 · Position Closed 5 (TGT 1 / SL 3)**, real symbols, real rejection reasons, both slippage-guard figures, both kill-switch instants. ⭐ The REAL 28-Aug date is kept (it falls inside S14's own default 7-day window). ⚠️ **S14 SURFACES 7 GAPS and the demo ⛔ does NOT paper over them:** 🔴 **NO error-code scheme exists** (a repo-wide `XXX-0000` search returns ZERO) ⇒ the artwork's `EXCH-1016`/`RISK-2001`/`CAP-3002` ⛔ CANNOT be built · 🔴 **a PASSING risk or capital gate writes NO ROW** ⇒ *Risk Passed*/*Capital Passed* show **NOT INSTRUMENTED, ⛔ never 0** — a 0 would wrongly read as *nothing ever passed* · Risk and Capital have **no timestamp of their own** ⇒ the timeline reads *n of 8 stages recorded* · ⛔ no resolution state · ⛔ no *Recovered In* ⇒ the artwork's *18 sec* cannot be built · ⛔ no retention policy on signals/trades/orders ⇒ the artwork's *67 Days* is NOT INSTRUMENTED · ⛔ no human attribution anywhere. 🔬 **89 S14 tests pass (+6** pinning the replay's honesty rules**)**, RED-capability proven by mutation; full suite **2104 passed, 1 failed** = the `test_c_venv_has_no_kiteconnect` environment artifact. 🔴 ⛔ **NOT `VERIFIED LIVE` — REPRODUCED data via the out-of-repo harness on :8501**; ⛔ no repo edit, ⛔ no demo DB, ⛔ no pointer. 📄 `UNPUSHED_LEDGER.md` **Entry 25**.

> 🖥️🧾🔝 **30-Aug-2026 — S13 AUDIT `<APPROVED>`; GUI TRACK NOW **11 GREEN / 2 QUALIFIED / 9 PENDING**. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED.** 👤 Rama approved ~20:1x IST; build `3fa2e78`. ⭐ S13 was **ALREADY FULLY BUILT** — every panel the PNG draws existed and was bound to real audit readers ⇒ this was a **visual pass, ⛔ not a rebuild**. 🔧 **(1)** **CRITICAL CHANGES moved out of the table-row rail** to sit beside the lower analytics — the relationship the design TXT names verbatim. ⚠️ ⭐ It is ALSO a measured defect fix: with a record selected the rail stood 🔬 **1474px against a 528px table = 946px of DEAD SPACE** (page 2457px) ⇒ now 🔬 **448px** (page 2074px). 🔴 **The void appeared ONLY AFTER a row was clicked** — ⛔ which is why an unselected screenshot never showed it. ⭐ The new `.aud-lower` band deliberately MIRRORS `.aud-main` (`minmax(0,1fr) 330px`) so the panel lands under AUDIT DETAILS; ⭐ a test pins BOTH rules to the same columns, because if they drift the panel stops lining up and the move is pointless. 🔧 **(2)** **Bottom export bar** → note-left/buttons-right with the shared `.btn-export`, as S09/S11/S12. ⚠️ The rule was **ALREADY `space-between`** — `flex-wrap: wrap` plus the long honesty note wrapped the buttons onto their own line at the LEFT; ⭐ `nowrap` is the load-bearing part. 🔧 **(3)** **One width for every filter select** — Action rendered ~2× its siblings purely because its option strings are longer; ⛔ a control's width must not encode its data. ⏸ **REPORTED, ⛔ NOT CHANGED:** the artwork puts OLD/NEW in a right sub-COLUMN of Audit Details (🔬 its rail ~412 CSS px vs our 330); the design TXT specifies the STACK itself — *OLD VALUE ↓ NEW VALUE* — which is exactly what renders, and widening the rail would squeeze a 9-column table ⇒ 👤 Rama's ruling owed. ⚠️ **S13 SURFACES SIX REAL INSTRUMENTATION GAPS, and the demo does ⛔ NOT paper over them:** 🔴 **WHO changed a setting is NOT CAPTURED ANYWHERE** — no human attribution exists in the schema, so the PNG's `ramakrishnan` User column ⛔ CANNOT be built; the column shows the recorded PROCESS or an explicit gap, and Top Users states *all recorded actors are PROCESSES, not people* and **excludes** the unattributed rather than assigning them to anyone · **TIMELINE: only ONE instant per record exists** ⇒ Applied is real, Requested/Validated/Confirmed are NOT INSTRUMENTED · **SOURCE:** only an inbound webhook proves its own origin (API) · **RETENTION PERIOD:** no audit retention policy exists ⇒ the oldest→newest span is labelled a MEASUREMENT, ⛔ never a policy · **CONTROL HISTORY:** `kill_switch_state` is ONE row ⇒ no history · **AUTHENTICATION:** no source at all. ⭐ The **OLD→NEW values ARE GENUINE** — produced by the real `_config_changes` diff of consecutive config snapshots, the SAME code path production uses, ⛔ not written by hand. 🔴 ⛔ **NOT `VERIFIED LIVE` — APPROVED ON DEMO DATA:** 🔬 the real VM snapshot holds **3** audit records across all of Jul–Aug, and 30-Aug is a Sunday. ⭐ The fill ran through the **out-of-repo review harness on :8501**, feeding RAW TABLE ROWS to the real readers so `build_audit` did its own diffing, filtering and aggregation on top — ⛔ no repo edit, ⛔ no demo DB, ⛔ no pointer, ⛔ nothing to revert. 🔬 **77 S13 tests pass (+4)**, RED-capability proven by mutation; full suite **2100 passed, 1 failed** = the `test_c_venv_has_no_kiteconnect` environment artifact. 🔬 Every added CSS selector is `.aud-page`-scoped, pinned by a test that walks the added block. ⛔ No backend, ⛔ no readers, ⛔ no bindings touched. 📄 `UNPUSHED_LEDGER.md` **Entry 24**.

> 🖥️⚡🔝 **30-Aug-2026 — S12 SYSTEM HEALTH `<APPROVED>`; GUI TRACK NOW **10 GREEN / 2 QUALIFIED / 10 PENDING**. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED.** 👤 Rama approved ~17:3x IST; build `bb1e0a9`. ⭐ A real pass against the artwork, ⛔ not a cosmetic patch. 🔧 **(1)** The band is **FIVE CARDS** (Overall/Healthy/Warning/Failed/Uptime) — the **UNKNOWN card is GONE**; ⛔ dropping the card must ⛔ NOT drop the STATE, so UNKNOWN's survival in the table, the status filter, the legend AND the colour map is pinned by test. 🔧 **(2)** Icons + per-status shares restored; `pctOf` returns an **em dash, ⛔ never 0.00%**, when the fleet size is unknown — a confident zero is a fabricated denominator. 🔧 **(3)** **VM metrics NESTED INTO the Uptime card** as the artwork draws them; the rail panel is deleted and the service table takes the full row. ⭐ RAM-available has no row in the artwork but IS measured, so it is kept — ⛔ losing a real number to a layout change is a silent regression; 🔬 all six still route through `gapCell`. 🔧 **(4)** Dependencies become **FIVE HORIZONTAL ICON CARDS** in a band of their own. 🔧 **(5)** ⭐ **THE THROUGHPUT PANEL — in the PNG, ABSENT from the design TXT** — built on the **EXISTING `activity_pulse` reader** (⛔ no new query, ⛔ no new table): signals by `received_at` · entry orders by `placed_at` · fills by **`trades.entry_time`**. ⚠️ The third series is the **FILL**, ⛔ NOT an order status — reading `orders.status` would count a broker ACK as a fill. ⛔ **AN EMPTY CHART IS A REAL ZERO**, ⛔ never NOT INSTRUMENTED: those three tables are read live, and claiming otherwise would assert the system cannot count its own orders. ⚠️ The artwork says PER MINUTE; a session is ~375 minutes and will not fit, so the counts are **summed into hours and the panel says so** — ⭐ the label follows the AGGREGATION, ⛔ not the drawing. 🔧 **(6)** Bottom **export bar** on the existing `/api/export/system-health`, + a Throughput sheet so the workbook cannot omit a visible panel. ⭐ **PRE-EXISTING DEFECT FIXED, found by MEASUREMENT:** the **13px typography floor was acting as a CAP** — `.sysh-page .num-neg` sits at the SAME specificity as `.sysh-page .sysh-ov-v` and appears LATER, so the instant Alpine put the semantic colour on the status word the artwork's largest text collapsed; 🔬 computed **13px where 1.7rem was written**, now 27.2px. ⭐ Restated one level deeper — ⛔ the floor itself is untouched and `test_b1_typography_floor` still passes. 🔬 **37 of 37 added CSS selectors are `.sysh-page`-scoped** and `components.html` is untouched, so ⛔ no other screen's `kpi_card` render moves. 🔴 ⛔ **NOT `VERIFIED LIVE` — APPROVED ON DEMO DATA.** ⚠️ 👤 **30-Aug is a SUNDAY, and on weekends/holidays the VM and trading system DO NOT RUN (SOFT_KILL)** ⇒ engine down, no uptime, NOT READY and empty series are the **CORRECT** state, ⛔ not a fault — and there was **no live data to review against until Monday**. ⭐ So the fill came from an **out-of-repo REVIEW HARNESS** that patches the **reader layer in-process** and serves the real app on **:8501** — ⛔ NO repo edit, ⛔ no demo DB, ⛔ no config pointer, ⛔ nothing to revert; 🔬 verified 4 modified files and 0 new untracked. ⭐ Patching at the READER boundary was deliberate: every rollup, the worst-wins logic, the readiness gates, the semantic colouring and the export all ran FOR REAL on demo inputs. 🔬 **117 S12 tests pass (+13)**; full suite **2100 passed, 1 failed** = `test_c_venv_has_no_kiteconnect`, the environment artifact that fails identically on a pristine checkout. 📄 `UNPUSHED_LEDGER.md` **Entry 23**.

> 🖥️⚡🔝 **30-Aug-2026 — S11 EXECUTION ANALYTICS `<APPROVED>`; GUI TRACK NOW **9 GREEN / 2 QUALIFIED / 11 PENDING**. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED.** 👤 Rama approved ~15:5x IST. ⭐ S11 was already BUILT (`3979b1b`, 15-Aug) with Scanner already removed — this was the **visual-approval pass** and its three corrections, ⛔ not a rebuild. 🔧 **(1)** `INTRADAY & DELIVERY EXECUTION ANALYSIS` takes the footprint the reference gives Scanner Ranking (**between Strategy and Symbol**), built from the **SAME generic `_rank`** on a pipeline label derived from the ENTRY product by the SAME rule the capital path uses (`pipeline_for_product`: not-CNC ⇒ intraday) — 🔬 on real VM data Intraday **187** · Delivery **84** · UNKNOWN **36** = **307 = execution_count exactly**, a true partition. ⛔ **UNKNOWN is its OWN ROW** — a trade with no ENTRY order has no product, and folding it in would fabricate the very dimension the panel exists to separate. 🔧 **(2)** **Symbol became a LEFT label column** (heading AND data together); ⛔ column SET unchanged so `COLS_KEY` stays v1 and no saved order is discarded. 🔧 **(3)** **NEW `TRADE STATE` FILTER** — ⚠️ the screen rendered TWO status-like columns and only one was filterable: `status` = the **DELAY BAND** (fast/moderate/slow) vs `trade_state` = the **LIFECYCLE position** (closed/open/pending/rejected) ⇒ *"show me only the CLOSED ones"* was impossible on a screen that displays the column. ⭐ It runs through the **SAME single filter gate** every panel reads, so KPIs, table, rankings, distribution, throughput, warnings **and the XLSX export** narrow together — ⛔ no panel can describe a different population. ⭐ The dropdown offers the **FULL vocabulary**, ⛔ not just states present this period (the option to isolate REJECTED must not vanish on a clean day). 🔬 260 → CLOSED 173 → OPEN 54 → REJECTED 16. ⭐ Also: the artwork's **bottom EXPORT BAR** (S11 had none), and the last user-facing Scanner wording REMOVED outright ⇒ 🔬 **0** rendered mentions. 🔴 ⛔ **NOT `VERIFIED LIVE` — APPROVED ON DEMO DATA** (260 synthetic trades), because the real-VM snapshot was too sparse to judge density: 🔬 **202 of its 307 rows never filled**. ⭐ Demo DB lives OUTSIDE the repo; pointer reverted byte-identically (md5 `6a6d93df…`, 0 pointer lines); 🔬 real DB mtime unchanged at **03-Aug 16:08** ⇒ never opened for writing. ⚠️ **THE 25-COLUMN ARTWORK STILL CANNOT BE BUILT** — 🔬 re-verified: `order_execution_log.exchange_timestamp` is a real column **NEVER WRITTEN** (the sole caller, `orders/slippage_recorder.py:149`, omits the key); risk/capital have no timestamp; `orders` has ONE instant. ⇒ 10 columns have no source and stay **NOT INSTRUMENTED**, ⛔ never `0.00`. ⏸ **REPORTED, ⛔ NOT CHANGED — 👤 Rama's ruling owed:** KPI **icons + sub-labels** (the icon needs the SHARED `kpi_card` macro every screen uses; the `MINTRADAY_001`-style labels need a new backend field) · the **page title** (S09/S10, both approved, use the identical house `panel-title` pattern). 🔬 **142 S11 tests pass**; 6 pinning tests updated to the new design and the Scanner assertion got **STRICTER** (1 mention → 0). 📄 `UNPUSHED_LEDGER.md` **Entry 22**.

> 🖥️🎨🔝 **14→28-Aug-2026 — GUI TRACK: ALL 22 SCREENS `<BUILT>`; 8 VISUALLY APPROVED. ⛔ NOT PUSHED, ⛔ NOT DEPLOYED** (branch `feat/screen10-slippage-analytics` @ `d0a4054`, worktree `D:\Projects\trading-system-gui09`; 🔬 `ls-remote` returns **0 refs**). 🔬 **APPROVAL STANDING, from `docs/audit/VISUAL_APPROVAL_LEDGER_19-Aug-2026.md` at HEAD: 🟢 8 APPROVED** (S01·S02·S03·S04·S05·S08·S09·S10) · **⏳ 2 QUALIFIED** — **S06** approved on the **PRE-correction** render (`b47e148` verified, re-rendered, ⛔ never seen) and **S07** owes a **RE-approval** (approved 14-Aug under the previous ledger; `66fc82e` landed after it, unseen; 👤 Rama's 19-Aug 13px-floor decision re-opened all 22) · **⏳ 12 NEVER SHOWN** (S11–S22). ⛔ **The earlier "9 approved / 13 awaiting" figure was WRONG in both halves — it is 8 / 12.** ⚠️ ⛔ **NEITHER S09 NOR S10 IS `VERIFIED LIVE`:** 🔬 S09 was approved on **SEEDED DEMO data** (the local DB holds **0 closed trades**, last written 03-Aug ⇒ 75 demo trades rendered) and S10 on a **read-only 2.5 MB extract of real VM data**, whose 🔬 amber/red slippage path is **LIVE BUT NEVER EXERCISED** (real slippage ~₹0.00–0.02, **100% within limit**, 0 near, 0 exceeded). ⭐ Composition/alignment are confirmed; ⛔ populated PRODUCTION behaviour is not. ⚠️ Also unseen: **S08**'s 2 post-approval restorations (gauge geometry · gated export button); **S04** owes Q1·Q2, **S05** owes Q3·Q4. 🔧 **ONE STRUCTURAL CHANGE, not a screen: `efeb0b7` made the column reorder ONE implementation, not fifteen** — `colDragMixin()` at `static/components.js:95`, with `Object.assign` placing page members LAST so existing screens keep their overrides. 🔬 **Measured 30-Aug: 16 of 30 screen templates use it, and ALL 16 iterate `cols` inside `<tbody>` ⇒ 16/16 genuinely move the DATA, 0 header-only** — ⚠️ which is the property that matters, because a table with POSITIONAL `<td>`s moves the LABEL and leaves the DATA behind. ✅ **THE REFIT IS STILL ESSENTIALLY FREE — 🔬 RE-MEASURED 30-Aug: ZERO COLLISIONS, whole tree.** 135 gui09 files vs 68 main files since merge-base `6fa8a1c` (14-Aug), **0 overlap**; 🔬 main has touched `ops_dashboard/` in **0** files, gui09 in **87**. ⇒ ⭐ a **rebase**, ⛔ not a merge fight. ⚠️ **89 ahead / 39 behind `effff24`** — ⛔ the previously recorded *"37 behind"* was measured against `52ccb4f`, ⛔ NOT `effff24` (a different comparand, ⛔ not drift); after the 30-Aug push it becomes **48 behind** `39292d3`, and it only grows with every main push. ✅ **DEMO-DB HYGIENE HELD:** the `gui_config.local.yaml` pointer was reverted **byte-identically**, the demo DB lives OUTSIDE the repo, and 🔬 the real DB's mtime is unchanged at **03-Aug 16:08** ⇒ never opened for writing. 🔴 **NEXT: S11 Execution Analytics — it is already BUILT (`3979b1b`, 15-Aug); ⛔ what it needs is its FIRST VISUAL APPROVAL, ⛔ not a build.** ⭐ The two cheapest wins are **S06** and **S07**, where the work is done and only 👤 Rama's sight is missing. 📄 Record: `UNPUSHED_LEDGER.md` **Entry 21** (this window's 90 commits, 🔬 measured).

> 🗂️✅🔝 **10-Aug-2026 — ITEM 6 / THE DAILY-CRITICAL EOD REPORT: SEED⁄STATE SPLIT. `<BUILT · TESTED · ⛔ NOT PUSHED · ⛔ NOT DEPLOYED>` — branch `fix/registry-seed-state-split` off the DEPLOYED `645728d`.** 🔑 **`config/strategy_direction_registry.yaml` STAYS TRACKED and becomes a READ-ONLY SEED; runtime state moves to `data_store/strategy_direction_registry.yaml`** (⭐ `data_store/` is ALREADY gitignored, `.gitignore:17` ⇒ ⛔ no new ignore line, ⛔ no new config key — (P) `git diff 645728d -- '*.yaml'` is EMPTY). **WHY IT WAS CRITICAL DAILY, TRACED:** `system_manager.py:1195` diffs the deployed tree → `:1218` `res.violation()` → `:884` sums → **`:1336` `severity = "CRITICAL" if (violations or reasons) else "INFO"`** — ⭐ **the report has NO WARNING severity, it is BINARY.** 🔴 **THE RECORDED PREMISE IS PARTLY REFUTED: the file is NOT “rewritten daily” — (P) `strategy_registry_officer.py:253-254` calls `save_registry` ONLY `if changed`. The real mechanism is a PERSISTENT UNCOMMITTED DIVERGENCE** (HEAD = the 17-Jul seed, **MEASURED 16 rows all `PENDING`, ⛔ not the recorded 18**) ⇒ ⛔ “treat a daily-regenerated file as expected” rests on a dead premise. 🔬 **UNTRACKING WAS MEASURED AND REJECTED: in a throwaway repo against the actual hook command (`deploy/hooks/post-receive:31`), the first `checkout -f` after `git rm --cached` DELETES the live file** — untracked-file protection does not apply to a path tracked at the OLD head ⇒ it would have cost `first_seen` for 16 strategies + a 16-row NEW alert burst. ⭐ **The split loses NOTHING: the seed is a FALLBACK (never an override), so the first post-deploy run re-CONFIRMs silently and `first_seen` survives.** 🧪 **RED-first MEASURED `8F/3P` pre-fix → `11P` post-fix; ⭐ the 3 pre-fix passes are exactly the anti-suppression CONTROLS (a genuine tracked drift MUST still go CRITICAL) — adjacent set `56P`.** ⛔ **`scripts/system_manager.py` is UNTOUCHED — (P) `git diff --name-only 645728d -- scripts/system_manager.py` is EMPTY ⇒ no severity weakened, no path whitelisted.** ⛔ **`registered_direction` NOT retired** (zero callers CONFIRMED, width = whole tree all file types; retiring it is its own decision). 📄 `docs/audit/registry_seed_state_split_10aug2026.md`. ⚠️ **PARITY: cron/report path — MODE-INDEPENDENT; ⛔ not “covered by paper”.**

> ⏱️🎯🔝 **07-Aug-2026 EVENING — THE ENTRY THROTTLE MEASURES *ENTRY → ENTRY*, AND THAT PRICES RULING 2's LOOSENING. ⛔ READ-ONLY; report = `docs/audit/rulings_1_2_verification_07aug2026.md` §8** *(appended, ⛔ not a new file — it closes that document's own OPEN-1/OPEN-2 and corrects its H1/H7 row 5)*.
> **(S) `_per_symbol_last[symbol]` is written at EXACTLY ONE place in the repo — `signals/entry_throttle.py:113`, inside `admit()`, on the admit path.** ⛔ **There is NO exit hook, no fill hook, no reconciler touch** *(width: repo-wide grep for `entry_throttle|EntryThrottle|per_symbol_cooldown|\.admit\(` over every `*.py`)*. `admit()` fires at **DISPATCH** — all **3** production `_placer.place(` sites (`signal_processor.py:1263 · :2028 · :2305`) are each immediately preceded by it (`:1249 · :2017 · :2296`); there is no 4th.
> 🔴 **SENCO 27-Jul, OPERANDS QUOTED FROM `logs/system_2026-07-27.log`:** entry-dispatch `10:02:12.687` · TGT exit `10:13:31.744` · re-entry dispatch `10:14:12.331`. ⇒ **ENTRY→ENTRY = 719.644 s = 2.399× the 300 s gate ⇒ correctly ADMITTED.** EXIT→re-entry = **40.587 s** (dispatch) / **79.413 s** (fill — this is the record's "80 s", reconciled). ⭐ **The throttle NEVER SAW 80 s. The card's "a 300 s cooldown did not stop an 80-second re-entry" is DISSOLVED, not explained — the two facts describe different intervals and NO control measures the second.**
> ⭐⭐ **PROVEN ON PRODUCTION DATA, not only read from source — the reject string prints its own operand:** `per_symbol PYRAMID 60s < 300s` @`10:07:14.035` sits **60.334 s** after `place_start` `10:06:13.701`; `61s` @`10:12:14.835` sits **60.932 s** after `10:11:13.903`. **Both match at printed precision, and PYRAMID NEVER EXITED — its 10:06 entry was BROKER-REJECTED (`MIS orders are currently blocked`)** ⇒ an exit-keyed cooldown had **no timestamp to measure from** and could not have printed either value.
> ⚠️ **NEW, in no document before today: `admit()` records BEFORE `place()` (the deliberate TOCTOU-free design) ⇒ A BROKER-REJECTED ENTRY STILL CONSUMES THE SYMBOL'S FULL 300 s** — PYRAMID is the measured instance (3 later signals blocked for a position that never existed). 🏷️ Same family as *"it counts its own rows where it means the broker's reality"*. ⛔ **Not a defect, not proposed for change.**
> 🔑 **THE NUMBER THAT PRICES OPEN-1: the cooldown's residual protection is exactly `max(0, 300 − holding_period)` ⇒ ZERO for any trade held >5 min. (P) MEASURED over 225 closed trades: 185 = 82.2 % held ≥300 s; median hold 1,596 s = 5.3× the cooldown.** ⇒ **turning gate 1 off does not fall back onto the cooldown — on ~82 % of trades it falls back onto NOTHING** *(gates 2+3 release at exit; concurrency slots are released by the exit; `max_daily_trades` has not bound since 10-Jul)*.
> 🔴🔴 **AND THE FACT THAT REFRAMES OPEN-1 ENTIRELY — GATE 1 DID NOT EXIST WHEN SENCO RE-ENTERED.** The tree deployed at 10:14 was `d3fa5b8` (26-Jul 23:19) and **both halves are ABSENT** from it. `656b62d` **27-Jul 13:54:12** wrote the code (*"DEFAULT OFF"*); `300a247` **27-Jul 15:24:31** set it `true` (*"TURN ON … (Rama, 27-Jul eve)"*). ⇒ **+3 h 40 min and +5 h 10 min AFTER the re-entry, same day: SENCO is the incident that CREATED gate 1, not a trade it happens to cover.** ⭐ Settles half of **OPEN-7** — there was no pre-existing rule to lag. ⛔ **Still a MEASUREMENT, not a recommendation; OPEN-1 is Rama's, and the counter-case (n=2 re-entries, ≈₹5 over five weeks) is untouched.**
> 📵 **Q5 — PARITY IS `(I)`, NEVER `(P)`: all 24 retained daily logs (`2026-07-07`→`2026-08-07`, the full 30-day window) read `mode=live`; ZERO paper days.** ⛔ "Never in paper" is NOT established — retention bounds the check. ⭐ **But the standing PAPER-NETS-BY-SYMBOL hazard does NOT reach this control** (it reads no broker quantity) ⇒ a paper drill here would be genuinely informative rather than vacuously green. ⛔ Not run, not proposed.

> 🚨🧺🔝 **07-Aug-2026 EVENING — ⛔ THE `330657774` SAFETY CHECK PASSED AND ITS PREMISE WAS REFUTED: ATULAUTO IS CLEAN; `DIFFNKG` IS THE LIVE-MONEY ITEM.** Answered from the **BROKER API** (`get_gtts`/`orders`/`holdings`/`positions`), ⛔ **never from `gtt_state`.**
> ✅ **`330657774` is ABSENT from the broker** (4 GTTs returned, no ATULAUTO at any status). **The system cancelled it itself:** `08:15:41.704` `cnc_gtt_monitor.forensic … "GTT active but holding flat (external close)"` → **`08:15:41.722 delete_gtt call_end … mode:"LIVE"`** → `08:15:41.795 gtt_exit exit_price 579.55 pnl −9.35` ⇒ `trd_e66ee17b…` **CLOSED `GTT_EXIT`**. ⇒ the *"no manual ATULAUTO buy"* DO-NOT **can be lifted — ⛔ BY RAMA, not by me.**
> 🔴 **BUT: `330944932` · DIFFNKG · `active` · SELL/SELL · CNC/CNC · triggers `[437.2, 459.45]` · created today `15:20:35` IS RESTING AT ZERODHA AGAINST A FLAT HOLDING.** **(P)** `holdings()` DIFFNKG `quantity=0, t1=0`; `positions()` CNC **`net=-1, buy=0, sell=1`**; `orders()` shows **ONE** DIFFNKG order today — `260807170745584` SELL CNC LIMIT **filled=1 COMPLETE 14:50:52**; and `trades.trd_010f8e21…` is still **`OPEN`** = the phantom. **The respawn is visible as a sequence:** `330658430` TRIGGERED (the 14:50 fill) → `330940420` created `15:05:26` TRIGGERED `15:08:11` → `330944932` created `15:20:35` **ACTIVE**. 🏷️ **This is F6 cost 4 — "three live SELL GTTs on a flat holding" — measured LIVE tonight, on the symbol no card named.** ⛔ **NOT CANCELLED (unauthorised); markets shut ⇒ the exposure is MONDAY.** ⭐ **`MANINFRA` is CORRECT and must NOT be swept with it** — `positions()` CNC `net=+4, buy=4, sell=0`, one matching `active` GTT `330856765`. ⚠️ *Stated not chased (G3): the `15:08:11` trigger has NO corresponding order in the broker's book.*

> 🏛️⚖️🔝 **07-Aug-2026 — RULINGS 1 + 2 TAKEN (Rama). ⛔ DOCS-ONLY — `git diff 348c226..HEAD -- '*.py' '*.yaml' '*.sql'` is EMPTY.** Report `docs/audit/rulings_1_2_verification_07aug2026.md` · text `docs/MASTER_PENDING_01-Aug-2026.md` §R.
> **WHERE THINGS NOW LIVE — the pointers this file exists for:**
> · **CONTROL INVENTORY → `ops_dashboard/docs/G2a_capacity_inventory.md`** *(THE AUTHORITY, 44 rows, joined on the dotted config key)*. ⛔ **`docs/audit/audit_05jul2026.md:527` is SUPERSEDED** — frozen history. ⛔ **`docs/design/sizing/config_surface_review_06aug2026.md` is MERGED IN** — evidence only, ⛔ never cite it as an inventory. ⛔ **DO NOT MOVE the G2a file** — `ops_dashboard/backend/services/capacity.py` consumes it and the location is a separate, untaken decision.
> · **THE SYMBOL GATES → `docs/design/sizing/delivery_config_surface_06aug2026.md` §7.1** *(the canonical table)* + `docs/web_claude/01_module_instructions/module_17_risk_engine.txt` *(whose check numbering was off by one — corrected in a note, ⛔ not in the list)*.
> **THE FOUR SITES A GREP WILL FIND, so they are written down once:** ① `SYMBOL_DIRECTION_DAILY_LIMIT` `signals/signal_processor.py:706-720` *(the ONLY one with a config key — `risk.one_trade_per_symbol_direction_per_day: true` — and the ONLY date-scoped one)* · ② `CONTRARY_POSITION` `capital/risk_engine.py:665-686` *(**0** fires in 109,254 signals)* · ③ `DUPLICATE_SYMBOL` `capital/risk_engine.py:688-694` · ④ ⚠️ **a FOURTH emitting the identical label** `allocation/portfolio_allocator.py:182-183`, inert (`allocator_mode: shadow`) — **in no inventory before today**. ⑤ *(different class, same key)* the 300 s per-symbol cooldown `signals/entry_throttle.py:97-107`.
> **🔒 SEQUENCING REVERSED — F6 IS A PREREQUISITE OF RULING 2, NOT A BENEFICIARY. 0 of 7 F6 costs retired.** ⛔ The loosening half is **one boolean**, and it is **NOT AUTHORISED, NOT FLIPPED, NOT STAGED.**
> ⚠️ **STALE-VALUE WARNING for anyone reading the old inventories:** `max_consecutive_losses` is **4**, not 5 · **`force_intraday_only` is `false`** and `delivery_enabled` is `true`, so the delivery caps tagged "INERT" in BOTH prior inventories are **LIVE** · `alerts.*` is **31** leaf keys, not 9 · `entry_gate.*` is **17**, not 9.

> 🏁🌙🔝 **05-Aug-2026 22:46:36 IST — FIRST CLEAN SHUTDOWN WITH A DELIVERY POSITION HELD. ⛔ NO CODE, NO PUSH, NO SCHEMA — an OPERATIONAL first, on deployed `0197923`.** Authorised by **Rama in his own words** (*"me also agreed!! [Go]"*); ⚠️ **the stated reason was AUTHORED BY ChatGPT and ADOPTED by Rama — ⛔ not written by him.** Procedure + full execution record: `docs/audit/STOP_PROCEDURE_05-Aug-2026.md` §(i). **(P) `_shutdown()` RAN TO COMPLETION WITH A REAL CNC POSITION ON THE BOOK — squared off nothing, cancelled nothing, released no capital, closed no position**; `ActiveState=inactive` · `SubState=dead` · `Result=success` · `ExecMainStatus=0`. ⭐⭐ **THE ARTIFACT THE WHOLE DECISION WAS TAKEN TO RECOVER — THE 17:35 CENSUS — EXISTS: 55 lines, `BEGIN day=2026-08-05 mode=live entries=70` … `END mismatches=0`** (it had been ABSENT all evening because a carried position defers `eod_self_exit` ⇒ no shutdown ⇒ no census). Evidence copied off-box to `~/Documents/trading-evidence/2026-08-05/` (both files; ⛔ deliberately outside the repo tree, per F2). **(d) PASS at 142 s** — still `inactive`, `token_watcher.log`'s last line still `[2026-08-05 08:15:05]` ⇒ **no start attempt**, exactly as the basis table predicted (window `[08:00,16:00)`; `Restart=on-failure` never auto-restarts an explicit stop). ✅ **THE FALSIFIABLE EXPECTATION SCORED — NOT REFUTED:** `cnc_gtt_placer acted 2` · `cnc_gtt_monitor acted 24` (⛔ neither `acted 0`, which would have been a finding on the day delivery traded). ⛔⛔ **AND NO FURTHER — `acted` COUNTS ROWS *EXAMINED*, NOT ACTIONS *TAKEN*** (`orders/cnc_gtt_monitor.py:145-153` appends a label on `healthy:`/`needs_review:`/`noop:` alike) ⇒ **the census is an ARTIFACT RECOVERED, NOT A QUESTION ANSWERED**; observed-vs-swept stays **(d)** until `get_gtts` is instrumented (**0** call sites logged). ⭐ **`fm.invariant_violation: acted 0`** — the boot-path `hard_kill` traced tonight for ChatGPT's Q3 has still **never fired**; ⚠️ **Thursday is its first run walking an OPEN CNC trade.** ✅ **Census arithmetic checked and it CLOSES: `entries=70` → 52 printed lines → `composition OK (62 registered, 62 expected)`** = 70 − 18 `infra` (emitter skips them) and 70 − 8 `expected-absent`; ⛔ not a defect, ⚠️ but the BEGIN line invites a miscount. 🔴 **THE OPERATIONAL LESSON, FILED AS PRACTICES §G1 INSTANCE #7: the stop's SUCCESS WAS THE ONLY THING NOT PRINTED** — the invocation carried a bracketed question inside the shell line, so five bogus unit names failed LOUDLY while `trading-system.service` stopped SILENTLY ⇒ ⭐ **a command's own output is not evidence of its effect; verify the STATE separately** (which is precisely why step (c) exists). ⚠️ **AND `Bash(ssh *)` INVERTED ITS RECORDED HAZARD:** read-only ssh ran unprompted all night, the ONE `sudo` was **DENIED twice** — `allowlist_preauthorises_destructive_02aug` now **REQUIRES RE-MEASUREMENT**; ⛔ boundary NOT probed. ➡️ **Thursday 06-Aug is now BRANCH A**: `docs/audit/THURSDAY_CONTINGENCY_06-Aug-2026.md` → **BRANCH A CONTINUED** (6 steps; ⭐ **step 3 = the T+1 carry, the measurement of the week**, expectation + baseline written BEFORE the fact; ⛔ **step 4 validates it — if ATULAUTO is still in `positions()`, step 3 measured NOTHING**). SYSTEM_MAP label moved to `<ROUND TRIP VERIFIED LIVE 05-Aug; CLEAN SHUTDOWN WITH POSITION HELD VERIFIED 05-Aug; T+1 CARRY UNVERIFIED>` — ⛔ **nothing wider.**

> 🔎📉🔝 **01-Aug-2026 — INTEGRITY AUDIT PHASE 2 (signal→secondary screening) — READ-ONLY, FINDINGS-ONLY; appended to `docs/audit/integrity_audit_2026.md` (local commit, ⛔ NOT pushed — rides the next deploy slot).** Measured 01-Aug 00:37–00:4x IST from the VM (deployed `297b587` == PC code; `mode=ro` + log greps; service `inactive`; ⛔ nothing fixed/tuned, 3-Aug/4-Aug untouched, forward-shadow not read). **⭐ G2 RE-MEASURED FRESH — STILL EXACTLY 25/100 points dead-at-0.0** (volume_surge 15 + atr_filter 10; `avg_volume_20d`/`atr` None on 17,714/17,714 window snapshots) **+ 20 more pinned at half (rsi/sector 0.5×16,705/16,705) ⇒ 45/100 of the score is constant, the max ACHIEVABLE score is 65 (measured: 49,209 rows since 12-Jun, 0 ever above), and the pass band is [60,65] — decomposing into TIME-BAND × PRICE-LEVEL** (reject walls 57 ×8,958 / 59 ×3,321 = the saturated profile outside 10:00-10:15; `spread_check` +5 on only 1.0% of rows). **🔴 THE PHASE'S TOP NEW FINDING (IA-P2-01, MED-HIGH): `EntryGate` HAS NEVER RECEIVED AN ENTRY** — constructed+started every boot (26 `EntryGate started` lines) but the P11a divert was never wired (no production `.add()` caller; `GATE_%` statuses 0 all-time; `gate_state` 0 rows ever) ⇒ **6 of 10 live strategies declared `pullback_wait_enabled: true` place IMMEDIATELY from the stale trigger AND skip the FIX-067 re-anchor on the false premise "the gate waits"**; `continue_from_gate` (~200 lines incl. its own sizing/reserve section) is production-unreachable. **IA-P2-02 (MED): `max_spread_pct: 0.005` is a UNITS mismatch** — schema convention is fraction (0.5%) but the step compares percent ⇒ demands 0.5bp, satisfiable only at mid≳₹1000 (or locked books): the 5-pt step is a price-level selector (the same-named `entry_gate.max_spread_pct: 0.5` uses percent — 100× apart). **IA-P2-03 (MED): the tier ladder is degenerate — HIGH unreachable, MEDIUM=65-only ⇒ `trades.tier_weight_applied = 0.5 on 415/415 ever`; "full size" does not exist.** IA-P2-04 dead knobs (`min_volume_surge` tuned 1.2-2.0 per strategy, zero effect) · IA-P2-05 the V3 shadow soak inherits the dead inputs (NEW-score mode 47/49, ceiling ~50-56; Context/Execution constant on 4-5 of 9 components ×474/474; shadow-gate leg `gate=PASS` 3,536/3,536 = zero divergence power) · IA-P2-06 resume paths lack the v3 observe hook (latent) · IA-P2-07 step-TIMEOUT→0.5 even for circuit/age (0 occurrences ever) · IA-P2-08 hygiene. **Floor history measured (D3 caveat): min_pass 55→60 (18-Jun `6450ee9`) →55 (06-Jul `8a3e0b7`) →60 (10-Jul 10:04 `61ae9cc`, mid-morning) — the pass population changed ~60× on 10-Jul; last sub-60 pass = 10-Jul.** R:R confirmed compute-then-gate everywhere (live TGT = 1.5×SL-distance by construction; v3 `gate_rr` never adjusts levels; shadow verdicts: WOULD_REJECT_RR 385/474 = 81%). PB-01 values recorded fresh (G4's decision still owed; 17 would-be records == 17 CONSUMED rows, reconciles exactly). Regime CANNOT gate (disabled, `data_store/regime/` empty, `extreme_flag` feedless even if enabled). Sector: `_sector_for` probes methods that exist nowhere ⇒ "UNKNOWN" ×474/474 (G3 widened). P2→P3 seam summary written (§P2.6): the seam carries only (entry, SL, intent, TIER, lot_size, perf_weight) — score/levels never cross; the tier is a constant; the basis diverges (momentum re-anchored, pullback raw-trigger); observers (v3/allocator) are log-only. NEXT: P3 sizing/risk.

> 🔎🧾🔝 **31-Jul/01-Aug-2026 — INTEGRITY AUDIT PHASE 1 (entry path: webhook→signal) — READ-ONLY, FINDINGS-ONLY; NEW campaign register `docs/audit/integrity_audit_2026.md` (Phase 1 = its first section; local commit, ⛔ NOT pushed — rides the next deploy slot).** Measured 31-Jul 23:48–23:5x IST from the VM (deployed `297b587` == PC code; `mode=ro` queries + log greps only; service `inactive`, the designed nightly state; ⛔ nothing fixed, no live webhook sent, the 3-Aug/4-Aug sequence untouched). **⭐ THE "~249/day SILENT KeyError DROPS" FIGURE RE-MEASURED DEAD ON THE CURRENT SYSTEM:** `KeyError`=0 across all 6 trading days 24→31-Jul, `Unhandled exception in pipeline`=0, `signal store FAILED`=0, `REJECTED_UNKNOWN_STRATEGY`=**0 all-time**, webhook 404s=**0 all-time** — the 19-Jul census inversion (never silent; closed `c22a25c` 14-Jul) CONFIRMED by fresh measurement. **🔴 THE PHASE'S ONE MED FINDING (IA-P1-01): `range_breakout_long`+`range_breakout_short` — enabled, mapped, loaded at every boot — have ZERO POSTs EVER (webhook_audit 12-Jun→31-Jul, 127,272 rows, every response code): dead at the CHARTINK SOURCE for 7 weeks and nothing in-system can notice (the 5-Jul Ph-5 per-scanner-silence HIGH, now measured biting; S10 re-verified still unwired). OQ: only Rama's Chartink dashboard can settle whether they exist/are armed.** Status updates measured: M-S2 remediation DEPLOYED-not-VERIFIED-LIVE (503s all-time=18, none since 1-Jul ⇒ the requeue path has never run) · **H-9 CLOSED BY REMOVAL** (Wave-3 deleted the late-tick machinery) · M-S1/FIX-067 + M-S5 closed in code · **the positional trio no longer trades on delivery params — Option A (10-Jul) dormants all 3 via the LAYER-0 breaker, measured: 100% of the ~360–2,562/day `REJECTED_STRATEGY_CONTROL` rows are the 3 positional scanners** (⇒ tradeable population = 12, ever-fired = 13/15) · P3-s14 tail still present (line drift :875→`webhook_receiver.py:951`; 0 `evicted STALLED` ever ⇒ never fired) · non-terminal strays all-time = 2 RESERVED rows (18-Jun) · sr_detector token-lookup NOT climbing (1/7/**3**). NEW LOWs registered (IA-P1-02..09): `signals.strategy`=scanner-name identity coupling · EOD/pb01 audit "accepted" conflation (accepted−rows = +27/+12/+16/+16/+19 = exactly the pb01 `symbols=` series) · WR13 pre-`try` audit hole (413s unaudited) · FIX-074 cast branch unreachable on Chartink-shaped payloads · **tick→candle dormancy UN-ENFORCED (one runtime waker: `order_placer.py:3393` subscribe on the exit-retry path; `_persist_candle` pre-registered `main.py:3415` ⇒ one exit LTP-validation failure un-dormants the feed + arms the stickiness hazard)** · QUEUE_FULL-retry tuple/row divergence · alias-map CWD path (H-11 class) · positional foregone-reject churn (~30–40% of daily signal rows). P1→P2 seam summary written (§P1.6): screener never reads the signals row; `signals.strategy` is the scanner name; 3 writers share one status column; trigger price reaches screening unsanitized; P2's effective universe is 10 strategies' signals, not 15.

> 🚑⚠️🔝 **28-Jul-2026 — WHERE KILL-SWITCH RECOVERY LIVES, AND THE TWO DOCS THAT GET IT WRONG (registered only; ⛔ nothing changed).** An **EMERGENCY** kill (anything whose reason is not one of the **two** literals in `SCHEDULED_KILL_REASONS`) **persists within the day**, so a **SAME-DAY restart** hits `StartupScenario.HALT` and **exits 4** — and in that window there is **no exit management and no 15:15/EOD squareoff**. ⭐ **RECOVERY = `deploy/resume.sh`** (stop unit → `systemctl reset-failed` → `scripts/clear_kill_switch.py`). ⛔ **It is named in NO incident document**, and both docs that touch this are **WRONG**: `docs/RUNBOOK.md:101` describes a *"Restart loop (exit code 4)"* — **the loop cannot occur** (`RestartPreventExitStatus=3 4` in `deploy/systemd/trading-system.service:35`, verified on the VM too; the unit exits ONCE and stays **`failed`** — journal `Failed with result 'exit-code'`, measured 21-Jul-2026 11:37:53; no `SuccessExitStatus` is declared, so exit 4 is a failure exit. ⭐ `inactive (dead)` is the HEALTHY nightly state after a clean exit 0; **`failed` is the HALT**) — from a cause (*"Stale SOFT_KILL"*) that was **fixed** 20-Jun by `clear_stale_state()`; and `docs/05_incident_response.md:40` says *"Manual resume: restart the service"*, **which is exactly what fails**. ⭐ **Detection is fine** — `liveness_probe` (`*/5 9-15 * * 1-5`) raises ONE CRITICAL within ~5 min during [09:00,16:00) on a trading day; it is *recovery guidance* that is missing, not monitoring. ⇒ a 2-line doc fix with ZERO deploy consequence, **not done today** (register-only session) — registered as **K1** in `docs/decisions/ACTIONS_not_decisions.md`.

> 🧾🔑🔝 **28-Jul-2026 — THE DEPLOY RECORD EXISTS AND NOBODY KNEW IT DID: `~/trading-system.git/logs/HEAD` (written when the day was docs-only).** ⚠️ **SUPERSEDED THE SAME EVENING — the "⛔ NOTHING deployed 28-Jul" clause that stood here is FALSE as of 18:46:19:** Rama overrode the calendar at 15:45 (*"Any pending commit, push, pull — do it now. PC == VM 100%"*) and **`52ead46..ce08668` (24 commits) WAS deployed**, gated on a clean 17:35 self-exit + zero orphaned reservations. ⭐ **This record then verified its own value the same night:** the reflog showed **both** halves at `2026-07-28 18:46:19 +0530` — `push` **and** `checkout` — which is exactly the half a `push`-only line cannot prove. Wed 29-Jul remains the T2 arm and a CHANGE FREEZE. ⭐ **801 entries, unbroken back to 3-May-2026, 125,774 B.** It records **BOTH halves of every deploy, timestamped to the second** — the `push` (receive-pack) *and* the post-receive `checkout -f` that actually **writes the deployed tree**: `52ead46 HEAD@{2026-07-28 09:15:25 +0530}: checkout: moving from main to main`. ⇒ ***"which commit was deployed when" is answerable TODAY***, with nothing to build: `ssh trading-vm "git -C ~/trading-system.git reflog show HEAD --date=iso"`. ⛔ **THIS CORRECTS MY OWN CLAIM MADE EARLIER THE SAME DAY** (*"the bare repo has NO reflog at all, no `logs/` dir, nothing records deployment"*) — that check ran `git reflog show main`, which **is** genuinely empty (bare repo ⇒ no *per-ref* log was ever created), and wrongly generalised from one ref to all. **The record is on HEAD.** ⭐ **MECHANISM, so nobody "fixes" it away:** git **appends to a reflog file that already exists** regardless of `core.logAllRefUpdates` — that setting only governs whether one is *created*; `logs/HEAD` was created 3-May by the initial `checkout: moving from master to main` and has been quietly recording ever since. ✅ **DONE — NOT OWED AGAIN.** (Was: `gc.reflogExpire` UNSET ⇒ git's 90-day default with the 3-May entries ~86 days old.) **`gc.reflogExpire` + `gc.reflogExpireUnreachable` were both set to `never` on 28-Jul ~15:3x — BEFORE that evening's push**, which was the whole point of the deadline. ✅ **Confirmed holding after the 18:46 deploy: the prior `52ead46 @ 2026-07-28 09:15:25` pair is still present alongside the new one**, so nothing was pruned. ⭐ It also supplies the *known-commit* half of the still-unbuilt deployed-tree-vs-HEAD check ⇒ that item got cheaper. ⭐ **Independent corroboration of §A:** `e6ec75b` shows push+checkout at **27-Jul 22:33:58**, 39s after its commit stamp — a second, VM-side witness that never entered the PC-side reasoning. Calendar §7.3 · actions `docs/decisions/ACTIONS_not_decisions.md`.

> 🧹📄 **28-Jul-2026 — STRAY-`.pyc` DETECTOR: NO NEW PATH (`<BUILT>`, UNPUSHED).** Recorded here because the anti-duplication answer was "extend, don't add": the check lives in the **existing** `scripts/system_manager.py` as an 11th `_check(...)` (`stray_pyc_check`), and its tests were appended to the **existing** `tests/unit/test_system_manager.py`. **No new file, no new directory, no cron entry, no report path** — the 18:45 `system_manager_eod` job simply emits one more section. ⛔ Also: `.gitignore:46` is `*.py[cod]`, so **`git status` is blind to a stray `.pyc` on the PC as well as the VM** (verified with `git check-ignore`) — correcting the opposite claim in `docs/audit/pyc_orphan_sweep_28jul2026.md`, now fixed there. ⛔⛔ **`git clean -xdf` MUST NEVER be run in the deployed tree** (`data_store/` live DB + backups, `logs/`, `.env` are all untracked/ignored there) — the prohibition is in the code comment, not only in the report.

> ✅🔴🔝 **19-Jul-2026 — MONDAY POST-SESSION CHECKLIST WRITTEN (docs-only; RUN Mon 20-Jul after 18:15, READ-ONLY; nothing run tonight — no Monday data).** `docs/audit/MONDAY_POST_SESSION_CHECKLIST.md` — the **ROW-LEVEL** replacement for hash-equality (the live DB will differ Monday because it traded). **Every item states its `mode=ro` query + what GOOD *and* what BAD look like** (S4's lesson: a broken day can look clean). Covers **A** boot/liveness (the clean-exit-0 signature; the `liveness_probe` DOWN alert), trading volume (bands surprising in EITHER direction: 0 trades bad, ~25 too), capital integrity (`RESET_PNL == −(Σpnl_delta−Σcosts)` = expected Option-B, E4/W10 not shipped), the ~Rs296 daily-loss number, kill/squareoff, cron completion, the pre-10:00 403 population · **B** the forward shadow — **exactly ONE new date `2026-07-20` (>1 = contamination = the one urgent STOP)** + the null-`sim_R` degradation that looks like success (token-absent → `sim_R=None`, cron exits 0) · **C** capture-first for the 5 still-unproven mid-day-restart events · **D** bad-Monday triage (capture→diagnose→report; **nothing fixed in a hurry**). memory `artifact-baseline-reconciliation-19jul`.

> 🗜️🧠🔝 **19-Jul-2026 — MEMORY.md COMPACTED (PC memory palace; NO repo/production change; docs-only report).** The active index hit **24,602 B / 24.03 KB against the 24.4 KB hard read limit** (~383 B margin, a *silent* failure) ⇒ the after-Monday deferral was lifted (snapshot-first precondition met; a quiet Sunday beats a live Monday). **Compacted by RELOCATION** (detail already in the linked topic files) → **18,915 B / 18.47 KB, margin 5.93 KB**; verified vs snapshot `_205049`: **0 wiki-links dropped, 0 `(file.md)` dropped, 0 real orphans, every board item + standing rule present by line #.** **⭐ CONVENTION (stops regrowth): ONE pointer line per batch in the index — detail goes in the topic file/report, never a paragraph here** (~0.9 KB/batch ⇒ the compaction alone buys ~6-7 batches; the convention ~15-18). Report `docs/audit/memory_compaction_19jul2026.md`.

> 🔑🧾🔝 **19-Jul-2026 — ARTIFACT BASELINE RECONCILED (READ-ONLY, docs-only, NO rec; PC == origin == VM bare; no cron changed).** Report `docs/audit/artifact_baseline_reconciliation_19jul2026.md`. The live-DB fingerprint moved **`6df0c09a…`(11:14) → `a7a1de53…`(18:00)** between batches — **BENIGN:** sole writer `gemini_weekly_patterns` (`cron 0 18 * * 0`, monitored:true) = **ONE `cron_heartbeat` row** (id 2421 @ 18:00:17); table diff vs the 11:12 census snapshot = **only `cron_heartbeat` +5**, sensitive tables IDENTICAL (signals 32928 · trades 361 · orders 610 · fm_ledger 2160 · ks 1); no token; service inactive; JSONL 7,827@16-Jul. **⭐ NEW BASELINE (Mon ref, VM path): live DB `a7a1de53…`@18:00 · JSONL `f7c964fd…` · analytics `bb229f44…`.** **⭐⭐ RULE: within-batch before==after ≠ cross-batch unchanged — a moved baseline must be flagged + reconciled.** The live DB advances on monitored-cron heartbeats + the Sun `db_retention --vacuum` even while DOWN ⇒ Monday's post-session check = **row-level, not hash**. "Attribution gloss" class named; sweep queued. [[artifact-baseline-reconciliation-19jul]]

> 🧹📉🔝 **19-Jul-2026 — MASKING-PREMISE SWEEP + REACHABILITY RE-CHECK (READ-ONLY + docs, NO rec; PC == origin == VM bare; 3 artifacts byte-identical before==after — live DB `a7a1de53…`, JSONL `f7c964fd…` 7,827@16-Jul, analytics `bb229f44…`).** Report `docs/audit/masking_premise_sweep_19jul2026.md`. The refuted **"a size multiplier is masked by the concentration cap"** (perf_weight is a **post-cap multiplier**, changes final qty **233/298**) was a HALF-corrected record — **SWEPT from 5 docs** (`00_INDEX` coupling · `06` body ×3 · triage coupling · `decision_packages` · `e4_w10`) with dated, struck-through-but-legible notes. **§B: the refutation moves NO reachability verdict** — the 4 sizing-guard UNREACHABLE verdicts (Q9 matrix rows 10/13/17/18) rest on their own algebra/config/pinning, not masking; **batch-4 reasoned the post-cap mechanism CORRECTLY — `06` misattributed the claim to it.** **#06 gate corrected: merit(D2/D3)+leverage, NOT the cap** (unlevered-regime caveat travels). **⭐ MEMORY-PALACE SNAPSHOT taken** (`memory_snapshots/memory_snapshot_2026-07-19_201338/`, 505 files/2.07 MB) ⇒ compaction precondition MET; **NOT compacted — after Monday.** [[masking-premise-sweep-19jul]]

> 📉🧮🔝 **19-Jul-2026 — CANDLE RETENTION (D4) = SURVIVES + PERFORMANCEALLOCATOR (#06) COMPUTABLE-NOW label HELD (READ-ONLY, docs-only, NO rec; PC == origin == VM bare; 3 artifacts byte-identical before==after MY session).** Report `docs/audit/candle_retention_and_perfallocator_feasibility_19jul2026.md`. **A:** `candles` pruned **90d** (`db_retention.py:63-70`, runs **every** calendar day, governs `analytics.candles` via `state_store.py:247`; anti-vacuity PASSED — Sunday VACUUM completed ⇒ DELETE ran, 0 deleted, earliest candle 19-Jun); OOS seed 13-16 Jul expires **~12-15 Oct** vs sample matures ~mid-Aug…~mid-Sep, rolling 90d≈62 trading days>34 ⇒ **D4 is POWER-bound, not data-bound.** **B:** breakdown persisted **v34** (298/361); the "masked by concentration" premise is **REFUTED** — perf_weight is a **post-cap multiplier** (2× ceiling `position_sizer.py:446/503/506`), changes final qty on **233/298 (78%)** over [0.5,2.0] / **151/298 (51%)** over [0.8,1.25]; `binding_constraint='concentration'` 298/298 confirms D1; conditional on **unlevered** sizing. **⚠️ STANDING: the live DB hash advances on `cron_heartbeat` writes even while the system is DOWN** (`gemini_weekly_patterns` id 2421 moved `6df0c09a…`→`a7a1de53…`@18:00, not me) ⇒ prove-untouched = before==after within a session. [[candle-retention-perfallocator-feasibility-19jul]]

> 📉🔬🔝 **19-Jul-2026 — FORWARD-SHADOW CAPACITY + D4 FEASIBILITY (READ-ONLY, docs-only, NO rec; PC == origin == VM bare; both artifacts untouched — live DB `6df0c09a…`, JSONL 7,827 lines @ 16-Jul, analytics.db `bb229f44…`; recorder NOT run).** Report `docs/audit/forward_shadow_capacity_and_d4_feasibility_19jul2026.md`. **The forward shadow WILL keep appending** (cron 18:15 Mon-Fri, monitored, fail-loud, idempotent per date+signal; **Mon = OOS day 4**; ⚠️ never run it manually — `--date` backfill contaminates). **D4 = PARTIALLY testable (OOS candles survived in analytics.db, unlike D3) but UNDERPOWERED** (19 OOS entered / 9 winners; "2.93R MFE" → OOS median 1.44R, cannot distinguish); facts verified (no engine 0/361, dead config LIMIT_TRIPLE). **Standing: 13-Jul per-signal basis NOT persisted** (band + exits). Triage D4 COMPUTABLE NOW → NEEDS RUNNING (~17-34 days); **2 of 3 "computable now" proved otherwise** ⇒ PerfAllocator UNVERIFIED; cross-regime needs a regime change not days. [[forward-shadow-capacity-d4-feasibility-19jul]]

> 📉🔬🔝 **19-Jul-2026 — D3 BAND INVERSION FAILED its first out-of-sample test (READ-ONLY, docs-only, NO rec; PC == origin == VM bare; live DB untouched `6df0c09a…`).** Report `docs/audit/d3_band_inversion_robustness_19jul2026.md`. Live gate = **`scoring_weights.yaml:22 min_pass=60`** (NOT `system_config:423` = V3 shadow); tiers med65/high80; all strategies `min_score:0`. **Forward shadow = the confirmation path** (`data_store/v3/forward_shadow_fs-v1.jsonl`, a JSONL, **3 genuine OOS days 07-14/15/16**, grows only when live). OUT-OF-SAMPLE: the **bottom filter (0-34 junk) replicates**, but the **60-65 inversion does NOT** (60-65 best-of-band, 50-54-vs-60-65 z=−2.72 reversed, pooled=noise p=0.27, ~powered); 07-13 day-dominated; natural dist 73% 55-59. ⚠️ one regime each way ⇒ NOT refuted, **unresolved**; triage **COMPUTABLE NOW → NEEDS RUNNING**; gates D1/#10/#08. **📌 CAPITAL VOCABULARY** (perm doc rule): ACTUAL Rs 9,875.60 / BUYING-POWER ×5 MIS / RISK / EXPOSURE — system unaware of leverage; label which. [[d3-band-inversion-robustness-19jul]] [[capital-vocabulary]]

> 💰🔢🔝 **19-Jul-2026 — E4/W10 OUTCOME-IMPACT (DOCS-ONLY, NO rec; PC == origin == VM bare; live DB untouched `6df0c09a…`).** Report `docs/audit/e4_w10_outcome_impact_19jul2026.md`. **The cost double-count would have changed N=0 of 23 daily-loss outcomes.** ⭐ CORRECTED the production threshold (decision file 01 **and** the `dual-daily-loss` memory carried FIXTURE values): ONE limit `daily_loss_limit_pct=0.03` = **3% × broker-capital ~Rs10k ≈ Rs300/day** (NOT Rs25k), single-source, **no absolute** (`capital.daily_loss_limit` deleted 24-Jun) ⇒ batch-2 single-source CONFIRMED. Worst day 07-07 cum_B −56.47 = **Rs 243 margin**; structural but not robust to ~5× D1 sizing. §C triage: D3/D4/PerfAllocator computable-now · D1/Regime need running · E4/W10-posture/Freeze/Prune need Rama · D2/Regime/Throttle blocked. **LESSON: read a threshold from code before computing against it.** [[e4-w10-outcome-impact-19jul]]

> 🗳️📋🔝 **19-Jul-2026 — DECISION PACKAGES + MEMORY-COMPACTION VERIFICATION (DOCS-ONLY, NO recommendations; PC == origin == VM bare; live DB untouched `6df0c09a…` before/after).** Report `docs/audit/decision_packages_19jul2026.md`; **the board = `docs/decisions/00_INDEX.md`** (10 files — E4/W10 · D1 · D2 · D3 · D4 · PerfAllocator · Regime · Freeze-min_pass · Prune-retention · Throttle-admission; each = choice/known/unknown/exposure/what-settles-it, **NO rec**). **§A: the MEMORY.md compaction was LOSSLESS** (0 category-iii; every open board item present; "alert-watcher soak" + "live-test cert" both survive in topic files). **Throttle = decision #10 with its counter-case** (scorer can't rank — M-S4 rho +0.003, the 60–65 band it sees is the worst ⇒ FCFS discards no edge; but ~23 approved/day discarded, 87% of orders hour-1, gate category free-text-only ⇒ invisible). **Free-text classification is now a CLASS OF FOUR** (sizing conc-vs-capital `daily_report.py:464` FIXED · `REJECTED_KILL_SWITCH` stage · `REJECTED_SIZING_VALID` · throttle gate; W9 = reason never persisted). [[decision-packages-19jul]]

> 📊🔎⚖️🔝 **19-Jul-2026 — THROTTLE SELECTION + RECORD CORRECTION (READ-ONLY; docs-only; PC == origin == VM bare; delta vs tag `deploy-19jul-consecutive-losses` = markdown ONLY; live DB PROVABLY untouched — sha256 `6df0c09a…` + mtime `11:14:29` before/after).** Report `docs/audit/throttle_selection_and_record_correction_19jul2026.md`; memory `throttle_selection_record_correction_19jul`. **⭐ The traded book is TIME-selected, not QUALITY-selected** — 64% of approved signals (139/217) are throttled by a pure in-memory temporal rate limiter (`signals/entry_throttle.py` @ `signal_processor.py:1170`, **after** `risk_engine.approve()` `:1075`); **132/139 = the global 20s `min_gap`**, blind to strategy/score/direction; score gap **0.21pt** (non-monotone), the strategy spread 21–72% is an **arrival-density confound**, throttling is **100% market-open** (ordered% **12.8% @10:00 → ~100% @10:15+**). **FILL 78→48: three EXPECTED mechanisms, 0 defects** (17 unfilled-LIMIT ~60s timeouts `entry_cancelled_zero_fill` + 6 broker MIS-blocks + 7 `slippage_exceeded` guard). **⇒ every expectancy figure describes "approved signals that cleared a 20s opening rate limiter AND filled a resting LIMIT within ~60s" — bounds the domain, does NOT invalidate; D1/D2/D3 are Rama's, expectancy NOT re-run.** **⭐ CENSUS SELF-CORRECTED: the data is NOT eroding** (90d retention + earliest 12-Jun ⇒ next daily prune **0 rows** until **~10-Sep**; the 09-Jul boundary was the **one-off 16-Jul manual prune**, not a sliding window). **Snapshot** `/home/ubuntu/preserved/signal_census_19jul2026/` (89.97MB DB + 2 gzipped CSVs, `chmod 444`, 6 complete days safe). **Record swept (dated notes):** Rs990 "LONGs 4.5×" **DIRECTION INVERTED** (rate LONG 10.00% vs SHORT 10.72%; count skew = 10.2× volume; **in D1's evidence, flagged**) · "~249/day KeyError" **already CLOSED everywhere** (`c22a25c`, never silent) · 07-08 quote-cliff interpretation inverted (**RE10 verdict STANDS**). **📌 `scripts/*.py --db <copy>` isolates only code BELOW the cron entry point** — 4 truthful `cron_heartbeat` rows written to the LIVE DB (ids 2417–2420, only table changed of 46, NOT reverted). [[throttle-selection-record-correction-19jul]]

> 📊🔎✅🔝 **19-Jul-2026 — SIGNAL MORTALITY CENSUS (READ-ONLY; docs-only `9eb7d71`; PC == origin == VM bare; delta vs tag `deploy-19jul-consecutive-losses` = markdown ONLY).** Report `docs/audit/signal_mortality_census_19jul2026.md`; memory `signal_mortality_census_19jul`. **Nothing changed** — no code/config/schema/flag/test, no regression (nothing executable moved), **live DB provably untouched** (sha256 + mtime identical before/after; all queries on a `mode=ro` snapshot, since deleted). **⭐ 5,845 of 30,769 admitted signals REACH THE RISK ENGINE = 19.00%** (~975 `approve()`/day; cross-verified to the unit against `risk_engine.approve` log lines) ⇒ **Q9's gates are consulted, not dead.** **⚠️ do NOT quote "3.54% of arrivals"** — 81% of arrivals are intentional dedup (~1 POST/min/scanner vs a **300s** TTL: 80% expected, 81.5% observed). **⭐ SILENT DROPS = 0** (0 unmapped / 0 NULL / 0 stuck; **97.60% deliberate DECISIONS**); the **134,342** webhook drops have no row and **no log line** (grep-proven) **but their COUNT is already in the daily xlsx tagged `[W9] PENDING_CAPTURE`** ⇒ tracked gap, not a blind spot. **🔴 MEASUREMENT BOUNDARY — `scripts/eod_cleanup.py:234` prunes STATUS-SELECTIVELY (`REJECTED*`/`EXPIRED`/`DUPLICATE`; keeps `SKIPPED_*`) ⇒ ONLY 09-Jul→16-Jul is COMPLETE; any share over all 32,928 rows is MEANINGLESS; pre-09-Jul rejection history is GONE (⚠️ CORRECTED 19-Jul: NOT sliding — 90d retention + earliest data 12-Jun ⇒ next daily prune deletes 0 until ~10-Sep; the 09-Jul boundary was the one-off 16-Jul manual prune).** **ALL 3 INHERITED CLAIMS WRONG IN PART:** 07-08 "63 after 10:22" count VERIFIED / meaning **INVERTED** (acceptance *accelerated* — 464→676→1,179→1,431→**1,462**/hr; 1.21% = **6th-LOWEST of 21 days**; the cliff is survivorship bias from the prune) · **"~249/day KeyError" INVERTED — never silent (ERROR + full traceback), the SAME killer as claim 1 (adding them double-counts), and fixed 14-Jul in `c22a25c`** · Rs990 threshold **exact** (0.00% below / ~42% above) + share **24.02%** VERIFIED, but **direction INVERTED** (LONG 10.00% vs SHORT 10.72%). **⭐ B1: batch 4's reachability table IS price-biased** (>Rs990 24.02% admitted → **34.73%** at the sizer → **0.14%** at the risk engine; mean px 732→875→**441**) — **bounds its domain, does not invalidate it.** **🔵 Reported not fixed:** 25,960 **403-POSTs ≈338k signals never counted** (cheaply estimable — size already stored) · `REJECTED_KILL_SWITCH` can't identify its stage · `is_sizing_rejection()` would claim the risk-engine's `REJECTED_SIZING_VALID` (both LATENT, 0 rows) · **live quote path has NO partial-response detection while paper NAMES the missing symbols** (`main.py:546-549`). [[signal-mortality-census-19jul]]

> 🛡️📌 **18-Jul-2026 — CT-GUARD ANTI-DECAY INVARIANT (item 1, DONE) + SECURITY-WATCHER (item 2, ⛔ REFUSED — NOT A BUG).** Report `docs/audit/ct_guard_invariant_and_secwatcher_18jul2026.md`; memory `ct_guard_invariant_18jul`. **ITEM 1 — the single-route claim was FALSE:** 5 modules bypassed the guard with raw `sqlite3.connect()` on a hardcoded live path (`ct140_eod_failure`, `ct143_verify`, `ct145_rapid_crash`, `vm_investigate`, `vm_investigate2`) — **SELECT-only (0 writes) but a plain `sqlite3.connect` is READ-WRITE ⇒ unguarded writable handles on production.** All 5 → `get_db_connection(readonly=True)`; **0 raw connects remain outside `ct_utils.py`.** NEW `tests/crash_test/test_ct_guard_invariant.py` — an **AST** scan (comments/strings can't fool it) enforcing **(A)** no raw `sqlite3.connect` outside `ct_utils`, **(B)** no live-DB path literal outside `ct_utils` (*you cannot open what you may not name*), **(C)** `StateStore(db_path=…)` never gets `LIVE_DB_PATH`. **PROVEN TO BITE: a planted bypass ⇒ 2 failed/3 passed; removed ⇒ 5 passed** (+ a coverage assertion so it can't pass vacuously). It caught a real leftover on day one (a dead live-path literal in `ct145`). Allow-list = 3 files, justified in-line. **PERMANENT RULE recorded** in `docs/SYSTEM_MAP.md` (⚠️ ENGINEERING RULE section) + the invariant docstring: *a destructive/crash-test harness must NEVER hold a writable handle on a live DB; all writable access via ONE fail-closed guard (no override, no env escape hatch); live reset is an OPERATOR tool in `scripts/` with backup+confirmation.* **ITEM 2 — REFUSED WITH EVIDENCE: `security-watcher` is working EXACTLY as designed, NOT an alert-watcher-style respawn loop.** **10 restarts in 10 min / 59 in an hour = the configured `RestartSec=60` heartbeat** (passes 60s apart to the second: 13:52:23→13:53:23→13:54:24); the unit file **documents** it ("Rising NRestarts is NORMAL — a heartbeat, not a crash-loop"), and the journal's first entry (19-Jun) shows systemd **refused `Type=oneshot`+`Restart=always`**, so the shape is an informed workaround. The scan **runs and alerts** (`pass complete (1 finding(s), 0 new alert(s))`, `ExecMainStatus=0`, state file **16.7s** old), and **nothing reads its systemd `ActiveState`** — all 3 monitors (`cron_officer.security_watcher_health`, `system_manager:825-829`, `preflight/checks/security.py`) use **state-file freshness**. ⇒ the `activating/auto-restart` sighting is just the transient between passes. **NOT changed** — altering a working live security service on the weekend before **Mon 20-Jul 08:15** (first real boot after the S4 fix) adds risk for zero functional gain. **⚠️ OPTIONAL (Rama's call, NOT applied):** convert to a **systemd timer** (`OnUnitActiveSec=60` + `Type=oneshot`/`Restart=no`) — canonical for scan-and-exit, consistent with the existing `cron-watchdog.timer`; removes **5,720 journal lines/day** and the `NRestarts=20959` "is it crash-looping?" ambiguity. Verify afterwards that `security_state.json` keeps refreshing (~60s).

> 📉🔬🔝 **18-Jul-2026 — Q10 REGIME THESIS VALIDATION: BLOCKED on the token; VERDICT ANYWAY = "NOT DETERMINABLE at n=23".** Report `docs/audit/regime_thesis_validation_18jul2026.md`; memory `regime_thesis_validation_18jul`. **NOTHING RAN** (no backfill/code/flag/schema; `mode=ro`). **🔴 RAMA ACTION: refresh the Kite token** — VM `data_store/session/zerodha_token.json` **absent** (session mtime `Jul 18 05:00` = the 05:00 delete cron; **08:15 TOTP refresh doesn't fire on a non-trading day**); PC copy **21-Jun 12:47** = dead. **`auto_refresh_token.py` deliberately NOT run** (credentials/TOTP = Rama's). **⭐⭐ Real finding: the ceiling is the TRADE side, NOT the index side — the backfill CANNOT rescue it.** Days per control cell: **LONG/INTRADAY 21 · LONG/POSITIONAL 15 · SHORT/INTRADAY 11** ⇒ ~7/~5/~4 per bucket = single-digit before analysis. **⚠️ Beta trap:** 91% long book ⇒ "Bull mornings did better" IS the null (beta, not edge); only the within-cell test separates them — the empty one. **Power (observed σ=13.54): LARGE effect → 47 trading days ≈ 2.2 months · MODERATE ≈ 9 months · SMALL ≈ 36 months.** **Pipeline PROVEN on real data:** 16-Jul, 45 bars in 09:15–09:59, 24142.10→24128.10 ⇒ **−0.058%**. Trades side computed (reproduces BK-1: 108/−25.57R intraday-long · 33/+4.50R positional · 14/+3.87R short · 155/−74.8). **Coverage: NIFTY = 1 day (16-Jul) ⇒ 22 of 23 missing**; backfill = ONE proven command `fetch_daily_candles.py --backfill --from 2026-06-15 --to 2026-07-16` (backup first; verify 23 days / 0 stock rows changed / integrity). **Tercile bands genuinely pending** (n=1 ⇒ not invented). **Run the backfill anyway (calibration asset + starts the clock), but do NOT build Phase 1 expecting a blessing.** **⚠️🔴 STANDING: do NOT flip `regime.enabled` mid-soak** (V3 consumes it, 8/40 Context ⇒ breaks pre/post comparability). **📌 Future Phase 1 design (recorded, NOT built): a configurable PLUG-IN inside `regime/` — configurable window (09:15–09:59), tunable thresholds, pluggable algorithms, VERSIONED regime logic.** [[regime-thesis-validation-18jul]]

> 📊🔎🔝 **18-Jul-2026 — REGIME PHASE 1 SCOPED (READ-ONLY; nothing built/enabled/pushed).** Report `docs/audit/regime_phase1_investigation_18jul2026.md`; memory `regime_phase1_investigation_18jul`. **3 PREMISES INVERTED.** **(1)** the existing `direction` axis is a **multi-month DAILY trend** (EMA50/200+ADX+swing, ≥201 daily candles — `regime/engine.py:142-188`, `:92`/`:109-115`), **NOT** the 09:15–09:59 move; **no window config exists** (intraday feeds only `day_type`, which waits out the open, `:237`) ⇒ **Phase 1 = a NEW ordinal opening-move axis INSIDE `regime/`** + window/band config (`config_loader.py:884` + `system_config.yaml:361`, both `extra="forbid"`) + append-only daily log. **(2)** the engine **never read Phase 0's `candles` table** — it reads the **LIVE Kite API** (`OhlcFetcher.fetch_by_token` `sr_detector/fetch.py:85-101` → `_sr_fetch_fn`, `main.py:2828`, wired `:2859-2864`) ⇒ Phase 0's candles are the **calibration/backtest** asset (what Q10 needs), not the feed; the engine was never blocked on Phase 0. **(3) 🔴 the V3 chain IS WIRED to its output** — `gate_extreme` (`v3_chain/runner.py:211`) + **`regime_fraction` = 8/40 Context** (`:225`, impl `:300-330`), plus PB-01 (`pb01_runner.py:153/198/214`); all **log-only/fail-open** (`screening/hard_gate.py:337-345`), **BUT flipping `regime.enabled` mid-soak corrupts the shadow baseline being accumulated for the enforce decision ⇒ DO NOT FLIP IT in Phase 1.** Gate today: `regime.enabled: false` (`system_config.yaml:362`, default `config_loader.py:895`); Phase 0 proved the token at **1-min × 1 day**, the engine needs **`day` × 400d ≥201 candles** — implied, not proven. Already ordinal (`engine.py:162-179`, `models.py:37`), and **Phase 2's map already exists**: `regime_pref_direction {BULL:1.0, SIDEWAYS:0.5, BEAR:0.0}` @ `w_regime_preference: 8.0`/40 (`config_loader.py:1086-1091`). Fail-safe confirmed (3 layers, 13 tests). Shadow sink: **NOT** `data_store/regime/regime_state.json` (overwritten ⇒ no history, `runner.py:89-98`) — recommend append-only `data_store/regime/regime_daily.jsonl` via a post-close cron sibling of the 15:40 `fetch_daily_candles`. **⭐ Q10 feasible but data missing: NIFTY candles = 1 DAY only (16-Jul) vs 155 filled trades / 23 days (15-Jun→16-Jul)** ⇒ backfill via the proven `fetch_daily_candles.py --backfill --from --to` (data-only, no migration), then correlate — **caveat n=23 + the 91%-long scanner-mix confound**. **DO Q10 FIRST.** Docs-only, **UNPUSHED**. [[regime-phase1-investigation-18jul]]

> 🚨✅🚀🔝 **DEPLOYED 18-Jul-2026 ~11:2x IST — MISSING/INVALID `direction` NOW ALERTS LOUDLY AT BOOT (Telegram+email, naming the file). PC == origin == VM bare == tag `deploy-18jul-missing-direction-alert` → `34fd6a5`.** **§2 finding = path (A) FAIL-BOOT, not silent-skip:** `load_all_strategies` (`strategies/loader.py:67-69`) raises on the FIRST bad YAML; the boot's real gate is preflight `check_strategy_configs` (`utils/startup_checks.py:1266`, called from `main.py:1887`) which catches it → `blocking_failures "invalid_strategy_configs"` → `main.py` `return 3` — **before the notifier exists (`main.py:2048`) ⇒ NO Telegram/email today**, one log line, first bad file only. **Fix (alert, then KEEP failing the boot):** NEW `strategies/loader.py::scan_strategy_errors()` (pure, collects ALL bad files, never raises, pydantic-`.errors()` reasons) + NEW `main.py::_alert_invalid_strategy_configs()` (ONE CRITICAL naming every file+reason; reuses `TelegramNotifier.from_env` + `write_critical_sentinel` — no parallel notifier; `write_sentinel=False` ⇒ one Telegram + ONE email, no duplicate; email fires even with no TG token; **fail-safe**, paper==live), wired guarded into the abort branch before `store.close()`+`return 3` (abort byte-identical). Covers ANY validation failure, not just `direction`. No schema change (v44, no migration); integrity/FK clean; backup `pre_deploy_missing_direction_alert_20260718.db` (`quick_check=ok`, v44, 361 trades). **PROVEN on the VM with deployed code: real 16 YAMLs → `[]` SILENT; broken temp copy → `missing required field 'direction'` / `... got 'BUYSELL'`; real config untouched.** RED-on-old on a base worktree (never stash); regression 11F/4918P vs base 41F/4877P ⇒ ZERO attributable. Bites at the next boot (Mon 20-Jul 08:15). Report `docs/audit/missing_direction_alert_done_18jul2026.md`; memory `missing_direction_alert_18jul`. [[missing-direction-alert-18jul]]

> 🧭✅🚀🔝 **DEPLOYED 18-Jul-2026 ~00:2x IST — STRATEGY-DIRECTION REGISTRY (auto-detect/notify/confirm/DIRECTION_CONFLICT + consumer routing). UNBLOCKS regime Phase 1. PC == origin == VM bare == `44fdbcb`; tag `deploy-18jul-strategy-registry` → `c2f82ff`.** `config/strategy_direction_registry.yaml` (3 fields: direction/registration_status/health, keyed on `StrategyConfig.name`) + `core/strategy_direction.py` (canonical accessor) + `scripts/strategy_registry_officer.py` (daily 16:22 Mon-Fri; mirrors `cron_officer._auto_discover`). `StrategyConfig.direction` (`strategies/schema.py:58`) stays the ONE source; registry mirrors it, never overrides. **FIX-182 correction:** conflict = a daily realized-vs-declared DB scan (ORPHAN_ADOPTION isn't strategy-attributed). Retired GUI `strategy_tower._family_of` name-parse; routed `eod_squareoff` vote→canonical (behaviour-neutral). No migration (YAML, v44). PROVEN on real data: 0 new, 11 confirmed, 0 conflicts. Report `docs/audit/strategy_direction_registry_done_17jul2026.md`; memory `strategy_direction_registry_17jul`. [[strategy-direction-registry-17jul]]

> 🧭🔎 **17-Jul-2026 — STRATEGY-DIRECTION INVESTIGATED (READ-ONLY; `082f610`, pushed with the registry above).** Report `docs/audit/strategy_direction_investigation_17jul2026.md`; memory `strategy_direction_investigation_17jul`. **⭐ direction is ALREADY structured — `StrategyConfig.direction` (`strategies/schema.py:58`, enum-validated), NOT name-derived.** Webhook carries NO side; `signals` has no side column; direction originates at the config field (`signal_processor.py:778`) → `orders.transaction_type` → `trades.direction`. **Declared==actual perfect (11 traded strategies), no strategy has both sides ⇒ NO conflict, addendum STOP not triggered.** Nuance: outcome derives from the declaration ⇒ can't catch a config mislabel except via `ORPHAN_ADOPTION`. Q4 mirror = `cron_officer._auto_discover :536`. Q5 name-parse is GUI-only (`strategy_tower._family_of`). Q6 = persist ONE `strategy_direction` registry + auto-detect+notify + DIRECTION_CONFLICT + retire name-parse. [[strategy-direction-investigation-17jul]]

> 📊✅🚀 **DEPLOYED 17-Jul-2026 ~22:5x IST — REGIME PHASE 0: NIFTY + sector index candles now ingested/stored. DATA ONLY. PC == origin == VM bare == `c8a2e6c`; tag `deploy-17jul-regime-phase0` → `4e2df39`.** Root cause of BK-1's empty index data: index tokens were never in either candle path. Fix (reuse): extended `scripts/fetch_daily_candles.py` (+ `config/index_universe.yaml`) to fetch 10 indices via the existing `historical_data`→`candles` path, called first in `_fetch_single_day`, fail-safe. No migration (`candles.volume DEFAULT 0`). PROVEN: NIFTY 256265 0→375 rows; stock ingestion unchanged (+10 tokens, 0 stock rows changed). Report `docs/audit/regime_phase0_index_ingestion_17jul2026.md`; memory `regime_phase0_17jul`. **⚠️ Phase 1+ (regime compute→weighting) = signal-path careful-loop, NOT built.** [[regime-phase0-17jul]]

> 📊🔎 **17-Jul-2026 — BK-1 LONG/SHORT + PER-SCANNER ANALYSIS (READ-ONLY; `bc35a62`, pushed with Phase 0).** Report `docs/audit/bk1_long_short_scanner_analysis_17jul2026.md`; memory `bk1_long_short_scanner_17jul`. **⭐ The "58% short" premise is INVERTED — the closed book (155 trades) is 91% LONG.** Loss concentrated in intraday longs (−0.239R, n=108); positional longs + shorts net positive. Biggest drain `vwap_bounce_long` (−12.1R, n=35). Losers are negative-EDGE (negative on gross), NOT cost-killed. Regime confound unmeasurable — no index candles in the DB. D2 data package; every stat has its n. [[bk1-long-short-scanner-17jul]]

> ✅🔒🚀🔝 **DEPLOYED 17-Jul-2026 ~21:1x IST — P1: `GET /health` honours `require_hmac` (+ P2/P3/P4). PC == origin == VM bare == `2b10b5e`; tag `deploy-17jul-p1-health-hmac` → `695aa84`.** `signals/webhook_receiver.py` `/health` gained `elif receiver._require_hmac: ok = False`, mirroring `/webhook` `:469` — a token-only `/health` now 401s under `require_hmac=True` (was 200 + system state = the AB-910 §1.7 oracle); valid HMAC still 200s; unauth 401 byte-identical both settings (S4 boot-seam preserved). **INERT until Rama flips `require_hmac`.** Report `docs/audit/p1_health_require_hmac_done_17jul2026.md`; memory `p1_health_require_hmac_17jul`. P2 `wired_system_authenticated` fixture · P3 route-map dashboard guard (37 routes) · P4 comments. Regression 43F/4842P ZERO attributable; backup `pre_deploy_p1_health_hmac_20260717_211747.db`; v44 no-migration. Target DOWN → loads at Mon 20-Jul 08:15 boot.

> 🔎🔴 **17-Jul-2026 — FIXTURE BLINDNESS SCOPED (READ-ONLY; now FIXED by P1 above).** Report `docs/audit/fixture_blindness_investigation_17jul2026.md`; memory `fixture_blindness_17jul`. **STEER: the premise was STALE — 10 of 11 security-gated surfaces have their secured branch TESTED; there is NOTHING to sweep.** The webhook auth branch IS tested (`test_webhook_receiver.py:288-341`; `/health` auth-ON via `test_fix134_backpressure.py:151` `_make_receiver_with_secret()`, 6 tests) and **the S4 fix already closed its own boot seam** (`test_startup_checks.py:768`/`:789` + wired `test_hardening_scenarios.py:298-331`). **🔴 ONE empty cell — `require_hmac=True` × `GET /health` — and the branch is ABSENT from prod:** `webhook_receiver.py:290-304` never consults `_require_hmac` (its only request-time site is `:469`, on `/webhook`). **PROVEN by runtime probe with a working control:** `/webhook?token=` → **401** ("token param is not accepted") but `/health?token=` → **200 + `kill_switch_active` + `queue_depth`** = the AB-910 §1.7 oracle, still open. **LATENT — arms when Rama flips `require_hmac` (an OPEN Rama-action).** **⚠️ FALSE CONFIDENCE:** `TestHealthAuth`'s 6 `/health` tests ALL run `require_hmac=False` (`_make_config()` at `test_fix134_backpressure.py:35-48` omits `webhook` entirely ⇒ `getattr(..., "require_hmac", False)`) — they are green and blind to this; BL-18's 5 tests are **constructor-only**. **BUILD:** P1 `/health` honours `require_hmac` (~3 lines + 2 tests, **LOOP**) · P2 auth-ON wired fixture (`tests/integration/conftest.py:332`) · P3 route-map dashboard guard test · P4 comments.

> ✅🔀🚀 **DEPLOYED 16-Jul-2026 ~18:5x IST (off-market): the 16-Jul COMBINED DEPLOY is LIVE — alert-watcher `--loop` + F1 trades.sector/gate-8-observe. VM==bare==`11abebb`** (= the validated tag `deploy-16jul-alertwatcher-f1`→`1d5337d` + 5 docs-only files; `git diff --name-only 1d5337d..11abebb` = markdown ONLY ⇒ deployed CODE == the regression-green tag; Rama approved the SHA deviation from the instruction's `f68d15d`, which was stale by 2 docs commits). Schema **v44 unchanged (no migration)**; integrity/FK clean; post-receive checkout + crontab auto-install OK. **Behaviour-neutral:** gate-8 `sector_cap_mode: observe` (log-only) · alert-watcher is monitoring. **alert-watcher FIXED: `activating`/auto-restart NRestarts=104,567 → `active`/`running` NRestarts=0**, single process PROVEN via the unit cgroup (`TasksCurrent=1`; a raw `pgrep -fc` says 2 = the ssh shell self-matching — use `ps -eo args | grep -c '[a]lert_watcher.py'`). Canary ALL GREEN incl. the new `respawn` probe. Backup `data_store/backups/pre_deploy_16jul.db`. Report `docs/audit/deploy_done_16jul2026.md`. **⏰ OWED: alert-watcher 24h soak re-sample (baseline 16-Jul 18:47:50 — RSS 37,548kb · 1 thread · 4 fd · 0 db-fd; ANY growth → rollback) → F1 observe soak ≥1 session (starts Fri 17-Jul; engine is paused tonight) → evidence → Rama-gated enforce flip.** M-C4 (`6c77525`) remains UNPUSHED.

> ✅🏁🚀 **DEPLOYED 16-Jul-2026 ~22:1x IST (off-market): the M-C CAPITAL-SAFETY CLUSTER IS LIVE-DEPLOYED. VM==bare==`06a61cb`; code == the drilled tag `deploy-16jul-mc-cluster`→`341cc57`** (`git diff --name-only deploy-16jul-mc-cluster..HEAD` = markdown ONLY ⇒ deployed code IS the tag; local main was `06a61cb`, NOT the SHA in the report — **the TAG is the code identity, re-derive at run time**). Schema **v44 unchanged, NO migration** (deployed `EXPECTED_SCHEMA_VERSION=44` == live v44 ⇒ the 08:15 boot skips migration); integrity/FK clean; services healthy; all 4 fixes verified in the checked-out tree (**M-C4 by AST**: `record_api_failure`'s `with self._lock` block has ZERO `soft_kill` calls). Backup `data_store/backups/pre_deploy_mc_cluster.db`. **⏰ GOES LIVE AT THE 17-Jul 08:15 BOOT** — `trading-system.service` is halted on the planned operator SOFT_KILL (dated 16-Jul ⇒ a PRIOR-DAY kill ⇒ `main.py:1607 clear_stale_state` auto-clears it); **NO restart owed, no operator action**. **⚠️ M-C8 rewrote the EMERGENCY path and the drill was MOCK-broker only ⇒ THE FIRST LIVE HARD_KILL IS THE REAL TEST** — watch for `flatten dispatched to worker thread` (good) vs `UNEXITED`/`CRASHED`/`SHUTDOWN WITH FLATTEN STILL RUNNING`/`could NOT start the flatten worker` (each ⇒ manual broker-truth verification). Report `docs/audit/deploy_done_mc_cluster_16jul2026.md`. Rollback: L1 revert a fix · L2 revert a merge · L3 reset to `16437ae`; all schema-free. — HISTORICAL (pre-deploy): M-C4 · M-C8 · M-C5 · M-C6 (+ the test_main logger-leak fix). Validated as ONE unit: fresh combined regression + a SANDBOX hard_kill drill (mock broker + scratch DB). Tag = the rollback point. **M-C5:** `commit_adopted_entry`'s guard is inside the lock but the commit is outside ⇒ two NON-GATING callers both commit and the loser's ValueError fires a **SPURIOUS hard_kill** (not a capital error). Fixed with an in-memory **CAS claim** taken under the lock already held + released in a `finally` — **NO lock across the commit I/O**. **M-C6:** a zero/negative multiplier floored to **1 lot** (capital against intent) → now `constraint="ZERO_MULTIPLIER"` skip; FIX-133's floor preserved for positive. Behaviour-neutral today (`min_weight=0.5` clamp) — the **prerequisite for lowering `min_weight`**. ⚠️ `PositionSizingTierConfig` HIGH/MEDIUM/LOW are **bare floats with no `ge=0` bound** ⇒ a negative tier multiplier loads cleanly from config. **Do NOT disturb the deployed `11abebb`.** Reports `docs/audit/mc_cluster_consolidation_16jul2026.md` · `mc5_mc6_testmain_16jul2026.md` · `mc8_async_hardkill_16jul2026.md` · `mc4_killswitch_lock_16jul2026.md`.

> 🧨✅ **M-C8 (in the cluster above):** the HARD_KILL flatten runs on a **non-daemon worker**, not the caller's fill/commit thread (it could starve the fill pipeline up to 2h). `hard_kill` fires-and-returns on the **adapter** path; the **legacy `cancel_fn` path stays SYNC — that split is load-bearing** (it is what keeps the 86 pre-existing kill-switch tests untouched). ⚠️ **the design's "no existing test calls `hard_kill()` with an adapter" was FALSE** — `test_p0_live_day1_fixes::TestBugC_KillSwitchExit` did, and broke; those 3 now call `_exit_all_trades_indestructible()` directly. **A test driving `hard_kill()` with an adapter gets a DISPATCH, not a result — `drain_flatten()` first, then assert.** State machine `IDLE→RUNNING→DRAINING→COMPLETE` under a **dedicated** lock (never the KS4 RLock — that would re-create M-C4). **The EXITING-blind race is CLOSED:** `_eod_self_exit_due` gates on `KillSwitch.is_flatten_in_progress`, **never** `count_active_positions()` (which cannot see a flatten in flight); `_shutdown` drains the worker **before** teardown; external SIGTERM gets a bounded **15s** grace (`TimeoutStopSec=30`) then CRITICAL. Files `capital/kill_switch.py` + `main.py`.

> 🧪⚠️ **`tests/unit/test_main.py` leaked a MagicMock into `main._log` — FIXED (test-only).** `main._main_locked` rebinds the global `_log` (`main.py:1538`/`:1626`); the patch restores `get_logger` but NOT `_log` ⇒ **any later test asserting on main's logging was SILENTLY VACUOUS**. Autouse fixture saves/restores it; the guard is deliberately LAST in the file (pytest runs in definition order).

> 🗂️⭐ **BATCH-vs-LOOP CLASSIFICATION (16/17-Jul) — a PROPOSAL awaiting Web Claude's approval; NOTHING executed:** `docs/audit/batch_classification_16jul2026.md`. **27 BATCH-SAFE** (docs 6 · repo-hygiene 1 · logging 5 · tests 3 · verify-only 6 · trivial-code 6) · **+3 batch-safe-by-risk but TOO LARGE ⇒ own run** (AB-910 · Q9 · BK-1) · **~70+ LOOP-REQUIRED** · **8 AMBIGUOUS defaulted to LOOP** (X1 GUI fail-open 2FA is the most security-severe item on the whole list). **📌 The 16-Jul register is STALE: B1 (trades.sector), B4 (alert-watcher) and M-C4/C5/C6/C8 are ALL CLOSED by today's two deploys — verified at the deployed head** ⚠️⚠️ **CORRECTED 28-Jul-2026: "B1 CLOSED" IS HALF TRUE AND MISLEADING. The WRITE path closed (the column stopped being NULL from 20-Jul), but the RESOLUTION did not — MEASURED in production: `sector` is NULL on 361 rows (15-Jun→16-Jul), `'UNKNOWN'` on 81 (20-Jul→28-Jul) and a real value on exactly ONE trade ever (`PSU_BANK`, 20-Jul). ⇒ the 40% sector cap still does not do its job, and the ERROR DIRECTION INVERTED — NULL made it silently permissive (`sector_exposure()` summed nothing), a single `UNKNOWN` bucket makes it sum the whole book. ⛔ Read `sector_exposure()`'s handling of 'UNKNOWN' BEFORE D1/sizing moves. Register §4 G3.** **(original line continues:)** **⇒ B2/M-S4 (`secondary_screener.py:407-410` still `atr/rsi/prev_close: None`, a quarter of the selection score dead) is now the TOP open engineering item. ⇒ B2/M-S4 (`secondary_screener.py:407-410` still `atr/rsi/prev_close: None`, a quarter of the selection score dead) is now the TOP open engineering item.** Rule applied: LOOP if it touches capital/kill/order/schema/sizing/regime/any live trading decision; **unsure → LOOP**. Group-E boundary: verification-only items produce a FINDING and STOP — **any implied fix EXITS the batch to LOOP**. memory `batch_classification_16jul`.

> ✅🗂️🚀 **BATCH-1 DEPLOYED 16-Jul ~23:55 IST (off-market) — PC `main` == VM bare == `eb9cffa`, 100% SYNCED, ZERO unpushed. Tag `deploy-17jul-batch1`→`eb9cffa` IS the deployed HEAD (code-identity delta EMPTY); schema v44 no-migration; NO restart owed. Report `docs/audit/deploy_done_batch1_16jul2026.md`.** ⏰ **When each item bites (deploy ≠ restart):** the GUI session key applies at gui-dashboard's NEXT restart — **that restart is the LAST one that logs Rama out**; cron items (win%/holiday guard/token/candles/alert escalation) on each job's next run; M-K5 at the next boot. — HISTORICAL (pre-deploy): 15 batch-safe items, one commit each, off base `362e166`. **No schema change, no trading-path change.** **🏆 M-SC2 CLOSED — runtime-proven** (screened CSV 1 row/40B on 13-15-Jul → **25 rows/2,033B on 16-Jul**, the first EOD after M-SC2b deployed). **🔴 CAPITAL FINDING (fix = LOOP): CHECK1/RMS pass `costs=0.0` ⇒ `pnl = gross_pnl - costs` is GROSS ⇒ the DAILY-LOSS LIMIT's input (`fm_ledger.pnl_delta`) AND available capital are credited with gross, not net** — and this book is gross −0.007R vs net −0.100R. **1 ESCALATED to LOOP:** P3-s14 (the webhook "dark window" is really an un-rolled-back dedup claim = entry-path control flow). **D2 impossible:** `tests/crash_test/ct_utils.py:63` hardcodes the LIVE DB path, opened writable. Killed: a **1.44MB base64-XLSX with trade data** in git · the **live token in the cron log** · a **DEPLOYMENT.md path that doesn't exist** · the **missing README** · the GUI logging Rama out every restart · **NR-4's stray D:\ folders (a test that was vacuous on Windows created them)**. **Prod-visible:** GUI sessions survive restarts · daily review skips non-trading days · headline win% 39.87→40.13 while a breakeven is in-window · a critical FAILED alert escalates when the cron registry is unreadable. Report `docs/audit/batch1_done_16jul2026.md`; memory `batch1_done_16jul`.

> 🔒🔎 **AB-910 — Audit-B Phase 9 (Ops) + Phase 10 (Security) PRODUCED 16/17-Jul (READ-ONLY; nothing fixed):** `docs/audit/audit_b_phase9_ops_phase10_security_16jul2026.md`. **5 HIGH, no CRITICAL.** **🔴 NEW: (1) the LIVE Telegram bot token is written to logs in CLEARTEXT — 984 lines / 10 debug files / ONGOING** (`urllib3.connectionpool` DEBUG logs the request line and **Telegram puts the credential in the URL PATH**; `core/logger.py:373`/`:409` sink ALL loggers at DEBUG) ⇒ **an attacker with it can FORGE alerts that look like the system's own**. **(2) `market_day_only` is DECORATIVE on 20 of 25 scripts** — 30 jobs declare it, **nothing enforces it**, only 5 self-guard (batch-1 fixed one; incl. 4 PAID gemini jobs running on holidays). **Confirmed:** the live **PROD 2FA seed sits in the PC dev tree** (hash-identical to the VM ⇒ both factors on one box) · **fail-open 2FA is LATENT NOT live** (the tracked config's prose placeholders make pyotp raise ⇒ fail-CLOSED) · **S2: live DB + all 33 backups on `/dev/sda1`, nothing offsite** · rpcbind:111 serves nothing · SSH is world-open **but key-only** (why the July bot got 0 successes). **Ranked fix queue = report §3.** memory `ab910_ops_security_audit_16jul`.

> 🚫📋 **Operator CARDS + decision sheets are NOT repo artifacts** — `*_CARD.txt`, `TONIGHT_IF_PC_IS_DOWN_*.txt` and `*_DECISION_SHEET_*.txt` are Rama's external working files, git-excluded (`.git/info/exclude`, local/not-pushed), never `git add`-ed. Tracked analysis reports live under `docs/audit/`. ⚠️ **CHANGED 28-Jul-2026 — THE REGISTER IS NOW THE EXCEPTION AND IT LIVES IN THE REPO: `docs/MASTER_PENDING_28-Jul-2026.txt`** (`d5b9d8c`), consolidating seven Downloads editions on Rama's explicit instruction; a read-copy stays in Downloads. 🔑 **The block was `.git/info/exclude:29`, NOT `.gitignore`** — grepping `.gitignore` finds nothing and is the wrong file; use `git check-ignore -v <path>`. Unblocked by a **negation on the canonical path only** (`!docs/MASTER_PENDING_*.txt`), so a stray register elsewhere stays excluded. memory `feedback_operator_planning_docs_external`.

> 🗂️📄 **FILE FORMAT BY LOCATION (Rama, 30-Jul-2026):** `Downloads/` = temporary, one-time, read-once ⇒ `.txt` or `.md` as suits · `docs/audit/` = permanent or repeatedly-read ⇒ **`.md` PREFERRED** (for the data/table formatting). ⛔ Retro-conversion is NOT a task — convert a file when you next touch it.

> 🏦 **F1 sector cap — DEPLOYED 16-Jul in `11abebb`, LIVE in OBSERVE:** `trades.sector` is populated at INSERT (`orders/order_placer.py:955` ← `InstrumentCache.sector`, frozen, `UNKNOWN`-bucketed); gate-8 `risk.sector_cap_mode` = **`observe`** (`config/system_config.yaml:187` — logs `WOULD_REJECT`, **does NOT reject**). **observe→enforce (activates the live 40% sector cap) is NOT flipped** — OFF-MARKET + Rama-gated after an observe soak ≥1 session (first data = Fri 17-Jul). Instant de-fang = set back to `observe`. memory `f1_trades_sector_16jul`.

> ⏸️🔴 **16-Jul-2026 ~10:54 IST — SYSTEM INTENTIONALLY PAUSED (planned pause for fix/review work; NOT a crash).** `SOFT_KILL` active (operator; reason "planned pause for pending fix/review work — no trading issue") → `trading-system.service` is **HALTED** (`startup_scenario=HALT` → **exit-4**, the designed halt-on-active-kill / Audit #18; `RestartPreventExitStatus=3 4` → stable, no flap). **Book verified FLAT at the broker.** First live boot on `c9fb298` was VERIFIED CLEAN (schema v44, no-migration, token 08:15, canary 08:20 ALL GREEN incl ✅ email). Pre-market kill window was missed (task@09:56) → ran live from 10:00 and took 3 trades, all closed cleanly by the engine (ACI SL-out · HUHTAMAKI TGT-win · PRAVEG). **RESUME = `deploy/resume.sh` (stop→clear→start), or it auto-clears at 08:15 tomorrow** (`clear_stale_state` wipes prior-day kills). Independent monitoring/EOD crons keep running (EOD emails unaffected). **⚠️ live working tree = `/home/ubuntu/systems/trading-system` (venv `/home/ubuntu/systems/venv`); bare `~/trading-system.git`.** Report `docs/audit/morning_verify_16jul2026.md`; memory `morning_verify_16jul`. **Finding: alert-watcher respawn loop (pre-existing, non-trading) — FIX BUILT+TESTED 16-Jul, UNPUSHED (deploy OFF-MARKET): `alert-watcher.service` → `--loop` daemon + `Restart=on-failure` + `TimeoutStopSec=35` + `StartLimit`; monitoring-canary `respawn` probe added so a respawn can never read HEALTHY. See `docs/audit/alertwatcher_loop_fix_16jul2026.md` / memory `alertwatcher_loop_fix_16jul`.**

> ✅🔀🚀 **DEPLOYED 16-Jul-2026 ~00:1x IST (off-market): 15-Jul COMBINED DEPLOY is LIVE. VM==bare==`c9fb298`; tag `deploy-15jul-combined` → `1645bd6` (single-tag rollback anchor) pushed.** Branch A (eod_cleanup FK-safe children-first prune + 90d retention · generate_screened_csv read-only M-SC2b · fm_ledger `id`→`ledger_id`) + Branch B (monitoring F1-F4 · canary 08:20 · deploy_assert) + record_heartbeat autospec/guard. **Schema v44 unchanged (no migration).** **F0 DONE** — fresh `ALERT_SMTP_PASSWORD` installed in VM `.env` (via stdin, 600, surgical; SMTP login OK + canary `✅ email`); alert-watcher exit-2 loop cleared (ExecMainStatus=0). **Phase-B prune DONE** — 113,377 old signals cleared (146k→32,623; screener 122,453→23,363), **trades 355 UNCHANGED, foreign_key_check CLEAN, integrity ok**. Steady-state retention stays **90d**. Backups: `data_store/backups/pre_deploy_combined_20260716_001521.db` + `pre_prune_20260716_002038.db`. Report `docs/audit/deploy_done_15jul2026.md`; runbook `docs/audit/integration_deploy_15jul2026.md`. ⏰ Rama: DELETE the decision sheet (holds the Q6 app-password); confirm 16-Jul EOD emails arrive. Rollback = L1 revert fix commits + off-market restart / L2 reset to the tag's first parent.

> 🚀🟢 **LIVE STATE (14-Jul-2026 — DEPLOYED `277d63e`, off-market, service DOWN; PC==VM==bare).** Q4+Q5+P11 bundle + PB-01 SHADOW flip (`bb9b1e7..277d63e`, 35 commits). Flags: **`v3_chain_mode: shadow`** (background enrichment of the 15 live strategies; verdict LOG-ONLY) · **`watchlist.enabled: true`** (PB-01 EOD capture + next-morning entry stage → would-be JSONL only, no position/capital/CNC/GTT) · **PB-01 strategy `enabled: false`** (fail-closed) · delivery TRIPLE-LOCKED (`force_intraday_only:true` + `trade_type:INTRADAY` + `delivery_enabled:false`) · `min_pass_score: 60` · sizing UNTOUCHED · schema **v44** (== live, no migration). **NO restart owed → next 08:15 boot loads it.** 15-Jul = supervised session (boot-clean + session-1 trade-count ≈ pre-deploy + M-S5 shadow-inning watch + PB-01 EOD capture at close). **🔴 `trades.sector` is NULL on 100% of rows → `StateStore.sector_exposure()` (`... AND sector=?`) always returns 0 for the resting book → the 40% sector cap has NEVER fired (a control that reports PASS while doing nothing); gate-8 TOCTOU hardening is INCOMPLETE. Harmless today (~₹198/pos « ₹3,957 cap); D1 (raise `max_concentration_pct`) is BLOCKED until `trades.sector` is populated at insert.** Rollback = revert main→`bb9b1e7` + off-market restart. Evidence `docs/audit/deployment_evidence_pb01_shadow_14jul2026.md`; memory `pb01_shadow_deploy_14jul`.

> 🚀🔗 **15-Jul-2026 COMBINED DEPLOY INTEGRATED to local `main` (UNPUSHED; VM==bare `2dc69d5`; tag `deploy-15jul-combined`).** Branch A (3 fixes: eod_cleanup FK/retention · generate_screened_csv M-SC2b · fm_ledger) + Branch B (monitoring hardening F1-F4 + canary + deploy-gate) MERGED. Overlap `generate_screened_stocks_csv.py` resolved — A read-only path + B `_cron_main` functional both present. Schema v44 unchanged; full combined regression green; EOD dry-run clean on a DB COPY; deploy_assert + integrity_check + foreign_key_check pass. Runbook + evidence `docs/audit/integration_deploy_15jul2026.md`. ⏰ **Rama: F0 restore `ALERT_SMTP_PASSWORD` · push off-market · verify · Phase-B prune.**

> 🛡️🔧 **15-Jul-2026 MONITORING HARDENING BUILT — branch `monitoring-hardening-15jul` (off `main` `0a77c92`), UNPUSHED; schema-free, test-first, paper==live.** Closes the two silent-failure classes. **F1** `alert_watcher` no crash-loop + Telegram fallback + `alert_watcher_degraded.json` marker · **F2** EXECUTION vs FUNCTIONAL status (`utils/cron_heartbeat.py` `functional_status` in the `message` col, NO schema; Cron Officer surfaces it + the F1 email-degraded line) · **F3** `check_cron_drift` marker-aware · **F4** purge `telegram_ban_until` + date-embed `alert_watcher_<date>.log` · **CANARY** `scripts/monitoring_canary.py` (daily 08:20, 4 alert paths) · **DEPLOY-GATE** `scripts/deploy_assert.py` (blocks only BLOCKER class) · **CHECKLIST** `docs/monitoring_prevention_checklist.md` (7 items/new job). Report `docs/audit/monitoring_hardening_15jul2026.md`; memory `monitoring_hardening_15jul`. **⏰ F0 (Rama): restore `ALERT_SMTP_PASSWORD`.** NOT pushed.

> 🩺🔴 **15-Jul-2026 WHOLE-SYSTEM HEALTH AUDIT (read-only, `3b9ccd1`, two verdicts).** A) TRADING = **PASS** (trades within caps, book flat, DB integrity clean, backups/token OK). B) MONITORING TRUTH = **FAIL: 🔴 SMTP EMAIL DEAD (Gmail `535 BadCredentials` / expired `ALERT_SMTP_PASSWORD`)** → 15-Jul EOD emails never delivered + **8/8 critical sentinels UNDELIVERED** while heartbeats say SUCCESS (Layer-9); **`alert_watcher` exit-2 restart-loop** (email-only, no telegram fallback — `scripts/alert_watcher.py:427` `return 2` on `SMTPAuthenticationError`); `check_cron_drift` false-alarms 4 exit_code_file jobs. **TELEGRAM (direct-emit) WORKS.** Broke after 14-Jul 21:20 (external credential expiry). **NO BLOCKER → DEPLOY REC: PROCEED with the 3-fix branch; ⏰ P0 = RESTORE SMTP CREDENTIAL (urgent, Rama)** then a monitoring-hardening cycle. Report `docs/audit/system_health_audit_15jul2026.md`; memory `system_health_audit_15jul`.

> 🟢🔬 **15-Jul-2026 SESSION-1 (supervised, first post-deploy) RAN CLEAN — read-only reconstruction + follow-up (deploy UNCHANGED `2dc69d5`; nothing fixed).** VM rode through Rama's local power outage (PC-independent — token auto-refreshes VM-side at 08:15, browserless); boot clean/NO migration (schema v44); book FLAT & reconciled; 12 trades ≈ baseline; prediction held. **Follow-up findings for the NEXT fix cycle (none applied):** 🔴 `eod_cleanup` FK prune BROKEN since the 14-Jul deploy — P10 `2e61fad` (`GLOB 'REJECTED*'`) × `screener_results` FK × `StateStore` `foreign_keys=ON` → `DELETE FROM signals` rolls back; 108k prune-stuck, unbounded; **M-K3 "cron FK-off" REFUTED**; non-safety, pull forward. 🟠 `generate_screened_csv` `store.transaction(readonly=)` TypeError (FIX-039, separate from `522da32` path-fix) → **M-SC2 RE-GRADED CLOSED→PARTIAL**. shadow-mismatch = known `broker_pnl=0.0` label-gap (today Rs 0.99, nothing new) → no fix, P1 shadow. **M-S5 watch RECALIBRATED** (323 in-baseline 122–840, guard correct). 18:15 forward-shadow cron FIRED. PB-01 not wired (spec ready — `:5000/webhook/pb01_breakout_retest?token=`, EOD, **must fire before 16:00**). Reports `docs/audit/{day_reconstruction,followup_investigation}_15jul2026.md`; memory `day_reconstruction_15jul`+`followup_investigation_15jul`.

> ⚠️🗄️ **SCHEMA MIGRATIONS RUN ON DB-OPEN, NOT "AT BOOT" (14-Jul-2026).** `StateStore.__init__`→`_initialize_schema()` migrates whenever ANY process opens the live DB with newer code (a cron/monitor/report/research script — whichever opens FIRST after a schema push), NOT only the 08:15 boot. 13-Jul: v42→v43 migrated 19:30 (`cron_watchdog`), v43→v44 at 23:41 (`forward_shadow_record`) — neither at boot. **RISK:** a during-market opener (`cron_officer` 09:20/hourly) would migrate the live DB mid-session with `main.py` running on it. **Corollary:** a "read-only verification" against the LIVE DB with newer code is NOT read-only — use `--db <copy>`. Rule + inventory + PROPOSED (unimplemented) guard: `docs/audit/migration_on_open_rule_14jul2026.md`; also in SYSTEM_MAP.md § Deploy.

> 🛡️🔎🟢 **Q8 VM-SECURITY FORENSICS (14-Jul-2026, read-only) — NO BREACH; changed nothing.** The "18:00–08:00 time-lock" = `scripts/copy_gate.py` (VM→PC file copies), **NOT an SSH gate**; SSH is 24/7 `0.0.0.0:22` key-only (`PasswordAuthentication no`/`PermitRootLogin no`) + fail2ban. ⚠️ **Never add an 18:00–08:00 SSH lock = that is Rama's off-market deploy window.** "Foreign SSH in the lock" premise REFUTED = a defeated Stockholm brute-bot (0/2246 success) + Rama's OWN Jul-13 09:16 key rotation (`BRi6…` vs baseline `uDRN8…`; run `scripts/approve_ssh_keys.py --apply` to re-baseline). Report `docs/audit/q8_vm_security_breach_forensics_14jul2026.md`; memory `q8_vm_security_forensics_14jul`. Owed = Rama's call on proposed controls (re-baseline key · SSH→Tailscale-only · key passphrase; recovery = OCI console).

> 🎯🛡️✅ **BUY-DAY FILTER #2b — THE RECONCILER'S THIRD SELL-UNDER-KILL SITE — `<BUILT>` 02-Aug `6495baa`(code+16 tests)+`c5c2668`(record+contract B-2a), ⛔ NOT DEPLOYED/PUSHED.** `order_reconciler._check2_inflight_orphan` (→`_flatten_broker_position`) flattened under an ACTIVE HARD_KILL, product-blind; it now reads the **SAME `core.constants.EMERGENCY_FLATTEN_PRODUCTS`** — **CNC SPARED loud** (new `check_name` `INFLIGHT_ORPHAN_SPARED_DELIVERY`) · NULL/unknown **FLATTEN + CRITICAL via the EXISTING shared `_alert_unknown_product`** (`site="reconciler_check2"`, **no second emitter**) · MIS/CO byte-identical. ⭐ **The spare is recorded as resolved NOWHERE — a spared CNC row is STILL an orphan and re-alerts EVERY cycle: the contract, not spam** (`_ORPHAN_CHECKS` correctly excludes it, so it is never mis-classified `ORPHAN_RECOVERY`). ⚠️ **The per-product-row hazard is UPSTREAM here and PRE-EXISTING: the cycle snapshot is symbol-keyed (`{p.symbol: p}` :863), so a same-symbol MIS row is collapsed before ANY check runs** — the kill_switch sweep (true per-(symbol,product) rows) is what still flattens it; re-keying would move checks 1-5 ⇒ D-8 work, not this. **0 status literals · 0 `holdings()` ⇒ Q4 ordering rule intact, D-8 still blocked.** RED-on-old 11F/5P; **regression 7F/5,491P NEW-set EMPTY** (base measured fresh same session; its 30 extra failures PROVEN worktree artifacts — 26 = the git-ignored `config/instruments.csv` absent from a worktree, 4 = python-not-on-PATH for subprocess preflight tests). ⛔⛔ **#2c CLOSED `<STOPPED AT STEP-1>` (02-Aug, docs-only) — A VALIDATED REDESIGN TRIGGER, NO CODE WRITTEN, and it AMENDS the line below.** #2c was carded to map the intent (CO→COVER_ORDER) at that sell; Step-1 proved the card's premise (*intent = a label*) **FALSE** and stopped before editing: **(a)** `intent` **IS** the live Kite `product` field (`product_resolver` INTRADAY→MIS·DELIVERY→CNC·COVER_ORDER→CO) · **(b)** ⛔ **Audit 3.1: a CO position CANNOT be squared by a reverse order at all** — broker rejects, auto-squares 15:20 with a **₹50+GST penalty**; the correct path is `cancel_order(entry_broker_id, variety="co")` on the parent bracket, **which `eod_squareoff` already does** ⇒ the mapping would only emit the order the broker refuses · **(c)** this path **cannot reach a parent order id** (`bp` is a POSITION row; ⭐ `trade_id` IS present = the redesign's only affordance) · **(d) 🔴 TODAY'S REAL BEHAVIOUR, worse than first disclosed: `product="MIS"` is ACCEPTED, does NOT net against the CO position, and OPENS A NAKED MIS SHORT while the CO position survives** · **(e)** LATENT not live — **double dormancy** (CO never used: 805/805 regular X6, declared by 12/15 YAMLs + discarded · AND `force_intraday_only: true` coerces non-INTRADAY back inside `place_order`) ⇒ the carded fix was a **no-op today**, only a new WARNING, arming silently on the breaker flip · **(f) ⭐⭐ STANDING RULE, beyond #2c: PAPER CANNOT VALIDATE PRODUCT SEMANTICS — paper nets by SYMBOL, live Kite nets per (symbol,product) ⇒ a paper drill of any product-semantics change is vacuously GREEN**; validate against live semantics or by construction, never by a paper run · **(g)** no `orders` row written here (RC18); **record-only latent hazard:** this caller never passes `variety`, `place_order` defaults `"regular"`, **nothing validates variety-against-product**. 🔴 Redesign **#2c-R** is with ChatGPT; ⛔ neither candidate architecture started. ⛔ **NEVER "fix" this by mapping the intent.** ⛔ #2c never touched Monday's critical path.

> ⚪ ~~**DISCLOSED not patched:** the flatten passes `intent="INTRADAY"` unconditionally ⇒ a **CO** position would exit under an MIS intent (H-5 class; never reached).~~ **← SUPERSEDED by #2c above (it understated the finding: a mapping fix cannot cure it).** 🔴 **R4 is now a DEPLOY-SLOT choice only** — Mon-eve stack or a named carry-pilot blocker; it never gated the flip. Record: `docs/audit/reconciler_product_filter_build_02aug2026.md`.

> 💀🚫✅ **#2c-R — ORPHAN-CO REFUSE-AND-ESCALATE — `<BUILT>` 02-Aug `42db913`(code+15 tests)+`bb25fa6`(record+stamps), ⛔ NOT DEPLOYED/PUSHED.** The approved redesign (Option 1). Under an ACTIVE HARD_KILL, `order_reconciler._check2_inflight_orphan` now **REFUSES** a **CO** position instead of selling it: **NO order of any kind is placed**, a CRITICAL names the reason, disposition **`INFLIGHT_ORPHAN_REFUSED_CO`** with **`success=False`** (correct-but-INCOMPLETE — unlike the CNC spare's correct FINAL state). **WHY:** Audit 3.1 — a CO position **cannot be squared by a reverse order at all**; the correct path is `cancel_order(entry_broker_id, variety="co")` and **this path has no parent bracket id**; the old code sold MIS, which is ACCEPTED, does NOT net, and **opened a NAKED MIS SHORT**. ⛔⛔ **`CO` IS a MEMBER of the shared `EMERGENCY_FLATTEN_PRODUCTS` (ONE name read by FIVE sites) ⇒ the refusal is a site-LOCAL branch placed BEFORE the membership test; CO was NOT removed from the constant** (that would have silently changed four other sites). `core/constants.py` **byte-untouched**; `kill_switch`/`eod_squareoff`/adapter/resolver byte-identical. **0 status literals · 0 `holdings()` ⇒ Q4 ordering rule intact, D-8 still blocked.** RED-on-old 11F/4P · **regression NEW-set EMPTY** (base fresh, same session+window) · **revert proven to ZERO BYTES vs `0a9e13a`**. ⚠️ **NEW: THE ALERT IS BOUNDED — CHECK6's FIX-B (wired, `main.py:2739`) marks a `PENDING_FILL` trade FAILED + releases its reservation on the 3rd consecutive cycle, after which the position routes to `_check2_orphan_adoption` (once-a-day suppression, `HUMAN_ORDER` label) ⇒ ~3 CRITICALs, NOT an unbounded stream, while the position is still live at the broker. ⭐ #2b's CNC spare inherits the SAME ceiling — its "re-alerts EVERY cycle" note is corrected, not left standing.** Disclosed, NOT patched (bounding is CHECK6's). ⭐⭐ **STEP-1b, REPORTED NOT PATCHED — NEEDS ITS OWN CARD: `kill_switch` HAS THE SAME CO DEFECT AT ALL THREE SELL SITES** (`:1629`/`:1708`/`:1789`) — maps CO→`COVER_ORDER` and **`variety` appears NOWHERE in that file** ⇒ coercion OFF = the order the broker REJECTS, coercion ON (today) = MIS = **naked short**. ✅ **`eod_squareoff` is CLEAN and is the REFERENCE implementation** (`variety="co"` cancel, `:1189`). 🅿️ **Option 2 (parent-id lookup → `cancel_order(variety="co")`) PARKED with an explicit trigger: CO intentionally enabled OR reliable parent-order lookup; OWNER = the CO protocol surface, ⛔ NOT the reconciler.** ⛔ Label ceiling: CO is **doubly dormant** ⇒ `<VERIFIED LIVE>` unreachable without a real CO position under a real HARD_KILL. Record: `docs/audit/reconciler_product_filter_build_02aug2026.md` §R1–R11.

> 🌳✅📋 **LEDGER #10 — DEPLOYED-TREE-vs-HEAD — `<BUILT — NOT DEPLOYED>` 03-Aug.** `scripts/system_manager.py` gains **CHECK 12 `deployed_tree_check`** (+`_resolve_deploy_git_dir`), wired into `run()` as the 12th spec/title. **No schema, no config, no new cron.** It reports **tracked drift** (`git diff --stat HEAD`) **AND untracked `.py`** — ⛔ **`checkout -f` leaves untracked files in place**, so an untracked `.py` is importable while being in NO commit (check 11 covers only the `.pyc` slice). **Path resolution: `~/trading-system.git` (VM bare repo — the work tree has NO `.git`) → `<root>/.git` (PC) → DEGRADE.** ⭐⭐ **THE AUDIT'S DESCRIBED TWO-LINER IS WRONG AS DESCRIBED — MEASURED: an EMPTY index makes `git diff HEAD` report every tracked file as deleted (`1250 files changed, 344936 deletions(-)` ON A CLEAN TREE); a ZERO-BYTE index is a THIRD behaviour (`fatal: index file smaller than expected`); and the REAL index would mean a monitoring job WRITES the deploy repo's index** ⇒ the correct form **COPIES the index and diffs the copy** (clean⇒empty · planted⇒detected · real index mtime UNCHANGED, all test-pinned so the copy step cannot be "simplified" away). ⛔⛔ **NEVER sets `soft_kill_reason` — `system_manager` CAN trip tomorrow's SOFT_KILL (`:1144-1145`), so this was KILL-ADJACENT, not additive.** Holds by construction (opt-in per check) + test-asserted. **Every failure path DEGRADES to a warning** (no git dir · no index · rc≠0 · 30 s timeout · OSError). ✅ 11 tests, **RED-on-old 11 FAILED at `f1972ec`**, REAL git repos not a mocked `subprocess` (§V4). ⛔ **Closes VERIFICATION, NOT ENFORCEMENT**; ⛔ not `<VERIFIED LIVE>` — first real run is on the VM. ⚠️ It answers *"tree == HEAD"*, **not** *"is the running PROCESS that code"*. Record: `docs/audit/ledger10_deployed_tree_check_03aug2026.md`.

> 🔁⛔📐 **LEDGER #3 — DESIGN REGISTERED 03-Aug `<REGISTERED — NOTHING AUTHORISED>`, DOCS-ONLY. Record: `docs/audit/ledger3_design_registration_03aug2026.md`; index: `MASTER_PENDING` §B.1 row 3.** ⛔ **No code, no design started, #3 fully GATED.** ⛔⛔ **THE PART THAT BINDS FUTURE WORK — R-3: ANY CHECK6 REDESIGN MUST EXPLICITLY STATE, FOR #2b AND #2c-R BY NAME, WHETHER ALERT COUNT / ESCALATION / REFUSAL SEMANTICS CHANGE.** CHECK6's 3-cycle FIX-B is the ONLY thing bounding **#2b**'s CNC spare (`6495baa`) and **#2c-R**'s CO refusal (`42db913`) to ~3 CRITICALs — and ⭐ **NEITHER ITEM'S CODE MENTIONS CHECK6**: the bound is emergent from cycle ordering (CHECK6 runs AFTER CHECK2), so a CHECK6 author retuning that counter gets **NO LOCAL SIGNAL** that two shipped items depend on it ⇒ both would regress silently. ⭐ **ENDPOINT FRAMING: the cancel-race (IA-P5-02) and the CHECK6 route are TWO INDEPENDENT PATHS TO ONE ENDPOINT** (the system's own position filed `HUMAN_ORDER`) ⇒ ⛔ **fixing either alone leaves it reachable by the other.** ⭐ **DIRECTION TO TEST AT STEP 1, NOT a settled mechanism: separate RELEASE-THE-RESERVATION from DISOWN-THE-POSITION** — one action today; **separability here is a measurement nobody has made.** **SPLIT: #3a `qty_filled` ships ALONE · #3b the endpoint covers BOTH paths** (⛔ don't bundle, ⛔ don't split the paths). ⚠️ **"~2 lines" is an ESTIMATE, not a measurement**; width = **405 of 805** wrongly zeroed (the other 400 CANCELLED are correctly zero). **R-4: FIX-FORWARD by default** — backfill only on evidence + separate approval (⛔ a wrong backfill is indistinguishable from correct data afterwards). **QUEUE UNCHANGED: #8b Step 2 → #2d → #3** (asked + answered; ⚠️ it had never been written down anywhere, and **`#2d` still has no register row**). 📜 **PROVENANCE: red-team DESIGN acceptances from ChatGPT — architecture review IS its remit ⇒ ⛔ NOT §G2 material** (G2 governs *Rama's* decisions, which is what R1–R5 were). ⛔ No new register row — **231 stands.**

> ⭐⭐🗓️ **RAMA'S RULINGS R1–R5 — RATIFIED 02-Aug-2026 ~16:32 IST, AUTHORITATIVE. Recorded at `docs/MASTER_PENDING_01-Aug-2026.md` §A-DEC.** 📜 **PROVENANCE MATTERS AS MUCH AS CONTENT: these are RAMA'S OWN decisions, confirmed by him directly** — ⛔ **which is exactly what distinguishes them from the THREE auto-filled prompts that IMPERSONATED an approval (practices §G1, 6 occurrences; #5 and #6 asserted rulings verbatim of this kind) and from ChatGPT's unprompted answers to Rama-only questions (§G2).** · **R1 — THE FILTER-SLIP GATE IS 3-PART: if the buy-day filter is not live, the ENTIRE flip postpones, not just the carry pilot ⇒ register A0 is CLOSED.** · ⭐⭐ **R2 — DEPLOY SLOT = OPTION Y ⇒ THE FLIP MOVES TO WED 5-AUG. Mon 3-Aug eve: push CODE + DOCS, NO flip flags · Tue 4-Aug: a SHAKEDOWN day (one more clean observation + the FIRST REAL EOD census, filter dormant-armed), then push the FLIP FLAGS alone that evening · Wed 5-Aug: THE FLIP.** ⛔ **4-Aug is NOT deleted — it is REASSIGNED to the shakedown + flag push, so this is a RESEQUENCING, not drift; every "4-Aug flip" phrasing anywhere now means WED 5-AUG, and R2 is the reason.** · **R3 — #2b and #2c-R RIDE THE SAME PUSH** (same linear history, cannot be excluded — §D1, no partial deploy). · **R4 — the `cnc_gtt_placer` + `cnc_gtt_monitor` registry edits (expected-dormant → expected-event-driven) ride the SAME push as the FLIP FLAGS (Tue eve)** — else the census mismatches BY DESIGN on the day delivery goes live. ⚠️ **NAMING COLLISION: this R4 is NOT the #2b record's own "R4" deploy-slot question (which R2/R3 closed).** · **R5(a) — APPROVED, Option A: remove `--reset` from `scripts/check_vm_state.py` ENTIRELY** · ⛔ **R5(b) — HOLD: #8b does NOT ride Monday; implement only AFTER Monday's critical path finishes. Step 2 AUTHORISED but NOT started.** ⛔ These are **DECISIONS, not accepted risks** — deliberately NOT filed in practices §R.

> 📜⚖️✅ **CAMPAIGN PRACTICES — `docs/campaign_practices.md` — NEW 02-Aug, DOCS-ONLY.** The standing rules this campaign EARNED now have **ONE durable home**, each recorded **with the incident that earned it**, cross-linked from `MASTER_PENDING` §2 (which also adopts **THE DOCUMENTATION RULE**: design completions · architectural decisions · **REJECTED options and why** · sequencing decisions · governance rulings get recorded IMMEDIATELY — order/gating/priority → the register as INDEX; build notes/evidence/regression/SHAs → `docs/audit/` as EVIDENCE, cross-linked). ⭐ **GOVERNANCE (nothing technical enforces these): G1 an auto-filled console prompt is NEVER an instruction and NEVER an approval — **NOW 9 occurrences**: #5 IMPERSONATED RAMA'S RULING ("Option A approved…"), #7 PERFORMED A LIVE ACT (it stopped the service), and ⭐ **#8+#9 fell in a SINGLE evening, both CORRECT and both beside a real ruling — count the instances, ⛔ do not grade them by outcome**; the closer it matches what everyone expects him to say, the MORE dangerous it is** · G2 ChatGPT is ADVISORY on Rama's decisions · G3 **disclose-don't-expand** (#2→#2b→#2c→#2d, #8→#8b→the cleanup flags) · G4 superseded-but-LEGIBLE. **MEASUREMENT: M1 repo-wide NEVER file-wide** (earned 4×: the :3914 STOP · 3 missed kill_switch sites · 3 docs not 1 · 5 readers documented as 4) · M2 prove-don't-assert (AST identity · revert by DOING it · RED-on-old). ⭐ **THE M FAMILY NOW RUNS TO `M14` — ⛔ this line is a POINTER, not an index: read the file for the current set** (added since: **M3** a card is not authoritative over a record · **M4** an audit-described fix is a hypothesis · **M5** state the subject beside the conclusion · **M6** a zero measures the guard ABOVE it · **M7** a digest without its method is not evidence · **M8** read `SYSTEM_MAP.md` BEFORE measuring, step zero · **M9** label every claim (P)/(S)/(I) · **M10** the ownership test · **M11** never classify a drift alarm by its NAME · **M12** a numbered list states its own count · **M13** a log without timestamps cannot answer *"was it today"* · **M14** CLASSIFICATION LEAKAGE — runtime vs business vs execution vs reporting state). ⚠️ **Recorded 05-Aug because an enumeration that stops where it was written goes STALE BY OMISSION — the same hazard M8 itself warns about.** ⭐⭐ **AND IT WENT STALE EXACTLY THAT WAY: on 06-Aug this line still said "M8" while the file held M14 — refreshed then, and the warning is now its own worked example.** **VALIDATION: V1 paper cannot validate product semantics · V2 `pytest tests/unit tests/integration -q` NOT `run_tests.py` + copy `instruments.csv` + DELETE partial logs · V3 a worktree base is sound only with the mitigation AND because the arithmetic closes (a base-only artifact can MASK a real failure) · V4 composition-truth not unit-construct.** **DEPLOY: D1 ⛔ THERE IS NO PARTIAL DEPLOY — `main` is linear, deploy is push→`checkout -f`, so anything committed rides the next push ⇒ only build what you'd be happy to deploy** · **D2 the BEHAVIOURAL SURFACE INVENTORY before EVERY window** *(⚠️ **RENAMED 06-Aug — the old "SHA gate" name made a reviewer test the wrong predicate and produce a confident FALSE FAIL: a commit cannot contain its own SHA**)*; it has caught its own stamp commits twice · D3 no push before 18:15, book flat, **Rama flattens MANUALLY — never build an auto-flatten** · D4 label honesty, `<VERIFIED LIVE>` NEVER for a latent path · **D5 the regression gate is a BEHAVIOURAL EQUIVALENCE gate — SETS not counts** *(06-Aug: BASE 55 → MERGE 29, merge-only **1** flake, base-only **27** ⇒ the push FIXED 27; ⛔ **a count would have blocked it**)* · **D6 git proves REPOSITORY sync, md5 proves DEPLOYMENT sync** — ⛔ `ahead 0` can NEVER stand for PC == VM; **D6.1 take the crontab md5 BEFORE every push** *(baseline `b8276da7…`, live == canonical)*. ⛔ **Process practice — distinct from `foundation_engineering_rules.md` / `trading_system_project_specific_rules.md` (PRODUCT rules); do not merge.** ⛔ No new register row — **231 stands** (the calibration guard is written into the rule itself).

> 🧪🔒✅ **LEDGER #8b — `scripts/check_vm_state.py --reset` — ✅ `<BUILT — NOT DEPLOYED>` 03-Aug `38f8ab9` (Step 2 DONE; rides the NEXT push, VM pinned at `d6c298d`).** ~~`<MEASURED, FIX GATED>` 02-Aug. ⛔ STEP 2 NOT STARTED, NO CODE WRITTEN; `check_vm_state.py` is UNTOUCHED.~~ **R5(b)'s hold released by the 18:22:51 push.** ⭐⭐ **IMPLEMENTED AS A LOUD BLOCK, NOT A SILENT DELETION — the design point, and it follows from the finding below: the flag's WORST property was PRINTING SUCCESS WHILE CHANGING NOTHING, so simply deleting the branch would have preserved that exact trap in a new form** (operator types `--reset`, sees the kill state print, gets **exit 0**, reads it as success). **Typing a flag is a DELIBERATE act ⇒ BLOCK** (fail-fast vs degrade): **exit 2 on stderr, naming BOTH sanctioned paths**, fired **BEFORE the live DB is opened**, and matching `--reset` in **ANY argv position** — the removed code only checked `argv[1]`. ✅ **5 tests `tests/unit/test_check_vm_state_reset_removed.py` (NEW PATH), RED-ON-OLD in a disposable worktree at `d6c298d`, test md5-identical both sides: 4F/1P — ⭐ T4 passing on old code is BY DESIGN (it guards that the diagnostics SURVIVED).** ✅ Collection **5,533** = 5,528 + exactly these 5. ⛔ **`--cleanup-pending`/`--cleanup-orders` DELIBERATELY UNTOUCHED — still gated, each needs its own card.** ⛔ **And this does NOT narrow the ALLOWLIST (§C.4 R12 still owed).** Below = the Step-1 measurement that earned the fix: The executable twin of #8: a doc fix cannot cure a script that still does the wrong thing on demand. ⭐ **WORST PROPERTY — it prints `Kill switch RESET to INACTIVE` while changing NOTHING on a running service** (`is_active()` reads in-memory state, `kill_switch.py:487-491`), **and `_persist_state` is `INSERT OR REPLACE` (`:898`) so the DB edit can be silently overwritten back to killed** ⇒ ineffective *and* impermanent. Also: **no `system_events` audit** · `triggered_at` left stale (**audit-integrity only** — `clear_stale_state` early-returns on INACTIVE, so no boot harm; recorded precisely, not inflated) · **HARD_KILL `--force` gate bypassed entirely** (cf. `clear_kill_switch.py:65-68`) · no `--dry-run` · no already-INACTIVE handling · **ZERO tests**. ✅ **NO automation caller** (absent from `config/cron_registry.yaml`; no hook/unit/deploy script) and ✅ **NO runtime coupling — zero importers, unreachable from `main.py`** (the fact the ride decision turns on; note it has no `__main__` guard, so an import *would* execute it — nothing imports it). ✅✅ **SECOND-COPY QUESTION CLOSED — RULED OUT (Rama's VM check, 02-Aug): `~/check_vm_state.py` DOES NOT EXIST, no stray copy outside the repo ⇒ a repo-only fix IS permanent.** ⭐ Honestly recorded: that **dissolves the stronger half** of the hold argument; the hold now rests only on *no urgency + Monday's stack is already large* — still the recommendation, **weaker than when made**, Rama's call. **RECOMMENDATION: Option A (remove `--reset`) + HOLD out of Monday's push.** ⛔ The read-only diagnostics MUST survive. ⛔⛔ **TWO FURTHER EXECUTABLE HAZARDS DISCLOSED, NOT FIXED (each needs its own card): `--cleanup-pending` (`:102-119`) mass-marks EVERY `PENDING_FILL` trade CANCELLED in the LIVE DB with NO capital release and NO audit — `PENDING_FILL` is exactly what CHECK2/CHECK6 arbitrate, so it can STRAND RESERVATIONS and collide with debt-ledger #3 (cross-referenced there); and `--cleanup-orders` (`:122-148`), same class. NEITHER has a sanctioned equivalent to redirect to.** ⭐ **GOVERNANCE FACT, registered to debt-ledger #11 + §C.4 R12, NOT to #8b: the assistant allowlist (`/permissions/allow`) pre-authorises an AI agent to run these destructive commands against the LIVE DB WITHOUT prompting (21 entries, 2 × `--reset`) — ⛔ removing the flag does NOT narrow the allowlist, and 11 of those entries are STALE (they name the path proven not to exist).** ⛔ One ledger item with #8 — **231 stands.** Record: `docs/audit/ledger8b_check_vm_state_step1_02aug2026.md`.

> 📕🛟✅ **LEDGER #8 — THE RAW-DB KILL-CLEAR IN THE RECOVERY DOCS — `<BUILT>` 02-Aug, DOCS-ONLY (0 `.py`), ⛔ NOT DEPLOYED until Monday's push.** ⭐ **The audit (IA-XDOCS-01) scoped ONE doc; a repo-wide sweep found THREE** — `docs/03_daily_operations_runbook.md:132`, **`docs/RUNBOOK.md:47`**, **`docs/disaster_recovery.md:164`** — each presenting a raw `UPDATE kill_switch_state …` + restart as *the* operator procedure. **All three corrected in ONE commit** (fixing one would not have been permanent); old text kept **struck-through-but-legible**. **THE SANCTIONED PATH, now stated in all three: LIVE/VM `sudo bash deploy/resume.sh` (`--force` for HARD_KILL) · PAPER/PC `python scripts/clear_kill_switch.py [--dry-run|--force]`** (⚠️ `resume.sh` is VM-only — systemd + `/home/ubuntu` paths — so the parity split is stated explicitly). **WHY the raw route is wrong, measured:** it has **NO effect on a running service** (`is_active()` reads in-memory state, `kill_switch.py:487-491`) · leaves `triggered_at`/`triggered_by` stale and writes **no `system_events` audit** · **bypasses the HARD_KILL `--force` gate** (an emergency kill re-trips if its cause is unfixed). ⛔⛔ **AND THE RESTART STEP IS THE REAL HAZARD: a boot with a kill still persisted returns EXIT 4 (`main.py:1925-1932`) and `RestartPreventExitStatus=3 4` (`trading-system.service:35`) means systemd will NOT bring it back.** HARD_KILL halts **always**; SOFT_KILL halts **same-day** (`utils/startup_checks.py:242-286`). ⭐ **The "Database Locked" section was the MORE dangerous half** — an **unconditional** `systemctl restart`, and after 15:15 the daily breaker SOFT_KILL is active by design, so that is the COMMON case; it now checks the kill state FIRST. ✅ A **prior-day** kill needs none of this (`clear_stale_state` auto-clears every type at 08:15). ⚠️ **NEW DISCLOSURE, reported NOT fixed (CODE, out of card scope): `scripts/check_vm_state.py --reset` (`:34-40`) does the SAME raw clear** — same three defects. ⛔ Label honesty: **a runbook has no `<VERIFIED LIVE>` until an operator uses it in a real incident.** ⛔ No new register row — **231 stands** (register §B.1 row 8 stamped).

> 📌➕ **#2c-R ADDENDUM (02-Aug, DOCS + ONE COMMENT ONLY — no behaviour change).** ⭐ **The CHECK6 relationship is now REGISTERED TO DEBT-LEDGER #3** ("fill/cancel seam truth", IA-P5-02 family): #3's scope **EXPANDED** to also own **capital-release-while-the-position-is-live** — CHECK6/FIX-B marks a `PENDING_FILL` trade FAILED + releases its reservation at 3 cycles while the position is STILL LIVE at the broker, then it routes to `_check2_orphan_adoption` and can be filed **`HUMAN_ORDER`** — the cancel-race's own endpoint reached by a **second, independent path**. This **RAISES #3's importance**; ⛔ **ordering UNCHANGED, ⛔ NOT a new row — 231 stands.** Forward pointers added to **BOTH** the #2b and #2c-R records so a future reviewer never re-derives it. 🔴 **Post-flip a CNC delivery holding SPARED by #2b follows exactly this path** — reachability **latent-on-latent** (HARD_KILL **and** an in-flight entry **and** a fill; HARD_KILL has never fired) ⇒ **documented, NOT flip-blocking.** ⛔ No CHECK6 code touched. ⭐ **AND `core/constants.py`'s COMMENT CORRECTED: it claimed FOUR readers of `EMERGENCY_FLATTEN_PRODUCTS`; there are FIVE** (kill_switch `:1585`/`:1683` · eod_squareoff `:1085`/`:1453` · order_reconciler `:2174`) — it predated the reconciler joining at #2b and so **understated the blast radius by one.** **Comment-only PROVEN, not asserted: AST-identical with docstrings stripped + every public binding equal + the frozenset value asserted `frozenset({"MIS","CO"})` + all 39 constant-reading tests green.** ⚠️ It also now records that **membership ≠ "will be sold" at site 5** (the reconciler REFUSES CO before its membership test).

> 🎯🛡️ **BUY-DAY PRODUCT FILTER (ledger #2 / Q4) — `<BUILT>` 01-Aug night `43043a2`+`2e606ec`, ⛔ NOT DEPLOYED/PUSHED.** Both HARD_KILL emergency sites (`kill_switch.py` local pass + broker sweep) filter on **`core.constants.EMERGENCY_FLATTEN_PRODUCTS` {MIS,CO} — THE single source; both scheduled EOD passes read the same name (no copy to drift)**. CNC → SPARED loud (honest attempted-count); NULL/NRML/unknown → **FLATTEN + CRITICAL** (`_alert_unknown_product`, both sites — the silent ""→INTRADAY fallback is gone). ⭐ Kite-per-product-row hazard test-pinned (a spared CNC row never shadows a same-symbol MIS row). Tests: `tests/unit/test_kill_switch_product_filter.py` + 3 superseded-contract updates (h5/BugC/flatten-chain, all superseded-but-legible). **Regression NEW-set EMPTY.** 🔴 OWED: the reconciler CHECK2 **third sell-under-kill site** (product-blind, CNC-unreachable pre-flip) — #2b Mon-eve or a named carry-pilot blocker; Mon PC kill drill pre-deploy. Record: `docs/audit/buyday_filter_build_01aug2026.md`.

> 🔬🛠️ **EFFECT-TELEMETRY (ledger #1, β/γ) — `<BUILT>` 01-Aug `132e571`, ⛔ NOT DEPLOYED/PUSHED.** NEW: `core/effect_telemetry.py` (counter handles · B2 boot assertion: **paper FAIL-FAST / live CRITICAL+continue** · EOD census). ⭐ **`config/expected_managers.yaml` = the C2/C6 SINGLE SOURCE OF TRUTH — ⛔ REVIEW RULE: a new manager ctor in main.py REQUIRES a registry entry IN THE SAME DIFF** (+ a `handle()`/`register_constructed()` call). **Census hook = `_shutdown()` entry, main.py:1236** — the 16:05 `daily_report` cron is OUT-OF-PROCESS and can never carry in-process counters. Frozen contract: `docs/audit/effect_verification_contract_01aug2026.md` · build record: `docs/audit/effect_telemetry_phaseB_build_01aug2026.md`. ✅ **B-2 `4959111`: the order_placer STOP is RESOLVED** (place() effect-point + NEW `placer.emergency_exit` dormant tripwire; registry 70, ×5 fetcher string fixed; paper boots no longer fail-fast). 🔴 **OPEN:** (a) deploy slot (Mon still boots `297b587`); (b) ⏳ **MON pre-deploy: PC-paper composition boot — B2 must PASS clean — + calm regression confirm** (3 isolation-passing flips in the known-flaky consec-losses family, record §6a); (c) **4-AUG FLIP-RIDER: the flip deploy MUST carry the `cnc_gtt_*` registry edits dormant→event-driven.**

> 📋⭐ **PENDING SOURCE-OF-TRUTH — CURRENT (01-Aug-2026): `docs/MASTER_PENDING_01-Aug-2026.md`** (`f9582e9`, local/unpushed). Consolidated WORKING register in four bands; **231 items reconciled** = 132 pre-audit (taken from the 30-Jul master's M=131 + F6, ⛔ **not** re-derived) + **95 integrity-audit finding IDs** + 4 new-this-period → **§A live-trading 6 · §B debt-ledger 96 · §C still-open 105 · §D settled 24.** ⛔ **`docs/MASTER_PENDING_28-Jul-2026.txt` + `docs/MASTER_PENDING_30-Jul-2026.md` are SUPERSEDED-as-working-register but RETAINED as the cited authorities — DO NOT DELETE; where they disagree with the 01-Aug file, THEY WIN.** ⭐ **§B is a running order, NOT an authorisation** — the fix campaign is a separate effort the audit does not authorise, and **the buy-day product filter goes FIRST, before anything holdings-aware (the Q4 ordering constraint).** 🔴 **§A0 = the ONE decision owed Rama BEFORE Mon 3-Aug's close.** ⚠️ Two corrections it records rather than smooths: the 30-Jul master's §6 count never deducts the G5/G6/G16 it closed in §3.7 (so its M=131 still contains 3 closed items — all named, nothing lost); and the audit's closing "18 commits/20 ahead" measures 19/21 because the commit that corrected that self-referential count cannot count itself. The line below is the SUPERSEDED 14-Jul pointer, kept for lineage.

> 🧾🔍 **PENDING SOURCE-OF-TRUTH (14-Jul) — 🔁 SUPERSEDED, see above:** `docs/audit/pending_reconciliation_14jul2026.md` — read-only census of the 10-Jul master list vs HEAD (Sections A–J); was then the authoritative pending register (supersedes the Downloads-only `MASTER_PENDING_TRUE_FINAL_10JUL.txt`). Counts CLOSED ~24 / OPEN ~69 / PARTIAL 7 / STALE 3 / UNKNOWN 1. Key: **M-S4 PARTIAL** (live bug persists `secondary_screener.py:407`), **Audit-B Ph9-10 never completed** (pending, not clean), **M-DP1 PARTIAL** (stale `deploy/post-receive` dup; live hook correct), **VM-security/foreign-IP-SSH item STALE** (Q8 refuted). DISCOVERED: Finding-1 sector-cap-dead (blocks D1). memory `pending_reconciliation_14jul`.

> 🧭🛠️🔒🏁 **V3 SHARED-ENGINE — STEP 10b INCREMENT 2 COMPLETE + SIDE-TASK A (12-Jul-2026, post-power-outage) — BUILT-but-UNPUSHED (same branch `v3-step10a-chain-12jul`; SHADOW / NO-ORDER).** PB-01 stateful FRONT-END. **T3** `v3_chain/pb01_entry.py::Pb01EntryStage` (09:20-11:00 daemon poll; **T0c=OhlcFetcher `fetch_interval("5minute",lookback_days=1)`** NOT CandleStore — CandleStore.volume sums cumulative volume_traded→wrong per-candle vol for G-CONFIRM; `truncate_to_asof`=**G-FORMING-CANDLE** chokepoint; gap→SKIPPED_GAP, walk closed candles first-terminal-wins, INVALIDATED on 5m close<LEVEL−hold_buffer, 11:00→EXPIRED_WINDOW; **restart-safe by RE-DERIVATION**, row status IS state, anti-rehydration loads only today). **T6** `v3_chain/pb01_runner.py::Pb01WouldBeRunner` (on_confirm→ONE would-be JSONL, **G-NO-ORDER by CONSTRUCTION**; reuses `gate_*`+`compose_score`+shared `regime_fraction`/`sr_target_fraction`; PB-01 SL=min(retest_low,LEVEL)−buf, TGT=nearest resistance→none=Kalyan G-RR skip; NO-LOOKAHEAD to conf.as_of). **Main wiring** only when `watchlist.enabled` (default-off=byte-identical); `webhook_receiver.set_eod_capture` late-bind; reverse-order shutdown. **T7** `--pb01` soak (capture OUTCOME + retest CONFIRMATION RATE + would-be verdicts; expectancy=pending outcome-replay, flagged not faked). **Side-task A:** `StrategyConfig.pipeline`(INTRADAY|DELIVERY)+`horizon`(SAME_DAY|NEXT_DAY|BTST|**SWING**) REQUIRED enums; 16 YAMLs tagged (12 INTRADAY·SAME_DAY + PB-01 INTRADAY·NEXT_DAY + **3 positional=DELIVERY·SWING — VERIFIED multi-day, NOT BTST**); `strategies/taxonomy.py`; surfaced in `strategy_status` (HTML/Telegram/plaintext Category col) + EOD `daily_report` sheet-6 (Pipeline/Horizon cols); metadata+display only, byte-identical. **ALL gates proven + full-suite stash-diff ZERO-new (41==41) + 418 pass. O4/B CONFIRMED already-satisfied.** memory `v3_step10b_increment2_complete_12jul`. ⏰ still owed: Mon SESSION-1 sanity + NIFTY probe. NEXT = Web Claude+ChatGPT review → off-market push.

> 🧭🔌🔒 **V3 SHARED-ENGINE — STEP 10b ADDENDUM · PB-01 SCANNER REGISTERED (12-Jul-2026) — fail-closed; BUILT-but-UNPUSHED (same branch `v3-step10a-chain-12jul`).** Registers the PB-01 "Breakout+Retest" Chartink scanner so Rama can save his alert. **Reusable checklist — a scanner = EXACTLY 3 config files** (grep-proven, no other per-strategy config): (1) `config/strategies/<name>.yaml` (`StrategyConfig`; loader globs `*.yaml`), (2) `config/scan_webhook_map.yaml` `scanners.<name>:{strategy,chartink_url}` (routing; `webhook_receiver` 404s unknown; `signal_processor` resolves scanner→strategy; **S10 coupling** = referenced strategy MUST have a loaded YAML else `ConfigMissingError` at boot), (3) `config/chartink_scanners.yaml` `scanners.<name>:<url>` (pre-flight connectivity, WARN-only). **+ ~7 test count-invariants** hard-assert 15 (test_config_loader/test_strategies/test_secondary_screener/test_slice2/test_fix133/test_p0_live_day1) — a 16th touches all. **PB-01 = `enabled:false`+`v3_playbook:true` → FAIL-CLOSED** (`strategy_will_trade`→WON'T/DISABLED under every trade_type×force → `_PipelineReject("STRATEGY_CONTROL")` before placement; `tests/unit/test_pb01_registration.py`). 15-live invariant preserved by filtering `v3_playbook`; **test_main stash-diff 4==4 zero-new.** **KEY:** the receiver 403s post-close EOD alerts (`webhook_receiver.py:455-457 is_entry_allowed`) → PB-01's EOD capture needs the Step-10b EOD route (TASK 0a). URL `http://161.118.187.249:5000/webhook/pb01_breakout_retest?token=<shared>`. Auth `?token=` (`require_hmac:false`), one shared secret (VM `.env` only). memory `v3_step10b_pb01_registration_12jul`. REMAINING = watchlist store/capture/entry/gates/score/chain/soak.

> 🧭🛠️🔌 **V3 SHARED-ENGINE — STEP 10a · THE CHAIN ON EXISTING SIGNALS (12-Jul-2026) — IMPLEMENTED behind default-OFF `v3_chain_mode`; STOPPED AT OFF; BUILT-but-UNPUSHED.** Ratified via V3 DECISION CONTENT SPEC v1.0 + the NO-LOOKAHEAD addendum. **TASK 0 = ASYNC** (no ZoneCache runs today → a sync S&R read would cold-miss/fail-closed; so a guarded fire-and-forget copy on the hot path → BACKGROUND worker computes the verdict; live path never slowed). NEW `v3_chain/` pkg (`models`/`truncate`/`score`/`runner`) + generic gates in EXISTING `screening/hard_gate.py` (`gate_rr`/`gate_htf`/`gate_extreme`) + `V3ChainConfig` + `system.v3_chain` block + `StrategyConfig.v3_playbook:bool=False`. **`v3_chain_mode: off|shadow` default off = BYTE-IDENTICAL** (runner not constructed → `v3_chain=None` → hot-path hook is one skipped flag check). shadow = observe → worker: fetch day/60m/30m (own OhlcFetcher over the SAME rate-limited closure) → **truncate every series to bars closed STRICTLY BEFORE signal time (NO-LOOKAHEAD)** → S&R via shared `scored_zones_from_candles` → G-RR(must-have, missing→FAIL) + G-HTF(fail-open) + G-EXTREME(fail-open) → 3-layer COMPOSER (Context 40 + Execution 20; Playbook null not renormalized; confluence MEAN so correlated factors can't double-count) → would-be JSONL `data_store/v3/would_be.jsonl` (V3-vs-LIVE SL/TGT/RR + gates + score + sr_sync_hit + as-of timestamps). **LOG-ONLY — never rejects/delays/alters a live order.** Soak `scripts/v3_shadow_soak_report.py --v3` = the G-KALYAN join (would-be R:R × REAL P&L). Proofs: G-OFF byte-identical (test_signal_processor 77 unchanged + test_main 4==4 stash-diff), G-NO-INTERFERENCE (raising shadow chain → live unchanged), G-CONFLUENCE + anti-lookahead V2 tests, RAMCOIND 264 + full 4516 collect clean. Tests `tests/unit/test_v3_chain.py`(20). memory `v3_step10a_chain_impl_12jul`. NEXT = Web Claude+ChatGPT review → off-market push → shadow soak → Step 10b (PB-01 + playbook gates + Playbook layer + overnight watchlist).

> 🧭📐🔌 **V3 SHARED-ENGINE — STEP 10 · PIPELINE PLUMBING (12-Jul-2026) — PLAN ONLY (no code/config/VM; deploy ledger UNCHANGED).** Design doc `docs/v3/V3_STEP10_PIPELINE_PLUMBING_PLAN.md` (TASK-0 inventory + P1-P10, for Web Claude + ChatGPT + Rama). How a signal flows END-TO-END through the V3 chain (regime→S&R→hard gates→score→allocator→sizing→would-be entry) in **SHADOW** (a would-be trade, NO order), threaded through the EXISTING pipeline via ONE flag-selected seam (R2 — no parallel processor). **TASK-0 HEADLINE:** Steps 1-9 built every module as an INDEPENDENT LOCAL shadow; **the CHAIN is missing** — no playbook registry/PB-01 (no `v3_playbook` field on `StrategyConfig`), the playbook-scope gates (confirmation/pullback/S&R-R:R/strong-HTF/extreme) are docstring stubs with **0 code** (`hard_gate.py:6-8`), the **3-layer score (Playbook 40/Context 40/Execution 20) is NOT built** (only the 8-step Execution seed `rescaled_total`), **regime + S&R produce but NOTHING consumes** (`runner.latest` unread; S&R observer-only), the **allocator scopes to an empty V3 set** (`v3_scope_fn`→always False, "none exist yet"), and the **overnight watchlist is absent** (`EntryGate` clears at EOD, FIX-046). **BUILT + ready:** the seam (`_process_one` sizing→allocator-hook `signal_processor.py:924-965`), the candidate primitive (`_build_candidate`→`ScoredCandidate`), sizing (`sl_price` seam), entry/mgmt/exit. **SEAM = V3 branch INSIDE `_process_one`** = additive enrichment + flag-gated STOP-and-emit would-be JSONL (`data_store/v3/would_be_trades.jsonl`, NO schema change); reuses screen/derive/size verbatim; NOT a parallel processor. **NEW default-OFF flags (byte-identical):** `v3_playbook: bool=False`, master `v3_chain_mode: off|shadow`, `watchlist.enabled: false`. PB-01 rides INTRADAY (delivery stays OFF; overnight = analysis watchlist, no CNC/GTT). Its promotion evidence is its OWN would-be track record (via `shadow_tracker`), not OLD-vs-NEW (no live twin). **SEQ:** substrate→wiring→3-layer score→playbook gates→would-be record→watchlist→allocator-scope→soak (steps 2-5 provable on existing signals first). memory `v3_step10_pipeline_plumbing_plan_12jul`. NEXT = Web Claude + ChatGPT + Rama review → separate build instruction.

> 🧭🏁🔒 **V3 SHARED-ENGINE — STEP 9 · 03.09 Exit Engine (12-Jul-2026) — VERIFY, ZERO code change (LIVE exits BYTE-IDENTICAL by construction — 0 exit-path files touched). ★ SHARED ENGINE 03.01–03.09 COMPLETE.** Gap-map+delivery-ledger `docs/v3/V3_STEP9_EXIT_ENGINE_GAPMAP.md`. Ratified: 15:15=entry cutoff ONLY (no flatten), 15:17=EOD squareoff MIS/CO. **T1 all PRESENT:** terminal exit close_trade(dbl-close)→`_cancel_oco_siblings`(`order_placer:4346`); **CNC EXEMPT from 15:17** (`eod_squareoff:1023` `p.product in ("MIS","CO")` FIX-015 +`:1390`+EOD6; test `test_open_delivery_positions_not_touched`); SYSTEM_OVERSELL `_detect/_flatten`(`reconciler:1557/1604`)→CHECK2 L3→CRITICAL+flatten; scheduled auto-clears / **HARD never auto-clears** (`kill_switch:250/256`) + `clear_stale_state`(`:199`); HARD cancel-FIRST `_cancel_trade_resting_exits`(`:971` FIX-190)→marketable-LIMIT flatten. **T2 DELIVERY-EXEMPTION LEDGER (Slice 2.5 input):** EXISTS(protect carried CNC)=15:17 filter+reconciler delivery-exclusion(`:785-797`)+FIX-183 GTT adoption+FIX-008 bootstrap-WARN+**CncGttMonitor WIRED**(`main:2338`, P1+P2 pass)+GTT/holdings ungated; PARKED(active delivery)=CNC ENTRY off+T2-canary DDPI-overnight UNPROVEN(`854112b`)+delivery sizing knobs(Step-5)+next-day proof. **Protective EXISTS+tested; active lifecycle PARKED.** **G1** byte-identical; **G2 RAMCOIND 241 pass**; **G3** CNC-exempt test-enforced; **G4** zero-new. paper==live. memory `v3_step9_exit_engine_12jul`. NEXT = review → next phase (intraday pipeline plumbing → stateful watchlist + next-morning entry → PB-01 shadow).

> 🧭🔒⚖️ **V3 SHARED-ENGINE — STEP 8 · 03.08 Trade Management (12-Jul-2026) — VERIFY, ZERO code change (LIVE trade-mgmt BYTE-IDENTICAL by construction — 0 `orders/*.py` touched):** 03.08 COMPLETE + RAMCOIND-battle-tested. Gap-map+T1 `docs/v3/V3_STEP8_TRADE_MGMT_GAPMAP.md`. **T1 SINGLE-SL-OWNER (headline):** protocol-partitioned at registration — CO_PLUS_TGT→SmartTgt (`order_placer:2095`, `smart_tgt.enabled`), LIMIT_TRIPLE+`trailing_sl_enabled`→Breakeven (`:2067`), LIMIT_TRIPLE+`structure_exit_enabled`→StructureExit (`main.py:2789`). **CO vs LIMIT_TRIPLE AIRTIGHT BY CONSTRUCTION** → **LIVE SL owner = SmartTgt ONLY** (all live intraday = CO_PLUS_TGT; LIMIT_TRIPLE strategies = DELIVERY-dormant). **Non-airtight seam = breakeven∩structure on same LIMIT_TRIPLE trade = CONVENTION (`main.py:2788`) not construction; TRIPLY-INERT today** (structure_exit false·0 trailing_sl_enabled·LIMIT_TRIPLE dormant) → **FLAGGED (T4-1), not fixed** (rec: boot fail-fast/auditor rule rejecting the co-enable). **T2:** all managers **modify-in-place** (`modify_order`, no cancel-replace → no 2-SL/0-SL) + **MONOTONIC** (SmartTgt double-guard `:566/568`; Structure only-tighten `:23`; Breakeven forward-only-once); ghost-SL-safe; modify-fail→old-SL-stands+CRITICAL@3(FIX-142); partial→`filled_qty`. **G1** byte-identical; **G2 RAMCOIND 154 pass** (4-layer intact); **G3 monotonic** test-enforced; **G4** zero-new. paper==live. memory `v3_step8_trade_mgmt_12jul`. NEXT = review → Step 9 (03.09 Exit).

> 🧭✅🔒 **V3 SHARED-ENGINE — STEP 7 · 03.07 Entry Engine (12-Jul-2026) — VERIFY, ZERO code change (LIVE entry BYTE-IDENTICAL by construction — 0 `orders/*.py` touched):** 03.07 is COMPLETE + RAMCOIND-battle-tested. Gap-map `docs/v3/V3_STEP7_ENTRY_ENGINE_GAPMAP.md` (all requirements PRESENT, cited): bounded LIMIT entry (`order_protocol_limit`; emergency=marketable-LIMIT capped 1%; SL=stop-limit P0); slippage `_compute_slippage_tolerance` min(SL_dist×0.22,Rs5)+band+override-hierarchy Symbol>Strategy>Band>Global + FIX-128 1% abort; TGT-recalc-from-fill (FIX-013) SL-signal-fixed; software OCO 1SL+1TGT (`close_trade` dbl-close-guard→`_cancel_oco_siblings`; tgt_retry never a 2nd); naked→`SLUnplaceableError`→`_emergency_market_exit`(FIX-148)→hard_kill; A-2 timeout→UNKNOWN_IN_FLIGHT no-retry→reconciler(authoritative broker book); partial→`filled_qty`; MIS+CNC-GTT(OFF); `clamp_exit_into_band`. **T2 SL seam = the existing `sl_price` param** (order path derives NO SL) — V3 03.03 `computed_sl` feeds it at the signal_processor seam on the V3 path LATER (no order-path change; LIVE SL stays FIXED_PCT). **T4 flagged (NOT changed):** ATR-into-LIVE-SL (Step-5 carry, LARGE money-path blast radius, belongs to the V3 playbook step). **G1** byte-identical; **G2 RAMCOIND MANDATORY 209 pass** (dup-exit+oversell+order_placer+tgt_retry+fix190+fix148; 4-layer intact); **G3** zero new. paper==live. memory `v3_step7_entry_engine_12jul`. NEXT = review → Step 8 (03.08 Trade Management — highest-freq SL modifier, RAMCOIND-critical).

> 🧭🛠️🧮 **V3 SHARED-ENGINE — STEP 6b · 03.05 Portfolio Allocator IMPLEMENTED (12-Jul-2026) behind default-OFF flag (built-but-unpushed; STOPPED AT OFF; OFF = FCFS BYTE-IDENTICAL):** applies the plan (A1–A8). NEW `PortfolioAllocatorConfig` (`config_loader` extra=forbid) + `system.portfolio_allocator` + `system_config.yaml`; **`allocator_mode: off|shadow|enforce` default `"off"`**, all knobs INERT (`enforce_scope v3_only`, `max_portfolio_deployment_pct null`, `long_short_skew_max null`, wall-clock window 60/2s). **One-seam ADMIT extraction:** `_process_one`'s ADMIT+place block → `_admit_and_place(self, ac, *, …)` (SINGLE callable for the fused path AND the enforce worker — R2 no fork) on a mutable `_AdmitCtx` (reservation_id/requeued/in_flight_incremented/placed) so the outer except/finally see the lifecycle on every path (incl. queue.Full leak-guard). Routing hook `if self._allocator is not None:` — OFF (allocator None) skips it → FCFS byte-identical; shadow=observe(copy)+fall-through; enforce+in-scope=submit+handed_off (worker owns the claim); enforce-out-of-scope=fall-through. NEW `allocation/` pkg (models/ranker[A7]/window_buffer/portfolio_allocator: mode/observe/submit, wall-clock window worker, `_simulate` greedy w/ inert concentration+skew+one-per-symbol, `_compute_regret` 3 metrics, enforce `_run_enforce_batch` reusing `admit_prepared`). signal_processor+= set_allocator/`_build_candidate`/`admit_prepared`/`reject_prepared`. main.py builds allocator ONLY when mode≠off. **gate-8 sector-TOCTOU NOT touched (separate task).** G-OFF: test_signal_processor 77 UNCHANGED + shadow-admits-identically + full-suite stash-diff zero-new. G-SHADOW-READY: regret computed, reserve/place NEVER called in shadow. G-REG: 308 pass; tests `test_portfolio_allocator.py`(25). paper==live. **STOPPED AT OFF.** memory `v3_step6b_portfolio_allocator_impl_12jul`. NEXT (Rama/off-market after review): push → off→shadow soak → enforce → remove FCFS. Next module = Step 7 (03.07 Entry).

> 🧭📐🧮 **V3 SHARED-ENGINE — STEP 6 · 03.05 Portfolio Allocator (12-Jul-2026) — PLAN ONLY (no code, no config, no behaviour change; ledger untouched):** design doc `docs/v3/V3_STEP6_PORTFOLIO_ALLOCATOR_PLAN.md` (P1-P10, for Web Claude + ChatGPT + Rama). The biggest NEW build — replace **FCFS/concurrent admission** (`signal_processor.py:319-341` dispatcher → 5-worker pool; each signal runs the whole pipeline independently; NO ranking; `:439` "Priority rank #1 of 1" = placeholder) with **batch-over-a-short-candle-window → rank by score → greedy admit within portfolio rules → atomic reserve via EXISTING primitives**. **Single seam** = the PREPARE→ADMIT boundary in `_process_one` (extract the `portfolio_lock` ADMIT block `:919-960` so both the fused OFF path and a batch worker call it). **SITS ON, reimplements none:** `fm.reserve`/`portfolio_lock`/`count_live_reservations`/`risk.approve`(10 gates)/`_enforce_strategy_position_cap`. **`allocator_mode: off|shadow|enforce` default `off` = BYTE-IDENTICAL.** shadow = non-blocking observer logs would-be ranked set + REGRET vs live FCFS (no reserve/place; counterfactual directional-approximate). enforce = 1 admission worker ranks `(score DESC,tier,triggered_at,signal_id)` + greedy-admits under ONE portfolio_lock. **NEW shared concentration cap** (portfolio-wide deployed-margin %, default INERT) COMPOSES with the existing per-sector 0.40 (no gate-8 rewrite). Idempotency: v1 admit-or-reject WITHIN the window (claim key = `signal_id` PK + receiver's `_claim_in_flight` symbol claim; `trades.signal_id` NOT DB-unique → behavioral). No-late-entry via window≪60s expiry + FIX-067 fresh-LTP re-anchor. Migration: SHADOW all-scanners regret; ENFORCE v1 = V3-path only (scanners keep FCFS as prior exposure). Cutover: off-market flip first → then remove fused FCFS (R5). memory `v3_step6_portfolio_allocator_plan_12jul`. NEXT = review → Step-6b implements.

> 🧭🔎💰 **V3 SHARED-ENGINE — STEP 5 · 03.06 Risk & Position-Sizing (12-Jul-2026) VERIFY + inert scaffold (built-but-unpushed; LIVE sizing BYTE-IDENTICAL):** 03.06 essentially COMPLETE in the existing `position_sizer`/`risk_engine`/`fund_manager` (gap-map `docs/v3/V3_STEP5_RISK_SIZING_GAPMAP.md`). SL-distance seam = the sizer's `sl_price` PARAM (V3 03.03 S&R SL feeds it LATER, no sizer change; LIVE SL still FIXED_PCT). ONLY gap closed = **inert delivery SIZING scaffold**: optional `position_sizing.delivery_risk_per_trade_pct` + `delivery_max_position_value_pct` (default None → global; used only `bucket=="positional"` which never happens live under force_intraday_only → **byte-identical**); reuses the sizer. Delivery NOT activated. **T5 flagged for ChatGPT (NOT changed): (a-i) kill_switch=None fail-open → rec boot-time fail-fast in main.py; (a-ii) sector-UNKNOWN fail-open → keep (conservative bucket); (b) ATR→LIVE-SL → defer to V3 shadow path + own parity proof (changes live sizes).** G1 byte-identical ✓ (`test_position_sizer_delivery_scaffold.py` 4 pass); G2 test_main stash-diff 26==26 → zero new. memory `v3_step5_risk_sizing_12jul`. NEXT = Step 6 (03.05 Portfolio Allocator, NEW ranked-admission core).

> 🧭🛠️⚖️ **V3 SHARED-ENGINE — STEP 4b · 03.03 Hard-Gate + 03.04 Scorer re-scale IMPLEMENTED (11-Jul-2026) behind default-OFF flag (built-but-unpushed; STOPPED AT OFF):** NEW `screening/hard_gate.py::HardGate` (at-circuit + circuit-proximity[RELOCATED shared fn; `secondary_screener._circuit_proximity_reason` now delegates] + freshness 60s; liquidity NO-OP) invoked in `secondary_screener.screen()`. **Flag `scoring.v3_hardgate_mode: off|shadow|enforce` — default `"off"` = BYTE-IDENTICAL 10-step path** (quoted: YAML reads bare `off` as False). Re-scale THRESHOLD-ONLY: `step_executor` gains `exclude_steps` (8 steps Σ80 in v3); shared `rescaled_total`/`tier_for`/`rescale_min_score` in hard_gate.py (scorer UNCHANGED); v3 thresholds are SEPARATE keys `v3_min_pass 50 / v3_medium 56 / v3_high 75` (OLD 60/80/65 untouched → reports/GUI/tests unaffected). A7: all 15 `min_score`=0 (re-scale inert). A9: high>med>min invariant in `config_loader.ScoringConfig` + `config_auditor` B5. **Offline parity tool `scripts/v3_hardgate_parity_recompute.py`** (recompute from `screener_results.step_results_json` → UNCHANGED/FLIP/TIER_SHIFT/NOW_GATED + explained-residual [age0.5/at-circuit] + data-fit) — synthetic-tested zero-unexplained; **BINDING real-data run PENDING on a VM backup** (0 local rows) = the sign-off artifact before shadow/enforce. G1 off byte-identical ✓; G3 full-suite 4459 pass/41 pre-existing fail, test_main stash-diff 26==26 → zero new; tests `test_hard_gate.py`(18)+`test_v3_hardgate_parity.py`(6). paper==live. **STOPPED AT OFF — no live behaviour changed.** memory `v3_step4b_hardgate_scorer_impl_11jul`. **G2 BINDING PARITY DONE (12-Jul, PARITY_OK):** `scripts/v3_hardgate_parity_recompute.py` over 114,807 real rows (VM backup `trading_system-2026-07-11.db`, read-only slim extract) → @50/56/75 UNCHANGED 114,693 (99.90%) / FLIP_PASS 104 (age-band) / FLIP_FAIL 10 (rounding-edge) / 0 UNEXPLAINED; harness 0 mismatch. Artifact `docs/v3/V3_STEP4B_PARITY_ARTIFACT.md`. 2 tool bugs fixed (col `step_results`; recompute OLD at current 60/80/65 — 55-era history confounded stored status). Flag STILL off. memory `v3_step4b_parity_artifact_12jul`. NEXT (Rama/off-market after ChatGPT sign-off): push → shadow → enforce.

> 🧭📐 **V3 SHARED-ENGINE — STEP 4 · 03.03 Hard-Gate + 03.04 Scorer re-scale (11-Jul-2026) — PLAN ONLY (no code, no config, no behaviour change):** design doc `docs/v3/V3_STEP4_HARDGATE_SCORER_PLAN.md` (P1-P8, for Web Claude + ChatGPT). Extract `circuit_check`+`signal_age` (step_executor steps 9&10, weight 10 each) into a NEW pre-scoring `screening/hard_gate.py` at the SINGLE live seam `secondary_screener.screen()` (only caller `signal_processor.py:778`); step_executor → 8 steps (Σ80). **Re-scale is THRESHOLD-ONLY** — re-weighting to Σ100 is scale-invariant (no-op) under the proportional scorer; recalibrate min_pass 60→50, medium 65→56, high 80→75 (age≤30s analytic ref, MUST be data-fit). **Exact parity impossible** (signal_age was folded into the pass decision) → residual quantified by an OFFLINE recompute from the persisted `screener_results.step_results_json` (no live run) + live shadow-compare. Whole change behind default-OFF `scoring.v3_hardgate_mode: off|shadow|enforce` (off = byte-identical live path). Ledger UNCHANGED (nothing built). memory `v3_step4_hardgate_scorer_plan_11jul`. NEXT = ChatGPT/Web Claude review → Step-4b implements.

> 🧭🛠️🧠 **V3 SHARED-ENGINE — STEP 3 · 03.02 Market Regime (11-Jul-2026) BUILT (NEW, SHADOW, gates nothing, default-OFF, built-but-unpushed):** pure new `regime/` package — index-level 3-axis regime (direction BULL/BEAR/SIDEWAYS · volatility HIGH/NORMAL/LOW · day-type TREND/RANGE/UNDETERMINED), each with ordinal confidence → preference_multiplier (HIGH 1.0/MED 0.5/LOW 0.2/TRANSITION 0.0), + positive-confirmation-only `extreme_flag` (emitted+logged, NOT wired to stop trading). New pure `core.candle_math.ema/adx` (substrate). **TASK 0:** NIFTY is NOT in the instrument cache → reused the OHLC fetch via new `OhlcFetcher.fetch_by_token` + config `regime.index_token` 256265 (⚠️ VM must verify live index historical access before enabling). **FAIL-SAFE: missing/broken data → status UNKNOWN + neutral multiplier 0, NEVER halts the book.** `MarketRegimeShadowRunner` computes/logs/persists once per cycle; `RegimeConfig` (config_loader + system_config.yaml `regime:` block, `enabled: false`); main.py builds+starts only when enabled (dormant, inert). R4: test_main clean-vs-dirty IDENTICAL (26==26) → zero new fails; 251 changed-area pass; 13 spec tests `test_market_regime.py`. Paper==live (pure). memory `v3_step3_market_regime_11jul`. NEXT = Step 4 (03.03 Hard-Gate extraction + 03.04 scorer re-scale, COUPLED) after review.

> 🧭🛠️🔍 **V3 SHARED-ENGINE — STEP 2 · 03.01 S&R Detection (11-Jul-2026) BUILT (SHADOW, default-OFF, built-but-unpushed):** ADAPT of `sr_detector/` (no parallel module). **Task 1** VWAP+ORB anchors — `sr_detector/zone_builder.py::intraday_anchors` (VWAP via new `core.candle_math.session_vwap`; ORB max/min over the config window) + `build_anchor_payload` (merges existing PDH/PDL/PDC + round); today-only fine fetch via new `OhlcFetcher.fetch_interval` (`fetch_timeframes` untouched); session bounds INJECTED from `MarketWindows` (main.py). **Task 2** `models.TF_ROLE` labels swings 30m=PRIMARY/1h=MAJOR (`build_swings_payload`). **Task 3** `SRAnalysis.confidence_class` default `ANCHOR_ONLY` (never flips this step; drives the later 03.03 R:R gate). **Task 4** READ-ONLY `scripts/sr_level_export.py` → flat CSV of anchors+swings per symbol×date for Rama's manual-marking validation. ONE default-OFF gate `sr_detector.intraday_anchors_enabled` (config_loader + system_config.yaml) → live shadow row BYTE-IDENTICAL; payload folds into the existing `confluence_evidence` JSON (**NO schema change**). **Gates NO capital — still shadow/observer-only.** R4: 64 sr_detector tests UNCHANGED; the 41 full-suite fails = pre-existing PC-env baseline (stash-diff PROVEN my change adds ZERO); 148 changed-area pass. Paper==live. Tests `test_sr_anchors.py`(12)+`test_sr_level_export.py`(4). memory `v3_step2_sr_detection_11jul`. NEXT = Step 3 (03.02 Market Regime, NEW) after review.

> 🧭🛠️ **V3 SHARED-ENGINE — STEP 1 Common Utilities (11-Jul-2026) BUILT (unwired, built-but-unpushed):** two NEW pure helpers in **`core/candle_math.py`** — `true_range_series`/`atr` (Wilder default + SMA; `period+1` data req, insufficient→None) and `resample(base_candles, target_tf, session_bounds)`→higher TF (int min | "day"; O=first/H=max/L=min/C=last/V=sum, ts=bucket start; session-open aligned via INJECTED bounds, no cross-day merge, pre/post-hours excluded; `ResampleResult` flags empty gaps + partial session-tail bars). REUSES the shared `sr_detector.models.Candle` (no parallel type); `resample` uses a **function-local Candle import** so `core.candle_math` has NO import-time dep on `sr_detector` (layer inversion avoided; verified). **NOT wired into any trading path; NO existing file modified; NO schema/cron/flags; paper==live.** Tests `tests/unit/test_candle_math.py` (26 pass); sr_detector regression 64 pass. Substrate catalog in `docs/SYSTEM_MAP.md`. memory `v3_step1_common_utils_11jul`. NEXT = Step 2 (03.01 S&R) after review.

> 🧭 **V3 SHARED-ENGINE — PHASE 0 (11-Jul-2026, READ-ONLY) COMPLETE:** source-verified gap analysis (Common Utils + 03.01–03.09) vs `main`@`8116b74` → `docs/v3/V3_PHASE0_INVESTIGATION_GAP_ANALYSIS.md` (deliverables A–F; self-contained for Web Claude + ChatGPT). **NEW builds:** 03.02 Market Regime (nothing exists), 03.05 Portfolio Allocator core (admission is FCFS — NO ranking; `max_concentration_pct` is per-trade not shared), ATR/true-range helper (unimplemented), timeframe-resample util, optional 15:15 hard-flatten (today 15:15=entry-cutoff only). **ADAPT-by-extraction:** 03.03 Hard Gate (circuit+signal_age entangled in `secondary_screener.py`/`step_executor.py` steps 9-10 + `_circuit_proximity_reason`). **ADAPT:** 03.01 S&R (`sr_detector/` shadow/observer-only), 03.04 Scoring (`quality_scorer.py`+`scoring_weights.yaml`, re-scale + move circuit/age to gates), 03.06 Risk&Sizing, 03.07/08/09 Entry/TradeMgmt/Exit. Delivery CONFIRMED OFF (force_intraday_only true / delivery_enabled false / 12 WILL·3 WON'T). NO code/commit/VM change this phase. memory `v3_phase0_investigation_11jul`. **NEXT = Step 1 Common Utilities after review.**

> 🖥️ **GUI Project (Frontend) — G0 investigation + G2a Dashboard (M1) BUILT (02/03-Jul):** G0 report `docs/gui_project/G0_BACKEND_INVESTIGATION_REPORT.md` (8 sections, cited). **DEPLOYED 03-Jul (merged to main `7b1c92d`; VM unit `gui-dashboard` ACTIVE @ 127.0.0.1:8500, Tailscale pending). G2a:** folder **`ops_dashboard/`** — isolated, read-only Flask+Waitress GUI, binds **127.0.0.1:8500** loopback only, own git-ignored `.venv` (NO kiteconnect). Key paths: app `ops_dashboard/backend/app.py` (`python -m backend.app`), config `ops_dashboard/backend/config/gui_config.yaml` (DB/logs/reports/config abs paths, port, auth hash+TOTP, unit list), DB touchpoint `ops_dashboard/backend/readers/db_reader.py` (mode=ro), auth `ops_dashboard/backend/auth.py` (`--setup`), capacity inventory `ops_dashboard/docs/G2a_capacity_inventory.md` (30 limit-keys), tests `ops_dashboard/tests/` (102 pass, v41+v42). APIs `/api/{dashboard,pipeline,capacity,strategy_panel}` (all login_required). Isolation I1-I7 test-gated. G0 headlines still stand: :8080 UNAUTH, no TLS, control plane ≈ clear-kill-switch + restart only, no push (poll), live LTP needs own broker session, 18:00-08:00 lock = copy-gate only. **G2b-1 (branch `gui-g2b1-03jul`): Strategy Control Tower (`/api/strategies`, 7 groups + 4 ranking views) + M2-M5 screens + capacity 6-group completion. Attribution doc `ops_dashboard/docs/G2b1_strategy_attribution.md` (R1-R6 approved permanent).** **G2b-2 (branch `gui-g2b2-03jul`, NOT merged): READ-ONLY DASHBOARD COMPLETE — scorecard (`backend/services/strategy_score.py`, thresholds in `gui_config.yaml scorecard:/silence:`) + silence tiers + mini-funnel + M7-M21 (risk/capital/exposure/pnl/services/vm/logs/audit/alerts/slippage/execution/statistics/reports/config/controls-stub; log tail hardened `backend/readers/log_reader.py`; config drift `backend/services/config_view.py`). Reports download gated OFF (Q3 pending). **G2b-3 (branch `gui-g2b3-03jul`): UI polish A1-A9 — hero KPI row + sparkline, icon sidebar (inline SVG), mode/kill chips (blink=HARD_KILL only), tokenized palette (0 rgba literals), mono numerals, sev-chips, closed-trades table (`/api/pnl.closed_trades` additive), V4 CDN grep gate. 238 tests v41+v42.** Run: `cd ops_dashboard && .venv/Scripts/python -m backend.app` → 127.0.0.1:8500. **G2c: DEPLOYED 03-Jul ~17:25 IST (ordering (a) executed — A-2/B-1 batch `a9d44e0` first, then GUI `--no-ff` → main `7b1c92d`, tip `33e9223`; VM unit `gui-dashboard` enabled+ACTIVE, smoke 302→/login + API-noauth 401, venv I4 PASS, overlay scp'd+600 §2b 3+5; E3 Tailscale LIVE + E4 reboot drill CLEAN PASS (03-Jul ~22:21, off-market); REMAINING: soak S1–S8 from Mon 06-Jul).** VM facts (STEP 0): 11Gi RAM / 2 vCPU / 83G free; `:111 rpcbind` pre-existing finding (→VM-hardening). VM deploy paths (post-push): venv `ops_dashboard/venv/`, prod overlay `ops_dashboard/backend/config/gui_config.local.yaml` (git-ignored, chmod 600, deep-merged — survives checkout -f), unit `ops_dashboard/deployment/gui-dashboard.service` → `/etc/systemd/system/gui-dashboard.service` (manual install), runbook `ops_dashboard/deployment/INSTALL.md`, access = Tailscale serve (tailnet-only, NOT funnel) → **https://trading-system.tail1cdc6d.ts.net** (VM host + TS machine `trading-system`, tailnet `tail1cdc6d.ts.net`, IP `100.74.84.44`; loopback:8500 behind tailscaled TLS; **CLI note:** working form is `tailscale serve --bg http://127.0.0.1:8500` — old positional `serve --bg https / <target>` REMOVED). memory `gui_g0_backend_investigation_02jul` + `gui_g2a_dashboard_03jul` + `gui_g2b1_tower_trading_capacity_03jul` + `gui_g2b2_complete_03jul` + `gui_g2b3_ui_polish_03jul` + `gui_g2c_deploy_prep_03jul`. **03-Jul readiness gate + G3 fact-base:** verdict `ops_dashboard/docs/G2_READINESS_VERDICT.md` (READY + display-only confirmed; 240 tests; POST=/login+/logout only) · control-plane facts `ops_dashboard/docs/G3_0_CONTROL_PLANE_INVESTIGATION.md` (kill-switch = in-memory sole authority, DB row boot-read-only; no hot reload; Q7 options table — Web Claude designs G3 from this) · soak evidence template `ops_dashboard/docs/SOAK_EVIDENCE_TEMPLATE.md` (S1–S8 day-tables + locked YES/NO verdict, F10+ gap numbering) · credentials = PC-created overlay → scp transfer (INSTALL.md §2a/2b). **G5 REDESIGN Phase A (04-Jul, READ-ONLY mapping) COMPLETE:** report `ops_dashboard/docs/G5_REDESIGN_PHASE_A.md` (22 specs from PC `gui/*.txt` vs live code; 5 sections + exec summary, cited). Target menu 5 groups (Dashboard·Trading·Analytics·Operations·Investigation; Recent Events+Alerts MERGED into screens). Headline: ~9 reuse-as-is / ~4 new-frontend / ~9 new-backend; NO new DB schema for any buildable screen. Walls: Controls=architecture (needs G3 control-plane, breaks read-only) · Positions/Holdings live-broker LTP/MTM (G4) · P1 `eod_broker_reconciliation` unpushed. NONE forced-rename (Capital&Risk spec-07 missing from target menu = placement flag; Reports has no home). Q3/XLSX = ~20 screens want filtered export (gate OFF pending). Soak stays isolated on `gui-g5-*`. memory `gui_g5_phaseA_04jul`. Git HELD (no branch/commit/push). **G5 Phase B (DESIGN) COMPLETE:** `ops_dashboard/docs/G5_REDESIGN_PHASE_B.md` (sections A–K; 22 screens + component/API/reader matrices + G5a-e rollout + itemized backlog). Bound by 10 LOCKED decisions. **Additive-only invariant** (equality-pinned contracts — rankings/6-units/13-stages/8-rows/6-groups — untouched; new data → NEW fields like `rankings_extra`). 13 shared components (9 new); API = 5 preserve / 12 extend-additive / 6-7 NEW endpoints (`/api/{scanner-attribution,trades,trade-story/<id>,analytics/pnl,activity}`); **NO new schema** (new readers `scanner_attribution`/`trade_story`/`pnl_analytics`/`activity_feed` all map to existing tables). Capital&Risk→Operations (composes /risk+/capital+/exposure+/capacity); Reports endpoint UNTOUCHED (L6); single System Score=screener_results.score (L8); Controls = read-only summary (A) + G3 placeholder no-writes (B, L4); Positions/Holdings two-state (L9). Rama gates: `table_export_enabled` DEFAULT OFF (Q3) + schema-pause P-1..P-4 (proposals only). memory `gui_g5_phaseB_04jul`. **G5a (Phase C · shared foundation) BUILT 04-Jul (frontend-only + 1 additive context-processor; NO schema/endpoint/reader/service change; 279 tests green v41+v42; base `80be23a`; Git HELD, branch `gui-g5a-04jul` when Rama commits):** 5-group nav (Dashboard landing · Trading · Analytics · Operations · Investigation) in `base.html`; not-yet-built screens = dimmed `nv-soon` placeholders (no href → no 404); non-menu screens (risk/capital/exposure/capacity/pnl/services/vm/statistics/reports/alerts) dropped off sidebar but routes+endpoints UNTOUCHED (still 200 direct-URL). **13 shared components** — macros `ops_dashboard/frontend/templates/components.html` (data_table/filter_bar/kpi_row+kpi_card/period_selector/events_alerts_panel/two_state_panel/export_button/score_chip/medal/status_chip/funnel_pipeline_card/lifecycle_timeline), Alpine mixins `ops_dashboard/frontend/static/components.js` (tableMixin/filterMixin/periodMixin, loaded sync before Alpine), styles in `style.css` `/* == G5a component framework == */`, contracts `ops_dashboard/docs/COMPONENTS.md`. `gui_flags` (table_export_enabled/reports_download_enabled, DEFAULT OFF) injected via `app.py` context processor → ExportButton gated OFF (Q3). AlgoCore rebrand = LABEL layer only (base/login/dashboard titles; internal name `ops_dashboard` unchanged). Additive-only invariant HELD (new data→new fields; 6/13/8/6/4 unmoved). Tests: `tests/test_nav.py` + `tests/test_g5a_components.py`. Docs: `ops_dashboard/docs/G5_REDESIGN_PHASE_{A,B}.md`. memory `gui_g5a_foundation_04jul`. **G5b (7 reuse screens) BUILT 04-Jul:** Configuration/System-Logs/Audit/Slippage/Strategies/Dashboard converted to G5a components + NEW consolidated **Capital & Risk** (`capital_risk.html`, route `/capital-risk` in `app.py:_PAGES`, nav link; composes PRESERVED /api/risk+capital+exposure+capacity into 4 zones; MTM=`two_state_panel` "Pending Broker Source (G4)"; old /risk /capital /exposure /capacity still 200 by direct URL). **Additive backend (read-only, NO schema):** `db_reader.scanner_for_trades`(shared scanner→trade join)/`strategy_sltgt_hits`/`exposure_by_direction`/`profit_factor_today`/`slippage_trend_today`; `/api/slippage`+=per_scanner/per_symbol/price_buckets/trend, `/api/exposure`+=by_direction, `/api/dashboard`.counters+=profit_factor, `strategy_tower`+=family/sl_tgt_hits/performance.profit_factor. api/ touched = analytics.py+risk_capital.py ONLY (0 endpoint removed/renamed, 0 schema, 0 renames/deletes; Reports untouched). conftest.py +2 YESTERDAY-dated linking signals (scanner-join fixture; invisible to today-scoped counts). **306 tests green** (v41+v42); pinned 6/13/8/6/4 unmoved; ExportButton OFF; honest G-gaps (G-1 MTM, G-5 author/user, resolution). tests `test_g5b_reuse.py`. base `80be23a`; Git HELD (branch `gui-g5b-04jul`). memory `gui_g5b_reuse_screens_04jul`. **G5c (5 NEW analytics screens) BUILT 04-Jul:** Strategy Ranking (`/strategy-ranking`), Strategy Health (`/strategy-health`), Scanner Attribution (`/scanner-attribution`), Trade Explorer (`/trades`), P&L Analytics (`/pnl-analytics`) — routes in `app.py:_PAGES`, 5 Analytics nav placeholders now live links. **Multi-period layer** `freshness.resolve_period` (today/week=trailing-7d/month=trailing-30d/custom, IST). **6 NEW read-only endpoints** in `backend/api/analytics2.py` (blueprint registered): `/api/{strategy-ranking,strategy-health,scanner-attribution,trades,trade-story/<id>,analytics/pnl}` — all `?period&from&to`, distinct from today-scoped `/api/pnl` (UNTOUCHED). Service `backend/services/analytics_period.py` (documented formulas: expectancy, profit_factor, **opportunity_quality**=100×accept_rate×trade_conv×win_rate, health_state Disabled→Silent→Warning→Quiet→Healthy). Period readers in db_reader (`closed_trades_range`/`trades_in_range`/`signals_scanner_funnel_range`/`webhook_by_scanner_range`/`screener_scores`/`trade_story_parts`) — all EXISTING tables, NO schema. conftest +`screener_results` table +3 multi-day gap_fade_long trades (streak-safe). **333 tests green** (v41+v42); 0 core/schema/rename; pinned 6/13/8/6/4 + HARD_KILL blink unmoved; ExportButton OFF; honest G-gaps (G-1 MTM two_state, G-2 trade-story timings "not captured"). tests `test_g5c_analytics.py`. base `80be23a`; Git HELD (branch `gui-g5c-04jul`). memory `gui_g5c_analytics_04jul`. **G5d (Operations/Investigation) BUILT 04-Jul — G5 BUILD COMPLETE (only G5e deploy remains):** **two-state Positions/Holdings** (SYSTEM side full; BROKER LTP/MTM/RR + Broker-Qty/Delta/Recon = honest `two_state_panel` "Pending Broker Source (G4[/P1])", never faked; `/api/positions` +`unavailable`{fields,reason}+`scanner`, `holdings_list` +trade-context join — additive). **Controls = read-only summary** (`/controls`, NEW `/api/controls-summary`; ZERO write path grep-proven; G3-future = disabled placeholder, L4). **Trade Logs** (NEW `/trade-logs`, `/api/trade-logs`; trades+scanner+score+`recon_actions_for_trades`; raw drill reuses `/api/logs?id=`). **Live Activity** (NEW `/live-activity`, `/api/activity`; merged 7-source feed via `services/operations.py`; L10). Blueprint `api/operations.py` registered. **Final 5-group nav complete, no nv-soon:** Operations = Live Activity·**System Health(→carried /services)**·Capital&Risk·Configuration·Controls; carried-not-redesigned = Signals/Orders/Execution/Services/VM (render+200). **354 tests green** (v41+v42); 0 core/schema/rename; pinned 6/13/8/6/4 + HARD_KILL blink unmoved; ExportButton OFF. tests `test_g5d_operations.py`; `test_nav.py` updated. base `80be23a`; Git HELD (branch `gui-g5d-04jul`). memory `gui_g5d_operations_04jul`. **G5e (DEPLOYMENT + VALIDATION PLAN) AUTHORED 04-Jul (PLANNING ONLY — no deploy/merge/push/restart; Git HELD, doc in working tree):** `ops_dashboard/docs/G5e_DEPLOYMENT_PLAN.md` (sections A–I). base `80be23a`(=origin/main=HEAD, deployed G2); **rollback baseline `7b1c92d`** (ops_dashboard functionally == 80be23a, only INSTALL.md doc diff) → recommends tag `gui-pre-g5-rollback@80be23a`. **G5 build = uncommitted working tree, NO `gui-g5*` branch yet; 44 `ops_dashboard/**` files (22 M + 22 ??).** MERGE-GATE: deploy branch off origin/main, stage ONLY `ops_dashboard/`, name-only diff must be `^ops_dashboard/` ONLY (STOP on any core/orders/capital/strategies/broker/signals/main.py/config/schema); EXCLUDE `PATHS.md`+`docs/SYSTEM_MAP.md` (mixed telegram/chartink) + `docs/audit/*` + `gui/`. Refresh (not fresh-install): off-market push→post-receive checkout→restart `gui-dashboard` ONLY→overlay `gui_config.local.yaml` survives (600+secure:true)→Tailscale persists→curl 302. GO iff: 354 tests green (clean env) · gate clean · smoke (21 menu+9 off-menu+contract) · **Rama browser sign-off (human gate)** · rollback tag · off-market. Q3/XLSX OFF. memory `gui_g5e_deploy_plan_04jul`. **G5 PRE-CUTOVER LOCAL VALIDATION DONE 04-Jul (local branch+commit+run+evidence ONLY — no deploy/merge/push/restart):** evidence `ops_dashboard/docs/G5_SIGNOFF_EVIDENCE.md`; local branch **`gui-g5-deploy`@`91cc207`** (NOT pushed; merge-gate CLEAN 45 files `ops_dashboard/**`); **clean-env `pytest` 354/0** (I4 kiteconnect-absent; 28/28 gate tests incl. pinned 6/13/8/6/4 + HARD_KILL blink + isolation + CDN gate); **route sweep 0 404/0 500** (live unauth 61 + authed 120); **CONDITIONAL GO** — Rama's browser sign-off (STEP 5) is the pending binding human gate. memory `gui_g5_local_validation_04jul`. **G5 REDESIGN DEPLOYED TO VM 04-Jul (off-market, GUI-only) — LIVE https://trading-system.tail1cdc6d.ts.net:** merge **`b4aea6f`** (origin/main == VM main HEAD; `gui-g5-deploy`@`91cc207` `--no-ff`); pre-G5 `80be23a`; **rollback tag `gui-pre-g5-rollback`→`80be23a`**. Binding gate clean-VM-venv `pytest` 354/0 (I4 pass; 28/28 pinned/isolation/CDN). Merge-gate `ops_dashboard/**` only at push+merge (post-receive main-only; crontab no-op). Overlay survived checkout -f (600/secure:true/real DB); ONLY gui-dashboard restarted → active; **trading stack untouched**; Tailscale persisted. Smoke 61-route 0×404/0×500 + content honest. **Real-data authed test-client vs SQL ALL MATCH** (P&L 81/43.67, psr 12/31.99, scanner 19572, trades 228, trade-story assembles, positions/holdings broker `unavailable`, controls read_only). G5.1 UI/UX backlog seeded (not blockers). memory `gui_g5e_executed_04jul`. Next = Rama browser review → G5.1 polish; soak S1–S8 from Mon 08:15. **GUI REDESIGN, one-screen-at-a-time (started 08-Jul-2026), Screen 01 (Login) BUILT:** branch `gui-redesign-login-08jul`@`3537632` (LOCAL/UNPUSHED, off `main`@`271d24f`); presentation-only, single file `ops_dashboard/frontend/templates/login.html` (self-contained — `/static/*` requires auth, so hero art is inline SVG, no separate asset/CDN); gap doc `ops_dashboard/docs/redesign/01_login_gap.md` (corrected the task's premise: the live login had no hero pane at all — `gui/01. Login-Screen.png` is a target mockup, never previously built). Added 40/55 hero/card split (mobile ≤780px hides hero), inline SVG chip+heraldic-eagle (4-side pins, neon glow), field placeholders, footer shield icon, two-tone title, Inter/IBM Plex Sans typography. Zero backend/route/API/auth change; 354/354 tests green. Awaiting Rama's browser sign-off before Screen 02 (Dashboard).

> 📌 **Deferred board (30-Jun) — only TWO open** (else closed/deployed/dormant): **(1) S&R V1 calibration** — DEFERRED/collecting; reopen DATA-gated (~50 fills + ~8–10 BIR-filled W/L, checkpoint ≈14-Jul) → PASS→Phase A / RECALIBRATE→zone params. **(2) Delivery Slice 2.5 T2** — **DEFERRED (02-Jul attempt; live NEVER run — proof script bit-rotted)**. Pre-checks PASSED (deploy both fixes live; delivery-lock gate GREEN). Drift in `scripts/t2_cnc_gtt_realtest.py`: L71 `core.`→`broker.order_state_machine`, L83 `RateLimiter(cfg.broker_limits)`, L319 `store=None`→wire (else no `gtt_state` row). Reopen after an OFF-MARKET whole-script repair + full static audit vs main.py → clean dry-run → commit → push off-market → run COMPLETE T2. **REPAIRED 07-Jul — branch `fix-t2-repair-07jul`@`ef442ab` (LOCAL/UNPUSHED, no live run yet): all 3 drifts fixed + throwaway-DB store wiring; runs against an ISOLATED `data_store/t2_proof_<YYYYMMDD_HHMMSS>/t2_proof.db` (own subdir so the ATTACHed `analytics.db` sibling is isolated too — never the live `data_store/analytics.db`); `--dry-run` clean PC+VM (gtt_state row in the throwaway, live DB gtt_state 0->0, no real order). Supersedes the parked `fix-t2-import-02jul`@b826ae0 (drift #1 only, discard). Next = supervised live run (memory `t2_repair_built_07jul`).** Detail: SYSTEM_MAP "Deferred items board" + memory `sr_v1_calibration_deferred_30jun` / `delivery_slice25_status_30jun`.

> ⏰ **Timezone (T4, 29-Jun) — NEVER `TZ='Asia/Kolkata' date` in Git Bash.** MSYS2 ships no
> zoneinfo, so that form silently returns **UTC** (off by 5:30 — the 29-Jun "13:47 vs 19:22"
> defect). For IST use `scripts/ist_now.sh` (authoritative, reuses `now_ist()`), or `now_ist()`,
> or `date -u` (UTC), or `TZ='IST-5:30' date`. **Before any deploy/restart:** run
> `python scripts/deploy_preflight.py` (anchors on `date -u` + the VM: PC↔VM UTC agreement ≤120s
> **+ VM-authoritative market-open gate** — refuses a mid-session deploy). `scripts/check_tz.sh`
> is the loud self-check (correctly FAILS on this MSYS2 PC). App + VM are unaffected (`now_ist()`
> = fixed +5:30, zoneinfo-free; VM NTP-synced) — this is an operator-tooling guard only.

## VM (161.118.187.249, user `ubuntu`, IST)
| What | Path |
|---|---|
| Project root (running tree) | `/home/ubuntu/systems/trading-system/` |
| Python venv (shared) | `/home/ubuntu/systems/venv/bin/python` (3.12.3) |
| Main DB (v41) | `data_store/trading_system.db` (v41 = +`config_snapshots`, W0 report-redesign foundation) |
| Analytics DB (ATTACHed) | `data_store/analytics.db` (holds `system_metrics`/`_daily`; T1 29-Jun: `disk_used_pct` now shutil-based — was −1.0 (psutil absent); cpu/mem still −1.0) |
| Broker token | `data_store/session/zerodha_token.json` (canonical — loader `zerodha_login.is_token_valid`/`load_token`, `auto_refresh_token`, and `main._invalidate_token` all agree; **H-11**: `main._TOKEN_PATH` const, dead-token rename → `zerodha_token.invalid` here on a BrokerAuthError shutdown) |
| Secrets | `.env` (root) + systemd drop-in (NOT in git) |
| Logs | `logs/system_YYYY-MM-DD.log`, `reconciler_*.log`, `trades_*.log`, `cron-*.log` |
| Master config | `config/system_config.yaml` (T5 29-Jun: `trading_hours.entry_end` 15:15→**15:00** = the per-strategy reality; `eod_entry_cutoff` stays 15:15. **C-2 02-Jul:** `webhook.per_ip_{rate_limit_enabled,burst,refill_per_sec}` added [token-bucket, 429]; `WEBHOOK_SECRET` now required in BOTH modes [`main.required_startup_secrets`]; bind still `0.0.0.0`/`require_hmac:false` pending the Phase-3 network decision — memory `c2_webhook_lockdown_02jul`. **C-2 prep 03-Jul (read-only):** Chartink IP findings `docs/audit/c2_chartink_ip_investigation_03jul2026.md` — 1 static IP 15/15 days, memory `c2_chartink_ip_investigation_03jul`. **WEBHOOK_SECRET ROTATED 03-Jul (VM-only, value not stored anywhere):** old token dead (exposed in chat ×2); single shared token for all 15 scanners; loads Mon 08:15; Rama updates all 15 Chartink `?token=` URLs; **Monday-open: confirm 1st real signal accepted**; memory `webhook_token_rotated_03jul`. **TELEGRAM SHADOW FIXED (03-Jul ~23:40):** deleted `telegram.conf` drop-in (held a REVOKED/401 token overriding `.env`) → `.env` now SINGLE source for the bot token; verified no override, `.env` token valid, backup shredded; activates Mon 08:15; **Monday-open: confirm main-process telegram alert delivers**; findings `docs/audit/telegram_token_shadow_investigation_03jul2026.md`, memory `telegram_token_shadow_FIXED_03jul`) |
| Cron source of truth | `config/cron_registry.yaml` (→ `core/cron_registry.py`; `officer:` block = Cron Officer settings) |
| Accounts | `config/accounts.csv` (primary: LFL836) |
| Reports | `reports/{daily,daily_review,flow_trace,system_manager,cron_officer,...}/` |
| Cron-job markers | `data_store/cron_marks/<job>.done` (exit-code markers the Officer reads) |
| Alert sentinels + retention | `data_store/critical_alert_*.flag`→`.delivered` (alert-watcher). `sentinel_retention` cron (02:05 daily) deletes `.delivered` >7d, **NEVER** `.flag`; no archive (email is the record). Tests must NOT use `sentinel_dir="data_store"` |
| DB backups + retention | `data_store/backups/` — daily `trading_system-*`/`analytics-*.db` (cron 01:00/01:05) + `pre_*` deploy/ad-hoc backups. **T2 retention** = `backup_retention.py --apply` (cron 02:00, monitored): category-aware keep-N (pre_*=20 / daily=14), dry-run default, never-delete-newest, scoped globs, >10-delete sanity cap. Replaced the old daily-only `find -mtime +7`. **S-1B.2 (06-Jul off-market):** ALL secret-bearing `trading_system-*`/`pre_*` main-DB backups predating the webhook-secret scrub were `shred -uz`'d (21 in `data_store/backups/` + root `.pre-deploy-2026-07-03` + `/home/ubuntu/backups/pre_deploy_02jul_securitybatch/trading_system.db`); analytics backups kept (never held the secret); fresh scrubbed `trading_system-2026-07-06-postscrub.db` seeded. Live `signals.webhook_payload` token=0. Rollback anchor `/home/ubuntu/db-backups/pre-s1b2-*.db` SHREDDED 07-Jul (after the 08:15 in-window scrubbed-DB open confirmed healthy) — no secret-bearing artifacts remain. memory `s1b2_history_scrub_06jul`+`wave3_config55_runtime_closure_07jul` |
| Cron audit | `data_store/cron_audit/` (Phase-1 findings + daily `job_list_<date>.json` snapshots) |
| Bare repo (deploy target) | `/home/ubuntu/trading-system.git/` (post-receive checks out tree) |
| Canonical cron | `deploy/cron/trading-system.cron` = `generate(cron_registry.yaml)` via `scripts/generate_crontab.py` (ASCII+LF). live==canonical==generate; **44 command-lines** (30-Jun: +`sr_detector_backfill` 15:58; 29-Jun: +`reconstruct_excursions` 15:50 +`control_tower` 17:05, superseding the standalone size-logger); post-receive auto-installs on push (regenerate the sha after a registry change) |
| Agent CLIs (outside project) | `~/tools/antigravity/agy` (Antigravity/`agy` — drives `gemini_*.py` AI-ops crons) · `~/tools/gemini/` (Gemini CLI, node) · `~/tools/claude/` (Claude Code; 4×/day heartbeat → `cron.log`). ✅ heartbeat now `cd`s into `~/tools/claude/` → governed by `AGENTS.md` + `.claude/settings.json` (no .py/DB/systemctl; verified 23-Jun). Live-crontab only (not in canonical cron) — see SYSTEM_MAP |

## Run a command on the VM
```bash
cd /home/ubuntu/systems/trading-system && . .env && \
  PYTHONPATH=. /home/ubuntu/systems/venv/bin/python <script.py>
```
Raw DB reads: use `core.db_connect.connect` (sets up the v28 ATTACH).

## Services (`systemctl`)
`trading-system.service` (main, `main.py --mode live`) · `token-watcher.service` ·
`alert-watcher.service` · `trading-watchman.service` · `security-watcher.service` ·
`cron-watchdog.timer` (19:30 daily watch-the-watcher, ARMED 23-Jun)

## Firewall (VM, verified 05-Jul-2026)
**iptables-nft**, persisted at **`/etc/iptables/rules.v4`** by **`netfilter-persistent`** (enabled+active). `nftables.service` is DISABLED and `/etc/nftables.conf` is NOT the persistence path — modify with `iptables` (nft-compat) + `sudo netfilter-persistent save`, NOT `nft -f /etc/nftables.conf`. INPUT policy ACCEPT but an explicit `-A INPUT -j REJECT --reject-with icmp-host-prohibited` (rule #7) is the catch-all; accepted before it: established, icmp, lo, tcp/22 (#5), tcp/5000 (#6). ufw enabled-but-INACTIVE (empty chains); `fail2ban` active (own `inet f2b-table`, bans SSH brute-forcers); Tailscale `ts-input`/`ts-forward` chains. S-1A allowlist = replace INPUT #6 with `-s 23.106.53.213/32` (see memory `s1a_webhook_allowlist_rotation_05jul`).

## PC (developer machine)
| What | Path / value |
|---|---|
| Local repo | `D:\Projects\trading-system` |
| Deploy | `git push origin main` → bare repo hook → VM working tree (**no scp; no auto-restart**) |
| SSH alias | `trading-vm` (key `trading_vm_secure`, passwordless) |

## Activate deployed code on VM (manual)
```bash
ssh trading-vm 'sudo systemctl restart trading-system.service'
```

## Top-level packages
`core/` infra · `broker/` integration+polling · `capital/` risk/kill-switch ·
`orders/` order lifecycle · `signals/` ingestion · `screening/` scoring ·
`data/` market data · `alerts/` telegram · `scripts/` ops+gemini ·
`ops/control_tower/` VM ops aggregator (Phase 1) · `tests/` (305)

## Strategy control — 3-layer (Slice 2, 24-Jun)
| What | Location |
|---|---|
| Resolver (single source of truth) | `strategies/control.py::strategy_will_trade(strategy, *, trade_type, force_intraday_only)` → `Verdict(will_trade, reason, product)` |
| LAYER 1 master gate | `system_config.yaml` `trade_type: INTRADAY|DELIVERY|BOTH` (default INTRADAY) → `core/config_loader.py` SystemConfig + validator |
| LAYER 3 per-strategy switch | `config/strategies/*.yaml` `enabled: true` (all 15) → `strategies/schema.py` `enabled: bool = True` |
| LAYER 0 breaker (unchanged) | `force_intraday_only` (load-time intent→INTRADAY rewrite in `strategies/loader.py`) |
| Entry gate (2 sites) | `signals/signal_processor.py` `_process_one` + `continue_from_gate` → `_PipelineReject("STRATEGY_CONTROL")`; placement path UNCHANGED (gate only rejects) |
| Status table | `scripts/strategy_status.py` (build + html/telegram/plain) → folded into Cron Officer 09:20 briefing (`CronReport.strategy_status`); Type=true intent, Verdict=gate |
| Default state | trade_type INTRADAY + force_intraday_only true + all 15 enabled → 15 WILL TRADE / 0 WON'T (zero behaviour change). Delivery (CNC) parked → Slice 2.5 |

## Order-exit safety — circuit-band placeability gate (NOCIL fix, 23-Jun)
| What | Location |
|---|---|
| Placeability primitive + result | `orders/price_math.py` → `clamp_exit_into_band()` / `ClampResult` (margin `DEFAULT_CIRCUIT_MARGIN_PCT`) |
| SL-unplaceable exception | `core/exceptions.py` → `SLUnplaceableError` (→ emergency-close + hard_kill) |
| The **only** 3 clamp call sites | `orders/order_protocol_limit.py` — `place_exits` (SL+TGT), `place_tgt_only` (TGT) |
| Pre-fill reject + fast-disable flag | `screening/secondary_screener.py` · config `entry_gate.circuit_proximity_reject_enabled` (default true) |
| Never-clamp exceptions (test-enforced) | `orders/order_protocol_co.py` (CO-TGT), `orders/order_reconciler.py` (G5b SL) |
| Tests | `tests/unit/test_nocil_clamp_fix.py` (E.1–E.9), `tests/unit/test_price_math.py` |

Detail: `docs/SYSTEM_MAP.md` → "Circuit-band placeability gate".

**Tick-size snap (single chokepoint, fail-safe 23-Jun):** every broker submission is tick-snapped at `broker/zerodha_adapter.py` → `_snap_order_to_tick` / `_resolve_tick` (fallback `DEFAULT_TICK` 0.05 + throttled WARN on a missing `tick_size`; covers `place_order` **and** `modify_order`, parity). `orders/order_placer._round_to_tick` was **removed** — the adapter is the sole snap point. Tests: `tests/unit/test_tick_failsafe_snap.py`.

**Order-tag length guard (single chokepoint, 01-Jul — branch `fix-emergency-exit-tag-01jul`, NOT pushed):** `broker/zerodha_adapter.py::place_order` truncates the tag (`if tag: tag = truncate_tag_for_broker(tag)`, ≤16 chars) right before `kite.place_order`, so **NO caller can submit an over-length tag** (Zerodha rejects >20 → "Invalid tags: max allowed tag length is 20"). Root-caused from the naked-position CHECK9 EMERGENCY exit (`order_reconciler.py:2133` passed the full 36-char `trade_id` → the last line of defense was rejected every time; 1-Jul BANSALWIRE). Call-site also fixed at :2133. The 4 `order_placer.py` `tag=trade_id` sites are safe (truncated downstream by the order protocols). Matching is by `order_id` not tag. Tests: `tests/unit/test_emergency_exit_tag_fix.py` (fail-on-old / pass-on-fix).

## MIS Learned Blocklist (source-free, 30-Jun) — NO_FILL Mechanism B
| What | Location |
|---|---|
| Store (JSON, no DB) | `data_store/mis_blocklist.json` `{SYMBOL: last_blocked_date}` (git-ignored runtime; atomic write) |
| Module | `core/mis_blocklist.py` — `MisLearnedBlocklist` (record/is_blocked + TTL) + `is_mis_block_rejection(exc)` |
| Record (always-on) | `orders/order_placer.py::_handle_placement_failure` (ONE guarded `record_block` on MIS-block 400; existing steps unchanged) |
| Drop (flag-gated) | `screening/secondary_screener.py::screen()` → `REJECTED_NOT_MIS_TRADABLE` (only when product==MIS) |
| Flag | `config/system_config.yaml` `mis_filter` (enabled:false/shadow:true/ttl_days:5) → `core/config_loader.py` MisFilterConfig |
| Tests | `tests/unit/test_mis_blocklist.py` (22). STAGED branch `mis-tradability-filter-30jun`, NOT pushed. NO schema/cron/external-source |

## Duplicate-exit safety — RAMCOIND fix (25-Jun): G5b can no longer over-sell
| What | Location |
|---|---|
| Root | G5b crash-recovery placed a 2nd SL outside the OCO via a TOCTOU race on the lagging local `orders` table → both filled on the stop-hit → −1 over-sell (RAMCOIND 25-Jun; also IRFC 17-Jun / NIACL 22-Jun) |
| **L2 keystone** invariant | `orders/order_reconciler.py` `_check_duplicate_exits` / `_dedupe_exit_leg` — exactly one live SL (LIMIT_TRIPLE only; CO excluded via `variety='co'`) + one live TGT per open trade; cancels the non-canonical (later-placed) extra, never drops to zero |
| **L1** G5b guard | `_g5b_crash_recovery_sl` → 10s settling window (`_entry_fill_age_seconds`) + authoritative broker/`_fill_map` check (`_already_has_live_sl`, `_G5B_SETTLING_WINDOW_SEC`) instead of the lagging table |
| **L3** over-sell / naked | `_check2_orphan_adoption` → `_detect_system_oversell` (CRITICAL + auto-flatten) · `_position_is_naked` (naked → WARNING, never silent; protected human → silent, FIX-182) |
| **L4** after-check | `orders/order_placer.py` `_verify_exits_placed` → flags `>1` live SL/TGT (`DUPLICATE_SL/TGT`) |
| Tests | `tests/unit/test_ramcoind_dup_exit_fix.py` · acceptance gate `tests/crash_test/test_ramcoind_oversell_prevented.py` (paper+live: position ends FLAT, never −1) |

No DB schema; parity (shared paper+live, no mode branch). Detail: `docs/SYSTEM_MAP.md` Changelog 2026-06-25 · memory `ramcoind_duplicate_sl_incident_25jun`.

## Naked-orphan ROOT-CAUSE fix — A-1 (timeout) + E-1 (crash), 02-Jul (branch `fix-a1e1-orphan-recovery-02jul`, UNPUSHED)
| What | Location |
|---|---|
| Root | An ENTRY that reached the broker but whose local `orders` row never persisted (A-1 place-timeout / E-1 crash-mid-place) was marked FAILED + capital released WITHOUT confirming broker absence → filled → disowned as human → **naked position** till 15:17 EOD. Enabler: the broker `tag` was written but never read |
| Broker oracle | `broker/zerodha_adapter.py` `get_all_orders()` — ALL of today's orders (any status) WITH `tag`+`product`; live `kite.orders()`, paper `_paper_fills` (tag+product retained across the synth-fill merge) |
| Correlation (PURE) | `orders/order_reconciler.py` `correlate_entry_by_tag()` — recompute `truncate_tag_for_broker(trade_id)`, side-filter to the ENTRY leg → MATCH / ABSENT / AMBIGUOUS (48-bit collision narrowed by symbol+qty) |
| Unified recovery PREPASS | `_recover_in_flight_entries()` → `_adopt_or_fail()` — runs at the TOP of every `_reconcile()` (startup sync `reconcile_once()` + 15-min poll), BEFORE CHECK1/G5b, so an adopted-OPEN trade is SL'd (G5b) + TGT-retried the SAME cycle. Both feeds (timeout `UNKNOWN_IN_FLIGHT` queue + crash orphaned-`PENDING`) → ONE path |
| Decisions | terminal+filled→ADOPT (protect; partial-then-CANCEL included) · terminal+zero-fill→FAILED+release (only evidence-based release) · non-terminal→DEFER (reserve kept) · ABSENT→FAILED after 3-poll budget · broker-unreachable→DEFER set · AMBIGUOUS→CRITICAL alert+defer · HARD_KILL+fill→FLATTEN (commit→EXITING→oversell-guarded emergency close) |
| Crash-cleanup reroute | `broker/order_monitor.py` `_cleanup_orphaned_pending_trades` — now DETECT-ONLY (WARNING; NO blind FAILED / NO orphan-release), defers to the reconciler recovery |
| Capital (crash-aware, exactly-once) | `capital/fund_manager.py` `commit_adopted_entry` / `restore_adopted_reservation` / `release_adopted_reservation` (+ `_restore_reserve_from_ledger`/`_resolve_reservation_id`/`_commit_exists`) — reconstruct a crash-lost reservation from the durable `fm_ledger` RESERVE row, then commit/release; three-balance invariant holds |
| State transitions | `core/state_store.py` `adopt_recovery_trade_to_open` (extended: atomic status flip + fill-field backfill) · `mark_recovery_trade_exiting` (HARD_KILL) · `fail_recovery_trade` |
| Preserved | RAMCOIND L1-L4 · CHECK9 (FACET-1 race-aware + FACET-2 oversell) · G5b (entry_time backfilled to `created_at` so the 10s settling window doesn't defer the recovery SL). Old `_check_unknown_in_flight` (blind FAILED) REMOVED. NO schema |
| Tests | `tests/unit/test_a1e1_recovery_matrix.py` (7 capital-proof cases + 13-row matrix incl. same-cycle-G5b integration + paper-parity) · `tests/unit/test_a1e1_orphan_recovery.py` (units) · `tests/unit/test_fix068_timeout_recovery.py` (order-placer side kept) |

Parity (one recovery path, both modes; live Kite / paper `_paper_fills` oracle). Detail: `docs/design/a1_e1_orphan_fix_design_02jul2026.md` · `docs/SYSTEM_MAP.md` Changelog 2026-07-02 · memory `a1_e1_orphan_fix_impl_02jul`.

## Delivery (CNC) — SLICE2.5-P1+P2 (25-Jun): durable GTT overnight protection (delivery_enabled=false)
| What | Location |
|---|---|
| Master lock (R2 split) | `system_config.yaml` `delivery_enabled` (default **false**). **P2 guard split:** gates ONLY the CNC **entry** (`zerodha_adapter.place_order`); protective GTT ops (`place_gtt`/`modify_gtt`/`delete_gtt`/`get_gtt(s)`/`get_holdings`) are NOT gated — protection survives disablement. Real CNC entry needs delivery_enabled=true AND force_intraday_only=false AND trade_type∈{DELIVERY,BOTH} |
| GTT placer | `orders/cnc_gtt.py` `CncGttPlacer` — OCO legs (SL limit = trigger − `capital.gtt_sl_limit_offset_pct` 3%; TGT limit = trigger − `sl_limit_offset_pct`), C8 validate, ONE GTT/trade. **P2:** persists to `gtt_state` (durable), `hydrate_from_store()` rebuilds the hot cache on boot, `forget()` for recreate |
| Durable state (v36) | `core/schema.sql` **TABLE 37 `gtt_state`** (gtt_id INTEGER PK, status ACTIVE→TRIGGERED→CLEANED/…, needs_review, FK→trades) = the one-per-trade source of truth (replaces P1's in-memory map). `core/state_store.py` DAO (insert/update/get_active/by_id/status/needs_review/verified + `mark_trade_closed_gtt`/`record_gtt_close_financials`) |
| Reconcile + GTT_EXIT | `orders/cnc_gtt_monitor.py` `CncGttMonitor.reconcile()` — re-verifies each GTT vs broker (get_gtts + holdings + positions), K6 ladder (healthy / **GTT_EXIT** finalise+capital-release / F6 re-protect / recreate / qty-mismatch CRITICAL-once+needs_review / orphan delete / >1-ACTIVE soft-kill) + 50-cap. Wired into `order_reconciler` (startup [4a] + 15-min in-hours [4b]); delivery trades EXCLUDED from the position/SL/exit checks |
| **Orphan-GTT ADOPTION (FIX-183, 26-Jun)** | `cnc_gtt_monitor.adopt_orphan_gtts()` — reconstructs a LIVE broker GTT with NO `gtt_state` row (SL/TGT split by trigger **magnitude**) + correlates to the open delivery trade (product-aware `get_all_open_trades` by symbol; 0/>1/already-ACTIVE → WARN-once, never adopt; never deletes a live GTT). Wired as a NARROW prepass at the **top of `order_reconciler._reconcile()`** (`_run_gtt_adoption_prepass`) so it runs BEFORE CHECK1 in BOTH startup+15-min — closes C2.1 (a carried row-less CNC GTT was mis-marked CLOSED_MANUAL because its holding is in holdings() not positions()). Alerts `source_module="cnc_gtt_adoption"` |
| Adapter GTT/holdings I/O | `broker/zerodha_adapter.py` `place_gtt`/`modify_gtt`/`get_gtt`/`get_gtts`/`delete_gtt`/`get_holdings` — live=Kite, paper=in-memory store (`_paper_gtts`/`_paper_holdings`, `seed_paper_holding`). Paper gtt ids are **NUMERIC** (`_PAPER_GTT_ID_BASE`) since gtt_state.gtt_id is INTEGER PK |
| Limit math | `orders/price_math.py` `calc_gtt_limit_price` (dedicated; not the intraday 0.5% path) |
| Centralized gate | `orders/full_entry_engine.py` `place_deferred_exits` (intent==DELIVERY → GTT, `ExitLegsResult.is_gtt`); `order_placer._finalize_cnc_gtt` (logs gtt_id; no day legs). INTRADAY unchanged |
| Tests / T2 | `tests/unit/test_cnc_gtt_slice25_p1.py` · `test_cnc_gtt_slice25_p2.py` · `test_cnc_gtt_monitor.py` · `test_cnc_gtt_step4_wiring.py` · `test_cnc_gtt_adoption_fix183.py` (FIX-183: incl. the C2.1 + MIS-regression tests) · `scripts/t2_cnc_gtt_realtest.py` (market-hours real-API/TPIN proof — the blocker before enabling delivery) |

P2 = durability + safety (schema v36 `gtt_state`, reconcile, GTT_EXIT, 15-min monitor); delivery_enabled stays **false** (no activation). **DEPLOYED to main `bad0aad` 25-Jun ~22:23 (one-time authorized; rule restored); schema v36 applies at the Fri 08:15 boot.** **FIX-183 (26-Jun) adds orphan-GTT adoption** (no schema) — **DEPLOYED to main `af4b784` 26-Jun ~11:11 (one-time authorized; rule restored); 11:11 restart self-exited 0 at the HOLIDAY guard (Muharram), real boot Mon 29-Jun 08:15; verified broker-session-free 5/5 (schema=36, Auditor 0 BLOCK, delivery_enabled=false).** Detail: SYSTEM_MAP Changelog 2026-06-25/26 · memory `slice25_p2_gtt_durability_25jun` · `fix_183_gtt_adoption_26jun`.

## Delivery (CNC) — SLICE2.5-PHASE-3 (26-Jun): delivery count caps + conditional capital (INERT)
| What | Location |
|---|---|
| (A) Delivery count caps | `system_config.yaml` `risk.max_open_delivery_positions`(3)/`max_daily_delivery_trades`(5) → `core/config_loader.py` RiskConfig. Product-keyed counts `core/state_store.py` `count_open_delivery_positions`/`count_daily_delivery_trades` (JOIN orders `leg='ENTRY' AND product='CNC'`, DISTINCT trade_id). Enforced `capital/risk_engine.py::_run_checks` — OPEN_POSITIONS/DAILY_TRADES **branch on `sizing_result.bucket=="positional"`** (else = global check, byte-unchanged; counts read lazily in `approve()`). **A5**: intraday branch unchanged → asymmetric coupling (delivery counts toward intraday cap, not vice-versa) — intentional, keeps live intraday cap byte-identical |
| (B) Conditional allocation | `system_config.yaml` `capital.conditional_allocation_enabled`(**false**) → `core/config_loader.py` CapitalConfig. Pure `capital/fund_manager.resolve_bucket_allocation()` → effective `(intraday_pct, positional_pct)` (off=fixed split; on: only-intraday 100/0, only-delivery 0/100, both=split, neither=100/0). `main.py` computes `delivery_active`/`intraday_active` + passes effective pcts to FundManager ctor + logs `capital.bucket_allocation`. **FundManager UNCHANGED**; no-borrow already in `reserve()` (consults only the intent's bucket) |
| SHARED (untouched) | `max_concentration_pct` (position_sizer, total-relative), SECTOR_EXPOSURE, `max_position_value_pct`, the whole intraday cap path |
| Tests | `tests/unit/test_phase3_delivery_caps_conditional_capital.py` (17: allocation cases + dormancy + delivery caps + ★inertness + ★MIS-regression + no-borrow) |

INERT by construction: flag default false → 70/30 unchanged; force_intraday_only=true coerces every strategy to intent=INTRADAY → bucket=intraday → delivery cap branch never hit + delivery counts 0. No schema; parity. 3854 unit tests / 0 regressions. **DEPLOYED to main `37b3db3` 26-Jun ~13:16 (one-time authorized batch w/ FIX-183-log + Phase 4; rule restored; Muharram holiday self-exit, real boot Mon 08:15; verified DORMANT — fixed 70/30, caps inert).** Detail: SYSTEM_MAP Changelog 2026-06-26 · memory `slice25_phase3_delivery_caps_conditional_capital_26jun`.

## Delivery (CNC) — SLICE2.5-PHASE-4 (26-Jun): trade_type reject-by-intent gate (CONFIRM + label split)
| What | Location |
|---|---|
| The gate (ALREADY existed, Slice 2) | `strategies/control.py` `strategy_will_trade` LAYER 1×2 — `trade_type` INTRADAY→reject DELIVERY-intent / DELIVERY→reject INTRADAY-intent / BOTH→accept; keyed on EFFECTIVE post-coercion intent. Wired into `signals/signal_processor.py` `_process_one` (~:625) + `continue_from_gate` (~:1330), BEFORE sizing. Dormant under current config |
| PHASE-4 label split (the one live touch) | `Verdict.cause` field on `control.py` (`CAUSE_OK`/`CAUSE_DISABLED`/`CAUSE_FORCE_BREAKER`/`CAUSE_TRADE_TYPE`). Gate maps `cause==CAUSE_TRADE_TYPE` → `_PipelineReject("TRADE_TYPE")` → `REJECTED_TRADE_TYPE` + own `_stats["rejected"]["TRADE_TYPE"]` tally; all other control rejects stay `STRATEGY_CONTROL`. Keyed on cause, NOT the message string; reason strings + accept/reject logic UNCHANGED |
| Tests | `tests/unit/test_phase4_trade_type_gate.py` (9: cause-per-layer, reason-unchanged guard, go-live matrix, ★dormancy, ★label split, ★label-bleed, contradictory-combo Auditor BLOCK) |
| Option B (declared-intent gating) | **PARKED** — would stop the 3 live `positional_*` declared-DELIVERY strategies (trade as coerced-intraday today); a strategy/perf decision, `strategy.enabled` is the cleaner mechanism. Declared intent recoverable via `scripts/strategy_status.py:74` (raw `validate_strategy`), not on the loaded object |

Dormant (INTRADAY+force=true → coerced → accept → split never fires). No schema; parity. 3863 unit tests / 0 regressions. **DEPLOYED to main `37b3db3` 26-Jun ~13:16 (one-time authorized batch w/ FIX-183-log + Phase 3; rule restored; Muharram holiday self-exit, real boot Mon 08:15; verified DORMANT — gate no-op, Auditor 0 BLOCK).** **Completes the Slice 2.5 build side**; next = T2 + C1-watch. Detail: SYSTEM_MAP Changelog 2026-06-26 · memory `slice25_phase4_trade_type_gate_26jun`.

## S&R V2 Phase A (SNR-V2, 27-Jun) — WAIT_FOR_RETEST entry side, SYMMETRIC LONG+SHORT (default-OFF, schema v38)
| What | Location |
|---|---|
| Master flag (default OFF) | `system_config.yaml` `sr_detector.wait_for_retest_enabled: false` (extends the V1 block) → `SRDetectorConfig`. ONE flag covers BOTH sides. Off ⇒ ZoneWarmer/RetestMonitor/Diverter NOT constructed ⇒ divert + enqueue are no-ops ⇒ byte-identical |
| Divert (pre-placement) | `signals/signal_processor.py` `_process_one` AFTER side, BEFORE sizing/reserve(`:818`) → `RetestDiverter.maybe_divert` (`screening/retest_monitor.py`). Reads `ZoneCache` SYNC — LONG matches a HIGH **resistance** zone, SHORT a HIGH **support** zone (miss/unknown-side/no-HIGH → fall through); parks into RetestMonitor; `signals.status=RETEST_WAITING`; audit row to `sr_detector_results` (BUYING_INTO_RESISTANCE / SELLING_INTO_SUPPORT). NO capital held |
| Confirm state machine | `sr_detector/retest_confirm.py` `evaluate(direction=…)` — ONE pure direction-parameterised FOLD. LONG: WAIT_BREAKOUT→WAIT_RETEST(close>band_high)→WAIT_CONFIRM(re-enter band)→CONFIRMED(close>band_high + strong UPPER close); REJECT timeout / close<band_low·(1−max_away%) [break_down]. SHORT mirror: break=close<band_low, confirm strong LOWER close, REJECT on close>band_high·(1+max_away%) [reclaim_up] |
| Monitor (restart-safe) | `screening/retest_monitor.py` `RetestMonitor` daemon — polls fresh 1m (OhlcFetcher lookback=`onem_lookback_days`, no cache), passes `parked.direction` into evaluate; CONFIRMED→`continue_from_retest`, REJECT→release; rehydrates from `retest_state`; `has_symbol` dedup |
| Resume (MARKET entry) | `signals/signal_processor.py` `continue_from_retest` (sibling of continue_from_gate) — re-check KS/window/control/governor (NOT 60s expiry); size+reserve (capital HERE); place MARKET (side from `parked.direction`), structure SL = band_low·(1−`sl_buffer_pct`%) LONG / band_high·(1+`sl_buffer_pct`%) SHORT, R:R-preserving TGT (direction-aware `_derive_target`) |
| MARKET route | `entry_order_type` (default LIMIT) threaded `order_placer.place → full_entry_engine.execute → {LimitTriple,CoPlusTgt}.execute → adapter.place_order` (MARKET→price=0). Adapter already supports MARKET; paper `_synth_fill` fills at LTP (parity) |
| Zone cache + warmer | `sr_detector/zone_cache.py` ZoneCache (resistance+support, TTL) · `sr_detector/zone_warmer.py` ZoneWarmer (daemon; enqueued at `signal_processor._dispatcher_loop._warm_zones`) · `sr_detector/zone_builder.py` (DRY zone-build shared with V1 detector) |
| Schema (v37→v38) | `core/schema.sql` TABLE 39 `retest_state` (pure add, FK→signals) + signals.status CHECK `OR GLOB 'RETEST_*'`; `MIGRATION_TABLES[38]=["signals"]` (FK-safe rebuild). DAO `core/state_store.py` insert/update/release/clear_all/get_all_retest_state. EOD clear via `eod_squareoff.set_retest_monitor` |
| Tests | `tests/unit/test_sr_v2_{retest_confirm,zone_cache,divert,monitor,market_path,continue}.py` (55: every LONG branch unchanged + full SHORT mirror) |
| Side-neutral knobs | one set serves both sides: `near_zone_buffer_pct` (was near_resistance_buffer_pct), `confirm_strong_close_frac` (was reclaim_strong_close_frac, + RetestParams field), `require_confidence`, `retest_max_away_pct`, `breakout_margin_pct`, `sl_buffer_pct` |

SHADOW→ACTIVE only when the flag is on; SYMMETRIC LONG+SHORT (ONE flag, ONE set of side-neutral knobs; NO schema change — direction already persisted). branch `snr-v2-phaseA-27jun` (on V1), NOT pushed; schema awaiting nod. Detail: `docs/SYSTEM_MAP.md` header · memory `snr_v2_phaseA_27jun`.

## S&R V2 Phase B (SNR-V2, 27-Jun) — Structure-Aware Exit, MIS LIMIT_TRIPLE only (default-OFF, NO schema)
| What | Location |
|---|---|
| Master flag (default OFF) | `system_config.yaml` `structure_exit.structure_exit_enabled: false` → `core/config_loader.py` `StructureExitConfig` (`SystemConfig.structure_exit`). Off ⇒ `StructureExitManager` NOT constructed, nothing subscribes ⇒ byte-identical. Knobs: `sl_buffer_pct`(0.2), `break_buffer_pct`(0.0), `require_strong_close`(true), `break_strong_close_frac`(0.6), `min_zone_confidence`(HIGH) |
| Controller | `orders/structure_exit_manager.py` `StructureExitManager` — RetestMonitor-shape lifecycle (start/stop + RLock); `start()` subscribes `on_1m_close` to `CandleStore.register_on_candle_close` (push 1m feed, multi-subscriber, paper-parity). Stopped in `main.py::_shutdown` |
| Feed + trade scope | per 1m close: `state_store.get_open_intraday_positions()` filtered to `order_protocol=='LIMIT_TRIPLE'` (MIS/CO-scoped query + LIMIT_TRIPLE ⇒ MIS LIMIT_TRIPLE; CO=CO_PLUS_TGT excluded; CNC never in the query). `ZoneCache.get(symbol)` SYNC — LONG→`.support`, SHORT→`.resistance`, HIGH-confidence only; miss/no-zone → leave SL |
| Zone selection | nearest HIGH zone the candle traded into, keyed on the candle EXTREME (`high` LONG / `low` SHORT) not the close — so the breaking candle still selects the zone it broke (close<band_low would exclude it). LONG=highest support with `band_low<high`; SHORT=lowest resistance with `band_high>low`. Already-broken-and-left-behind zone excluded |
| ACTION B (break, first) | a 1m CLOSE beyond the band (`±break_buffer_pct`) + a STRONG close (`sr_detector/retest_confirm._strong_close`: LONG support-break ⇒ `is_long=False` strong-LOWER; SHORT ⇒ `is_long=True` strong-UPPER — opposite polarity to the entry confirm). Exit in the MANDATORY order: (1) `trades.status='EXITING'` (mirror `kill_switch.py:956`), (2) cancel resting SL+TGT (`leg IN ('SL','TGT')`, mirror `kill_switch._cancel_trade_resting_exits`), (3) reverse-aware flatten — marketable LIMIT (`marketable_limit_price`, LTP±`EMERGENCY_EXIT_BUFFER_PCT`)/MARKET fallback, broker-flat ⇒ skip (no double-exit). EXITING-first ⇒ reconciler CHECK1/G5b never re-arm (`order_reconciler.py:781,1106`) |
| ACTION A (trail, else) | new SL = `support.band_low×(1−sl_buffer_pct/100)` LONG / `resistance.band_high×(1+…)` SHORT, `round_to_tick` nearest. TWO real **pre-modify** guards (root-cause, NOT broker-rejection): ONLY-TIGHTEN (LONG new>cur / SHORT new<cur) + WRONG-SIDE vs LTP (LONG ltp>new / SHORT ltp<new — mirrors `smart_tgt_manager.py:861`). LIMIT moves with the trigger (`calc_sl_limit_price`). `adapter.modify_order(order_id, price, trigger_price, symbol)` (`order_id`=broker PK). Debounce: ≤1 move per (trade, `candle.ts`) — armed only on modify success |
| Single SL owner | the structure-exit controller is the ONLY SL-leg writer — do NOT co-enable any `strategy.trailing_sl_enabled` (BreakevenManager stays dormant/unwired). No DB schema (stateless: EXITING covers in-flight exits; debounce map in-memory, re-derives on boot) |
| Wiring (shared infra) | `main.py`: `_struct_exit_on = system.structure_exit.structure_exit_enabled`; the ZoneCache/ZoneWarmer block builds when `_v2_on OR _struct_exit_on`; `StructureExitManager` built + `start()`ed (warmer `start()` idempotent) after the RetestMonitor block; `_shutdown(structure_exit_manager=…)` unsubscribes |
| Tests | `tests/unit/test_structure_exit_manager.py` (23: trail only-tighten/wrong-side/snap/limit-moves/static/debounce/modify-fail; break LONG+SHORT in-order, weak/wick no-exit, strong-off; gap backstop ×2; zone HIGH-only/nearest/already-broken; CO excluded; **real paper==live** parity for modify/cancel/flatten + mode-agnostic; **dormancy**) |

NO schema change; default-OFF dormant; paper==live (real-adapter parity test). branch `snr-v2-phaseB-27jun` (on Phase A), NOT pushed; PAUSED for review. Detail: `docs/SYSTEM_MAP.md` header · memory `snr_v2_phaseB_27jun`.

## S&R Detector V1 (SNR-DETECTOR-V1, 27-Jun) — shadow support/resistance detector (default-OFF)
| What | Location |
|---|---|
| Master flag (default OFF) | `system_config.yaml` `sr_detector.enabled: false` → `core/config_loader.py` `SRDetectorConfig` (`SystemConfig.sr_detector`). Disabled ⇒ detector NOT constructed (`main.py` passes `sr_detector=None`) ⇒ zero pipeline change. Other fields = confluence/zone tuning knobs |
| Pure package | `sr_detector/` (imports only core/+stdlib): `models`, `fetch` (token + 3-TF windows + session cache + fail-safe), `pivots`, `zones` (clusters + volume profile), `confluence` (multi-method/multi-TF → HIGH/MED/LOW + evidence), `flags` (BUYING_INTO_RESISTANCE / WEAK_BREAKOUT / NO_VOLUME_CONFIRMATION / LOW_CONFIDENCE_STRUCTURE / NO_CLEAR_STRUCTURE + retest PROPOSAL), `detector` (single bg worker; observe/start/stop) |
| Seam (async, non-gating) | `signals/signal_processor.py` `_sr_observe(...)` AFTER a successful `place()` in BOTH `_process_one` + `continue_from_gate`; enqueues + returns immediately; never blocks/raises. Built+injected in `main.py` (ctor param `sr_detector`) only when enabled; stopped in `_shutdown` |
| Fetch (rate-limited) | `main.py` `_make_sr_fetch_fn(market_kite, rate_limiter)` → `rate_limiter.acquire("historical")` then `kite.historical_data(...)`; `_build_market_data_kite(is_paper, kite_client)` (live=kite_client; paper=read-only kite from token file — api_key NOT hardcoded). Adapter UNTOUCHED |
| Table (schema v37) | `core/schema.sql` TABLE 38 `sr_detector_results` (MAIN db; FK→signals; pure addition). DAO `core/state_store.py` `insert_sr_detector_result`/`get_sr_results_for_backfill`/`update_sr_outcome`. .backup-tested v36→v37 clean — **AWAITING Rama nod before live apply** |
| EOD backfill | `scripts/sr_detector_backfill.py` (join sr_detector_results→trades by signal_id; fills actual_*/win_loss/pnl; terminal-only). **Cron-wired 30-Jun @ 15:58 Mon-Fri** (`sr_detector_backfill`, after `eod_verify`; marker-verified `exit_code_file`). `scripts/sr_corp_action_spotcheck.py` = one-time split-adjust check (VM/live) |
| Tests | `tests/unit/test_sr_detector_{units,fetch,observer,backfill}.py` (37; incl. ★non-blocking + ★dormancy/parity + real-store v37 + seam) |

SHADOW-only (no reject/SL/TGT/STM/entry change). Parity (paper+live; row tagged `mode`). branch `snr-detector-v1-27jun`, NOT pushed. Detail: `docs/SYSTEM_MAP.md` header · memory `snr_detector_v1_27jun`.

## MFE/MAE excursions — Option B post-EOD reconstruction (28-Jun, DEPLOYED, schema v39)
| What | Location |
|---|---|
| Root cause | `candles.ts` (space/naive, 15:40 `fetch_daily_candles`) vs `trades.*_time` (ISO-T+05:30) compared LEXICALLY in `compute_trade_excursions` → 0 candles → empty `trade_excursions`; compute also ran on the hot intraday-exit path before candles exist |
| Datetime-safe compare | `core/state_store.py` `compute_trade_excursions` (parse both → tz-aware IST via `_parse_ist_dt`, pre-filter the indexed `date` col; storage format UNCHANGED; **direction math verbatim**) |
| Reconstruction job | `scripts/reconstruct_excursions.py` — post-EOD; PID lock `data_store/locks/`, cron_heartbeat, `ensure_candles` fetch-if-missing (token-file kite, paper+live parity), `INSERT OR REPLACE` idempotent. Modes `--daily/--date/--all-closed/--dry-run` |
| Taxonomy / guard | WRITTEN / WOULD_FETCH (dry-run preview) / SKIPPED_UNRECONSTRUCTABLE (NULL-exit OR sub-minute<1m) / FAILED (→ CRITICAL sentinel). Guard `written+would_fetch+skipped+failed==examined` |
| Audit table (v39) | `core/schema.sql` TABLE 40 `excursion_reconstruction_runs` (pure addition) + DAO `insert_excursion_reconstruction_run` |
| Removed | the dead compute + silent DEBUG swallow in `orders/order_placer.py::_handle_exit_fill` (`_persist_candle` UNTOUCHED — separate ticket) |
| Cron | `reconstruct_excursions --daily` **15:50 Mon-Fri** (`50 15 * * 1-5`, own marker) + `fetch_daily_candles` now emits `cron_marks/fetch_daily_candles.done` |
| **SIGN convention** | **SIGNED** — `mfe_pct` = best favourable move vs entry (MAY be negative if never-favourable); `mae_pct` = worst adverse (MAY be positive if never-adverse); direction-signed, NOT floored. 1-min reconstruction (sub-minute trades get no row) |
| Tests | `tests/unit/test_reconstruct_excursions.py` (15) |

3B/3C historical backfill (`--all-closed`) DEFERRED → next trading day + live token (expect written=41 / skipped=5 / failed=0). Detail: `docs/SYSTEM_MAP.md` header · memory `mfe_mae_excursions_empty_28jun`.

## B-1 daily-loss unrealized-MTM — IMPLEMENTED (02-Jul, f5fd4d9, off main, unpushed, SHADOW)
| What | Location |
|---|---|
| Design + impl | `docs/design/b1_daily_loss_unrealized_mtm_design_02jul2026.md`; memory `b1_daily_loss_unrealized_mtm_impl_02jul` |
| Gate | `capital/risk_engine.py` DAILY_LOSS — reads `get_unrealized_mtm_status()`; flag-gated (shadow default) |
| Reconciler refresh | `orders/order_reconciler._refresh_unrealized_mtm()` (15s, get_quote, set-based prune) |
| FundManager | `mark_unrealized_mtm_refreshed` / `get_unrealized_mtm_status` / `prune_unrealized_mtm` (`_unrealized_mtm` separate dict) |
| Flag | `system_config.yaml risk.daily_loss_include_unrealized: false` (shadow → flip true to enforce) |

## P1 broker-authoritative EOD reconcile — IMPLEMENTED (02-Jul, 4817032, off main, unpushed, SHADOW)
| What | Location |
|---|---|
| Job | `scripts/eod_broker_reconcile.py` @15:58 (standalone creds `reconcile_positions._resolve_credentials`) |
| Pure engine | `compute_verdict(broker, local, ...)` → per-dim + overall VERIFIED/ISSUES/UNVERIFIED |
| Verdict table | `eod_broker_reconciliation` (schema v42 pure-add); writer `state_store.upsert_eod_broker_reconciliation` |
| Flag | `system_config.yaml eod_reconcile.authoritative: false` (shadow → flip true to make authoritative + retire eod_verify) |
| Cron | `cron_registry.yaml eod_broker_reconcile` @15:58 (canonical crontab regenerated) |
| Tests | `tests/unit/test_eod_broker_reconcile.py` (15; TEST A required-down⇒UNVERIFIED, TEST B margin-gated⇒VERIFIED) |
| Shadow metric / cutover | `eod_broker_reconciliation.mismatch` (eod_verify=VERIFIED but P1≠ = false-VERIFY caught); criteria in memory |

Detail: memory `p1_eod_broker_reconcile_impl_02jul` (+ design `p1_eod_broker_reconcile_build_design_02jul`).

## Audit remediation status + P1/P2 dependency map (02-Jul, read-only)
| What | Location |
|---|---|
| Status table + dependency map | `docs/audit/audit_remediation_status_02jul2026.md` |
| Deployed baseline | `origin/main = 9becf8c` (C-1 scrub + C-2 phase-2 + A-1/E-1 live) |
| OPEN HIGH (fix before/with P1) | **B-1** daily-loss `unrealized_mtm` dead — `risk_engine.py:497-510` + writers test-only `fund_manager.py:1404/1416` |
| PARTIAL HIGH (after P1, Rama's call) | **C-2** network — `system_config.yaml:179,187` (`bind_host:0.0.0.0`, `require_hmac:false`) |
| Push queue (unpushed) | `fix-c1-completion-02jul@aa02f9f` (stacks A-2 `fd09a38` + C-1) · `fix-t2-import-02jul@b826ae0` parked |

Detail: memory `audit_remediation_status_02jul`.

## P1 EOD broker-sync / observer assessment (02-Jul, read-only)
| What | Location |
|---|---|
| Assessment + gap list + build seq | `docs/design/p1_eod_broker_sync_assessment_02jul2026.md` |
| EOD cron chain | `reconcile_positions.py`@15:45 (standalone, positions, detect-only) · `eod_cleanup.py`@15:50 (DB only) · `eod_verify.py`@15:55 (**local-only, FALSE-VERIFY**) · `cron_officer --eod-summary`@18:50 |
| In-process (process-dependent) | `orders/eod_squareoff.py`@15:17 (flatten) · `orders/order_reconciler.py` 15s CHECK1-9 loop |
| Observer | `ops/control_tower/` runner@17:05 (freshness only, no broker session) |
| Orphaned / dead | `scripts/reconcile_pnl.py` (NOT scheduled; would ERROR on generic creds) |
| Capital sync to broker | `FundManager.sync_from_broker` — startup + 09:15 one-shot ONLY (never EOD/periodic) |
| Key bug | `eod_verify.py:61` queries `system_net_pnl`/`broker_net_pnl`; real cols `system_pnl`/`broker_pnl` (`core/schema.sql:783-785`) |

Detail: memory `p1_eod_broker_sync_assessment_02jul`.

## C-1 credential-exposure inventory (02-Jul, read-only)
| What | Location |
|---|---|
| Full inventory + remediation plan | `docs/audit/c1_credential_exposure_inventory_02jul2026.md` (names/locations only, no values) |
| Source audit | `docs/audit/system_security_audit_02jul2026.md` (§ C-1) |
| Remote (exposure surface) | `origin = trading-vm:~/trading-system.git` (VM bare repo, SSH) — **NO public forge** → internal-only |
| Only real-secret commit | `9f58848` (`.env.example`, 10 vars) — scrubbed by `7bc3367`; HEAD = `FILL_WHEN_READY` placeholders |
| Live api_key in tracked scripts (HEAD) | `scripts/fetch_daily_candles.py:30`, `scripts/gemini_data_integrity_check.py:45`, `scripts/reconstruct_excursions.py:61` (LFL836 api_key — ACTIVE, non-authenticating alone) |
| Prevention (LIVE) | placeholder `.env.example` · gitignored+untracked `.env` · secret-scan hook `deploy/hooks/{pre-commit,secret_scan.py}` |
| Status | 6 exposed secrets DEAD (rotated 02-Jul) · 2 api_keys ACTIVE · history-purge PLANNED (memory `c1_secret_remediation_02jul`) |

Detail: memory `c1_secret_remediation_02jul` + `post_rotation_creds_02jul`.

## SSH key baseline / re-baseline (security_monitor) — 28-Jun
| What | Location |
|---|---|
| Monitor + check | `scripts/security_monitor.py` `check_authorized_keys` (list-aware) — CRITICAL on a fingerprint not in the allowed set or count > baseline |
| Committed default baseline | `config/security.yaml` `security.expected_key_fingerprint` (single) — git-tracked; a deploy's `checkout -f` resets it |
| **Durable operator override** | `data_store/security/ssh_key_baseline.json` (NOT git-tracked) — `apply_operator_ssh_baseline` overlays it (wins when present); survives deploys |
| **Re-baseline after a legit rotation** | `python scripts/approve_ssh_keys.py` (dry-run diff) → `--apply` (writes the override + re-seeds `security_state.json` + ALWAYS Telegrams). Operator-run only; never auto/monitor-triggered |
| Current authorized key (28-Jun) | `SHA256:uDRN8BJTmfNGFfCLofbnXkIGtWF6qTtrREQrQJGKduk` (ED25519 `oracle-vm-2026`) — Rama's legit rotation, NOT a compromise |

Detail: `docs/SYSTEM_MAP.md` security-watcher row · memory `ssh_key_rebaseline_28jun`.

## SATS — static analysis · 🗑️ TOOLING REMOVED 29-Aug-2026 (scripts archived)
🔴 **`D:\Projects\trading-system\sats\` NO LONGER EXISTS.** The two tool venvs
(`semgrep-env` 373 MB + `bandit-env` 43 MB = 415 MB, 10,152 files) were deleted on
29-Aug-2026. They were created 22-Jun-2026, run once, and never touched again.
`sats/` was always git-ignored (`.gitignore:92`) and never deployed, so nothing on
the VM changed and nothing tracked was lost — `git log -- sats/` was empty.

**What was KEPT** (27 KB — the work product, now tracked):
| What | Path |
|---|---|
| Archive root | `docs/archive/sats_20260622/` |
| Scan scripts | `scan_bandit.bat`, `scan_semgrep.bat` |
| The one scan ever produced | `semgrep_20260622_203318.txt` (22 findings, 22-Jun triage) |
| Semgrep baseline | `semgrep_baseline.txt` (pinned `65439ff`) |

⚠️ **A future scan needs re-baselining before it means anything.** `scan_semgrep.bat`
passes `--baseline-commit 65439ff` (22-Jun-2026) so it reports only findings NEWER than
that commit. Everything since — months of work — sits outside that window, so running
the script as-archived would under-report. Delete `semgrep_baseline.txt` for a full scan,
or re-pin it to the commit you actually want to diff against.

**To use it again:** create fresh venvs (`pip install bandit semgrep`), restore the two
`.bat`s from the archive, and fix their hardcoded `sats\...-env\Scripts\` exe paths to
wherever the new venvs live. Nothing else is needed — the 415 MB was entirely
reconstructible, which is why it was not kept.

⚠️ Bandit apparently never ran: only a **semgrep** report exists in the archive, despite a
43 MB Bandit venv having been installed for it.

💭 `"sats"` remains in `_PYC_SCAN_EXCLUDED_DIRS` (`scripts/system_manager.py`). It is now
**STALE but inert** — the directory it excluded is gone, so it silences nothing. Left in
place deliberately: removing it is a code change to a monitoring check for no behavioural
benefit. (The same file's comment warns against adding entries speculatively; this one is
a leftover, not a speculative addition.)

## Pre-flight (`scripts/preflight/`) — daily pre-market readiness, ALERT-ONLY
3 phases via cron Mon-Fri: **A 08:30** (infra) · **B 09:14** (engine readiness) · **C 09:15→09:20**
(signal warmup). Sentinel `data_store/preflight/today.json` · log `logs/preflight.log` · markers
`data_store/cron_marks/preflight_phase_{a,b,c}.done`. Run: `python -m scripts.preflight.orchestrator
--phase A|B|C [--dry-run --as-of-date YYYY-MM-DD]`. Schema v33 (preflight_runs / preflight_check_results
/ preflight_autofix_log). Cron Officer briefing embeds its sentinel banner. (Replaced premarket_healthcheck.)

## Config Sanity Auditor — BUILD 2 (25-Jun), the Config Authority enforcement layer
| What | Location |
|---|---|
| Rule engine (single source) | `core/config_auditor.py` → `audit()` / `audit_system_config()` (startup subset `ACG`) / `audit_app_config()` (full A–G) → `ConfigAuditReport` (`AuditFinding`: PASS/INFO/WARN/BLOCK) |
| Check groups | **A** contradictions (BLOCK, incl. #10) · **B** single-source regression guards · **C** capital-relative sanity (conc < pos-cap ladder) · **D** active-override listing · **E** launch-phase reminders · **F** stale-default guard · **G** cross-field |
| STARTUP caller (fail-fast BLOCK) | `core/config_loader.py` `SystemConfig._cross_field_sanity_checks` → `raise_if_blocked()` (ValidationError, "CONTRADICTORY CONFIG") |
| PRE-FLIGHT caller (Phase A → 09:20) | `scripts/preflight/checks/config_sanity.py` (7 rows, group "Config Sanity"; one memoised audit run) |
| Tests | `tests/unit/test_config_auditor.py` (37) |

Detail: `docs/SYSTEM_MAP.md` Changelog 2026-06-25 · memory `build2_config_sanity_auditor_25jun`. No DB schema; parity (no mode branch).

## Control Tower (`ops/control_tower/`) — VM ops aggregator (Phase 1, 29-Jun)
| What | Location |
|---|---|
| Daily runner | `ops/control_tower/runner.py` — cron **17:05 Mon-Fri**; size-logger FIRST (trend size cols) → aggregator (detection + findings lifecycle + health + status + reporter). **Skips non-trading days** (SKIPPED heartbeat — no false alerts; `--force` bypasses). `marker_name`/heartbeat `control_tower`; superseded the standalone 1a size-logger cron |
| Aggregator | `ops/control_tower/aggregator.py` `run_aggregation(...)` — 4 read-only adapters (security `last_run.json` / cron heartbeats+markers / `config_auditor` / excursion runs) + freshness engine (`freshness.py`) + disk findings (`disk.py`); `self_job="control_tower"` excludes the in-flight runner from its OWN cron freshness check (check_cron_drift @18:00 is its external monitor) |
| Reporter | `ops/control_tower/reporter.py` — (A) noise-controlled Telegram DELTA (CRITICAL always unless acked; HIGH only if NEW/reopened; MED/LOW/INFO never) via the **REAL** `TelegramNotifier`; (B) pull report `data_store/control_tower/report_<date>.{html,csv}` (full picture, never pushed) |
| ACK CLI | `ops/control_tower/cli.py` → `ct list` / `ct ack <id>` / `ct unack <id>` (ACK suppresses a finding from the health score; unack reopens) |
| Severity / health | `ops/control_tower/severity.py` — security WARNING→MEDIUM (dynamic-IP fatigue), CRITICAL→CRITICAL; config BLOCK→CRITICAL, WARN→HIGH. Bands (`health.py`, OPEN-only): ≤50 HEALTHY / ≤150 WARNING / ≤300 ATTENTION / else CRITICAL (weights C100/H50/M20/L5). Disk thresholds (`disk.py`): HIGH ≥85% / CRITICAL ≥95%; backup-growth ≥500MB, log-growth ≥300MB (history-gated) |
| Tables (schema v40) | `control_tower_findings` (dedup category/resource/reason; OPEN→ACKNOWLEDGED→RESOLVED) · `_runs` · `_trends` (per-date sizes+health) · `_status` (per run_date roll-up + `last_successful_run`) · `_freshness` (per stage) |
| Security input | `data_store/security/last_run.json` (written by `scripts/security_monitor.py`; the aggregator's security adapter reads it). **SS-B 26-Jul-2026:** written from the **PRE-dedup** findings on **every ~60 s pass**, so it reflects the CONDITION not the alert — and now carries `persistent[]` / `persistent_count`, the keys still present but no longer alerting each pass. ⭐ **This is the channel that answers "what is still broken" once `_dedup` stops re-alerting a persistent condition; `max_severity` is still the ORIGINAL severity, not the downgraded repeat.** |
| Tests | `tests/unit/test_control_tower_phase1{a,b,c,d}.py` |

`daily_report` now records a `cron_heartbeat("daily_report")` on successful xlsx write (Phase 1d source-fix for the 23-Jun false "daily_report missed"). Detail: `docs/SYSTEM_MAP.md` Changelog 2026-06-29 · memory `control_tower_phase1a_29jun`.

## Config snapshots — W0 (daily-report redesign foundation, 01-Jul, schema v41)
| What | Location |
|---|---|
| Table (v41, pure addition) | `core/schema.sql` `config_snapshots` (snapshot_id/date/ts/account_id/mode/trade_type/config_hash/config_json) + `idx_config_snapshots_date`. Records the FULL resolved runtime config per date so the report reads config-as-it-was DB-purely |
| Writer (one entry point) | `core/config_snapshotter.py::snapshot_config(store, app_config, *, snapshot_date, snapshot_ts, account_id, mode, trade_type)` — serializes `AppConfig.model_dump(mode="json")` → stable/sorted JSON; sha256 → `config_hash`; **idempotent per (date, config_hash)** (same-config restart = no-op; same-day config change = new row). No secrets (CL5: env-var NAMES only; `smtp.password` empty in prod) |
| Wiring (parity) | `main.py` after mode is finalized (post interactive flip, after `--status`/`--dry-run` return; ~L1824) → fires in BOTH paper + live (mode is a column); non-fatal (never blocks startup) |
| Version bump | `core/state_store.py` `EXPECTED_SCHEMA_VERSION = 41`; `core/schema.sql` trailing INSERT → `'41'`; `core/migrations.py` v36→v41 pure-addition note (NO MIGRATION_TABLES entry) |
| Data contract | `docs/report_data_contract.md` (NEW) — the permanent SYSTEM-writer ↔ REPORT contract; Config rows seeded (per-strategy params = NEEDS_CAPTURE follow-up, NOT in AppConfig) |
| Tests | `tests/unit/test_config_snapshotter.py` (13: canonical-json stable, hash deterministic, same-config→1 row, changed→2 rows, round-trip, real `load_all` → 1 row + idempotent) |

STAGED — NOT pushed (Rama pushes off-market). NO cron / NO flags / NO trading-code. Report reads only `config_snapshots`, never YAML. Detail: memory `w0_config_snapshots_01jul`.

## Daily Trade Review — ORDERS forensic sheet (report redesign, 01-Jul)
| What | Location |
|---|---|
| Generator (NEW, DB-pure) | `reports/daily_trade_review.py` — `main(--db,--date,--output-dir)` → `reports/output/daily_trade_review_report_<date>.xlsx`. Sheet 1 = `1_Orders` (one row/trade, 69 cols/9 groups A–I); sheet 2 = `2_Signals` (one row/STORED-signal, 21 cols); sheet 3 = `3_Reconciliation` (5 integrity blocks + OVERALL); sheet 4 = `4_Config` (sectioned key/value of the day's AppConfig from W0 config_snapshots); sheet 5 = `5_Strategies` (5 tables: per-strategy/time-bucket/direction/RR daily + trailing confidence-weighted composite ranking); sheet 6 = `Slippage` (6 blocks: summary+3-leg decomposition, band analysis, strategy/stock-wise, worst-20, 10-day trend); + `Dashboard` (FINAL, placed FIRST — summarizes all six). **FINAL TAB ORDER: Dashboard · Reconciliation · Orders · Signals · Strategies · Slippage · Config** (numeric prefixes dropped; sheets built-then-rendered-in-order). **TIER-1 report COMPLETE (7 sheets).** **Phase C cutover DEPLOYED (01-Jul ~21:37, main `01e07b7`)**: LIVE @ **16:07 Mon-Fri** (`cron_registry.yaml` job `daily_trade_review`, monitored → heartbeat); `daily_review` **DELETED** (git rm module+test+entry; git history=rollback); `daily_report` kept in parallel for a ~3–5-day bake-in. `main()` `--date` defaults to today IST. v41 migration applied; manual-run gate PASSED. Rollback/steps: `docs/phase_c_cutover_runbook.md` |
| Signals sheet basis (HONEST) | one row per signal that REACHED STORAGE. Webhook dupes are dropped at dedup BEFORE any INSERT (`webhook_receiver` → `status='DUPLICATE'`, no row) → per-signal `Received=Qual+Rej+Dup` CANNOT hold. Totals = **funnel-first** (Received webhook → storage/18.3% → Qualified → Traded) + storage partition **Δ=0**. `build_signal_records`/`render_signals_sheet`; `_signal_bucket`(unmapped-aware)/`_signal_stage` |
| Reconciliation sheet (integrity backbone) | `build_reconciliation`/`render_reconciliation_sheet` — 5 blocks + OVERALL verdict. CAPITAL = REAL cross-source check `Σ fm_ledger.RELEASE_USED.pnl_delta` == `Σ trades.net_pnl` ≤ ₹1 (the `RESET_PNL` row is a by-design daily EOD reset, NOT pollution). *(**W10 FIXED 17-Jul** — `get_daily_realized_net_pnl` no longer double-subtracts; `pnl_delta` is NET by contract. This block stays as-is: it is a genuine **cross-source** check — ledger vs `trades` — not a re-derivation of the reader, and both sides now move together on backstop closes since CHECK1/GTT write `trades.net_pnl` FROM `pnl_delta`.)* Partition blocks (1,3) FAIL on an unmapped status. BROKER = PENDING_CAPTURE (W2/W3). **FAIL-injection proven** (capital-drift → CAPITAL+OVERALL flip to FAIL). `_trade_bucket` |
| Config sheet (sheet 4, first W0 consumer) | `build_config_data`/`render_config_sheet` — sectioned two-column key/value of the day's `AppConfig` from `config_snapshots.config_json` (latest by `snapshot_ts`); nested → indented sub-rows; NO snapshot → `— pending W0` placeholder. Sections SYSTEM/RISK/SCORING/SLIPPAGE/BROKER COSTS/STRATEGY(pending W0.1). `_cfg_get`/`_cfg_val`. Build-gate spot-check PASS (min_pass_score=60 / daily_loss_limit_pct=0.03 / leverage INTRADAY=5.0 / entry_end=15:00) |
| Strategies sheet (sheet 5, first analytics) | `build_strategy_data`/`render_strategies_sheet` — 5 tables from the `trades` truth layer (reuses the day's Orders `records` + Signals `srecords`; `strategy_metrics` NOT used — thin/CLOSED-only). win=net>0. T1 per-strategy · T2 30-min entry-bucket · T3 long/short · T4 RR (planned vs filled + `actual_rr`) · T5 RANKING over a **trailing N sessions** (default 20). Composite `(0.40·win%+0.40·ROI+0.20·RR)_norm × min(n/20,1)` — confidence stops a low-sample strat topping a high-sample one. `_build_t5_ranking`/`_minmax_norm`/`_bucket_for`. Build-gate PASS (independent T1 recompute + raw-SQL cross-check + composite sanity on real 30-Jun VM backup) |
| Dashboard sheet (FINAL, placed FIRST) | `build_dashboard_data`/`render_dashboard_sheet` — summarizes all six. **Summary-agrees-with-detail** (every headline from the same sources the detail sheets use → cannot disagree). 7 sections: recon banner · coverage panel · yesterday-vs-today deltas (prior day = data-driven `MAX(date)<today`) · trading · profitability · capital (`fm_ledger` opening/peak/drift + `_max_concurrent` sweep) · deterministic highlights (rule-based, NOT AI). `_prior_trading_day`/`_day_summary`/`_max_concurrent`. Build-gate = full cross-agreement (net==Orders TOTALS, banner==Recon OVERALL, long/short/closure==Strategies/Orders, slip==Slippage, MFE/MAE==independent, yesterday==independent prior-day, tab order) ALL PASS on real 30-Jun VM backup |
| Slippage sheet (sheet 6, last analytics) | `build_slippage_data`/`render_slippage_sheet` — 6 blocks from `trade_slippage_log` (roll-up `orders/slippage_recorder`) + Orders `records`. **Decomposition = 3 legs** `(entry+sl−tgt)×qty` (matches `calc_rr_damage_pct`; ENTRY/SL adverse+, TGT favourable−; sums to total). **Tier NOT persisted** (`slippage.tiers` resolved at runtime by `broker/slippage_engine.tier_for` via InstrumentCache, paper-fill model) → Block 2 buckets by recorded `price_band`; configured tier bps shown as reference (W12 = persist tier per trade). Full-precision aggregate, round-once (no rounding leakage). `_slip_tiers_for_date`. Build-gate PASS (total==Σindependent + decomposition-sums-to-total + band-recompute on real 30-Jun VM backup) |
| Closure classifier (FLAG 2, HONEST) | `daily_trade_review.classify_closure(trade, recon_rows)` — precedence order preserved but RECON_CLOSE narrowed to reconciler-INITIATED only (`STUCK_EXITING`). Bare `MANUAL_CLOSE` = reconciler *detecting* an external close (daily-EOD/operator/RMS collapse, no per-trade trigger) → honest **SYSTEM_CLOSE**, NOT RECON_CLOSE. Positive EOD marker→EOD_SQUAREOFF; `%ORPHAN%`/`SYSTEM_OVERSELL`→ORPHAN_RECOVERY; RECON_EOD_CLOSE only if both. W8 = permanent fix (per-trade closure_source) |
| Sources | `trades` spine + joins `orders`/`signals`/`screener_results`/`trade_slippage_log`/`trade_excursions`/`sr_detector_results`/`reconciliation_log`/`config_snapshots` (all DB; via StateStore getters + `fetch_all`) |
| Pending captures (labels) | `— pending W2` broker-margin · `— pending W5` exit-trigger ts (FLAG 1 absent) · `— pending W6` MFE/MAE provenance (FLAG 3) · `— pending W7` exchange-order-id · **W8** per-trade closure_source (FLAG 2 — split SYSTEM_CLOSE into EOD vs manual/RMS) · **W9** persist per-signal drops / raw receiver log (Signals) · **W0.1** strategy-config snapshot writer (fills Config STRATEGY section) · ~~**W10** `get_daily_realized_net_pnl` double-cost fix~~ **✅ FIXED 17-Jul** (E4+W10 = ONE contract migration: `pnl_delta` is NET, `costs` never re-subtracted; branch `e4-w10-pnl-contract`, **UNPUSHED — deploy gated on Rama's risk-posture sign-off**; `docs/audit/e4_w10_done_17jul2026.md`) · **W11** NULL-exit CLOSED_MANUAL trades lack net_pnl → can FAIL CAPITAL honestly on such days · **W12** persist the resolved slippage tier per trade (enables true per-tier actual-vs-expected; also EOD/manual-exit slippage capture) |
| Tests / gate | `tests/unit/test_daily_trade_review.py` (**57**: 51 sheet-build + Phase-B.1 6 = win%-helper/win%-denominator/skew-skip/coverage%/filled-vs-entered + Phase-C entrypoint default-date+heartbeat); build-gates = Orders forensic + Signals totals + Config spot-check + Strategies aggregate/composite + Slippage totals/decomposition + Dashboard cross-agreement, all vs real 30-Jun VM-backup, ALL PASS. Cutover also: `test_cron_registry.py::test_phase_c_cutover_expectations` |
| Phase C cutover (01-Jul) | `config/cron_registry.yaml` (daily_review `enabled:false`; new `daily_trade_review` 16:07 Mon-Fri) → `deploy/cron/trading-system.cron` regenerated (post-receive auto-installs on push). `core/cron_registry.py::expected_heartbeat_jobs` now skips disabled jobs (no false MISSED for the retired daily_review). `scripts/system_manager.py` EOD check added for the new xlsx. Runbook: `docs/phase_c_cutover_runbook.md`. W13 guardrail: keep `shadow_tracker.enabled` + `innings` table (Multi-Inning view deferred, backfillable) |

Phase B (parallel-run, 6 dates ×2 model runs) + Phase-B.1 fixes = double-confirmed GO. Phase C cutover PREPARED — Rama pushes OFF-MARKET (branch carries W0 v41 migration → DB backup first). Detail: memory `phase_b_parallel_run_01jul` + `phase_c_cutover_01jul`; contract `docs/report_data_contract.md`; runbook `docs/phase_c_cutover_runbook.md`.

## Self-maintaining cron (registry → canonical → auto-install, ARMED 23-Jun)
| What | Path / fact |
|---|---|
| Source of truth | `config/cron_registry.yaml` (executable) |
| Generator | `scripts/generate_crontab.py --generate [--out FILE]` → `deploy/cron/trading-system.cron` (ASCII+LF, deterministic). Also `--gate` (zero-drops proof), `--selftest` (byte round-trip), `--bootstrap`, `--check` |
| Equality | live `crontab -l` == canonical == `generate(registry)` (43 command-lines as of 29-Jun; regenerate the sha after a registry change) |
| **post-receive** (ARMED) | `~/trading-system.git/hooks/post-receive` (from `deploy/hooks/post-receive`) — auto-installs the crontab on every push **iff** `generate==canonical`, else WARN+skip |
| **pre-receive** (DEFERRED) | `deploy/hooks/pre-receive` — **NOT installed** (by choice 23-Jun); would hard-reject a push whose canonical != generate. Arm: install + inject `CRON_GUARD_DRYRUN=1` (dry-run) → clear to enforce. Break-glass: `rm` the hook |
| pre-commit (local clones) | `deploy/hooks/pre-commit` — cron regen on registry change **+ C-1 secret scan** (`deploy/hooks/secret_scan.py`, 02-Jul): blocks a real credential / real `.env` / non-placeholder `.env.example`. Install: `cp deploy/hooks/pre-commit .git/hooks/ && chmod +x`; tests `tests/unit/test_secret_scan.py` |
| **cron-watchdog** (ARMED) | `/etc/systemd/system/cron-watchdog.{service,timer}` — systemd (NOT cron); **19:30 IST daily**; asserts `cron_officer_eod`+`check_cron_drift` heartbeated → CRITICAL sentinel (cron-independent) if not. 1st run 24-Jun |

Detail: `docs/SYSTEM_MAP.md` (Deploy + Cron Jobs + Systemd) · memory `cron_framework_armed_23jun`.

## Off-market deploy plan — A-2 · C-1 · B-1 · P1 batch (02-Jul, for 03-Jul+)
| What | Path / fact |
|---|---|
| Runbook | `docs/ops/offmarket_deploy_plan_03jul2026.md` — push/verify/shadow/cutover for the four-fix batch |
| Baseline | `main == origin/main == 9becf8c`, **schema v41** |
| Push order | GATE = 03-Jul 08:15 boot of `9becf8c` clean → then after-17:05/weekend (never 15:30–17:05): merge **C-1** `fix-c1-completion-02jul` (carries A-2 `fd09a38`) → **B-1** `fix-b1-daily-loss-mtm-02jul` `f5fd4d9` → **P1** `fix-p1-eod-broker-reconcile-02jul` `4817032` → `git push origin main` |
| Schema | v41 → **v42** (P1 only; pure-add `eod_broker_reconciliation`; back up both DB files first) |
| Shadow flags (default OFF) | `risk.daily_loss_include_unrealized: false` (B-1) · `eod_reconcile.authoritative: false` (P1) |
| Activation | A-2 + B-1 at Mon 06-Jul 08:15 boot · P1 first shadow EOD Mon 06-Jul 15:58 · crontab `@15:58` on push |
| Cutover (earliest) | B-1 enforce ≈ Mon 13-Jul · P1 authoritative + retire `eod_verify`/`reconcile_pnl` ≈ Mon 20-Jul (≥2 clean shadow weeks + ledger-dimension deepening + demonstrated UNVERIFIED) |
| Deferred (not in batch) | C-1 history-purge · optional api_key rotate · C-2 network Phase-3 · P1 ledger-deepening · T2 full-repair · W10 double-cost |

---

## 🔴 WHICH LOCAL TREE IS ON WHICH BRANCH — read before ANY commit (03-Sep-2026)

⚠️ **The path this file elsewhere calls "Local repo" is NOT the deployed line.**
On 03-Sep a docs commit was nearly made on a feature branch while believing it was
main; only an explicit branch check caught it.

| directory | branch | is it the deployed line? |
|---|---|---|
| `D:\Projects	rading-system` | **`feat/delivery-config-split`** (F2-SIZING, ~98 dirty files) | 🔴 **NO** — never build, gate or commit here |
| `D:\Projects	rading-system-gui09` | tracks the deployed tip (`ce7cea7` at time of writing) | ✅ **YES** — the working line |
| `D:\Projects	rading-system-main` | local `main` @ `3dff752`, **90 ahead / 94 behind** | 🔴 **NO** — a plain `git push origin main` here pushes OVER the deployed SHA |

**SESSION-START AND PRE-COMMIT CHECK — one command, always:**
```
git rev-parse --abbrev-ref HEAD && git rev-parse --short HEAD && git status --short | wc -l
```
Do not commit until that prints the branch you intend. A directory named `main`
is not `main`; the default working directory is not the deployed branch.

Push only with an explicit refspec: `git push origin <sha>:refs/heads/main`.
